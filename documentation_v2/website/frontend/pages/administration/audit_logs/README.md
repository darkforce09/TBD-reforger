# System Audit Logs (`pages/administration/audit_logs/`)
Administrative audit console tracking administrative mutations, actor IDs, IP origins, and severity tags (`/admin/audit`). Includes colocated terminal audit log visual design references.
