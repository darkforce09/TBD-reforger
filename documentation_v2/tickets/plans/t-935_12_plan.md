# T-935.12 — Plan

## Context
Schemas (`terrain-manifest.schema.json`, `map-object-instance.schema.json:38-51`) describe JSON
only; gates schema_gates.rs:3366 and golden_gate.rs:483 check JSON goldens; `.gitattributes:3-6`
lists LFS rules for png, tbd-sat and bvh. All emitters and loaders (.2–.11) are on main.

## Approach
1. Schemas: optional `objects.binary`, `dem.raw`, `labels`, `water`, `buildings` blocks with
   enum-pinned container/pod names; binary row documented beside the JSON row.
2. `.gitattributes`: `*.bin`, `*.rkyv`, `*.dem`, `*.tbd-bath` under packages/map-assets → LFS.
3. Golden `map-object-chunk-sample.bin` emitted from the golden JSON with the T-935.2 emitter.
4. `golden_gate.rs`: decode golden .bin, compare with golden JSON decode; `schema_gates.rs`:
   verify-terrain-strict validates the blocks and rejects dangling binary paths.
5. Perturbation: flip one byte of the golden .bin → gate red; restore, touch, green.

## Risks
- Density `*.bin` files already committed: if a rule change would re-route them, scope the new
  rule to the new directories (`objects/chunks/*.bin`) instead of a bare `*.bin`.

## Verification
- `cargo xtask ci schema-validate`; `cargo xtask ci verify map-object-golden`
- `cargo xtask ci verify-terrain-strict`
- `cargo xtask platform wave gate --slice T-935.12`
