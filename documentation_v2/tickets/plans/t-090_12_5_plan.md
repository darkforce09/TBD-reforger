<!-- T-917.6 ready-gate plan doc for T-090.12.5 (generated 2026-09-04 from the operator-approved program plan
     ~/.claude/plans/plan-written-claude-plans-t090-12-world-glistening-unicorn.md, itself reconciled from
     ~/.claude/plans/t090-12-world-bvh-every-object.md; Cursor owns the prose polish). -->

# T-090.12.5 — Editor: object LOS verdict, viewshed wash, world-los bench

## Context

`los_tool.rs` is terrain-only and allowlisted at 2041 lines; the viewshed is a synchronous one-shot; the frontend's native build has no `world` feature, so object types must be frontend-local; `WorldResidency` is taken out of its `RefCell` during settles.

## Approach

`los_world.rs` (native, pure): `ObjectVerdict`, `combine`, `first_block_dist`, `format_combined`, `ObjectPass` (terrain-visible cells, nearest-first, 32 m / 16 m over the disc, 8 m within 1 km, 8 ms per frame, generation cancel), `encode_viewshed_rgba_merged`. `los_world_wasm.rs` maps `WorldLos → ObjectVerdict` and builds the cell-test closure from `with_occluder`. `occluder_host.rs` fetches the manifest, hot set, descriptors and BLAS by residency events. `los_tool.rs` diff ≈ 20 lines at the seams (`ProjectedShot.objects`, header text, viewshed payload); `viewport.rs` rAF hook steps the pass. New `/debug/world-los` page on the Interior*/SceneVegetation lanes; `routes.csv` gains both debug rows.

## Risks

Object-wash wall time on a forest cell (progressive, cancellable, `Untested` never reads clear). Host `None` mid-settle (`ObjectVerdict::NotLoaded`, retry next frame). No new render lanes, so no lane-count pins move.

## Verification

`cargo test -p website-frontend` (goldens for `format_combined` / `combine` / palette / pass order / `NotLoaded`; wiring test `t090_12_world_los_wiring.rs`); wasm32 clippy `--all-targets`; `trunk build --release`; `gate s-routes` green (28 → 30); live-check block with one `capture shot` URL per state; measured fill time in the commit body; in-browser parity replay equals native.
