# T-935.19 — Plan

## Context
Step 2. The loader stops fetching per-chunk files and resolves a camera rect to byte ranges through
T-935.18's index. The working precedent is the satellite path:
`apps/website/frontend/src/editor/world_assets/tbd_sat.rs:112-137` (`index_range_end`, a 12-byte
prefix sizing both versions), `:145-160` (`parse_header` version dispatch), and
`satellite.rs:185-199` (`fetch_index_head` — Range `bytes=0-11`, compute `end`, Range `bytes=0-{end}`,
then per-tile ranges through `fetch_range_resilient :266`).

## Approach
1. Range-fetch the index head, then the object ranges, from `world_host.rs`, reusing
   `world_assets/fetch.rs` (`fetch_range_outcome`, `RangeBody`).
2. Move `occluder_host.rs` off `WorldResidency` (`:39, :87, :265, :272, :280`) onto the index.
3. Flip `packages/map-assets/everon/manifest.json` to name the container.
4. Perturbation: reintroduce a per-chunk fetch on the pan path; T-935.17's allocation assertion must
   go red; restore, `touch`, green.

## Risks
- **Sequenced after T-935.18** — it needs the container to exist.
- **T-935.16 owns `build.rs` and `world_host.rs` too** and cannot share a wave with this row.
- Crossing a former chunk boundary must cost nothing; that is the whole point, and T-935.17's counter
  is what proves it.

## Verification
- `cargo test -p website-frontend`
- `cargo xtask platform wave gate --slice T-935.19`
- Full-width pan: zero residency allocations, pasted from the probe.
