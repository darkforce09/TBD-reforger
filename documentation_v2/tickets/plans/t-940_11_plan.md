# T-940.11 — Plan

## Context
admin.rs:668-673 maps only Restart to an agent action; Kick/ChangeMap → 503 (:765-771). Bridge: services/game_agent.rs
(T-289 socket filter). After T-940.7 (admin.rs).

## Approach
1. Verify on main: rcon kick → 503; paste the red.
2. game_agent.rs: `Kick{player_id}`, `ChangeMap{scenario_id}` with strict validation and wire tests.
3. admin.rs: map the commands; audit each dispatch; Custom stays 503.
4. Perturbation: accept a non-numeric id → validation test red; restore, touch, green.

## Risks
- Host-side filter grammar lives outside the repo. Operator checklist: extend the agent filter to accept `kick <numeric>` and `change-map <known-scenario>` before enabling.

## Verification
- `cargo xtask db test-it`
- `cargo xtask platform wave gate --slice T-940.11`
