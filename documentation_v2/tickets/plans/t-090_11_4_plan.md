<!-- T-917.6 ready-gate plan doc for T-090.11.4 (generated 2026-09-03 from the operator-approved program plan
     ~/.claude/plans/implementation-plan-tender-seahorse.md; Cursor owns the prose polish). -->

# T-090.11.4 — Compound building: DoorState, glass/foliage LOS, instance wash

## Context

With BLAS instances available, one ray must walk shell + instances kind by kind: opaque terminal, glass traversed with a small concealment, foliage attenuated by depth, closed leaf blocking, open aperture clear.

## Approach

`crates/map-engine-core/src/geometry/rigid.rs` (pure transforms) and `building_compound.rs` (+tests, feature `blueprint`): `Instance`, `DoorSpec`, `DoorState`, `CompoundBuilding::{assemble, trace, evaluate_los, set_door, flatten}`; `LosHitKind` gains Glass/DoorLeaf/DoorAperture/WindowFrame/Foliage/Prop; `building_section` takes a `TriSource` and tags cuts with `SegClass`; `building_viewshed::level_wash_compound`. `BuildingBlueprint::evaluate_los` untouched (pin).

## Risks

Yaw/pitch/roll composition order — pinned by the T-090.11.3 recon bounds test (#[ignore] until the fixture exists). Foliage pairing needs closed canopy meshes — unpaired exits use a default depth.

## Verification

`cargo test -p map-engine-core --all-features`; clippy `-D warnings`; glass / frame / door-flip / foliage / transform-precision tests; farmhouse door-inclusive pin ≥ 395/400 phantom 0.
