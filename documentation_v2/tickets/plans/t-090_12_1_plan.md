<!-- T-917.6 ready-gate plan doc for T-090.12.1 (generated 2026-09-04 from the operator-approved program plan
     ~/.claude/plans/plan-written-claude-plans-t090-12-world-glistening-unicorn.md, itself reconciled from
     ~/.claude/plans/t090-12-world-bvh-every-object.md; Cursor owns the prose polish). -->

# T-090.12.1 — World export v2: pitch, roll, scale rows

## Context

The staged raw export already carries `pitchDeg`/`rollDeg` per row (`staging/export/raw-entities.jsonl`, 2026-07-05) but `build.rs:245` reads `headingDeg` only; scale is captured nowhere; the chunk tuple is schema-pinned to exactly five numbers; the plugin that wrote the full export was deleted in `edfae8405`.

## Approach

Schema chunk branch `minItems 5, maxItems 8` + canonicality in `golden_gate.rs` S11; golden chunk sample gains one 8-wide row; `manifest.json` `objects.schemaVersion` 1.1.0 + `transforms` / `scaleSource` / `workbenchVersion`; `narrow_instance_row_v2` + `WorldChunk` pitch/roll/scale columns; `KeptRow` + emitter in `build.rs` (rows exactly 5 or 8 wide); rebuild every phase from the staged export (scale absent); resurrect `TBD_WorldFullExportPlugin.c` in both addon twins with `scale`; `instances-verify --world-row` pins. 1b after the operator re-export: rebuild with scale, report the delta.

## Risks

Prefab-id renumbering on the September re-export (expected; rebuilt through `verify-phase`, `redensify`, `reclassify`, stated in the body). `terrain-manifest` may pin `objects.schemaVersion` (re-bless in the same commit).

## Verification

`cargo test -p tbd-tools`; `cargo test -p map-engine-core --all-features`; `cargo test -p xtask` (farmhouse 88/88 ≤ 2 cm; garbage-container pitch/roll); `cargo xtask ci schema-validate`; `verify map-object-golden`, `type-inventory`, `map-object-enums`, `terrain-manifest everon`; row-length census by kind in the commit body.
