<!-- T-917.6 ready-gate plan doc for T-090.11.3 (generated 2026-09-03 from the operator-approved program plan
     ~/.claude/plans/implementation-plan-tender-seahorse.md; Cursor owns the prose polish). -->

# T-090.11.3 — Workbench oracle: recon children, door-inclusive parity

## Context

Socket transforms decoded offline in T-090.11.2 are unproven, and the 400-pair oracle excludes door entities and glass panes.

## Approach

Extend `TBD_BlueprintReconPlugin.c` (per child: localPos, anglesDeg, pivotId, meshPath, DoorComponent params), `TBD_BuildingTraceExtract.ExecuteParity` (`includeDoors`), `EMCP_WB_TbdBlueprint.c` (actions `recon2`, `parity-doors`); drive both over `cargo xtask mcp call`; commit the dumps as xtask fixtures; `map instances-verify` pins every `xobSocket` instance within 2 cm / 1°; rotation-order test on tilted furniture children.

## Risks

New plugin params need an operator Script Editor compile. If Workbench is unavailable, the scripts still land and the two fixtures are reported missing; on socket mismatch the emitter falls back to `--sockets-from-recon` and the mismatch table goes in the commit body.

## Verification

`cargo xtask map instances-verify --instances … --recon …`; `cargo test -p xtask`.
