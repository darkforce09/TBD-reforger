# T-940.12 — Plan

## Context
No reservation tiers; registration is first-come with a capacity check (events.rs:1768-1798). After T-940.3.

## Approach
1. Verify on main: a guest registers before the guest tier opens; paste the red.
2. `migrations/0027_reservation_tiers.sql`: event_reservation_tiers(event_id, tier, seats, opens_at).
3. Register handler: resolve tier from roles, refuse before opens_at, count per tier, overflow to open once open.
4. models/event.rs + core/dto.rs; pages/operations/event_hub.rs shows tiers, remaining seats, opening times.
5. Perturbation: skip the opens_at check → test red; restore, touch, green.

## Risks
- Events without tiers keep today's behaviour (one open tier).

## Verification
- `cargo xtask db test-it`; `cargo xtask mk ci-local-leptos`; `cargo xtask mk leptos-gates`
- `cargo xtask platform wave gate --slice T-940.12`
