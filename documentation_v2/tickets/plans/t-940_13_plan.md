# T-940.13 — Plan

## Context
Ingest stores per-player counters only; deployments.rs:83 AAR is an external link. After T-940.4 (telemetry.rs).
Existing T-136 owns the AAR replay and the in-game capture.

## Approach
1. Verify on main: results with an events array store zero events; paste the red.
2. `packages/tbd-schema/schema/telemetry-events.schema.json` + golden fixture; `cargo xtask ci schema-validate`.
3. `migrations/0028_telemetry_events.sql`: match_events with (match_id, t) index.
4. telemetry.rs validates and bulk-inserts; models/telemetry.rs row type; unknown kind rejected with its index.
5. Perturbation: accept an unknown kind → reject test red; restore, touch, green.

## Risks
- Payload size: bulk insert in chunks; schema caps the array length.

## Verification
- `cargo xtask ci schema-validate`; `cargo xtask db test-it`
- `cargo xtask platform wave gate --slice T-940.13`
