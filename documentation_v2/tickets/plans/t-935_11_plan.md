# T-935.11 — Plan

## Context
build.rs writes prefabs (:479), forest-regions (:593) and type-inventory (:746) as JSON;
prefab.rs:15/33 and regions.rs:10 parse them; world_host.rs fetches prefabs :133, roads :146,
regions :156. Packs after T-935.3 (world_host.rs), T-935.2 (build.rs) and T-935.6 (tools
world/mod.rs, store.rs sniff), so it can flip all four frontend fetches.

## Approach
1. `tools/tbd-tools/src/world/catalog_emit.rs` (+ `world/mod.rs`, calls from `build.rs`):
   PrefabCatalogArchive (prefabs + type inventory) and ForestRegionsArchive writers.
2. `prefab.rs`, `regions.rs`: `from_archive`.
3. `world_host.rs`: archive URL per asset when `manifest.objects.binary` names it; bytes go to
   store.rs, which sniffs the format.
4. Parity test on everon; perturbation shuffles prefab order → red.

## Risks
- Type inventory today is plain JSON (not gz): keep its archive optional in the manifest so the
  schema gate (T-935.12) can require it only when present.

## Verification
- `cargo test -p map-engine-core --all-features catalog`; `cargo test -p tbd-tools catalog_emit`
- `cargo xtask mk leptos-gates`
- `cargo xtask platform wave gate --slice T-935.11`
