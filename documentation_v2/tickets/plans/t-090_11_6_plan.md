<!-- T-917.6 ready-gate plan doc for T-090.11.6 (generated 2026-09-03 from the operator-approved program plan
     ~/.claude/plans/implementation-plan-tender-seahorse.md; Cursor owns the prose polish). -->

# T-090.11.6 — Viewer bench: interior lanes, doors, glass, trees

## Context

The compound model and the lanes exist; the operator's bench must draw the furnished building on them and let doors be toggled while rays pass glass and foliage.

## Approach

New `apps/website/frontend/src/pages/debug/building_interior.rs` (pure `InteriorLanes` builder, `door_at` hit-test, ray lane moved here); `building_viewer.rs` fetches `<slug>.instances.json` + `blas/*.bvh`, holds `compound` + `door_states` signals, click inside a leaf toggles it, `?scene=1` loads `<slug>.scene.json` conifers; the seven borrowed lanes are no longer uploaded.

## Risks

`building_viewer.rs` is SIZE-3 allowlisted — new code lives in the new module and the file shrinks. WebGL2 headless screenshots need a live API on :8080 (T-166 memory).

## Verification

`cargo test -p website-frontend`; clippy wasm32 `--all-targets`; `trunk build --release`; five live screenshots (walls/doors/glazing/furniture lanes, door toggle + wash, glass-transparent ray, frame block, scene trees) in the commit body.
