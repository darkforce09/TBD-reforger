**Status:** live — resume file for the Documentation V2 program

# Documentation V2 progress checkpoint

The resume file for the Documentation V2 program: the baseline taken before any move, one roster
row per planned agent, and the operator checkpoints. The orchestrator updates a roster row when it
commits that agent's work; each verifier updates the rows of the phase or wave it verifies. The plan
is [refactor_program_plan.md](/documentation_v2/refactor_program_plan.md); the rules every agent
follows are in [refactor_writing_brief.md](/documentation_v2/refactor_writing_brief.md).

## Resume instructions

Read this file, then the program plan, then the writing brief. Continue the roster in order from the
first row that is not `done`: rows run in the order listed, and rows the plan runs in parallel (P0-2
to P0-4, P2-5 with P2-6, R01 with F01, the writers of one wave, G1 with G2) may run together, at
most four writers at a time. Stop at each checkpoint in the Checkpoints table and wait for the
operator's sign-off before starting the row after it. Before P2-1, get the operator's confirmation
that no other session writes under `docs/` and that `git status -- docs` is clean.

Roster statuses: `pending`, `running`, `done — awaiting commit`, `fix-list` (a verifier's FIX-LIST
is open), `done` (the commit column holds the commit).

## Baseline (2026-09-23)

P0-1 ran each command once from the repository root through `hcargo`, the host cargo wrapper the
base brief names, one command per call. The logs sit under `logs/P0-1/` in the program scratchpad
the base brief names, outside the repository.

| Command | Exit | Log | Failure excerpt |
|---|---|---|---|
| `hcargo xtask ticket check --strict` | 0 | `logs/P0-1/a_ticket_check_strict.log` | none: `check OK`, with 10 warnings that an `acceptance` item is command-shaped (T-853, T-912.1, T-912.2, T-913.1, T-915.1, T-916.2, T-917.1, T-919, T-920.1, T-921) |
| `hcargo test -p xtask` | 101 | `logs/P0-1/b_test_xtask.log` | 773 passed, 3 failed: the three expected `tooling_prose_rules` failures, quoted below |
| `hcargo test -p ticket-engine` | 0 | `logs/P0-1/c_test_ticket_engine.log` | none: 206 unit tests and 1 trybuild test passed |
| `hcargo test -p developer-tools` | 0 | `logs/P0-1/d_test_developer_tools.log` | none: 263 passed, 4 ignored |
| `hcargo test -p ticketboard` | 0 | `logs/P0-1/e_test_ticketboard.log` | none: 180 passed, 3 ignored |
| `hcargo test -p verification-core` | 0 | `logs/P0-1/f_test_verification_core.log` | none: 68 unit tests and 1 doc-test passed |
| `hcargo xtask ci verify-coding-standards` | 1 | `logs/P0-1/g_verify_coding_standards.log` | `ROUTE-TAG CHECK: FAIL — 11 unwired tag(s), 0 undocumented route(s)`; doc layout, `file-length` (3271 `.rs` files, 0 violations) and `no-select-star` pass |
| `hcargo xtask mod compile` | 0 | `logs/P0-1/h_mod_compile.log` | none: `OK: compiled clean`, 0 warnings in TBD sources |

### Baseline failures and findings

- `tooling_prose_rules::every_rust_file_named_in_prose_exists`, expected; P1-1 clears it by
  archiving the tools_v2 program records. First line:
  `prose names a Rust file that is nowhere in the workspace:` then
  `tools_v2/PHASE_FIVE_HANDOFF.md:2093: agent.rs`. All six hits are `agent.rs` in that file (lines
  2093, 2823, 3000, 3039, 3183 and 3188).
- `tooling_prose_rules::nothing_narrates_its_own_history`, expected; it belongs to the other
  session's equipment export code, committed in `ba275f244`. First line:
  `prose narrates a past state; describe the present one:` then
  `tools_v2/xtask/src/commands/mod_ops/equipment_vehicle_export/legacy_archive.rs:10: const JOURNAL: &str = ".legacy-archive.json";`.
  The hits are 8 lines in `legacy_archive.rs` and 8 in the same folder's `tests/validation.rs`.
- `tooling_prose_rules::only_a_layout_module_spells_a_repository_path`, expected; P1-4 clears it.
  First line: `a production source spells a repository path; put it in its crate's layout module:`
  then `tools_v2/xtask/src/verifications/api_readiness/fingerprint.rs:17: "docs/verification/api_v2/",`
  and `tools_v2/xtask/src/verifications/api_readiness/register.rs:10`.
- `verify-coding-standards` route tags, not expected; it comes from another session's uncommitted
  work. The 11 unwired tags sit on the untracked handlers under
  `apps/website/api_v2/src/community_content/handlers/equipment_data_viewer/`; the first is
  `source_inspection.rs:10  @route GET /api/v1/debug/equipment-data/containers  ->  handler
  containers is NOT registered in the api_v2 domain route tables on that method+path`. The handlers
  are mounted by an uncommitted `.merge(handlers::equipment_data_viewer::routes())` line
  (`apps/website/api_v2/src/community_content/routes.rs:18`): a sub-router the route-tag check does
  not read, since it reads only the eight domain `routes.rs` files.
- `ticket check --strict` passes although two child tickets cite `spec` files that do not exist:
  `T-068.10.5` (`docs/specs/Mission_Creator_Architecture/t068_10_5_weapon_variants.md`) and
  `T-159.15.0` (`docs/platform/t159_15_render_loop_handoff.md`). Both registry loaders admit only
  parent-id files as tickets (`tools_v2/ticket-engine/src/registry/typed_projection.rs:167`,
  `tools_v2/ticket-engine/src/registry/ticket_file_storage/storage.rs:114`) and the "spec missing on
  disk" rule walks registry tickets (`tools_v2/ticket-engine/src/validation/runner.rs:116`), so no
  child file's `spec` is ever checked. They are the only 2 of the 488 child files with a missing
  spec. Their `citations` name `t068_10_5_weapon_families.md` and `t159_15_render_loop_handoff.md`,
  which exist under `.ai/artifacts/`.
- A re-run of `hcargo xtask ticket check --strict` after the program files were written
  (`logs/P0-1/a2_ticket_check_strict_after_files.log`) exits 101 at compile time: another session is
  writing `tools_v2/xtask/src/commands/mod_ops/equipment_gameplay/` (untracked), whose `mod.rs`
  declares `policy_codegen`, `projection` and `resource_projection` before their files exist. All
  eight baseline runs finished before that edit began. None of the eight baseline commands reads
  `documentation_v2/`, so the program files cannot change a baseline result.

## Roster

| id | phase | role | status | commit | open questions |
|---|---|---|---|---|---|
| P0-1 | 0 | Program files and baseline | done | f8511271c | Personal paths in the program files replaced with `<repo>`, `<scratchpad>` and `$HOME` placeholders before the commit; `ticket check --strict` never checks child-ticket `spec` paths (see the baseline findings) — operator question pending |
| P0-2 | 0 | Manifest, mechanical rows | done | — (scratchpad outputs; committed by P0-5) | CP1: set names, dashboard/server_intel set placement, tbd-export evidence folder case clash with code `Verification/`, near-duplicate collapses (4 DESIGN.md trailing newline, Arsenal header) |
| P0-3 | 0 | Manifest, judgment rows | done | — (scratchpad outputs; committed by P0-5) | CP1: 11 questions in judgment_notes.md (vanilla_carve_coverage, SHIPPED_HISTORY topic, macos_ux_architecture, frontend _template, CLAUDE-CODE-START, eden wiki_manifest, KB-002, admin_tickets panel, feature doc file naming, frontend README primary, pending_merge naming) |
| P0-4 | 0 | Ticket plan and follow-up tickets | done (step 3 blocked) | — (scratchpad outputs; committed by P0-5) | CP1: top-level ticket ids exhausted at T-999 (schema.json:18 and sync/gap_analysis.rs:12 allow 3 digits) — widen or file as children; citation value format; rename ticket status deferred vs queued; rewrite bare-name mentions only where the basename changes |
| P0-5 | 0 | Manifest assembly and pin catalogue | done (≈645k tokens — over the 500k cap) | 660b70833 | 21 CP1 questions in refactor_move_manifest/cp1_questions.md |
| P0-5f | 0 | Apply the checkpoint-1 answers to the manifest (fix run) | done (≈256k tokens) | f79a84a42 | — |
| P1-1 | 1 | Archive the tools_v2 program records | done (≈145k tokens) | this commit | — (`every_rust_file_named_in_prose_exists` passes; the two remaining `tooling_prose_rules` failures are unchanged from the baseline; pin catalogue rows 77–82 applied, the `tools_v2/README.md` links now sit on lines 14, 16 and 19–23) |
| P1-2 | 1 | Retire the generated ticket views | done (≈415k tokens) | this commit | 3 questions answered (see amendments). Pin catalogue drift: row 102 gives the old watch list as TICKETS_DIR, TREE_DIR, ROADMAP — the optional list was repo root, docs tree, roadmap directory (ticket dir armed separately); P2-2 rows 193–194 (ROADMAP paths in the `file_watch` tests) now sit at :115/:119 |
| P1-2f | 1 | Fix run: lines that still describe the retired views (committed with P1-2) | done (≈135k tokens) | this commit | Test renamed `ship_regenerates_queue_from_post_state_reload_pin`. For G2: `.ai/tickets/AI_PLAYBOOK.md:9` names the marker `<!-- ticket-sync:next -->`; the real one is `<!-- ticket-sync:next:start -->` (`validation/constants.rs:16`) |
| P1-2c | 1 | Retire the `ticket milestone` verb (operator decision on a P1-2 question) | done (≈116k tokens) | this commit | Kept the `milestone` key: it belongs to `FROZEN_27` (`key_contract.rs:5-34`), the frozen record of the untyped ticket-file keys; the live allow-list (`ENCODING_C_KEYS` ∪ `ALLOWED_NEW`) already rejects it. Not ours: `mod_ops::equipment_gameplay::tests::gameplay_preserves_numeric_tokens_and_rejects_broken_publication` fails in the other session's untracked module; `fmt --check` diffs in its `equipment_vehicle_export/publication.rs:98` and `mod_ops/mod.rs:1` |
| P1-2b | 1 | Widen the ticket id pattern to 3+ digits; file the three follow-up tickets | done (≈423k tokens) | this commit | Filed T-1000 (scenario-to-mission rename, deferred, `order = 9811` hand-set), T-1001 (mod wave gate Makefile, idea), T-1002 (branch/worktree/status cleanup, idea, executor human), T-1003 (retire the untyped ticket-tree load path, idea). One shared id-order key, `ticket_engine::store::ticket_id_order_key`; `wave.lock` wave 0 gains T-1000 and T-1002. Still in plain string order, cosmetic only: `Corpus.tickets` key order, the `wave.lock` `[owns]`/`[depends_on]` tables, ticketboard `measured/aggregation.rs:139` |
| P1-2d | 1 | Fix run: `ticket set-status deferred|cancelled` on a ticket without an `order` leaves the registry red (tooling gap found by P1-2b) | done (≈221k tokens) | this commit | Mint: deferred, cancelled, shipped (also `ship`/`done`) get `append_order` (`ops/ordering.rs:87`); refuse: queued, ready, running, review; children stay order-less; a pre-write rule (`ops/validation.rs:115`) refuses a changed parent left without an order. Polish for the P1-6 fix run: garbled phrase in the `ship` doc comment in `ops/transitions.rs` ("the full-corpus map is the a dotted child id resolves here"); `cli/shipping.rs:48` comment does not mention the minting |
| P1-3 | 1 | Retire the prose gates | done (≈268k tokens) | this commit | Also edited `ticket-engine/src/sync/tests/gap_analysis_tests.rs:11` (it builds `CorpusPins`; kept). `schema-validate` now runs six sub-gates (~5 s warm). Pin catalogue rows 125–140 applied. Launch additions: checks `ticket check --strict`, the empty-grep proof, file sizes; deletions by plain `rm` |
| P1-3f | 1 | Fix run: comments made stale by P1-3 and P1-2d (committed with P1-3) | done (≈107k tokens) | this commit | Polish for the P1-6 fix run: the `ship` doc (`ops/transitions.rs`) and the `cli/shipping.rs` comment say the SHA "stays hand-edited", but `ticket stamp-sha` writes `shipped_at` |
| P1-4 | 1 | Api-readiness constants and layout existence tests | done (≈271k tokens) | this commit | `only_a_layout_module_spells_a_repository_path` passes. All 45 classified paths exist. An unclassified `documentation` item fails `every_documentation_item_is_classified` in each crate. `SPARSE_CHECKOUT_SETS` (10 paths) is must-exist too. For P2-2/H1: changing a value needs no test edit; deleting a constant (`LAYOUT_TARGET_DIR`) or adding one needs its row. Pin drift: the `starts_with` match is `fingerprint.rs:114`; pin rows 236–237 → `sync/runner.rs:28-38`; rows 276–282 → match by constant name. Polish for the P1-6 fix run: `api_readiness/fingerprint.rs:43` spells `tools_v2/xtask/deploy/deploy.env`, which `repository_layout::DEPLOY_ENV` already holds |
| P1-5a | 1 | Documentation gates, part 1: tracked tree, README coverage (Contents grammar), markdown placement, layout constants, two verbs | done (≈448k tokens) | this commit | Baseline before the cutover: readme-coverage 865 violations (387 folders without README, 478 READMEs without a Contents block); markdown-placement 40 (38 `.md` in code trees, `docs/` tracked, `ANALYSIS_AND_INVENTORY.md` 931 lines). Added `TICKET_DOCUMENTS_DIR`, `PROGRAM_RECORDS_PREFIX`, `ROADMAP`/`GAP_ANALYSIS` re-exports (sync-managed size exemption, per the operator decision). `CURSOR_RULE_DIRS`, `PERMALINK_BASE` carry `#[allow(dead_code)]` until P1-5b reads them; `DOCUMENTATION_ROOT` duplicates `TREE_DIR` once P2-2 repoints it (P2-2 or H1 merges them) |
| P1-5af | 1 | Fix run: exempt folders' READMEs skip the Contents check; spacer lines allowed; `verifications/README.md` lists `documentation/` | done (≈179k tokens) | this commit | One exemption predicate (`path_regions.rs` `below_exempt_folder`) feeds both checks; readme-coverage now 387 folders without README + 408 READMEs without a matching Contents block; `--path` inside an exempt folder judges nothing (exit 2) |
| P1-5b | 1 | Documentation gates, part 2: link check core — Markdown link scan, heading anchors, target resolution (repo-root/relative, folders, line anchors, own-repo permalinks), `link-check [--report]` verb | done (≈482k tokens — over the cap) | this commit | Tree-wide before the cutover: 548 documents, 471 links, 28 breaks (27 missing target, 1 non-permalink repository URL). Added `PROJECT_INSTRUCTIONS` (CLAUDE.md). Rule pipeline: `DocumentRule` (`judges`/`judge`/`finish`/`totals`). For H1: a CI link-check job needs full history (`fetch-depth: 0`) or old permalinks report unknown blobs. Live break for Phase 2+: `documentation_v2/website/frontend/apps/editor/arsenal/visual_reference/arsenal_tokens/DESIGN.md:1` links `../aegis_tokens/DESIGN.md` in the wrong folder |
| P1-5bf | 1 | Fix run: link-check excludes `.ai/artifacts/`; accepts the repository's non-code pages and `tree/<sha>` folder permalinks; `verifications/README.md` names link-check | done (≈274k tokens) | this commit | New `link_check/permalink_targets.rs`; `ARTIFACTS_DIR` re-exported into the layout module (classified); break label "unknown permalink object" (accepted). Tree-wide now: 533 documents, 436 links, 26 breaks, all missing targets — 20 in `docs/**` READMEs (the cutover), `apps/mod/tbd-framework/README.md:202` and `apps/website/README.md:41` (README writers), 3 in the archived tools_v2 records (P2-5), 1 live `documentation_v2` arsenal DESIGN.md (Phase 2+) |
| P1-5c | 1 | Documentation gates, part 3: backticked-path check and xtask command-citation check inside `link-check` | done (≈401k tokens) | this commit | Tree-wide: 820 breaks (791 backticked paths — 764 in the blueprint `documentation_v2/ANALYSIS_AND_INVENTORY.md`, archived in Phase 2; 3 command citations). `HISTORICAL_PATH_SPELLINGS` is empty. Writer inputs: `documentation_v2/runbooks/database_operations.md:47,52` cites `cargo xtask deploy db backup-drill` (code: `db backup-drill`, `deploy db drill`) → F13; `apps/mod/README.md:65` cites `mod test-phase1-api` (code: `mod test-game-runtime-api`) → R14; `apps/mod/tbd-framework/README.md:111` cites `docs/STAGING-SERVER.md` (real: `docs/mod/STAGING-SERVER.md`) → R14; `t0xx`/`tXXX`/`T-0xx` placeholders in `.ai/tickets/CLAUDE_CODE_PROMPT.md:14,15`, `.ai/tickets/README.md:53`, `.cursor/rules/application-code-forbidden.mdc:33` should read `<id>` → G2; relative `apps/…` spellings in `frontend/src/v2/README.md:43`, `pages/navigation/README.md:13`, `pages/mission_hub/review_workspace/README.md:10` must become full paths → R03/R04 |
| P1-5cf | 1 | Fix run: command citations check `mk` and `ci` targets against their real target lists; `link-check` help text names all three rules | done (≈208k tokens) | this commit | Lists read in place: `mk` from `commands/build/recipes.rs` `TARGETS`, `ci` from `commands/ci/task_definitions.rs` `TASKS`, other leaves from clap's possible values. No new breaks: the 3 command-citation breaks stay the ones P1-5c found |
| P1-6 | 1 | Phase 1 verifier | done (≈423k tokens) | this commit | FIX-LIST 7 (2 known, 5 minor, #7 optional): fingerprint.rs:43 DEPLOY_ENV; ship/shipping.rs SHA 'hand-edited'; 3 pub docs link private fns; NUMSTAT doc; unused xtask re-exports; xtask README groups; dup .ai/tickets literals. Red, other session's: history-word, equipment_gameplay, 12 route tags. |
| P1-6f | 1 | Fix run: the P1-6 FIX-LIST (7 items) | done (≈133k tokens) | this commit | All 7 applied; `hcargo doc -p ticket-engine` shows no warning for them |
| P1-6g | 1 | Fix run: pre-existing design citations and a false doc in the files Phase 1 touched; five rustdoc warnings in ticket-engine | done (≈130k tokens) | this commit | `hcargo doc -p ticket-engine` has no warnings; `readiness.rs` doc states the rule's real reach |
| P2-1 | 2 · commit 2a | Mover | done (≈211k tokens) | 19738aff0 | 973 `git mv` (589 move, 205 move+rename, 102 merge-into, 77 archive) + 85 `git rm` (52 collapse, 33 delete); 13 rows confirmed applied earlier; 5 near-duplicates verified byte-exact; `docs/` removed (114 empty folders, no untracked or ignored file); 17 emptied `documentation_v2` folders removed with `rmdir`; `git diff --cached -M -B` = 973 R100 + 85 D. Script and logs: `<scratchpad>/logs/P2-1/` |
| P2-2a | 2 · commit 2a | Pins, part 1: pin-catalogue rows of kind code, test and config (69 rows, 29 files) | done (≈420k tokens) | 19738aff0 | All 69 rows. `TREE_DIR` = `documentation_v2`; `PLANS_DIR`/`SPECS_DIR` under `documentation_v2/tickets/`; new `WEBSITE_DOCUMENTATION_DIR`, `MOD_DOCUMENTATION_DIR`; xtask `DOCUMENTATION_ROOT` = re-export of `TREE_DIR`; developer-tools `MOD_DOCS_DIR` → `DOCUMENTATION_ROOT`. Dirty file touched: `xtask/src/commands/deploy/tests/website/tests.rs` (own hunk at HEAD :107 only; the other session's hunk at :42). Red not P2-2a's: 2 registry tests (P2-3), the other session's 2 xtask tests, `actual_register_patterns_recognize_cargo_success_cases_only` (register line, P2-2b); `mk ci-local-leptos` stops at fmt on the other session's untracked data_viewer code (wasm clippy and frontend tests pass apart from that code's doc audit) |
| P2-2b | 2 · commit 2a | Pins, part 2: pin-catalogue rows of kind comment (87 rows, 72 files; 27 EnfScript) | done (≈393k tokens) | 19738aff0 | All 87 rows; register entry `missions_review_workspace` → moved `page.md` dropped (433 implementation paths all tracked); EnfScript ASCII check empty, `mod compile` OK, no `.meta`/rdb churn. Dirty files, own lines only: `.gitignore:3,24`, `apps/website/api_v2/.env.example` (old :26 → 26-27, old :85 → 86-87). Writer inputs: `TBD_Log.c:106-107`/`TBD_FrameworkManager.c:1195-1196` say the staging runbook quotes `[TBD] Stage →`, the runbook quotes `[TBD][Stage] … LOBBY` → F14; `ci.yml:252-259` narrates engine-split phases → H1; Caddyfile `/home/sam/tbd/repo` is the server's deploy path (left) |
| P2-3 | 2 · commit 2a | Tickets | done (≈336k tokens) | 19738aff0 | 658 ticket files rewritten (rewrite 1,084/1,086 — the 2 unmatched are the pre-P1-2 spellings of T-068.10.5 and T-159.15.0; keep 333; drop-view-link 10; manual 46 → 4 open rewritten, 42 closed left as history); 27 orphan rows (12 spec, 15 citations); T-320/T-354 lost one citation each (owns already names the path); waves unchanged, lock owns snapshot refreshed for 41 tickets; queue.json path spellings only; `ticket check --strict` green; T-1004 filed (idea) "Comment hygiene: design citations, ticket ids and history words" |
| P2-4 | 2 · commit 2a | Verifier for 2a | done (≈399k tokens) | 19738aff0 | FIX-LIST 3: T-090.12.7 summary; queued T-120/T-181.16 extension-less document names; `capability_verdicts.tsv:10` (→ P2-6). Every check green or the other session's; `-M -B` = 973 R100 + 85 D; markdown-placement only 8 live docs over 500 lines (Phase 5); readme-coverage 901, link-check 1,878 (2b/4/5). Staging lists: `<scratchpad>/logs/P2-4/23_*.txt` |
| P2-3f | 2 · commit 2a | Fix run: P2-4 FIX-LIST items 1–2 and the T-090.12.7 shipped-history deliverable | done (≈114k tokens) | 19738aff0 | Also dropped the matching `context` clause (accepted); waves unchanged; T-090.12.7 `approach` (line 29) still says "sync INDEX and TAGS" (both merge into F04/F17 targets) — left |
| P2-5 | 2 · commit 2b | Link rewriter, frozen corpus | done (≈375k tokens) | f996f8583 | 576 files: 856 links (355 moved, 18 merge-final, 1 collapsed, 155 repo-root normalised, 327 permalinks), 576 status lines (494 frozen record, 14 archived — see …, 68 archived); diff audit 0 exceptions; breaks 860 → 5 (P2-5f) |
| P2-5f | 2 · commit 2b | Fix run: unlink 4 never-committed artifact links in frozen specs; link-check treats empty-text full references as plain text | done (≈148k tokens) | f996f8583 | Frozen trees 0 breaks (576 documents, 1,715 links); whole tree 248 breaks, all live content (130 missing target, 109 backticked paths, 9 commands) |
| P2-6 | 2 · commit 2b | Link rewriter, live corpus | done (≈372k tokens) | f996f8583 | 124 files: 660 link destinations, 122 backticked paths, 90 status lines; diff audit 0 exceptions; its scope 1,018 → 248 breaks, all content-level (86 retired ticket views, 126 never-existing code paths, 17 never-existing docs paths, 8 split folders, 2 personal paths, 9 cited commands). Breaks by writer: F15 55, F07 48, F17 30, P3-2 22, F16 18, F04 15, F13 8, F02 8, F03 6, F12 5, F01/F08/F09 4, G2 4, root README 4 (G1), R14/F05 3, F14 2, R03/R04/R05/R11/F18 1 — list: `<scratchpad>/logs/P2-6/work/breaks_by_writer.txt`. 103 plain-text `docs/` mentions in 44 files remain for the writers; `CLAUDE.md:207-208` atlas lists `docs/` (G1) |
| P2-7 | 2 · commit 2b | Verifier for 2b | done (≈388k tokens) | f996f8583 | FIX-LIST 3: the status headers shifted 17 line-number pins by 2 (11 in live docs, 6 in open tickets T-711, T-935.15, T-404). Otherwise PASS: frozen corpus 0 breaks and only link/status/unlink changes; live corpus only allowed changes; 248 live breaks all content-level; status lines 0 violations; ticketboard spec reader PASS (770 spec/plan values, all tracked `.md` under the viewer cap); staging list `<scratchpad>/logs/P2-7/staging_2b.txt` (706 paths, no foreign hunk) |
| P2-6f | 2 · commit 2b | Fix run: P2-7 FIX-LIST (line pins shifted by the status header; T-404 archived-file deliverable) | done (≈133k tokens) | f996f8583 | Pins re-verified by content (7 needed a different number: pre-existing drift); 5 more gap-analysis pins fixed; T-404 lost the archived-file owns entry and its requirement clause; wave repack changed only T-404's owns snapshot; sync round-trips; link-check 248 (unchanged) |
| P3-1a | 3 | README standard and six README kind templates (area root, crate root, domain, leaf, page, app) | done (≈414k tokens) | this commit | `standards/readme_standard.md` (309 lines, grammar verbatim from the gate README) + templates README + 6 kind templates, each with a real-folder sample whose Contents equals `git ls-files`. Writer inputs (doc vs code): R11 `apps/website/README.md:27` (db seed applies 5 seeds, `db/operations.rs:126-132`; layout omits shared/, Dockerfile, staging compose), `apps/website/shared/is_http_url_cases.rs:5,7,11` stale paths; R01/G1 dev-login roles (`developer_login.rs:78`: guest, enlisted, leader, mission_maker, admin; default admin) in `api_v2/README.md:133` and `CLAUDE.md:246`; R01 `api_v2/README.md:120` vs missing `TRUSTED_PROXIES`; R02 `missions/README.md:18,44,120`; F09 `website/api_v2/api_overview.md:20` lists nonexistent `/ingest/missions`; R07 editor README title/tree and `editor/mod.rs:8` isolation claim; R09 `spatial/los/interior/README.md` boilerplate; F08 `apps/debug/building_viewer_page.md:6,14`; R17 `deploy staging` message |
| P3-1b | 3 | Six more README kind templates (command-line, data, mod scripts, mod assets, deploy/config, documentation_v2 folder), five document templates, templates folder README | done (≈404k tokens) | this commit | 17 templates in total, 12 real-folder samples all matching `git ls-files`. Writer inputs: R17 `tools_v2/xtask/deploy/README.md:16` (staging writes its own unit, `payloads.rs:89-113`; `fleet-host-agent.service` missing) + F14 `runbooks/game_server_staging/README.md:422`; R11 `contracts_v2/fixtures/missions/invalid/README.md:8,9`, `missions/README.md:12`, `contracts_v2/rules/README.md:49`; R12 `Systems/AI/README.md:10`; R14 `Prefabs/README.md:7`; F17 `known_bugs/README.md:10`, `kb_002…md:47,50`; F02 `event_schedule_page.md:14,34,49,57` (`scope` not `status`); F07 `decisions.md:352`; P3-1 fix: `readme_area_root.md:100-102` seeds before migrations (seeds need migrated tables; psql lacks ON_ERROR_STOP → silent) — same order in `CLAUDE.md` §3 (G1). Code findings for the next ticket batch: IP/personal paths in `deploy/website/help_text.rs:20-21`, `deploy.env.example:18,23-26,63`, `Caddyfile.website:9,33`, `tbd-website-backup.service:11`, `staging/config.rs:237,261`; AI scripts lack `@authority` and carry ticket ids; `xtask db --help` says `make db-up`; `find_chromium` in `sleep_ms.rs`; seeds lack `ON_ERROR_STOP` |
| P3-1v | 3 · CP3a | Reviewer standing in for the operator's CP3a review of the standard and templates | done (≈352k tokens) | this commit | FIX-LIST 7 blocking + 8 minor; grammar identical to the gate (436/436 words); all 12 sample Contents match; ~70 claims checked. Writer input: `contracts_v2/definitions/mission.schema.json:5` cites nonexistent `GET /missions/:id/compiled` (R11/F12) |
| P3-1f | 3 · CP3a | Fix run: all 15 P3-1v findings | done (≈322k tokens) | this commit | All 15 applied; samples match and are in name order; templates pass readme-coverage and link-check. Standard 315 lines |
| TF1 | 3 | Ticket filing: code findings from P3-1a | done (≈108k tokens) | this commit | T-1005 frontend layering inversion (chore), T-1006 staging deploy never starts the API (bug), T-1007 `TRUSTED_PROXIES` missing from `.env.example` (bug), T-1008 api_v2 missions comments cite a nonexistent inject tool (docs); all idea. Tooling finding: `ticket add` accepts but ignores `--program`, `--surfaces`, `--impact`, so every filed ticket gets scope `repo/docs` (to file with the next batch) |
| TF2 | 3 | Ticket filing: code findings from P3-1b and TF1 | done (≈115k tokens) | this commit | T-1009 `ticket add` discards --program/--surfaces/--impact (`ticket-engine/src/cli/mutations.rs:20`) (bug); T-1010 deploy files carry a literal host and personal paths (chore); T-1011 AI scripts lack `@authority` (chore); T-1012 `db --help` names retired make targets (bug); T-1013 browser launch lives in `cdp/sleep_ms.rs` (chore); T-1014 `db seed` exits 0 on a failed statement (bug); all idea. Noted, not filed: EnfScript `TBD_WaypointRuntime.c` (881 lines) and `TBD_GroupState.c` (524) exceed law 7's 500 lines; the file-length gate checks `.rs` only |
| TF3 | 3 | Ticket filing: admin UI bugs (F01), EnfScript annotations and stale comments (P3-2), server admin UI gap | done (≈143k tokens) | this commit | T-1015 confirm dialogs misstate detach/delete (bug); T-1016 calendar shows only the 20 earliest events (bug); T-1017 personnel search placeholder (bug); T-1018 Save Draft and broadcast toast (bug); T-1019 failed attaches toasted as success (bug); T-1020 EnfScript `@replicated`/`@rpc`/`@route` gaps (feature); T-1021 stale comments naming nonexistent things (docs); T-1022 admin UI to manage game servers — the API exists, no page uses it (feature); all idea, scope `repo/docs` (T-1009) |
| TF4 | 3 | Ticket filing: api_v2 findings from R01a/R01b | done (≈132k tokens) | this commit | T-1024 domain comments naming nonexistent routes/modules (docs); T-1025 `.env.example` lacks `MISSION_VERSION_MAX_BODY_BYTES`, `SKIP_MIGRATE`, `RUST_LOG` (bug); T-1026 `@contract` tags on three models (feature); T-1027 stale comments in core, workers, seeds, tests (docs); T-1023 minted with the wrong class and removed (id gap). Full spot lists: `<scratchpad>/logs/TF4/` |
| TF5 | 3 | Ticket filing: F01b's remaining findings; T-1015 amended | done (≈97k tokens) | this commit | T-1028 re-publishing a pinned announcement unpins it (bug); T-1029 CSV export for the audit logs page (feature — `ticket add` minted class `audit`, hand-corrected); T-1015 gains a note covering the guard test `event_manager/tests/event_manager.rs:119-126` and `lifecycle.rs:62-64` |
| TF6 | 4 | Ticket filing: wave 1–2 code findings | done (≈463k tokens) | this commit | T-1030 mission write-lock gaps (bug); T-1031 show kills/deaths/K-D on the deployments page once telemetry flows (feature — the placeholder is correct today); T-1032 multi-folder drop moves one folder (bug); T-1033 status bar OPEN button (bug); T-1034 hidden slot census (bug); T-1035 paper-doll keyboard (bug); T-1036 sign-in callback return path and error copy (bug); T-1037 content manager fetches before the admin gate (bug); T-1038 modpack drag handles (bug); T-1039 world-LOS frame pump after unmount (bug); T-1040 hand-copied cargo table (bug); T-1041 doc-audit allowlist vs law 7 (chore); T-1042 ticket ids in code/test names and UI strings (chore; 54 file names, 167 identifiers); T-1043 dead frontend code (chore); T-1044 api_v2 missions `/compiled` comments (docs); T-1045 stale frontend comments (docs). Skipped as covered: `role_notice` (T-846), unmounted panels (T-946.33/.39/.45/.49, T-851). T-1005 gained a notes line |
| TF7 | 4 | Ticket filing: wave 3 code findings (R07.2, R08.1–R08.3, V4.2, V4.3, V4.3f) | done (≈197k tokens) | this commit | T-1046 compile findings misfile refused mission blocks as win conditions (bug; the messages reach authors through the review pages); T-1047 decide whether Mission Creator validation gets cargo and loadout policy (feature); T-1048 align mission block checks with the schema (chore); T-1049 `@contract` on map-engine mission structs (feature); T-1050 re-hydrate keeps stale connections (bug); T-1051 minted vehicle/object id collisions (audit); T-1052 scatter stability across Rust releases (audit); T-1053 upload vs save duplicate-slot checks (audit; the upload check is the frontend's `dossier_upload.rs:93`); T-1054 unused store helpers (chore); T-1055 the engine-layers gate omits `editing` (chore); T-1056 stale map-engine mission data comments (docs, 158 files). Notes added to T-1004, T-1005, T-1042, T-1043, T-1045. Nothing dropped as false |
| TF8 | 4 | Ticket filing: map-engine findings of R09.1–R09.3, R10.1, R10.2 | done (TF8 stopped at the usage limit after minting T-1057–T-1059; TF8r ≈228k tokens) | this commit | T-1057 occluder residency events pile up (bug); T-1058 basemap switch-back never restores the satellite (bug, needs a browser check); T-1059 empty text uploads and stale labels (audit); T-1060 stub re-fetch and satellite budget (audit); T-1061 world LOS eviction ignores recency (audit); T-1062 derive the grid, basemap, peaks and forest from the terrain size (feature); T-1063 bench stress helpers and calibration hide (audit); T-1064 guards for binary formats and the doll shader layout (chore); T-1065 lint render code in the wasm32 clippy step (chore); T-1066 map object schema names a nonexistent file (docs); T-1067 dead map-engine code (chore); T-1068 stale map-engine comments outside mission data (docs); T-1069 rename unclear modules and numbered splits (chore); T-1070 map nouns in graphics-engine names, widen rule 2 (chore); T-1071 decide whether ignored inputs are intended (audit). Notes on T-1004, T-1042, T-1049, T-1055, T-1056. Dropped as false: doll picks off until the first resize (the only caller resizes first). The LFS pointer panic is T-972 |
| TF9 | 4 | Ticket filing: findings of R10.3, R11.1, R11.2, R11.3 | done (≈248k tokens) | this commit | T-1072 CI map-water task builds tbd-sat v2 against the v1 manifest (bug); T-1073 compiler golden regeneration writes outside contracts_v2 (bug); T-1074 world build-objects panics on a manifest without objects (bug); T-1075 orphan BLAS and stale terrain manifest docs (chore); T-1076 derive terrain checks and the create dialog from the registry (feature); T-1077 hosted commands run the post-change tail on no-op edits: dirty flag and draft save, not an undo step (audit); T-1078 unused text parameters and stale graphics-engine comments (chore); T-1079 shared URL-guard header paths (docs); T-1080 contract schema descriptions, `$id` hosts, workflow name (chore); T-1081 decide whether the unregistered Workbench registry and map export plugins stay so (audit). Notes on T-1004, T-1042, T-1062, T-1066, T-1067, T-1068. Dropped as false: `compute.rs:32` rebuilding its readback flag |
| TF10 | 4 | Ticket filing: mod findings of R12, R13, R14.1, R14.2 | done (≈246k tokens) | this commit | T-1082 record end reason and winner for extraction/VIP/trigger endings (bug); T-1083 keep the marker client poll off dedicated servers (bug); T-1084 arm the mission browser input context every frame (bug); T-1085 feed the pre-game screens live catalogs (feature); T-1086 mission runtime gaps: params, AI defaults, late-join audio (feature); T-1087 task HUD snapshot only on change (chore); T-1088 `@contract` on EnfScript JSON structs (chore); T-1089 uncalled mod methods and components (chore); T-1090 mod comments citing missing files (docs); T-1091 xtask mod texts saying tbd-export loads the framework (docs); T-1092 split EnfScript files over 500 lines and gate their length (chore); T-1093 pin icon GUIDs (chore); T-1094 fail mod compile on an addon depending on the framework (chore); T-1095 pin the apps/mod MCP configs (chore); T-1096 decide the dedicated-server Steam app id (audit); T-1097 the two CRF-only prefab GUIDs (audit). Notes on T-1004, T-1011, T-1042. `@route`/`@replicated` gaps are T-1020. Dropped as false: `GetNetLine`/`GetNetCount` uncalled; `@authority` on pure `TBD_Log`/`TBD_Sha256` |
| TF11 | 4 | Ticket filing: findings of R14.3, R15.4, R16, R17.1, R17.2 | done (TF11 stopped at the usage limit after T-1105; TF11r ≈247k tokens) | this commit | T-1098 top-bar count icon key `group` (bug); T-1099 unused mod UI icons, tab strip mode, stale comments (chore); T-1100 map water export unreadable by `map water` (bug); T-1101 map export road classes, spline transforms, DEM result (bug); T-1102 stale map export comments (docs); T-1103 wave gate migration 0016 default (bug); T-1104 dev-bootstrap `--api` and milestone seed use the retired container (bug); T-1105 milestone date mismatch (bug); T-1106 mod wave gate `enf::` filter matches nothing (bug); T-1107 `mk reclaim-target-ci --dry-run` deletes (bug); T-1108 mcp selftest reads a missing fixture as empty (bug); T-1109 rust-ci and `db test-it` isolation (audit); T-1110 deploy `--force` checksum repoint (audit); T-1111 `ai guard` chained commands (chore); T-1112 deploy errors cite wrong deploy.env lines and timer links (bug); T-1113 delete or render the `tbd-reforger.service` template (audit); T-1114 rename xtask files named after one helper (chore); T-1115 host-bridge copies and slice worktree tests (chore); T-1116 deploy.env vs environment precedence (audit); T-1117 stream `export-terrain` output (chore); T-1118 xtask build/ci Makefile narration (docs); T-1119 xtask help and error texts that contradict the code (docs); T-1120 dead xtask code and the font table generator (chore). Notes on T-1004, T-1006, T-1010, T-1012, T-1065, T-1072, T-1081, T-1093, T-1096. Dropped as false: the `ssh_argv.rs:26` exclude (a gitignored local folder, `apps/mod/.gitignore:5`), the ci help codes (they exist in `coding_standards/README.md`), the "missing spec" at `terrain_export.rs:9-10` |
| TF12 | 4 | Ticket filing: findings of R18.1, R18.2, R19, R20.1, R20.2, R20.3 | done (≈292k tokens) | this commit | T-1121 `verify ui-layouts` skips subfolders (bug); T-1122 `world validate-exports` fails on committed Everon chunks (bug); T-1123 decide chunk index `.bin` vs `.json.gz` (audit, FOR THE OPERATOR); T-1124 density check ignores the canopy blur (bug); T-1125 library code exits mid-gate (chore); T-1126 v-suite passes with no matching route (chore); T-1127 widen verify-phase density and E2c checks (chore); T-1128 raster pipeline inputs from the terrain (feature); T-1129 no-crf-leak font/backpack false positives (audit); T-1130 xtask verification tidy-ups (chore); T-1131 should Markdown invalidate API readiness evidence (audit); T-1132 developer-tools file renames (chore); T-1133 developer-tools duplicate helpers (chore); T-1134 developer-tools help and comment contradictions (docs); T-1135 blueprint fixture tidy-ups (chore); T-1136 zoomsweep configurable (feature). Notes on T-1004, T-1009, T-1010, T-1042, T-1055, T-1075, T-1097, T-1114, T-1118, T-1119. Key-order repair (amendment): `notes` moved above `created_at` in 83 tracked tickets, 13 re-quoted to the canonical render, values unchanged (checked against HEAD); `hcargo test -p ticket-engine` 235 passed incl. `corpus_roundtrip_real_tree_byte_identical`. Dropped as false: `unresolved` not counted (`assemble_manifest.rs:39` counts it) |
| TF13 | 4 | Ticket filing: findings of R21.1–R21.4, R15a and P4f's code-side leftovers | done (≈259k tokens) | this commit | T-1137 gate the ticket-engine, verification-core, ticketboard and fleet agent tests and clippy (chore); T-1138 ticket-engine messages broken by lost line continuations (bug, three sites); T-1139 stale ticket-engine/verification-core comments (docs); T-1140 duplicate id helpers and test-only public functions (chore); T-1141 should the ticketboard watch the gap analysis (audit); T-1142 ticketboard copies of engine logic (audit); T-1143 Remove dialog misses descendants `remove --force` deletes (bug; live case T-111 → T-067.1); T-1144 ready form lacks plan and tier gates (feature); T-1145 ticketboard comment residue and misplaced tests (chore); T-1146 one RCON password rule (audit); T-1147 unread `.nvmrc` (chore); T-1148 map export files overwriting each other and cell-edge duplicates (bug); T-1149 tbd-export tidy-ups (chore); T-1150 `mission_lookup` header (docs). Notes on T-1004, T-1042, T-1081, T-1102. Canonical form checked: corpus round-trip passes |
| P3-2 | 3 | Documentation standards, glossary skeleton and entry README | done (≈571k tokens — over the cap) | this commit | `standards/documentation_standards.md` 452 lines (§1–§10 kept for pinned code citations; Go/TS rules dropped); `glossary.md` 46 entries (`### term`); entry README 123 lines; 134 facts dispositioned. Writer inputs: F01 — `personnel_roster_page.md:30` and the personnel `page.md` source's "Edit Roles" contradict `personnel/role_dialog.rs:65-84` (roles follow Discord) and `role_management.rs`; `frontend/src/router.rs:288` breadcrumb "Comms Broadcaster" for `ContentManagerPage`. R01b/G1 — `server_infrastructure/mod.rs:1-2` and `CLAUDE.md:107,138` claim an RCON console; RCON lives in `apps/fleet_host_agent/src/rcon/`. G1 — `CLAUDE.md:32` limits the Rust doc rules to two trees (the workspace also has fleet_host_agent and ticketboard). Ticket batch: `core/configuration/mod.rs:298-299` stale RCON comment; EnfScript law-8 gaps (`TBD_FrameworkManager.c:300-321` `[RplProp]` without `@replicated`; `TBD_MissionBrowser.c:167` without `@rpc`; REST call sites without `@route`); ticket ids/history in comments outside the prose-tested crates (frontend, mod, engines); `route_tags.rs:6-8,29` describes retired Go/TS rules |
| R01a | 3 · pilot | README writer: api_v2 crate root, `migrations/`, `seeds/`, `src/` root, `src/bin`, `src/core/**`, `src/background_workers/**` (18 folders) | done (≈523k tokens — past the hard stop) | this commit | 3 rewritten, 15 new; ~45 facts carried. Writer inputs: F09 `website/api_v2/api_overview.md:19-22` (four nonexistent routes; 6 workers listed, 12 exist; `../` links); G1 CLAUDE.md dev-login roles; F19 the glossary's SSE entry (the audit feed does not run on `Hub`). Ticket batch (TF4): 13 stale code comments (`core/database/mod.rs:4-5` frozen 0001 vs 49 migrations; `configuration/mod.rs:61` and `.env.example:40` `/map-assets`; `middleware/mod.rs:3-4` order; `http_client/mod.rs:1-2`; `observability/health_probe.rs:72-73`; `token_purge_worker.rs:17`, `event_lifecycle_sweeper.rs:25` "aborted on drop"; `db/operations.rs:124-125`; `seeds/discord_roles.sql:9`; `tests/db_migrate.rs:3`; `durable_ratelimit.rs:14`; `rust-toolchain.toml:2`); `.env.example` lacks `MISSION_VERSION_MAX_BODY_BYTES`, `SKIP_MIGRATE`, `RUST_LOG` |
| R01b | 3 · pilot | README writer: api_v2 `src/{identity_and_access, administration, community_content, match_telemetry, command_center, server_infrastructure}/**` (24 folders) | done (≈371k tokens) | this commit | 6 domain READMEs rewritten, 18 new (≈15k tokens per folder). Writer inputs: F09 `documentation_v2/website/api_v2/api_overview.md:21,24` (no `/ingest/server-status`; heartbeats are `/game-runtime/sessions/{sessionId}/heartbeats`; no RCON route; membership-grace and `/fleet/scenarios` missing); F13 `runbooks/local_development.md:265` (role PATCH answers 409); G1 `CLAUDE.md:246` (dev-login roles). Ticket batch (TF4): comments `server_infrastructure/mod.rs:1-2` (RCON), `administration/routes.rs:4` (not every route is AdminUser), `match_telemetry/handlers/mod.rs:4` (`attendance_attribution` does not exist), `developer_login.rs:65` (4 roles listed, 5 accepted), `server_registry.rs:8-9,78` and `server_intel.rs:32,37` (stale names); models without `@contract` (`current_profile.rs`, `fleet_command.rs`, `machine_credential.rs`) |
| F01 | 3 · pilot | Doc writer: `website/frontend/pages/administration/**` | stopped at the hard stop (≈495k tokens) | — | Research only: verified every admin page (routes, access, API calls and server meaning, states, exact texts, design references, open tickets, dispositions of all 19 sources) into `<scratchpad>/logs/F01/handoff_verified_facts.txt`; wrote nothing. Writer inputs for R05: `pages/administration/README.md:17` ("doctrine articles" — announcements only), `event_manager/README.md:66` (the API accepts `modpack_id`/`server_id`, `event_create_update.rs:105-106`), `audit_logs/README.md:14-16` (`target_type` filter, `filter_bar.rs:149`) |
| F01b | 3 · pilot | Doc writer: F01's targets, written from F01's verified handoff | done (≈453k tokens) | this commit | 6 feature docs (117–282 lines), 18 folder READMEs (6 design sets: design-phase references), 13 sources merged and deleted, glossary audit-logs entry corrected, backlinks in 7 code READMEs; gates pass. Writer inputs for R05: `event_manager/README.md:66-67` (operations carry `modpack_id`/`server_id`), `personnel/README.md:13-14,23-24` (no role picker; a Discord note), admin root `README.md:13,17` (no promotions, no doctrine articles); R05 also owns the "The page README lists no calls, so the DTOs are named here" lines in F01b's six Data sections, to replace with links once it writes the README Data sections |
| P3-4 | 3 | Tooling fix: the documentation gates accept `--with-untracked` | done (≈341k tokens) | this commit | One `DocumentationGateArgs` (`--path`, `--with-untracked`) and one `GateRequest`; the tree reader adds `git ls-files --others --exclude-standard`; summaries say `--with-untracked (untracked files included)`. Caveat for writers: the flag also sees another session's untracked files (4,558 today) — scope `--path` to your slice and ignore failures in foreign untracked paths |
| P3-3 | 3 · pilot | Pilot verifier over R01a, R01b, F01/F01b | done (≈325k tokens) | this commit | FIX-LIST 11 + a 12-rule STYLE-LOCK. Claims: R01a 38/0 wrong, R01b ~60/0, F01b ~60/1; all 66 pilot files conform on structure; scope clean. Item 11 (R05 at its wave): replace F01b's route facts, state texts and "lists no calls" lines with links to the page READMEs' Routes, Data and States |
| P3-3f | 3 · pilot | Fix run: P3-3 FIX-LIST items 1–10; write `documentation_v2/refactor_style_lock.md` | done (≈486k tokens) | this commit | All 11 applied; style lock rules 1–13 + don't-copy list, 20 models script-checked; also corrected 4 pilot contradictions (low-FPS warning timing, audit-row rule, glossary link placement, "event" wording) |
| P3-3g | 3 | Fix run: `documentation_v2/README.md` Contents covers the new program records | done (≈85k tokens) | this commit | The ten `refactor_*` file lines collapsed into one `refactor_*` glob (future records need no edit); the root README passes readme-coverage |
| R02 | 4 · W4.1 | README writer: 15 folders — `apps/website/api_v2/src/missions` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈634k tokens — far past the stop) | this commit | 13 new, 2 rewritten; four leaves 85–91 lines (kept). Template fix list: `standards/templates/readme_domain.md:79-80` (not every author/admin write takes the write lock: `mission_versions.rs:52-61,207-213`, `mission_armory.rs:101-110`) and `:99-101` (only `mission_title_terrain`/`historical_mission_title_terrain` are called by other domains). F09 input: `verification_evidence/mission_artifacts.md:57` `{artifactId}` and its refusal list omits `SERVER_INACTIVE`, `EVENT_MISSION_NOT_ON_SERVER`. Ticket batch (TF6): `services/cargo_catalog.rs:22-31` names nonexistent callers; `GET /missions/:id/compiled` cited but unrouted (`mission_compile.rs:29,30,92`, `schema_validators.rs:132`, `zone_quantisation.rs:161,173,176`, `mission_versions.rs:86,280`, `version_payload.rs:25`; test `mission_compile_diagnostics.rs:162-165` asserts it); version save, set-current and armory replace skip `lock_editable_mission`; `validation/mod.rs:3-4` vs `mission_fields.rs:30-31`; `models/mod.rs:6` |
| R03 | 4 · W4.1 | README writer: 14 folders — `apps/website/frontend` (see `refactor_phase4_slices.tsv`) | done (R03 cut off by the usage limit; R03r ≈462k tokens) | this commit | 14 READMEs (7 new, 7 rewritten), each re-verified at HEAD. Ticket batch (TF6): `src/v2/tests/doc_audit/allowlist.rs:13` is a dated grandfather allowlist that can exempt the 500-line rule, against law 7; `core/api/client/fetched.rs:5` `Fetched`/`ApiFailure` are dead surface; `api_v2/seeds/content_golden.sql:5` vs `:1106`; frontend `Cargo.toml:133` comment names the graphics crate (the dev-dependency is the map engine); `src/v2/core/mod.rs:9` says every child is ungated (`test_support` is `#[cfg(test)]`); code and test names carrying ticket ids (`ui/mod.rs:45`, `tests/ui_t633_range_and_select.rs`, `…_t668_recipes`) with R04's. T-1005 addendum: `src/v2/core/mod.rs:8`, `src/v2/mod.rs:11` claim core never imports an app |
| R04 | 4 · W4.1 | README writer: 18 folders — `apps/website/frontend/src/v2/pages (folder only)` + 3 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈508k tokens) | this commit | 16 rewritten, 2 new. Ticket batch (TF6): deployments page shows "No telemetry recorded" although the API serves kills/deaths/K-D and the DTO reads them (`deployments/page.rs:12-13`, `member_service_record.rs:6-8`, `dto/events.rs:197-205`; guard test `tests/deployments.rs:126` "Until T-397", T-397 shipped); `role_notice` query added by `router.rs` `auth_denial_redirect` is read by no page; test file names carrying ticket ids (`server_intel_t385.rs`, `server_intel_t773.rs`, `create_dialog/tests/dialog_t671_…rs`). Covered elsewhere: `router.rs` ticket ids/history (T-1004), "Scenario Creator" (T-1000), editor boundary (T-1005) |
| R05.1 | 4 · W4.1 | README writer: 11 folders — `apps/website/frontend/src/v2/pages/account` + 3 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈391k tokens) | this commit | 4 new, 7 rewritten; navigation written as a page (it holds `NotFoundPage`, the router's `*` route). Phase 5 inputs: F04 `account_pages.md:15,29,47` (tier `none`; link status via `GET /me/link/status`, no PATCH), `app_layout_and_navigation.md:38-40,47` (Vehicle Database link; "Comms Broadcaster"/`campaign`); F03 `vehicle_database_page.md:78,85`, `modpacks_page.md:22,24-25,36`, `wiki_page.md:76,85-86,93`, `mortar_calculator_page.md:12,22-24,35,48` (server computes; number fields; behind AuthGate). Ticket batch (TF6): `auth_callback/page.rs:107-109` (claims a return to the start page; always `/`; quote mismatch) and missing messages for `server_error`/`oauth_host_mismatch`; stale paths in `mortar/weapon_selector.rs:20-21`, `tests/mortar.rs:184-185,364,372`; `modpacks/pack_editor.rs:3` "reorderable" with no control; `navigation/layout.rs:39` chromeless claim; `navigation/membership_status.rs:1-3` lacks the module header |
| V4.1 | 4 · W4.1 | Wave verifier over W4.1's writers | done (≈321k tokens) | this commit | FIX-LIST 10 (2 truth: R02 operations omits `command_center` in Depends on and two tests in Used by; 8 style: short `/api/v1` paths, partial paths, glossary placement, `mod.rs`/`tests/` roles, breadcrumbs "›" vs the rendered "/"). ~400 claims checked; gates pass on all four slices; scope clean |
| V4.1f | 4 · W4.1 | Fix run: V4.1's FIX-LIST + R05.2's breadcrumbs + the domain template sample's two wrong claims | done (≈200k tokens) | this commit | All 11 items; 16 breadcrumbs now "A / B". Leftovers for the next fix run: `api_v2/src/missions/services/README.md:21` repeats the false every-write-takes-the-lock claim (and `mission_write_lock.rs:1`'s module doc — TF6); `pages/administration/event_manager/README.md:24` `tests/` role wording; `api_v2/tests/user_stats_service.rs:347` missing from operations services' Used by |
| R05.2 | 4 · W4.2 | README writer: 13 folders — `apps/website/frontend/src/v2/pages/administration` (see `refactor_phase4_slices.tsv`) | done (R05.2 cut off by the usage limit; R05.2r ≈374k tokens) | this commit | 13 READMEs (6 new, 7 rewritten); the six admin feature docs now link the READMEs' `#routes`, `#data`, `#states` (two wrong doc facts dropped with the duplicates); style-lock model ranges re-pointed. Access-sheet states stay in the event manager README's States (the access folder is a domain kind). Ticket batch (TF6): `audit_logs/filter_bar.rs:9` comment says five fields, `:28` also matches `target_type`; `personnel/role_dialog.rs:9,13-18,66-75` dead `ROLE_OPTIONS`, a stale "role picker" comment and unused parameters; `content_manager/page.rs:47` fetches before `AdminGate` wraps the page (`:118`) |
| R06.1 | 4 · W4.2 | README writer: 16 folders — `apps/website/frontend/src/v2/apps/editor/ui (folder only)` + 3 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈550k tokens) | this commit | 12 new, 4 rewritten. Ticket batch (TF6): five Mission Creator panels nothing mounts (`inspector/{radio_panel,tasks_panel,audio_emitters,weather_timeline}.rs` say the settings dialog hosts them; `modals/settings_modal/mission_dialog.rs:131-138` does not; `modals/mod.rs:27-28`) — a decision ticket (mount or delete); dead `placed_vehicles_panel` (`inspector/vehicles_panel.rs:1,86-91`); stale `map_engine_core::mission::…`/`operations::update_environment` names and T-753/wave-115 history in inspector comments; `modals/mod.rs:8-9,18-19`, `inspector/mod.rs:34-36`, `arsenal/panels.rs:138` stale comments; paper-doll hotspots take clicks only although called keyboard-accessible (`arsenal/panels.rs:287-289`); `shell/eden_chrome.rs:18` re-exports an unmounted `BottomToolbelt` |
| R06.2 | 4 · W4.2 | README writer: 19 folders — `apps/website/frontend/src/v2/apps/editor/ui/docks` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈637k tokens) | this commit | 17 new, 2 rewritten. Ticket batch (TF6): multi-folder drag onto the left dock header moves only the anchor (`dock_left/view/full_dock.rs:17`, `tree/single_row.rs:344` vs `outliner/mod.rs:10-12`); `favourites/panels.rs:245-246` stale (stamps and ORBAT vehicle adds are listed: `recent/mod.rs:81`); status bar "OPEN" button without handler (`toolbar_and_status.rs:225`); slot census and summary rendered inside a hidden element (`menu_row.rs:242`); Arrange shortcuts restated in `mission_editor/page_effects.rs:43-61`, `arrange_for_code` test-only; `orbat_refile` always false (`full_dock.rs:143`), branch unreachable; tooltips naming tickets (`factions_panel.rs:96` T-078 cancelled, `toolbar_and_status.rs:205` T-667); doc-comment defects in `dock_left/*.rs`, `markers/icons.rs:77,106,138`, the misplaced `build_orbat` doc, `docks/mod.rs:17-18` four tabs |
| R07.1 | 4 · W4.2 | README writer: 16 folders — `apps/website/frontend/src/v2/apps (folder only)` + 5 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈451k tokens) | this commit | 10 new, 6 rewritten; aar/planner are placeholders ("reserved for …"; their design notes stay in `pending_merge/F08/`); debug benches written page-like. Ticket batch (TF6): `debug/world_los/live/mount.rs` has no `on_cleanup`, so the frame pump keeps running after unmount (`graphics-engine/src/loop/pump.rs:109`; the building viewer sets it at `live/wiring.rs:453`); `debug/world_los.rs:60` shows a ticket id on screen; stale comments `debug/building_interior.rs:37`, `building_viewer/geom.rs:199-219`, `building_viewer.rs:8-9`, `live/wiring.rs:7` (no key listener), `apps/mod.rs:3-4` ("four applications", two modules); `cargo_defaults_by_character` test-only (`editor/arsenal/rules/cargo_capacity_and_delivery.rs:37`); `CARGO_CONTAINERS` hand-copied into the map engine with no equality test and a differing caveat (`map-engine/…/wire_safety/scan.rs:241-244`). F03 input: `documentation_v2/website/frontend/apps/debug/building_viewer_page.md:6`. F08 input: `apps/editor/README.md:7` `../../` links |
| V4.2 | 4 · W4.2 | Wave verifier over W4.2's writers | done (≈345k tokens; first run stopped at the usage limit) | this commit | FIX-LIST 10 (1 substantive: the ORBAT Manager README misses the coherency gate and a core UI test among its users; 9 style); ~120 claims, 0 false; gates clean; consistency across ui/editor/admin docs holds. T-1005 note for the next ticket batch: `apps/editor/mod.rs:7-8` says the editor uses the graphics engine, which no apps crate depends on |
| V4.2f | 4 · W4.2 | Fix run: V4.2's FIX-LIST + the style lock's two stale "don't copy" entries | done (≈285k tokens) | this commit | All 11 items (6 extra comma-variant Boundaries bullets under `apps/editor/shell/` fixed too; `dock_right/shell` `mod.rs` role names its re-export, 116 characters, accepted); style lock's two stale "don't copy" entries now generic |
| R07.2 | 4 · W4.3 | README writer: 19 folders — `apps/website/frontend/src/v2/apps/editor/bridge` + 3 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈619k tokens) | this commit | 8 rewritten, 11 new; `mission_editor/` is a domain (its route component sits in the parent `editor/mission_editor.rs`). Phase 5 inputs: F05 `feature_inventory/README.md:137` (only the middle button pans); F07 `decisions.md:326-327`, `ux_spec.md:99-100` (a warm return always fetches; autosave writes only the local IndexedDB draft). Next ticket batch (TF7): stale comments `bridge/mod.rs:8`, `editor_context/mod.rs:16`, `input/mod.rs:12` (pointer_up and ZDrag write the document directly), `shell/mod.rs:33-36,56`, `eden_chrome.rs:8-10`, nonexistent `editor_ops`/`mission_editor::on_load`, `mission_saving.rs:36`; no UI caller for the clipboard exporters, `merge_mission_now`, `other_missions`; the `window.__editorObjectWash` probe has no reader (full list: R07.2's scratchpad log) |
| R08.1 | 4 · W4.3 | README writer: 13 folders — `apps/website/map-engine/src/data (folder only)` + 6 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈525k tokens) | this commit | 12 rewritten, 1 new. Next ticket batch (TF7; bug only where the code is clearly wrong): refused authored blocks reported under `COMPILE-WIN-CONDITIONS` (`compile_graph.rs:313-322`, bug); the engine-layers gate lists 9 of 11 top-level modules (`xtask engine_layer_rules.rs:108-110,141,144`, gap); four policy/catalog validator rules never apply in the product because the Mission Creator passes only asset ids (question, may be intended); ticket ids in runtime messages (`roster.rs:52` T-200, `vehicles.rs:80` T-425); a nonexistent route in author-facing finding messages (`flatten/diagnostics.rs:117,140`) and comments; missing `@contract` in `ast/entities.rs`, `ast/scenario.rs`; stale `//!` headers (`Position: mission/...`), retired-code references (compile.ts, Go), `registry.rs:219`, `rules.rs:119`, `document.rs:46,50`, `mission_armory.rs:167`, `cargo_catalog.rs:15-17`, `contracts_v2/rules/kit-aliases.json` `$comment`. F03 input: `mortar_calculator_page.md:35` (the page posts to `/fire-missions/solve`) |
| R08.2 | 4 · W4.3 | README writer: 11 folders — `apps/website/map-engine/src/data/scenario/extensions` (see `refactor_phase4_slices.tsv`) | done (R08.2 stopped at the usage limit; R08.2r ≈223k tokens) | this commit | 11 rewritten. Next ticket batch (TF7): refused authored blocks (tasks, weather timeline, audio, spawn modules, tactical graphics) are reported through `diagnostics.win_conditions` with location `/winConditions` and a "falls back to the value it derives" message although nothing is derived (`compile_graph.rs:315-324`, `diagnostics.rs:150-157`) — real defect (dropping with a warning is intended); stale comments `authored/authored_block.rs:17,49,125,130`, `tactical_graphics/shapes.rs:22`, `mission/extensions/…` headers, `ast/authoring.rs:25,55`; possibly intended: blank-string checks that trim in some blocks and not in others, the weather preset list held twice, empty `spawnModules`/`tacticalGraphics` arrays allowed by the schema but refused |
| R08.3 | 4 · W4.3 | README writer: 12 folders — `apps/website/map-engine/src/data/store` (see `refactor_phase4_slices.tsv`) | done (R08.3 stopped at the usage limit; R08.3r ≈497k tokens) | this commit | 12 rewritten (4 drafts reviewed, 8 written). Next ticket batch (TF7): `rows/hydrate.rs:30-48` clears every row map except `connections` although `:135` loads them, so re-hydrating a live document (`editing/persist/server_adoption.rs:139`) keeps stale connections — likely defect; `mint_id` (`operations/entity/identity.rs:55-64`) checks slot ids only but also mints vehicle/object ids with a per-session counter (`editing/host.rs:48`) and unchecked inserts (`rows/vehicles.rs:37,217`, `rows/entities.rs:28`) — possible defect; stale TS-era wording and nonexistent names in rows/crdt/operations comments (full list: R08.3r's scratchpad log); possibly intended: unused `slot_hidden_rows`, test-only helpers, `remove_slot` without callers, duplicated squad helpers, unwritten vehicle `label`, `DefaultHasher` seed vs the same-scatter promise (`geometry.rs:102,112`). Out-of-slice stale comment: `frontend/…/imp/mission_saving.rs:35-36` |
| V4.3 | 4 · W4.3 | Wave verifier over W4.3's writers | done (≈363k tokens; stopped just past its 350k stop) | this commit | FIX-LIST 13 (2 truth: R07.2 `mission_editor/` says no test compares `page_effects.rs` with the arrange list, while three tests do; R08.3 `store/` says the Mission Creator reaches the document only through `crate::editing`, while the page holds the handle and writes the document directly too; 11 precision and style). About 160 claims checked; gates pass on all 55 folders; scope clean. Item 4 (the Boundaries labels) already landed with V4.2f (fbc44c2a8). Next ticket batch (TF7): `store/crdt/undo_groups/clocks.rs:26` names the wrong caller of `install_wasm_now` (the real one is `frontend/…/bridge/host_state/undo_grouped_gestures.rs:62-64`); `xtask …/architecture/editor_orbat_coherency.rs:29` says `--features "doc mission"` while the code passes `scenario store` (:209); history and ticket-stub comments in `map-engine/src/data/mod.rs:6-10` and `mission_saving.rs:62-71` (add to T-1004) |
| V4.3f | 4 · W4.3 | Fix run: V4.3's FIX-LIST (items 1–3, 5–13) + V4.1f's two open leftovers | done (≈247k tokens) | this commit | All 14 items; both gates exit 0 over 103 READMEs. It also fixed the same claim that `scenario` never reads `store` in `data/README.md` How it works, and added the document helpers to the `store/crdt` Used by. Leftovers for the next fix run: the `bridge/document_host` Used by leaves out `tests/t780_connection_line.rs:372`, `tests/t819_crewed_render_hide.rs:157` and the helper `tests/mission_editor/source.rs:73`; bare or one-segment paths that point outside the folder at `mission_editor/canvas_mount:59` (`arsenal/`), `store/operations/entity:78,83` (siblings) and in the `bridge/document_host` Used by; an unverified "every surface" claim at `api_v2/src/missions/services/README.md:19`. Next ticket batch (TF7): the upload path's duplicate-slot check (`check_duplicate_slot_ids_in_payload`) refuses a payload that the engine's `duplicate_slot_ids` lets through. `shell/document_commands/imp/mission_saving.rs:30-43` records this as an open gap, alongside a garbled phrase and a reference to a slice report; it may be a defect |
| R09.1 | 4 · W4.4 | README writer: 16 folders — `apps/website/map-engine/src/spatial` + 2 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈624k tokens) | this commit | 15 rewritten, 1 new (all were boilerplate). Next ticket batch (TF7): world LOS residency `tick` never advances, so LRU eviction runs in path order (`los/world/state.rs:61-62`, `residency.rs:133,202-212`) — possible defect, correctness holds; Everon-only constants for peaks and forest density bins (`locations/loader.rs:91-102`, `vegetation/density.rs:6-19`, `loader.rs:36-37,144`) — limitation; unused re-export modules (`trace/`, `bvh/tree/`, `locations/routes/`) and dead functions (`blocked_fn`, `compose_forest_mesh`, `forest_mass_from_corners`, duplicate `FOREST_FILL_RGB`, `clear_world_buildings`, `ArchiveBoot::blas_by_pid`) — cleanup; stale comments and broken doc links (`raycast.rs:195`, `viewshed.rs:99,109`, `node.rs:73`, `sidecar.rs:45,231`, `locations/loader.rs:37`, `lib.rs:56`, `towns.rs:145,163`); every `//!` header says "graphics engine" in the map engine; history, retired-TS and spec-id comments; malformed `#[doc]` strings (`los/world/mod.rs:23-60`); missing `@contract` on `descriptor/model.rs`, `manifest.rs` |
| R09.2 | 4 · W4.4 | README writer: 13 folders — `apps/website/map-engine/src/streaming` (see `refactor_phase4_slices.tsv`) | done (≈553k tokens — past the 400k hard stop) | this commit | 13 rewritten (three exempt `tests/` READMEs untouched). F09 input: no live document covers map streaming (chunk residency, memory budget, loaders); `documentation_v2/website/map-engine/` should. Next ticket batch (TF7): `streaming/loaders/occluder_loader.rs:218` returns before draining residency events when the occluder never became ready (prefab load failed, `world_loader/bootstrap.rs:65-70`), so the queue fed at `scheduler/viewport.rs:154,200` grows for the session — bug; possible defects: an undelivered stub never enters `known_empty` (`viewport.rs:127,179`, `loaders/residency.rs:120`), so a later pin fetches it again; the satellite's reserved bytes are never released (`memory/budget/accounting.rs:49`); limitation: the grid and basemap are fixed at 12,800 m for every terrain (`host/queries.rs:9`) while Arland is 4,096 m (`terrain-registry.json:18`); cleanup: test-only `ingest_budget_exhausted_at` (`scheduler/budget.rs:21`), duplicate `BUILDING_MIN_ZOOM` (`buffers/revision.rs:12` vs `overlay/lod.rs:28`), callerless `invalidate_chunk`/`release_inflight` and the `host/mod.rs:36` re-export; stale comments: `loaders/fetch.rs:21`, `loaders/store.rs:41` (nonexistent `super::binary::BinaryError`), one-line tables `loaders/prefab.rs:168`, `loaders/store.rs:131`, `host/mod.rs:35`, `scheduler/viewport.rs:239`, `chunk_math.rs:9`, retired JS/TS references (`chunk.rs`, `manifest.rs`, `chunk_math.rs`, `budget.rs`, `viewport.rs`, `revision.rs`), template `//!` headers, `apps/website/frontend/Cargo.toml:31-32`; test gap: `eviction_order_is_ascending_last_used` (`scheduler/residency/tests/cases_1.rs:203`) never checks the order. Ticket-id folder names are T-1042 |
| R09.3 | 4 · W4.4 | README writer: 16 folders — `apps/website/map-engine/src/world/architecture` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈530k tokens — past the 400k hard stop) | this commit | 16 rewritten; dropped the old `los` Contents entry (no such folder, `architecture/mod.rs:10-17`); water/ now says the host loads the lakes/rivers/ponds archive but never draws it. Next ticket batch (TF8): switching the basemap from map back to satellite never restores the satellite: `load_map_basemap` replaces the texture (`world/terrain/satellite/quadtree/basemap.rs:75-95`), while `show_satellite_basemap` (`:22-26`) only restores opacity (callers `streaming/host/preferences.rs:9-23`). This is a likely defect that needs a browser check. Limitations: archived blueprints lose door state and furniture size (`architecture/blueprint/archive.rs:105,148`, `attribution_2.rs:61,86`); `relief/host.rs:28` fixes the grid at 12,800 m; `dem/png.rs:52-58` accepts 16-bit RGB/RGBA against `:27`'s grayscale claim. Possibly intended: hillshade slope measured in samples, not metres (`relief/hillshade.rs:32`); a 429's Retry-After is logged but ignored (`quadtree/retry.rs:198-219`); no map-engine model carries `@contract`. Hostile input only: `streamer/validation.rs:19,107` can overflow `offset + length`. Unused: `relief/contours.rs:351`, `roads/airfield.rs:28,31,74,206` (`:28` duplicates `roads/styling.rs:309`), `dem/grid.rs:38`. Stale comments: "graphics engine" headers, ticket/history notes (`architecture/mod.rs:6-8`, `satellite/textures.rs:15-18`, `water/mod.rs:11`), `compound/assembly.rs:69` (wrong file), `section/cutter.rs:204`, retired TS/deck.gl references across dem/relief/roads, `editing/tools/line_of_sight/capture.rs:209` (`map_engine_core`), `water/loader.rs:22`, and `tools_v2/developer-tools/src/blueprint/archive_emission/archive_writer.rs:151` against `blueprint/archive.rs:19`. Naming (law 4): `attribution_1.rs`/`attribution_2.rs`, `compound/scene/` (holds no scene), `satellite/quadtree/` (holds no quadtree); a frontend editor test reads the nine quadtree files by path. Ticket ids `t935_10/` and `t938_4_measure_visits_and_bytes` fall under T-1042 |
| R10.1 | 4 · W4.4 | README writer: 16 folders — `apps/website/map-engine (folder only)` + 5 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈557k tokens — past the 400k hard stop) | this commit | 14 rewritten, 2 new (`frame/`, `frame/upload/`); it dropped 7 nonexistent module folders from the old root Contents. F09 inputs: `pending_merge/F09/documentation_v2__website__map_engine__readme.md:8-10` (camera physics, frustum culling and MGRS do not exist; rkyv lives in `io/`; there is no top-level `symbology/` and no MIL-STD-2525); `standards/engine_boundary_rules.md` (§1, §2C, §2C.1, §2D, §5) is cited by `engine_layer_rules.rs:194-238`, `task_definitions.rs:293` and `frame/tests/damage_discipline.rs:12` but does not exist yet. G1 input: CLAUDE.md's atlas lists `symbology/` as top-level (it is `overlay/symbology/`), omits `overlay/`, and credits `camera/` with MGRS. F19 input: glossary terms lane, frame packet, render engine. Next ticket batch (TF8): possible defects: an empty text upload leaves the old height and road labels visible (`frame/upload/text.rs:73-78,169-174` vs `:122-125`; the sender is `world/environment/locations/loader.rs:185-187`); `seed_stress`/`clear_stress` assume calibration is the last batch (`diagnostics/bench/frame_1.rs:99-102,149-152`; they have no callers); `hide_calibration` marks no damage (`frame/lifecycle.rs:315-322`, may be intended); `wasm-ci`'s wasm32 clippy passes no features (`task_definitions.rs:435-437`), so render code may go unlinted; `peaks_tests.rs:271-274` panics on an LFS pointer. Gate gap: rules 4 and 7 omit `editing` (T-1055); `engine_layer_rules.rs:111` and `engine_layer_boundaries.rs:72` say ten modules, while `lib.rs` declares eleven. Contradictions in comments: `map-engine/Cargo.toml:15-16`, `frontend/Cargo.toml:31-32`, `camera/viewport.rs:44`, `frame/upload/mod.rs:5-6`, `lib.rs:56`; the missing goldens generator is named at `tests/fixtures/deckgl_ortho_goldens.json:3`. Cleanup: dead exports (`camera/ortho/camera/mod.rs`, `frame/cull.rs:23-95` getters, `lifecycle.rs:45-51`, 8 readback checks and `readback_rgba`), duplicated helpers (`js_round`, three sleep helpers, probe constants), retired-file references and phase comments across `frame/`, `camera/`, `lib.rs`, `Cargo.toml:21-27`; developer-tools comments naming nonexistent modules (`blueprint/bvh/construction.rs:3,412`, `blueprint/ingest.rs:5`, `map_verification/labels.rs:4`, `world_export_pipeline/vegetation_density.rs:2,21`). Limitation: no native test validates `doll.wgsl` |
| V4.4 | 4 · W4.4 | Wave verifier over W4.4's writers | dropped (operator direction: no verifier per wave) | — | — |
| R10.2 | 4 · W4.5 | README writer: 13 folders — `apps/website/map-engine/src/doll` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈274k tokens) | this commit | 13 rewritten (all boilerplate before). Next ticket batch (TF8): `contracts_v2/definitions/map-object-instance.schema.json:9` names the nonexistent `crates/map-engine-core/…/pod.rs` (the real file is `io/pod/instance.rs:19`); limitations: nothing checks that `REGION_KEYS` (`doll/scene/instances.rs:12`) keeps the frontend's `RAIL_REGIONS` order; `decode_tbdd` (`io/density/tbdd.rs:123`) never checks the version (there is no `TBDD_VERSION` constant); nothing ties `INSTANCE_STRIDE`/`UNIFORM_SIZE` (`doll/renderer/pack.rs:7`, `lifecycle_1.rs:17`) to `shaders/doll.wgsl`; possible defect (low): `DollEngine::create` (`lifecycle_1.rs:267-268`) starts its CSS size at the device pixels, so picks are off until the first `resize`; cleanup: broken intra-doc links (`io/containers/tbds.rs:18,75`, `io/pod/instance.rs:13,44`), stale comments (`io/mod.rs:4-6`, `io/density/tbdd.rs:81,122`, `doll/scene/instances.rs:11`, `io/containers/tbdb.rs:99-101`, `doll/interaction/picking.rs:67`), re-export facades that only tests import (`doll/renderer/engine/mod.rs:7` is imported by nothing), the undocumented `ContainerHeader`, the repeated `cfg(feature = "io")`, and the TBDD constants duplicated in `tools_v2/developer-tools/src/world_export_pipeline/vegetation_density.rs:23-25` |
| R10.3 | 4 · W4.5 | README writer: 19 folders — `apps/website/map-engine/src/editing` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈440k tokens) | this commit | 12 new (`editing/**`, `overlay/`), 7 rewritten (`overlay/symbology/**`). F09 input: `documentation_v2/website/map-engine/` should cover the editing layer (host, hosted commands, undo) and draft persistence (classify, adopt, snapshot pair), then these READMEs link it. Next ticket batch (TF9): possible defect: `commit_document_edit` and composition edits run the post-change tail (an undo step) even when nothing changed (`editing/hosted_commands/document_edit.rs:14-19`, `composition_library.rs:74-80`, against `hosted_commands/mod.rs:7-8`); limitation: line-of-sight walks and the frozen-camera clamp are fixed to Everon (`terrain_survey::everon_manifest`, `tools/selection/gesture.rs` `TERRAIN_W`/`TERRAIN_H`); comment contradictions: `viewshed_scheduler/host.rs:4-5` against `terrain_lane.rs:73-75`; `overlay/lanes.rs:4-5` against `Grid` ranked 35 (`:201,272`); `atlas/raster.rs:13-14,20`; `commands/export_text.rs:12` (a nonexistent `/compiled` route, as in T-1044); cleanup: dead `terrain_survey::compute_viewshed_for`, the test-only viewshed wash lane and readouts, `instances/slots/mod.rs` re-exports, unused `lod.rs:22` `FOREST_FILL_MAX_ZOOM`; stale comments (`map_engine_core` in `capture.rs:209`, `export_text.rs:108,153`, `pick.rs`, `marquee.rs`; `wash_palette.rs:14`; ticket ids in `capture.rs:133-157`, `projection.rs:37,40`, `wash_palette.rs:9,52`, `gesture.rs`, `marquee.rs`, `pick.rs`, `export_text.rs`; misplaced doc lines `object_wash.rs:178`, `object_verdict.rs:16`; truncated `chain.rs:136-142`; dangling banners `tool_mode.rs:78`, `leg.rs`; placeholder `//!` headers across `overlay/**`); ticket-named tests `overlay/tests/tests/draw_order_t*.rs` (T-1042). The id and adopt findings are T-1051 and T-1050 |
| R11.1 | 4 · W4.5 | README writer: 16 folders — `apps` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈354k tokens) | this commit | 2 rewritten roots (`apps/website/`, `assets_v2/`), `apps/README.md` new, graphics engine 11 new and 2 rewritten, `shared/` new. It verified that the map engine names the graphics engine's `frame`, `device`, `pipeline` and `loop` only in `frame/mod.rs` and `frame/pump.rs`, but `world/`, `overlay/`, `spatial/` and `diagnostics/` import `draw`, `layout` and `text` directly; no frontend crate depends on it. G1 input: `CLAUDE.md:168-171` graphics-engine folders are wrong (real: `device/ draw/ frame/ layout/ loop/ pipeline/ shaders/ text/`). F13 input: `runbooks/local_development.md:354,369` LFS figures are wrong (about 2,000 objects; chunks are in LFS per `.gitattributes:20`). F09 input: carry the engine-layers rule table into `engine_boundary_rules.md`. P4f: `assets_v2/README.md` needs an `equipment/` line once another session commits that folder. Next ticket batch: ticket/history comments (`loop/pump.rs:99`, `layout/mod.rs:8`, `frame/atlas.rs:7`, `pipeline/mod.rs:32`, `draw/grid.rs:20`, `shared/is_http_url_cases.rs:1,5,7,11` stale paths); `engine_layer_rules.rs:47,210` match a `text::gpu` that no longer exists; limitation: rule 2 checks five map nouns while `BuildingInstance`, `create_building_pipeline`, `create_forest_density_pipeline`, `create_map_shader`, `grid_lines(over_hillshade)`, the `slot-icon-lane` label (`device/buffers/pool.rs:123`) and `text/layout.rs:88` name map concepts; cleanup: unused `_tint`/`char_m` (`text/layout.rs:61,101`), `draw/cull/compute.rs:32` rebuilds its readback flag, `draw/instances.rs:62` says a 28-entry table (it is 32), `draw/cull/oracle.rs:11` links a nonexistent `crate::scene`, and boilerplate `//!` headers in about 15 files. The staging deploy that never starts the API is T-1006 |
| R11.2 | 4 · W4.5 | README writer: 18 folders — `assets_v2/glyphs` + 2 more unit(s) (see `refactor_phase4_slices.tsv`) | done (R11.2 stopped at the usage limit twice; R11.2r ≈255k tokens) | this commit | 14 rewritten, 4 new; `contracts_v2/` written as an area root. Wrong claims dropped: `db seed` imports the catalogs (it is `db registry-import`), a `/api/v1/missions/registry` route (it is `/api/v1/registry`), byte-identical loadout round trips (values only), wrong definition and fixture counts. F12 inputs: `documentation_v2/contracts_v2/definitions/bridge_messages.md:21` broken `samples/` link, plus "Draft"/"Phase 0.2" wording; the storage_spec design could move to a feature doc; the schema evolution policy has no live home (only the archived relocation plan). Next ticket batch (TF9): bug: `map-engine/…/flatten/tests/cases_3.rs:434` climbs two levels instead of three (`apps/contracts_v2`); may be intended: `TBD_RegistryItemsExportPlugin.c:26` plugin attribute commented out; cleanup: `prefab-classify.json:3` names retired `tbd-tools`, T-244 and t090 specs; `.github/workflows/schema.yml:1` retired name; mixed `$id` hosts (`tbd.example`/`tbd.local`); `bridge-messages.schema.json` history description; `TBD_RadioBridgeStub.c:1-30` history header. T-1049, T-1056, T-1066 already cover the rest |
| V4.5 | 4 · W4.5 | Wave verifier over W4.5's writers | dropped (operator direction: no verifier per wave) | — | — |
| R11.3 | 4 · W4.6 | README writer: 16 folders — `assets_v2/terrains` (see `refactor_phase4_slices.tsv`) | done (R11.3 stopped at the usage limit twice; R11.3r ≈259k tokens) | this commit | 13 new, 3 rewritten (root, everon, arland); dropped the old wrong claims: verifiers skip `queued` terrains, nothing hard-codes a file name, one directory per tier, the export flips the registry, "voxelised interiors". F09/F12 input: no live doc covers terrain export and map assets. Next ticket batch (TF9): bug: `task_definitions.rs:200` runs `map build-unified` without `--container-version`, so it writes tbd-sat v2 while `everon/manifest.json:55` says v1 and the same task's `verify-unified` refuses it (`map_assets_root.rs:84-89`); possible defect: `build_world_objects_opt.rs:310` `expect("manifest.objects")` panics on Arland's manifest; cleanup: 3 orphan BLAS files (`LightSwitch_01/02`, `GarbageAmmunition_USSR_01`); retired `terrains.ts` in `terrain_manifest.rs:305,309,333,338` and the schema's `worldBounds` text; the schema's `buildings` description contradicts `occluder_loader.rs:124,221`; `buildings.blas` never read (`occluder_loader.rs:195`); limitations: CI never checks Arland's manifest (`validate_all.rs:171-176`, `task_definitions.rs:339-372`); the terrain contract is hard-coded (`terrain_manifest.rs:229-246`); the create dialog offers Arland with no data (`create_dialog/dialog.rs:160`); the registry `status` is informational |
| R12 | 4 · W4.6 | README writer: 12 folders — `apps/mod/tbd-framework/Scripts/Game/TBD/Systems` (see `refactor_phase4_slices.tsv`) | done (first run stopped at the usage limit before writing; relaunch ≈393k tokens) | this commit | 12 rewritten; `mod compile` exit 0; no .meta/rdb churn. Eleven wrong claims dropped (e.g. the equip harness, the dynamic spawner, the weather preset applied, scatter spawning props, zone RPC warnings, wrong class names). F10 inputs: `tactical_marker_palette_specification.md:6,87` names a nonexistent `TBD_MarkerService.c` (the class is `Markers/TBD_MarkerData.c:51`); no feature doc covers the Systems runtime (deployment authorization, one life, play area, triggers, radio); `mod_design.md`'s H1 "TBD_MOD_DESIGN". Next ticket batch (TF10): possible defect: `Markers/TBD_MarkerComponent.c:42` `GetWorkspace()` guard likely starts the client poll on dedicated servers (contradicts `TBD_RadioComponent.c:44-51`); limitations: `TBD_MissionParams.c:132,168` `Get`/`Has` have no caller; AI defaults only for waypoint-armed groups (`TBD_GroupState.c:286`); late joiners never hear armed emitters (`TBD_AudioEmitter.c:350-366`); missing `@route` tags (`TBD_DeployedMission.c:184`, `TBD_MissionArtifactVerification.c:86`, `TBD_RosterLoader.c:249`, `TBD_DeploymentRequestQueue.c:342`, `TBD_DeploymentEndQueue.c:195`) and `@contract` tags on JSON structs (audio, weather, spawner, roster, deployment); cleanup: uncalled radio client/bridge methods, stale comments (`TBD_MissionParams.c:10`, `TBD_RadioClient.c:7`, `:250` citations), `TBD_LoadoutEquipHelper.c` declares `TBD_LoadoutApplication`, ticket/history comments, Law 7 sizes (`TBD_SpawnManager.c` 4231 lines and four more over 1500) |
| R13 | 4 · W4.6 | README writer: 19 folders — `apps/mod/tbd-framework/Scripts/Game/TBD/API` + 2 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈430k tokens) | this commit | 9 new, 10 rewritten; `mod compile` exit 0; no .meta/rdb churn; all 15 mod REST call sites match api_v2 routes. Wrong claims dropped (the `vehicle:*` alias, a `POST_GAME` stage, the spectator referee mode, a persistent audit trail, "Ready not yet wired", and more). F10 inputs: SHA-256 performance measurement, the lobby/briefing/players/mission catalog adapter plans, kit sheet `K` records; the screens run on mock catalogs (Open work). R14.1 input: `tbd-framework/README.md` registry-override claim (`TBD_Registry.c:27-28`: used only when the mod file is missing). Next ticket batch (TF10): limitation: no script calls `TBD_LobbyClient.Request/Claim/Release/Deploy` (`TBD_LobbyClient.c:146-189`), and the Lobby, Briefing, Players and MissionSelector screens read mock catalogs; possible defect: `TBD_MissionBrowser.c:84` and `UI/TBD_MissionSelectorScreen.c:17` name F6 for the selector, but F6 is `TBD_MissionCycle`; cleanup: `TBD_RegistryPocComponent` attached by no prefab; missing `@route` (`TBD_RuntimeSession.c:251,262`, `TBD_RuntimeSessionClosing.c:81`, `TBD_FleetCommandPoller.c:137`, `TBD_FleetCommandExecution.c:185`, `TBD_FleetLoadMissionAction.c:53`, `TBD_DeployableMissionList.c:56`, `TBD_MissionDeploymentRelay.c:69`) and `@contract` tags on every JSON struct; missing `@authority` (Registry, Log, BackendConfig, Sha256, Spectator, PlayersCatalog); stale comments (`TBD_SpectatorHost.c:17` rate, `api_v2/src/handlers` paths in `TBD_PlayerIdentity.c:9,14`, `TBD_IdentityLink.c:6-9`, `TBD_ResultsReporter.c:28,48-50`), ticket and history comments |
| R14.1 | 4 · W4.6 | README writer: 14 folders — `apps/mod (folder only)` + 6 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈371k tokens) | this commit | 7 new, 7 rewritten; `mod compile` exit 0; the four known link breaks fixed; all 19 tbd-emcp handler-to-MCP-tool mappings verified. Wrong claims dropped: tbd-export depends on the framework (`tbd-export/addon.gproj:4-8`), 128 players (64), the `GAME_SERVER_TOKEN` service token (`SERVICE_TOKEN`), `mod dev-server` as a launcher, `mod test-phase1-api`, gamepad bindings. Heads-up: each addon's rdb lists README.md files, so Workbench will likely rewrite the rdbs when it next opens an addon. F10/F11/F14 inputs: whether standalone Action/ActionContext `.conf` files load without `InputManagerSettings`; an addon's `MenuConfigs` replaces vanilla's list (only in `T-181.25.toml:9`); Steam app id 1874900 vs 1890870 (`debug/direct_join.rs:98`, `compile/execution.rs:94,132`). Next ticket batch (TF10): possible defects: `TBD_MissionBrowser.c:74` arms its input context once (contexts expire per frame, `TBD_SpectatorCamera.c:226-228`); `TBD_MissionSelector.conf.meta:5-15` uses `LayoutResourceClass` for non-PC platforms; cleanup: F6 wording (`TBD_MissionBrowser.c:85,89`), nonexistent `chimeraMenus.conf` cited (`TBD_MenuStack.c:23`, `TBD_ShellScreen.c:24`), stale `setup server-profile` checklist (`server_profile.rs:117-118`), "override" comments (`server_profile.rs:91`, `mission_test.rs:103`), tbd-export-depends-on-framework comments (`development_bootstrap.rs:4-5,72,131`, `compile_help.txt:46`, `compile/execution.rs:384`, `report_compile_errors.rs:158`), unpinned `npx -y enfusion-mcp` with absolute paths in `apps/mod/.mcp.json:5-9` and `.cursor/mcp.json`; licence: `Data/registry.json:1382,1762` two upstream-only GUIDs fail `verify no-crf-leak` by design; vendored `EMCP_WB_ModifyEntity.c` 582 lines; `EMCP_WB_Layers.c:4` lists an unimplemented action; decide: should the compile gate enforce that no addon depends on `TBD_Framework` |
| V4.6 | 4 · W4.6 | Wave verifier over W4.6's writers | dropped (operator direction: no verifier per wave) | — | — |
| R14.2 | 4 · W4.7 | README writer: 15 folders — `apps/mod/tbd-framework/Scripts` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈456k tokens — past the 400k hard stop) | this commit | 5 new, 10 rewritten; `mod compile` exit 0; coverage over all 46 Scripts/worlds folders clean. Wrong claims dropped: safe start locks vehicles, the INACTIVE/ACTIVE/CONTESTED/COMPLETED task states, objectives feed the win evaluator, task HUD elements, a lobby client calling `Set()`, the Orchestrator plan. P4f leftovers: the Systems READMEs say "sources stay ASCII" (e.g. `Systems/README.md`, `Systems/Zones/README.md`); it must read "lines added stay ASCII" (`TBD_PlayAreaComponent.c` has 34 non-ASCII lines). G1 input: the CLAUDE.md atlas says tbd-export depends on the framework (`tbd-export/addon.gproj:5-8`). F10 inputs: `documentation_v2/mod/tbd-framework/UI/README.md` (bare-path fallbacks exist, `TBD_UILayouts.c:244-263`; tbd-export dependency; stale Scripts tree); `objective_capture_hud_specification.md:6` layout path (`UI/layouts/Hud/`); the safe start HUD spec against the code's pop-ups and chat (`TBD_SafestartManager.c:1042-1080`); the lobby, mission and briefing specs describe live data but only mocks render. F15 inputs: `two_client_playtest/README.md:1326` (the dev world places four entities), `:1331` (`Systems/Radio/`). F11 input: `documentation_v2/mod/README.md` hard-codes 192.168.0.140 and cites tickets. Next ticket batch (TF10): possible defects: extraction/VIP and trigger `end_mission` end the round without reason or winner, so the banner can read "admin" (`TBD_WinConditionEvaluator.c:531`, `TBD_TriggerRuntime.c:2017`, `TBD_FrameworkManager.c:796-838`); the false "nothing will end the round" banner (`TBD_ObjectivesComponent.c:752-767`); limitations: no caller for the four catalogs' `Set()`; per-second ungated Reliable task-HUD RPCs (`TBD_TaskStateMachine.c:640-647`); dead code (`BuildBoardForPlayer`, `IsNightVisionAllowed`, `TBD_RequestObjectiveHud`); missing `@replicated` on five `RplProp` fields (`TBD_FrameworkManager.c:300-321`) and missing `@authority` on stage and task methods; stale comments (`Backend/TBD_MissionLoader.c`, `chimeraMenus.conf`, `world-boot.sh`, `golden-missions/`, the chat-consumer note); `TBD_UIIcons` pins no icon GUIDs (`:134-136`); nine scripts over 500 lines (the file-length gate covers Rust only) |
| R14.3 | 4 · W4.7 | README writer: 16 folders — `apps/mod/tbd-framework/UI` (see `refactor_phase4_slices.tsv`) | done (≈391k tokens) | this commit | 4 new, 12 rewritten; all 68 `.meta` GUIDs match `TBD_UILayouts`; wrong claims fixed (shell line budget, the briefing tab strip, `FontProperties` everywhere, pinned icon GUIDs). P4f leftovers: the Pause README links the Discord identity link spec, but linking is a chat command with no layout, so drop the link; the UI Common/Core script READMEs could carry the handler APIs this writer dropped from `layouts/Common` (`SetItems`, `Mount` signatures, the 30 Hz scroll-bar tick, the shrink-wrap rule). F10 inputs (full list `<scratchpad>/logs/R14.3/phase5_spec_disagreements.md`): the UI index (`:7,37-58,85-88`), lobby, mission selection, briefing (4 tabs and 10 topics, not 6 and 11), end screen, debrief, objective HUD, safe start and play area warning layouts that do not exist, the marker palette, identity link as a chat command, spectator, in-game menu. Next ticket batch (TF11): bug: `TBD_SessionTopBar.c:113` loads icon key `group`, which does not exist, so the count icon always hides; cleanup: 38 icon GUIDs never pinned (`TBD_UIIcons.c:125-127`), seven unused icons, unused vertical `TBD_TabStrip` mode, `TBD_UILayouts.c:1-31` history header and `:177-178` orphan comment, `TBD_UITheme.c` 622 lines; limitation: `VoiceDock` in `TBD_LobbyFactionList.layout` has no script user |
| R15a | 4 · W4.7 | README writer: the tbd-export folders the other session is not changing — Missions, Prefabs, Scripts/Game, MapExport (folder only) and its Core, Locations, Plugins, Registry, Objects, worlds | done (≈378k tokens) | this commit | 18 new; `mod compile` exit 0; the only live MapExport entry point is `EMCP_WB_TbdBlueprint` through `mcp wbcall`. F11 inputs: `pending_merge/F11/documentation_v2__mod__tbd_export__readme.md` names a nonexistent `TBD_TerrainExportPlugin` and claims building collision meshes; the full-export → `copy-export-profile` → `export-terrain` runbook and "no registered entry point" (T-1081) need a feature doc. F10/F15 inputs: `documentation_v2/mod/tbd-framework/UI/README.md:107` and `pending_merge/F15/CLAUDE-CODE-START.md:24` say tbd-export depends on the framework. Next ticket batch (TF13): possible defects: `TBD_MapExportObjects.c:165` overwrites `vegetation/vegetation_meta.json`; Fences/Bridges/Aviation/Powerlines/Anchors sweeps record cell-crossing entities twice (e.g. `TBD_MapExportFences.c:74`); heuristic building blueprints can replace measured ones (`TBD_MapExportBuildings.c:178`, `TBD_BuildingTraceExtract.c:122`); the anchors `verification.json` shape does not match `terrain-anchors.schema.json`; `Missions/TBD_Export_Everon.conf` has no tracked `.meta`; limitations: hard-coded arsenal list (`TBD_MapExportArsenal.c:202-252`), no-op props export, the road export component hard-codes map name, world size and an empty material, and runtime and Workbench road exports share file names; cleanup: unused `DeriveMapName`, `TBD_ExportPaths`/`BuildPath`, plugin fields; stale headers and output lists; ticket ids (`TBD_WorldFullExportPlugin.c`, `TBD_WorldTraceParity.c`, `EMCP_WB_TbdBlueprint.c`) |
| R15.1 | 4 · W4.7 | README writer: 16 folders — `apps/mod/tbd-export (folder only)` + 11 more unit(s) (see `refactor_phase4_slices.tsv`) | re-sliced: stable folders done by R15a; the in-flux folders belong to the other session (operator); the three root READMEs wait for its commit (R15r) | — | — |
| R15.2 | 4 · W4.7 | README writer: 16 folders — `apps/mod/tbd-export/Scripts/WorkbenchGame/EquipmentExport` (see `refactor_phase4_slices.tsv`) | re-sliced: stable folders done by R15a; the in-flux folders belong to the other session (operator); the three root READMEs wait for its commit (R15r) | — | — |
| V4.7 | 4 · W4.7 | Wave verifier over W4.7's writers | dropped (operator direction: no verifier per wave) | — | — |
| R15.3 | 4 · W4.8 | README writer: 14 folders — `apps/mod/tbd-export/Scripts/WorkbenchGame/EquipmentVehicleExport` + 2 more unit(s) (see `refactor_phase4_slices.tsv`) | re-sliced: stable folders done by R15a; the in-flux folders belong to the other session (operator); the three root READMEs wait for its commit (R15r) | — | — |
| R15.4 | 4 · W4.8 | README writer: 18 folders — `apps/mod/tbd-export/Scripts/WorkbenchGame/MapExport/Terrain` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈383k tokens) | this commit | 18 new; `mod compile` exit 0; every MapExport `[WorkbenchPluginAttribute]` is commented out, so nothing runs these exporters (T-1081). R15.1 input: the old `apps/mod/tbd-export/README.md` "Plugins,TBD,… in Workbench" claim is false; R15.3 input: `terrain_export.rs:69-72` names the unregistered "Export TBD World Objects (full)" menu entry (`Objects/TBD_WorldFullExportPlugin.c:19`). R11.3 area / P4f leftover: `assets_v2/terrains/everon/dem/README.md:18` says DEM row 0 is north, but the exporter writes row 0 at z = 0 (`TBD_MapExportDEM.c:128-130`, `map-engine …/dem/sampling.rs:30`). F11 input: a map-export feature doc under `documentation_v2/mod/tbd-export/` (which exporters are live, the missing profile-to-scratch staging step, DEM orientation). Next ticket batch (TF11): possible defects: the water export cannot reach `map water` (file names `TBD_MapExportWater.c:94-97` vs `inland_water_archive.rs:61-67`; river fields `TBD_MapExportRivers.c:399,412` vs `reduce_depth.rs:156,177`); `water/lakes.json` written twice (`TBD_MapExportWater.c:96`, `TBD_MapExportLakes.c:85`); asphalt highways land in both road files (`TBD_MapExportPavedRoads.c:266-270`); unset-width roads pass the runway test (`TBD_MapExportRunways.c:187`); spline points unrotated in rivers/dirt/tracks/paths/runways; splines emitted once per matching entity; DEM export reports success when its meta file fails (`TBD_MapExportDEM.c:176-177`); limitation: lakes and ponds write fallback area/depth as measured; stale comments: `dem_elevation.rs:4` row-0 claim, `TBD_MapExportWater.c:9-12`, `TBD_SatelliteExportPlugin.c:5`, `TBD_VegetationExportPlugin.c:4-7`, `TBD_MapExportRoads.c:375`, `export_profile.rs:49` (nonexistent plugin); cleanup: unused `WORLD_PATH` |
| R16 | 4 · W4.8 | README writer: 19 folders — `tools_v2/xtask/src/commands/mod_ops` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈434k tokens — past the 400k hard stop) | this commit | 18 new, `mod_ops` rewritten; all 20 website API routes the mod client calls exist. The committed view has 18 `mod` subcommands. The other session's uncommitted `equipment_gameplay/` and two `equipment_vehicle_export` files need Contents lines in `mod_ops/` and `equipment_vehicle_export/` when that session commits (its commit, or P4f if it has landed by then). `tooling_prose_rules` fails only on committed `mod_ops/equipment_vehicle_export/legacy_archive.rs` and its tests (the word "legacy"; the baseline red, the other session's code). F15/F16 inputs: `mod_slice_workflow` (the `mod wave` gate steps, the `land` order, slice-worktree guards and oracle lanes), `factory_waves` (the gate table in `wave_execution/gate/README.md`, `wave --close` spelling, `land --bookkeeping`, `verified`, the run-main stamp, `reclaim` flags), `two_client_playtest` (`mod playtest` flags, 180 s confirmation, credential revoke, stale Workshop copy gate). Next ticket batch (TF11): bugs: `platform/wave_execution/migrate/gate_db_migrate_claim_body.rs:23` defaults to a nonexistent `0016_backfill_pre_t326_…` migration name; `mod dev-bootstrap --api` starts container `tbdevent-postgres` and runs `npm run dev` in `api_v2` (`development_bootstrap.rs:223-235`; the same container name in `db/milestone_announcement.rs:143,208`); possible defects: the mod wave gate's `enf::` test filter may match zero tests (`developer-tools/src/lib.rs:9` is `enfusion_tooling`); `platform/preflight/execution.rs:42` suggests `make clean-targets` with no Makefile; cleanup: three more host-bridge copies (`compile_host.rs`, `world_boot/boot_environment.rs:27-60`, `platform/preflight/ok.rs:17-50`); `slice_worktree/tests.rs` outside `tests/`; `development_bootstrap.rs:273` personal HOME fallback; `slice_worktree/git_plain.rs` hard-codes a Co-Authored-By line; wording contradictions (`wave_execution.rs:54`, `land.rs:13`, `platform/cli.rs:24-27`, `compile_help.txt:46`, `mission_test.rs:103`) |
| R17.1 | 4 · W4.8 | README writer: 16 folders — `tools_v2/xtask (folder only)` + 10 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈359k tokens) | this commit | 12 new, 4 rewritten; `mcp selftest` exit 0; `tooling_prose_rules` fails only on the baseline `legacy_archive.rs` (other session). F13/F14 inputs: `game_server_staging/README.md:422` (staging writes its own unit, `payloads.rs:89-113`) and `:813` (`-a2sPort 2001` vs 17777); the local_development, website_deployment and testing_and_ci facts in `<scratchpad>/logs/R17.1/handoff.md` (e.g. `mk reclaim-target-ci --dry-run` deletes; exit 79/80 without `TBD_SSH_HOST`/`TBD_REMOTE_DIR`; linger for the backup timers). Next ticket batch (TF11): possible defects: `mk reclaim-target-ci --dry-run` deletes for real (`build/recipes/execution.rs:34-43`); a missing MCP fixture reads as an empty transcript (`mcp/call_selftest.rs:449`); staging errors cite nonexistent `deploy.env` lines (`staging/config.rs:201-208`); relative `Documentation=file://` in the backup timers; `ci-local-leptos` clippy without `-D warnings` (`task_definitions.rs:449`); limitation: the `ai guard` Bash rule checks only the first segment (`guards.rs:162`); cleanup: IP and personal paths still in HEAD (`deploy.env.example:18,22-26,63-64`, `Caddyfile.website:9,33`, backup units, `deploy/website.rs:13`, `staging/config.rs:237,261`: T-1010); the unread `tbd-reforger.service` template (decide: delete it or render from it); misleading names `shell_word.rs`, `split_cmd.rs`, `build/tests/recipes.rs`; retired Makefile/bash narration and codes (`task_runner.rs:1-80`, `recipes.rs:1-38`, `fetch/cli.rs:6`); wrong counts or tests cited (`recipes.rs:3`, `task_runner.rs:36-40`, `shell_word.rs:152`, `task_definitions.rs:377`); `durable_ratelimit.rs:14`; `db --help` says `make db-up` (T-1012) |
| V4.8 | 4 · W4.8 | Wave verifier over W4.8's writers | dropped (operator direction: no verifier per wave) | — | — |
| R17.2 | 4 · W4.9 | README writer: 16 folders — `tools_v2/xtask/src/commands/db` + 6 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈470k tokens — past the 400k hard stop) | this commit | 13 new, 3 rewritten; `mcp selftest` 20/20; `deploy website --dry-run` checked against a scratch deploy.env. P4f leftover: `tools_v2/xtask/deploy/README.md:39-41` (R17.1's) says `deploy website` exits 79/80; the code exits 1 (`website.rs:78-85,408-417`), and the message cites deploy.env "line 79/80". F13/F14 inputs in `<scratchpad>/logs/R17.2/handoff.md`: database_operations (db and deploy db commands, scratch allow-list, five dump checks, drill boot audit, checksum repair), website_deployment (dry-run step list, exit 1, the `tbd-website-api` state folder, remote repair with `--force`, the "Phase D" name), game_server_staging (settings checks, `--verify-boot` needs `TBD_ADDONS_STAGING`), enfusion_mcp_tooling (socket/lock/pid/log paths, entry-point tiers, exit codes). Next ticket batch (TF11): possible defects: rust-ci still runs the fixed `rust_it` recipe (`ci/task_definitions.rs:462`, `shell_word.rs:288`) while `db test-it` uses a random database (`test_it.rs:55-64`), and `db test-it --help`, `api_v2/tests/common/database.rs:39,87,255` and `ci.yml:47-48` say `rust_it`; milestone title 22 vs body 21 August (`db/milestone_announcement.rs:39,42`); limitations: server-side `--force` repoints any drifted checksum (`deploy/website/remote_steps.rs:59`); `direct_join.rs:29,33` hard-codes a LAN address and a profile path; remote-logs env-over-deploy.env precedence is the reverse of staging (`remote_logs/execution.rs:307-318`, may be intended); `world` output only at run end (`map/terrain_export.rs:167`); cleanup: `db --help` lists a nonexistent `migrate` (`cli/mod.rs:65`); wrong allow-list citations (`database_operations/execution.rs:396`, `db/operations.rs:79`); `map/terrain_export.rs:9-10` cites a missing spec; `debug/cli.rs:47` help wording; misleading file names (`read_addon_guid.rs`, `ssh_argv.rs`, `sha384_hex.rs`, `shell_quote.rs`); `ssh_argv.rs:26` excludes a nonexistent `apps/mod/Tbd_framework/`; dead `Paths` fields (`direct_join.rs:46-53`); `verify_dump.rs:151` wording; `gen font-table` lives in `repository_access.rs:116` and `graphics-engine/src/text/font.rs` lacks its generated banner; the make-parity harness without a Makefile |
| R18.1 | 4 · W4.9 | README writer: 16 folders — `tools_v2/xtask/src/verifications (folder only)` + 7 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈398k tokens) | this commit | 9 new, 7 rewritten; the engine-layers gate runs 8 rules (1, 2, 3a, 3b, 4, 5, 6, 7), confirmed live; `documentation/README.md` documents the three gates, `--with-untracked`, the exemptions and that no CI step runs them yet. P4f leftover: `standards/readme_standard.md:92-94` says untracked files are invisible to the gate; add the `--with-untracked` sentence, since the standard claims to quote the gate's grammar. F17 input: `standards/commit_checklist.md:9` states the retired Cursor doc-ownership split. F09 input: the per-rule table in `verifications/architecture/README.md` (subjects, matchers, pins 8 and 3+2, ratchet semantics, exit 2 on a missing root; rules 5 and 6 included). F13 input: which gates ci-local, the wave gate and the `language-gates`/`mod-gates-hosted` jobs run. Next ticket batch (TF11): cleanup: the engine-layers rule lists omit 5 and 6 (`commands/verify/cli.rs:91`, `ci/task_definitions.rs:293`, `ci.yml:261`); `editor_orbat_coherency/source_audit.rs:87` "grep exited"; dead narrow check `ci/schema_parity/source_audit.rs:135`; `faction_library_seeds/extract_fn_body.rs` name hides its contents; hard-coded API paths outside the layout module (`database/sql_deserialization.rs:45`, route_tags, engine and orbat constants); history in `route_tags.rs:13,17,72`; may be intended: every Markdown file under `apps/` and `tools_v2/` feeds the api-readiness fingerprint, so README commits invalidate API evidence (`api_readiness/fingerprint.rs`); the wave gate's `VERIFY_STEPS` (`gate.rs:59`) has no engine-layers row. `verify route-tags` exits 1 on the other session's untracked equipment-data handlers |
| R18.2 | 4 · W4.9 | README writer: 17 folders — `tools_v2/xtask/src/verifications/language_bans` + 10 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈483k tokens — past the 400k hard stop) | this commit | 8 new, 9 rewritten; `ticket check --strict` exit 0; `verify` 25 verbs, `ticket` 27, `schema` 16. P4f leftover: the verifications root README (R18.1's) lists `language_bans` as holding "CI shell policy" (ci-shell dispatches to `verifications/ci`, `verify/dispatch.rs:24`). F16/F17 inputs in `<scratchpad>/logs/R18.2/handoff.md`: ticket_run_pipeline (`ticket run` → `platform slice-run`, executor `claude-code`, exit 1 with none ready), factory_waves (`ship --no-repack` then one `wave repack`, `--reserve`, `slice-collisions`, the width rule), ticket_identifiers (default plan path), coding_standards (language-ban table, file-length roots). Next ticket batch (TF12): bug: `verify ui-layouts` walks only the top level of `UI/layouts/` (`ui_layouts.rs:237-241`) while all 65 layouts sit in subfolders, so it always exits 1 and breaks the mod wave gate; possible defect: `no-crf-leak` reports 6 hits, not 2: the two registry GUIDs, `@CRF_Framework` at `TBD_MissionSelectorData.c:65` and `TBD_MissionSelectorMock.c:146`, and a font GUID and a backpack GUID that are probably false positives (the comment filter misses trailing `//!<`); stale header lines 1367/1747 (`upstream_code_leaks.rs:14`) (T-1097 notes); limitation: `client_addons.rs:95` hard-codes a LAN IP and `mcp_game_root.rs:25,37` a personal home path; cleanup: "GO-7 retired" (`contract_citations.rs:188`) vs `verify/cli.rs:67`; the "I1-I7" help (`schema/cli.rs:62`); `object_type_inventory.rs:88-91`; `checks.rs:1-10`; `object_registry_aliases.rs:9`; `validate_file.rs:4`; `cargo_target_directory.rs:1-8` (Makefile); `source_audit.rs` `/tmp/tmp.t437` prefix; `strip_c_comments.rs` also holds the RED perturbations; `ticket run` hint names "Composer 2.5" and `T-0xx` (`cli/batch.rs`). The four `make` targets of the mod wave gate are the Phase 0 ticket |
| R19 | 4 · W4.9 | README writer: 20 folders — `tools_v2/developer-tools/src/blueprint` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (R19 stopped at the usage limit; R19r ≈313k tokens) | this commit | 11 new, 9 rewritten. Other-session follow-up: its untracked `browser_testing/equipment_data_viewer/` needs a README and a Contents line in `browser_testing/README.md` when it lands (docs ship with their code); P4f adds the line if the folder is committed by then. F14 inputs: `editor_gates.md` (`:28-30` font cache path and `TBD_GATE_FONT_CACHE`, `:74` no cgroup check, `:93` font probe is a 5 s log watch, `:61` satellite bundle 152 MB, `:158-163` `render-check --api-proxy`, `:168-171` v-suite gets the font cache, `:40` `ci ci-chrome`, `:47` "DOM never stabilized", `../../` links and T-ids); `editor_capture.md` (`:17` personal path, `:44` stale link text, `:23-25` history). Next ticket batch (TF12): possible defects: `gate v-suite verify --only <unknown>` reports 0/0 and exits 0 (`routes.rs:388-392`); `unresolved` not counted in the manifest tally (`assemble_manifest.rs:33-38`, may be intended); limitation: `zoomsweep` hard-codes ports and has no blank-canvas floor; cleanup: `smoke` help omits six smokes (`cli.rs:47-48`); `--all-layers` missing from usage (`archive_command.rs:163`); `archive_writer.rs:7,9` dead module paths; comment contradictions (`cdp.rs:4` lavapipe flags, `editor_smoke_tests.rs:9,36`, `ensure_gate_font_cache.rs:96-100,313`, `mesh_format/u16le.rs:260`); history, ticket and Node-harness comments across browser_testing |
| V4.9 | 4 · W4.9 | Wave verifier over W4.9's writers | dropped (operator direction: no verifier per wave) | — | — |
| R20.1 | 4 · W4.10 | README writer: 16 folders — `tools_v2/developer-tools (folder only)` + 6 more unit(s) (see `refactor_phase4_slices.tsv`) | done (R20.1 stopped at the usage limit; R20.1r ≈334k tokens) | this commit | 5 new, 11 rewritten; `hcargo test -p developer-tools` 265 passed, 4 ignored. P4f leftover: `standards/templates/readme_command_line.md:115`'s sample lists `gate equipment-data-viewer`, a subcommand only in another session's uncommitted changes; the committed `gate` has 8 subcommands. F12 input: no feature doc covers the map raster pipeline or the Enfusion oracle. Next ticket batch (TF12): limitations: `export-height-labels` always reads the Everon DEM (`map_labels/export_height_labels.rs:9`); `verify-cartographic` requires ticket-numbered logs `t152_{0..9}_verify_log.md` (`build_tile_pyramid.rs:151`); the "only everon supported this slice" messages (`aerial_orthophoto/*.rs:7,9,256`); cleanup: history comments (`map_raster_pipeline/mod.rs:1`, `world_export_pipeline/mod.rs:1-2`, `run_modes.rs:14`, `satellite_archive.rs:4-9`); `route_drift.rs:7` says set diffs but compares bytes (`:63`); fixture paths spelled in `dom_oracle/routes.rs:108` and `route_drift.rs:19` outside a layout module; `ci.yml:92` says the mod gate filters `enfusion_tooling::`, the code `enf::`. The map-water container finding is T-1072 |
| R20.2 | 4 · W4.10 | README writer: 11 folders — `tools_v2/developer-tools/src/map_verification` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈351k tokens) | this commit | 8 new, 3 rewritten; `world` and its 18 subcommands checked against `cli.rs`. P4f leftover: `tools_v2/developer-tools/src/README.md` (R20.1's) says xtask reads `vegetation_density`; no xtask use exists. F11/F12 inputs: no feature doc covers the world export pipeline or the map verification gates; the export runbook (Workbench → `copy-export-profile` → `export-terrain` → `verify-phase` → registry bump) exists only as `terrain_export.rs:56-87` output. Next ticket batch (TF12): bugs: `world validate-exports` cannot pass on committed Everon, since it gunzips `cells[].path` (`artifact_integrity.rs:74-75`) while the committed chunk index lists `.bin` paths and the builder writes `.json.gz` (`object_partitioning.rs:413`); decide which side is authoritative (audit plus bug); the density check compares unblurred counts to the blurred canopy (`artifact_integrity.rs:182-183` vs `build_world_objects_opt.rs:72-73`); possible defects: `std::process::exit` in library code kills `verify-phase` E6 (`object_partitioning.rs:79`, `build_world_objects_opt.rs:~55`); `phase_gate` passes P6–P10 that `build-objects` rejects; no plugin writes `TBD_WorldExport_subregion.jsonl` (`export_profile.rs:27`); limitations: `verify-phase` D/F run only for P2 (`verify_phase.rs:11`); E2c scans module roots only; the BLAS gate never lists the folder (orphan `.bvh` unseen); cleanup: misleading file names (`labels/read_json.rs`, `town_labels.rs`, `tempdir.rs`, `may_clear_density_dir.rs`, `redensify_from_committed.rs`, `reclassify/obj_get.rs`), duplicate `js_num` and `REQUIRED_EVERON_TOWNS`, `t935-12-*` temp names, `repo_root` imported from `browser_testing`, `census`/`validate-exports` spawn `cargo run -p xtask`; comment contradictions (`world_export_pipeline/mod.rs:29-31`, `map/cli.rs:111-113`, `object_partitioning.rs:76`) |
| R20.3 | 4 · W4.10 | README writer: 10 folders — `tools_v2/developer-tools/test_fixtures` (see `refactor_phase4_slices.tsv`) | done (≈237k tokens) | this commit | 9 new, 1 rewritten; every fixture test passes; no fixture is in LFS. P4f leftovers: `developer-tools/src/blueprint/bvh/prefab_catalog/README.md:37-40` says `tokenizer_and_block_shapes` is pinned against the `.et` fixtures, but it parses an inline string (`tests/prefab/tests.rs:6-23`); R15.1 input: the old `apps/mod/tbd-export/README.md:18` says `mcp call` where the raw call is `mcp wbcall`. Next ticket batch (TF12): cleanup: `GarbageContainer_01_children.json` read by nothing; a ticket id inside an assertion string (`compound_tests.rs:66`), dated history notes (`compound_tests.rs:9-10`, `world_row/tests.rs:71-72`), ticket ids in `EMCP_WB_TbdBlueprint.c:15` and `TBD_WorldTraceParity.c:1`; limitations: the blueprint golden JSON has no parity test against the shipped Everon copy; the door parity recording's action is unnamed; may be intended: `House_Base.et` and `House_Wood.et` share an ID |
| R21.1 | 4 · W4.10 | README writer: 16 folders — `apps/fleet_host_agent` + 6 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈385k tokens) | this commit | 9 new, 7 rewritten; the approved test edit removes the README-format half of `apps/ticketboard/src/tests/architecture_rules.rs` (HEAD :69-89) and allows `README.md` beside `main.rs` in `src/` (:57), which the required `src/README.md` needs; ticketboard tests (179) and clippy pass; fleet-host-agent tests pass. The launch prompt was wrong about the unit: `tools_v2/xtask/deploy/systemd/fleet-host-agent.service` is tracked and installed by `deploy/staging/host_agent.rs:8` when `TBD_INSTALL_HOST_AGENT=1`. P4f follow-up: `apps/ticketboard/src/tests/README.md:3` says the architecture test enforces "module documentation". F12 inputs: no feature doc covers the ticketboard or the fleet host agent (the fleet safety model and RCON protocol live in READMEs); the tools_v2 root links only archived plans, so a live tools_v2 architecture doc is missing. Next ticket batch (TF13): limitation: CI runs no tests or clippy for `fleet-host-agent` or `ticketboard` (`ci.yml:28-33`, `ci/task_definitions.rs`); cleanup: comments with ticket ids cut out (`ticketboard/src/main.rs:85`, `application/background_loading.rs:12`, `core/ui/mod.rs:9`); the test name `module_roots_and_documentation_describe_the_entire_source_tree` still says documentation; `enfusion_mcp_node_package/.nvmrc` is read by nothing; personal paths in `apps/mod/.cursor/mcp.json:6-11` and `apps/mod/.mcp.json:7-9` (T-1095); may be intended: RCON password minimum measured in bytes by staging (`host_agent.rs:56`) but in characters by the agent (`secret_files.rs`); `node_free_tests.rs:108` synthetic path |
| V4.10 | 4 · W4.10 | Wave verifier over W4.10's writers | dropped (operator direction: no verifier per wave) | — | — |
| R21.2 | 4 · W4.11 | README writer: 16 folders — `apps/ticketboard/src/document_viewer` + 3 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈249k tokens) | this commit | 4 rewritten, 12 new; ticketboard tests 179 passed. The launch brief was wrong: the file watch covers `.ai/tickets/**`, `CLAUDE.md` and the ROADMAP only, not the gap analysis (`repository_status/services/file_watch.rs:128-138`). Next ticket batch (TF13): possible defect: the board does not reload when `ticket sync` rewrites the gap analysis; may be intended: the board load skips the vocabulary and id-to-file checks of `Corpus::load` (`corpus_loading.rs:53`), and the viewer's estimate check leaves out `factor == TOKENS_PER_LOC` (`estimated/validation.rs:37-40`); cleanup: `measured/services.rs:40` `tbd_tickets`, "Mirror of xtask" comments (`measured/models.rs:4,18`, `estimated/models.rs:18`), stripped-ticket-id residue (`projection.rs:158-159`, `background_loading.rs:11-12`, `measured_tables.rs:11,34`), history in `scope.rs:42,45` |
| R21.3 | 4 · W4.11 | README writer: 15 folders — `apps/ticketboard/src/ticket_actions` + 2 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈284k tokens) | this commit | 3 rewritten, 12 new; ticketboard tests 179 passed. Next ticket batch (TF13): possible defect: the Remove dialog's warning list uses the dotted-id prefix while `remove --force` follows `children[]` and `parent` links (`transitions.rs:198` vs `ticket-engine/src/ops/ordering.rs:9-10`); limitation: the ready form omits the plan and ready-tier gates (`mark_ready.rs:64-106`); decide: the viewer's copies of wave-lock, dispatchable and scope-vocabulary logic (`lock_file.rs:13-97`, `wave_projection.rs:58`, `scope_facets.rs:33-73`) versus imports from ticket_engine (audit); cleanup: a ticket id in on-screen text (`detail_sections.rs:200`) and `t9154_` egui ids (T-1042), cut-out comments (`transitions.rs:151`, `detail_sections.rs:199`, `ticket_card.rs:142,196`), `requests.rs:59` names `xtask/src/main.rs`; tests of `ticket_registry` functions placed in `ticket_browser`; `live_lock_parses_verbatim` depends on the committed `wave.lock` |
| R21.4 | 4 · W4.11 | README writer: 16 folders — `tools_v2/ticket-engine` + 1 more unit(s) (see `refactor_phase4_slices.tsv`) | done (≈431k tokens — past the 400k hard stop) | this commit | 8 new, 8 rewritten, plus the exempt `ticket-engine/tests/README.md` (it said "Migrated"). `verification-core` tests 69 passed; `ticket-engine` 234 passed, 1 failed: `corpus_roundtrip_real_tree_byte_identical` on `T-1004.toml`, whose `notes` line our ticket batches appended after `created_at` (see the TF12 amendment). F16/F17 inputs in `<scratchpad>/logs/R21.4/handoff.md`: ticket_run_pipeline (`cmd_run` batch rules, the receipt path, land stamping and `--bookkeeping`), factory_waves (ship → commit → `stamp-sha`, `ship --no-repack`, `--reserve`, lock width, wave numbering), ticket_identifiers (commit-subject and gap-analysis id rules, the plan path). Next ticket batch (TF13): possible defect: neither CI nor the wave gate runs `cargo test -p ticket-engine` or `-p verification-core` (`gate_dispatch.rs:269-283`), which is how the key-order breakage went unseen; bug: broken error-message strings from a lost line continuation (`validation/command.rs:153`, `wave_lock/persistence.rs:68`); cleanup: stale comments (`verification-core/Cargo.toml:1-2`, `model/scope.rs:78`, `commit_subjects.rs:51-52`, `encoding.rs:18`, `store.rs:30`, `vocabulary.rs:19,30`, `wave_lock/model.rs:26,34`, `compiler.rs:55`, `lib.rs:31-33`, `verdict.rs:24-28`, the Python and "Value path" narration in `registry/mod.rs`, `store.rs:5-23`, `mutation_support.rs:17-35`, `references.rs:20`); duplicate `is_parent_id`/`parent_numeric_id`; test-only public code (`run_estimates`, `on_disk_ids`, `corpus_ids`, key sets, `ShippingStatus::load`); xtask hard-codes 30/3600 instead of `DEFAULT_POLL`/`DEFAULT_MAX` |
| V4.11 | 4 · W4.11 | Wave verifier over W4.11's writers | dropped (operator direction: no verifier per wave) | — | — |
| P4f | 4 · close | Closing fix run: the README leftovers recorded in roster rows (V4.3f's and the writers' own) | done (≈211k tokens) | this commit | 14 of 16 items edited across 61 files. The "lines added stay ASCII" wording is in 48 mod READMEs. The UI Common/Core READMEs gain the `Mount`/`SetItems` signatures, the 30 Hz scroll tick and the shrink-wrap rule. The DEM row 0 is now stated as z = 0 (south). The remaining items: deploy exit 1, the `--with-untracked` grammar sentence, `assets_v2/` moved to the data examples, `equipment-data-viewer` dropped from the template sample, the document_host Used by pins, the full paths, the `mission_lookup` role, `vegetation_density` and the `tokenizer_and_block_shapes` claim. Item 13 ("CI shell policy") needed no change. Item 16: none of the other session's equipment folders is committed yet. Follow-up leftovers (P4f2, after R21.2/R21.3): `apps/ticketboard/src/tests/README.md:3`. Code-side for TF13: `mission_lookup.rs:1` "every surface", `dem_elevation.rs:4` row-0 comment. For F17/F18: `documentation_v2/standards/` has no README, `coding_standards/README.md` has no Contents, and 29 link breaks remain in `coding_standards/README.md`, `commit_checklist.md` and `ticket_identifiers.md` |
| P4f2 | 4 · close | Closing fix: `apps/ticketboard/src/tests/README.md:3`, then a repo-wide measurement of the three gates classified by owner | done (≈102k tokens) | this commit | Tests README line 3 now lists what the test enforces. Committed-view gates, zero Phase 4 misses: readme-coverage exit 1, 207 of 1622 checks. documentation_v2 accounts for 179: F10 72, F18 21, F03 21, F02 19, F08 14, F11 11, F04 6, F12 4, F17 4, F09 3, F05/F14/F15/F16 1 each. The other session's tbd-export roots and folders account for 28. link-check exit 1, 215 breaks. Live documentation_v2 docs account for 207: F15 55, F07 48, F17 30, F16 18, F04 15, F02 8, F13 8, F03 6, F12 5, F08 4, F09 4, F05 3, F14 2, F18 1, including `cargo xtask deploy db backup-drill` cited twice in `pending_merge/F13/database_operations.md`. Phase 6 accounts for 8: `.ai/tickets/CLAUDE_CODE_PROMPT.md:14-15`, `.ai/tickets/README.md:53`, `.cursor/rules/application-code-forbidden.mdc:33`, root `README.md:13-14`. Frozen records have 0 breaks. markdown-placement exit 1, 8 live docs over 500 lines: `feature_inventory/README.md` 1799 (F05/F06), `two_client_playtest/README.md` 1421 (F15), `game_server_staging/README.md` 813 (F14), `editor/decisions.md` 637 (F07), `factory_waves/README.md` 608 (F16), `interactions/README.md` 562 (F08), `briefing_specification.md` 555 (F10), `coding_standards/README.md` 546 (F17). Code trees hold no stray Markdown, and `docs/` is empty. Code-side for tickets: `architecture_rules.rs:1,47` still say "documentation" |
| F02 | 5 · order 1 | Doc writer: `pages/{command_center, operations}/**` | done (≈441k tokens — past the 400k hard stop) | this commit | 28 docs: command center (announcements incl. `/announcements/:id`, dashboard, server intel) and operations (schedule, event hub, ORBAT selection `/events/:id/missions/:emid/orbat` new, deployments, leaderboards) with visual reference READMEs; `pending_merge/F02/` emptied (13 sources). P5f leftovers: `apps/website/frontend/src/v2/pages/operations/leaderboards/README.md:3,70` say every row has a dossier button, but only the first place does (`board_table.rs:168-199`). F19 input: `glossary.md:420` says the service record shows aggregate combat figures; it shows none. F04a input sent (message): the frontend README's route links. Open for the operator: T-940.1, T-940.2, T-940.3 and T-940.12 are `ready` but the api_v2 code does them (`waitlist_promotion.rs`, withdrawn/no_show, `event_administration.rs:115-140`, member/guest/open pools). Next ticket batch (TF14): bugs: invented intel lines shown as live (`server_list.rs:129-140`, back since d37663767); leave-of-absence form says the reason is optional but the API requires one (`leave_of_absence.rs:185`, `dto/events.rs:235` vs `leave_requests.rs:32,44,52`); possible defects: raw Discord id as announcement byline (`article_viewer.rs:69,96`); `server_statuses LIMIT 1` unordered (`live_dashboard.rs:187-195`); `next_event` ignores access policy and prefers the viewer's own event (`:57-80`); OFFLINE drawn green (`server_uplink.rs`); ranks restart at 1 inside a search (`leaderboards.rs:96-98`); "No ORBAT slots defined" on a failed fetch (`slotting_selector.rs`); the hub hero can shrink; limitations: the announcements feed loads 20 and never calls `GET /announcements/{id}`; the hub offers Register/Join on locked or closed events; `T-MINUS LIVE NOW` and a `#` modpack link (`hero_countdown.rs`); the schedule fill bar is stale after registering; no pager on schedule, leaderboards, leave queue |
| F03 | 5 · order 1 | Doc writer: `pages/{mission_hub, doctrine_and_info, field_tools}/**` | done (≈440k tokens — past the 400k hard stop) | this commit | 31 live docs (2,133 lines): the mission hub (library, overview, review workspace = the artifact workspace), doctrine and info (wiki, vehicles, modpacks), field tools (mortar), and the debug building viewer and world LOS pages; `pending_merge/F03/` emptied (18 sources). Phase 5 fix leftover (P5f): `apps/website/frontend/src/v2/pages/mission_hub/library/README.md` How it works says writes need authorship only, but the API also requires `mission_maker` (`mission_write_lock.rs:36`). F08 input: the apps README needs a `debug/` line. Open for the operator: T-085 (deferred) asks for a wiki Markdown renderer, and a subset exists. Next ticket batch (TF14): bugs: a demoted author still sees Manage/Edit Armory/reply and gets 403 (`dossier_sheet.rs:102`, `overview/page.rs:96`); a bookmarked mission archived by its author lists but 404s (`mission_library.rs:93-97`); gap: "Share for review" toasts but sends nothing (`dossier_collaboration.rs:24`); question: the review workspace route requires `mission_maker` while the API serves any author (`router.rs:109-114`, `mission_reviews.rs:145`); gaps: modpack form has no size field, Delete no confirmation, Workshop URL and vehicle `profile_image_url` unchecked as http(s); fire-mission save/list skip the event existence and visibility checks (`fire_missions.rs`); stale comments (`server_registry.rs:138` GET only, `weapon_selector.rs:20-21`, "Scenario Creator" in `review_workspace/page.rs:1` and `router.rs:108`, map-engine `coverage_1.rs:1-2`) and code comments claiming authorship-only writes (`dossier_sheet.rs:92-101`, `dossier_body.rs:33-36`, `overview/page.rs:92-95`) |
| F04a | 5 · order 1 | Doc writer: website/frontend/README.md (route table: every route to page folder to feature doc), website/frontend/pages/README.md, the frontend core docs (see `refactor_phase5_slices.tsv`) | done (≈223k tokens) | this commit (its four `git mv` archive renames landed in 822f8af4d) | `website/frontend/README.md` hub (187 lines; route table of 29 routes plus the fallback, each with component, access tier, code folder, feature doc) and `pages/README.md` (85) new; no core docs folder (the in-code core READMEs carry every fact); the frontend ROADMAP, INDEX, TRACKING and docs README archived in `archive/go_and_react_era_design/`; four pending_merge/F04 drafts merged; backlinks in the frontend, v2 and core READMEs. F19 inputs (still open): T-085, T-086, T-087, T-088 (deferred), T-068 (deferred), T-090 (ready). F17 input: `standards/commit_checklist.md:32,48` points at the retired INDEX and uses a `../../` link. F04b input: `app_layout_and_navigation.md:100` "T-086 stub". F08 input: `apps/editor/README.md:5-7` has no Contents and a retired TICKET_LEAD link. P5f leftover: `server_control_page.md` Open work omits T-086 (deferred, open). Next ticket batch (TF14): ticket and React-router history comments in `router.rs:1-4,17,187` and `main.rs:1-5`; "Scenario Creator" at `router.rs:114`; the other session's uncommitted `/debug/data-viewer` route references a `DataViewerPage` with no file (possible build break if committed as is) |
| F04b | 5 · order 2 | Doc writer: website/frontend/pages/{account, navigation}/** (see `refactor_phase5_slices.tsv`) | done (≈256k tokens; resumed once after the usage limit) | this commit | `account_pages.md` (206) and `app_layout_and_navigation.md` (213) rewritten; account, navigation and topbar visual reference READMEs new; `pending_merge/F04/` emptied (9 sources); backlinks in three in-code READMEs. Wrong claims dropped (minimum role, `PATCH /me`, 204, the sidebar breakpoint 1280 px vs `lg`, a server status indicator and a bell that do not exist, the T-086 stub). F17 input: `design_system/design_tokens.md:11-12` says primary is `#3b82f6`, but `aegis.css:17` sets `#adc6ff` (`#3b82f6` is `--color-action`, :26). Next ticket batch (TF14): possible defect: the membership panel shows for every verified admin in every frame, the Mission Creator included (`membership_status.rs:95-96`); limitations: settings never shows the link code's 10-minute expiry and does not poll the status (`settings/page.rs:228-236`); `/login` shows the sign-in card to a signed-in viewer; cleanup: `membership_status.rs:1-3` lacks the module header; `navigation/layout.rs:14` "today the mission editor" (four chromeless routes, T-1045) |
| F09a | 5 · order 1 | Doc writer: website/README.md, website/api_v2/** (overview, env reference, domain docs, verification_evidence/README.md index) (see `refactor_phase5_slices.tsv`) | done (≈328k tokens) | this commit | `website/README.md` new; `api_v2/README.md` and `api_overview.md` rewritten (all 12 workers, routes by domain, four nonexistent routes dropped); `environment_variables.md`, `decisions.md` (6 dated entries) and `verification_evidence/README.md` (indexes the 15 files: 73 requirements, 91 checks) new; backlinks in 8 in-code READMEs; the api-readiness receipts go stale until H2's `--execute` run. P5f leftover: `apps/website/api_v2/src/core/configuration/README.md` should link `environment_variables.md`. F13 input: `runbooks/local_development.md:197,258` repeats the false "blank `DISCORD_GUILD_ID` writes `auth.role_sync_skipped`" claim. Open for the operator: T-940.1/.2/.3/.11/.12 and T-906 look met by the code (not in Open work). Next ticket batch (TF14): stale `.env.example:122-126` audit-row claim (docs); leftover RCON comment `configuration/mod.rs:298-299` (cleanup); `JWT_ACCESS_TTL_MIN` accepts zero or negative (`configuration/mod.rs:151`, possible defect); may be intended: `SKIP_MIGRATE` and `RUST_LOG` outside the api-readiness configuration digest (`fingerprint.rs:41-94`); frozen evidence notes disagree with code (`mission_artifacts.md:57` `{artifactId}`, `:82-93` missing refusal codes; `progress_checkpoint.md:7-8`) are reading notes in the index, not edits |
| F09b | 5 · order 2 | Doc writer: website/map-engine/**, website/graphics-engine/**, standards/engine_boundary_rules.md (see `refactor_phase5_slices.tsv`) | done (≈329k tokens; resumed once after the usage limit) | this commit | `standards/engine_boundary_rules.md` new (412 lines; §1, §2C, §2C.1, §2D, §5 resolve for every code citation; per-rule table, pins, ratchet, exit codes; §6 known gaps); `website/map-engine/` (README, overview, map streaming, editing layer, draft persistence) and `website/graphics-engine/` (README, overview) new; `engine-layers` gate exit 0; `pending_merge/F09/` emptied. P5f leftovers: the `website/README.md` table rows for the two engines should link the new doc folders; `archive/engine_split/engine_split_program.md` status line becomes "archived — see engine_boundary_rules.md"; `apps/website/map-engine/src/frame/tests/README.md:8` and `apps/website/frontend/src/v2/README.md:58` re-point to `/documentation_v2/standards/engine_boundary_rules.md` §2C. G1 input: `CLAUDE.md:159,162,168-171` (MGRS, top-level symbology, wrong graphics-engine folders). Open for the operator: T-907 (ready) and T-937 (queued) look met. Next ticket batch (TF14): stale `map-engine/Cargo.toml:15-16,24-25` comments (T-1068 class); `graphics-engine/src/frame/damage.rs:13` says "for the map engine" inside the renderer (minor) |
| V5.1 | 5 · W5.1 | Wave verifier over F02, F03, F04 and F09 | dropped (operator direction: no verifier per wave) | — | — |
| F05 | 5 · order 2 | Doc writer: Mission Creator feature inventory, part 1 (areas A–M) and its folder README | done (≈347k tokens plus four helpers; resumed twice) | this commit | The 1,799-line inventory split into 15 area files and an index (a verbatim check against the original passed); 8 areas rewritten and verified (ATTR, BOTTOM, DATA, FILE, KEY, LEFT, MAP; 84 entries: 64 shipped, 6 partial, 14 not built); `feds_schema.md` rewritten; backlinks in 34 in-code READMEs. F06 input in `<scratchpad>/logs/F05/handoff.md` (entry format, bare rows, the ORBAT tree now lives in the ORBAT Manager, fix `performance_at_scale.md:104`). P5f leftovers: `apps/website/frontend/src/v2/apps/editor/README.md:3` and the glossary say "2D/3D" (there is no 3D view); `feature_inventory/README.md:3` "2D/3D" (F06); references to `feature_inventory.md` or `reference/feds_schema.md` remain in F07's `eden_gap_analysis.md` (6, 15, 74, 472), `decisions.md` (89-90, 141-143) and `mission_creator_roadmap.md` (112, 116, 208, 529), in F16's `runbooks/cursor_workspace_setup.md:187` and in F17's `standards/commit_checklist.md:53`. Open for the operator: T-715, T-720 and T-939.2 look fixed. Next ticket batch (TF14): bugs: the first Save Version on a new mission 409s (`mission_editor.rs:120` prefill vs `mission_lifecycle.rs:126`); a draft differing only in zones, triggers, comments, connections or title is marked clean at boot (`local_versus_server.rs:43`, `server_reconciliation.rs:195`); "Load server version" can blank the title (`server_reconciliation.rs:263`, `hydrate.rs:53`); deleting the last folder confirms, does nothing, marks unsaved (`rows/layers.rs:28`, `editor_layers.rs:97`); "Placing into:" can name a different folder than placement uses (`places.rs:103`, `layers.rs:125`); likely bugs (not run): leaked keydown closures (`window_keydown.rs:236,282`) panic after leaving the editor and double undo on remount; the virtual tree's resize listener leaks (`virtual_tree.rs:144`); minor: Space skips the camera-settle refresh and ignores vehicles; edge grid labels use fixed dock widths (`map_furniture.rs:79`); terrain read before the draft restore finishes (`boot_tasks.rs:324`); two default folder names; stale comments listed in `<scratchpad>/logs/F05/helper_*.md`. Vehicle delete is T-837; basemap is T-1058 |
| F06 | 5 · order 3 | Doc writer: Mission Creator feature inventory, part 2 (areas N–Z) | done (≈504k tokens — past the 400k hard stop) | this commit (its backlinks in `apps/editor/arsenal/**` READMEs land with F08b, which edits the same sections) | 7 areas verified (87 entries: 66 shipped, 7 partial, 14 not built) with 26 new IDs; index rows filled; backlinks in 30 in-code READMEs. P5f items: write `feature_inventory/connections.md`, since connections (Sync to, Group to, Trigger Owner, the Connections panel) have no area; `bridge/host_state/armed_placement/README.md:33` says a release over the chrome cancels placement, but it stays armed (`armed_place.rs:16`). F07b input sent (message): stale gap-analysis rows and owner tickets. Open for the operator: T-083 (deferred) and T-146 (ready) look met. Next ticket batch (TF14): bugs: side-filed object leaves in the Factions tree cannot be armed but are recorded as recently placed (`faction_catalog_trees.rs:77-81`, `entity/arming.rs`, `palette/mod.rs:272-290`); the horizontal drag never snaps (`transform.rs:52`, `pointer_up.rs:295-352`); Delete strips a selected vehicle's connections and keeps it (`clipboard.rs:27-29`, fold into T-837); the Export JSON version comes from the Save dialog field (`view.rs:191-196`); minor: the asset picker's stored world point is unused (`asset_picker.rs:8-13`); a drag starting on an unselected entity skips `refresh_selection` (`pointer_move.rs:250-264`); the Arrange menu enables at 1 while the chords need 2 (`menu_row.rs:96-106`); UI text cites cancelled T-078 (`factions_panel.rs:96`) and shows "COMP-SAVE-001" (`menu_state.rs:43-47`); the cluster grid uses Everon's bounds (`bridge_1.rs:236-240`); stale comments (`menu_entries.rs:94-96`, `exports.rs:86-92`, `transform.rs:209`); performance: every pick rebuilds the slot table and point index (`picking.rs:13`, `selection_universe.rs:107`), every edit rebinds the whole slot lane (`history.rs:271-300`); the warm-session marker is read only by a test probe (`session.rs:80`) |
| F07a | 5 · order 2 | Doc writer: website/frontend/apps/editor/{mission_creator_roadmap.md, ux_spec.md} (see `refactor_phase5_slices.tsv`) | done (≈365k tokens) | this commit | `ux_spec.md` (297) and `mission_creator_roadmap.md` (181) rewritten; the ROADMAP marker block is byte-identical and `ticket check --strict` exits 0; four archive copies (the execution half of `decisions.md`, the React-era roadmap, UX spec and page spec); `pending_merge/F07/` emptied. F07b inputs: cut `decisions.md` down to its decisions log (lines 296–369), since the execution half is archived as `archive/handoffs_and_kickoffs/mission_creator_agent_execution.md`; `decisions.md:320,322,325,326-327` contradict the code (240 px docks, the middle button pans, Backspace hides the chrome, a warm return always fetches). P5f leftover: "2D/3D" in `apps/website/frontend/src/v2/apps/editor/README.md:3`; `glossary.md:272` (F19); `CLAUDE.md:3,144` (G1). F19 inputs: T-295, T-132, T-131, T-704, T-212, T-674, T-675, T-935.15–.20, T-110, T-294, T-129, T-140, T-141; the open question of whether the mission armory derives from the slot loadouts. Open for the operator: T-936, T-937 and T-938 are `queued` with every child shipped. Next ticket batch (TF14): Ctrl+X copies only slots but deletes comments too (`window_keydown.rs:138-140`, minor defect); `GET /missions/{id}/export` has no client caller (`mission_export.rs:18`, decide); `editor/mod.rs:1` stale doc comment |
| F07b | 5 · order 3 | Doc writer: website/frontend/apps/editor/{decisions.md (split if over 500 lines), eden_editor_reference/eden_gap_analysis.md} (see `refactor_phase5_slices.tsv`) | done (≈412k tokens; resumed once after the app crash) | this commit | `decisions.md` cut from 637 to 497 lines: 37 dated entries (28 from the source log, 9 new that supersede entries the code no longer follows); `eden_gap_analysis.md` rewritten at 500 lines, all 191 rows re-checked, eden_id set unchanged, summary recomputed (73 match, 39 partial, 48 missing, 3 deferred, 26 na, 2 tbd_only), verify block greps `interactions/*.md` (83); scrape manifest header fixed, data byte-identical; `ticket check --strict` exit 0; the `t739` doc pin tests pass. `mk ci-local-leptos` exit 1 at `cargo fmt` on another session's untracked `apps/debug/data_viewer/**` and `equipment_data_viewer_parity.rs` (not ours; H2 needs a quiet tree). P5f leftovers: `eden_editor_reference/README.md` and `tools_v2/ticket-engine/src/sync/README.md:13` say `ticket sync` keeps the gap-analysis ticket column in step, but its parser only reads headers with `priority |` (`sync/gap_analysis.rs:52`) and these have none; `interactions/transformation.md:25` marks altitude N/A while `bridge/gizmo_z.rs` and `bridge/overlays/z_drag.rs` drag elevation. Open for the operator: T-146 and T-083 look met; whether AI skill should be authorable now that waypointed seats run AI (OBJ-SKILL). Next ticket batch (TF14): Mission Creator controls and compiler emit for fields a mod reader already reads (T-680, T-681, T-678, T-679, T-677, T-673, T-682, all existing; notes only); mount the weather-timeline, radio, tasks and audio panels (existing unmounted-panels ticket); `snap_translate` has no caller (with F06's drag-snap bug); stale `env.rs:25-27` (the mod reads `viewDistance`) |
| F08a | 5 · order 2 | Doc writer: website/frontend/apps/editor/eden_editor_reference/{README.md, interactions/ (split), attributes, ui_anatomy} (see `refactor_phase5_slices.tsv`) | done (≈261k tokens) | this commit | `eden_editor_reference/README.md` new; `interactions/` split from 562 lines into 8 topic files plus an index (83 IDs, set-checked); `attributes.md` (93 ATTR-FIELD IDs unchanged, new code-checked counterpart section) and `ui_anatomy.md` rewritten; Eden facts kept, 13 Mission Creator claims verified. F07b inputs: `eden_gap_analysis.md:31,47,121` link breaks; its verify block (:22-25) greps the old `interactions.md` (now `interactions/*.md`, 83 IDs); the header of `eden_wiki_scrape_manifest.yaml` (same folder) cites a nonexistent `scripts/tools/scrape-eden-wiki.mjs` and "legacy parity tier", and F07b takes it too. F07a input: `mission_creator_roadmap.md:117-121` uses `eden/*.md` labels. F06 input sent (message): the 7-tab right dock, the Eden file map; no area covers connections, comments or compositions yet. Next ticket batch (TF14): the States tab shows roadmap text ("wired to the compiler in a later phase", `attributes_modal.rs:280`); stale `env.rs:5-6,19` comments; may be intended: Alt while placing forces an empty vehicle (Eden inverts the crew toggle), and Space centres the view (Eden cycles the widget) |
| F08b | 5 · order 3 | Doc writer: website/frontend/apps/editor/{README.md, arsenal/, visual_references/}, website/frontend/apps/{README.md, planner/, aar/} (see `refactor_phase5_slices.tsv`) | done (≈335k tokens) | this commit (with F06's backlinks in the four arsenal READMEs) | 17 docs (1,087 lines): the workspaces index, the Mission Creator folder index with a full Contents, `arsenal/arsenal_loadout_editor.md` (14 loadout rows, 3D doll with SVG fallback, cargo, verdict, import/export, Copy/Apply/Remove Everything), 7 visual reference set READMEs (all design-phase references), planner and AAR docs (planned, not built); `pending_merge/F08/` removed. P5f leftovers: "2D/3D" at `apps/website/frontend/src/v2/apps/README.md:13`; `apps/website/frontend/src/v2/apps/editor/ui/arsenal/README.md` could link the arsenal feature doc. G1 input: CLAUDE.md's MIL-STD-2525 `symbology/` claim. Open for the operator: no ticket covers the planner workspace; T-136 puts the replay among routed pages while `apps/aar/` is the reserved workspace, and T-136/T-132 own nonexistent paths. Next ticket batch (TF14): stale comments `ui/arsenal/panels.rs:287-288` (`DOLL_REGIONS`, `SoldierSilhouette.tsx`) and `api_v2/src/missions/services/mission_compile.rs:92` (`/compiled`, T-1044 class) |
| V5.2 | 5 · W5.2 | Wave verifier over F05–F08 | dropped (operator direction: no verifier per wave) | — | — |
| F10a | 5 · order 3 | Doc writer: mod/tbd-framework/{mod_design.md, capability verdicts index, UI/README.md} and the UI screens lobby, mission_selection, briefing (see `refactor_phase5_slices.tsv`) | done (≈388k tokens; resumed once after the app crash) | this commit | `mod_design.md` rewritten in place (`MOD_DESIGN` pin; 14 `@idx` citations and the §2, §5, §6 headings byte-exact; H1 "Mod design"); `capability_verdicts.md`, `README.md` and `UI/README.md` (fixed) written; `vanilla_source_coverage.md` rewritten live; lobby, mission selection and briefing folders with specs describing the built dock shells (briefing 555 → 143 lines), visual reference and reference screenshot READMEs; `enf citations` and `enf capability` exit 0 before and after. F10b inputs in `<scratchpad>/logs/F10a/handoff.md` (set README pattern; safe start, play area and identity link have no layout; the marker palette has no UI; no code for admin help). `enf capability` rewrites line 18 of `.ai/artifacts/enf-index/capability_matrix.tsv` on every run (stale committed copy); `.ai/artifacts/` is outside this program, so the orchestrator restored it. Open for the operator: T-941 queued with all 8 children shipped; T-946.58's summary names `TBD_MissionLoader.GetSlotById`, which the lobby no longer reads. Next ticket batch (TF14): the stale committed `capability_matrix.tsv` line; `capability_verdicts.tsv` has no HAVE rows for shipped screens; `TBD_MissionSelectorScreen.c:18,29` F6 wording (T-1084) |
| F10b | 5 · order 4 | Doc writer: mod/tbd-framework/UI/ remaining screens (end screen, debrief, objective HUD, safe start HUD, play area warning, tactical marker palette, identity link, spectator, in-game menu) and their visual reference folders (see `refactor_phase5_slices.tsv`) | running | — | — |
| F11 | 5 · order 3 | Doc writer: `mod/README.md`, `mod/tbd-export/**`, `mod/tbd-emcp/**` | done (≈347k tokens) | this commit | `mod/README.md` rewritten (no IP, no ticket ids); `tbd-emcp/` index and `workbench_mcp_bridge.md` new; `tbd-export/` indexes plus `MapExport/map_export.md` (only `EMCP_WB_TbdBlueprint` via `wbcall` and the runtime road component are live; all 32 MapExport plugin attributes commented out; the five-stage pipeline; the missing staging step and subregion writer; DEM row 0 south) and `MapExport/terrain_export_runbook.md`; the frozen acceptance report moved verbatim to `verification_evidence/implementation_acceptance.md`, with folder-index READMEs for the evidence folders; `pending_merge/F11/` emptied. F18 input: `runbooks/README.md` indexes `mod/tbd-export/…/MapExport/terrain_export_runbook.md`. F14 inputs: `game_server_staging/README.md:3` title carries the IP; `enfusion_mcp_tooling.md` carries history and its own selftest count. F15 input: `pending_merge/F15/CLAUDE-CODE-START.md:24` says tbd-export depends on the framework. Frozen `field-mapping/inventory.json` holds personal `/home/…` paths (left verbatim). No new tickets (T-1081, T-1091, T-1095, T-1096, T-1100–T-1102, T-1117, T-1148, T-1149 cover the gaps); candidate note for T-1081: pass `--profile` in the export-terrain steps or make the default profile path Proton-aware (`export_profile.rs:20-23` vs `blueprint/ingest.rs:24`) |
| F12 | 5 · order 3 | Doc writer: `tools_v2/**`, `ticketboard/**`, `fleet_host_agent/**`, `contracts_v2/**`, `assets_v2/**` docs | running | — | — |
| F13a | 5 · order 4 | Doc writer: runbooks/{local_development.md, database_operations.md} (see `refactor_phase5_slices.tsv`) | running | — | — |
| F13b | 5 · order 4 | Doc writer: runbooks/{website_deployment.md, testing_and_ci.md} (see `refactor_phase5_slices.tsv`) | pending | — | — |
| V5.3 | 5 · W5.3 | Wave verifier over F10–F13 | dropped (operator direction: no verifier per wave) | — | — |
| F14a | 5 · order 4 | Doc writer: runbooks/game_server_staging/ (split) (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F14b | 5 · order 5 | Doc writer: runbooks/{editor_gates.md, editor_capture.md, enfusion_mcp_tooling.md, spawn_determinism.md} (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F15a | 5 · order 5 | Doc writer: runbooks/two_client_playtest/ (split) (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F15b | 5 · order 5 | Doc writer: runbooks/mod_slice_workflow.md (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F16a | 5 · order 5 | Doc writer: runbooks/factory_waves/ (agent-neutral; Law 2 tooling exception) (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F16b | 5 · order 6 | Doc writer: runbooks/{ticket_run_pipeline.md, cursor_workspace_setup.md} (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F17a | 5 · order 6 | Doc writer: standards/{coding_standards/ (split), where_does_x_go.md, commit_checklist.md, ticket_identifiers.md} (see `refactor_phase5_slices.tsv`) | pending | — | — |
| F17b | 5 · order 6 | Doc writer: known_bugs/**, design_system/** (see `refactor_phase5_slices.tsv`) | pending | — | — |
| V5.4 | 5 · W5.4 | Wave verifier over F14–F17 | dropped (operator direction: no verifier per wave) | — | — |
| F18 | 5 · order 7 | Doc writer: folder-index READMEs for every documentation_v2 folder no other writer owns | pending | — | — |
| F19 | 5 · order 8 | Doc writer: `glossary.md`, `product_roadmap.md`, final pass on `documentation_v2/README.md` | pending | — | — |
| V5.5 | 5 · W5.5 | Wave verifier over F18 and F19 | dropped (operator direction: no verifier per wave) | — | — |
| F19b | 5 · after CP4 | Apply the operator's CP4 strikes to `product_roadmap.md` | pending | — | — |
| P5f | 5 · close | Closing fix run: the in-code README and doc leftovers Phase 5 writers record | pending | — | — |
| G1 | 6 | `CLAUDE.md` full refresh | pending | — | — |
| G2 | 6 | Cursor rules and `.ai/tickets` instruction files refresh | pending | — | — |
| G3 | 6 | Verifier over G1 and G2 | pending | — | — |
| H1 | 7 | Documentation gates into `ci-local` and `ci.yml` | pending | — | — |
| H2 | 7 | Full verification matrix, program files archived, close-out | pending | — | — |

## Plan amendments

| date | amendment | source |
|---|---|---|
| 2026-09-23 | P1-2 also extends ticket validation so every ticket file (parent and child) gets the spec/plan existence check, and first repoints T-068.10.5 and T-159.15.0 at their real `.ai/artifacts/` files so the check stays green until the cutover moves them | operator decision |
| 2026-09-23 | Personal absolute paths in the program files were replaced by `<repo>`, `<scratchpad>` and `$HOME` placeholders; launch prompts carry the concrete values | orchestrator |
| 2026-09-23 | Mod UI screenshots and Stitch sets are mapped to per-screen folders by P0-3 (judgment rows), not by P0-2 | orchestrator |
| 2026-09-23 | The operator compacts the main session at every phase boundary (and at wave boundaries in Phases 4–5). Before each boundary the orchestrator writes a "Phase handoff" block below, then signals "safe to compact"; compaction happens only when no agent is running | operator decision |
| 2026-09-23 | The orphan-spec decisions are applied by P2-3 (tickets), not P2-2; the P0-4 section of the plan names P2-2 by mistake | P0-4 finding |
| 2026-09-23 | P2-4's `docs/` grep also excludes `.ai/tickets/` (14 retired wave-plan mentions and other kept strings stay by design) | P0-4 finding |
| 2026-09-23 | P2-1 accepts 5 near-duplicate collapses (4 DESIGN.md copies differing by a trailing newline, Arsenal/DESIGN.md differing by a 2-line header); the manifest note records each difference | P0-2 finding |
| 2026-09-23 | Follow-up tickets are filed after xtask compiles again and after the operator rules on the three-digit id limit (CP1) | P0-4 finding |
| 2026-09-23 | P1-1 also owns tools_v2/README.md (lines 13-18 link the records it moves) | P0-5 finding |
| 2026-09-23 | P1-5's link and backticked-path checks treat `documentation_v2/refactor_*` program files as records (they spell old and future paths by design); once archived, the archive rules apply | P0-5 finding |
| 2026-09-23 | P2-2 moves the `.editorconfig-checker.json` excludes from the two docs/specs mockup folders to the new `visual_references/` and `design_system/token_exports/` locations (generated exports, byte-pinned) | P0-5 finding |
| 2026-09-23 | The committed plan copy spelled the retired wave-plan fossil strings; reworded (the fossil guard in `ticket check` would fail on them) | P0-5 finding |
| 2026-09-23 | Size agents tighter: P0-5 finished at ≈645k tokens. Scripted agents get narrower scopes; the orchestrator stops any agent nearing 450k | orchestrator |
| 2026-09-23 | CP1 answered: the operator accepted the recommended answer to all 21 questions; P0-5f applies 1–3 (set rename, per-page dashboard and server-intel sets, `verification_evidence/` folders) and records the decisions in the writing brief | operator decision |
| 2026-09-23 | New agent P1-2b (78 agents in total): after P1-2, widen the ticket id pattern in `.ai/tickets/schema.json:18` and `tools_v2/ticket-engine/src/sync/gap_analysis.rs:12` to three or more digits (tests included), then file the three follow-up tickets from `documentation_v2/refactor_followup_tickets.md` top-level: the scenario-to-mission rename (status deferred), the mod wave gate's missing Makefile, and one human cleanup ticket for the 44 stale branches, 6 worktrees and the 4 merged-but-ready tickets | operator decision (CP1 question 17) |
| 2026-09-23 | Launch prompts: each agent gets its plan prompt (role, grant, ownership, checks) with the placeholders replaced by concrete values and the base brief given by pointer, plus the amendments in this table, plus any ownership or check that the orchestrator's pre-launch scoping of the code finds missing. Every such addition is logged in this table with the agent's id before or with its commit | operator decision |
| 2026-09-23 | P1-1 launch additions: exact targets from manifest rows 1066–1072; the moved files stay byte-identical (no status line, no link edits; P2-5 does both in commit 2b) so git records pure renames; the two README edits use repo-root links to the archived files and must not present the archived plan as live authority; extra checks `hcargo xtask ticket check --strict`, `git status --porcelain -- tools_v2 documentation_v2/archive` and `git diff -M --cached --stat` (7 renames, no content change); if the other session breaks the xtask build, record the compiler error and report without touching their code | orchestrator pre-launch scoping |
| 2026-09-23 | New fix run P1-2f (orchestrator review of P1-2): lines that still describe the retired views — ticket-engine `Cargo.toml:8` (description), `src/lib.rs:1`, `src/cli/tests/mod.rs:240`, `src/cli/mutation_support.rs:36-38`, `src/cli/status.rs:92-95`, `src/cli/shipping.rs:84` (comments; present tense, no ticket references); `.ai/tickets/AI_PLAYBOOK.md:9`, `.ai/tickets/README.md:3`, `.ai/tickets/CLAUDE_CODE_PROMPT.md:74`, `.ai/tickets/SPEC_TEMPLATE.md:113`, `.cursor/rules/tbd-platform.mdc:15`, and `CLAUDE.md:178,238` (dirty file: those two lines only, staged hunk-wise). Ahead of G1/G2's full refresh, because live instructions otherwise name deleted files. Committed together with P1-2 | orchestrator review |
| 2026-09-23 | P1-2 questions answered: (1) ticketboard's file watch stays as it is — it never watched the gap-analysis file, and its git-status panel now lists that file's changes; (2) retire `cargo xtask ticket milestone` now — new agent P1-2c after P1-2f removes the verb (`ticket-engine/src/cli/queries.rs` `cmd_milestone`, its re-export at `cli/mod.rs:27`, xtask `commands/ticket/{cli.rs, dispatch.rs}`), its tests and every helper only it uses, and the `milestone` key in `registry/ticket_file_storage/key_contract.rs` if nothing else reads it (0 of 1,458 ticket files carry it); own commit; (3) the spec and plan existence check keeps its reach: every status except idea and cancelled | operator decision |
| 2026-09-23 | P1-2c launch additions: ownership of xtask `commands/ticket/{cli.rs, dispatch.rs, README.md, tests/**}` and any ticket-engine test that names the verb; the `milestone` key in `registry/ticket_file_storage/key_contract.rs` sits in a list marked FROZEN, so the agent removes it only if the list is the allow-list of current keys, and keeps it (reporting why) if the list is a compatibility contract for parsing existing files; plus `ticket-engine/src/cli/mutation_support.rs:33-34`, which cites a design document in a source comment (law 8), reworded in the same pass | orchestrator pre-launch scoping |
| 2026-09-23 | **Operator delegation (overnight run).** The operator is unavailable and has told the orchestrator to keep going until the program is done. From here the orchestrator: answers every agent question itself (the recommended option, backed by code evidence); sends every problem to a fix agent without waiting; reviews and decides CP2, CP3a and CP3b itself, with a verifier agent's verdict; decides CP4 non-destructively (items that look unplanned move to an "unscheduled" section of `product_roadmap.md`, never deleted); keeps the live server-address check of CP2 open for the operator (no remote commands); before P2-1 replaces the operator's confirmation with checks (`git status -- docs` clean, other local sessions listed and told to hold `docs/` writes); keeps one commit per agent and the phase handoff blocks (auto-compaction replaces the manual compaction). Every decision taken this way is logged in this table as "orchestrator decision (delegated)" for the operator's review | operator decision |
| 2026-09-23 | P1-2b launch additions: besides the two patterns, ids of four or more digits must sort after T-999. Six places break ties by comparing id strings, which puts T-1000 between T-100 and T-101: `registry/mod.rs:222` (`ticket_sort_key`), `sync/markers.rs:53`, `wave_lock/packing.rs:86`, `wave_lock/collisions.rs:189`, `metrics/summary.rs:29`, `registry/ticket_file_storage/storage.rs:147`. P1-2b owns them and their tests and gives them one shared id-order key (numeric parent id, then the full id string), which keeps every existing id in its current order, so queue.json, the roadmap block and wave packing stay byte-identical for today's corpus. It audits the numeric id parsers (`store.rs:40,142`, `ticket_file_storage/encoding.rs:32`, xtask `wave_execution/gate/gate_dispatch.rs:57`, `wave_execution/test_cmd.rs:71,242`, ticketboard `projection.rs:43`) and changes them only if one assumes three digits. It also owns the new ticket files `ticket add` writes and what `ticket sync` then rewrites | orchestrator pre-launch scoping |
| 2026-09-23 | P1-2c question (retire `FROZEN_27`, `frozen_27_matches_live_corpus` and `union_ticket_keys`, whose test always returns early on today's all-typed corpus?): keep them while `load_registry` (`registry/mod.rs:17-31`) still has its untyped path; P1-2b files a fourth follow-up ticket, status idea, to retire the untyped ticket-tree load path together with those three | orchestrator decision (delegated) |
| 2026-09-23 | P1-2b questions: (1) keep T-1000's hand-set `order = 9811`, the value `ticket reorder T-1000 T-981` mints (no verb could run while `set-status deferred` had left the registry red); (2) keep the three extra ordering sites (`registry/typed_projection.rs:184`, `wave_lock/parking.rs` `pack_last` sort and `wave_zero` list), the same fault as the six named ones. P1-2b's tooling gap is fixed now by a new fix run, P1-2d: `set-status` to `deferred` or `cancelled` mints the append order (`reorder` after the highest-ordered ticket) when the ticket has none, because order carries no dispatch meaning there; `queued` keeps its refusal (order is dispatch priority); no ticket verb may leave `ticket check` red | orchestrator decision (delegated) |
| 2026-09-23 | P1-2d decisions: keep the `ship`/`done` fix (same missing-order bug as `set-status shipped`); keep the pre-write order rule, which now refuses the one edge where `reorder` anchored on a negative order would write order 0 (no current ticket is affected; orders run 10–9811) | orchestrator decision (delegated) |
| 2026-09-23 | P1-3 questions: (1) the retired `specification_consistency` gate 7 checked that cited commands exist; P1-5b's link checker takes that over for live docs — every backticked `cargo xtask <group> [<verb>]` must name an existing subcommand path in the xtask command tree (placeholders and flags ignored); (2) keep P1-3's one-line edit to `ticket-engine/src/sync/tests/gap_analysis_tests.rs`. New fix run P1-3f: `xtask/src/cli/mod.rs:88` (`schema --help` headline), `commands/ci/task_runner.rs:114` (sub-gate count), `commands/db/tests/operations/tests.rs:4-7` (`LANE_COMMANDS` reader), plus P1-2d's polish items (`ticket-engine/src/ops/transitions.rs` `ship` doc phrase, `cli/shipping.rs:48`) | orchestrator decision (delegated) |
| 2026-09-23 | P1-4 launch additions: the existence tests classify every constant of the three `documentation` modules. Live paths must exist (xtask: `FACTORY_PACK_WAVE`, `LAYOUT_TARGET_DIR`, the six runbook/design paths, the new `API_READINESS_*` pair; ticket-engine: `TREE_DIR`, `PLANS_DIR`, `PLAN_TEMPLATE`, `SPECS_DIR`, `ROADMAP`, `GAP_ANALYSIS`, `TOKEN_ESTIMATE_FACTOR_DOC`, every path in `ARCHIVED_WAVE_PLAN_READERS` and `STALE_TICKET_ID_SCAN_ROOTS`; developer-tools: `MOD_DOCS_DIR`, `CAPABILITY_VERDICTS`, `EDITOR_GATE_RUNBOOK`). Historical or prefix-only values (`ARCHIVED_WAVE_PLANS`, `RETIRED_QUEUE_VIEW_PREFIX`, `NUMSTAT_EXCLUDED_PREFIXES`, `SCAN_EXEMPT_PREFIXES`) are listed in the test as exempt, each with its reason. `LAYOUT_TARGET_DIR` (`docs/website/`) therefore binds P2-2 to repoint it at the cutover until H1 deletes it | orchestrator pre-launch scoping |
| 2026-09-23 | P1-5 splits into P1-5a and P1-5b (sequential, one commit each): at ≈400k tokens P1-5 sat at the cap P1-2 (415k) and P1-2b (423k) already reached. Delegated decisions for P1-5a: (1) the machine-checked Contents grammar — the first ```text block after `## Contents`; line 1 is the folder's repo path plus `/`; every other non-blank line is one direct-child entry: optional tree-drawing prefix (`├── `, `└── `, `│   `, spaces), then the entry (a name or a glob of `*`, `?`, `[…]`, `{a,b}`; a directory entry ends in `/`, a file entry does not), then two or more spaces, then a non-empty role; a nested (deeper) entry or a `/` inside an entry is an error; untracked children are ignored. P3-1 documents this grammar verbatim. (2) README coverage spans the code trees and `documentation_v2/` (F18 makes the latter green), except folders under `tests/`, `generated/`, a dot-folder, and `documentation_v2/pending_merge/`. (3) Markdown placement: code trees hold no `.md` except README.md (today 38 others, all in the manifest; none under tests or fixtures). For P1-5b: `documentation_v2/refactor_*` records are skipped by the link and backticked-path checks while they sit at the documentation root; once H2 archives them the archive rules apply | orchestrator decision (delegated) |
| 2026-09-23 | P1-4 questions: (1) the classification check stays on the `documentation` modules — the cutover changes only those, and the top-level constants keep their hand-listed existence tests (some are absent by design: `DEPLOY_ENV` is gitignored, `.ai/artifacts/enf-index` is pipeline output); (2) keep the register path in the register-read error | orchestrator decision (delegated) |
| 2026-09-23 | P1-5a questions: (1) READMEs inside exempt folders (`tests/`, `generated/`, dot-folders, `pending_merge/`) are exempt from the Contents check as well — those folders sit outside the README rule; (2) a line of only tree-drawing characters (`│`) is a spacer and allowed; (3) tree roots need a README (`apps/README.md` is R11's). Accepted P1-5a's extra constants and the ROADMAP/GAP_ANALYSIS size exemption (operator decision: sync-managed tables stay in one file). Fix run P1-5af applies (1) and (2) and updates `tools_v2/xtask/src/verifications/README.md` | orchestrator decision (delegated) |
| 2026-09-23 | P1-5b splits again into P1-5b (link-check core) and P1-5c (backticked paths, command citations): P1-5a needed ≈448k tokens for two gates, and the link checker has more parts | orchestrator (budget amendment) |
| 2026-09-23 | P1-5b launch additions: a github.com URL into this repository that is not a sha permalink (`tree/…`, a branch name) is a break, since the brief requires permalinks by sha; own-repo permalinks are checked in one `git cat-file --batch-check` process and their `#L` anchors against the blob's line count; a link to an untracked file is a break (the gate reads git's view); the rule pipeline is built so P1-5c's two rules plug in without touching the scan | orchestrator pre-launch scoping |
| 2026-09-23 | P1-5b questions: (1) the link gate excludes `.ai/artifacts/` (out of the program's scope); (2) only code URLs into this repository must be sha permalinks — `blob/<full sha>/<path>` and `tree/<full sha>/<path>` (checked as blob and tree) pass, a branch name or abbreviated sha is a break, and the repository home page and non-code pages (issues, pulls, actions, releases, wiki) are external links; (3) an undefined shortcut reference `[text]` stays plain text (CommonMark). P1-5b's other decisions accepted (exact fragments after percent-decoding; `#L` on `.md` is a heading fragment unless `?plain=1`; `main.rs:12` is a path; bare URLs and HTML `href` not scanned). Fix run P1-5bf applies (1) and (2) | orchestrator decision (delegated) |
| 2026-09-23 | P1-5c scope: (1) backticked paths — inline code spans in live documents only; a token with a `/` whose first segment is a tracked top-level folder must name a tracked file or a folder holding tracked files, or a path git ignores (runtime output, one batched `git check-ignore --stdin`); globs, `<…>`, `{…}`, `$VAR`, tokens with spaces (commands) and URLs are skipped; a trailing `:line` or `:line-line` is stripped; a layout constant lists historical spellings a live document may name on purpose, each with its reason. (2) command citations — every `cargo xtask …` in an inline code span OR a fenced code-block line of a live document names an existing subcommand path in xtask's own clap command tree (`CommandFactory`); flags, `<…>` placeholders and arguments after a leaf are ignored. Fenced blocks count because runbooks cite commands there | orchestrator decision (delegated) |
| 2026-09-23 | P1-5c questions: (1) `docs` stays a candidate root for backticked paths after the cutover, so a stale `docs/…` spelling in a live document keeps failing; (2) a relative spelling whose first segment collides with a top-level folder is not resolved from the document's folder — writers spell full repository paths (law 4); (3) command citations also check the targets of `mk` and `ci` (and any leaf whose first positional argument has a closed value set) against the real lists, as the retired gate 7 did — fix run P1-5cf. P1-5c's six beyond-spec behaviours accepted | orchestrator decision (delegated) |
| 2026-09-23 | P1-6 launch additions: the audit covers the whole Phase 1 range `093c901f0..HEAD` (every agent and fix run); it also runs the three documentation gates and records their totals as the pre-cutover baseline; its FIX-LIST must carry the two known polish items (`api_readiness/fingerprint.rs:43` spells the path `DEPLOY_ENV` holds; the `ship` doc in `ops/transitions.rs` and the `cli/shipping.rs` comment say the SHA "stays hand-edited" although `ticket stamp-sha` writes it); it edits only its own roster row, since the orchestrator maintains the rest of the file | orchestrator pre-launch scoping |
| 2026-09-24 | P1-6 verdict FIX-LIST (7 small items; every Phase 1 check green; red tests only the other session's). Delegated calls: item 5 — drop the unused `PLANS_DIR`/`SPECS_DIR`/`TREE_DIR` re-exports from the xtask layout module and their `allow(unused_imports)` (P2-2 or H1 re-exports `TREE_DIR` when it merges `DOCUMENTATION_ROOT` into it); item 6 — fix `tools_v2/xtask/README.md:11` now rather than wait for R17; item 7 (optional) — fix too, same class as item 1. Fix run P1-6f applies all seven | orchestrator decision (delegated) |
| 2026-09-24 | Fix run P1-6g (P1-6f findings, fixed per the operator's "if something needs to be fixed, fix it"): `ticket-engine/src/ops/transitions.rs:203` (error text) and `validation/readiness.rs:60,103` cite "t920 spec Decisions log #2"; `readiness.rs:67-79` claims shipped history is untouched while the rule covers Shipped, and cites "spec §Non-goals"; rustdoc warnings at `corpus_pins.rs:1`, `metrics/estimates/git_changes.rs:56`, `wave_lock/collisions.rs:16,18`. Bounded to this list | orchestrator decision (delegated) |
| 2026-09-24 | P1-6g finding: design-document citations in comments remain across ticket-engine (`validation/readiness.rs:5,42-48`, `validation/runner.rs:46`, `ops/readiness.rs:17,23,103,115`, `ops/validation.rs:69-110`, `model/scope.rs:107-148`, `validation/debt.rs:56,85`, `validation/shipping.rs:74`, `validation/scope.rs:30,54`, `validation/body.rs:15`, `registry/ticket_file_storage/encoding.rs:60`, `model/tickets.rs:23`); the prose rule's ticket-id pattern misses spellings like "t920 spec" and "spec §". Not chased in this program; P2-3 files follow-up ticket T-1004 (idea): extend `tooling_prose_rules` to catch design-document citations and clear every offender | orchestrator decision (delegated) |
| 2026-09-24 | P2-1 launch additions: skip the `done-by-P1-1`/`done-by-P1-2` rows after confirming they are applied; run every ORDER:1 row before its ORDER:2 row; for the 5 NEAR-DUPLICATE collapses verify the difference matches the note exactly before `git rm`; drive the rows from a scratchpad script with a full dry-run validation pass first; never delete an untracked or ignored file (if any remain under `docs/`, report them and leave the folder); no cargo runs (the tree is expected to fail its tests until P2-2a); the staged diff must show R100 for every move and D for every delete, with per-action counts equal to the manifest's | orchestrator pre-launch scoping |
| 2026-09-24 | P2-2 splits by the pin catalogue's kind column: P2-2a takes code, test and config rows (69 rows, 29 files), P2-2b takes comment rows (87 rows, 72 files: 27 EnfScript `.c`, 33 `.rs`, CI, config comments). Three files hold rows of both kinds (`.github/workflows/ci.yml`, `developer-tools/src/enfusion_tooling/capability.rs`, `xtask/src/commands/ci/task_definitions.rs`); P2-2b runs after P2-2a | orchestrator (budget amendment) |
| 2026-09-24 | P2-1 question: the six ORDER:2 primaries refill paths that HEAD already holds, so plain `git diff --cached -M` shows 6 M + 6 A + 6 D for them; P2-4 checks rename detection with `-M -B` (973 R100 + 85 D) and accepts those rows by design | orchestrator decision (delegated) |
| 2026-09-24 | P2-2a launch additions: rows matched by string or constant name (line numbers drifted); every new value cross-checked against the manifest target; `TREE_DIR` and xtask's `DOCUMENTATION_ROOT` become one spelling (re-export), with the P1-4 classification rows kept consistent; `mk ci-local-leptos` proves the `include_str!` pin; expected red to attribute, not fix: registry-driven tests until P2-3 rewrites the ticket paths, the other session's two tests, comment-grep tests P2-2b fixes; no `ticket check`/`sync` (P2-3's) | orchestrator pre-launch scoping |
| 2026-09-24 | P2-2a questions: (1) xtask `DOCUMENTATION_ROOT` stays a re-export alias of `TREE_DIR`; (2) developer-tools `MOD_DOCS_DIR` → `DOCUMENTATION_ROOT` accepted; (3) the api-readiness register (`documentation_v2/website/api_v2/verification_evidence/requirements.json`) names moved documents as implementation files (`:917` → a `page.md` now in `pending_merge/F03/`) — P2-2b checks every implementation path in the register and drops each entry that names a moved document (code folders keep only README.md); (4) deleting the REORG_CHANGELOG special case (the archive exemption covers it) accepted | orchestrator decision (delegated) |
| 2026-09-24 | P2-2b questions: "Contract spec:" at `TBD_RegistryScan.c:1464` accepted; `.gitignore:3` stays one line. T-1004 (filed by P2-3) widens to comment hygiene across Rust and EnfScript: design-document citations, ticket ids (`T-181.x` in EnfScript comments) and history words the prose rules miss (ticket-engine list from P1-6g, `satellite_archive_container.rs:7-8` "allowlisted giants", `playtest_server.rs:31-33,46-48`, `TBD_RadioBridgeStub.c:10` `§6`, `TBD_FrameworkManager.c:1195` "legacy"), plus extending `tooling_prose_rules` to catch them | orchestrator decision (delegated) |
| 2026-09-24 | P2-3 launch additions: the 46 `manual` rewrite rows (folder mentions that split across several targets) are rewritten to the most specific current location in OPEN tickets (idea, queued, ready, running, review, deferred) and left as written in shipped or cancelled tickets (history; `ticket check` does not read prose paths, and P2-4's `docs/` grep excludes `.ai/tickets/`); rewrites are applied by a field-scoped exact string replacement script that proves each row applied once; after `ticket check --strict` is green P2-3 files T-1004 (idea) through `ticket add` | orchestrator decision (delegated) |
| 2026-09-24 | P2-3 questions: (1) T-090.12.7 requirement pointing at the frozen T-090.11 spec accepted (frozen documents stay readable); (2) T-933's two named runbooks accepted; (3) T-1004's shortened title and class `feature` accepted (whoever picks it up may reclassify); (4) dropping the T-320/T-354 citations accepted (`ticket check` forbids splitting one path across owns and citations). P2-4's FIX-LIST carries T-090.12.7's summary ("new debug-world-los.md" → the moved `world_los_page.md`) | orchestrator decision (delegated) |
| 2026-09-24 | P2-4 FIX-LIST: items 1–2 (T-090.12.7 summary; T-120 "STAGING-SERVER" and T-181.16 "PLAYTEST_RUNBOOK" in queued tickets) go to fix run P2-3f before commit 2a; item 3 (`documentation_v2/mod/tbd-framework/capability_verdicts.tsv:10`) and `.ai/tickets/plan_template.md:1` ("docs/plans/<id>_plan.md" inside an HTML comment) join P2-6's scope in commit 2b. P2-4 question: T-090.12.7 (ready) owns and plans to append to `documentation_v2/archive/shipped_history/shipped_history.md`, a frozen archive under the History decision — the deliverable is dropped (the ticket registry and git history record shipped work); P2-3f removes that owns entry and the matching requirement/acceptance item | orchestrator decision (delegated) |
| 2026-09-24 | After commit 2a (19738aff0): the API v2 completion session was told its new resume path (SendMessage); memory repointed (`MEMORY.md`, `api-v2-completion-program.md`, `feedback-api-v2-completion-subagent-slices.md`, `documentation-v2-program.md`). Commit 2b launch rules: (1) a link to a `merge-into` source points at its final merge target when that file exists, else at the source's `pending_merge/` path — the Phase 5 writer who deletes the source repoints inbound links (link-check forces it); (2) links use the repo-root form `/documentation_v2/…`, backticked path mentions the repo-relative form `documentation_v2/…` (no leading slash, so the backticked-path rule checks them); (3) P2-5's status lines: `**Status:** frozen record` on `documentation_v2/tickets/**`, `**Status:** archived — see <live replacement>` on `documentation_v2/archive/**` when the manifest names one, else `**Status:** archived`; (4) P2-6 also owns `documentation_v2/mod/tbd-framework/capability_verdicts.tsv:10` and `.ai/tickets/plan_template.md:1` (P2-4) | orchestrator decision (delegated) |
| 2026-09-24 | P2-6 questions: (1) its 11 folder mappings accepted (`docs` → `documentation_v2`, `docs/website/backend` → `documentation_v2/website/api_v2`, `docs/platform/known-bugs` → `documentation_v2/known_bugs`, `docs/specs/Mission_Creator_Architecture` → `…/frontend/apps/editor`, …); four split folders stay as written for the writers; (2) the four imported Stitch token exports keep no status line (their YAML front matter is exported data on line 1); (3) the root `README.md`, which no plan table assigned, goes to G1 with CLAUDE.md in Phase 6 | orchestrator decision (delegated) |
| 2026-09-24 | P2-5 questions: (1) its two historical-path permalinks accepted; (2) breaks 1–3 — links in frozen specs to `.ai/artifacts` files never committed (`t144_arma3_map_architecture_study.md:109,110`, `t152_2_reforger_icon_art.md:30`, `t177_mc_chrome_orbat_cutover.md:7`) — are unlinked, keeping the visible text (a link-only change); break 4 (`t-936_7_plan.md:10` `points[][2]`) is fixed in the gate: a full reference whose link text is empty is plain text (CommonMark renders it so; never an intended link) — fix run P2-5f; (3) the writers who create the 8 missing replacements update those archive files' status lines to `archived — see …` (F09 `engine_split_program.md`; P3-1 `frontend_page_spec_template.md`; F19 `mod_milestones.md`, `tbd_reforger_platform_build_plan.md`, `discord_milestone_1_post.md`; F18 `docs_specs_readme.md`, `docs_website_archive_readme.md`, `docs_platform_readme.md`); (4) `macos_ux_architecture.md` keeps its link to `documentation_v2/design_system/` (F17 writes the UX methodology there) | orchestrator decision (delegated) |
| 2026-09-24 | P2-7 FIX-LIST → fix run P2-6f: add 2 to the 11 live-doc line pins and the open-ticket pins (T-711, T-935.15 — its plan pin was already one off, so it becomes :34); T-404 (deferred) owns and plans to edit the archived `frontend_data_provenance.md` — that deliverable is dropped, as with T-090.12.7 (archived files are frozen). Pins inside frozen docs and closed tickets stay as written (history) | orchestrator decision (delegated) |
| 2026-09-24 | P3-1 splits into P3-1a (the standard + six README kind templates) and P3-1b (six more kinds, the six document templates, the templates folder README, the archive status line of `frontend_page_spec_template.md`): 12 kind samples verified against real folders plus six templates exceed one agent's budget. CP3a is decided under delegation from reviewer P3-1v's verdict | orchestrator (budget amendment) |
| 2026-09-24 | P3-1a questions: (1) one template for documentation_v2 folders, `readme_documentation_folder.md`; the planned `folder_index.md` is dropped (the standard itself states the README core); (2) R21 drops the README half of ticketboard's architecture test and keeps its module-root half — the readme-coverage gate is the single README checker (law 3), wired into CI by H1; (3) `apps/website` is a two-kind README (area root + deploy/config), so R11 adds Configuration and Installed by. Code findings are filed as follow-up tickets by a ticket-filing run (no code change in this program): the `v2/core` → `apps/editor` layering inversion (`core/auth/store.rs:259`, `core/ui/{search_box.rs:18, select.rs:15, slider.rs:19}`; pages `review_workspace/page.rs:17-18`, `library/dossier_upload.rs:35`); `cargo xtask deploy staging` prints "API + Postgres" but its compose command lacks `--profile api` (`deploy/staging/remote/ssh_argv.rs:214-223`); `TRUSTED_PROXIES` (`core/configuration/mod.rs:133`) absent from `.env.example`; api_v2 missions comments that describe a nonexistent inject tool (`missions/mod.rs:2`, `services/mission_document.rs:4,44`, `handlers/mission_default_overrides.rs:96`) | orchestrator decision (delegated) |
| 2026-09-24 | P3-1b questions: (1) `deferred` tickets count as open work in a feature doc's `## Open work` (parked, not done); (2) glossary entries use `### term` headings for stable anchors — P3-2 adopts them | orchestrator decision (delegated) |
| 2026-09-24 | CP3a (delegated): all 15 P3-1v findings are applied as the reviewer specified — seed order (db up → API boot migrates → db seed) in both samples and a run-order rule; the parent rule rewritten (boundary-crossing items one line each; no child inventory), mod-assets sample re-rooted at `Prefabs/Systems/`; the path rule (outside the folder: full repository path; inside: folder-relative); a visual-reference-set note; the README vs feature doc split (README: what the code declares; feature doc: flows, rules, reasons, design, open work, decisions, linking the README); Open work links a ticket's spec or its `/.ai/tickets/T-<id>.toml`; the minor items; README indexes inside `documentation_v2/tickets/` and `archive/` are live documents (`**Status:** live`, updated as files land) although the trees they index are frozen; the same-commit rule joins the writing rules; the R21 to-do leaves the standard (the lasting rule: the readme-coverage gate is the one README checker). The standard locks when fix run P3-1f reports every item resolved; the pilot (R01 ∥ F01) and P3-3 exercise it before CP3b | orchestrator decision (delegated) |
| 2026-09-24 | P3-1f questions: (B5) the `EQUIPMENT_DATA_DIR`/`EQUIPMENT_EXPORT_SOURCE_DIR` rows in `readme_crate_root.md` document variables that exist only in another session's uncommitted work — removed (truth is judged against committed code; the equipment work documents them in its own commit); P3-2 removes the two rows as its first step; (M11) "each with the test or gate that holds it wherever one does" kept | orchestrator decision (delegated) |
| 2026-09-24 | R01 splits into R01a (20 folders: crate root, migrations, seeds, `src/` root, `src/bin`, core, background workers) and R01b (24 folders: the six domains); the pilot runs R01a ∥ R01b ∥ F01 (three writers, disjoint files) after P3-2. P3-2 launch additions: remove the two uncommitted-variable rows from `readme_crate_root.md`'s sample; the glossary must define `#mission`, `#event`, `#mission-creator` (the templates link them) and every term the pilot slices need | orchestrator (budget amendment; pre-launch scoping) |
| 2026-09-24 | P3-2 questions: (1) `documentation_standards.md` keeps numbered §1–§10 headings (a standards document, not a README; code comments cite its section ids); (2) prose may cite `file:line` beside a symbol name, never a bare line number; (3) "lines added to EnfScript are ASCII" is a standard (brief rule 7; engine constraint); (4) glossary merges (slotting → slot, addon → mod; singular panel headings) accepted | orchestrator decision (delegated) |
| 2026-09-24 | Pilot launch additions (R01a, R01b, F01): describe committed code only — a file another session has dirtied is read through `git show HEAD:<path>`, and untracked files (e.g. `community_content/handlers/equipment_data_viewer/`) are neither listed nor described; new untracked READMEs are gate-checked on a scratch copy of the index (`GIT_INDEX_FILE`), never the real one; writers get the verified facts other agents found in their slice (dev-login roles, `.env.example` gaps, seed order, the RCON claim, the personnel roles and the "Comms Broadcaster" breadcrumb); a hard stop at ~400k tokens after P3-2 reached ≈571k | orchestrator pre-launch scoping |
| 2026-09-24 | F01 questions: (1) F01b writes F01's docs from its verified handoff with a fresh budget; (2) feature docs link a page's in-code README file (section anchors optional — R05 rewrites the page READMEs later); (3) T-940.1, T-940.3, T-940.11, T-940.12 (ready) and T-086 (deferred) describe work the API already does or replaced — left unchanged FOR THE OPERATOR (closing or re-scoping tickets is a product decision). F01b also corrects the glossary's audit-logs entry (no CSV export or live feed exists). Next ticket batch (TF3): the admin UI discrepancies F01 verified (detach and delete confirm texts contradict the server, `event_mission_attachment.rs:309-326`/`event_create_update.rs:321`; the calendar fetches only 20 events and shows a failed fetch as empty, `state.rs:144,256-264`; the personnel search placeholder promises Discord-id search, `page.rs:207` vs `personnel_roster.rs:90-96`; Save Draft sends nothing and the broadcast toast ignores Discord failures, `editor_form.rs:109,123-129,218`; failed mission attaches are toasted as success, `schedule_dialog.rs:87-103`), plus P3-2's comment findings. Lesson for Phases 4–5: research-heavy slices outgrow one agent — slices are sized from the pilot's measured cost, and a writer that nears the cap saves a verified-facts handoff | orchestrator decision (delegated) |
| 2026-09-24 | R01b questions: its 18 new READMEs commit with the 6 rewrites; a domain root lists routes one bullet per path with methods and access tiers inline (compact form accepted for the pilot; P3-3 judges it for the style lock) | orchestrator decision (delegated) |
| 2026-09-24 | R01a questions: (1) the three documentation gates get a `--with-untracked` flag (judge untracked, not-ignored files as if tracked) so writers can check new files before the orchestrator commits — new tooling fix run P3-4 before Phase 4; the scratch-index workaround (`GIT_INDEX_FILE`, which `hcargo` does not pass through) is retired; (2) READMEs keep linking `api_overview.md` although its content is stale — F09 rewrites it. Budget: R01a finished at ≈523k despite the 400k stop, R01b at ≈371k for 24 folders — Phase 4 slices are sized at about 15 folders per writer | orchestrator decision (delegated) |
| 2026-09-24 | F01b questions: (1) re-publishing a pinned announcement unpins it (`editor_form.rs:157` sends `is_pinned: false`; `announcements_admin.rs:304`) joins the next ticket batch; operations without `server_id` are covered by T-1022; (2) the API's audit CSV export has no page button — filed as a feature idea in the next batch; (3) R05 owns F01b's "lists no calls" lines (see F01b's row); (4) T-1015 also covers the event-manager guard test that pins the wrong delete text (`event_manager/tests/event_manager.rs`) and the `lifecycle.rs:60-65` doc comment — the next batch appends that to T-1015's notes | orchestrator decision (delegated) |
| 2026-09-24 | CP3b (delegated): the pilot passes after fix run P3-3f applies P3-3's items 1–10; P3-3's STYLE-LOCK (12 rules with model files, plus a don't-copy list) is adopted and written to `documentation_v2/refactor_style_lock.md`, which every Phase 4 and Phase 5 writer prompt names as required reading; the style locks | orchestrator decision (delegated) |
| 2026-09-24 | Phase 4 re-sliced from the pilot's measured cost (R01a ≈29k, R01b ≈15k tokens per folder; the plan's 20 writers would each exceed the cap): 664 remaining code folders → 43 README writers of 4–20 folders (subtrees kept together), 11 waves W4.1–W4.11 of up to four writers, each wave followed by its verifier V4.n, which may run while the next wave writes (verifiers are read-only). The writer list with exact units is the program record `documentation_v2/refactor_phase4_slices.tsv`. Every writer prompt adds `refactor_style_lock.md`, `--with-untracked` for its own new files, the committed-code-only rule, and the facts earlier agents recorded for its folders (roster rows and amendments) | orchestrator (budget amendment) |
| 2026-09-24 | R05.1 questions: (1) `pages/navigation` stays a page README (first-match kind rule: it holds the router's `*` route); (2) page READMEs may link the API domain README for the calls they make, besides their feature doc | orchestrator decision (delegated) |
| 2026-09-24 | Phase 4 runs as rolling slots: whenever a writer finishes, the next slice in table order starts, so four writers stay busy; each wave's verifier V4.n starts once all of that wave's writers are committed. R05.2 also owns the interim lines in F01b's six admin feature docs (Data openers, route facts, state texts) and replaces them with links to its READMEs' Routes, Data and States (P3-3 item 11) | orchestrator decision (delegated) |
| 2026-09-24 | R02 questions: (1) a quoted enum value such as `legacy_unknown` is fine (identifiers are quoted as spelled); (2) leaves above ~60 lines stay when their Contents and tables need it (the guide is soft); (3) glossary links only where the word carries the glossary sense | orchestrator decision (delegated) |
| 2026-09-24 | R04 questions: (1) the deployments figures and the unread `role_notice` become tickets (TF6); (2) leaves of ~70 lines holding 7–8 files are fine | orchestrator decision (delegated) |
| 2026-09-24 | Usage limit hit (HTTP 429, reset 11:00): R03, R05.2, R06.1 and R06.2 stopped mid-run; `/tmp` was also cleared, so every scratchpad log from earlier agents is gone (commits are intact). Relaunch rules: a slice with partial README edits in the tree gets a RESUME run that reviews the existing files against the code and the standard, completes the rest and runs the checks (R03r: 14 of 14 folders written, final checks pending; R05.2r: 9 of 13 written plus the feature-doc link replacements); a slice with nothing written relaunches fresh (R06.1, R06.2). Concurrency drops to three writers (the plan's rule when usage limits bite) | orchestrator decision (delegated) |
| 2026-09-24 | R06.1 questions: (1) whether to mount or delete the five unmounted editor panels is a product call — TF6 files one decision ticket and the READMEs state that nothing mounts them; (2) interface text such as "Mission Settings dialog" is not glossary-linked (the next plain use is); (3) 69–73-line leaves covering 5–10 files stay | orchestrator decision (delegated) |
| 2026-09-24 | V4.1's FIX-LIST goes to fix run V4.1f, which also changes R05.2's admin breadcrumbs to the rendered "A / B" and corrects the two false claims in `standards/templates/readme_domain.md`'s sample (write lock, cross-domain reads): the template is locked for structure, not for wrong facts | orchestrator decision (delegated) |
| 2026-09-24 | R06.2 questions: (1) 64–70-line leaves stay; (2) a bare test-file name after the paragraph gave its folder's full path is fine (the locked path rule allows it); (3) its code findings join TF6 | orchestrator decision (delegated) |
| 2026-09-24 | R07.1 questions: (1) the debug benches (`debug/building_viewer/`, `debug/world_los/`) keep page sections — they are the debug app's routed pages although their route components sit one level up; (2) aar and planner keep a one-clause "reserved for …" purpose; their design notes go to F08 | orchestrator decision (delegated) |
| 2026-09-24 | Operator direction: this is a documentation program — no code is written beyond the planned tooling (Phase 1 gates and the Phase 2 path pins; H1's CI wiring in Phase 7). Code problems found along the way are recorded as idea tickets, never fixed here. The deployments "No telemetry recorded" placeholder is correct while no telemetry flows; it is filed as a feature note (render the served figures once telemetry exists), not a bug | operator decision |
| 2026-09-24 | R08.1 questions: (1) content-heavy leaves (`flatten/` 105 lines with 18 files, a diagram and a key table; `validator/` 85 with a 16-rule table) stay; (2) a quoted test name containing "legacy" is a quoted identifier, fine; (3) Related documentation may link schema files and contract READMEs while `documentation_v2/website/map-engine/` does not exist (F09 creates it) | orchestrator decision (delegated) |
| 2026-09-24 | R07.2 questions: 70–80-line leaves stay; a folder holding one child folder gets a thin README | orchestrator decision (delegated) |
| 2026-09-24 | Second usage-limit stop (reset 16:00): R08.2 (11 of 11 drafted, final reflow), R08.3 (4 of 12 drafted, crdt subtree) and V4.2 stopped; their scratchpad handoff notes survived. Relaunch: R08.2r and R08.3r resume from their handoff notes; V4.2 reruns (read-only) | orchestrator decision (delegated) |
| 2026-09-24 | R08.2 questions: Markdown table rows may exceed ~100 characters (the guide is for Contents lines); boilerplate READMEs inside exempt `tests/` folders stay untouched | orchestrator decision (delegated) |
| 2026-09-24 | Recorded after the fact (V4.2): R05.2r re-pointed three model line ranges in the locked style lock (personnel 27–65 and 67–130, event manager 122–140) when its link replacements moved them — the rule text is unchanged; accepted. V4.2f rewrites the two "don't copy" entries that named the pilot's interim admin lines as generic guidance, since those files no longer carry the patterns | orchestrator decision (delegated) |
| 2026-09-24 | R08.3 questions: (1) a README states what the code does today even where that looks like a defect (hydrate keeps `connections`); the defect goes to a ticket; (2) `entity` (99 lines, 20 children) and `rows` (126, 38 children) stay | orchestrator decision (delegated) |
| 2026-09-24 | R09.1 questions: unused surface and dead functions go to the next batch as cleanup ideas; the never-advancing `tick` and the Everon-only constants are filed as a possible defect and a limitation (not as bugs); bare asset paths after the paragraph named `assets_v2/terrains/everon/` follow the path rule | orchestrator decision (delegated) |
| 2026-09-24 | V4.3's FIX-LIST goes to fix run V4.3f without item 4, which V4.2f already landed (fbc44c2a8). V4.3f also takes V4.1f's two open leftovers: `api_v2/src/missions/services/README.md:21` says every author-or-admin mission write takes the lock (the gaps are T-1030), and the operations services Used by leaves out `apps/website/api_v2/tests/user_stats_service.rs` | orchestrator decision (delegated) |
| 2026-09-24 | V4.3f questions: 101-character Contents lines stay, because the ~100-character guide is soft and V4.2f accepted 116; quoted test names with a ticket-like prefix (`t190_a_…`) are identifiers, as ruled for R08.1 | orchestrator decision (delegated) |
| 2026-09-24 | R10.1 launch additions: the facts earlier agents recorded for its crate. These are: the `//!` headers that call the map engine "the graphics engine", the broken doc link at `src/lib.rs:56`, the engine-layers gate listing 9 of 11 top-level modules (describe it as it is), and consistency with `src/data/README.md`'s feature gating. Also: the parallel writers' folders (R09.2 streaming, R09.3 world architecture and terrain) are off limits, and the crate-wide readme-coverage run is judged only on its two folder-only READMEs | orchestrator pre-launch scoping |
| 2026-09-24 | Operator direction: "We don't need a verifier for each wave." V4.4–V4.11 and V5.1–V5.5 are dropped. Quality rests on each writer's own gate checks, the orchestrator's diff spot checks, and the repo-wide gates in H1 and H2. Implementation (orchestrator): README leftovers that writers or fix runs record are collected in one closing fix run, P4f, at the end of Phase 4; G3 (the Phase 6 verifier) stays as planned | operator direction |
| 2026-09-24 | R09.2 questions: (1) 65–71-line leaves holding 5–9 behaviour files stay; (2) folders named with ticket ids get READMEs whose Contents root spells the path as it is (T-1042 renames them); (3) map streaming gets its deeper doc from F09 under `documentation_v2/website/map-engine/`, recorded as an F09 input | orchestrator decision (delegated) |
| 2026-09-24 | R09.3 questions: (1) `t935_10/` falls under T-1042, and until then READMEs spell the path as it is; (2) 59–75-line leaves stay; (3) the map-to-satellite basemap finding becomes a TF8 ticket, because it needs a browser check before it can be a known-bug entry (F17 owns `known_bugs/`); (4) linking `/assets_v2/terrains/everon/README.md` before R11.3 rewrites it is fine, because the path stays | orchestrator decision (delegated) |
| 2026-09-24 | Ticket batches split by wave so each stays under the cap: TF7 takes wave 3's findings (the frontend editor and the map-engine data), and TF8 takes the map-engine spatial, streaming, world and crate-root findings (R09.1–R10.1). R10.2 launch additions: the facts recorded for its folders (the "graphics engine" headers, the nonexistent `super::binary::BinaryError` link to `io::archives::codec::BinaryError`, the absence of `@contract` in the map engine), and R10.1's folders are off limits | orchestrator decision (delegated) |
| 2026-09-24 | TF7 questions: (1) T-946.86 (`ready`) may already be done, since `save_now` calls the duplicate-slot guard (`mission_saving.rs:44`). Its status is left unchanged FOR THE OPERATOR, like T-940.x and T-086; (2) map-engine data tickets use scope `engine/core`, as T-946.31 does; (3) "Check/Decide" tickets whose outcome may be a bug fix use class `audit` | orchestrator decision (delegated) |
| 2026-09-24 | R11.1 launch additions: its slice is 17 folders (`apps/` and `apps/website/` root READMEs, `graphics-engine/**`, `shared/**`, the `assets_v2/` root). The TSV's `apps` unit also spans the pilot's api_v2 folders, which R01a and R01b already wrote. Verify that the map engine reaches the graphics engine only through `map-engine/src/frame`, and that no frontend apps crate depends on it; wide readme-coverage runs are judged only on its own root READMEs | orchestrator pre-launch scoping |
| 2026-09-24 | R10.1 questions: (1) boilerplate READMEs inside exempt `tests/` folders stay untouched, as ruled for R08.2; (2) map-engine terms (lane, frame packet, render engine) go to F19's glossary fill as an input | orchestrator decision (delegated) |
| 2026-09-24 | R11.1 questions: (1) `assets_v2/` keeps the data kind, because its children are served data and Format plus Producers and consumers fit; P4f updates `readme_standard.md`'s example list, which names it an area root (a factual example, not a structural rule); (2) 62–69-line leaves stay | orchestrator decision (delegated) |
| 2026-09-24 | R10.3 question: the stale README files inside exempt `tests/` folders (e.g. `overlay/tests/README.md`, titled `core/pipeline/tests` and listing a missing file) stay untouched in Phase 4. Whether to delete them is collected FOR THE OPERATOR at the Phase 4 handoff, because deleting files is theirs to approve | orchestrator decision (delegated) |
| 2026-09-24 | Third usage-limit stop (the session limit, reset 21:00 local): R11.2 and R11.3 stopped before writing anything and relaunch fresh with the same prompts. R11.2 also gets the other session's untracked `contracts_v2` equipment definitions, fixtures and rules to leave out. TF8 stopped after minting T-1057–T-1059 with the inferred repo/docs scope; resume run TF8r corrects them to engine/core (T-1059 becomes `audit`) and continues | orchestrator decision (delegated) |
| 2026-09-24 | TF8r questions: (1) TF9 adds notes to T-1068 where R10.3's comment findings overlap it (`overlay/` headers, `capture.rs:209`); (2) the scopes stand (T-1066 engine/core, T-1070 engine/render); (3) R11.1's remaining graphics-engine items (history comments, unused `_tint`/`char_m`, `draw/instances.rs:62`, `draw/cull/oracle.rs:11`, `compute.rs:32`) go to TF9 | orchestrator decision (delegated) |
| 2026-09-25 | Fourth usage-limit stop (the weekly limit, reset 02:00 local): R11.2 (14 of 18 folders drafted) and R11.3 (12 of 16 drafted) resume as R11.2r and R11.3r from their handoff notes and working-tree drafts; R12 had written nothing and relaunches fresh | orchestrator decision (delegated) |
| 2026-09-25 | R11.3r question: the map-asset findings go to TF9 as separate tickets by nature (the v1/v2 satellite CI bug; orphan BLAS cleanup; the manifest/schema text), grouped only where one fix covers them | orchestrator decision (delegated) |
| 2026-09-25 | R11.2r questions: (1) the 91-line `fixtures/missions/valid/` leaf keeps its per-file table; (2) READMEs link no archived record for the schema evolution policy; F12 gives the policy a live home and the root README links it then | orchestrator decision (delegated) |
| 2026-09-25 | TF9 questions: (1) the internal sync `ticket add` runs is by design and changes nothing for idea tickets, so no ticket; (2) T-1081 stays an audit idea; whoever picks it up rechecks the plugin registrations after the other session commits its equipment export work | orchestrator decision (delegated) |
| 2026-09-25 | R13 question: "the screens run on mock catalogs" is a limitation (unbuilt wiring), not a defect, so it gets a TF10 ticket and F10's Open work; `known_bugs/` holds defects only | orchestrator decision (delegated) |
| 2026-09-25 | R14.1 mid-run addition (message): R13's verified facts for the tbd-framework root README. These are: the profile registry is used only when the mod file is missing, the registry alias prefixes, the F6–F9 key bindings, and dropping the root's ticket ids, mermaid diagram and domain-count mismatch | orchestrator (writer input) |
| 2026-09-25 | R14.1 questions: both are code or config changes, so both become TF10 idea tickets and nothing changes here: (1) a compile-gate check that no addon depends on `TBD_Framework`; (2) pinning `apps/mod/.mcp.json` to the local `enfusion-mcp@0.6.1` install | orchestrator decision (delegated) |
| 2026-09-25 | Another session is restructuring `apps/mod/tbd-export/Scripts/WorkbenchGame/{EquipmentExport, EquipmentVehicleExport, VehicleExport}`: 126 uncommitted paths, including its own untracked READMEs. R15.1–R15.3, whose slices hold those folders and the tbd-export and WorkbenchGame roots that summarise them, move to the end of Phase 4. This reorders them; nothing is deferred. Before launching them, the orchestrator checks that session's work is committed; if it is not, the question goes FOR THE OPERATOR. R15.4 (MapExport Terrain and Vegetation, clean) runs now and is told to stay out of the three folders in flux | orchestrator decision (delegated) |
| 2026-09-25 | R14.2 questions: (1) the long leaf READMEs for very large scripts stay (the guide is soft, the 500 cap holds); F10 may move depth into feature docs later; (2) the ASCII rule covers added lines only (BASE BRIEF rule 7), so P4f rewords the Systems READMEs | orchestrator decision (delegated) |
| 2026-09-25 | R14.3 question: the Pause README drops its link to the Discord identity link spec, because linking is a chat command with no layout (P4f removes it); F10 places the spec where the code lives | orchestrator decision (delegated) |
| 2026-09-25 | R15.4 question: whether the fully unregistered MapExport suite is intended stays open in T-1081 (audit) FOR THE OPERATOR's product call; the READMEs describe the committed state | orchestrator decision (delegated) |
| 2026-09-25 | TF10 questions: (1) T-946.55 (`ready`) looks done (`TBD_ObjectivesComponent.c:36-40`) and is left unchanged FOR THE OPERATOR with T-946.86, T-940.x and T-086; (2) the orchestrator replaced the personal home path in T-1095's summary and notes with neutral wording (style rule: no personal absolute paths), a text-only edit before its first commit; (3) T-1096 claims nothing unverified: its notes only frame the decision as stable or experimental | orchestrator decision (delegated) |
| 2026-09-25 | R16 questions: (1) a hard-coded path that does not exist is described by its effect, naming only the tracked file; (2) `127.0.0.1:8080` stays as a documented loopback default (the no-IP rule covers hosts, not loopback) | orchestrator decision (delegated) |
| 2026-09-25 | R17.1 questions: (1) deleting the unread `tbd-reforger.service` template or rendering from it is a code decision, so a TF11 ticket; (2) the `legacy` prose-rule failure is the baseline red in the other session's `legacy_archive.rs` and is not fixed here | orchestrator decision (delegated) |
| 2026-09-25 | R18.1 question: yes, the standard's gate grammar gains the `--with-untracked` sentence. It is a factual correction, not a structural change to the locked standard, and P4f makes it | orchestrator decision (delegated) |
| 2026-09-25 | R17.2 question: READMEs do not spell the fixed deploy prefix (a personal home path); "the fixed deploy prefix" stands, and the literal is T-1010's cleanup | orchestrator decision (delegated) |
| 2026-09-25 | R18.2 questions: (1) TF12 extends T-1097 with the six `no-crf-leak` hits and files the font and backpack GUIDs as possible false positives (audit); (2) the `ui-layouts` walk is a code bug, so a TF12 ticket, not fixed in this program | orchestrator decision (delegated) |
| 2026-09-25 | Fifth usage-limit stop (session limit, reset 10:10 local): R19 (about 13 of 20 folders drafted), TF11 (T-1098–T-1105 filed, scope and notes fixed through T-1102) and R20.1 (9 drafts, no handoff) resume as R19r, TF11r and R20.1r from their handoff notes and working-tree drafts | orchestrator decision (delegated) |
| 2026-09-25 | R19r question: another session's new untracked folders (e.g. `browser_testing/equipment_data_viewer/`, `mod_ops/equipment_gameplay/`) get their READMEs from the session that commits them, since docs ship in the same commit as their code. P4f adds the parent Contents lines for any that have landed by then; the rest are listed FOR THE OPERATOR at the Phase 4 handoff | orchestrator decision (delegated) |
| 2026-09-25 | R20.1r question: yes, the command-line template's worked sample drops `equipment-data-viewer` (P4f). It is a factual fix to the sample, since templates describe committed code | orchestrator decision (delegated) |
| 2026-09-25 | TF11r questions: (1) T-1006's scope (`repo/docs`, the fix is in xtask) is left FOR THE OPERATOR with the other existing-ticket field changes; (2) `ticket add`'s class inference from the title verb becomes a notes line on T-1009 in the next batch; (3) T-1119's "four more" count stands | orchestrator decision (delegated) |
| 2026-09-25 | Conflict raised FOR THE OPERATOR. The plan gives R21 `apps/ticketboard/src/tests/architecture_rules.rs`, and CP3a decided R21 drops that test's README half: it requires `## Responsibility`, `## Public surface`, `## Dependency rules`, `## Files` and relative links per source file in the 9 module READMEs (`:69-89`), which the README standard does not have. The operator's later no-code direction does not list this test edit among its exceptions. The ticketboard module slices (R21.1's `application` and `core`, R21.2, R21.3) wait for the answer; R21.4 (ticket-engine, verification-core) runs meanwhile | orchestrator decision (delegated) |
| 2026-09-25 | Operator answer: allow the ticketboard test edit. R21.1 also owns `apps/ticketboard/src/tests/architecture_rules.rs`: it removes only the README-format half (`:69-89`, the four sections and the per-file links) and keeps the module-layout half, so readme-coverage is the single README checker. R21.1 runs first; R21.2 and R21.3 follow once it is committed | operator decision |
| 2026-09-25 | R20.3 question: the 66-line Doors leaf keeps its per-variant resolver table | orchestrator decision (delegated) |
| 2026-09-25 | R20.2 question: which chunk index paths are authoritative (the committed `.bin` or the builder's `.json.gz`) is a code and data decision FOR THE OPERATOR; TF12 files it as an audit that states both readings | orchestrator decision (delegated) |
| 2026-09-25 | R21.4 found our own defect: TF agents appended `notes` after `created_at` in amended tickets, breaking ticket-engine's canonical key order (`corpus_roundtrip_real_tree_byte_identical` fails on T-1004). TF12, which is running, gains extra ownership (by message): the key order of every ticket whose `notes` sits after `created_at`, with values unchanged, until the round-trip test passes. Earlier batches' prompts lacked the rule | orchestrator decision (delegated) |
| 2026-09-25 | R21.4 questions: whether ticket-engine and verification-core tests join the wave gate or CI, and whether `run_estimates` backs a future command or goes, are code decisions; a later ticket batch (TF13) files them | orchestrator decision (delegated) |
| 2026-09-25 | R21.1 questions: (1) its one extra test line (`architecture_rules.rs:57`, allowing `README.md` beside `main.rs` in ticketboard's `src/`) is accepted under the operator's approval: it serves the same purpose, making readme-coverage the single README checker, and without it the `src/README.md` the gate requires fails the flat-source check. Reported to the operator; (2) `tools_v2/README.md` keeps its links to the archived tools_v2 plans until F12 writes a live tools_v2 architecture doc and replaces them | orchestrator decision (delegated) |
| 2026-09-25 | P4f question: the standard's area-root rule reads "the top of a code tree", which literally covers `assets_v2/`, while the example list now calls it data. Clarifying the rule text is a change to the locked standard and is held FOR THE OPERATOR at the next unlock; the example stands | orchestrator decision (delegated) |
| 2026-09-25 | TF12 questions: (1) every later ticket-filing prompt carries the canonical-form rule: `notes` above `created_at`, canonical quoting, and `hcargo test -p ticket-engine corpus_roundtrip_real_tree_byte_identical` green before finishing; (2) the slightly loose line numbers in the T-1123, T-1130 and T-1132 summaries stand, since their notes lines are exact | orchestrator decision (delegated) |
| 2026-09-25 | R15.1–R15.3 re-sliced: R15a takes the tbd-export folders the other session is not changing, and runs now. The folders in flux (EquipmentExport, EquipmentVehicleExport, VehicleExport) and the roots that summarise them (the tbd-export, Scripts and WorkbenchGame root READMEs) wait for the operator's call, because the other session still has 127 uncommitted paths there, including its own new READMEs | orchestrator decision (delegated) |
| 2026-09-25 | Operator answer: leave the tbd-export folders in flux to the other session (docs ship with code). That session owns the READMEs of `EquipmentExport`, `EquipmentVehicleExport` and `VehicleExport`. This program writes only the tbd-export, Scripts and WorkbenchGame root READMEs after it commits, then checks the rest against the standard. R15.1–R15.3's remainder is therefore conditional (R15r) | operator decision |
| 2026-09-25 | R21.2/R21.3/R15a questions: the gap-analysis watch, the viewer's copies of engine logic and the subregion file are code decisions for TF13 tickets; R15a's 130- and 107-line leaves keep their action and format tables; MapExport/README keeps "no script here writes" the subregion file (T-1081 holds the rest) | orchestrator decision (delegated) |
| 2026-09-25 | P4f2 questions: (1) F11 writes the folder-index READMEs of `documentation_v2/mod/tbd-export/…/verification_evidence/**` (they are documentation, not the other session's code); (2) `documentation_v2/website/frontend/apps/` goes to F08 and `pages/` to F04; (3) `eden_editor_reference/`'s README goes to F08, whose area covers the folder, while F07 writes only its gap-analysis file | orchestrator decision (delegated) |
| 2026-09-25 | Phase 5 re-sliced from Phase 4's measured cost and the source sizes: F01 needed two runs for about 1,100 source lines, and verification against the code dominates. F04, F07, F08, F09, F10, F13, F14, F15, F16 and F17 each split in two, for 28 doc writers. The program record is `documentation_v2/refactor_phase5_slices.tsv`. Writers run in rolling slots (at most 3) in `order`; a writer marked "after X" starts only once X is committed. With no wave verifiers, quality rests on each writer's gates, the orchestrator's diff checks and H1/H2. Each doc-writer prompt adds: the style lock, the no-code rule, links to the Phase 4 READMEs rather than repeated code detail, the findings its slice's roster and amendment rows record (the writer greps the checkpoint for its id), and handoff notes every ~3 files with a 400k stop | orchestrator decision (delegated) |
| 2026-09-25 | TF13 questions: (1) merging T-1150 into T-1024 is a registry change left FOR THE OPERATOR; (2) T-1141 stays as an audit idea, since closing it is the operator's call | orchestrator decision (delegated) |
| 2026-09-25 | F03 questions: (1) the create dialog's facts fold into the library doc, since the dialog has no route; (2) decisions stay as bullets inside each feature doc, as the pilot does. Phase 5 README leftovers outside a writer's Related-documentation ownership go to a closing fix run, P5f | orchestrator decision (delegated) |
| 2026-09-25 | F02 questions: (1) the status of T-940.1/.2/.3/.12 is FOR THE OPERATOR with the other stale-status tickets; (2) the invented server-intel lines go to TF14 with the rest, as a bug | orchestrator decision (delegated) |
| 2026-09-25 | F09a questions: (1) cancelling or re-scoping T-940.x and T-906 is FOR THE OPERATOR; (2) F09b gains extra ownership of the `map-engine/` and `graphics-engine/` Contents lines in `documentation_v2/website/README.md` | orchestrator decision (delegated) |
| 2026-09-25 | Orchestrator slip: F09a's commit 822f8af4d also carried F04a's four `git mv` archive renames, which were already staged in the index (renames only, no content change). From now on the orchestrator checks `git diff --cached --name-only` before every commit | orchestrator decision (delegated) |
| 2026-09-25 | F04a questions: (1) `archive/go_and_react_era_design/` stays the topic for the four archived frontend hub files; (2) no extra backlinks for `core/api/{client,dto,endpoints}` or `test_support/class_r_scrub` | orchestrator decision (delegated) |
| 2026-09-25 | F05 started four research helpers of its own; their completion notices reach the main session, not F05, which then waits forever. The orchestrator messaged F05 where the results are (the last assistant message in each helper's transcript) and told it to start no more helpers. Later writer prompts say: no helper agents | orchestrator decision (delegated) |
| 2026-09-25 | Sixth usage-limit stop (the session limit, reset 15:10 local): F04b, F05 and F09b are resumed by message with their context intact, rather than relaunched. Resuming this way keeps the agent's verified facts, so it replaces fresh resume runs from now on | orchestrator decision (delegated) |
| 2026-09-25 | F09b questions: T-907 (ready) and T-937 (queued) look met by the code; closing or re-scoping them is FOR THE OPERATOR. F08a launch addition: the Eden catalogs describe an external product, so Eden facts are kept, not verified against our code; only claims about the Mission Creator are verified | orchestrator decision (delegated) |
| 2026-09-25 | F05 questions: (1) reading "A–M" as the first letters of the area names stands (PERF goes to F06); (2) T-715, T-720 and T-939.2 are FOR THE OPERATOR with the other stale-status tickets | orchestrator decision (delegated) |
| 2026-09-25 | F08a questions: (1) yes, "2D/3D" goes (P5f fixes the editor README; F19 the glossary); (2) the scrape manifest header goes to F07b, which owns the gap analysis in the same folder | orchestrator decision (delegated) |
| 2026-09-25 | F07a questions: (1) whether the mission armory derives from the slot loadouts is a product question, carried to F19 and FOR THE OPERATOR; (2) the copy-based archive of the execution half stands, because `decisions.md` keeps its path, which tickets cite; F07b cuts it to the decisions log | orchestrator decision (delegated) |
| 2026-09-25 | F06 questions: (1) T-083, T-146 and the gap analysis's cancelled or shipped owner tickets are FOR THE OPERATOR, and F07b refreshes the gap rows; (2) connections get their own area file, `feature_inventory/connections.md`, which P5f writes with verified entries | orchestrator decision (delegated) |
| 2026-09-25 | F08b questions: whether to file a planner ticket, and where T-136's replay lives (`apps/aar/` or a routed page), are product decisions FOR THE OPERATOR, also carried to F19 | orchestrator decision (delegated) |
| 2026-09-25 | The desktop app crashed; F07b and F10a stopped mid-task and are resumed by message with their transcripts intact. F08b's commit had not run, so it is committed after the crash | orchestrator decision (delegated) |
| 2026-09-25 | F07b questions: (1)–(3) T-146, T-083 and the OBJ-SKILL authoring question are FOR THE OPERATOR; (4) the source decisions log (lines 296–369) is not added to the frozen archive copy, because git history holds it (HEAD a4235fe16) | orchestrator decision (delegated) |
| 2026-09-25 | F11 questions: (1) moving the old evidence README verbatim to `implementation_acceptance.md` and making the README a folder index is accepted; (2) yes, F18's `runbooks/README.md` indexes the terrain export runbook. F10a's staged `git rm` deletions were unstaged before F11's commit and are committed with F10a | orchestrator decision (delegated) |
| 2026-09-25 | F10a questions: its three `git rm` deletions were unstaged before F11's commit and land here as plain deletions; the regenerated `capability_matrix.tsv` line is not committed (`.ai/artifacts/` is out of scope) and goes to a ticket | orchestrator decision (delegated) |
| 2026-09-25 | Writer prompts from F10b on add: no `git rm`/`git mv` unless granted (delete with plain `rm`), because staged entries leak into other agents' commits; and never run `enf capability`, which rewrites an out-of-scope artifact | orchestrator decision (delegated) |
| 2026-09-23 | P2-5 and P2-6: `git mv` keeps link text, so a relative link inside a moved file is resolved against the file's pre-move path (the manifest's source column) before it is rewritten. P1-1's archived records hold three such links: `archive/tools_v2_refactor/analysis_and_inventory.md:5` and `architecture_plan.md:5` (sibling names that are now snake_case) and `phase_one_handoff.md:12` (`../docs/tools/editor_capture.md`, manifest row 816) | P1-1 finding |
| 2026-09-23 | P1-2 launch additions: ownership of `tools_v2/ticket-engine/src/metrics/estimates/git_changes.rs` (the only other user of `GENERATED_QUEUE_VIEW_PREFIX`, which the plan renames `RETIRED_QUEUE_VIEW_PREFIX`); `tools_v2/ticket-engine/src/validation/**` and its tests (the child-ticket spec and plan check), with additive-only changes in `tools_v2/ticket-engine/src/registry/ticket_file_storage/` if validation needs a helper that lists child ticket files; the `spec` field of `.ai/tickets/T-068.10.5.toml` and `.ai/tickets/T-159.15.0.toml` (through a `ticket` verb if one sets it); the view claims at `tools_v2/README.md:6` and `tools_v2/ticket-engine/README.md:3,12` (the plan's `src/README.md` does not exist; the crate README is meant); one extra check, `hcargo xtask ticket sync`, which must write no view file, with any queue.json, ROADMAP-marker or gap-analysis diff listed as sync output. Added at launch: a `git rm` grant for exactly the eight deletions (`sync/queue_views.rs`, `sync/registry_views.rs`, the five `docs/TICKET_*.md` views, `docs/MILESTONES.md`); ownership of `tools_v2/ticket-engine/src/sync/tests/**` in case a test names a removed item; two more checks, `hcargo build -p xtask` (downstream crate) and `hcargo test -p xtask tooling_prose_rules` (P1-2 edits tools_v2 Markdown; only the two baseline failures may remain) | orchestrator pre-launch scoping |

## Phase handoffs

Written by the orchestrator at each phase (and Phase 4–5 wave) boundary, before the main session is compacted. Each block names: commits landed, decisions and amendments, environment state (other sessions, xtask build status, dirty files), open questions, and the exact next agents to launch with any prompt adjustments. A fresh session resumes from the newest block.

### Phase 0 handoff (2026-09-23) — Phase 0 closed, CP1 answered

**Commits:** f8511271c (program plan, writing brief, this file) · 10f15eb5f (P0-1 row, first amendments) · d2ce2160b (phase handoff protocol) · 660b70833 (move manifest, ticket rewrites, orphan-spec links, pin catalogue, CP1 questions) · f79a84a42 (CP1 answers 1–3 applied by P0-5f, `refactor_followup_tickets.md`, plan evidence-folder wording).

**Decisions:** CP1 answered — the operator accepted all 21 recommendations (`refactor_move_manifest/cp1_questions.md`; rules in the writing brief's "Checkpoint 1 decisions"). Every amendment above applies from Phase 1 on.

**Environment at close:**
- Another session holds ~161 uncommitted entries (equipment data viewer, equipment export, equipment gameplay) across api_v2, frontend, tools_v2, apps/mod and contracts_v2. Shared dirty files: `CLAUDE.md`, `.gitignore`, `apps/website/api_v2/.env.example`, `apps/mod/tbd-export/resourceDatabase.rdb` (never stage the last one). Stage any shared file hunk-wise.
- `hcargo build -p xtask` exits 0 (log `<scratchpad>/logs/orchestrator/xtask_build_phase0_end.log`); it was broken earlier the same day by the other session's in-flight `equipment_gameplay` module, so re-check before each Phase 1 agent.
- Red, not ours: `verify-coding-standards` route-tag check (the other session's `equipment_data_viewer` routes); `tooling_prose_rules::nothing_narrates_its_own_history` (`equipment_vehicle_export/legacy_archive.rs`).
- Red, fixed by Phase 1: `only_a_layout_module_spells_a_repository_path` (P1-4), `every_rust_file_named_in_prose_exists` (P1-1).
- `docs/` has no uncommitted changes; the API v2 completion session writes `docs/verification/api_v2/` — pause it before Phase 2.
- Everything Phase 1+ needs is committed under `documentation_v2/refactor_*`; the scratchpad intermediates are not needed.

**Next — Phase 1, sequential, one green commit per agent:** P1-1 → P1-2 → P1-2b → P1-3 → P1-4 → P1-5 → P1-6. Prompts are in the plan's Phase 1 section, adjusted by the amendments:
- Before each agent: `hcargo build -p xtask` exits 0; if the other session breaks it again, stop and ask the operator.
- P1-1 also owns `tools_v2/README.md` (lines 13-18).
- P1-2 also extends spec/plan existence validation to every ticket file (parents and children) and first repoints T-068.10.5 → `.ai/artifacts/t068_10_5_weapon_families.md` and T-159.15.0 → `.ai/artifacts/t159_15_render_loop_handoff.md`.
- P1-2b (new): widen `.ai/tickets/schema.json:18` and `tools_v2/ticket-engine/src/sync/gap_analysis.rs:12` to `[0-9]{3,}` with tests; then file the three tickets in `documentation_v2/refactor_followup_tickets.md` top-level (rename program → deferred; mod wave gate Makefile → idea; human cleanup of 44 branches, 6 worktrees and the 4 merged-but-ready tickets → idea).
- P1-4: `API_READINESS_EVIDENCE_PREFIX` keeps the value `docs/verification/api_v2/` until P2-2 sets `documentation_v2/website/api_v2/verification_evidence/`.
- P1-5: treat `documentation_v2/refactor_*` as records; evidence folders are named `verification_evidence/`.
- Budget: stop agents near 450k tokens (P0-5 overran at ≈645k); keep scopes narrow.

### Phase 1 handoff (2026-09-24) — Phase 1 closed (orchestrator under operator delegation)

**Commits:** 2574b0ed2 (P1-1) · d6af2b08e (P1-2, P1-2f) · 83468bbec (P1-2c) · dc9063c64 (P1-2b) · 183f098e3 (P1-2d) · fde9ec46a (P1-3, P1-3f) · d4467ec4f (P1-4) · c4bed34ca (P1-5a) · ee4945304 (P1-5af) · c2345fdd2 (P1-5b) · 8e92b50e3 (P1-5bf) · 4e8abf897 (P1-5c) · 9d258455e (P1-5cf) · 6dc849532 (P1-6, P1-6f) · P1-6g (see its roster row).

**Decisions:** the operator answered P1-2's three questions, then delegated the rest of the program ("Operator delegation (overnight run)" amendment). Every later decision is an amendment row marked "orchestrator decision (delegated)"; every launch-prompt addition is an amendment row marked "orchestrator pre-launch scoping".

**Environment at close:**
- Peer sessions (ListAgents): API v2 completion, OFCRA/OMTK mod refactoring, move-spotify-cache — all idle. The other session's uncommitted work (equipment data viewer, export, gameplay) is unchanged; never stage it.
- Red, not ours: `tooling_prose_rules::nothing_narrates_its_own_history` (mod_ops/equipment_vehicle_export), `mod_ops::equipment_gameplay::tests::gameplay_preserves_numeric_tokens_and_rejects_broken_publication` (untracked module), `verify-coding-standards` route tags (12, untracked equipment_data_viewer handlers).
- Documentation gates (not yet in CI), pre-cutover baseline: readme-coverage 795 violations (387 folders without README, 408 READMEs without a matching Contents block); markdown-placement 40 (38 `.md` in code trees, 869 tracked files under `docs/`, the 931-line blueprint); link-check 820 breaks (26 missing targets, 791 backticked paths — 764 in the blueprint `documentation_v2/ANALYSIS_AND_INVENTORY.md`, 3 cited commands).
- Manifest check: 875 `docs/` rows = 869 tracked `docs/` files + the 6 views P1-2 deleted (rows `done-by-P1-2`); 7 `done-by-P1-1` rows; 6 ORDER:1/ORDER:2 pairs; 5 NEAR-DUPLICATE collapses; 3 `.ai/artifacts` spec rows.

**Next — Phase 2.** Commit 2a is ONE commit after P2-4 passes: P2-1 → P2-2a → P2-2b → P2-3 → P2-4. Then commit 2b: (P2-5 ∥ P2-6) → P2-7. Then CP2 (delegated; the live server-address check stays with the operator).
- Pre-flight before P2-1 (replaces the operator's confirmation): ListAgents shows every peer session idle; `git status --porcelain -- docs` is empty; `hcargo build -p xtask` exits 0.
- P2-1: apply every manifest row exactly (skip `done-by-P1-1`/`done-by-P1-2`); vacate ORDER:1 paths before ORDER:2 moves; collapse the 5 NEAR-DUPLICATE rows per their note (sha differs by design); `git mv` only, no content edits.
- P2-2 splits into P2-2a (pin catalogue rows of kind code, test and config: the three `documentation` layout modules — match rows by constant name, line numbers drifted — fixtures, `include_str!`, ticketboard, config files; `LAYOUT_TARGET_DIR` repointed; `TREE_DIR` repointed and re-exported into xtask, merged with `DOCUMENTATION_ROOT`) and P2-2b (rows of kind comment: api_v2, fleet agent, developer-tools, xtask texts, EnfScript comments with ASCII-only added lines and `mod compile`). The existence and classification tests of P1-4 bind every repointed constant.
- P2-3 as planned (rewrite TSV, orphan-spec decisions, wave repack, sync, check --strict).
- P2-4 as planned plus the three gates: markdown-placement must show `docs/` gone and no `.md` besides README.md in code trees.
- After commit 2a: message the API v2 completion session (SendMessage) with its new resume path (`documentation_v2/website/api_v2/verification_evidence/progress_checkpoint.md`) and update memory (`MEMORY.md` API v2 line, `api-v2-completion-program.md`, `feedback-api-v2-completion-subagent-slices.md`).
- P2-5: resolve relative links inside moved files against their pre-move path (manifest source column) before rewriting.
- Budget: stop agents near 430k; P1-5a/P1-5b/P1-2b/P1-6 each reached 420–480k.

### Phase 2 handoff (2026-09-24) — Phase 2 closed, CP2 decided under delegation

**Commits:** 19738aff0 (commit 2a: P2-1 mover, P2-2a/P2-2b pins, P2-3/P2-3f tickets, P2-4 verifier) · b2f08dffd (checkpoint) · f996f8583 (commit 2b: P2-5/P2-5f frozen links, P2-6/P2-6f live links, P2-7 verifier).

**State:** `docs/` is gone; every tool reads `documentation_v2/`; ticket registry, waves and sync outputs point at the new paths. Frozen corpus (`documentation_v2/tickets/**`, `documentation_v2/archive/**`): 0 link breaks, status lines on all 576 files. Live corpus: 248 content-level breaks, listed by writer in `<scratchpad>/logs/P2-6/work/breaks_by_writer.txt` (F15 55, F07 48, F17 30, P3-2 22, F16 18, F04 15, …). Gates: markdown-placement 8 (live docs over 500 lines — F-writers split them), readme-coverage 901 (Phase 4/5), link-check 248.

**Environment:** the API v2 completion session was told its new resume path (it and OFCRA/OMTK were idle). The other session's uncommitted equipment work is untouched. Red, not ours: the history-word prose rule and the equipment_gameplay test; `mk ci-local-leptos` stops at fmt on the other session's untracked data_viewer code.

**Open for the operator:** CP2's live server-address check; every "orchestrator decision (delegated)" row in the amendments table.

**Next — Phase 3.** P3-1 splits: P3-1a (the README standard + six README kind templates: area root, crate root, domain, leaf, page, app) → P3-1b (the other six kinds: command-line, data, mod scripts, mod assets, deploy/config, documentation_v2 folder index; the six document templates; the templates folder README; the archive status line of `frontend_page_spec_template.md`). The Contents grammar is copied verbatim from `tools_v2/xtask/src/verifications/documentation/README.md`. Then CP3a (delegated: a reviewer agent's verdict), P3-2, the pilot R01 ∥ F01, P3-3, CP3b (delegated).

### Phase 3 handoff (2026-09-24) — Phase 3 closed, CP3a and CP3b decided under delegation

**Commits:** 7bb4a521e (P3-1a) · af2c55d3e (P3-1b) · 66037b77e (P3-1v, P3-1f — CP3a) · 1e7bd2c2f (P3-2) · dbbb08790 (R01b) · f1c3c8372 (R01a) · a4db61abb (F01, F01b) · 221d22e4c (P3-4, `--with-untracked`) · P3-3/P3-3f (this block's commit) · tickets T-1005–T-1029 in 305ce576f, 07654169c, 6c30f20e6, 167caf418, 3c20c184a.

**State:** the README standard (`standards/readme_standard.md`) and 17 templates are locked; `standards/documentation_standards.md` and `glossary.md` exist; the api_v2 crate (42 folders) and the administration pages are written to the standard; the style lock (`refactor_style_lock.md`, 13 rules) binds Phases 4–5. Gates: the pilot folders pass; `--with-untracked` lets writers check new files before a commit.

**Lessons:** writers cost 15–30k tokens per folder and overshoot a prompt's stop (R01a ≈523k, P3-2 ≈571k); research-heavy slices are split or handed off (F01 → F01b); code findings go to ticket batches (TF1–TF5), never into this program's code.

**Open for the operator:** the CP2 server-address check; every "orchestrator decision (delegated)" row; T-940.1/.3/.11/.12 and T-086 look superseded (not changed).

**Next — Phase 4:** 43 README writers in 11 waves (`refactor_phase4_slices.tsv`), up to four writers per wave, then the wave's verifier V4.n (it may overlap the next wave's writers). P3-3g (the entry README's Contents) runs beside W4.1. Writer prompt: the plan's README writer template + concrete values + the locked standard, templates, glossary and style lock + `--with-untracked` on its own slice (ignore another session's untracked paths) + committed code only + the slice's recorded facts (grep the roster and amendments for its id) + a handoff-and-stop near 350k tokens.

### Phase 4 handoff (2026-09-25) — Phase 4 closed except R15r (conditional) and TF13 (running)

**Commits:** the README writers R02–R21.4, R15a (the committed-view READMEs of every code folder in apps/, tools_v2/, contracts_v2/, assets_v2/); fix runs V4.1f, V4.2f, V4.3f, P4f (eaee28787), P4f2 (94a07725d); ticket batches TF6–TF12 (T-1030–T-1136); the approved ticketboard test edit (ba7b12cc8). See the roster rows for each commit.

**Decisions:** per-wave verifiers dropped by the operator after V4.3 (quality = writers' gates + orchestrator diff checks + H1/H2); the operator approved removing the README-format half of ticketboard's architecture test (plus the one line allowing `src/README.md`); the operator left the tbd-export folders in flux (EquipmentExport, EquipmentVehicleExport, VehicleExport) to the other session — this program writes only the tbd-export, Scripts and WorkbenchGame root READMEs once that session commits (R15r), then checks the rest against the standard. Ticket files must keep ticket-engine's canonical form (`notes` above `created_at`, canonical quoting; TF12 repaired 83 files).

**State:** committed-view gates (P4f2): zero Phase 4 misses; remaining readme-coverage and link-check violations belong to Phase 5 writers (counts per writer in the P4f2 row), the other session's tbd-export folders, and Phase 6 (`.ai/tickets/*.md`, `.cursor/**`, root README). Five usage-limit stops were survived by resume runs from scratchpad handoff notes. The other session still holds 127 uncommitted paths under apps/mod/tbd-export plus equipment folders in assets_v2, contracts_v2, xtask mod_ops and developer-tools browser_testing (their READMEs and parent Contents lines come with its commit).

**Open for the operator:** the CP2 server-address check; every "orchestrator decision (delegated)" row; T-940.1/.3/.11/.12, T-086, T-946.55 and T-946.86 look superseded or done (not changed); T-1006's scope; T-1081 (all MapExport plugins unregistered), T-1123 (chunk index `.bin` vs `.json.gz`) and the other audit tickets; the stale READMEs inside exempt `tests/` folders (delete or keep); the area-root rule text in the locked standard.

**Next — Phase 5:** 28 doc writers per `refactor_phase5_slices.tsv`, rolling slots of at most 3 in `order`, "after X" rows only once X is committed; then CP4 (F19 roadmap) and F19b. Doc-writer prompt: the plan's doc writer template + the slice's targets and sources + the locked standard, templates, glossary, style lock + no code + link the Phase 4 READMEs instead of repeating code detail + the findings recorded for the writer (grep the checkpoint for its id, e.g. "F10 input") + shared pending_merge folders split by target (the "a" writer takes the files matching its targets, the "b" writer the rest) + handoff notes every ~3 files and a 400k stop. TF13 (running) closes Phase 4's ticket batches; R15r waits on the other session.

## Checkpoints

| Checkpoint | Follows | The operator reviews | Status |
|---|---|---|---|
| CP1 manifest | P0-5 | `refactor_move_manifest.md` and its questions; P0-5 applies the answers in a fix-agent run | answered 2026-09-23 — all 21 recommendations accepted; P0-5f applies 1–3 |
| CP2 cutover | P2-7 | commits 2a and 2b; ticketboard's spec reader; the server address, identified by its SSH host key | decided 2026-09-24 under delegation: 2a (19738aff0) and 2b pass their verifiers after fix runs; ticketboard's spec reader verified by code reading and a scripted check (770 spec/plan values, all tracked `.md`); the live server-address check stays OPEN for the operator (no remote commands) |
| CP3a standard | P3-1 | the README standard and its samples; changes go through a P3-1 fix run | decided 2026-09-24 under delegation: reviewer P3-1v's 15 findings applied by P3-1f; the standard is locked (the pilot and P3-3 exercise it before CP3b) |
| CP3b pilot slices | P3-3 | the R01 and F01 pilot output; the style locks | decided 2026-09-24 under delegation: the pilot passes after P3-3f; the style lock is `documentation_v2/refactor_style_lock.md` |
| CP4 roadmap | V5.5 | `product_roadmap.md`; the operator strikes unplanned items and F19b applies the strikes | pending |
