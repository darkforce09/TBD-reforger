<!-- AUTO-GENERATED by cargo run -q -p xtask -- ticket sync — DO NOT EDIT -->


# Ticket Brainstorm

`idea` + `deferred` only.

## program

- **T-068** (deferred) — Virtual Arsenal (registry + loadout export) [] — Cargo ladder shipped (T-068.15.1 @ 85acbb13 → .15.2 @ 4fb156b7 → T-068.11 @ c66494c6 → T-068.12 @ 0be53e16). T-068.13 LOBBY picker moved to T-181.9.1; LOBBY slot picker → T-068.14 Phase-2 E2E. Hub: t068_virtual_arsenal_program.md + t068_15_cargo_program.md. BLOCKED: the only remaining slice is T-068.14 (executor: human) — a live two-client E2E on a dedicated server. Not agent-actionable; moved off `ready` on 2026-07-26 so the fleet does not pick it up.
- **T-181** (deferred) — TBD Framework — Arma-3-parity event mod (factory + spine) [] — Rebuild the Arma 3 mission workflow inside Reforger: website Mission Creator = Eden, compiled mission JSON = the .pbo, and the mod supplies everything Reforger does NOT ship (lobby, briefing, slotting, spawn, spectator, admin). Stage 1 FACTORY shipped: host-aware execution, a 1.3s headless Enfusion compile gate on the native dedicated server (no Workbench), a mechanical CRF symbol index (266 files / 71,606 LOC), and a capability matrix whose UNTRIAGED gate turns a forgotten capability into a build error. Stage 2 is the TBD-owned event spine (ONE LIFE + admin respawn; macOS-methodology UI on Aegis tokens). CRF is an Arma-Public-License ORACLE — indexed, never vendored. ABSORBS T-068.13. BLOCKED: the only remaining slice is T-181.16 (executor: human) — a live two-client E2E on a dedicated server. Not agent-actionable; moved off `ready` on 2026-07-26 so the fleet does not pick it up.
- **T-946** (idea) — Wave close: child tickets, ledger-seated labels, batch ship [] — wave --close could not close any wave: the close-time view saw parent tickets only, lock labels ran past what the marker oracle accepts, and per-id repacks stopped a wave ever emptying.

## work

- **T-083** (deferred) — Top menu bar [] — Eden-style top menu bar with file/edit/view stubs.
- **T-085** (deferred) — Wiki markdown renderer [] — Render doctrine wiki pages from markdown at /wiki.
- **T-086** (deferred) — Server Control + RCON API [] — Live server control panel wired to RCON backend.
- **T-087** (deferred) — CMS rich text editor [] — Rich text editor for announcements and CMS content.
- **T-088** (deferred) — Multi-server picker [] — Select among multiple game servers in intel views.
- **T-093** (deferred) — Continuous autosave polish [] — Background autosave UX and conflict handling polish.
- **T-094** (deferred) — Typed-array IconLayer [] — Typed-array slot icon buffer for mega-mission render path.
- **T-096** (deferred) — Live game-server telemetry bridge [] — Bridge live game-server events into telemetry ingest.
- **T-110** (deferred) — Terrain base + sparse deltas [] — Binary terrain base layer plus sparse delta props for 1M+ map objects.
- **T-121** (deferred) — Terrain DEM export automation [] — Follow-ons: Arland re-export, optional game-mode heightmap fallback. MCP hardening shipped @ e7e7232 (mcp-call/daemon/consume/selftest). Core GetSurfaceY plugin shipped in T-091.0. Spec: t121_terrain_dem_export_automation.md.
- **T-205** (deferred) — Vehicle seats, crew roles, turret bindings [] — Vehicle data has no seats, crew roles, cargo capacity or turret bindings
- **T-206** (deferred) — Item data is 78% empty [] — Item data is 78% empty
- **T-327** (deferred) — Chat shows `#tbd link` before TBD can suppress [] — `#tbd link` code is visible in chat before TBD can suppress it
- **T-404** (deferred) — Doc/comment hygiene: three in-code statements that are now false [] — Doc/comment hygiene: three in-code statements that are now false
- **T-652** (deferred) — Rocks are not rendered [] — Rocks are not rendered
- **T-707** (deferred) — wave.sh test-split comment transposes its own measurement [] — wave.sh test-split comment transposes its own measurement
- **T-708** (deferred) — capture shot: hang-fallback latency regressed 25s to 130s [] — capture shot: hang-fallback latency regressed 25s to 130s
- **T-709** (deferred) — capture zoomsweep lost the per-zoom console error tap [] — capture zoomsweep lost the per-zoom console error tap
- **T-710** (deferred) — capture port: pin the parity-bearing pure functions with tests [] — capture port: pin the parity-bearing pure functions with tests
- **T-711** (deferred) — Doc sync: t090 §N3 and t152_7 contour references [] — Doc sync: t090_render_lod_contract §N3 superseded by T-639; t152_7 stale contour reference
- **T-712** (deferred) — Contour band: honest annotations and default view acceptance [] — Contour band: honest constant annotations + sample the default view in acceptance
- **T-713** (deferred) — Summit rings invert on depressions; crater basins render dark [] — Summit-ring rule inverts on depressions — every crater ring renders dark
- **T-714** (deferred) — Contour re-march hitch: 0.66-1.3 s per rung crossing [] — Contour re-march hitch: 0.66-1.3 s native per rung crossing (est 1-4 s wasm)
- **T-715** (deferred) — Hidden-layer slots vanish from Outliner/ORBAT docks instead of dimming [] — Hidden-layer slots vanish from Outliner/ORBAT docks instead of dimming
- **T-716** (deferred) — Context menu honesty: Go Here, multi-select, keydown field hijack [] — Context menu honesty: Go Here goes elsewhere; enabled rows dead on multi-select; keydown hijacks fields
- **T-717** (deferred) — Continue-without-map before hydrate resurrects the boot overlay forever [] — Continue-without-map before hydrate resurrects the boot overlay forever
- **T-718** (deferred) — Slot removal crew residue; picker flags; undo Alt lies [] — Crew residue: scrub seats on slot removal; seat-picker honesty; undo-step and Alt comment falsities
- **T-719** (deferred) — Debug HUD: invisible under DockRight; AltGr chords spuriously toggle it [] — Debug HUD: invisible under DockRight; AltGr chords spuriously toggle it
- **T-720** (deferred) — Layer-drag latch survives out-of-dock release; next + click silently reparents [] — Layer-drag latch survives out-of-dock release; next + click silently reparents
- **T-721** (deferred) — Status bar under docks: blur, dead OPEN, Eden fix [] — Status bar runs under both docks — readouts blurred, OPEN click-dead
- **T-722** (deferred) — Route sweep passes typo URIs; gate never validates concrete URIs [] — Route sweep passes on a typo-d URI — completeness gate never validates URIs
- **T-725** (deferred) — Zoom labels lag; northings under bar; hide Failed scale fallback [] — Furniture polish: zoom channel lags 1s on still-pointer wheel; northings hide under the bar; Failed-state fallback
- **T-728** (deferred) — Save-composition affordance reads a channel selection never bumps [] — Save-composition affordance reads a channel selection never bumps
- **T-729** (deferred) — Owner-line materialize per frame; zones mislabels triggers [] — Owner-line overlay runs a full materialize per frame; zones panel mislabels trigger drafts
- **T-730** (deferred) — LoS asymmetric clear, SnapReadout overlap, dead ViewshedState [] — LoS sub-mode residue: asymmetric clear, SnapReadout in the cursed corner, write-only ViewshedState
- **T-731** (deferred) — ROW_ACTIVE border-t skews virtual tree by 1px [] — ROW_ACTIVE border-t skews the virtual tree window by 1px per selected row
- **T-734** (deferred) — Validation panel: full-compile cost, wasm panic, seam pin [] — Validation panel at scale: per-edit full compile, wasm panic honesty, seam pin
- **T-825** (deferred) — Design session scopes universal outliner list Attributes edit rule [] — PROGRAM: everything placed is an outliner citizen; all editing is Attributes; the right dock only places
- **T-829** (deferred) — v5_orbatWindowed smoke fails under verifier harness [] — Gate smoke virtual-outliner v5_orbatWindowed fails deterministically under the verifier harness — config-sensitive smoke
- **T-835** (deferred) — No-Widget button should show select-cursor glyph [] — The No-Widget button wears the select-cursor glyph, not a 'none' symbol
- **T-840** (deferred) — Draft chip saved just now on boot before edit [] — The draft chip says 'saved just now' on boot of a content-bearing mission — before any edit
- **T-844** (deferred) — Vehicle delete no-op; fold into T-837 [] — Delete does nothing to a selected vehicle — delete_selection partitions comments and slots only
- **T-846** (deferred) — role_notice query is written on editor denial but never read [] — role_notice query is written on editor denial but never read
- **T-847** (deferred) — push_drag_preview Class-R pins omit bind_squad_link_preview [] — push_drag_preview Class-R pins omit bind_squad_link_preview
- **T-851** (deferred) — Delete orphaned placed_vehicles_panel after T-818 removed the Placed strip [] — Delete orphaned placed_vehicles_panel after T-818 removed the Placed strip
- **T-852** (deferred) — Attributes modal paint should use modal_stack::z_class instead of hard-coded z-50 [] — Attributes modal paint should use modal_stack::z_class instead of hard-coded z-50
- **T-931** (deferred) — Seat zoom viz deferred [] — Seat zoom viz (glyphs/numbers/crewed occupancy) — operator deferred not now
- **T-893** (deferred) — T-853 wave 228: deploy db bash-port RED-arm harness [] — T-853 wave 228: commit bash↔port RED-arm harness for deploy db ports
- **T-909** (deferred) — T-468 still regex-walks ci.yml run: — not T-901 serde_norway [] — T-468 still regex-walks ci.yml run: — not T-901 serde_norway
- **T-910** (deferred) — Uppercase extensions bypass case-sensitive language ban [] — Language ban extension match is case-sensitive (.SH/.PY/.MK without shebang stay green)
- **T-137** (deferred) — Discord platform rework [] — Discord structure + bot flows: slot confirm, reminders, AAR links. Complements T-118 website slotting; mostly ops/human.
- **T-947** (idea) — World-parity DEM skip passes green and silently [] — xtask/src/map_world_los_tests.rs:200 returns green with zero assertions when load_dem errs. The gate vacuity guard only matches a leading skip: prefix and only wraps test api, so nothing catches it.
- **T-948** (idea) — audit_notify tests pass green with no database [] — apps/website/api/tests/audit_notify.rs skips six DB tests when TEST_DATABASE_URL is unset and reports ok in 0.01s. T-311 wrote the opposite rule in the same wave: a missing URL is a FAIL.
- **T-949** (idea) — Leaderboard paging test cannot fail on its named claim [] — tests/leaderboards_paging.rs asserts page-set completeness on a six-row board, where both orderings return the same set. Only the in_order assertion has red power. Seed tied rows or rename it.
- **T-950** (idea) — Audit SSE stream has no client [] — T-940.6 ships /audit-logs/stream but no frontend consumes it; the admin audit page is a paged before= Resource. Either wire the page to the stream or record the endpoint as API-only.
- **T-951** (idea) — Audit listener registry is never pruned [] — services/audit_notify.rs keys a listener map on the Arc address of connect_options and never removes entries. One leaks per pool created, and a freed address can be recycled into a new pool key.
- **T-952** (idea) — website-api set_var mutates a shared test process [] — apps/website/api/src/db.rs uses set_var under a mutex in a 281-test binary. The SAFETY note claims no C getenv reader, which is unverifiable when libc is linked. Use the existing from_lookup injection instead.
- **T-953** (idea) — Migration numbering gap 0022-0024 [] — apps/website/api/migrations jumps 0021 to 0025. A sibling slice landing 0022-0024 later would apply after 0025 on any migrated database, and two slices can collide on one number.
- **T-954** (idea) — lfs_paths_in_range never checks check-attr answered for every path [] — xtask/src/wave/push.rs walks check-attr triples without comparing their count to the paths fed. A truncated answer from a zero-exit child reads as nothing-is-LFS. Count triples against the NUL-terminated input.
- **T-955** (idea) — git-lfs probe misses the operator's Homebrew install [] — xtask/src/wave/push.rs probes git lfs version on PATH, which fails under the factory's pinned PATH even though git-lfs 3.7.1 exists. The normal-push branch never runs. Probe the pre-push hook or an absolute path.
- **T-956** (idea) — ci-local no longer mirrors ci.yml [] — mk_ci_tasks.rs and CLAUDE.md both say ci-local mirrors ci.yml, but T-298 added the tbd-tools lane to the workflow only, and no gate asserts parity. Add the task to ci-local or drop the mirror claim.
- **T-957** (idea) — Vanilla reference is rotated pre-T-305 output [] — 2483 extracted .c files under apps/mod/vanilla_reference/Scripts are shifted 56 bytes by the pre-T-305 pak reader, and the committed enf-index TSVs were built over them. Re-extract, re-index, recommit the TSVs.
- **T-958** (idea) — Density lane count comment says 57, list says 61 [] — mk_ci_tasks.rs describes the tbd-tools lane as 57 tests; cargo test --lib --list reports 61 after T-305 added four. Correct the comment.
- **T-959** (idea) — Reroute run lanes onto the run target [] — Four lanes still build into the shared cache: ci_editor_api.rs api build and run, gate_mcp_call_selftest.rs mcpd, three hostrun verify steps in wave/gate.rs, and mk_build.rs rust-api. Each needs TBD_RUN_TARGET_DIR, one line per site.
- **T-960** (idea) — Road census is unreachable from the classify rules [] — Roads export as prefab-less RoadEntity rows, so classify.rs never sees them, and build.rs hardcodes segments 0 and byRoadClass empty. Wire both from the 887 segments build_roads_from_topo already computes.
- **T-961** (idea) — utility kind is dropped by the P5 phase filter [] — phase_kinds P5_props omits utility, so build.rs deletes utility rows instead of counting them. Adding it yields 107 prefabs and 7145 instances, and needs the store.rs prefab-count pin re-pinned.
- **T-962** (idea) — Road segment count is 887, not 888 [] — build.rs comment and four smokes.rs assertions claim 888 segments; roads.json.gz ships 887. The live smoke asserts road_segments === 888.
- **T-963** (idea) — Run stamp covers a directory, not each binary [] — One tbd-built-from describes a whole profile dir while cargo build compiles only what argv names, so a mixed-provenance dir reads green. The stamp also ignores a dirty tree. Stamp per binary and record dirty. Blocks T-959.
- **T-964** (idea) — exact_tree_count_chunk has no zoom gate [] — density_ladder.rs gates vegetation by zoom in exact_tree_count but not in exact_tree_count_chunk, which feeds the density grid, so texel sums disagree below the vegetation zoom. Trees are also ungated below their own zoom.
- **T-965** (idea) — Batch waiver matches by substring, not error kind [] — check.rs waives every error containing the repack phrase; nine distinct wave_lock errors carry it, including a wave listing a ticket with no file. Waive by a typed error kind.
- **T-966** (idea) — world binary feature cannot be taken alone [] — world/mod.rs and Cargo.toml claim binary can be taken without the parser stack, but lib.rs gates pub mod world on the world feature, so a binary-only consumer fails to compile.
- **T-967** (idea) — ObjectInstancePod pad is documented as zero but unenforced [] — pod.rs says _pad is always written as 0; a non-zero pad round-trips intact and breaks derived PartialEq. Zero it on construction or exclude it from equality.
- **T-968** (idea) — Oracle 2 label-to-set map is synthetic after relabelling [] — carry_emptied relabels pending entries, so a re-close of a disavowed label can now corroborate against a set that was never that label's. The shipped test asserts the fixture, not the changed function.
- **T-969** (idea) — clippy map-engine step is not the all-features floor [] — wave/gate.rs runs clippy with doc, mission and world, omitting png, while claiming to be the all-features floor.
- **T-970** (idea) — Chunk .bin files are neither LFS-tracked nor ignored [] — packages/map-assets objects/chunks .bin matches no gitattributes or gitignore rule, so about 37 MB of untracked binaries sit in the tree after a real export with nothing stopping a careless add.
- **T-971** (idea) — verify-phase reds on the new chunk .bin files [] — gates.rs walks a scratch build unfiltered and reports files absent from the committed tree as missing. T-935.2 emits 315 chunk .bin files that are deliberately uncommitted, so verify-phase reports 315 missing.
- **T-972** (idea) — peaks test treats an LFS pointer as a real DEM [] — dem/peaks.rs returns early when the DEM file is absent but not when it is an LFS pointer, so in a slice worktree it panics on decode instead of skipping. Treat a pointer as absent.
- **T-973** (idea) — land passes a gate base past the wave opening [] — platform wave land passes the pre-merge HEAD as the gate base. When the command center commits between close and land, that base post-dates the wave opening and the post-merge gate refuses. Derive the base instead.
- **T-974** (idea) — TBDD per-byte ban matches two literal spellings [] — tbdd.rs bans the strings u16_le and from_le_bytes with a bracket. A shift-or assembler or from_le_bytes with try_into passes. Ban the shape, scoped to the decode body.
- **T-975** (idea) — Density golden fixture pin is skip-shaped [] — density.rs guards the golden fixture with an exists check, so the pin goes silently vacuous if the file disappears. Make a missing golden a failure.
- **T-976** (idea) — Label host doc names the wrong two requests [] — world_assets/mod.rs documents LabelHost init as fetching locations.json and road-names.json. On the archive path the count is still two but the files are manifest.json and map_labels.rkyv. One-line doc fix.
- **T-977** (idea) — Town label gates read the JSON source directly [] — xtask/src/label_gates.rs parses locations.json rather than the manifest, so the gates stay on the JSON after a manifest cutover. Correct while dual emission holds; revisit at the cutover slice.
- **T-978** (idea) — Raw DEM path still materialises one f32 grid [] — dem_load.rs decodes to u16 then builds an f32 grid because three consumers take a float slice: hillshade, the dem_vectors downsample and the retained grid. Net is 2 buffers against the PNG path's 4; removing the last needs those three files.
- **T-979** (idea) — Shared cache serves stale rmeta to the gate driver [] — Building xtask in the shared target dir failed on an unresolved import with no compile line for the crate defining it, killing two gates before any step ran. Touching the sources cleared it.
- **T-980** (idea) — T-935.8 verify command filters out its own tests [] — The ticket's verify line filters on building, which excludes every occluder descriptor test the slice added. A vacuous verify; use the module filter or none.
- **T-982** (idea) — map-blueprint archive subcommand name is wrong [] — The ticket and docs name cargo xtask map-blueprint archive, which does not exist. The real command is cargo xtask map blueprint-from-voxels archive. Also the ticket says six golden blueprints; the tree has three.
- **T-983** (idea) — Archive sidecars should be fetched with the descriptors [] — The removed fold ran after the awaited descriptor fetch, so it could add nothing usable. Issuing the archive's sidecar list concurrently with the descriptor round is the actual round-trip saving.
- **T-984** (idea) — New SPA world-asset code is executed by no test [] — The four frontend files this wave changed are wasm-only, so the native test build compiles them to nothing and no test runs them. About 450 lines of boot logic are compile-checked and never executed.
- **T-985** (idea) — Archive boot drops the hot-set prefetch [] — Archive boot returns before the blas hot list. FOLDS INTO T-935.13 (wave 244 giant slice).
- **T-986** (idea) — v-suite oracles have drifted on 22 of 25 routes [] — The frozen route oracles were last refreshed at T-173 while the pages have moved repeatedly since, so gate v-suite verify fails 22 of 25 routes. Only four are clean. Needs a re-freeze or an explicit decision.
- **T-987** (idea) — Frontend wasm clippy carries 76 pre-existing errors [] — Running clippy on website-frontend for wasm32 with warnings denied reports 76 errors, 121 with all targets. The gate's clippy step is change-scoped so it never sees them.
- **T-988** (idea) — Forest regions file grows 5.7x when smoothed [] — The smoothed everon rings take forest-regions.json.gz from 43638 to 248877 bytes, 15528 to 42762 vertices. The world load fetches it and no size budget gate covers it.
- **T-989** (idea) — T-149 verify line names a command that does not exist [] — The ticket says cargo xtask ci verify map-object-golden. That subcommand does not exist; the gate is cargo xtask schema map-object-golden.
- **T-990** (idea) — F6 gate compares raw rings to a smoothed catalogue [] — The phase gate re-derives forest regions with the raw tracer and compares the array to the committed file. The emit now writes smoothed rings, so they can never match after a regeneration.
- **T-991** (idea) — TbdSatIndexV2 has no schema_version field [] — Six of the seven binary archives declare a schema version; this one relies on the container header instead. The check is satisfied, but the asymmetry is worth removing.
- **T-992** (idea) — Mip geometry is implemented twice [] — The level dimension and tile rect arithmetic exists in both the emitter and the reader. Two agreeing implementations is a decent oracle now but a drift hazard; the shared home is the core crate.
- **T-993** (idea) — Satellite boot still hardcodes container v1 [] — Satellite boot rejects any container but v1. FOLDS INTO T-935.13 (wave 244 giant slice).
- **T-994** (idea) — Forest smoothing emits self-crossing rings [] — The area-restoring offset moves pinch vertices in opposite directions, so rings cross themselves and their siblings. Measured 0 crossings in, 38 out on the everon corpus. The area check cancels the bowtie exactly and cannot see it.
- **T-995** (idea) — Smoothed forest regions are dropped before render [] — The world host filters out every region of kind forest, and all 36 everon regions are forest, so no smoothed edge reaches the map. The larger payload is fetched on load and discarded.
- **T-996** (idea) — The v2 satellite verifier checks less than v1 [] — The v1 verifier cross-checks the index world bounds and terrain against the manifest; the v2 verifier takes no manifest and the v2 index carries neither field. v2 is now the build default.
- **T-997** (idea) — Everon tiling pins never read the committed bundle [] — Two tests say the expected tiling is read off the bundle, but both compute it from hardcoded constants with no file access. The real bundle does verify, yet no gate step runs that verifier.
- **T-998** (idea) — Container version is read as u16 and u32 [] — The browser reads a 16 bit version and the tool reads 32 bits, so a v2 container with non-zero flags is version 2 to one and unsupported to the other.
- **T-999** (idea) — Everon road archive is emitted but not committed [] — The emitter is proven end to end and produces a 435452 byte archive from the committed roads json, but the asset itself lives outside every slice's owns so nothing is committed. The loader slices downstream will have nothing to fetch.
