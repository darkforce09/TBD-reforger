# T-946.64 — Plan

## Context
`gate_slice` (xtask/src/wave/gate.rs) runs check, wasm32, fmt, clippy, schema, catalogue drift,
two db_migrate steps and the VERIFY_STEPS loop — no `cargo test` of any kind. Wave 253 shipped two
deterministically-failing frontend tests (T-937.4 dead Class-R probe, T-938.4 f32 golden widening)
that only the wave-level gate caught, after merge.

## Approach
1. Verify on main: `gate --slice` output shows no test step → paste the step list.
2. `changed.rs`: `frontend_tests_changed(ctx, base)` beside `wasm_changed`, reusing
   `wasm_scope_touched` so map-engine-core is in scope (that is how T-938.4 reached a frontend test).
3. `gate.rs`: register it after the clippy step as `test (frontend, changed)`.
4. Perturbation: break a `website-frontend` test → the step goes red; restore, touch, green.

## Risks
- Scope too narrow: a literal `apps/website/frontend/` prefix would miss the core-crate case.
  Mitigated by reusing `wasm_scope_touched`.
- Shared-target false green (T-596): the step must use `checkrun_argv` + `gate_check_target`, never
  the shared warm cache — a *test* step reporting a cached PASS is worse than no step.
- Gate runtime grows for every frontend-touching slice; accepted, it is the cheapest place to catch
  this class.

## Verification
- `cargo test -p xtask`
- `cargo xtask platform wave gate --slice T-946.64`
