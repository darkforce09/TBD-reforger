<!-- T-917.6 ready-gate plan doc for T-090.12 (generated 2026-09-04 from the operator-approved program plan
     ~/.claude/plans/plan-written-claude-plans-t090-12-world-glistening-unicorn.md, itself reconciled from
     ~/.claude/plans/t090-12-world-bvh-every-object.md; Cursor owns the prose polish). -->

# T-090.12 — Every building, every object: world-scale BVH LOS

## Context

T-090.11 proved pak → COLL → `SurfaceKind` sidecar → compound instances → multi-hit LOS on one prefab (3983/4000 oracle agreement). The editor's LOS shot (`los_tool.rs::occlusion`) and viewshed (`dem::sample::compute_viewshed`) still see terrain only, and chunk rows carry yaw alone. Operator plan approved 2026-09-04.

## Approach

Seven slices on `main`, executed 0 → 1a → 2 → 3 → 4a → operator session → 1b → 4b → 5 → 6: (1) chunk wire v2 with pitch/roll/scale + resurrected full-export plugin + transform pins; (2) `bvh-batch --all-prefabs` → descriptors, BLAS library, manifest, verify lane; (3) `WorldOccluder` (AABB TLAS per chunk, chunk DDA, descriptor expansion, proxy fallback, coverage) + `map world-los`; (4) Workbench `world-parity` oracle + two pinned cells; (5) editor object verdict, sliced viewshed object wash, `/debug/world-los`; (6) close-out; (7) Cursor docs pass.

## Risks

Re-export churn renumbers prefab ids (rebuild through the existing gates, report the delta). Engine foliage semantics on the Projectile layer unknown (dual-policy replay, the matching policy is pinned). Object-wash time (coarse-first, 8 ms slices, 8 m only within 1 km, never fake-clear). Workbench unavailable (scripts land, pins stay `#[ignore]`, named as missing).

## Verification

Per slice: `cargo test -p map-engine-core --all-features`, `-p xtask`, `-p tbd-tools`, `-p website-frontend`, `-p map-engine-render`; clippy `-D warnings` (core all-features, frontend/render wasm32); `cargo xtask verify file-length`; `cargo xtask ticket check`; schema/golden lanes when they move; `trunk build --release` for slice 5. Pins: farmhouse world-row 88/88 ≤ 2 cm; garbage-container pitch −3.04 / roll −4.75; compound door parity 3983/4000 unchanged; two cell oracles at measured agreement (bar 98 % objects-only).
