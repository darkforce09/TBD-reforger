# T-935.13 — Plan

## Context
Every loader has a dormant binary branch and the gates accept the new blocks; the everon manifest
still names json.gz / PNG paths. build.rs:503 still writes gz-JSON chunks; flate2 is still a
`world` dependency of map-engine-core.

## Approach
1. Measure before: `du` per asset class; boot timings from the editor perf log on everon.
2. Regenerate everon with each emitter (build-objects, build-roads, elevation.dem, labels-rkyv,
   water, map-blueprint archive, unified v2). Data files are emitter outputs, never hand-edited.
3. Edit `packages/map-assets/everon/manifest.json` and `objects/chunks/manifest.json`; delete
   `objects/chunks/*.json.gz` and the prefabs/roads/forest-regions `.json.gz`.
4. `build.rs`: remove the gz chunk writer; `crates/map-engine-core/Cargo.toml`: drop flate2 from
   `world`; frontend `world_assets/mod.rs`: binary loaders default, JSON branches removed.
5. Gates, world-los identical to the pre-cutover run, measure after, write the commit body.

## Risks
- DEM PNG and descriptor JSON stay as emitter inputs; a lingering flate2 import fails at compile time and is fixed at the call site.

## Verification
- `cargo xtask ci verify-terrain-strict`
- `cargo xtask map world-los --cell 18_0 --probe 9350,15,280 9380,15,290`
- `cargo test -p map-engine-core --all-features`; `cargo xtask mk leptos-gates`
- `cargo xtask platform wave gate --slice T-935.13`
