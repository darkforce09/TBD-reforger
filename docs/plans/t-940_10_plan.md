# T-940.10 — Plan

## Context
services/mortar.rs:51-60 vacuum model; pages/public/mortar.rs is fully online. New workspace crate shared by both.

## Approach
1. Verify on main: a 100 m elevation delta returns the flat solution; paste the red.
2. `crates/tbd-ballistics` (Cargo.toml, src/lib.rs; root Cargo.toml member; api + frontend deps): elevation delta, drag, wind, dispersion, battery sheaf; wasm-clean; unit tests against a published table.
3. mortar.rs delegates; responses gain dispersion.
4. mortar.rs page computes locally; API path kept for saved fire missions.
5. Perturbation: zero the drag term → table test red; restore, touch, green.

## Risks
- Cargo.lock churn: one dependency addition, no version bumps.

## Verification
- `cargo test -p tbd-ballistics`; `cargo xtask db test-it`; `cargo xtask mk ci-local-leptos`; `cargo xtask mk leptos-gates`
- `cargo xtask platform wave gate --slice T-940.10`
