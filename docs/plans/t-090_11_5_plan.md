<!-- T-917.6 ready-gate plan doc for T-090.11.5 (generated 2026-09-03 from the operator-approved program plan
     ~/.claude/plans/implementation-plan-tender-seahorse.md; Cursor owns the prose polish). -->

# T-090.11.5 — Dedicated architectural render lanes

## Context

The viewer uploads walls to ROADS_CASING, furniture to AIRFIELD_APRON, cuts to CONTOURS/FOREST_OUTLINE and the ray to MISSION_ZONES — a prototype hack against the draw-order contract.

## Approach

`crates/map-engine-render/src/draw_order.rs`: 13 variants after `WorldTownLabels` (InteriorSlabs, InteriorFurniture(+Outline), InteriorWalls(+Outline), InteriorPortals(+Outline), InteriorGlazing(+Outline), InteriorStairs, SceneVegetation(+Outline)) plus `InteriorProbe` above `Viewshed`; `role_id` 11..=23; `lane_order` shift; `ALL_LANES` 48; both u32 maps; `lane_order_pins` updated (tests may move to `draw_order_tests.rs`).

## Risks

Lane count follows the fill/outline-twin precedent; collapsing twins loses hairline weight at low zoom — flagged, not assumed.

## Verification

`cargo test -p map-engine-render`; `cargo clippy -p map-engine-render --target wasm32-unknown-unknown -- -D warnings`.
