<!-- T-917.6 ready-gate plan doc for T-090.12.6 (generated 2026-09-04 from the operator-approved program plan
     ~/.claude/plans/plan-written-claude-plans-t090-12-world-glistening-unicorn.md, itself reconciled from
     ~/.claude/plans/t090-12-world-bvh-every-object.md; Cursor owns the prose polish). -->

# T-090.12.6 — Close-out: ci-local replay, final numbers, ship

## Context

Every code slice has shipped with its own gates; the program still needs the CI mirror run end to end, the measured numbers written into the program plan, and the registry stamped.

## Approach

`cargo xtask ci ci-local` on `main`; append the final pins, bench timings and library bytes to `docs/plans/t-090_12_plan.md` Verification; `ticket ship` + `stamp-sha` for `.6` and `T-090.12`; the registry commit carries the Docs-for-Cursor list for `T-090.12.7`.

## Risks

A red `ci-local` lane is fixed in a code commit under the owning slice before the close-out, never worked around.

## Verification

`cargo xtask ci ci-local` exit 0; `cargo xtask ticket check` OK; `shipped_at` present on every slice and the umbrella.
