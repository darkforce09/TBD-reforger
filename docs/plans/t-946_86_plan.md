# T-946.86 — Plan

## Context
Four features shipped in wave 255 have zero production call sites: the canvas Z gizmo arm
(`canvas/gestures.rs:626`), the outliner multi-select drop (`panels/outliner_drag.rs:9`), the
tactical draw trigger (`state/operations/tactical_graphics.rs:188`) and the duplicate slot-id guard
(`state/operations/slot_ids.rs:4`). Spec:
`docs/specs/audit_2026_09/t946_86_wave255_dead_code.md`.

## Approach
1. Verify all four on main first: grep each symbol, paste the zero counts, and drive the editor to
   show the feature is dead. A report claiming one of these was already wired has been wrong once.
2. `.82` — read `z_drag` in `onpointermove` and `onpointerup`, call `set_z_drag_readout`, commit
   elevation inside one undo group, release pointer capture. Leave `gizmo_z.rs` alone.
3. `.83` — consume `outliner_drag::PENDING_DRAG` through `plan_drop` on drop and apply every id in
   one `with_batch` transaction. Stay inside `outliner_drag.rs` + `outliner_tree.rs`.
4. `.84` — one control under `panels/`, copying `zones_panel.rs:37-44`. Button, not keybinding.
5. `.85` — call `duplicate_slot_ids` from `save_now`; refuse naming callsign and id.
6. Perturbation, one per fix: sever the new call site, watch the pin go red verbatim, restore,
   `touch`, green.

## Risks
- **`state/operations.rs` is T-939.2's this wave.** Nothing here needs it.
- **`class_r_scrub::live_source` truncates at the first `#[cfg(test)]`** — every new test goes at the
  bottom of its file. This trap has bitten in three consecutive waves.
- `outliner_tree.rs:1845` pins the string `complete_layer_drop_onto_folder`; removing the call is
  fine, removing the string is not.
- A keybinding for the tactical tool reddens two `help_modal.rs` tests (`tactical_graphics.rs:175-182`).

## Verification
- `cargo xtask mk ci-local-leptos`
- `cargo xtask platform wave gate --slice T-946.86` — must end `SLICE GATE: PASS`
- Grep proof in the report: every named symbol has ≥1 production call site
