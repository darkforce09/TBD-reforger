<!-- AUTO-GENERATED by ./scripts/ticket sync — DO NOT EDIT -->


# Ticket Lead Dashboard

## Running / Review


## Ready

- **T-090** (900) — Map visualization program [ready] — Map Engine v2 through sea-band + contours @ `bd481cf1`. **Active:** **T-090.5.5** tree/veg/prop glyphs. Single lane.
- **T-743** (4619) — Paste-at-original lands 20m off, and three artifacts state otherwise — ACTION-PASTE-ORIG-001 not closed as named [ready] — MAJOR from the wave-113 adversarial verifier, independently confirming the T-669 slice agent's contradiction of its own ticket. FACT: MissionDocCore::paste_slots' no-anchor arm is UNCONDITIONAL — `_ => (PASTE_NUDGE, PASTE_NUDGE)` at store.rs:2175-2178 with PASTE_NUDGE = 20.0 (store.rs:4225). Ctrl/Cmd+Shift+V calls paste_at_cursor(None, None), hits that arm, and every slot lands at (x+20, y+20) clamped. So the shipped behaviour is 'paste NEAR original, +20m on both axes', not 'paste at original'. THREE ARTIFACTS ARE WRONG ON THIS POINT OF FACT and must be corrected regardless of what is decided: the T-669 registry summary ('paste_at_cursor(None, None) is LITERALLY paste-at-original'), gap_analysis.md:265, and interactions_sweep.md:233. NOT the slice's fault and not silently fixable: the 20m nudge is deliberate byte-parity with the JS oracle ydoc.pasteSlots, so removing it is a semantics change with a golden-test blast radius, not a slice-sized edit. The slice shipped the arm as specified and made its own in-code comment honest, but the help row copy (eden_help.rs:108, 'Paste at the source position') repeats the overstatement. OPERATOR DECISION NEEDED — three options: (a) re-scope the row to 'paste without moving to the cursor (oracle 20m nudge)' and soften the help copy, (b) add an exact-coordinate paste variant as new work, (c) change PASTE_NUDGE semantics and accept the golden-test churn. ALSO folded in from the same report: the T-669 summary's claim that this ticket 'touches exactly one file' is stale — it also touched eden_help.rs, forced by T-692's coverage pins. Diagnosis: editor_verify/wave113.md F-1, F-4.
- **T-777** (4653) — Paste and place-composition land every slot at z = 0.0 — NEEDS OPERATOR DECISION alongside T-743 [ready] — MINOR from the wave-127 focused re-verifier. THIS IS THE ONE WAVE-127 FINDING THAT WAS NOT FIXED, AND IT IS DELIBERATE: it sits on T-743's reserved decision surface. Wave 127 closed a four-instance family where an x/y write with z=None made `update_slot_position` store `pz = 0.0` (store.rs:2779-2783) with nothing re-sampling terrain afterwards — fixed at `attrs_update_position`/`_multi` (F-2), `commit_positions` (F-5, reached by Align/Distribute/place-pattern) and the marquee drag (F-6). The re-verifier then proved that family CLOSED (zero `move_entities` frontend callers; exactly one `move_entities_and_vehicles`; five `update_slot_position` sites, three z-resolved and two rotation-only which cannot trip the mutator's `x.is_some() || y.is_some()` arm). WHAT REMAINS IS A DIFFERENT CLASS — a CREATE path, not a missed caller: `editor_ops.rs:642` (`paste_at_cursor`) pushes `zs.push(0.0)` for every pasted slot and explicitly drops `position.z` from the copied extras, and `place_composition` (store.rs:2394/2409) does the same, symmetric for vehicles. Ctrl+V a rooftop entity and the copy lands on the deck. NO AUTHORED WORK IS DESTROYED — there is no cut, the source is untouched, so this is a wrong-landing-position defect rather than the silent corruption the rest of the wave fixed. WHY IT WAS NOT FIXED AUTONOMOUSLY: the `zs.push(0.0)` carries the in-code rationale 'DEM not ready — byte-parity with the flat-map case', which is the SAME byte-parity-with-the-JS-oracle argument that makes T-743 (`PASTE_NUDGE` = 20 m, store.rs:4998-4999) an operator decision rather than a slice-sized fix. Both are 'where does a pasted entity land', both are pinned to the JS oracle, and changing z unilaterally could pre-empt whichever of T-743's three paths the operator picks. DECIDE THE TWO TOGETHER. The z axis is arguably independent of the x/y nudge — preserving the source z is compatible with all three T-743 paths — but that is the operator's call to make, not the factory's. Diagnosis: editor_verify/wave127.md, focused re-verification section.
- **T-781** (4657) — Comments still cannot be composed — PLACE-COMMENT-001's last undelivered clause [ready] — RESIDUE of T-748 (wave 141) and, before it, T-651 (wave 114). The spec row PLACE-COMMENT-001 asks for comments that are 'Draggable, copy/paste, layerable, COMPOSABLE' plus a 'Comment icon at position'. T-748 (wave 141) delivered the map glyph, so the icon clause is closed. THE COMPOSABLE CLAUSE IS NOT: compositions capture from the SELECTION, and comments sit in no selection lane, so a comment can never be part of a composition. T-748's own diagnosis called this out — 'composable was a silent drop, which is what the HARD GATE bars' — and it is scope accounting rather than broken code: nothing misbehaves, the feature simply does not exist. THE INTERESTING QUESTION IS WHERE TO PUT COMMENTS. They are deliberately absent from the compiled mission (the same property that keeps them out of the render SoA before T-748 gave them a glyph), so a composition containing a comment must carry it as authoring metadata and must NOT emit it into the compiled output. Check how `place_composition` (crates/map-engine-core/src/doc/store.rs) captures and replays, and whether the comment can ride the same rows without reaching flatten. WATCH THE Z RULE: `place_composition` creates at z = 0.0 (see T-777) and that is on the operator's reserved decision surface with T-743 — do not change paste/composition landing semantics as part of this ticket; carry whatever the operator decides. ACCEPTANCE: select a comment together with entities, make a composition, place it, and confirm the comment comes back at the right offset, survives undo/redo/restore, and does NOT appear in the compiled mission.
- **T-782** (4658) — Resolve the player-count question: the editor shows placed slots; the library shows per-side and total [ready] — OPERATOR DECISION 2026-08-08, closing the T-694 player-cap question that has been open since wave 113. THE RULING: in the EDITOR the figure an author sees is HOW MANY SLOTS ARE PLACED DOWN — the derived count, not an author-typed cap. In the MISSION LIBRARY a mission should show how many slots there are PER SIDE and the TOTAL. CURRENT STATE, verified by the orchestrator: two numbers exist and nothing reconciles them. `max_players` (dto.rs ~:851) is AUTHOR-DECLARED — typed once into the create dialog — and is copied into the compiled mission by `compiled_meta`, so it is not dead. `MissionDocCore::slot_count` is the number of seats the document actually contains. The editor currently shows BOTH side by side and deliberately does not resolve the disagreement; the rationale lives at `eden_settings::PLAYER_COUNT_DISAGREE_NOTE`. This ruling resolves it: slot_count is the truth for the editor's player figure. This is consistent with the operator's wave-113 decision (derived slot count, no min_players). EDITOR HALF (do now): make the derived slot count the figure the author sees, and retire the disagree-note in favour of the decided answer rather than leaving a note explaining an unresolved argument. Do NOT delete `max_players` — it reaches the compiled mission. ONE SUB-QUESTION LEFT FOR THE OPERATOR, do not invent it: should `max_players` remain author-typed at all, or be DERIVED from slot_count so the compiled mission cannot disagree with the document? The ruling settles what is displayed; it does not settle what is compiled. LIBRARY HALF (design deferred by the operator): show slots per side plus the total on the mission library view. The operator explicitly said the exact presentation is to be worked out later — bring a proposal, do not guess at layout. Note the per-side breakdown does not exist as a derived value yet; check whether flatten or the doc can produce it without a new wire field before assuming a schema change.

## Next queued (top 10)

- **T-120** (1200) — Staging soak + golden mission smoke [queued] — Pinned game/mod version soak; golden-mission smoke on staging server.
- **T-146** (1470) — Asset Browser Data Wiring [queued] — Hook up T-150 registry items (vehicles/crates/…) to Asset Browser for map drag-place. Unblocked by T-150; after or parallel T-068.9 ingest preferred.
- **T-170** (1670) — Prod default flip to Leptos SPA [queued] — Make the Leptos SPA the production default: set `SPA_DIST_DIR` to the trunk release dist in the prod env, move the Discord OAuth redirect origin, staging soak, then flip. OPERATOR-GATED (env + Discord app settings are operator-held); claude-code prepares configs/runbook + verifies the staging soak gates. Runbook anchor: docs/website/DEV_RUNBOOK.md §SPA serving; T-159 finish program left this as the sole operator-gated residual.
- **T-673** (4300) — Marker style and Area markers — the $defs/marker widening [queued] — Six attribute ids: `ATTR-FIELD-MRK-SIZE`, `-MRK-ROTATION`, `-MRK-SHAPE`, `-MRK-BRUSH`, `-MRK-COLOR`, `-MRK-ALPHA`. BLOCKING EVIDENCE: `$defs/marker` in the compiled schema is exactly {x, z, icon, label} and CLOSED — the compiled schema carries 25 `"additionalProperties": false`, so these fields are contract-blocked, not merely unbuilt. Icon-vs-Area is Eden's entire second marker model, so this is a widening plus a mod-side reader, not a UI ticket. MUST SHIP AFTER T-069, which authors the four schema-carried fields; folding these into T-069 would convert a factory-dispatchable ticket into a workbench one, which is the specific mistake this split exists to prevent. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-674** (4310) — T-216 follow-on: slot identity reaches the wire [queued] — Four attribute ids — `ATTR-FIELD-OBJ-CALLSIGN`, `-OBJ-RANK`, `-OBJ-STANCE`, `-OBJ-UNIT-NAME` — plus the two TBD-only keys `tag` and `leaderSlotId`. `OBJ-UNIT-NAME` rides this slice rather than standing alone; the sweep says so explicitly. THE CONTRACT DELTA IS ALREADY WRITTEN OUT at `crates/map-engine-core/src/mission/flatten.rs`:2620-2632 — five `$defs/slot` / `$defs/group` keys. T-216 is `shipped`, but it shipped THE LEDGER AND ITS TRIPWIRE, not the fix: flatten.rs:2584-2649 enumerates six author-facing values the compile silently drops (a squad's leaderSlotId, a slot's tag / callsign / rank / stance, and the entire vehicle roster) and records that `make verify-t180` stayed green throughout BECAUSE NOT ONE OF ITS 22 TESTS CHECKED. `stance` additionally needs an Enfusion spawn-pose call — word-boundary `stance` in apps/mod is ZERO (the 39-file `grep -rl stance` result is every hit being the substring inside `instance`). Widening `/compiled` without care 500s every mission. Related: T-242. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-675** (4320) — Vehicle roster reaches the game — the compile half of T-076 [queued] — The cross-boundary half that was split out of T-076 so the crew UI could be dispatched. No attribute ids of its own; it closes the sixth entry in the T-216 drop ledger. BLOCKING EVIDENCE: `crates/map-engine-core/src/mission/flatten.rs`:2631 states the delta as "document root + a new $def", key `vehicles`, and :2634-2640 PROVES the already-declared `entities[]` cannot carry it — `$defs/alias` is `^(kit|comp|veh|preset|layer|prop|item):[a-z0-9_]+$` and `apps/mod/tbd-framework/Data/registry.json` holds exactly ONE `veh:` row, so alias substitution would reproduce the T-200 silent-substitution defect with a 10-tonne vehicle. Without this, T-076's crew UI authors state the game never sees. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-676** (4330) — Trigger activation and effects — the Enfusion runtime [queued] — Twelve of the thirteen `ATTR-FIELD-TRG-*` ids: everything except the geometry/palette half, which is the factory ticket T-079. An activation model (condition, repeat, timeout, owner side) and an effects model are a NEW ENFUSION RUNTIME in `apps/mod/tbd-framework/` plus the schema keys to carry them. Geometry can ride the shipped T-582 zone draw tool, which is why the editor half was separable at all. Read `$defs/zoneRules`' 16 keys before designing this: they have no ATTR-FIELD id and were never triaged against Eden, but they are the closest thing TBD already has to trigger semantics. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-677** (4340) — Waypoints — group movement orders [queued] — OPERATOR DECISION 2026-08-02: AI units ARE coming, so this is no longer speculative. Was titled "blocked on AI units existing at all" because every slot currently spawns AI-disabled (TBD_SpawnManager.c:963,1166), which left 13 attribute ids with no subject.

Nine `ATTR-FIELD-WP-*` ids plus six interaction ids (`RIGHT-MODE-004`, `CONN-WP-ACT-001`, `CONN-WP-ATTACH-001`, `CONN-RAND-START-001`, `KEY-WP-001`, `ACTION-WP-QUICK-001`). THE GATE IS NOT THE SCHEMA — it is that there is no subject. `apps/mod/tbd-framework/.../TBD_SpawnManager.c`:963 and :1166 spawn every body with AI DISABLED, so a waypoint has nothing to command. The attributes sweep calls this "the single biggest scoping correction available to T-079": waypoints cannot be built, in the editor or anywhere else, until AI units exist. Ship together with the group AI state ticket or not at all — they share the gate exactly. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-678** (4350) — Group AI state: combat mode, behaviour, formation, speed [queued] — OPERATOR DECISION 2026-08-02: AI units ARE coming — this ticket has a subject now. Previously gated behind the same question as T-677.

Four attribute ids: `ATTR-FIELD-GRP-COMBAT-MODE`, `-GRP-BEHAVIOUR`, `-GRP-FORMATION`, `-GRP-SPEED-MODE`. Same AI gate as waypoints, and the sweep says they should ship together or not at all. Evidence that nothing exists to hang these on: word-boundary `combatMode` and `speedMode` return ZERO in both the frontend and the mod tree; `formation` returns one frontend hit and it is prose in a comment at editor_ops.rs:1324. `$defs/group` is closed, so each field is a widening plus an Enfusion reader. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.
- **T-679** (4360) — Placement scatter: radius and area shape [queued] — Three attribute ids: `ATTR-FIELD-OBJ-PLACEMENT-RADIUS`, `-OBJ-SHAPE`, `-GRP-PLACEMENT-RADIUS`. `$defs/slot` and `$defs/group` are both closed, so this is one key on each plus a spawn-side reader that scatters within the radius. THE CHEAPEST COHERENT SCHEMA-WIDENING SLICE in the workbench program, and a 2D top-down editor is the best possible surface for it — the scatter circle is drawable — so it is the one most worth doing early if the operator wants an easy first workbench win. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.

## Dependency graph (scoped)

```mermaid
flowchart LR
  T661[T-661] --> T069[T-069]
  T118[T-118] --> T114[T-114]
  T114[T-114] --> T120[T-120]
  T115[T-115] --> T120[T-120]
  T117[T-117] --> T120[T-120]
  T150[T-150] --> T146[T-146]
  T159[T-159] --> T170[T-170]
  T069[T-069] --> T673[T-673]
```
