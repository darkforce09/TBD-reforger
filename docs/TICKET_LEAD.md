<!-- AUTO-GENERATED by ./scripts/ticket sync — DO NOT EDIT -->


# Ticket Lead Dashboard

## Running / Review


## Ready

- **T-090** (900) — Map visualization program [ready] — Map Engine v2 through sea-band + contours @ `bd481cf1`. **Active:** **T-090.5.5** tree/veg/prop glyphs. Single lane.

## Next queued (top 10)

- **T-120** (1200) — Staging soak + golden mission smoke [queued] — Pinned game/mod version soak; golden-mission smoke on staging server.
- **T-146** (1470) — Asset Browser Data Wiring [queued] — Hook up T-150 registry items (vehicles/crates/…) to Asset Browser for map drag-place. Unblocked by T-150; after or parallel T-068.9 ingest preferred.
- **T-170** (1670) — Prod default flip to Leptos SPA [queued] — Make the Leptos SPA the production default: set `SPA_DIST_DIR` to the trunk release dist in the prod env, move the Discord OAuth redirect origin, staging soak, then flip. OPERATOR-GATED (env + Discord app settings are operator-held); claude-code prepares configs/runbook + verifies the staging soak gates. Runbook anchor: docs/website/DEV_RUNBOOK.md §SPA serving; T-159 finish program left this as the sole operator-gated residual.
- **T-638** (3970) — Collapse and expand both docks [queued] — Operator-requested. Eden's mechanism, measured across all 75 screenshots: a 24x24 CHEVRON in each panel's outer top corner inside the tab strip (left x 0..23 y 47..70, right x 1896..1919 y 47..70); expanded points outward (« left, » right) and collapsed is the SAME bbox with the glyph flipped; collapsed is neither a rail nor a vanish — the panel becomes exactly that 24x24 stub docked at the screen corner, overlaying the map; the viewport REFLOWS, map canvas 1440 -> 1920 px, +33%. Keys: `E` Entity List, `R` Asset Browser, `Backspace` whole interface (T-662 frees Backspace); menu path `View > Interface`. No toolbar button — edge chevron and keyboard only. Code: DockLeft and DockRight (post-split eden_dock_left.rs / eden_dock_right.rs, from eden_chrome.rs:2813 and :2961) plus the four layout constants that must become dynamic — STRIP_TOP_PX :38, DOCK_LEFT_PX :40, DOCK_RIGHT_PX :42, TOOLBELT_BAND_PX :45, all now in eden_layout.rs. THIS IS A CHOKEPOINT — three hot files at once — because those constants are read by three files: mission_editor.rs reflows the canvas at :1985 (`e.resize(rect.width(), rect.height(), dpr)`, so the device buffer really is reallocated — that answers the draft's "confirm it resizes rather than stretching") AND its on-canvas hit test :1668-1671 reads all four directly, so a collapsed dock changes what counts as on-canvas; select_tool.rs:461-466 consumes the same four for `farthest_empty_px` in the marquee self-check, which is the one nobody would guess. Keys go in the editor keydown :1005-1030. STILL OPEN: whether the camera holds its world position across the reflow.
- **T-659** (3980) — Slot census badge + generated mission summary line [queued] — Per-side live counts in the editor header (`WEST 78 · EAST 74 · IND 8 · TOTAL 160`) plus a summary line composed from the document. This REPLACES two of the MissionAnalyzer's rules by making the malformed state unreachable rather than caught — the "should be unrepresentable" pile — and WOG got a 94%-consistent community naming convention out of the counter alone. Code: the header is `TopCommandStrip` (post-split eden_top_strip.rs, from eden_chrome.rs:831); the existing OBJ/SEL/SZ counters in BottomToolbelt :3745-3766 are the pattern to copy. Per-side counts derive from the ORBAT snapshot — `orbat_manager_snapshot` (editor_ops.rs:1190) and `vehicle_rows` :1751. Cheap and visible, which is why it is early in the validation group's internal order (panel -> engine -> census -> rule content).
- **T-657** (3990) — Validation: ORBAT and slot rules [queued] — Rule content in the T-656 engine, so it modifies only `crates/map-engine-core/src/mission/validate.rs`; it READS `crates/map-engine-core/src/mission/orbat.rs` (359 lines) and doc/store.rs and modifies neither. Rules: every slot resolves a role and a squad; no default/empty identity fields; no leaderless squads; no duplicate callsigns within a side. This COLLAPSES the MissionAnalyzer's R3+R4+R5 — three overlapping rules with hardcoded name lists that exist ONLY because Eden packs role and callsign into one string — into one query over TBD's typed fields; R3 is the one rule FNF rated `error` rather than `warning`. It also revives D3-D8, six commented-out per-squad coverage rules, as ONE rule parameterised by squad template. Depends on T-656 for the primitives; collides with T-656 / T-658 / T-660 on validate.rs, which is why the four are in four different waves. Validation is ALWAYS ON — not behind a debug flag, not on save, not on export.
- **T-642** (4000) — Ruler: persistent polyline with per-leg distance and bearing [queued] — Operator-requested, and one of the two dead buttons. Both stubs are adjacent lines in one component: eden_chrome.rs:3711-3713 (Ruler, `class=TOOL_DISABLED disabled=true`, MaterialIcon name="straighten") and :3715-3717 (LoS) — `grep -n TOOL_DISABLED` returns exactly three hits, the const :74 and those two buttons — so T-642 and T-643 collide by construction and sit one wave apart. NO PRIOR ART: 3den Enhanced has no ruler beyond a 2-point measure with a 20-second notification as the readout, so the design is ours. Do: click a chain of points; per-leg distance AND bearing plus a running total; persists on the map until dismissed. Decisions left open inside the ticket — leg labels on the line vs in the status bar; whether a leg reports slope/delta-elevation (the DEM makes it free); how to dismiss (Esc, a close affordance, or tool-switch); whether rulers survive save/reload. Code: wire the `straighten` button in post-split eden_toolbelt.rs (drop `disabled=true`, add active state via TOOL_ACTIVE :72) with readouts in the readout block :3721-3766; click-chain capture in mission_editor.rs (`onpointerdown` :1395, `onpointerup` :1644, Esc in the keydown :1005-1030 — there is no other gesture host); tool-mode arbitration in select_tool.rs (`LeftGesture` :62 and `PendingLeft` :49 are the state machine a third mode must enter, `frozen_camera` :86 is the unproject it needs); the polyline model in a NEW `apps/website/frontend/src/ruler_tool.rs` (module name INFERRED; wave 0 pre-declares the stub). THE RULE THIS TICKET EXISTS TO HONOUR: removing `disabled` without the tool working is worse than the current state — it turns an honest stub into a lie. The corpus agrees twice over: FNF's `Use Default Loadout` checkbox is read by nothing in the entire worktree, and OFCRA's duration override is displayed to clients while the server ignores it.
- **T-650** (4010) — Compositions: save and place [queued] — MERGES T-078 (cancelled in favour of this row). Closes `COMP-SAVE-001`, `COMP-EDIT-001`, `COMP-PLACE-001`, `RIGHT-MODE-002` and the three metadata fields `ATTR-FIELD-COMP-TITLE` / `-AUTHOR` / `-CATEGORY`. Steam Workshop compositions (`COMP-WORKSHOP-001`, `COMP-SUBSCRIBE-001`) stay deferred — no equivalent exists. High corpus value: compositions are FNF v4's #1 strength, WOG ships 28 group presets, and Eden's F2 tab is Compositions, one of six top-level asset tabs. SCOPE AGAINST T-180 FIRST — CLAUDE.md records T-180 as already shipping "templates/vehicles", so part of this may exist. Code: `RIGHT-MODE-002` is a new palette mode — PaletteKind (eden_chrome.rs:1833) has only Character / Vehicle / Object with arms :1944-1950, tab strip :3041-3046, all now in post-split eden_dock_right.rs; save-selection is a read plus a write and place is a multi-slot paste, so `copy_selection` (editor_ops.rs:394) / `paste_at_cursor` :436 are the shapes to reuse. OPEN QUESTION, unresolved and load-bearing: WHERE COMPOSITIONS ARE STORED. Zero repo-wide hits for `save_composition` / `user_composition` / `custom_composition`; the only `composition` occurrences in the frontend are asset_catalog.rs:345-844, a path-string classifier that CONSUMES Bohemia's compositions. A title, a server-assigned author and a category are user-scoped rows, not mission-document state — if the operator accepts that framing this ticket drops `crates/map-engine-core/src/doc/store.rs` and gains NEW apps/website/api/src/handlers/compositions.rs + a migration + models/mod.rs + dto.rs, which is still `claude-code` (the Rust workspace, not packages/ or apps/mod/) and packs better. Re-open the owns row if that call is made.
- **T-658** (4020) — Validation: every placed asset resolves in the live catalogue [queued] — Rule content in the T-656 engine — modifies only `crates/map-engine-core/src/mission/validate.rs`. Revives the MissionAnalyzer's dead rule D13: every placed asset must resolve in the live registry, which catches modset drift the MOMENT an asset is placed rather than at compile. The upstream analysis marked this `on-save` "only as the conservative default for a cold registry" — THAT CAVEAT DOES NOT APPLY HERE: `apps/website/frontend/src/mission_editor.rs`:36-93 holds `mod registry_session`, a `thread_local!` SPA-session cache of the catalogue, so the check is live and free. Depends on T-656.
- **T-643** (4030) — Line of Sight: point-to-point ray [queued] — Operator-requested; ships BEFORE the viewshed. The second of the two dead buttons — the LoS stub is eden_chrome.rs:3715-3717 (`TOOL_DISABLED disabled=true`, MaterialIcon name="visibility"), adjacent to the ruler stub, which is why this and T-642 are one wave apart. Click observer, click target -> clear/blocked plus the terrain profile between them. Samples the loaded DEM (`crates/map-engine-core`, 6400^2 uint16, +/-0.204 m against 11 survey anchors at T-091.0). Cheap, immediately useful for checking a firing position, and it PROVES the DEM sampling path that T-644 then reuses. Code: the button in post-split eden_toolbelt.rs; two-click capture in the same pointer handlers as the ruler (mission_editor.rs); the ray walk and profile in a NEW `apps/website/frontend/src/los_tool.rs` (module name INFERRED; wave 0 pre-declares the stub); `crates/map-engine-core/src/dem/sample.rs` (232 lines, holds DemManifest and the elevation sampler) needs a SEGMENT sampler, not a point sampler. DECIDE INSIDE THE TICKET: eye height above ground for observer and target — a standing soldier is not the terrain surface — and whether the profile renders as an inline chart or an overlay. Same honesty rule as T-642: do not un-disable the button until the tool works.

## Dependency graph (scoped)

```mermaid
flowchart LR
  T118[T-118] --> T114[T-114]
  T114[T-114] --> T120[T-120]
  T115[T-115] --> T120[T-120]
  T117[T-117] --> T120[T-120]
  T150[T-150] --> T146[T-146]
  T159[T-159] --> T170[T-170]
  T661[T-661] --> T662[T-662]
  T661[T-661] --> T638[T-638]
  T662[T-662] --> T638[T-638]
  T661[T-661] --> T659[T-659]
```
