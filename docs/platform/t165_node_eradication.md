# T-165 — Node/JS eradication: every .mjs tool → Rust

**Status:** ACTIVE (T-165.0–.9 shipped; next T-165.10 closure) · **Executor:** claude-code (operator-approved
plan) · **Sequel to:** T-161/T-162 (Python eradication). **Plan of record:** the operator-approved
T-165 plan (session plan file); this hub tracks slice state + evidence.

## In one sentence

Port all repo-authored Node tooling (90 .mjs, 13.8k LOC — schema toolchain, map-asset pipeline,
T-159 CDP gate harness, MCP broker, misc verifies) to Rust; the only Node left standing is the
third-party `enfusion-mcp` runtime for Workbench (the floor), and no CI job needs Node at all.

## Locked decisions

| ID | Decision |
|----|----------|
| **N1** | `xtask` stays sync (schema gates, citations, codegen, small verifies); new async crate **`tools/tbd-tools`** owns CDP harness (`gate`), MCP broker (`mcpd`), map/world pipelines. |
| **N2** | `freeze.js`/`dom.js` are browser-INJECTED payloads — they survive as **verbatim `const &str`** in Rust; never re-implemented natively (frozen V-golden byte-parity by construction). |
| **N3** | Image policy: no external processes. Pure-Rust encoders everywhere except the **one lossy-WebP leg** (map-view pyramid; `webp` crate = cargo-vendored libwebp C). Lossless legs = `image-webp` (pure Rust). Committed assets untouched (no gate byte-compares WebP). |
| **N4** | Per-slice `.mjs` deletion is gated on the **reverse-dependency edge list** (createRequire borrowers of tbd-schema node_modules + cross-script path spawns). tbd-schema npm dies at the END of T-165.9 (last pngjs borrower). |
| **N5** | T-165.8 world artifacts: one-time migration — Rust double-build determinism + **decompressed-content byte-identity** vs committed + exact census (1623/1,216,109/315/888/36/625) → re-commit Rust-encoded artifacts → E6 stays raw-byte thereafter (node-zlib vs flate2 gz bytes differ by construction). |
| **N6** | mcp lane: broker+stub port only (`xtask mcp` client already Rust); selftest contract frozen — byte-exact stdout + exit codes 0/1/2/3/4, 19/19. |
| **N7** | quicktype → typify for the 4 tractable schemas; `loadout.rs` becomes hand-maintained (quicktype output provably lossy — empty `Wear{}`/`Equipment{}`; zero consumers) with serde round-trip tests vs fixtures. |

## Slice ladder

| Slice | Scope | Status |
|-------|-------|--------|
| **T-165.0** | `tools/tbd-tools` scaffold (workspace member; tokio + tokio-tungstenite pinned to lock-resolved versions) + dead-set deletion | **shipped** |
| **T-165.1** | Text/JSON gates → `cargo xtask schema …` (citations, t090-specs, n6, n10, map-object-enums, type-inventory, terrain-manifest, flatten-orbat-slots); parity proven side-by-side (8× verdict+exit MATCH, negative probes, count parity); Makefile + ci.yml + contracts.yml citation steps → cargo; 7 .mjs deleted (verify-type-inventory.mjs kept — spawned by census-types/validate-export until .8) | **shipped** |
| **T-165.2** | validate suite → `cargo xtask schema validate` + `validate-file` (Registry-resolved cross-file $refs, FK walkers, ENF-4 `$defs` pointer validators). Parity: **130/130 PASS both runners, label-set diff empty, negative probe rc=1 both**. ci.yml schema job + schema.yml + deploy-staging V1 → cargo (schema CI is Node-free); validate.mjs + validate-file.mjs deleted | **shipped** |
| **T-165.3** | codegen → `cargo xtask schema codegen` (typify; run-to-run hash-idempotent; regenerated 4 contracts — `regress`-validated patterns, an upgrade over quicktype). `loadout.rs` hand-frozen: faithful versioned oneOf + patternProperties wear/equipment maps + double-Option null-vs-absent, value-level round-trip tests vs BOTH fixtures. `registry_import.rs` adapted; contracts.yml codegen-drift job Node-free; codegen.mjs deleted; quicktype devDep dropped | **shipped** |
| **T-165.4** | Golden S-gates + terrain/DEM + labels → Rust. .4a tbd-tools libs (geometry/density/forest) · .4b S2–S14 golden gate (12/12 parity; S13 TBDD encode byte-identity; S14 js_num Value-equality) · .4c glyphs GL-G1..G6 + height-labels with the wasm-era branch RESTORED natively (declutter + ASL oracle — Node permanently skips these) · .4 close: locations G3–G7 + town-labels G1–G5+fade + road-names G3–G7 gates on `map-engine-core::world`, terrain-alignment (png-crate u16 decode + core `sample_elevation_meters`, **byte-identical** output incl. `js_fixed3` toFixed tie semantics; ±probe), t152 aggregator → `runCargo`, `verify-terrain`/`-strict` Makefile npm lines → xtask (**Makefile npm surface = 0**), 7 .mjs deleted (verify-{locations,height-labels,town-labels,road-names,terrain-alignment} + height-labels-export + dem-sample libs). KEPT per edge list: raw-u16-to-dem-png (spawner ports @ .8), locations-export lib (export-locations), verify-type-inventory (census-types), tbd-schema npm (ajv/pngjs borrowers → .9) | **shipped** |
| **T-165.5** | CDP harness core → Rust. `tbd-tools` lib: `cdp` (tokio-tungstenite CDP client — launch/flags/playwright-cache resolution, request-id map + one-shot & persistent event routing, evaluate/navigate/screenshot/Input/Fetch primitives), `serve` (axum static SPA + COOP/COEP + SPA fallback + apiProxy + mapAssetsDir), `inject` (freeze.js/dom.js **verbatim consts** + byte-parity test vs the .mjs sources), `vsuite` (25-route table, fixture interception, stability loop, diffNode, dist identity, manifest), `sroutes`. Bin: `gate v-suite <freeze|verify|accept>` / `gate s-routes` / `gate serve`. **Parity: verify 25/25 with BYTE-IDENTICAL stdout vs the Node runner** (`js_len` UTF-16 length semantics; `JSON.stringify`-undefined-key omission), s-routes byte-identical, golden-corruption probe rc=1/rc=1 byte-identical failure output, restore green. reqwest TLS dropped (loopback-only). Node driver untouched (deleted @ .6) | **shipped** |
| **T-165.6** | Smokes → Rust; **Node driver DELETED**. `tbd_tools::smokes` ports all 19 driver scripts: 16 editor smokes (selfcheck GPU readback, doc/persist/save-export bridges, pan/select/cur camera+pick, marquee-drag/undo/outliner-palette/attributes/keyboard-settings/arsenal trusted-input scenarios, hillshade map-asset lane, hydrate live-backend lane) + gate_r_auth + render-check + smoke_mutations. Bin: `gate smoke <name>` / `gate editor-suite` (glob-order, set-e semantics) / `gate r-auth` / `gate render-check`. **Acceptance: both harnesses green on the same dist — editor suite 16/16 node rc=0 + rust rc=0 (18 pass-markers each), V 25/25, r-auth + render-check + mutations byte-identical, empty-dist probe rc=1/rc=1.** Two stale gate contracts fixed identically on BOTH sides pre-parity (r-auth catch-all 401→200 — post-.25 dashboard boot queries looped refresh 326×; render-check expect → "COMMAND CENTER" — app renders line-broken brand). `make leptos-gates` = cargo; `git rm` driver/ (24 scripts + freeze.js/dom.js — the verbatim consts in `inject.rs` are now the single source) + manifests/extract-leptos-routes.mjs; README render-check doc → cargo | **shipped** |
| **T-165.7** | MCP broker + stub → Rust (`mcpd` in tbd-tools). Broker: tokio UnixListener, one enfusion-mcp child (init paid once), newline-JSON socket protocol, serialized `tools/call` queue, id→2 remap, per-call timeout (-32001) / child-unavailable (-32000), auto-restart on child exit, idle (1800 s) + max-life (4 h) self-reap with socket+pidfile unlink, SIGTERM/SIGINT cleanup. Stub: `--stub` / `MCP_STUB=1` (broker wins on `--socket` — child spawns argv-less so env-selection is recursion-proof), byte-exact STUB-OK/STUB-DAEMON-OK/STUB-STDERR-MARKER contract. Runner resolution FIXED per plan: `.js`/`.mjs` → node, else exec native (mcp-call.sh tier1 + mcpd both); `mcp-daemon.sh` launches the built binary via `lib/mcpd-bin.sh` (no node/cargo wrapper in argv; pkill → `mcpd --socket`). **`mcp-call-selftest.sh` 19/19 ALL PASS** (unchanged assertions, byte-exact strings, rc 0/1/2/3/4); probes: .mjs tier1 branch pre-delete rc=0, idle=2 s self-reap (socket unlinked, status stopped, zero strays). Deleted: `lib/mcp-daemon.mjs`, `fixtures/stub-runner.mjs` (41 tracked non-mod .mjs left). MCP_TOOLING.md synced | **shipped** |
| **T-165.8** | World-export pipeline → Rust + **E6 migration executed**. `tbd_tools::world`: `pak` (Enfusion .pak VFS — FORM/PAC1 chunks, entry tree, zlib, first-pak-wins), `topo` (.topo decoder), `edds` (EDDS+LZ4+**BC7 via pure-Rust bcdec_rs** — vendored-wasm replacement; Pillow-derived golden ported, **10/10 Eden cells RGBA hash-identical** vs node), `classify` (rules classifier + fatal-parse jsonl streaming), `build` (build-world-objects + build-roads: **945/945 scratch artifacts content-identical** vs Node incl. js_normalize for float-authored rule ints), `gates` (verify-phase G1–G12/P1-*/PH-P2-*/D/F/R/SIZE/E6 — E6 double-builds in-process; **P5 15/15 PASS, P2 failure-set exact parity 1232+312**), `aux` (validate-export-artifacts **byte-identical**, census-types + 3 spike gates **stdout-identical** + spike inventory byte-identical, copy-export-profile, catalog-sap-cells, raw-u16-dem-png, phase-gate). **N5/E6 migration: 318 committed .gz re-encoded via flate2 — 945/945 decompressed-content-proven vs Node build, zero non-gz changes, census exact 1623/1,216,109/315/888; E6 raw-byte green thereafter.** export-terrain.sh + verify-spike-all.sh + Makefile (map-export/-validate/-verify-phase/-census) → `world` bin; E2c literal-id gate scans the Rust modules. Deleted 16 .mjs (41→25). KEPT for .9: decode-topo/decode-edds/vendor-bc7 (image-lane importers) | **shipped** |
| **T-165.9** | Image pipeline → pure Rust (`tbd_tools::map` + `map` bin); **tbd-schema npm DELETED**. `img` (png/Lanczos/HSL/stddev-with-magick-alpha-semantics/box-blur/WebP header parse; **image-webp lossless + `webp`-crate lossy — the one vendored-C leg per N3**), `sap` (seam metrics + verify-sap-seams **byte-identical** + verify-sap-ortho (native orientation guard; AE 0.008 vs 0.003, margin 0.2) + stitch + bridge), `unified` (verify **byte-identical**, builder), pyramid verify **byte-identical** (sat+map views) + builder (lossy leg smoked z0–1), `glyphs` (resvg atlas — **mapping JSON identical to committed**, `xtask schema map-glyphs` green on the Rust-encoded atlas), `carto` (landcover + cartographic compose with resvg road strokes replacing magick MVG + 3 `node -e` patch subcommands + verify-t152 aggregator rc=0 end-to-end), `water` (composite + analyze — **153/153 accepted bodies exact** on the pre-water ortho, road corridor px exact, rejects 9153 vs 9154), `labels` (export-locations **byte-identical to Node AND committed** after the localeCompare sort fix; export-height-labels **natively restored** — wasm-dead since T-159.29.3 — output **byte-identical to committed**). Makefile image lane (14 lines incl. the 3 `node -e`) → `map` bin; **28 files deleted** (decode-edds/topo, vendor bcdec wasm/C, stitch/blend/seam lib, 6 verifiers, 5 builders, 3 exporters+libs, build-tile-pyramid.sh) + **packages/tbd-schema package.json/lock/node_modules** — npm fully out of the schema package; gate-7 tolerates the deleted manifest. Tracked non-mod .mjs **25 → 2** (.10 scope). Also fixed en route: T-165.8.2 stale make recipes + catalog E2c-allow placement | **shipped** |
| T-165.10 | Bash logic + closure: 0 tracked .mjs; `verify no-node` gate; zero setup-node in CI; doc sync | queued |

## T-165.0 — dead-set deletion evidence (deleted, not ported; ~650 LOC off the port surface)

| File | Evidence it was dead |
|------|---------------------|
| `driver/gate_v.mjs` | live-React-oracle V diff — React deleted at T-159.29.3; superseded by the frozen `gate_v_suite.mjs` |
| `driver/spike_upload_140mb.mjs` | self-labeled transport-spike evidence (T-159.24 verify log records the result); not a suite gate |
| `manifests/extract-react.mjs` | first read is `apps/website/frontend/src/router.tsx` — tree deleted; the 5 S-manifests are frozen goldens (t159_gates README §Status) |
| `scripts/website/differential.mjs` + `run-differential.sh` + `differential_seed.sql` | T-145 Go-vs-Rust differential — the Go backend it boots no longer exists |
| `scripts/website/tools/scrape-eden-wiki.mjs` | one-shot scraper; outputs committed under `.ai/artifacts/eden-wiki/` |
| `scripts/verify-monorepo-migration.sh` (+ `make verify-migration`) | **already red**: V15 runs `go build ./...` in `apps/website` (zero `.go` files since T-145); migration long complete |

Recovery: all in git history @ tag T-164 and earlier.
