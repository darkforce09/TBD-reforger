# Coding-standards opt-out allowlist (CODING_STANDARDS.md §8.1). File-level only.
# Created T-125.2; consumed by `cargo run -q -p xtask -- verify file-length` (T-165.1 port).
#
# Function-level opt-outs live INLINE (#[allow] with a reason), never here. Only file-level
# SIZE-2 (MC-perf) and SIZE-3 (named debt) entries belong in this file.
#
# T-171: the Go/React-era entries (apps/website/internal/**, apps/website/frontend/src/**.tsx)
# were removed — those files were deleted at the T-145 Go→Rust and T-159.29.3 React cutovers.
# The list is currently empty; add entries as `- rule: SIZE-x` / `path:` / `reason:` /
# `expires:` blocks when a file needs a documented opt-out.
