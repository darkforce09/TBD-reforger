# T-152.22 operator screenshots

One PNG per O-row minimum (12 files):

```
o01_map_loads.png
o02_fences_z1.5.png
o03_harbor_piers.png
…
o12_basemap_switch.png
```

Capture from Mission Creator @ Everon, **Map** basemap, after `make web` + hard reload.

Commit with T-152.22 close-out tag (LFS if large).
