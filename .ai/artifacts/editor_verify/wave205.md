# Editor wave 205 — adversarial verify

**Target:** merged main `44eb1412` (base `97838b48`; merges f7866101 T-795 · f9e72d96 T-799 · c67688aa T-806 · barrier reconciliation `44eb1412`). SPA :3000 (trunk **DEBUG** watching main), API :8080, dev-login.
**Method:** the wave-204 stdlib-CDP driver reused from the session scratchpad (playwright chromium-1228 `chrome-linux64` `--headless=new`, SwiftShader, fresh browser/profile/mission per probe, `?force=webgl&sat=preview`). 10 live probes (A, B, B2, C, C2, D, D2, E, E2, F), pixel analysis via the scratch PNG decoder, `wave.sh test --slice T-795 -p website-frontend` into the private per-slice target dir, **5 pin perturbations across 4 files** (each restored byte-exact — `cmp` verified — + touch, verbatim reds), ajv 2020-12 (ajv 8.20.0 from `scripts/mod/node_modules`, node run with a borrowed `libatomic.so.1` from a container overlay — read-only, nothing installed). Scope purity: T-795 merge tree == slice tree (empty diff); T-799 and T-806 merge deltas byte-identical to their slice deltas (diff-of-diffs empty); the barrier commit touches exactly `eden_top_strip.rs` (+6/−17) and is exactly the pre-declared `widget_is` → `widget_digit()==digit` reconciliation. The wave touched exactly 5 files.

---

## Findings

`SEVERITY | file:line | what is wrong | how you proved it`

**MAJOR | T-795 FIX(c) "heading arrow stays visible" vs crates/map-engine-core/src/slots_gpu.rs:96,268 (yaw hardcoded `0`) + crates/map-engine-render/src/scene.rs:292 (glyph 0 = symmetric ring r24/r10) | The slice's NOT-REPRODUCING claim for the heading-arrow clause is false as measured: there is NO heading visualization for slots in rotate mode — or any mode — so the clause it closed as "already fixed since the review" is actually OPEN, along with the "(live heading preview)" parenthetical. | Live: selected slot, Rotate mode, ring drag released due EAST → compiled `headingDeg` 90.0 exactly; second drag released SOUTH → 180.0 exactly; the two 68×68 screenshots around the slot (`b205_arrow_east.png` / `b205_arrow_south.png`) are pixel-identical (ink quadrants E219/W141/S194/N177 both times) — nothing on the map moved when the heading turned 90°. Code: every slot instance packs `yaw = 0` (`slots_gpu.rs:96`, selected patch `:268` — "slot rows always carry yaw 0 / glyph 0") and glyph 0 is the rotationally-symmetric ring (`marker_glyph_coverage` cell 0), so no heading can render; `pack_vehicle_instances` likewise takes no yaw. The directional streak visible in `b205_rotate_slot.png` is a terrain contour line. NOT the acceptance: every registry ACCEPTANCE item for T-795 measures green (see register) — this is a claim-vs-pixels finding on the FIX(c) sub-clause, which needs a re-open or its own ticket, not a revert.**

**MINOR | apps/website/frontend/src/mission_editor.rs:4996-5006 (ring-drag comment) vs editor_ops.rs:1676 `rotate_selection_to_face` | The ring drag does not "rotate the selection about its centre" (the ticket FIX wording, repeated verbatim in the gesture comment): it sets each entity's heading to FACE the release aim, each about its OWN position — positions never move, relative headings are not preserved. | Measured: 2 slots at (5920,6560)/(6760,6120), ring drag released at world (6340,6172) → headings became 132.732° and 277.058° — each EXACTLY the compass bearing from that slot to the release aim (expected 132.732/277.058), both positions unchanged to 1e-9. A centre-rotate would have orbited the positions and applied one common delta. The single-entity case degenerates to exact agreement ("heading changes by the drag angle": released east → 90.0, south → 180.0), so the acceptance line holds; the multi-select semantic is an Eden-parity delta that `rotate_selection_to_face`'s own doc discloses ("each entity about its OWN position") but the ring comment and ticket wording overstate.**

**NIT | apps/website/frontend/src/eden_top_strip.rs:132 + mission_editor.rs:1330 | Two doc comments still describe the PRE-renumber keymap: `SetWidget` doc says "from its `1`/`2` digit (Translate / Rotate)"; the dispatch overview says "`set_widget` (Digit1/2 → `from_digit`)". Both contradict the shipped 1 None / 2 Translate / 3 Rotate (the field-level docs beside them are correct). | Read from merged main; the live behavior (probe A) follows the new mapping on every surface, so these are stale prose only.**

**NIT | apps/website/frontend/src/mission_editor.rs:1353-1355 `widget_is_rotate` | The dispatch field is dead after the barrier — zero readers anywhere (the doc's justification "Retained for the T-797 Rotate plate that reads it directly" stopped being true at `44eb1412`, which moved that plate onto `widget_digit`). | `grep widget_is_rotate` across the frontend: only the declaration (:1355) and the registration (:3611) remain. Harmless (native no-op), but the doc claims a reader that does not exist.**

**NIT | apps/website/frontend/src/eden_dock_right.rs:3887 armed-panel copy | "Placing a Attack marker" / "a Observation post" — the indefinite article never agrees now that T-806 humanized the armed label (pre-wave it interpolated the raw slug). | Probe D/F: `armed_copy == "Placing a Attack marker"`. Cosmetic; surfaced by, not regressed by, this wave.**

**Notes (documented, no severity):**
- **The barrier's three-way read has no native pin.** `widget_is`'s `active == (d.widget_digit)()` semantic is covered live (probe A) but by no cargo test: the wave-202 order pin (`plates_subscribe_to_dispatch_generation_before_reading_it`) pins the generation-read ORDER (perturbation P5 proved it bites) and the T-634 pin pins the three buttons' existence, but a revert to the old two-way `widget_is_rotate` read with the generation read kept would stay green natively. Recipe: add a `widget_digit` needle to the `widget_is` body check in the wave-202 pin.
- **Probe honesty:** probe C's "second click did not fire" was a probe artifact — the compiled-export toast covered the right-anchored dropdown rows, so the CDP clicks landed on the toast. Probe C2 re-ran with `elementFromPoint` hit-checks: every activation hittable, every activation = exactly one blob + one anchor, all four row-click `timeStamp`s distinct (28726.5 / 30535.3 / 32347.4 / 40565.0), blob times 1:1 with them.
- **The compiled document carries no `gameMode` surface at all** (schema `meta` has none; measured top-level keys `environment/factions/flow/meta/orbat/radioPlan/schemaVersion/slots/winConditions/zones`), so the acceptance's "identical gameMode" leg is only satisfiable as "the JSON export sources it from the row" — which it now does (`pve_coop` measured). There is nothing left on the compiled side to disagree.
- **Known environment issues seen and not filed** (per brief): none reproduced — fresh browser per probe avoided the second-engine-boot crash; `__editorCamSet` under `?force=webgl` panicked zero times across 10 probes; chrome logs carry no crash/fatal lines.

---

## Is main safe to build the next wave on? **YES.**

Tree clean at `44eb1412` (report is the only addition); frontend suite **1148 / 0 / 0** (+10 over wave 204's 1138, matching the 10 new `#[test]` fns), re-green after all perturbations restored; all 5 perturbations went verbatim-RED across 4 files; scope purity exact for all four commits; every live acceptance for all three tickets measured green; the MAJOR is a false "already-fixed" claim on a FIX sub-clause (heading visualization), not broken code or data risk — it needs the clause re-opened, not a revert. All 12 probe missions deleted (204s; final list shows zero `VERIFY205 *`).

---

## Verified clean (measured)

**Key-map parity sweep (attack 1) — six surfaces, zero disagreements (probe A):** press 2/3/1 → hint `Translate`/`Rotate`/`No Widget` and plates `[0,1,0]`/`[0,0,1]`/`[1,0,0]`; digits 4/5 change NOTHING (census: reserved-unbound, no keydown arm, no help row — live-confirmed); toolbar buttons (aria `No widget`/`Translate widget`/`Rotate widget`) each light exactly their own `TOGGLED_PLATE` (`bg-primary/20`) and the hint agrees; Edit-menu rows `Widget: No Widget (1) / Translation (2) / Rotation (3)` present and dispatching (click → plate+hint flip); eden_help SHORTCUTS rows read exactly `1 No widget (bare drag still moves the selection)` / `2 Translate widget` / `3 Rotate widget (drag the ring to rotate the selection)` with no 4/5 row; digits typed INSIDE the title input and the dock search do not switch modes (Rotate held through `1`,`2` keystrokes — `in_editable_field` guard live); Space still centres (camera panned 900/700 m away flew back to the selection; widget mode untouched). Census prose re-derivation (22 codes / 35 bindings) pinned in-suite (`the_prose_census_numbers_are_derived` green).

**Rotate ring is a control (attack 2, probes B/B2):** ring drawn at the pivot with **r = 42 SVG units == `WIDGET_RADIUS_PX`** (the shared const, live). Drag FROM the ring: selection PRESERVED (2 → 2), one gesture = one txn, **ONE Ctrl+Z reverted both headings to 0.0**. Hit-test boundary at the shared band [32, 52] px: press at **54 px** (band + 2) → MARQUEE (selection destroyed to 0, headings untouched); press at **50 px** (band − 2) → ROTATE (selection kept 2, headings moved 127°/278.5°). Inner edge native-pinned (`press_on_ring_grabs_the_ring_band_only`: centre rejected, ±(tol−0.5) accepted; perturbation P2 disc-widening RED "the centre is not the ring"). Empty-ground marquee works in ALL THREE modes (enclosing marquee → SEL 2 in None/Translate/Rotate; empty click clears). 'No Widget' direct drag on a selected entity still moves (both selected slots translated +320/+192 m — Eden drag-move semantics via the variant-independent LG::Move). Translate gizmo RENDERS on selection (DOM: 2 axis lines + 2 arrowhead polygons + centre dot; 2,770 ink px at the pivot; screenshot `b205_translate.png`) — T-795 (b) not-reproducing claim CONFIRMED. Rotate overlay = ring + centre dot (`b205_rotate_slot.png`).

**T-799 exports (attack 3, probes C/C2):** blob-intercept live — **one click = exactly ONE `createObjectURL` + ONE anchor click** for Export JSON AND Export Compiled, from the export dropdown AND the File menu (File-menu row also latch-gated: fired once). A REAL second click (distinct `timeStamp`) still exports — measured twice per export type, 4 activations : 4 blobs : 4 anchors. Retitle via the strip title input ("VERIFY205 RETITLED") then export both: JSON `title == "VERIFY205 RETITLED"`, compiled `meta.name == "VERIFY205 RETITLED"` (the compile-time live-doc override); `maxPlayers` row-sourced and identical (JSON `maxPlayers: 32` == compiled `playerRange: [1, 32]` == the create-dialog row); JSON `gameMode: "pve_coop"` from the row. Filenames exact: `mission-<id>.json` / `mission-<id>.compiled.json`. camelCase intact: zero snake_case keys to depth 4 in either payload. **Compiled ajv-validates CLEAN** vs `packages/tbd-schema/schema/mission.schema.json` (ajv 8.20.0, 2020-12 dialect, allErrors, zero errors — the F-34 baseline holds). Latch rule pinned native: same-stamp duplicate dropped, later stamp passes, `0.0` never latches (perturbation P3 RED with the verbatim zero-stamp message).

**T-806 picker (attack 4, probes D/D2):** row count **11 == `CANONICAL_MARKER_GLYPH_COUNT` == `scene::MARKER_GLYPH_COUNT`** (compile-time `const _` tie exists at eden_dock_right.rs:3580, wasm32-gated, and the const's doc comment names the source; runtime `debug_assert` + self-healing fallback in `canonical_marker_rows`); labels `Dot/Objective/Point of interest/Observation post/Destroy/Attack/Defend/Waypoint/Flag/Medical/Circle` — case-folded unique (no case-twin rows); **11 DISTINCT SVG DOM signatures** (tag+attribute serialization per row: circle/rect/polygon/path structures all differ; zero generic `place` pins in the picker); slug demoted to tooltip (`Slug: …` on all 11). Search matches slugs AND names AND folded aliases: `waypoint` → Waypoint, **`wp` → Waypoint** (alias), `rally` → Flag, `medic` → Medical, `poi` → substring family, exact name works, nonsense → "No icon matches." Picking **Attack** arms the canonical slug, places on click, and the DOC stores `"attack"` (the only `icon` value in the export envelope). The MAP draws the attack glyph — triangle-up visible (`d2_z*` shots) and the T-790 register holds: attack/defend/flag placed via the NEW picker give 3 distinct 8×8 ink signatures at all 3 zooms (pair diffs 15/18/17 at 5.58 m/px · 21/17/20 at 1.30 · 18/12/14 at 1.06 — wave-204 magnitudes). Placed-markers list below unchanged: caption-first rows (`Attack (BLUFOR)` + `font-mono` position) — the T-825 boundary respected, no new editing surface. Perturbation P4 (`attack`→`assault`) RED with "'Attack' must store the canonical slug `attack`".

**Hollow-pin sweep (attack 5):** 11 new test fns this wave (mission_editor 2 — `widget_variant_matches_eden_numbering` replacing the old cycle pin + `press_on_ring_grabs_the_ring_band_only`; mission_commands 7 t799 pins + 1 helper; eden_dock_right 2 picker pins) plus materially-updated existing pins (eden_help census tables/prose for Digit3 · eden_top_strip T-634 three-button pin + T-797 dispatch pin). Ten of eleven are PURE-fn tests (cannot be hollow — they call production code); the one new `include_str!` pin (`picker_rows_draw_glyph_svgs_and_arm_the_canonical_slug`) fragment-assembles every needle, scopes to the `Marker icons`→`Authored markers` block, uses `only_body` (0-or-2+ panics) for the SVG fn, and each needle resolves to exactly ONE production occurrence (grep-verified :3816/:3862/:3866). **5 spot-perturbations across 4 files, each RED with its own verbatim message, each restored byte-exact (`cmp` clean) + touch → suite re-green 1148/0:** P1 `from_digit` 1→Translate (mission_editor) · P2 ring band→whole disc (mission_editor) · P3 zero-stamp guard dropped (mission_commands) · P4 canonical slug swap (eden_dock_right) · P5 generation-read reordered after dispatch-read (eden_top_strip — the wave-202 order pin caught the barrier-adjacent edit).

**Register re-checks (attack 6):** **eden_top_strip** — chip t0 `data-issue-total=0` "No issues"; hint+dropdown Esc ladder peels ONE per press (`{hint,dd}` → `{hint}` → `{}`, probe E2); ONE hint card, Help-menu row wears the check while open, toggle closes, View menu carries no duplicate (menu itself removed); Save-dialog rect smoke: version input in-viewport at **1920×1080 (top 495 / bottom 521)** AND **1366×768 (339/365)** with **ZERO backdrop-filter ancestors**; dialog Esc closes the dialog alone; dropdown panel anchored and visible at the chip (top 55, left 1295, w 320). **mission_editor** — keep-multi: ctrl-click ×3 → SEL 3 → dblclick member → "3 slots selected · multi-edit", SEL unchanged 3 → Esc closes modal, SEL still 3; composition stamp: save "V205 Trio" (3 selected) → arm → map click → `slot_count` **3→6** → ONE undo → **3**; armed marker pointerup placement: arm → click → dock rows 0→1 (the ring short-circuit did not perturb the promotion path); KeepArmed: arm survives a dock-tab flip, Esc cancels it; zone-draw Esc mid-drag creates nothing (T-792 pins additionally green in-suite). **eden_dock_right** — NATO→US_Army merged tree renders + `class:/mod:` grammar hint; Favourites | Recently placed sub-tab pair present, recently-placed rows head-first (composition + US Rifleman); catalog failure-view + composition pins green inside the 1148 (no DB flip this wave, per wave-204 precedent). **T-790 marker render at 3 zooms** — re-measured through the new picker (above).

**T-795 NOT-REPRODUCING claims (attack 7):** (b) translate gizmo on selection — **CONFIRMED present** (DOM + 2,770 ink px + screenshot). (c) heading arrow in rotate mode — **REFUTED** (the MAJOR above): no heading renders anywhere, any mode, any zoom probed.

**Suites:** website-frontend **1148 / 0 / 0** (17.6 s, private dir `~/.cache/tbd-target-T-795`, deleted after) — run before perturbations and re-run after all restores. map-engine-core / map-engine-render untouched this wave (frontend-only + the ajv step) — not re-run, per brief.

## Attacked and FAILED to break
Scope purity of all four wave commits (diff-of-diffs empty ×2, tree-equal ×1, barrier exact) · the six-surface key-map agreement (keyboard, hint, three plates, Edit menu, help rows, census) · digits 4/5 inertness · the editable-field digit guard (title + dock search) · Space flyTo + mode preservation · the ring band's outer boundary from both sides (54 px marquees, 50 px rotates) · selection preservation through ring drags · face-aim heading math (exact to 3 decimals, both slots) · position invariance under ring rotate · one-undo-reverts-all · empty-ground marquee in all three modes · No-Widget drag-move · translate-gizmo presence (DOM + pixels) · the export latch from three activation sites (dropdown ×2 rows, File menu) · one-blob-one-anchor per gesture · real-second-click passage (4/4 distinct stamps) · retitle propagation into BOTH payloads · row-sourced maxPlayers/playerRange agreement · compiled ajv 2020-12 cleanliness · filename + camelCase contracts · the 11-row picker census (count, labels, case-uniqueness, SVG distinctness, slug demotion) · slug/name/alias search including 'wp' · the pick→store→render handshake (slug `attack` in the doc, triangle-up on the map, 3 distinct glyphs × 3 zooms) · the placed-markers caption-first boundary · all 5 pin perturbations (verbatim RED, byte-exact restore, re-green) · the include_str pin's needle uniqueness · keep-multi/dblclick multi-edit · composition save/arm/stamp/one-undo · armed-pointerup placement + KeepArmed + zone-Esc · the strip's Esc ladder, one-hint-home, Save-dialog rects at both viewports, dropdown anchoring · the full frontend suite twice.

## Environment left as found
HEAD `44eb1412`, `git status` clean (this report is the only addition; all four perturbed files `cmp`-identical to their pre-perturbation copies and touched). All 12 `VERIFY205 *` missions DELETEd (204s in every probe's stderr; final API list shows **zero** remaining). My chromiums dead (`pgrep ms-playwright` empty), all `profile-205*` dirs removed, chrome logs crash-free. Private target dir `~/.cache/tbd-target-T-795` (1.1 G) deleted after the final suite run. No packages installed (ajv ran from the repo's existing `scripts/mod/node_modules`; node borrowed `libatomic.so.1` read-only from a container overlay via `LD_LIBRARY_PATH`). DB rows untouched beyond the deleted probe missions.
