# Editor wave 204 — adversarial verify

**Target:** merged main `b008bfc8` (base `a07a755e`; merges 3c1eab05 T-790 · fcec12e7 T-798 · 3c9e7024 T-803 · completion 2318dcf8 · rustfmt b008bfc8). SPA :3000 (trunk **DEBUG** watching main), API :8080, dev-login.
**Method:** stdlib-CDP driver rebuilt from `tools/tbd-tools/{smokes,cdp}.rs` idioms (playwright chromium-1228 `--headless=new`, SwiftShader, fresh browser/profile/mission per probe — one engine boot per browser). 20 live probes (A…M), pixel analysis via a scratch PNG decoder, `wave.sh test` runs into private per-slice target dirs (deleted), one pre-wave dist build (a07a755e worktree, host trunk) for a smoke bisect, 5 pin perturbations (restore byte-exact + touch, verbatim reds). Scope purity: each merge == its slice commit file-for-file; the wave touched exactly 8 files.

---

## Findings

`SEVERITY | file:line | what is wrong | how you proved it`

**MAJOR | T-798 acceptance-as-written (registry T-798) vs editor_ops.rs:4654 `ensure_side_faction` + map-engine-core/src/mission/validate.rs:736 `declares_players` | The error-from-nowhere F-11 exists to kill is ALIVE on a truly-fresh mission, and the ticket's own acceptance cannot hold: "fresh empty mission: chip shows 1 error at load" is false (shows 0), and "place a marker: count unchanged" is false (0→1). | MEASURED truth table on a fresh `VERIFY204 A` mission: t0 chip = `data-issue-total=0`, "No issues"; after ONE armed marker place the chip flipped to `1 · "1 error"` within 0.7 s; after markers 2 and 3, count unchanged at 1. Mechanism as the slice disclosed: the first marker mints `faction-{SIDE}` (`ensure_side_faction`), flipping V1's `applies` gate (`declares_players`) — V1 is DELIBERATELY conditional (T-658: a factionless draft is not "missing" a spawn), so an empty mission truthfully reads 0 and the first mark still summons V1. The slice's shipped halves are real (see register: load-time eval on hydrated missions works, chip/dropdown/Backspace/contrast lanes all measured) — the residual is the faction-minting path, which is OUTSIDE this slice's files and cannot be fixed in validation_panel/eden_top_strip. The acceptance as written is unachievable against shipped V1 semantics; either the minting path or V1's gate needs its own ticket, and T-798's acceptance line needs rewording to the hydrated-mission form it actually delivers.**

**MINOR | apps/website/frontend/src/eden_top_strip.rs:1829 (chip accent) | The chip's error-count contrast passes the review's own plate-calc method but the LIVE effective contrast over the 55 %-alpha glass strip measures below 4.5:1 over normal terrain. | Calculated, not eyeballed. Live text pixels are exactly `#f87171` (`text-error-alert`; computed style `rgb(248,113,113)` — the F-36 fix is in). Static pair vs the plate token `#1f2937`: **5.31:1** (review F-36 used this method: `#ef4444` vs plate = 3.90:1, matching its 3.9 figure — so by the ticket's method the fix passes). But the strip is `bg-surface-container-lowest/55 + backdrop-blur` over the map: measured effective backgrounds behind the count across 4 screenshots gave ratios **4.34 / 4.19 / 4.05 / 4.01** (AA-rendered fg) and 4.31–4.50 (pure token fg). Improvement over the 3.9 defect, and AA-passing on the opaque token — but a strict live measurement lands under 4.5 over bright terrain. One step darker plate under the count (or an opaque count pill) would close it.**

**MINOR | crates/map-engine-render/src/scene.rs:406 `pack_marker_caption_bytes` + engine.rs:4546 (`self.zoom()` at bind time) | Marker captions are NOT screen-space (the ticket's word): gap and glyph size are frozen in WORLD meters at the zoom current when the lane last bound, so zooming without a doc edit scales the caption and pushes it past the 40 px acceptance window at close zoom. | Measured: markers bound at boot zoom (4.0 m/px, design gap 14 px → 56 m). First caption ink from the marker centre: ≤26 px at 5.58 m/px, **43 px at 1.30 m/px, 52 px at 1.06 m/px** — an exact fit to the frozen-world model (predicted 10/43/52.8 px), exceeding "within ~40 px" at 1.06 m/px until any doc change rebinds. The ICON half is correctly screen-space (glyph ink stable 531/490/420 px across the three zooms). Captions stayed readable and unambiguous at all 3 zooms (screenshots `a_z*_flag.png` — "RALLY ALPHA" beside the pennant); any edit re-normalises; but zoom alone never rebinds the lane. A zoom-bucketed re-pack (or packing at draw zoom like place-name labels) is the fix shape.**

**MINOR (pre-existing — NOT this wave; bisected) | tools/tbd-tools/src/smokes.rs:2812 `v5_orbatWindowed` (and :2806 — the ORBAT-modal h2 `wait_for` result is discarded) | The gate's `virtual-outliner` smoke fails deterministically in this environment — on the merged dist AND on a pre-wave dist — so if the wave-close gate runs the editor suite next, it will fail on something wave 204 did not break. | `gate smoke virtual-outliner --dist dist-gate-frontend` (the gate's own release dist of merged main, built 07:36): `v5_orbatWindowed: false`, `pass: false` — **3/3 runs**, with v1–v4 and v6 all true (so the left outliner windows fine in release; the failure is the ORBAT modal's stats never reading `total>50 && rendered<total`). Built a dist from PRE-WAVE `a07a755e` (throwaway worktree, host trunk, gate trunk cache) and ran the identical command: **identical failure signature** → wave 204 is not the cause. The smoke also discards the modal-open wait result, so a modal that never opens fails silently into v5. Not filed, not fixed — flagged for the operator/close gate.**

**NIT | apps/website/frontend/src/eden_dock_left.rs:1091 `first_folder_label` vs editor_ops.rs:4496 `ensure_layer` | Two resolvable divergence edges between the strip's named fallback and the real destination — not reachable in normal flows, not reproduced live. | (a) `first_folder_label` answers with the first TOP-LEVEL Folder; `ensure_layer` takes `rows.first()` of ALL layers (id-sorted, nested included) — a drag-reparent that nests the lexicographically-first layer under a later one makes the two disagree. (b) layer ids sort lexicographically (`layer-10` < `layer-2`), so "first" shifts once ≥11 layers exist — both resolvers shift TOGETHER (same `layer_rows` sort), so only edge (a) can lie. In every measured state (fresh doc, created layers, stale-active fallback) strip and landing agreed exactly (see register).**

**Notes (documented, no severity):**
- **Gate glyph-coverage honesty (attack 6):** nothing in the gate can catch a marker-glyph render regression. `smokes.rs` has zero references to marker glyphs/MarkerComposite; the 72 native map-engine-render tests (list == run, 0 filtered) pin the PURE halves (alias table, atlas bytes, caption packing, cells 0/1 byte-parity) but the `BatchPayload::MarkerComposite` DRAW arm's only live coverage is this report's screenshot probe. Recipe: extend `smoke_fullmap` (or add `smoke_markers`) — place two markers with different icons via the dock, `readback_rgba`, assert the two 52 px cells have distinct ink masks and a caption band beside the labelled one.
- **Debug-build windowed-tree crossing kills the headless renderer:** crossing 50 flattened outliner rows on the DEBUG trunk bundle under headless SwiftShader hung/killed the renderer 4/4 (a `seed_slots` batch crossing 40→50 hung > 600 s; three layer-chain runs died with zygote fork errors at ~45–50 rows; a 48-row run was healthy: stats `total=48=rendered`, threshold 50). RELEASE windows fine (gate smoke v2/v3/v6 green on the merged dist). Consequence: T-803's ">50 scroll-out/in, treatment intact" check remains **uncaptured live** — the release smoke proves windowing + windowed-row-select on merged main, the per-row reactive `is_active` closure is the structural half, but the exact scripted check needs a release serve. Recipe: run the same probe against `trunk serve --release`, or add a drop-target arm to `smoke_virtual_outliner`.
- **Hydration window shows "No issues":** on a hydrated mission the chip reads "No issues" from 0.4 s until hydration completes (12.0 s under debug SwiftShader; the flip needed zero edits). The `rechecking` signal exists but the chip does not surface a "checking…" state. Cosmetic.
- **Save-version prefill collision (wave-203 MINOR, unchanged):** the dialog still prefills `0.1.0`; on a mission whose initial version is 0.1.0 the save 409s silently from the operator's view (probe A's save "succeeded" in UI flow terms but persisted nothing until an explicit `0.2.0` was typed). Known, filed last wave, untouched by this wave.

---

## Is main safe to build the next wave on? **YES.**

Tree clean at `b008bfc8`; suites 1138/0 (frontend), 72/0 (render, `--list`==run), 655/0 (core, --all-features); all five perturbed pins went verbatim-RED and restored GREEN; no data at risk (every probe mission deleted, 204s confirmed); the MAJOR is an acceptance-vs-shipped-semantics conflict whose code halves all measure correct — it needs a scoping/re-ticket decision, not a revert; the one deterministic smoke failure predates the wave (bisected against a pre-wave dist).

---

## Verified clean (measured)

**T-790 (a) outside-owns audit:** `git show 44a69763 -- mission_history.rs` is EXACTLY the disclosed edit: the two T-760 feed sites (`rebind_engine_from_doc` :361, `after_doc_change` :417 — 2 lines each: 4-tuple destructure + widened `markers_bind` call) + `marker_lane_xy_tints` collapsed to a one-line delegation to `mission_editor::marker_lane_fields` (doc comment updated). Nothing else smuggled. Feed pins bind: deleted the :417 feed line → `t760_markers_bind_feed::rebind_and_after_doc_change_both_feed_markers_bind` RED with its verbatim "after_doc_change must call markers_bind" message → restored byte-exact + touch → GREEN. T-780 pins (`t780_connections_bind_pick_bridge`, `t780_connection_line`) green inside the 1138/72 runs.

**T-790 (b) live at 3 zooms:** placed attack/defend/flag + caption "RALLY ALPHA" (via the real dock: arm on pointerdown, map click, row-select, Marker text input). Dock rows read `6400,6400 / 6400,7000 / 7280,6400` — exactly the camera math of the click points. At 5.58 / 1.30 / 1.06 m/px (`__editorCamSet`, no vulkan panic seen), camSet to each marker's DOCK coords put its glyph ink centroid within ≤10.1 px of canvas centre (dock-vs-map agreement). 3 DISTINCT glyph signatures at every zoom: 8×8 ink-mask signature pair-differences 14/19/15 (5.58), 21/17/20 (1.30), 18/12/14 (1.06) of 64 cells; visually triangle-up / triangle-down / pennant (screenshots). Caption beside its marker at all zooms (gap numbers → MINOR above); uncaptioned controls show no adjacent text.

**T-790 (c) alias table:** schema `$defs/marker.icon` enum = 64 keys; the match covers 59 explicitly + the 5 dot-family keys via the documented `_ → Disc` fallback = **64/64, zero strays, zero duplicate arms** (set-checked against the schema). Case/separator folding (`normalise_alias`: trim + lowercase + `-`/space→`_`) collapses `Waypoint`/`waypoint`; unknown alias → Disc — both pinned natively (`marker_glyph_mapping_is_distinct_and_folds_case`, `every_schema_alias_maps`), and the mapping pin went RED when defend was collapsed onto attack's glyph (perturbation 5, restored).

**T-790 (d) engine hygiene:** `clear_stress` explicitly destroys BOTH MarkerComposite buffers (engine.rs:2119-2126); re-binds go `upsert_lane→remove_lane→Drop` (frees on Drop, the shared lane convention). Live leak probe: 12× (arm dot → place → Ctrl-Z) at 1.41 m/px — 0 exceptions/console errors, marker rows restored to 3. Atlas widening: cells 0/1 byte-parity pin green (slots/vehicles/comments pixel-unchanged).

**T-798 measured:** floating card gone (no `[data-validation-panel]`; pin `the_floating_card_is_gone`). Chip in the strip head with `data-validation-chip` + `data-issue-total`. Dropdown: opens on click; **Esc closes it in one press** (twice measured); co-sits with the Controls Hint (both transients) and the ladder peels ONE per press: `{hint, dropdown}` → Esc → `{hint}` → Esc → `{}`. While the Save dialog is open the chip is COVERED (elementFromPoint) — the dropdown structurally cannot contest the dialog — and one Esc closed Save ALONE (chip untouched, no other overlay). Click-away scrim eats outside clicks (measured: a strip-button click with the dropdown open closed the dropdown and did not press the button). V1 copy BYTE-VERBATIM vs the engine string ("This mission declares a faction but has no slots — there is nowhere for a player to spawn. Add at least one slot to a squad.") in both fresh and hydrated dropdowns; severity legend present. **Backspace:** with the dropdown OPEN — 105 visible buttons → **0**, chip gone, legend gone, strip gone; pixel audit of all four chrome bands shows pure map (screenshot); second Backspace restores. **Hydrated t0 (F-11):** authored faction+marker+zero-slots payload (verified in `json_payload`; semver 0.2.0 saved via an explicit version), fresh boot, ZERO edits: chip appears 0.43 s ("No issues") and flips to "1 error" at hydration completion (12.0 s, debug SwiftShader) — the load-time eval reaches the truth without an edit, which the pre-T-798 panel never did. Bounded-poll structure perturbation-proven (`load_time_eval_retries_until_the_source_is_ready` RED when the re-arm branch was removed; restored). **Save-dialog rect smoke:** version input in-viewport at 1920×1080 (top 494.5, bottom 520.5) AND 1366×768 (338.5/364.5); dialog ancestor chain has ZERO backdrop-filter nodes (portal intact); the chip dropdown is `MENU_PANEL`-anchored (pin `the_dropdown_is_anchored_via_menu_panel`) — no new containing-block victim.

**T-803 measured:** empty doc → strip already names the real destination ("Placing into: Default Layer", muted). Create B → row wears the EXACT `ROW_DROP_TARGET` recipe (`bg-tertiary/15 text-tertiary ring-1 ring-inset ring-tertiary/50`, live className) + `my_location` chip; strip flips to "New Layer 1" in primary. Selected SLOT row simultaneously wears plain `ROW_ACTIVE`; computed-style delta on **4 axes**: background `oklab(0.9098…/0.15)` tertiary vs `oklab(0.8281…/0.2)` primary · inset ring shadow vs `none` · border-top `0px` vs `1px` · chip present vs absent. Units land under B (tree order: both Riflemen between B and the next folder). Create C: strip + treatment flip to C **immediately** (read mid-rename, before Enter). Place → lands under C. Fallback honesty: undo×2 (place + create-C) → strip goes MUTED "New Layer 1" → place → the unit landed **under New Layer 1** (tree dump) — the named fallback is the real `rows.first()` destination (strip and `ensure_layer` share the id-sorted `layer_rows`; created ids start `layer-0` < the default `layer-1`). Alt-click B → all 3 slot rows painted selected (subtree select intact). Windowing: below threshold eager (`__outlinerStats.editorLayers total=48 rendered=48 threshold=50`); >50 on merged RELEASE dist: windowed + subset + windowed-row-selects (gate smoke v2/v3/v6 true); the exact >50 scroll-out/in treatment check → uncaptured live (debug-env note above, recipe given). Perturbations: one folder site swapped back to `ROW_ACTIVE` → `t803_drop_target_reads_differently` RED ("found 1 of 2"); restored GREEN.

**Hollow-pin sweep (attack 4):** 21 new test fns across 6 files, every include_str! pin scrubbed (`live_code`/`live_source` cut the test module; `only_body` panics on 0 or 2+ matches — shadow-copy proof) or scoped to the pre-`#[cfg(test)]` production half (`eden_dock_left::production()`); needles are production-required strings. **5 spot-perturbations across 5 files** (mission_history feed line · eden_tree ROW swap · strip chip accent · validation_panel poll re-arm · scene defend-glyph collapse) — each RED with its own verbatim message, each restored byte-exact (`git status` clean) + touch → GREEN.

**Register re-checks:** docks bottom **1044 == 1044** bar.y (exact, both docks). Grid eastings `040…090` at 250 px spacing = exactly 1000 m·s with **0.0 px** drift (6 labels, camera math vs label text). Keep-multi: map ctrl-click ×3 → SEL 3 → dblclick a member → "3 slots selected · multi-edit", SEL unchanged 3 → Esc closes the modal, SEL still 3. Composition stamp: Save composition… (3 selected) → Title "V204 Trio" → Save → row → arm → map click → `slot_count` **3→6** → ONE undo → **3**. Recently-placed surface lives on the shared Favourites tab (sub-tab pair "Favourites | Recently placed" present; head-first/dedup pin green; dock files untouched this wave). Catalog: NATO → US_Army merged tree + grammar hint render live (no DB flip this wave — dock untouched, quick spot per brief). Toolbar plates: `plates_subscribe_to_dispatch_generation_before_reading_it` green in-suite; dock-tab plates flip classes reactively on click; SNAP chip cycles off→move on click (toolbelt chord re-drive not achieved via CDP key events — toolbelt untouched this wave).

**Suites:** website-frontend **1138 / 0 / 0** (was 1122 — +16 this wave) · map-engine-render **72 / 0**, `--list` == 72 == run, 0 filtered (--all-features) · map-engine-core **655 / 0 / 1 ignored** (--all-features). All in private per-slice target dirs, deleted after.

## Attacked and FAILED to break
The mission_history outside-owns diff (nothing smuggled) · T-760/T-780 feed pins (perturbation RED/GREEN) · 3-glyph distinctness at 3 zooms · caption presence beside its marker · dock-vs-map coordinate agreement · the 64-alias table vs the schema enum (64/64) · case-fold + unknown-alias fallback pins · MarkerComposite destroy-on-clear + 12× place/undo leak loop · the widened atlas's cells-0/1 byte parity · chip reads the eval sink at t0 (fresh AND hydrated) · V1 copy byte-verbatim · dropdown Esc/one-layer/scrim/covered-under-dialog semantics · the hint+dropdown Esc ladder · Backspace full-chrome hide with the dropdown open + pixel-pure map + restore · Save-dialog rect at both shipping viewports + portal chain · chip anchored (no fixed-position victim) · ROW_DROP_TARGET vs ROW_ACTIVE 4-axis distinctness · drop-target chip + strip naming (armed, muted-fallback, create-flip immediacy) · fallback-place honesty (strip name == landing layer) · Alt-click subtree · below-threshold outliner stats · keep-multi dblclick · composition save/arm/stamp/one-undo · grid-label exactness · dock/bar layout equality · 5/5 hollow-pin perturbations · full three suites · scope purity of all five wave commits.

## Environment left as found
HEAD `b008bfc8`, `git status` clean (this report is the only addition). All 19 `VERIFY204 *` missions DELETEd (204s; final sweep shows zero remaining). My chromiums killed (no `chrome-linux64` strays; profiles removed). Private target dirs `~/.cache/tbd-target-T-{790,798,803}` removed. Pre-wave worktree removed + pruned; its dist deleted. NOTE: the pre-wave dist build reused the gate's `target-gate-trunk` cache (host-side), so that cache's fingerprints now reflect an a07a755e build — the next gate trunk build will recompile from it (cache-only, no artifact contamination: `dist-gate-frontend` itself untouched). DB rows untouched beyond the deleted probe missions.
