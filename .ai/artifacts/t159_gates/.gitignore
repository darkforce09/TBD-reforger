# Gate run artifacts (regenerable). Committed goldens live in manifests/ and v/oracle-freeze/
# (the T-159.29.1 frozen React DOM oracle — NOT regenerable once React is deleted). Smoke/run
# captures and per-slice logs are not committed.
v/*
!v/oracle-freeze/
logs/
.devtoken
fixtures/api/.devtoken
