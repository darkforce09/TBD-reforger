YOU ARE A PLATFORM-FACTORY SLICE AGENT for editor wave 209.

## WORKTREE (mandatory first action)
cd /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-812
export CARGO_TARGET_DIR=/run/media/system/Disk_2/Projects/TBD-Reforger/target
pwd && git branch --show-current
# MUST show the worktree path and branch slice/T-812. If you see the repo root or main, STOP and report that — do not start work.

## THE TICKET
T-812 — Bookmark rename truncates the typed name to its last character; the bookmark ADD input never focuses

## BRIEF (registry summary — verbatim authority)
WAVE-200 VERIFIER RE-TICKET (wave200.md F2 MAJOR + F7 MINOR). F2 VERIFIED LIVE: the rename input is controlled through the `renaming` signal read by the whole bookmark-list closure — every keystroke re-renders the list and rebuilds the input node, and T-785's new on_load then re-focuses AND select()s the full text on every remount, so each keystroke replaces the entire draft: typed 'Ridge OP Two' per-char -> input value 'o', committed row label 'o' (two independent reads). A name-destroying regression ON the field the wave fixed. F7 VERIFIED LIVE: the bookmark ADD ('name this view') input eden_dock_left.rs:275-304 has bare autofocus on a reactive insert — focused_on_mount:false, real 'g' flipped GRID with the naming box open. FIX: the mid-edit draft must live where the list render does not track it (local draft signal or uncontrolled input; commit on blur/Enter; Escape abandons); select-all ONLY when a rename session opens, never on remount (which must stop once decoupled); ADD input gets NodeRef + on_load focus+select and the same remount check. TRAPS: this file carries T-785's shipped pins ~:1932-1961 which currently claim layer-rename coverage — repoint/rename them honestly for the new mechanism (T-811 adds the real layer pin); bookmark jump/delete rows must keep working mid-rename. ACCEPTANCE (verifier probe): rename -> type 'Ridge OP Two' -> committed row named 'Ridge OP Two', focus kept throughout; ADD input focused_on_mount === true, 'g' lands in the field, GRID unchanged; Escape abandons without committing.

## YOUR FILES (OWNS — touch ONLY)
apps/website/frontend/src/eden_dock_left.rs

## SIBLING OWNS THIS WAVE (do not touch)
T-811 → apps/website/frontend/src/eden_tree.rs
T-812 → apps/website/frontend/src/eden_dock_left.rs
T-813 → apps/website/frontend/src/attributes.rs
T-814 → apps/website/frontend/src/ui.rs; apps/website/frontend/src/eden_top_strip.rs
T-815 → apps/website/frontend/src/orbat_manager.rs

## NON-NEGOTIABLE
- Verify the defect is still broken on this checkout BEFORE coding (defect_verified_on_main).
- Acceptance is MEASURED: re-run probes with real per-char CDP input against live SPA on :3000 (API :8080). Reference harness: tools/tbd-tools/src/smokes.rs idioms. Scratchpad probes may exist under /tmp/claude-1000/-home-Samuel/f79c73b6-e03c-49f1-8e14-6b61c7dc9af4/scratchpad/ (cdp.py, 785*.json, 786*.json, esc.json) — consult freely, trust nothing.
- smokes.rs is unowned this wave; if you must update a blur-commit seam assert, update it and call it out.
- yrs_persist.rs and every file not in OWNS: read-only.
- Commit on slice/T-812; explicit paths only; subject "T-812: …"; end with:
Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- Before reporting: from the worktree run:
  CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target bash scripts/platform/wave.sh gate --slice T-812
  Must end SLICE GATE: PASS. Delete private test dir $HOME/.cache/tbd-target-T-812 before reporting if you created one via wave.sh test.
- Bare cargo may fail (GLIBC). Prefer:
  distrobox-host-exec env CARGO_TARGET_DIR=/run/media/system/Disk_2/Projects/TBD-Reforger/target sh -c 'cd /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-812 && cargo check -p website-frontend'
  BUT run wave.sh DIRECTLY, never wrapped in distrobox-host-exec.
- NO sub-agents. NO .py committed. No doc/ticket/plan edits. No make schema-validate. No make ci-local. No git stash. Tree clean at end.
- LEAVE trunk :3000 and API :8080 up.


HARD RULES:
1. NO sub-agents. 2. NO .py files committed. 3. Bare `cargo` fails (GLIBC). Shared cache is ONLY for check/clippy/build — never for running tests:
     distrobox-host-exec sh -c 'cd <worktree> && CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo check|clippy|build …' — never /tmp. wasm32 check.
   Ad-hoc TEST must use the mechanical path (no shared-dir template on this line; cargo does NOT rebuild across worktrees sharing a target dir — shared-dir `cargo test` remains the T-742/T-649 foreign-binary class):
     bash scripts/platform/wave.sh test --slice T-xxx -p website-frontend
   (pins CARGO_TARGET_DIR=$HOME/.cache/tbd-target-T-xxx; never /tmp; delete that dir before reporting). Cross-check `--list` total vs run total every time — disagreement means the binary is not yours. Slice gate still uses the shared/gate dirs as normal.
4. Commit on slice/T-xxx; explicit paths; subject "T-xxx:"; end message with:
Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
5. Before reporting: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target bash scripts/platform/wave.sh gate --slice T-xxx from the worktree — must end SLICE GATE: PASS. Gate lock WAITING = serialisation, not a hang.
6. No make schema-validate in worktrees. 7. No doc/ticket/plan edits. 8. Tree clean at the end.
9. CONTEXT DISCIPLINE — these are enforced by a PreToolUse hook, not by good intentions:
   - Locate before you read: Grep for the symbol, then Read with offset/limit. A whole-file Read
     of anything over ~400 lines is refused; pass offset or limit and it is allowed.
   - Never re-read a path you have already read in full. Scroll back. A ranged re-read is legal.
   - Never run an uncapped `rg`/`grep` in Bash — use the Grep tool (it caps with head_limit and
     honours .gitignore), or append `| head -50`.
   - Run noisy builds through the filter: `cargo xtask ai run -- '<cmd>'`. It drops passing tests
     and progress chatter, and always passes failures and the gate verdict through unchanged.


## YOUR REPORT — return EXACTLY these fields (FINAL REPORT schema)
pwd_branch:            <output of `pwd && git branch --show-current`>
defect_verified_on_main: [ {claim, path:line} ]
changes:               [ {path, line, why} ]
perturbation:          { red_output: <VERBATIM red>, restored_green: true|false }
gate_verdict_tail:     <pasted verbatim, must end SLICE GATE: PASS>
files_outside_owns:    [ {path, why} ]   # empty list if none — never omit
found_not_fixed:       [ {path:line, repro} ]
deviations:            [ ... ]
commits:               [ <sha> ]

A test that printed skip: is a FAIL. perturbation.red_output must be real captured output.
