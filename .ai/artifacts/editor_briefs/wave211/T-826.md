# Wave 211 — T-826 slice brief (Grok 4.5) — STAGED, not dispatched

**CWD:** `/home/Samuel/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-826`
**Branch:** `slice/T-826`
**Model:** Cursor Grok 4.5 (operator standing override)

## TICKET (verbatim)

**id:** T-826
**title:** Markers store side without minting phantom factions
**main_goal:** Split ensure_side_faction so markers store side without minting.

**Operator decision (a) — already made 2026-08-10:** marker path stores side WITHOUT minting faction; lazy mint at first slot.

**requirement:**
- Marker path stores side WITHOUT minting faction; lazy mint at first slot.
- V1 fires only when slot or faction actually authored not briefing mark.
- Check flatten.rs compile reads of unminted marker side before landing.

**verify / ACCEPTANCE:**
- Fresh mission place marker chip stays 0 no phantom V1.
- Author first slot of that side V1 fires as expected.
- Compiled export unchanged for marker-only mission minus phantom faction.

## OWNS (touch ONLY)

- `apps/website/frontend/src/editor_ops.rs`

## Sibling owns (do NOT touch)

- T-842 → glyph_math.rs *(parallel)*
- T-843 → smokes / xtask / recipe docs *(parallel)*
- Lock mates T-825/827/828/830/833 — not this wave

## HARD RULES

```
HARD RULES:
1. NO sub-agents. 2. NO .py files committed. 3. Bare `cargo` fails (GLIBC). Shared cache is ONLY for check/clippy/build — never for running tests:
     distrobox-host-exec sh -c 'cd <worktree> && CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo check|clippy|build …' — never /tmp. wasm32 check.
   Ad-hoc TEST must use the mechanical path:
     cargo xtask platform wave test --slice T-826 -p website-frontend
   (pins CARGO_TARGET_DIR=$HOME/.cache/tbd-target-T-826; never /tmp; delete that dir before reporting). Cross-check `--list` total vs run total every time.
4. Commit on slice/T-826; explicit paths; subject "T-826:"; end message with:
Co-Authored-By: Cursor Grok 4.5 <noreply@cursor.com>
5. Before reporting: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo xtask platform wave gate --slice T-826 from the worktree — must end SLICE GATE: PASS.
6. No cargo xtask ci schema-validate in worktrees. 7. No doc/ticket/plan edits. 8. Tree clean at the end.
9. CONTEXT DISCIPLINE — Locate before read; ranged Read; no uncapped bash rg; `cargo xtask ai run -- '<cmd>'` for noisy builds.
```

## FINAL REPORT — required schema

```
pwd_branch:            <output of `pwd && git branch --show-current`>
defect_verified_on_main: [ {claim, path:line} ]
changes:               [ {path, line, why} ]
perturbation:          { red_output: <VERBATIM red>, restored_green: true|false }
gate_verdict_tail:     <pasted verbatim, must end SLICE GATE: PASS>
files_outside_owns:    [ {path, why} ]
found_not_fixed:       [ {path:line, repro} ]
deviations:            [ ... ]
commits:               [ <sha> ]
```
