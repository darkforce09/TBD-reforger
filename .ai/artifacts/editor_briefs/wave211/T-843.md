# Wave 211 — T-843 slice brief (Grok 4.5) — STAGED, not dispatched

**CWD:** `/run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-843`
**Branch:** `slice/T-843`
**Model:** Cursor Grok 4.5 (operator standing override — slice agents + verifier)

## TICKET (verbatim)

**id:** T-843
**title:** Editor suite red; rect smokes never run automated
**summary:** The editor smoke suite is red and nothing automated runs it — two rect smokes are dead guards
**main_goal:** Fix stale editor smoke expectations and wire rect smokes into automated gate path.

**requirement:**
- Update two stale expectations to shipped behaviour m suffix current dock text.
- Record option (b): cargo xtask mk leptos-gates required editor pre-close; chromium stays out of every wave gate.
- Deliver T-829 absorption acceptance v5_orbatWindowed deterministic both harness configs.

**verify / ACCEPTANCE:**
- cargo xtask mk leptos-gates (gate editor-suite) exits 0 on main.
- Both rect smokes execute prove perturb one leptos-gates path goes red then restore.
- EDITOR_FACTORY_FOR_CURSOR.md names cargo xtask mk leptos-gates as required pre-close for rect smokes.

**⟡ PREP DECISION (do not re-decide):** option (b) — chromium OUT of every `cargo xtask platform wave gate`; `cargo xtask mk leptos-gates` is the required editor-factory pre-close path for rect smokes (`save-dialog-rect`, `entrance-motion-rect`). Prove via perturb → red → restore on that path. Absorbs T-829.

## OWNS (touch ONLY)

- `tools/tbd-tools/src/smokes.rs`
- `xtask/src/mk_build.rs`
- `xtask/src/wave/`
- `docs/platform/EDITOR_FACTORY_FOR_CURSOR.md`
- `docs/website/EDITOR_GATE_RUNBOOK.md`

## Sibling owns (do NOT touch — lock wave mates, not this editor wave)

- T-825 → outliner.rs, eden_dock_right.rs, attributes.rs
- T-826 → editor_ops.rs *(parallel sibling this wave)*
- T-827 → eden_top_strip.rs
- T-828 → map-engine-render engine.rs
- T-830 → eden_tree.rs
- T-833 → mission_editor.rs, select_tool.rs
- T-842 → glyph_math.rs *(parallel sibling this wave)*

## HARD RULES

```
HARD RULES:
1. NO sub-agents. 2. NO .py files committed. 3. Bare `cargo` fails (GLIBC). Shared cache is ONLY for check/clippy/build — never for running tests:
     distrobox-host-exec sh -c 'cd <worktree> && CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo check|clippy|build …' — never /tmp. wasm32 check.
   Ad-hoc TEST must use the mechanical path (no shared-dir template on this line; cargo does NOT rebuild across worktrees sharing a target dir — shared-dir `cargo test` remains the T-742/T-649 foreign-binary class):
     cargo xtask platform wave test --slice T-843 -p website-frontend
   (pins CARGO_TARGET_DIR=$HOME/.cache/tbd-target-T-843; never /tmp; delete that dir before reporting). Cross-check `--list` total vs run total every time — disagreement means the binary is not yours. Slice gate still uses the shared/gate dirs as normal.
4. Commit on slice/T-843; explicit paths; subject "T-843:"; end message with:
Co-Authored-By: Cursor Grok 4.5 <noreply@cursor.com>
5. Before reporting: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo xtask platform wave gate --slice T-843 from the worktree — must end SLICE GATE: PASS. Gate lock WAITING = serialisation, not a hang.
6. No cargo xtask ci schema-validate in worktrees. 7. No doc/ticket/plan edits except the recipe docs in OWNS. 8. Tree clean at the end.
9. CONTEXT DISCIPLINE — these are enforced by a PreToolUse hook, not by good intentions:
   - Locate before you read: Grep for the symbol, then Read with offset/limit. A whole-file Read
     of anything over ~400 lines is refused; pass offset or limit and it is allowed.
   - Never re-read a path you have already read in full. Scroll back. A ranged re-read is legal.
   - Never run an uncapped `rg`/`grep` in Bash — use the Grep tool (it caps with head_limit and
     honours .gitignore), or append `| head -50`.
   - Run noisy builds through the filter: `cargo xtask ai run -- '<cmd>'`. It drops passing tests
     and progress chatter, and always passes failures and the gate verdict through unchanged.
```

## FINAL REPORT — required schema

```
pwd_branch:            <output of `pwd && git branch --show-current`>
defect_verified_on_main: [ {claim, path:line} ]
changes:               [ {path, line, why} ]
perturbation:          { red_output: <VERBATIM red>, restored_green: true|false }
gate_verdict_tail:     <pasted verbatim, must end SLICE GATE: PASS>
files_outside_owns:    [ {path, why} ]
found_not_fixed:       [ {path:line, repro} ]
deviations:            [ ... ]
commits:               [ <sha> ]
```
