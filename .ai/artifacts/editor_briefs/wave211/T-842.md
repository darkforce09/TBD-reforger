# Wave 211 — T-842 slice brief (Grok 4.5) — STAGED, not dispatched

**CWD:** `/home/Samuel/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-842`
**Branch:** `slice/T-842`
**Model:** Cursor Grok 4.5 (operator standing override)

## TICKET (verbatim)

**id:** T-842
**title:** World glyph clamp-not-wrap breaks tree prop text rotation
**summary:** (from TOML) World glyph clamp-not-wrap breaks tree/prop/text rotation
**main_goal:** Fix world glyph_math clamp-not-wrap so rotations 181–359 render correctly.

**requirement:**
- Fold angle into (-180,180] before encoding same slots_gpu fix shape.
- Update yaw_encoders_agree so two encoders agree again.

**verify / ACCEPTANCE:**
- Rotation 270 visibly different from 180 measured tip extent bearing.
- Full 0..360 sweep monotonic no plateau.
- yaw_encoders_agree full-domain byte parity between encoders.

## OWNS (touch ONLY)

- `crates/map-engine-core/src/world/glyph_math.rs`

## Sibling owns (do NOT touch)

- T-826 → editor_ops.rs *(parallel)*
- T-843 → smokes.rs, xtask mk/wave, EDITOR_FACTORY / EDITOR_GATE_RUNBOOK *(parallel)*
- Lock mates T-825/827/828/830/833 — not this wave

## HARD RULES

```
HARD RULES:
1. NO sub-agents. 2. NO .py files committed. 3. Bare `cargo` fails (GLIBC). Shared cache is ONLY for check/clippy/build — never for running tests:
     distrobox-host-exec sh -c 'cd <worktree> && CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo check|clippy|build …' — never /tmp. wasm32 check.
   Ad-hoc TEST must use the mechanical path:
     cargo xtask platform wave test --slice T-842 -p map-engine-core
   (pins CARGO_TARGET_DIR=$HOME/.cache/tbd-target-T-842; never /tmp; delete that dir before reporting). Cross-check `--list` total vs run total every time.
4. Commit on slice/T-842; explicit paths; subject "T-842:"; end message with:
Co-Authored-By: Cursor Grok 4.5 <noreply@cursor.com>
5. Before reporting: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target cargo xtask platform wave gate --slice T-842 from the worktree — must end SLICE GATE: PASS.
6. No cargo xtask ci schema-validate in worktrees. 7. No doc/ticket/plan edits. 8. Tree clean at the end.
9. CONTEXT DISCIPLINE — Locate before read; ranged Read; no uncapped bash rg; `cargo xtask ai run -- '<cmd>'` for noisy builds.
```

## FINAL REPORT — required schema

```
pwd_branch:            <output of `pwd && git branch --show-current`>
defect_verified_on_main: [ {claim, path:line} ]
changes:               [ {path, line, why} ]
perturbation:          { red_output: <VERBATIM red>, restored_green: true|false }
gate_verdict_tail:     <pasted verbatim, must end SLICE GATE: PASS>
files_outside_owns:    [ {path, why} ]
found_not_fixed:       [ {path:line, repro} ]
deviations:            [ ... ]
commits:               [ <sha> ]
```
