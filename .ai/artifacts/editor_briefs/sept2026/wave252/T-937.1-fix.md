You are the hotfix agent for **T-937.1-fix** (Class-R haystack).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-937.1-fix
branch **slice/T-937.1-fix** (branched from main `51a23c8bd`).

## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-937.1-fix
Do NOT use the shared `/home/Samuel/.cache/tbd-target` for `cargo test` binaries (T-742).
Do NOT `cargo test -p xtask` (mod compile may be elsewhere).
No Enfusion. No schema. Do not restart :3000/:8080.

FIRST: `cd` worktree; `pwd && git branch --show-current` must be `slice/T-937.1-fix`. Then prove the defect.

## THE DEFECT (measured, wave 252 close gate)

`cargo xtask platform wave gate 884fb3627` → **GATE: FAIL** on `test frontend`.

Repro (same private dir the gate uses, from **main** or this worktree):

```
CARGO_TARGET_DIR=/run/media/system/Disk_2/Projects/TBD-Reforger/target-gate-frontend \
  cargo test -p website-frontend -- \
  editor::panels::dock_right::tests::a_composition_captures_comments_and_authored_elevation
```

Panic:

```
T-601: expected exactly one `fn place_composition(` in the live source, found 0
```

at `apps/website/frontend/src/editor/arsenal/mod.rs:2436`.

Root cause: T-937.1 inserted this on `MissionDocCore` **before** `place_composition` (~line 524 of `crates/map-engine-core/src/doc/store.rs`):

```
/// T-937.1 — whether `root[key].field` is a native `YArray`
#[cfg(test)]
pub(crate) fn id_list_is_native(...) { ... }
```

Class-R `cut_test_module` / `live_source` truncates at the **first** `#[cfg(test)]` in the file (see `t573_mixed_drag_preview.rs` comment). That hides `pub fn place_composition` (line 2446) from the dock_right pin. Wave 251 gate was green; this is new with T-937.1.

Callers of `doc.id_list_is_native` are only in `crates/map-engine-core/src/doc/id_arrays.rs` tests (`hydrate must migrate …`). Production already has `id_arrays::is_native_array`.

## THE FIX

1. **Delete** `MissionDocCore::id_list_is_native` from `store.rs` entirely. No `#[cfg(test)]` on that impl before `place_composition`.
2. Keep the hydrate assertions. In `id_arrays.rs` `mod tests`, call `is_native_array` (or a helper that lives **inside** `id_arrays.rs` `#[cfg(test)] mod tests` — never a cfg(test) item on the production `MissionDocCore` impl in `store.rs`).
3. Do **not** weaken the dock_right pin. Do **not** edit `dock_right.rs`, `arsenal/mod.rs`, or frontend at all.
4. Do **not** put a new `#[cfg(test)]` anywhere in `store.rs` before `place_composition`.

## YOUR FILES (touch ONLY these)

- `crates/map-engine-core/src/doc/store.rs`
- `crates/map-engine-core/src/doc/id_arrays.rs`

## VERIFY (must paste)

1. Prove RED before the edit (the frontend pin, or `rg -n 'cfg\(test\)' store.rs` showing line 525 before 2446).
2. After the edit: `rg -n 'id_list_is_native|cfg\(test\)' crates/map-engine-core/src/doc/store.rs` — no `id_list_is_native`; no cfg(test) before `place_composition`.
3. `cargo test -p map-engine-core --features doc,mission id_arrays` (or the crate's live feature set that compiled T-937.1 tests) — hydrate native-array asserts still GREEN.
4. From this worktree, with a **private** CARGO_TARGET_DIR (not shared tbd-target):

```
cargo test -p website-frontend -- \
  editor::panels::dock_right::tests::a_composition_captures_comments_and_authored_elevation
```

must be GREEN. If worktree LFS makes unrelated DEM tests fail, do **not** "fix" them; this command is one test.

5. `cargo fmt` on the two files.

Commit on `slice/T-937.1-fix` with subject like:
`T-937.1: keep Class-R haystack — move native-array probe out of store.rs impl`

Co-Authored-By: Grok <noreply@x.ai>

## RETURN

Merge SHA (or unpushed commit SHA), files touched, RED→GREEN paste, found_not_fixed. Do not merge to main. Do not ship. Do not drop the worktree.
