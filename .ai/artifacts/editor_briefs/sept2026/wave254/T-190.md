You are the slice agent for **T-190** (Two tabs on one mission clobber each other).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-190  branch slice/T-190
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-190`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-190
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-190 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-190. Then prove the defect.
## THE JOB
Two tabs on one mission write one IndexedDB key; the last debounce wins and the reload prompt blames
server drift. Three deliverables:
1. New `state/tab_lock.rs` (registered in `state/mod.rs`): a BroadcastChannel per mission id
   announcing presence; a second tab shows a read-only banner until the first closes.
2. `persist.rs` reads the stored blob and applies it as a yrs update BEFORE writing, so two tabs
   converge instead of clobbering.
3. The conflict modal shows object counts and timestamps for BOTH options and marks
   "Load server version" as destructive.
**Plus two findings folded in by operator decision 2026-09-07 — they are deliverables, not extras:**
- **T-946.51** — `SAVE_IN_FLIGHT` is set and reset but **never read**. Two comments claim it
  serialises the hidden flush against pagehide; the real guard is `lock_for(id)`. Its test passes on
  the identifier alone. Either make it load-bearing or delete it, and fix the comments either way.
- **T-946.54** — `note_unreadable` retries 80/160/320 ms before inserting into `UNREADABLE`, so for
  ~560 ms `run_save` passes the T-374 guard and can overwrite a record the session already failed to
  read. Latch first, retry after.

## TWO THINGS THE TICKET GETS WRONG — READ BEFORE PLANNING
**1. The conflict modal is not in `mission_editor.rs`.** `ConflictInfo` (exactly two fields,
`payload_json` and `semver`) is `canvas/overlays.rs:898-905`, and the `ConflictDialog` component is
`:906-970`. `mission_editor.rs` holds only the re-export (`:81`), the signal (`:1263`) and the mount
(`:2818-2823`). `overlays.rs` is in your owns for this reason. Today the dialog shows a title, one
sentence built from `semver_label` (`:961-964`), and two buttons — no counts, no timestamps, and
"Load server version" is styled as the AFFIRMATIVE button (`:955`). The destructive convention in
this codebase is `text-error` / `bg-error/*` (`shell/layout.rs:271`,
`pages/operations/faction_manager.rs:311`).

**2. `BroadcastChannel` and `navigator.locks` already exist here.** The ticket says they appear
nowhere in the repo. They have since T-155, in `core/client.rs`: `with_refresh_lock` at `:422-430`
and `subscribe_peer_rotations` at `:509-532`, both reached through `js_sys::Reflect` — and
`client.rs:509-512` says why: "`web_sys::BroadcastChannel` is not in this crate's web-sys feature
list, and adding it is a `Cargo.toml` change for one object." **Follow that precedent.** You do not
own `Cargo.toml` and must not need it.

## ANCHORS (verified 2026-09-07)
`persist.rs`: write primitive `put_raw` `:350-360` (one ReadWrite txn, `store.put`, a blind whole-key
overwrite); `save_state_as` `:476-478`; debounce `DEBOUNCE_MS` `:140` from `save_status::IDLE_DEBOUNCE_MS`;
`run_save` `:827-900+` with four guards — `is_cancelled` `:833`, T-221 owner token `:836`, T-374
content probe `:844-873`, T-374 unreadable record `:874-888` — and then it writes. **No read of the
existing record, no merge.** `slots_digest` calls `core.materialize()` at `:1081`.
CRDT APIs already available: `MissionDocCore::apply_update` `store.rs:483`, `encode_state` `:512-517`,
and `probe.apply_update` is already used inside `persist.rs:718` (`blob_has_content`).
**Load-bearing for the merge:** `apply_update` carries a client-id collision guard
(`store.rs:483-503`) returning `Err` when `my_clock != 0 && claimed > my_clock`, with a `my_clock == 0`
fresh-restore carve-out. Applying a SIBLING tab's blob is fine (client ids are minted per
`MissionDocCore::new()`); replaying THIS tab's own earlier blob into an already-authored doc is not.
Read `:462-478`, which enumerates exactly which cases fire, and quote it in your report.
`hydrate.rs`: the `conflict` set site is **`:353-358`** in the `Local::Diverged` arm, not `:220-239`
(that is the parameter in the `hydrate_from_server` signature). Other params `:377`, `:418`.
`state/mod.rs` is 38 lines of module declarations; `tab_lock` sits between `session` (`:34`) and
`title_prefer` (`:38`). Note the `save_status` precedent at `:29-32`: deliberately **ungated** so
`hcargo test -p website-frontend` can reach it natively. Do the same for `tab_lock` where you can.
## TRAPS THAT WILL BITE THIS SLICE
- **Do not add a `#[cfg(test)]` item above `mission_editor.rs:3033`.** That file documents its own
  boundary at `:3033-3037`: the Class-R scrubber and the keymap census both treat the first literal
  `#[cfg(test)]` as "everything after this is fixture", so a test-gated import up top truncates every
  scrub of the file to nothing. Same rule for `overlays.rs`, which `:12-14` says deliberately carries
  no `#[cfg(test)]` at all so `live_code` keeps all of it.
- `mission_editor.rs` is file-length allowlisted — call-site lines only, no new logic there.
- Do not widen a tolerance or threshold in a golden to absorb a behaviour change. If a numeric
  contract moves, grep for identical assertions in files you do NOT own and STOP and report.
- `materialize(&self) -> SlotSoa` is pinned this wave (T-937.3 owns `store.rs`). If its signature
  moves under you, STOP and report — do not edit `store.rs`.
## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/state/persist.rs
apps/website/frontend/src/editor/state/hydrate.rs
apps/website/frontend/src/editor/state/tab_lock.rs   (new)
apps/website/frontend/src/editor/state/mod.rs
apps/website/frontend/src/editor/mission_editor.rs
apps/website/frontend/src/editor/canvas/overlays.rs
## SIBLINGS THIS PACK (do NOT touch)
T-936.7 mission/{tactical_graphics,mod,extensions,flatten,compile}.rs + both schemas +
canvas/{tactical_graphics,mod,gestures,commands,render_sync}.rs + state/{history,operations,
operations/tactical_graphics}.rs · T-937.3 doc/store.rs + state/operations/entity.rs · T-938.5
building_viewshed*.rs + dem/sample.rs + tools/* · T-938.6 world_assets/* + canvas/viewport.rs
## RULES
1. Verify defect first with a wasm test: two stores writing one key end with the last write —
   paste the RED. Also show the modal today carrying no counts or timestamps.
2. Perturbation: skip the merge → the convergence test goes RED verbatim; restore, `touch` the file,
   green. A git restore does not re-trigger cargo.
3. `hcargo test -p website-frontend`, tail pasted. `hcargo test -p map-engine-core --all-features` if
   you touch anything it compiles.
4. Explicit git paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER `git stash` (eats LFS pointers).
   Commit after every self-contained step so an outage leaves reviewed work.
5. `hcargo xtask platform wave gate --slice T-190` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`. A test printing `skip:` is a FAIL. hrustfmt --edition 2021 here.
7. New code goes in NEW files — file-length allowlists are never extended.
8. No subagents. No ship/merge/push. No docs/ or .ai/tickets/ edits. Leave :3000 and :8080 up.
9. No silent deferrals: the whole acceptance is the deliverable, T-946.51 and T-946.54 included. If a
   piece is genuinely blocked or needs a file you do not own, STOP and report it precisely.
## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave254/REPORT-T-190.md` in the worktree.
Branch committed and clean. No push.
