You are the slice agent for **T-936.7** (Tactical graphics: phase lines, boundaries, curved arrows).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-936.7  branch slice/T-936.7
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-936.7`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-936.7
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-936.7 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-936.7. Then prove the defect.
## THE JOB
A top-level `tacticalGraphics` block: `[{id, kind: phase_line|boundary|axis_of_advance|curved_arrow,
points: [[x,z]...] minItems 2, label?, sideKey?, style?}]`, `additionalProperties: false`. New
`mission/tactical_graphics.rs` (model + validator: point count per kind, finite coords) registered in
`mission/mod.rs` and in `extensions.rs` AUTHORED_BLOCKS. New `canvas/tactical_graphics.rs` registered
in `canvas/mod.rs`: draws all four kinds (Catmull-Rom for curved arrows), vertex drag editing,
selection, undoable in one step per gesture. Rendering goes through the existing overlay layer — no
new render lane in map-engine-render.

**READ THIS BEFORE YOU TOUCH extensions.rs — T-946.53.** The AUTHORED_BLOCKS row alone does NOT reach
`/compiled`, and two comments in the tree say it does. They are wrong and you are correcting them.
`flatten.rs:1439-1449` `authored_block_value` is a six-arm hand `match` ending `_ => None`, so a
registered key with no arm is dropped **silently** — no compile error, no diagnostic. The full set,
all of which you own:
  1. `extensions.rs:75-101` — the AUTHORED_BLOCKS row.
  2. `extensions.rs:370` — `assert_eq!(AUTHORED_BLOCKS.len(), 6)` becomes 7.
  3. `extensions.rs:369` — `assert!(!is_authored_block("tacticalGraphics"))`, plus the decoy keys at
     `:382` and `:397`, which use `tacticalGraphics` as their *unlisted* example. Re-point them.
  4. `flatten.rs:~1429` — a named `#[serde(rename=...)] Option<serde_json::Value>` field on
     `EditorPayload`. There is deliberately no `#[serde(flatten)]` catch-all (`flatten.rs:1402-1407`),
     so without the field serde never sees the key.
  5. `flatten.rs:1441-1447` — the new match arm.
  6. `compile.rs:1310-1328` — a test whose message reads ``"`tacticalGraphics` has no AUTHORED_BLOCKS
     row until T-936.7"``. It goes red on arrival; re-point its decoy, do not delete it.
  7. `mission.schema.json` root `properties` — the root is `additionalProperties: false`.
Then fix the two lying comments in the SAME commit: `extensions.rs:10-13` ("Nothing in `compile.rs`,
nothing in `flatten.rs`") and `mission-editor-payload.schema.json:34` ("touch neither `compile.rs`
nor `flatten.rs`").
Precedent to copy end-to-end: `spawnModules`, commit `33be8aff9`.
## ANCHORS (verified 2026-09-07 — the plan doc's are stale)
`$defs/marker` is `mission.schema.json:1494-1617`, NOT :1171. Reuse its vocabulary for `style`:
the fill key is spelled **`brush`** (`:1588-1601`, 8 values), `color` is `$defs/hexColor`
(`^#[0-9a-fA-F]{6}$`), `shape` is `["icon","rectangle","ellipse","polyline"]`, `alpha` 0..1.
`$defs/polygon` (`:583-592`) is `minItems: 3` — a 2-point phase line CANNOT reuse it.
The marker icon enum already contains `"phase_line"` (`:1545`).
Canvas precedent for a polyline with vertex pick + drag: the T-780 connection line —
`render_sync.rs:82` `connection_segments`, `:120` `connection_lane_verts`, `:145` `pick_connection`
(point-to-segment, tolerance in world metres unprojected from pixels at `gestures.rs:984-988`),
rebound every doc tick from `history.rs:415-455`, never cached at the authoring call site.
Multi-click vertex authoring already exists for zones: `operations/entity.rs:3503 begin_zone_draw`,
`:3598 zone_draw_pop_vertex`, Esc via `canvas/commands.rs:137`. Read them; do not re-invent.
One core transaction + one `history::after_local_edit()` = one Ctrl+Z (`entity.rs:1122-1124`).
## TRAPS THAT WILL BITE THIS SLICE
- **`flatten.rs` runs its OWN naive split probe at `:8210`** with needles at `:2979`, `:3395`,
  `:3498`, `:3770`. Your edits land at ~1409-1459. **Do NOT add an inline `#[cfg(test)]` anywhere
  above line 2979** — it truncates that haystack and false-reds an unrelated test.
- **`state/history.rs` is yours, but `after_local_edit` has 49 callers inside `entity.rs`, which is
  T-937.3's file this wave.** You may ADD to history.rs; you must NOT change an existing public
  signature there.
- Any `#[cfg(test)]` item you add must sit BELOW every production item a probe searches for.
  `class_r_scrub::live_source` cuts from the FIRST `#[cfg(test)]` to EOF, same as the naive split.
- Do not widen a tolerance or threshold in a golden to absorb a behaviour change. If a numeric
  contract moves, grep for identical assertions in files you do NOT own and STOP and report.
## YOUR FILES (touch ONLY these)
packages/tbd-schema/schema/mission.schema.json
packages/tbd-schema/schema/mission-editor-payload.schema.json
crates/map-engine-core/src/mission/tactical_graphics.rs   (new)
crates/map-engine-core/src/mission/mod.rs
crates/map-engine-core/src/mission/extensions.rs
crates/map-engine-core/src/mission/flatten.rs
crates/map-engine-core/src/mission/compile.rs
apps/website/frontend/src/editor/canvas/tactical_graphics.rs   (new)
apps/website/frontend/src/editor/canvas/mod.rs
apps/website/frontend/src/editor/canvas/gestures.rs
apps/website/frontend/src/editor/canvas/commands.rs
apps/website/frontend/src/editor/canvas/render_sync.rs
apps/website/frontend/src/editor/state/history.rs
apps/website/frontend/src/editor/state/operations.rs
apps/website/frontend/src/editor/state/operations/tactical_graphics.rs   (new)
## SIBLINGS THIS PACK (do NOT touch)
T-937.3 doc/store.rs + state/operations/entity.rs · T-190 state/{persist,hydrate,tab_lock,mod}.rs +
mission_editor.rs + canvas/overlays.rs · T-938.5 building_viewshed*.rs + dem/sample.rs +
tools/{viewshed_scheduler,mod,los_tool}.rs · T-938.6 world_assets/{memory_budget,mod,satellite}.rs +
canvas/viewport.rs
## RULES
1. Verify defect first: a payload carrying `tacticalGraphics` flattens WITHOUT it — paste the RED.
2. Perturbation RED verbatim, restore, `touch` the file, green. A git restore does not re-trigger cargo.
3. `hcargo test -p map-engine-core --all-features` (without `--all-features` feature-gated modules
   never compile — vacuous pass). Also `hcargo test -p website-frontend`, tail pasted.
4. Explicit git paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER `git stash` (eats LFS pointers).
   Commit after every self-contained step so an outage leaves reviewed work.
5. `hcargo xtask platform wave gate --slice T-936.7` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`. A test printing `skip:` is a FAIL. hrustfmt --edition 2024 for
   map-engine-core; 2021 for the frontend.
7. New code goes in NEW files — file-length allowlists are never extended.
8. No subagents. No ship/merge/push. No docs/ or .ai/tickets/ edits. Leave :3000 and :8080 up.
9. No silent deferrals: the whole acceptance is the deliverable. If a piece is genuinely blocked or
   needs a file you do not own, STOP and report it precisely — do not write "follow-up".
## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave254/REPORT-T-936.7.md` in the worktree.
Branch committed and clean. No push.
