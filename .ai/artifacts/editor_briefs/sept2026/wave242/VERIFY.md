You are the adversarial verifier for wave 242 of the TBD-Reforger factory. Your job is to find what the three slice agents and the command center got WRONG, not to confirm they were right. Do NOT fix. Do NOT commit. Do NOT file tickets. Leave main exactly as you found it (restore anything you mutate; `git status --porcelain` must be empty when you finish). No subagents. Token economy: read only what you attack; ranges for files over 200 lines.

Repo: /run/media/system/Disk_2/Projects/TBD-Reforger, branch main (do not create branches).

ENVIRONMENT. You are in the `claude-desktop` container (glibc 2.36); the shared cache is host-stamped (glibc 2.43), so run EVERY cargo/rustfmt command on the host through the wrappers, which pass cwd, stdin and exit code through:
  /home/Samuel/.cache/tbd-bin/hcargo <args>     /home/Samuel/.cache/tbd-bin/hrustfmt <args>
`podman` is host-only: `distrobox-host-exec podman exec tbd_reforger_db psql -U tbd -d postgres -c '<SQL>'`.
Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `map-engine-core` is ALWAYS `--all-features`.
Do NOT run `cargo test -p xtask` while `cargo xtask mod compile` is running — they share `~/.local/share/tbd-server-addons` and two T-878 tests fail under contention (T-946.13).
KNOWN AND ALREADY FILED — do not re-report: the shared cache can serve a stale/foreign artifact (T-979); `dem::peaks` fails on an LFS-pointer DEM in a worktree but must PASS on main (T-972); `v-suite verify` fails 22/25 on drifted oracles (T-986); frontend wasm clippy has 76 pre-existing errors (T-987); `verify file-length` has 10 pre-existing SIZE-3 violations; the editor renders no forest regions (T-995).

THE SPAN. Base marker `cd37d1a78` (wave 241 CLOSED). Landed:
- `fc19f80f9` T-935.14 — `crates/map-engine-core/src/world/{prefab_load.rs (new),residency.rs,mod.rs}`, `apps/website/frontend/src/editor/world_assets/world_host.rs`: the prefab catalogue archive reaches the residency, with a gzip-vs-rkyv sniff and a JSON fallback.
- `69508bacc` T-935.12 — `packages/tbd-schema/schema/{terrain-manifest,map-object-instance}.schema.json`, `packages/tbd-schema/golden/map-objects/map-object-chunk-sample.bin` (new), `xtask/src/{golden_gate.rs,schema_gates.rs}`, `.gitattributes`: the binary formats become schema-described, golden-gated and LFS-routed.
- `757f9e3fa` T-674.2 — six files across BOTH mod trees: the Enfusion reader for slot identity and squad leader.

COMMAND-CENTRE work in the span, and attack it hardest of all:
- `dbaf92891` T-946.23 — THE MOD GATE COMPILED THE WRONG COPY OF EVERY SHARED SCRIPT. The Enfusion VFS overlays by path, last addon wins, and the gate listed `TBD_Framework,TBD_Export`, so all 139 shared paths compiled from tbd-EXPORT while the shipping tree went unread. The fix is (a) flip to `TBD_Export,TBD_Framework` and (b) `mirror_lockstep`, which compares the two trees' shared scripts with comments and string literals stripped. THIS IS A GUARD, SO A HOLE IN IT IS SILENT.
- `0b9c05c8b` T-674.2 — six `UNREAD_WIRE_FIELDS` rows retired (`rank`, `stance`, `unitName`, `leaderSlotId`, `callsign`, `tag`) and six `mission.schema.json` descriptions rewritten. Two of the six had NON-ZERO baselines (16 and 42) pinning unrelated identifiers, and retiring them drops those tripwires.

Highest-risk claims to attack:
1. T-946.23, hardest. (a) Prove the framework copy of EVERY shared path is now compiled, not just the one file the command centre probed — plant an error in a DIFFERENT shared framework script and in a framework-only script, and say what each proves. (b) Find a way for two mirrors to diverge in CODE and still pass `mirror_lockstep`: a difference only inside a string literal that changes behaviour, a file present in framework but not export (which the check skips by construction — is that hole real?), a `#ifdef`-like construct, a difference the whitespace normalisation eats. (c) Prove the flipped order changed nothing but scripts — no asset, prefab or config resolves differently. (d) Did the fix make any OTHER step of the gate vacuous?
2. T-674.2. (a) The cross-squad `leaderSlotId` guard: prove a uid from another squad yields NO leader, and find any path that reaches a leader without the membership re-check. (b) `ApplySlotIdentity` claims to run on EVERY spawn including respawn — find a spawn path that skips it. (c) The slice says Reforger exposes no replicated free-text name on a character, so `callsign`/`unitName`/`tag` are bound and logged but not applied. Attack that claim: find a replicated setter it missed. (d) A missing rank or controller component must be REPORTED, not skipped — find a component whose absence is silently swallowed. (e) A pre-1.3 mission must behave exactly as before.
3. T-935.12. (a) The golden `.bin` is deliberately NOT LFS-routed while `*.bin` under `packages/map-assets` is, and `objects/density/*.bin` is explicitly unset. Prove each of those three states is what the tree actually has, and that no committed density tile changed. (b) S15's second decoder must be genuinely independent — it must not share `TbdcHeader` or `ObjectInstancePod`; prove a field REORDER is caught. (c) The new `verify-terrain-strict` blocks cannot be exercised on everon (no binary files exist yet). Find where that makes a check vacuous rather than merely unexercised.
4. T-935.14. (a) Archive-versus-JSON residency parity: find an input where the two lanes build different tables. The slice states the everon corpus never takes either lookup's FALLBACK branch — verify that, and check the constructed fixture really does. (b) It REFUSES a catalogue with a duplicate `prefabId` while the JSON lane resolves one inconsistently per lookup. Confirm the JSON-lane inconsistency is real and say whether the asymmetry is safe. (c) It changed `occluder.init` gating from a successful FETCH to a loaded CATALOGUE — a behaviour change. Find a case where the occluder now fails to initialise and used to.
5. Command centre and gate vacuity. (a) Every landed ticket `shipped` with `shipped_at` at its landing merge; `ticket check` exit 0; `cargo test -p xtask` green; receipts match tips. (b) The wave's close label — say whether a pending `[[emptied]]` entry formed on its own or `wave repack --reserve` was needed. (c) Prove the wave gate read this wave's code, `mod compile` included. `--list` versus run totals for every crate you test.

Severity table (also the command center's triage table):
- BLOCKER: main is broken, data at risk, or a gate reported success on code it never examined.
- MAJOR: a shipped ticket does not do what it claims, or can destroy operator-authored work.
- MINOR / NIT: everything else.

Report (≤ 60 lines): findings first, each with severity, file:line, the exact command + verbatim output that proves it, and the fix shape (one line). Then an explicit list of what you attacked and FAILED to break. Then confirm main is untouched (`git status --porcelain` empty, HEAD sha).
