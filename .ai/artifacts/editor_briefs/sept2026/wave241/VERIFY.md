You are the adversarial verifier for wave 241 of the TBD-Reforger factory. Your job is to find what the three slice agents and the command center got WRONG, not to confirm they were right. Do NOT fix. Do NOT commit. Do NOT file tickets. Leave main exactly as you found it (restore anything you mutate; `git status --porcelain` must be empty when you finish). No subagents. Token economy: read only what you attack; ranges for files over 200 lines.

Repo: /run/media/system/Disk_2/Projects/TBD-Reforger, branch main (do not create branches).

ENVIRONMENT. You are in the `claude-desktop` container (glibc 2.36); the shared cache is host-stamped (glibc 2.43), so run EVERY cargo/rustfmt command on the host through the wrappers, which pass cwd, stdin and exit code through:
  /home/Samuel/.cache/tbd-bin/hcargo <args>     /home/Samuel/.cache/tbd-bin/hrustfmt <args>
`podman` is host-only: `distrobox-host-exec podman exec tbd_reforger_db psql -U tbd -d postgres -c '<SQL>'`.
Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `map-engine-core` is ALWAYS `--all-features`.
KNOWN AND ALREADY FILED — do not re-report: neither gate compiles Enfusion, so `mod compile` was run by hand (T-946.17); the shared cache can serve a stale/foreign artifact (T-979); `dem::peaks` fails on an LFS-pointer DEM in a worktree but must PASS on main (T-972); `v-suite verify` fails 22/25 on drifted oracles (T-986); frontend wasm clippy has 76 pre-existing errors (T-987); `verify file-length` has 8 pre-existing SIZE-3 violations; the editor renders no forest regions at all (T-995), so nothing T-935.11 emits for them is observable in the SPA.

THE SPAN. Base marker `52a038a77` (wave 240 CLOSED). Landed:
- `c32fdd0cd` T-675.1 — `crates/map-engine-core/src/mission/flatten.rs`: the authored vehicle roster onto top-level `vehicles[]` at schema 1.3, reusing T-674.1's version latch.
- `6447f4fb9` T-935.11 — `crates/map-engine-core/src/world/{prefab.rs,regions.rs,mod.rs}`, `tools/tbd-tools/src/world/{catalog_emit.rs,mod.rs,build.rs}`, `apps/website/frontend/src/editor/world_assets/world_host.rs`: prefab catalogue, forest regions and type inventory to rkyv, with archive fetch branches for roads and regions.
- `4c0542b87` T-676 — `apps/mod/tbd-framework/Scripts/Game/TBD/Zones/TBD_TriggerRuntime.c` (new, 2107 lines): the Enfusion trigger activation and effects runtime.

COMMAND-CENTRE work in the span, and attack it as hard as the slices:
- `2392a2513` T-946.3 — 15 em-dashes transliterated in three tbd-export WorkbenchGame scripts so `cargo xtask mod compile` reaches the compiler at all. Check nothing else changed in those files and that no test or tool pins the old bytes.
- `6e72b0b4d` T-946.12 — `wave repack --reserve`, which freezes an operator-named set of shipped ids as a pending `[[emptied]]` entry. It is the only path by which a membership the derived carry cannot see enters the lock. Attack it as a GUARD: can a reservation record a target the close would refuse, survive its own marker, collide with an open wave, or be re-derived away by a later repack?
- `a0a762416` T-946.14 — `wave land`'s post-merge gate now derives its own base instead of using pre-merge HEAD. Prove the land gate this wave actually read all three slices, and that the change did not make some OTHER step vacuous or double-counted.
- `aa9ca991f` T-676 — four `UNREAD_WIRE_FIELDS` rows retired (`editorTriggers`, `activation`, `effects`, `variantId`) plus the two schema descriptions that asserted no reader. VERIFY EACH RETIREMENT INDEPENDENTLY: count the identifiers yourself, and check no field was retired whose reader does not actually exist.
- `ee1b92fc5` T-706 — the `schemaVersion` description rewritten. Check every factual claim in the replacement against the tree.

Highest-risk claims to attack:
1. T-676, hardest — 2107 lines of Enfusion that NO Rust gate compiles and NO automated test executes. (a) It reads `editorTriggers[]` through a SECOND typed `JsonLoadContext` pass because the shared document struct declares no such field. Prove that pass cannot silently bind the wrong key, and that an ABSENT key is distinguishable from an empty one — the slice says `JsonLoadContext` allocates nested refs even for absent keys, so null is not a presence test. Find a document where a trigger is silently dropped or silently armed. (b) `SnapshotPlayers` was changed mid-slice to fail closed; prove that an empty snapshot cannot make `not_present` read TRUE, because a `not_present` + `end_mission` mission would end the round the moment the runtime loses sight of players. (c) The heartbeat is a `modded class SCR_BaseGameMode` fenced by `IsFrameworkWorld()`. Prove nothing ticks on a vanilla world and that a stale timer from a replaced world stops. (d) `timeoutSeconds` is a CONTINUOUS dwell and `repeat` re-arms only after the condition falls: find an input where a gap does not reset, or where a trigger fires twice without falling. (e) `detected_by` is 250 m proximity with no line of sight — check that is stated where an author would see it, not only in a constant.
2. T-935.11. (a) The absent-optional sentinel is NaN, while `archives.rs:168` documents `half_extents` as zero-when-absent. Everon has no zero half-extents, so the ambiguity is invisible today. Construct the row that makes the two disagree, and say which side is wrong. (b) The three parity tests are order-sensitive on the reader side and, after `5df92a67b`, on the writer side too. Find an ordering the pins still miss. (c) `world_host`'s archive paths are deliberately NOT gated on `matches_this_build` while the chunk template is. Find a manifest where that asymmetry serves a stale or foreign archive. (d) The slice edited `crates/map-engine-core/src/world/mod.rs`, which is NOT in its owns, for two `pub use` lines. Confirm the diff is additive names only. (e) The prefab archive has NO frontend fetch branch; the slice says one would be dead because the SPA's only prefab consumer is `residency.load_prefabs_gz`. Verify that claim — if any other path could consume it, the omission is a defect, not a design choice.
3. T-675.1. (a) Ten drop-whole causes, each meant to drop the ROW and never trim a field. Find one that trims, or one that is unreachable. (b) One authored placed vehicle now emits TWO rows, the T-425 `entities[]` alias twin and the roster row, matched only by `uid`. Prove `uid` is always present on both and always equal. (c) The 1.3 latch is on the SERIALISED roster. Find an input where the version bumps with no `vehicles[]` on the wire, or the reverse.
4. Command centre. (a) Every landed ticket reads `status = "shipped"` with `shipped_at` at its landing merge; `ticket check` exit 0; `cargo test -p xtask` green. (b) The wave's close label: this wave shipped ids one at a time again — say whether a pending `[[emptied]]` entry formed on its own, and if not, whether `--reserve` was needed and used correctly. (c) The gate receipts under `.ai/artifacts/verdicts/` match each slice's landed tip.
5. Gate vacuity. The land gate and the wave gate both PASSed. Prove each read this wave's code — in particular that `mod compile` ran over `TBD_TriggerRuntime.c` and not merely over an unchanged tree, and that the frontend steps read `world_host.rs`. Then pick the two steps most likely to pass without reading the new code and prove them. `--list` versus run totals for every crate you test.

Severity table (also the command center's triage table):
- BLOCKER: main is broken, data at risk, or a gate reported success on code it never examined.
- MAJOR: a shipped ticket does not do what it claims, or can destroy operator-authored work.
- MINOR / NIT: everything else.

Report (≤ 60 lines): findings first, each with severity, file:line, the exact command + verbatim output that proves it, and the fix shape (one line). Then an explicit list of what you attacked and FAILED to break. Then confirm main is untouched (`git status --porcelain` empty, HEAD sha).
