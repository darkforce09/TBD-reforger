You are the slice agent for ticket **T-676** (Trigger activation and effects — the Enfusion runtime) on the TBD-Reforger factory.
WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-676   (branch slice/T-676)

## ENVIRONMENT — THIS RUN (read before the first command)
You are inside the `claude-desktop` container (Debian, glibc 2.36). The shared warm cache
/home/Samuel/.cache/tbd-target is stamped for the HOST toolchain (Fedora, glibc 2.43). Running cargo in the
container against it is the GLIBC_2.xx-not-found trap (T-853). So EVERY cargo / rustfmt command runs on the
host through these wrappers (they pass cwd, stdin and exit code through):
   /home/Samuel/.cache/tbd-bin/hcargo <cargo args>
   /home/Samuel/.cache/tbd-bin/hrustfmt <args>
Wherever this brief or the ticket says `cargo xtask …`, run `hcargo xtask …`. `hcargo` forwards CARGO_TARGET_DIR
(default the shared cache), CARGO_BUILD_JOBS=4, TEST_DATABASE_URL and TBD_IT_BASE_DB from your shell.
Never run bare `cargo`; never build into a worktree-local target/. git, grep, sed and file edits run in the
container as normal. A slice gate waits on the gate lock (`target/.tbd-gate.lock`, polls every 30 s, up to
3600 s) when a sibling is gating — that wait is normal, not a hang.

MEASURED THIS RUN, and it will bite you: the shared cache can hand you ANOTHER SLICE'S test binary (this is
T-300, open). A `--list` that shows tests your worktree does not contain is the tell — cross-check
`grep -c '#\[test\]'` against the module you are testing. When it happens, re-run in a private
`CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-676` and delete it on cleanup.
Also measured: `packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask
`map_blueprint::*` / `map_world_los::*` tests fail there with `bad magic [118, 101, 114, 115]` ("vers").
That is pre-existing and environmental — do not chase it, and do not claim it as your red.

GATE SCOPE: `wave gate --slice` is the CHEAP gate — `cargo check`, `wasm32 (frontend)`, `fmt (changed)`,
`clippy (changed crates)`, `schema` and the pin gates. The `trunk build` and the four `test *` steps are
WAVE-level and will NOT run on your slice. If your change can only be proven by one of those, run it yourself and
say so in the report.

FIRST THREE ACTIONS, before anything else:
  1. `cd` there, run `pwd && git branch --show-current`, paste the output in your report. If it does not show
     the worktree path and slice/T-676, STOP and report — do not start work.
  2. `export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target`. If you build a binary you then RUN, build THAT into
     `CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-676` and delete it on cleanup; verify binary provenance first.
  3. Read the full ticket + plan + spec — they are the handoff, there is no prior chat:
       cat .ai/tickets/T-676.toml
       docs/plans/t-676_plan.md
       docs/specs/existing/t676_trigger_runtime.md  → read `$defs/zoneRules` (16 keys) in packages/tbd-schema/schema/mission.schema.json before designing activation
     Then read CLAUDE.md §HARD GATE and docs/platform/PLATFORM_FACTORY.md sections 'THE SIGNATURE DEFECT' and 'Known traps'.

NOTE FROM THE COMMAND CENTER — READ THIS TWICE, YOUR SLICE IS THE ODD ONE OUT.

You are writing ENFUSION SCRIPT (`.c`), not Rust. Nothing in the Rust gate compiles your file. Your gate is
  hcargo xtask mod compile
which builds tbd-framework against the native Linux dedicated server headlessly (no Workbench, no GUI).
It was BLOCKED until an hour ago by three tbd-export files that failed its pre-compile ASCII scan; the command
center transliterated 15 em-dashes (T-946.3, `2392a2513`) and the gate now reaches the compiler. Its verdict on
main, immediately before you started:
    OK: compiled clean
        Module: Game; loaded 5739x files; 11293x classes
        Compiling Game scripts took: 837.529000 ms
        0 warning(s) in TBD sources
That is your baseline. If the ASCII scan fails again, that is a NEW regression — report it, do not work around
it. And do NOT run `cargo test -p xtask` while the mod gate is running: they share
`~/.local/share/tbd-server-addons` and two T-878 tests fail under contention (T-946.13).

  * `apps/mod/tbd-export` is ASCII-ONLY and its scan is a hard gate. `apps/mod/tbd-framework` — YOUR tree — is
    exempt today, but write ASCII anyway: Workbench itself hard-rejects non-ASCII punctuation.
  * T-706 owns `packages/tbd-schema`. You must NOT touch the schema. If the runtime needs a key the schema does
    not carry, STOP and report it — that is a real blocker, not a follow-up (rule 14).
  * T-079 (zone geometry palette) is SHIPPED, so the geometry your runtime activates already exists.
  * Enfusion traps that have cost this project days: `vanilla` is a reserved word; `Format` takes at most 9
    parameters; method arguments may not be strong refs; `FileHandle` members need `ref`. A broken module kills
    the whole compile, so build in small steps and compile often.
  * IN-GAME BEHAVIOUR CANNOT BE PROVEN HERE. Rule 2's perturbation still applies to anything mechanically
    checkable (the compile, any parsing you can exercise); for the rest, every behaviour you could not prove goes
    in `manual_checklist` as ONE human-runnable line each. That list is the deliverable for the eye pass — an
    empty one from this slice would be a lie.

## THE TICKET
Trigger activation and effects — the Enfusion runtime

Main goal: Ship Enfusion trigger activation and effects runtime in tbd-framework.

Context:
  - Twelve ATTR-FIELD-TRG ids except geometry palette half in T-079.
  - zoneRules 16 keys lack ATTR id; closest existing trigger semantics.
  - Schema split to T-706; this ticket is Enfusion runtime only.
  - Operator: agents may edit the Enfusion mod scripts; gate = cargo xtask mod compile; in-game behaviour goes on a human checklist.

Current state:
  - Geometry rides shipped T-582 zone draw tool; editor half separable.
  - New Enfusion runtime in apps/mod/tbd-framework Zones.
  - Executor claude-code; headless compile verified no Workbench.

Requirements:
  - Activation model: condition repeat timeout owner side in TriggerRuntime.
  - Effects model plus schema keys to carry them at runtime.
  - Do not touch packages/tbd-schema; T-706 owns widening.

Approach (the ticket's own; deviate only with a stated reason):
  - Read zoneRules keys before designing activation semantics.
  - Implement TBD_TriggerRuntime.c reader after T-079 geometry lands.

Done means:
  - TBD_TriggerRuntime activates authored triggers per condition, repeat, timeout and owner side and fires their effects at runtime.
  - zoneRules keys keep today's semantics; no packages/tbd-schema edits; headless compile clean.

## YOUR FILES (touch ONLY these; new files must have exactly these names)
apps/mod/tbd-framework/Scripts/Game/TBD/Zones/TBD_TriggerRuntime.c
If the work genuinely requires a file outside this list, STOP and report which file and why — do not widen silently.
Siblings running right now own (do NOT touch):
T-935.11: tools/tbd-tools/src/world/catalog_emit.rs, tools/tbd-tools/src/world/mod.rs, tools/tbd-tools/src/world/build.rs, crates/map-engine-core/src/world/prefab.rs, crates/map-engine-core/src/world/regions.rs, apps/website/frontend/src/editor/world_assets/world_host.rs
T-675.1: crates/map-engine-core/src/mission/flatten.rs


## NON-NEGOTIABLE RULES — every one was learned the hard way
1. VERIFY THE DEFECT STILL EXISTS ON MAIN BEFORE WRITING CODE. If already fixed, STOP and report with the proving command.
2. PROVE NON-VACUITY BY PERTURBATION. For every test you write: break the thing it guards, capture the RED output
   VERBATIM, restore, `touch` the restored file (a git checkout restore does not re-trigger a cargo rebuild), re-run
   green. PASTE THE RED OUTPUT IN YOUR REPORT. Perturb the PRODUCTION side where you can, not only the test's oracle:
   a wave-236 slice found its first pin passed a real production break, and only the stronger oracle caught it.
3. `cargo test -p map-engine-core` ALWAYS with `--all-features` (without it feature-gated modules never compile — vacuous pass).
   Cross-check `--list` total vs run total.
4. COMMIT EARLY AND OFTEN on the slice branch with explicit paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER
   `git stash` (deletes LFS pointer files). Use `git -c filter.lfs.process= -c filter.lfs.required=false …` if git
   complains about LFS. Leave the tree COMMITTED AND CLEAN whenever you pause or finish.
5. GATE: `hcargo xtask platform wave gate --slice T-676` from inside the worktree; must end `SLICE GATE: PASS`.
   "REFUSING to pass — resolved to NO crate" / "rustfmt was invoked ZERO times" means your diff has no lintable Rust,
   not that your code is broken.
6. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `cargo xtask ci schema-validate` does not work
   in worktrees (`cargo xtask schema validate` does).
7. File-length allowlists (`cargo xtask verify file-length`) are never extended: new code goes in NEW files; the
   allowlisted giants grow only by call-site lines. If a file you own would cross the limit, STOP and report the count.
8. No `.py`, `.sh`, `.mjs` files committed (hard gates). Throwaway probes go in /tmp, never the source tree.
9. Edition per crate: read the crate's Cargo.toml and format with that edition (`hrustfmt --edition <n>`). Do not
   trust a brief that names an edition — a wave-236 slice was told 2021 for a crate that is 2024, and in wave 238
   the COMMAND CENTER formatted a 2021 crate as 2024 and the gate caught it. The two editions order a `use` group
   differently, so this shows up as a one-line `fmt (changed) FAIL`. `apps/website/frontend` is 2021;
   `crates/map-engine-core` and `tools/tbd-tools` are 2024.
16. VALIDATE MEANING, NOT ONLY LAYOUT. If you read a binary archive, check its `schema_version` as well as running
   `access_checked` — rkyv proves the buffer is readable, not that your build agrees with the writer about what
   the fields mean. A wave-238 slice shipped without that check on the occluder archive: a shifted flag bit would
   have marked the wrong buildings non-occluding, permanently and silently. Its sibling got it right.
17. DEAD MECHANISMS ARE WORSE THAN MISSING ONES. Before you add a fast path, prove it can fire. A wave-238 slice
   added a prefetch fold that ran AFTER the await it was meant to overlap, so it could only ever queue work that
   was already queued or could never be used. Write the test that shows the saving, or do not add the path.
10. Class-R byte-parity pins must scrub the test module out of the haystack (`class_r_scrub::live_source`).
11. Do NOT spawn subagents. Do the work yourself.
12. You do NOT ship: no push, no merge, no ticket status edits, no docs/ or .ai/tickets/ edits, no filing tickets.
    Report findings; the command center files.
13. trunk serve (:3000) and the dev API (:8080) are running — LEAVE BOTH UP.
14. No silent deferrals: the ticket's whole acceptance is the deliverable. If a piece is genuinely blocked, STOP and
    report it precisely — do not write "follow-up".
15. If you contradict this brief, assume you looked and the command center remembered — say it plainly with the command.

18. AN ANSWER ABOUT THE WORLD MUST CARRY THE WORLD IT WAS BUILT FOR. Wave 240's water mask answers "is this
   tile water?" from a raster whose extent arrives in a SEPARATE manifest field, so a stale `worldBounds` gives
   confident wrong answers with nothing to notice (filed T-946.9). If your archive answers spatial questions, put
   the extent it was built for inside the archive and refuse a caller that disagrees. Same wave, same shape: a
   Range fetch parsed only the TOTAL out of `content-range` and never the START, so a server answering a different
   range was accepted (T-946.10). Parse the start.

## YOUR REPORT — the deliverable, every field required
pwd_branch · defect_verified_on_main [{claim, path:line, command}] · changes [{path, line, why}] ·
perturbation {red_output VERBATIM, restored_green} · gate_verdict_tail (last 15 lines, must end SLICE GATE: PASS) ·
files_outside_owns [] · found_not_fixed [{path:line, repro}] · deviations [] · commits [sha] ·
manual_checklist [one human-runnable line per behaviour you could not prove mechanically]
