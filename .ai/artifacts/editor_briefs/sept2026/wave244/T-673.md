You are the slice agent for ticket **T-673** (Marker style and Area markers — Enfusion reader) on the TBD-Reforger factory.
WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-673   (branch slice/T-673)

## ENVIRONMENT — THIS RUN (read before the first command)
You are on the **HOST** (Fedora, glibc 2.43). Native `cargo` / `rustfmt` work. Do **not** wrap commands in
`distrobox-host-exec` or `hcargo` — those are for the claude-desktop container, which you are not in.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Use that for ordinary builds and tests. If you build a binary you then RUN, build THAT into
`CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-673` and delete it on cleanup.
A slice gate waits on the gate lock (`target/.tbd-gate.lock`, polls every 30 s, up to 3600 s) when a
sibling is gating — that wait is normal, not a hang.

MEASURED THIS RUN: the shared cache can hand you ANOTHER SLICE'S test binary (T-300). A `--list` that
shows tests your worktree does not contain is the tell. When it happens, re-run in the private dir above.
`packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask map tests fail
there with `bad magic [118, 101, 114, 115]` ("vers"). Environmental — do not chase it. Run
`cargo xtask schema validate`, NOT `cargo xtask ci schema-validate`.

GATE SCOPE: `wave gate --slice` is the CHEAP gate — check, wasm32 (frontend), fmt (changed), clippy
(changed crates), schema and pin gates. `trunk build` and the four `test *` steps are WAVE-level.
**NEITHER GATE COMPILES ENFUSION (T-946.17)** — you touch `.c` files, so `cargo xtask mod compile` is
yours to run and to paste. Do NOT run `cargo test -p xtask` while `mod compile` is running (T-946.13).

**T-946.27 — copy before the first `mod compile`:** `mirror_lockstep` fails in every slice worktree
over 19 untracked `Scripts/WorkbenchGame/EnfusionMCP/*.c`. They are gitignored on the framework side
(`apps/mod/.gitignore:28`) and exist only in the main checkout. Copy them in, do not commit them:
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/

FIRST THREE ACTIONS, before anything else:
  1. `cd` to the worktree, run `pwd && git branch --show-current`, paste the output in your report. If it
     does not show the worktree path and `slice/T-673`, STOP and report — do not start work.
  2. `export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target`.
  3. Read the full ticket + plan + spec — they are the handoff, there is no prior chat:
       cat .ai/tickets/T-673.toml
       docs/plans/t-673_plan.md
       docs/specs/existing/t673_marker_style_reader.md
     Then CLAUDE.md HARD GATE and docs/platform/PLATFORM_FACTORY.md 'THE SIGNATURE DEFECT' and 'Known traps'.

## NOTE FROM THE COMMAND CENTER — READ TWICE
You write ENFUSION SCRIPT (`.c`), not Rust. Your gate is `cargo xtask mod compile`. Baseline on main:
`OK: compiled clean` — get that verdict before you change anything, and paste both runs.

  * T-706 already widened `$defs/marker` (`9228a458`). `mission.schema.json:1176-1298` declares 11 keys.
    `golden-missions/schema-1_3-wire-fields.json:227-257` already carries all six attributes. The ticket
    notes that say the schema is closed and the executor is `workbench` are STALE. Do not touch
    `packages/tbd-schema`.
  * The six fields (`size`, `rotation`/`rotationDeg`, `shape`, `brush`, `color`, `alpha`) must be declared
    on `TBD_MissionMarkerStruct`, which lives in **TBD_MissionLoader.c**. `JsonLoadContext` binds by
    member name — there is no other home.
  * Also **TBD_MarkerController.c**: the wire is four positional `array<>` RPC parameters
    (`TBD_RpcDo_Markers(array<int> xs, array<int> zs, array<string> icons, array<string> labels, ...)`).
    Six more columns change both signatures. Keep framework and export twins in lockstep.
  * Color design (command-center call, do not stop): the wire is `#rrggbb`; `SetColorEntry` at
    `TBD_MarkerClient.c:335` takes `SCR_EScenarioFrameworkMarkerCustomColor`. Parse the hex, map to the
    nearest enum index, **and** keep the hex string on the struct. Icon-only markers must render unchanged.
  * `size`'s `UNREAD_WIRE_FIELDS` row is owned by **T-681** (expected 0). Your marker `size` identifiers
    will trip it. That is expected. Do not edit `xtask/src/schema_gates.rs`.
  * EXPECTED GATE FAILURE, NOT YOURS TO FIX: binding the six fields trips `UNREAD_WIRE_FIELDS` in
    `xtask/src/schema_gates.rs` (`rotationDeg`/`brush`/`color`/`alpha` expected 0; `shape` expected 32;
    `size` expected 0 owned by T-681). Report the exact red. Command center retires/re-pins after landing
    (precedent `0b9c05c8b`, `d9bf95230`). Do not edit schema_gates.rs or packages/tbd-schema.
  * Salvage tag `salvage/t853-dropped/T-673` is unreviewed reference only. Do not inherit it wholesale.
  * IN-GAME BEHAVIOUR CANNOT BE PROVEN HERE. Every behaviour you cannot prove mechanically goes in
    `manual_checklist` as ONE human-runnable line. An empty list from this slice would be a lie.
  * **T-946.26:** a NEW framework-only `.c` is invisible to `mirror_lockstep` (it walks export only).
    If you add a new script, add BOTH twins and confirm both files exist on disk before you claim done.

## THE TICKET
Enfusion reader for six MRK attrs. Main goal: ship Enfusion marker style reader; no schema edits.

Requirements:
  - Read six MRK attribute ids in TBD_MarkerData and TBD_MarkerClient.
  - Declare them on TBD_MissionMarkerStruct in TBD_MissionLoader.c; grow TBD_MarkerController.c RPCs.
  - Editor UI for marker style attrs where applicable (if that requires a file you do not own, STOP and report).
  - Do not touch packages/tbd-schema.

Done means:
  - TBD_MarkerData and TBD_MarkerClient read size, rotation, shape, brush, color and alpha from the
    mission payload at spawn; icon-only markers render unchanged.
  - packages/tbd-schema untouched; headless `cargo xtask mod compile` clean (aside from the expected
    UNREAD_WIRE_FIELDS red on the *schema* gate, which you report and do not fix).

## YOUR FILES (touch ONLY these; new files must have exactly these names)
apps/mod/tbd-framework/Scripts/Game/TBD/Markers/TBD_MarkerData.c
apps/mod/tbd-framework/Scripts/Game/TBD/Markers/TBD_MarkerClient.c
apps/mod/tbd-framework/Scripts/Game/TBD/Markers/TBD_MarkerController.c
apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionLoader.c
apps/mod/tbd-export/Scripts/Game/TBD/Markers/TBD_MarkerData.c
apps/mod/tbd-export/Scripts/Game/TBD/Markers/TBD_MarkerClient.c
apps/mod/tbd-export/Scripts/Game/TBD/Markers/TBD_MarkerController.c
apps/mod/tbd-export/Scripts/Game/TBD/Backend/TBD_MissionLoader.c
The export twins are MANDATORY. If the work genuinely requires a file outside this list, STOP and report
which file and why — do not widen silently.
Siblings running right now own (do NOT touch):
T-935.13: apps/website/frontend/src/editor/world_assets/{mod,world_host,occluder_host,labels,water,satellite,dem_load,tbd_sat}.rs,
  crates/map-engine-core/{Cargo.toml,src/world/store.rs},
  packages/map-assets/everon/{manifest.json,objects/chunks/manifest.json},
  packages/tbd-schema/schema/terrain-manifest.schema.json,
  tools/tbd-tools/src/world/{build,reclassify,gates}.rs,
  xtask/src/{map_world_los,verify_blas_manifest,label_gates,schema_gates}.rs,
  xtask/src/map_blueprint/{library,library_cli,world_row}.rs

## THE ENFUSION LOCKSTEP — NEW SINCE WAVE 242, AND IT WILL REFUSE A ONE-SIDED EDIT
There are TWO mod trees and you must edit BOTH:
  apps/mod/tbd-framework/...   <- the tree the shipping dedicated server actually loads
  apps/mod/tbd-export/...      <- the mirror, ASCII-ONLY, scanned as a hard gate

What the lockstep demands (xtask/src/gate_mod_compile.rs:641-711):
  * Same relative path in both trees.
  * The two copies must be identical after: comments stripped (line AND block), all whitespace collapsed,
    and 23 ASCII_FOLD substitutions applied. CODE AND STRING-LITERAL CONTENTS MUST MATCH EXACTLY —
    a differing resource GUID inside quotes fails, deliberately.
  * The tbd-export copy must be PURE ASCII in every `Scripts/**/*.c`. WRITE ASCII IN BOTH.

THE HOLE: `mirror_lockstep` walks the EXPORT tree only. A brand-new script created in tbd-framework
with no export twin is INVISIBLE — the gate says OK, and it is still wrong. If you add a new `.c`, ADD BOTH COPIES.

Enfusion traps: `vanilla` is reserved; `Format` takes at most 9 parameters; method arguments may not be
strong refs; `FileHandle` members need `ref`; there is NO ternary operator; long boolean chains hit
"Formula too complex". A broken module kills the whole compile.

THE ALLOCATE-ON-ABSENT LANDMINE: `JsonLoadContext.ReadValue` ALLOCATES a nested `ref <class>` field EVEN
WHEN THE JSON KEY IS ABSENT. `if (x.nested)` is ALWAYS TRUE. Presence must be a scalar sentinel
(`Y_ABSENT = -1000000`) or a container `Count()`.

Binding pattern: one `JsonLoadContext` pass, binds BY CLASS MEMBER NAME. Plain classes, public fields, no
macro. Read TBD_MissionSlotStruct.c:47-136 as the template.

## NON-NEGOTIABLE RULES — every one was learned the hard way
1. VERIFY THE DEFECT STILL EXISTS ON MAIN BEFORE WRITING CODE. If already fixed, STOP and report with the proving command.
2. PROVE NON-VACUITY BY PERTURBATION. For every test you write: break the thing it guards, capture the RED output
   VERBATIM, restore, `touch` the restored file, re-run green. PASTE THE RED OUTPUT IN YOUR REPORT.
3. `cargo test -p map-engine-core` ALWAYS with `--all-features`. Cross-check `--list` total vs run total.
4. COMMIT EARLY AND OFTEN on the slice branch with explicit paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER
   `git stash` (deletes LFS pointer files). Use `git -c filter.lfs.process= -c filter.lfs.required=false ...` if git
   complains about LFS. Leave the tree COMMITTED AND CLEAN whenever you pause or finish.
5. GATE: `cargo xtask platform wave gate --slice T-673` from inside the worktree; must end `SLICE GATE: PASS`.
   "REFUSING to pass — resolved to NO crate" / "rustfmt was invoked ZERO times" means your diff has no lintable Rust.
   Also run `cargo xtask mod compile` and paste the verdict. The schema-gate UNREAD_WIRE_FIELDS red is expected;
   the slice gate may still fail on it — report that exact output; do not "fix" it by editing schema_gates.rs.
6. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `cargo xtask ci schema-validate` does not
   work in worktrees (`cargo xtask schema validate` does).
7. File-length allowlists are never extended: new code goes in NEW files; allowlisted giants grow only by call-site lines.
8. No `.py`, `.sh`, `.mjs` files committed. Throwaway probes go in /tmp.
9. Edition per crate: read the crate's Cargo.toml. This slice is Enfusion; rustfmt edition only matters if you
   touch Rust, which you should not.
10. Class-R byte-parity pins must scrub the test module out of the haystack (`class_r_scrub::live_source`).
11. Do NOT spawn subagents. Do the work yourself.
12. You do NOT ship: no push, no merge, no ticket status edits, no docs/ or .ai/tickets/ edits, no filing tickets.
13. trunk serve (:3000) and the dev API (:8080) are running — LEAVE BOTH UP.
14. No silent deferrals. `dem.raw` unfilled and T-981 are the other slice's operator calls, not yours.
15. If you contradict this brief, assume you looked and the command center remembered — say it plainly with the command.
16. VALIDATE MEANING, NOT ONLY LAYOUT.
17. DEAD MECHANISMS ARE WORSE THAN MISSING ONES.
18. AN ANSWER ABOUT THE WORLD MUST CARRY THE WORLD IT WAS BUILT FOR.

## YOUR REPORT — the deliverable, every field required
pwd_branch · defect_verified_on_main [{claim, path:line, command}] · changes [{path, line, why}] ·
perturbation {red_output VERBATIM, restored_green} · gate_verdict_tail (last 15 lines) ·
mod_compile_verdict · unread_wire_fields_red (verbatim, expected) ·
files_outside_owns [] · found_not_fixed [{path:line, repro}] · deviations [] · commits [sha] ·
manual_checklist [one human-runnable line per in-game behaviour you could not prove mechanically]
twins_confirmed [every .c path, both trees exist on disk]
