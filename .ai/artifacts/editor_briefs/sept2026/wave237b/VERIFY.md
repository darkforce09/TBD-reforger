You are the adversarial verifier for wave 237 of the TBD-Reforger factory. Your job is to find what the three slice agents and the command center got WRONG, not to confirm they were right. Do NOT fix. Do NOT commit. Do NOT file tickets. Leave main exactly as you found it (restore anything you mutate; `git status --porcelain` must be empty when you finish). No subagents. Token economy: read only what you attack; ranges for files over 200 lines.

Repo: /run/media/system/Disk_2/Projects/TBD-Reforger, branch main (do not create branches).

ENVIRONMENT. You are in the `claude-desktop` container (glibc 2.36); the shared cache is host-stamped (glibc 2.43), so run EVERY cargo/rustfmt command on the host through the wrappers, which pass cwd, stdin and exit code through:
  /home/Samuel/.cache/tbd-bin/hcargo <args>     /home/Samuel/.cache/tbd-bin/hrustfmt <args>
`podman` is host-only: `distrobox-host-exec podman exec tbd_reforger_db psql -U tbd -d postgres -c '<SQL>'`.
DB-backed tests need `tbd_verify_w237_it`; drop it when done. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `map-engine-core` is ALWAYS `--all-features`.
KNOWN ENVIRONMENTAL RED on a slice worktree only (filed as T-972): `packages/map-assets` LFS payloads are pointer files there, so `dem::peaks::everon_peaks_max_above_350` fails `Invalid PNG signature`. ON MAIN it must PASS — if it does not, that IS a finding.

THE SPAN. Base marker `4f2d4598f` (wave 236 CLOSED). Landed:
- `9fcf4b218` T-924 — `xtask/src/wave/verdict.rs` (new), `gate.rs`, `land.rs`, `mod.rs`: a gate-verdict receipt at `.ai/artifacts/verdicts/<slice>.json`, written by the SLICE gate on pass AND fail, and `cmd_land` refuses a slice whose receipt is missing or stale.
- `2c57d0162` T-935.5 — `crates/map-engine-core/src/geometry/tbdd.rs`, `tools/tbd-tools/src/density.rs`, `crates/map-engine-core/Cargo.toml`: TBDD decode by Pod header + `bytemuck::cast_slice`, with a Class-R parity oracle over all 625 everon tiles.
- `e68ad8c7c` T-935.2 — `tools/tbd-tools/src/world/binary_emit.rs` (new), `build.rs`, `mod.rs`, `Cargo.toml`: `build-objects` dual-emits a TBDC `.bin` beside every chunk `.json.gz`.
Command-center commits in the span are registry filings plus `T-946: wave.lock — one repack after wave 237 shipped`.

Highest-risk claims to attack:
1. T-924, and attack it hardest — it is a GUARD, so a hole in it is silent. (a) The receipt is written to `ctx.main_root`, and its directory self-ignores with a runtime `.gitignore` containing `*`. Prove the receipt is invisible to `git status` in BOTH the worktree and main, and that the self-ignore cannot be defeated (what if `.ai/artifacts/` is already covered, or the file is deleted?). (b) The refusal compares `git rev-parse slice/<t>` against the receipt sha. Find a way to land an UNGATED slice anyway: a receipt for a different slice, a receipt written by a gate that examined nothing, a slice branch fast-forwarded after gating, `--bookkeeping`, or a land that never enumerates the slice. (c) The write happens before the FAIL return — confirm a RED gate really does leave a receipt saying FAIL, and that `land` refuses on it. (d) `record_slice_gate` is called from ONE site; are there gate paths that pass without reaching it?
2. T-935.5. (a) The parity oracle lives in the test module and the scrubber excises it. Prove the scrubber actually removes the reference loop from the haystack, and that the acceptance pin would catch a per-byte assembler reintroduced under a different name (`u16::from_le_bytes`, a macro, an inlined helper). (b) 354 of 625 everon tiles are all-zero ocean; the agent found one of its own pins graded against one. Check EVERY other pin in both files for the same vacuity — does each touch a tile with signal? (c) `bytemuck` became a NON-optional dependency of `map-engine-core` (outside the ticket's owns). What does that add to the SPA's wasm bundle, and does `--features binary` still pull what T-935.1's doc claims? (d) Decode a deliberately unaligned buffer and a hostile header on a 32-bit target; the `want` computation moved to u64 — prove `Truncated` rather than a panic.
3. T-935.2. (a) The emitter narrows rows through the LOADER's `narrow_instance_row_v2` rather than the builder's raw f64s, to match `-0.0`, defaulted trailers and the `<= 0.0` scale clamp. Find an input where `.bin` and `.json.gz` still disagree. (b) The parity test decodes each `.bin` twice, once by `cast_slice` and once at raw byte offsets — verify the second decoder is genuinely independent (it must not share `ObjectInstancePod`), and that a field REORDER is caught. (c) The `.bin` files are neither committed nor ignored (T-970) and `verify-phase` will red on them (T-971) — confirm both, and check whether any OTHER consumer walks that directory and would now see them.
4. Command centre. (a) `ticket ship --no-repack` ×3 then one repack froze wave 237's full set. Confirm the pending entry matches the three landed tickets exactly and that no label collides with an open wave. (b) The three tickets must read `status = "shipped"` with `shipped_at` equal to their landing merges (`9fcf4b218`, `2c57d0162`, `e68ad8c7c`). (c) `cargo xtask ticket check` exit 0.
5. Gate vacuity. The full gate PASSed. Pick the three steps most likely to pass without examining the new code and prove for each whether it really ran over it. `--list` versus run totals for every crate you test. In particular: did anything execute `binary_emit`'s 315-chunk parity test and `tbdd`'s 625-tile parity test, or are they only reachable by a command the gate never runs?

Severity table (also the command center's triage table):
- BLOCKER: main is broken, data at risk, or a gate reported success on code it never examined.
- MAJOR: a shipped ticket does not do what it claims, or can destroy operator-authored work.
- MINOR / NIT: everything else.

Report (≤ 60 lines): findings first, each with severity, file:line, the exact command + verbatim output that proves it, and the fix shape (one line). Then an explicit list of what you attacked and FAILED to break. Then confirm main is untouched (`git status --porcelain` empty, HEAD sha).
