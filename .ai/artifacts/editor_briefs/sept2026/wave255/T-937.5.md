You are the slice agent for **T-937.5** (Payload item schemas, duplicate slot guard, 8 MB ceiling).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-937.5  branch slice/T-937.5
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-937.5`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-937.5
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-937.5 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-937.5. Then prove the defect.
## THE JOB
Three distinct validation improvements across the payload schema and editor library:
1. `mission-editor-payload.schema.json`: `slots` and `editorLayers` currently have bare `{"type": "array"}`
   without item schemas. Add `$defs/editorSlot` and `$defs/editorLayer` with `additionalProperties: false`,
   referencing existing payload definitions. Measure against committed fixtures before tightening.
2. New `apps/website/frontend/src/editor/state/operations/slot_ids.rs` (registered in `operations.rs`):
   `pub fn duplicate_slot_ids(doc: &map_engine_core::doc::MissionDocCore) -> Vec<(String, String)>`
   detecting any duplicate slot IDs appearing under the same callsign (or squad), leveraging T-937.3's
   `doc.slot_exists` and `id_arrays` helpers. The editor upload/save path refuses duplicates with a message
   naming both the callsign and the duplicated slot id.
3. `mission_library.rs:1452`: `UPLOAD_MAX_BYTES` drops from `64 << 20` to `8388608` (8 MiB, matching
   `mission.schema.json:6` and backend `api contract/validate.rs`). The upload UI in `mission_library.rs`
   surfaces the current picked file's bytes against the 8 MB ceiling before the upload button is clicked.

## ANCHORS (verified 2026-09-07 — the ticket's are stale)
- `packages/tbd-schema/schema/mission-editor-payload.schema.json:70-71`: `slots` and `editorLayers` bare arrays (moved from :42-43).
- `apps/website/frontend/src/editor/library/mission_library.rs:1452`: `UPLOAD_MAX_BYTES: usize = 64 << 20;`.
- `apps/website/frontend/src/editor/library/mission_library.rs:1458-1468`: `oversize_refusal(bytes: usize) -> Option<String>`.
- `apps/website/frontend/src/editor/library/mission_library.rs:1753, 1768`: `up_size` signal created at :1753 and discarded at :1768.
- `apps/website/frontend/src/editor/library/mission_library.rs:1930-1975`: file pick handler setting `up_size` and checking `oversize_refusal`.
- `apps/website/frontend/src/editor/library/mission_library.rs:1991-2070`: `upload_document` version upload.
- `apps/website/frontend/src/editor/state/operations.rs:33-44`: submodule registrations (T-936.7 registered `tactical_graphics`).
- `crates/map-engine-core/src/doc/store.rs:832`: `slot_exists(&self, id: &str) -> bool` (shipped by T-937.3 in wave 254).

## TRAPS THAT WILL BITE THIS SLICE
- **Do not close top-level payload schema**: top-level `mission-editor-payload.schema.json` intentionally remains open
  per its own :60 rationale (`payloadExtras` passthrough). Only constrain the `items` inside `editor.slots` and `editor.editorLayers`.
- **Examine live payloads & goldens**: Verify `packages/tbd-schema/registry/mission-editor-payload.sample.json` and all
  golden fixtures pass schema validation after adding item schemas. Run `hcargo xtask schema validate`.
- `operations.rs` is a facade: register `pub mod slot_ids; pub use slot_ids::*;`. Ensure `slot_ids.rs` compiles cleanly
  on both native (if tested) and `wasm32-unknown-unknown`.
- `class_r_scrub::live_source` truncation: any test in `slot_ids.rs` must sit at the bottom.

## YOUR FILES (touch ONLY these)
packages/tbd-schema/schema/mission-editor-payload.schema.json
apps/website/frontend/src/editor/library/mission_library.rs
apps/website/frontend/src/editor/state/operations/slot_ids.rs   (new)
apps/website/frontend/src/editor/state/operations.rs

## SIBLINGS THIS PACK (do NOT touch)
T-242 flatten.rs, mission.schema.json ·
T-257 doc/store.rs ·
T-939.1 outliner_tree.rs, outliner_drag.rs, panels/mod.rs ·
T-939.3 overlays.rs, gestures.rs, gizmo_z.rs, canvas/mod.rs

## RULES
1. Verify defect first: a payload with a slot item carrying `{bogus: 1}` currently validates; paste the RED.
2. Perturbation: drop the callsign key from the duplicate check; test must go RED; restore, `touch`, green.
3. `hcargo test -p website-frontend` and `hcargo xtask schema validate`.
4. Explicit git paths. NEVER `git add -A`, NEVER `git stash`. Commit early and often.
5. `hcargo xtask platform wave gate --slice T-937.5` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`.
7. No subagents. No ship/merge/push. Leave :3000 and :8080 up.
8. No silent deferrals.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave255/REPORT-T-937.5.md` in the worktree.
Branch committed and clean. No push.
