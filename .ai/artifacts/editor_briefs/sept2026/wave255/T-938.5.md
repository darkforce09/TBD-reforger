You are the slice agent for **T-938.5** (Viewsheds sliced across frames with caps).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-938.5  branch slice/T-938.5
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-938.5`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-938.5
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-938.5 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-938.5. Then prove the defect.
## THE JOB
Placement blocks the wasm thread for a whole raycast sweep and oversize radii are unbounded. Make
both viewshed loops resumable, cancellable, budgeted jobs:
- `building_viewshed.rs` and `dem/sample.rs` expose resumable batch iterators plus a cancel token.
- New `tools/viewshed_scheduler.rs` (registered in `tools/mod.rs`): one active job per tool,
  animation-frame batches <= 4 ms, cancel on new placement.
- `los_tool.rs` `place_viewshed` submits to the scheduler; nothing else changes in that file.
- Sliced results are **bit-identical** to the synchronous path on a fixture (test).

**CAPS — operator decision 2026-09-07, split per subsystem.** The ticket's original single pair
(radius <= 400 m, cells <= 250k) refused the shipped default: the terrain viewshed default is
**2000 m** (`dem/sample.rs:390`, re-exported `los_tool.rs:797`, used `:815`) and at the 8 m
`PROFILE_STEP_M` a centred observer is 501x501 = **251,001 cells**, over that cap by 1,001. So:
- **Building wash: radius <= 400 m** (16x its 25 m default).
- **Terrain viewshed: cells <= 300k**, and the 2000 m default is UNCHANGED.
Over-cap is refused with a message naming which cap and the measured value.

## ANCHORS (verified 2026-09-07)
`building_viewshed.rs` (288 lines): `WASH_CELL_M` `:37`, `WASH_EYE_M` `:39`, `WASH_RADIUS_M` `:41`,
`MAX_WASH_DIM` `:43`, `grid_rect` `:141-150`, `level_wash` `:156-175`, `level_wash_compound`
`:180-195`, `compound_wash` `:213-221`, `wash_band` signature `:225` with its loop at `:251-268`,
`level_washes` `:274-284`.
**The building loop is pure over its index** — it accumulates only `wash.cells` via `push`, no running
horizon, every cell's verdict a function of `(col,row)`, `obs`, `eye_y` and the injected `blocked`
closure. Slicing is mechanical: pre-size `vec![Unknown; cols*rows]`, write by index, carry a `cursor`.
All grid geometry is computed at `:246-249` before the loop, so resume recomputes nothing.
`dem/sample.rs` (1185 lines): `compute_viewshed` `:408`, radius defaulting `:420-424`, `OVERSAMPLE`
`:491`, ray/step setup `:492-499`, radial loop `:501-553`.
**The terrain loop is NOT pure over its index** — `let mut max_angle = f64::NEG_INFINITY;` at `:507`
is a running horizon advanced at `:550-552`, and `Visible` is written stickily across rays
(`:543-546`). **Checkpoint at a RAY boundary (`ri`), or persist `max_angle` with `(ri, s)`, and
preserve ray order — otherwise your results are not bit-identical and the fixture test is a lie.**
There is no radius clamp today; `:420-424` only substitutes the default for non-finite/non-positive.
`place_viewshed` is `los_tool.rs:908` (not :904), body `:908-919`; `compute_viewshed_for` `:810-825`.

## THE IDIOM ALREADY EXISTS — COPY IT, DO NOT INVENT ONE
T-090.12.5's object-wash pass is already a cancellable, frame-sliced, budgeted job over the same
`Viewshed` raster:
- `ObjectPass` `tools/los_world.rs:268-285` with `pub cursor: usize` `:281` and
  `pub generation: u32` `:282`; `new` `:290`; **`step(&mut self, vs, eye_z, ground, test, budget_ms,
  now) -> bool` `:449-465`** — breaks on `if now() - start >= budget_ms` `:474-476`, advances
  `self.cursor += 1` `:487`, sets `done` `:471`. `OBJECT_PASS_BUDGET_MS` `:211` (yours is 4 ms).
- Cancel token: a monotonic `static GENERATION: Cell<u32>` `los_world_wasm.rs:36`, bumped on start
  `:131-134` and by `cancel_object_wash()` `:147-150`. That IS "cancel on new placement".
- The self-rescheduling rAF closure shape: `canvas/viewport.rs:60-137`.
`persist.rs:728` also has a `type IsCancelled = Box<dyn Fn() -> bool>;` if you prefer a predicate.
`map-engine-core` holds no cancellation state — the core returns after its budget and the caller
decides whether to call again. Keep that split.

## CONSTRAINTS THAT WILL BITE THIS SLICE
- **`canvas/gestures.rs` and `canvas/viewport.rs` are NOT yours** — they belong to T-936.7 and
  T-938.6 this wave. `gestures.rs:1310-1311` calls `place_viewshed` and uploads at `:1313-1324`;
  `viewport.rs:88` pumps the object-wash tick. **Keep `place_viewshed`'s call-site contract
  unchanged and give your scheduler its OWN rAF closure inside `viewshed_scheduler.rs`.** That is
  also what preserves `mission_editor_tests/t644_viewshed_wiring.rs:79-86`, an unowned source pin
  requiring `place_viewshed(` to still PRECEDE `viewshed_upload(`. If you conclude the call site
  genuinely must change, STOP and report — do not edit either file.
- **Put the radius clamp in `wash_band` / `level_wash*`, NEVER in `grid_rect`.**
  `building_viewshed_tests.rs:180-186` (`oversized_radius_coarsens_the_cell_to_the_cap`) calls
  `grid_rect([0,0], 1000.0, WASH_CELL_M)` and asserts it COARSENS to `MAX_WASH_DIM` rather than
  refusing. That file IS yours now, but the test encodes real behaviour — do not "fix" it to suit a
  clamp in the wrong place.
- `apps/website/frontend/src/pages/debug/building_viewer.rs` is **unowned** and consumes
  `level_wash`, `level_wash_compound`, `level_washes`, `WashParams`. Its tests assert per-cell
  `Visibility` equality (`:1283-1330`), a census band `south_lit > 100 && < 2500` (`:1315-1318`) and
  `1e-9` rect goldens (`:1273-1276`). Any change to iteration order or cell-centre rounding moves
  them. **This is the file T-938.4 broke last wave by exactly this route.**
  `tools/los_world_tests.rs` (unowned, `#[path]`-mounted into `los_world.rs`) has 58 `assert_eq!`
  over `dem::sample::{Viewshed, Visibility}`.
- `los_tool.rs:1692` is a Class-R self-pin banning the tokens `MissionDocCore`, `store.rs`,
  `hydrate`, `after_local_edit`, `editor_ops` in that file. A scheduler submission must not introduce
  them. A second self-pin at `:1809-1811` requires two literal RGBA strings to survive.
- `los_tool.rs` AND `dem/sample.rs` are both file-length allowlisted — new code goes in new files;
  those grow by call-site lines only.
- Any `#[cfg(test)]` item you add must sit BELOW every production item a probe searches for
  (`class_r_scrub::live_source` cuts from the first one to EOF). Today `building_viewshed.rs` cuts at
  `:286`, `dem/sample.rs` at `:568`, `los_tool.rs` at `:1253` — all safe; keep them that way.
- **Do not widen a tolerance or threshold in a golden to absorb a behaviour change.** If a numeric
  contract moves, grep for identical assertions in files you do NOT own and STOP and report.
## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/building_viewshed.rs
crates/map-engine-core/src/building_viewshed_tests.rs
crates/map-engine-core/src/dem/sample.rs
apps/website/frontend/src/editor/tools/viewshed_scheduler.rs   (new)
apps/website/frontend/src/editor/tools/mod.rs
apps/website/frontend/src/editor/tools/los_tool.rs
## SIBLINGS THIS PACK (do NOT touch)
T-936.7 mission/{tactical_graphics,mod,extensions,flatten,compile}.rs + both schemas +
canvas/{tactical_graphics,mod,gestures,commands,render_sync}.rs + state/{history,operations,
operations/tactical_graphics}.rs · T-937.3 doc/store.rs + state/operations/entity.rs · T-190
state/{persist,hydrate,tab_lock,mod}.rs + mission_editor.rs + canvas/overlays.rs · T-938.6
world_assets/{memory_budget,mod,satellite}.rs + canvas/viewport.rs
## RULES
1. Verify defect first: time `place_viewshed` at r=25 on a golden building — paste the blocking
   duration. Show the absent radius clamp.
2. Perturbation: make the batch iterator skip its last row → the fixture equality test must go RED
   verbatim; restore, `touch` the file, green. A git restore does not re-trigger cargo.
3. `hcargo test -p map-engine-core --all-features` (without it, feature-gated modules never compile —
   vacuous pass). Also `hcargo test -p website-frontend`, tail pasted.
4. Explicit git paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER `git stash` (eats LFS pointers).
   Commit after every self-contained step so an outage leaves reviewed work.
5. `hcargo xtask platform wave gate --slice T-938.5` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`. A test printing `skip:` is a FAIL. hrustfmt --edition 2024 for
   map-engine-core; 2021 for the frontend.
7. New code goes in NEW files — file-length allowlists are never extended.
8. No subagents. No ship/merge/push. No docs/ or .ai/tickets/ edits. Leave :3000 and :8080 up.
9. No silent deferrals: the whole acceptance is the deliverable. If a piece is genuinely blocked or
   needs a file you do not own, STOP and report it precisely — do not write "follow-up".
## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave255/REPORT-T-938.5.md` in the worktree.
Branch committed and clean. No push.
