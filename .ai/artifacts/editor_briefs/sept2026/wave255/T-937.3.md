You are the slice agent for **T-937.3** (Materialize side-key cache and slot_exists fast path).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-937.3  branch slice/T-937.3
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-937.3`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-937.3
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-937.3 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-937.3. Then prove the defect.
## THE JOB
`materialize()` resolves every slot from scratch on every call. Add a per-slot side-key cache it
reads through, and give `entity.rs` a `slot_exists(id)` fast path that does NOT drop hidden slots.
Acceptance: existence checks answer true for hidden slots and never materialize the document;
materialize resolves each distinct side key once per call and returns output identical to today's.

## TWO THINGS THE TICKET GETS WRONG — READ BEFORE PLANNING
**1. There is no change observer.** The ticket says the cache is "invalidated by the store's existing
change observer." **No such thing exists** — zero hits for `.observe(`, `observe_deep`,
`observe_update`, `yrs::Subscription` anywhere in `map-engine-core` or the frontend. `MissionDocCore`
(`store.rs:71`) has no cache fields; its only interior mutability is `init_mode: Cell<bool>` (`:260`)
and `undo_cap_hidden: Cell<usize>` (`:266`). About 70 mutators take `&self`, not `&mut self`. So you
either add `RefCell`/`Cell` state plus explicit invalidation, or you introduce the observer yourself.
Say which you chose and why in the report. Do not go hunting for a thing that is not there.

**2. `materialize(&self) -> SlotSoa` is a PINNED SIGNATURE this wave.** `persist.rs:1081` calls it
inside `slots_digest`, and `persist.rs` belongs to T-190, running right now. Changing `&self` to
`&mut self`, or returning a borrow, forces an edit into a sibling's file. Interior mutability only.
(`mission_editor.rs:2352` reads `&soa.side_keys` and `canvas/render_sync.rs:914,928` rebuild
`out.side_keys` — also not yours.)

## ANCHORS (verified 2026-09-07 — the ticket's are ~6 lines stale)
`materialize()` is `store.rs:737`; the read txn opens `:738`; the per-call prelude is `:743-758`; the
slot loop is `:766-807` (not `:772-812`); dict flush and return `:809-813`.
Per-slot work is **seven** yrs reads, not five — the ticket omits `read_bool(&txn,&slot,"editorHidden")`
at `:775`, which runs BEFORE the early-`continue` and so costs on every dropped row too:
`:775` read_bool · `:778` read_position · `:786` read_stance · `:788`/`:789`/`:793` read_str
(role, tag, squadId) · `:800-805` resolve_slot_side_key.
The existing layer/hidden cache is `:743-758` (`slot_layer` keyed by slot id, `hidden_layers` keyed by
layer id via `layer_flag_effective`, `store.rs:5779`) — both are `let mut` LOCALS, per-call, dropped
on return. That is the shape to extend, and `:741-742` states the intent.
`resolve_slot_side_key` is a free fn at `store.rs:6781-6804`: up to 2 `MapRef::get` + 2 `read_str`
and a fresh `String` on every path, memoizing nothing. Second caller: `squad_link_inputs` `:834`.
"Hidden" is two independent flags OR'd: layer-hidden (T-665, `"hidden"` on an `editor_layers` row,
resolved up the `parentId` chain) and entity-hidden (T-701, `"editorHidden"` bool on the slot row,
stored only when true — `set_slot_editor_hidden_in_txn` `:6760-6768` removes the key on false).
The drop is one `continue` at `store.rs:772-777`. The raw `self.slots` map still holds the row, and
`slots_json` (`:682`) applies no visibility filter — that is your fast path.
`slot_attrs_exists` is **`entity.rs:1903`** (not :1885), body
`core.materialize().ids.iter().any(|s| s == id)`; sole call site `:3341`.
The two warning comments are exactly where the ticket says (`:1795` on `live_slot_ids`, `:1847` on
`mint_ids`) and they already spell out the rule — "**Never `materialize()`**… a hidden slot's id is
INVISIBLE to a materialize-sourced uniqueness proof" — which `slot_attrs_exists` 100 lines below
then violates. Point them at your new fn.
## TRAPS THAT WILL BITE THIS SLICE
- **`store.rs` defeats the sanctioned scrubber.** There is a real `#[cfg(test)] fn new()` at
  **`:5382`** inside `impl RemintMap`, with production code below it. `cut_test_module`
  (`arsenal/mod.rs:2170`) and the naive `split("#[cfg(test)]").next()` both cut from there to EOF, so
  `resolve_slot_side_key` (`:6781`), `read_position` (`:6725`), `read_str` (`:6743`),
  `read_bool` (`:6754`) and `layer_flag_effective` (`:5779`) are ALL invisible to a scrub.
  **Any Class-R probe you write must assert over the CALL SITE at `:800`, never the definitions** —
  a definition-grep sees an empty haystack and passes vacuously. (There is also a decoy at `:2649`:
  a backticked `#[cfg(test)]` inside a doc comment, which the naive split cuts on and the scrubber
  does not.) This exact class killed T-937.1's probe in wave 252 and T-937.4's in wave 253.
- Any `#[cfg(test)]` item you add must sit BELOW every production item a probe searches for.
- Do not widen a tolerance or threshold in a golden to absorb a behaviour change. If a numeric
  contract moves, grep for identical assertions in files you do NOT own and STOP and report.
## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/doc/store.rs
apps/website/frontend/src/editor/state/operations/entity.rs
## SIBLINGS THIS PACK (do NOT touch)
T-936.7 mission/{tactical_graphics,mod,extensions,flatten,compile}.rs + both schemas +
canvas/{tactical_graphics,mod,gestures,commands,render_sync}.rs + state/{history,operations,
operations/tactical_graphics}.rs · T-190 state/{persist,hydrate,tab_lock,mod}.rs + mission_editor.rs +
canvas/overlays.rs · T-938.5 building_viewshed*.rs + dem/sample.rs + tools/* · T-938.6
world_assets/* + canvas/viewport.rs
Note `state/history.rs` is T-936.7's this wave, and `entity.rs` calls `after_local_edit` 49 times —
if that signature moves under you, STOP and report rather than editing history.rs.
## RULES
1. Verify defect first: a test with a hidden slot shows `slot_attrs_exists` returning false for an
   existing slot — paste the RED. Also measure the resolution count before.
2. Perturbation: skip cache invalidation on a side change → the stale-side test must go RED verbatim;
   restore, `touch` the file, green. A git restore does not re-trigger cargo.
3. `hcargo test -p map-engine-core --all-features` (without it, feature-gated modules never compile —
   vacuous pass). Also `hcargo test -p website-frontend`, tail pasted.
4. Explicit git paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER `git stash` (eats LFS pointers).
   Commit after every self-contained step so an outage leaves reviewed work.
5. `hcargo xtask platform wave gate --slice T-937.3` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`. A test printing `skip:` is a FAIL. hrustfmt --edition 2024 for
   map-engine-core; 2021 for the frontend.
7. New code goes in NEW files — file-length allowlists are never extended.
8. No subagents. No ship/merge/push. No docs/ or .ai/tickets/ edits. Leave :3000 and :8080 up.
9. No silent deferrals: the whole acceptance is the deliverable. If a piece is genuinely blocked or
   needs a file you do not own, STOP and report it precisely — do not write "follow-up".
## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave255/REPORT-T-937.3.md` in the worktree.
Branch committed and clean. No push.
