You are the slice agent for **T-938.6** (Wasm memory budget guard with HUD readout).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-938.6  branch slice/T-938.6
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-938.6`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-938.6
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-938.6 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-938.6. Then prove the defect.
NOTE: your worktree may still carry a throwaway `t946_64_gate_probe` module at the end of
`canvas/viewport.rs` from a command-centre gate test. It should already be reverted — if
`git status` is not clean when you arrive, `git checkout -- apps/website/frontend/src/editor/canvas/viewport.rs`
and say so in your report.
## THE JOB
No budget exists: each loader allocates independently and the first out-of-memory aborts the wasm
instance. Satellite L0 is 655 MB RGBA.
- New `world_assets/memory_budget.rs` (registered in `world_assets/mod.rs`): per-asset registered
  peak, total budget (default 1536 MB, query-param override), `reserve(bytes) -> Decision`.
- `satellite.rs` consults the budget before loading a mip level; a `Degrade` decision raises the mip
  floor by one and logs it.
- Debug HUD: reserved bytes against the budget, and the current satellite floor.
- Measured peaks per asset on everon, recorded in your report.

## ANCHORS (verified 2026-09-07)
**The HUD is not rows.** It is ONE `RwSignal<String>`, rendered at `panels/toolbelt.rs:693-707` in
the `data-status-hud` slot, and formatted in exactly one place: **`canvas/viewport.rs:120-126`** — the
`"z {:.2} · c{chunks} · glyph {glyphs} · {fps:.0} FPS · rf {rf_ms:.2}ms ({rf_eq:.0} eq){}"` line.
The extension seam is the trailing `hud_suffix()` (`tools/los_world_wasm.rs:232`), a
`pub fn -> String` returning `" · occl …"` or empty. **Add a `hud_suffix()`-shaped `pub fn` in your
module and concatenate it there.** `viewport.rs` is in your owns for exactly this.
Gating is three-deep and you should not change it: `chrome_hidden` on the StatusBar, `hud_shown`
(`debug_hud_shown`, seeded false, toggled Ctrl/Cmd+Alt+D at `canvas/commands.rs:287`), and a
non-empty string. Pinned by `mission_editor_tests/t635_debug_hud.rs:37,60,86-88` and
`t670_scale_signal.rs:94` — both unowned.
**The mip floor is `base`.** `satellite.rs:486-493` inside `load_unified_full` (`:462`):
`texture_limit(engine)` `:53` → `pick_base_level_for_limit(&index, limit.map(|l| l.device))` `:487`
→ `let base = base as usize;` `:493` → `index.mips.get(base)` `:494`. `base` drives `mip_count`
`:501` and the upload plan `.skip(base)` `:514` with relative level `(li - base)` `:516`.
"Raise the floor by one" = increment `base` between `:493` and `:494`.
`report_chosen_level` (`:69-95`) already `warn!`s when `base > 0` — the natural home for the Degrade
log. `satellite.rs:520-526` already sums exact satellite transfer bytes and reports
`BootEvent::Budget(BootSeg::Satellite, total)`; that is the closest existing per-asset byte figure,
though it is transfer bytes, not RGBA-resident bytes. T-935.13 shipped the v2 container: header
dispatch in `tbd_sat.rs:150-156`, `fetch_index_head` `satellite.rs:170-184`.
Query-param idiom for a NUMERIC override: `UrlSearchParams` off `window.location().search()` —
`pages/debug/world_los.rs:110-112`. Best model is `world_host.rs:867-878`, which does the param AND
a `window.__global` fallback (`?t9382=1` or `window.__t9382Log`). The substring form
`s.contains("sat=preview")` at `satellite.rs:97-103` is for flags, not values.
T-938.2's counter (`world_host.rs:47-70`, publisher `:881-900`) counts **allocations, not bytes**, so
it is not a ledger you can read from — but copy its shape: private struct + `VecDeque` ring +
flag gate + `js_sys::Reflect` publish.
## CONSTRAINTS THAT WILL BITE THIS SLICE
- **`t629_satellite_resolution.rs:143-148` is a byte-exact source pin and it is UNOWNED.** It requires
  the literal `pick_base_level_for_limit(&index, limit.map(|l| l.device))` to survive verbatim, and
  `:118-173` requires `max_texture_dimension_2d()` to appear in **exactly two** places with
  `!src.contains("unwrap_or(8192)")`. **Put the budget in a SEPARATE statement after
  `let base = base as usize;` — never fold it into the `pick_base_level_for_limit(...)` line.**
  The same test also requires `report_chosen_level(&index, base, limit)`, a `logging::error!` +
  `return false;`, and a `logging::log!` after `tex_layer_commit` to stay present.
- `t628_boot_progress.rs` scrubs `satellite.rs` (`:491`) and `world_assets/mod.rs` (`:580,:642,:680`).
  A new `BootEvent::Budget` figure or an extra early return can trip it. Unowned.
- **`pick_base_level*` lives in `tbd_sat.rs:469-484`, which is NOT yours.** The floor logic must sit
  in `satellite.rs` or `memory_budget.rs`.
- `world_assets/mod.rs` is `#![cfg(target_arch = "wasm32")]` (`:4`) and its modules are private `mod`,
  not `pub mod`, with narrow explicit re-exports (`:15-16`). A new sibling inherits the wasm gate, so
  native tests cannot live there — put them where `hcargo test -p website-frontend` can reach them, or
  say plainly why they cannot exist.
- Registering `mod memory_budget;` changes the haystack for `panels/dock_left.rs:2147` and
  `tools/ruler_tool.rs:1465`, which both scrub `world_assets/mod.rs`. Check their needles first.
- `world_assets/bridge.rs` (unowned) publishes `satW`/`satH`/`satMips` to JS (`:11-14`, `:59-62`);
  raising the floor changes `satMips` for any consumer.
- Any `#[cfg(test)]` item you add must sit BELOW every production item a probe searches for
  (`class_r_scrub::live_source` cuts from the first one to EOF). `satellite.rs` and
  `world_assets/mod.rs` currently have NONE — the whole file is haystack. Keep it that way if you can.
- **Do not widen a tolerance or threshold in a golden to absorb a behaviour change.** If a numeric
  contract moves, grep for identical assertions in files you do NOT own and STOP and report.
## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/world_assets/memory_budget.rs   (new)
apps/website/frontend/src/editor/world_assets/mod.rs
apps/website/frontend/src/editor/world_assets/satellite.rs
apps/website/frontend/src/editor/canvas/viewport.rs
## SIBLINGS THIS PACK (do NOT touch)
T-936.7 mission/{tactical_graphics,mod,extensions,flatten,compile}.rs + both schemas +
canvas/{tactical_graphics,mod,gestures,commands,render_sync}.rs + state/{history,operations,
operations/tactical_graphics}.rs · T-937.3 doc/store.rs + state/operations/entity.rs · T-190
state/{persist,hydrate,tab_lock,mod}.rs + mission_editor.rs + canvas/overlays.rs · T-938.5
building_viewshed*.rs + dem/sample.rs + tools/{viewshed_scheduler,mod,los_tool}.rs
T-938.5 needs `viewport.rs` untouched in the ways it depends on — it pumps `tick_object_wash` at
`:88`. Add to the HUD format; do not restructure the rAF loop.
## RULES
1. Verify defect first: show that no budget exists and that each loader allocates independently —
   paste the evidence (the `?t9382`-style probe or a load under a constrained heap).
2. Perturbation: make `reserve` never return `Degrade` → the floor test must go RED verbatim;
   restore, `touch` the file, green. A git restore does not re-trigger cargo.
3. `hcargo test -p website-frontend`, tail pasted.
4. Explicit git paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER `git stash` (eats LFS pointers).
   Commit after every self-contained step so an outage leaves reviewed work.
5. `hcargo xtask platform wave gate --slice T-938.6` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`. A test printing `skip:` is a FAIL. hrustfmt --edition 2021 here.
7. New code goes in NEW files — file-length allowlists are never extended.
8. No subagents. No ship/merge/push. No docs/ or .ai/tickets/ edits. Leave :3000 and :8080 up.
9. No silent deferrals: the whole acceptance is the deliverable, the HUD readout and the measured
   per-asset peaks included. If a piece is genuinely blocked or needs a file you do not own, STOP and
   report it precisely — do not write "follow-up".
## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist ·
measured_peaks (per asset, everon)
Write `.ai/artifacts/editor_briefs/sept2026/wave255/REPORT-T-938.6.md` in the worktree.
Branch committed and clean. No push.
