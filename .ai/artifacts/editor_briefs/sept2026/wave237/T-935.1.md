You are the slice agent for ticket **T-935.1** (world::binary — POD, containers, rkyv archives) on the TBD-Reforger factory.

WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-935.1   (branch slice/T-935.1)

## ENVIRONMENT — THIS RUN (read before the first command)
You are inside the `claude-desktop` container (Debian, glibc 2.36). The shared warm cache
/home/Samuel/.cache/tbd-target is stamped for the HOST toolchain (Fedora, glibc 2.43). Running cargo in the
container against it is the GLIBC_2.xx-not-found trap (T-853). So EVERY cargo / rustfmt command runs on the
host through these wrappers (they pass cwd, stdin and exit code through):
   /home/Samuel/.cache/tbd-bin/hcargo <cargo args>
   /home/Samuel/.cache/tbd-bin/hrustfmt <args>
Wherever this brief or the ticket says `cargo xtask …`, run `hcargo xtask …`. `hcargo` forwards CARGO_TARGET_DIR
(default the shared cache), CARGO_BUILD_JOBS=4, TEST_DATABASE_URL and TBD_IT_BASE_DB from your shell.
Never run bare `cargo`; never build into a worktree-local target/. git, grep, sed and file edits run in the
container as normal. A slice gate waits on the gate lock (`target/.tbd-gate.lock`, polls every 30 s, up to
3600 s) when a sibling is gating — that wait is normal, not a hang.

MEASURED THIS RUN, and it will bite you: the shared cache can hand you ANOTHER SLICE'S test binary (this is
T-300, open). A `--list` that shows tests your worktree does not contain is the tell — cross-check
`grep -c '#\[test\]'` against the module you are testing. When it happens, re-run in a private
`CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-935.1` and delete it on cleanup.
Also measured: `packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask
`map_blueprint::*` / `map_world_los::*` tests fail there with `bad magic [118, 101, 114, 115]` ("vers").
That is pre-existing and environmental — do not chase it, and do not claim it as your red.

FIRST THREE ACTIONS, before anything else:
  1. `cd` there, run `pwd && git branch --show-current`, paste the output in your report. If it does not show
     the worktree path and slice/T-935.1, STOP and report — do not start work.
  2. `export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target`. If you build a binary you then RUN, build THAT into
     `CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-935.1` and delete it on cleanup; verify binary provenance
     before trusting any run.
  3. Read the full ticket + plan + spec prompt block — they are the handoff, there is no prior chat:
       cat .ai/tickets/T-935.1.toml
       docs/plans/t-935_1_plan.md
       docs/specs/Mission_Creator_Architecture/t935_map_binary_storage.md  → section "## Claude Code prompt — T-935.1"
     Then read CLAUDE.md §HARD GATE and docs/platform/PLATFORM_FACTORY.md sections 'THE SIGNATURE DEFECT' and 'Known traps'.

NOTE FROM THE COMMAND CENTER. This is the foundation slice of the T-935 map-binary program: slices .2–.13
import these types unchanged, so the shapes you land are a contract. Touch NO emitter and NO loader. `map-engine-core`
must still build WITHOUT the feature — check both (`hcargo build -p map-engine-core` and
`hcargo test -p map-engine-core --all-features`).

## THE TICKET
Add the `binary` feature to map-engine-core: 32-byte ObjectInstancePod, TBDC/TBDE/TBDB headers, rkyv 0.8 archive types for roads, labels, water, catalog, regions, buildings, satellite index, optional manifest blocks, checked access and round-trip tests.

Main goal: Every binary map format has one authoritative Rust definition in map-engine-core that emitters and loaders share.

Context:
  - Operator spec 2026-09-04: hybrid rkyv (metadata) + raw #[repr(C)] POD (bulk GPU data); flatbuffers rejected. This slice is the foundation every other T-935 slice depends on.
  - Anchor 2026-09-04: rkyv is used nowhere; bytemuck only in crates/map-engine-render/Cargo.toml:21; GPU Pod structs only in map-engine-render/src/scene.rs:32/47/72.
  - The operator's 24 B sketch dropped pitch/roll/scale; T-090.12.1 put them on the wire (manifest objects.schemaVersion 1.1.0, transforms yaw+pitch+roll+scale), so the POD is 32 B.

Current state:
  - crates/map-engine-core/Cargo.toml:33,45,48: the `world` feature pulls flate2 + serde_json(float_roundtrip); no rkyv, no bytemuck anywhere in the crate.
  - crates/map-engine-core/src/world/chunk.rs:20: WorldChunk SoA (positions, prefab_idx u16, rotations, z, pitch, roll, scale, cls_codes u8, rows_by_class) — the shape the POD must round-trip.
  - crates/map-engine-core/src/world/mod.rs declares the world submodules; no `binary` module exists. manifest.rs:11/24 defines the manifest structs with no binary fields.

Requirements:
  - Feature `binary` in crates/map-engine-core/Cargo.toml: rkyv 0.8 with bytecheck validation, bytemuck with derive; no flate2 or serde_json pulled by this feature.
  - ObjectInstancePod: #[repr(C)] Pod+Zeroable {x,y,z,yaw,pitch,roll,scale: f32, prefab_id: u16, class_code: u8, _pad: u8}; size_of == 32, align 4, little-endian on disk.
  - Headers TBDC (chunk container), TBDE (DEM), TBDB (bathymetry): 32-byte #[repr(C)] Pod structs, field order without padding, magic + version u16, per spec section 3.
  - rkyv archives per spec section 4: RoadNetworkArchive, MapLabelsArchive, WaterVectorsArchive, PrefabCatalogArchive, ForestRegionsArchive, BuildingBlueprintArchive, TbdSatIndexV2; `access_checked::<T>(&[u8])` wraps rkyv::access with validation.
  - manifest.rs gains serde(default) optional blocks: objects.binary, dem.raw, labels, water, buildings, satellite unified encoding tbd-sat-v2; absent blocks parse exactly as today.
  - Round-trip unit test for every header and archive; a truncated or bit-flipped buffer is an Err from access_checked or parse, never a panic.

Approach (the ticket's own; deviate only with a stated reason):
  - Add feature `binary` (dep:rkyv 0.8 + bytecheck, dep:bytemuck derive) to crates/map-engine-core/Cargo.toml; `world` enables `binary` so existing consumers (frontend, xtask) get the types without Cargo changes.
  - Create crates/map-engine-core/src/world/binary/mod.rs (pub mod pod, chunk_container, archives; access_checked) and register `pub mod binary` behind cfg(feature) in world/mod.rs.
  - pod.rs: ObjectInstancePod with compile-time size/align assertions. chunk_container.rs: TbdcHeader, TbdeHeader, TbdbHeader, magic consts, `parse(&[u8]) -> Result<(&Header, &[u8])>`.
  - archives.rs: the rkyv types from spec section 4 with derive(Archive, Serialize, Deserialize) and bytecheck; `to_bytes` and `access_checked` helpers.
  - manifest.rs: add the optional blocks from spec section 5 with serde(default); existing everon manifest.json must still parse identically (test).
  - Tests: build each value, serialize, access_checked, compare; flip one byte in a relative pointer and assert Err. Run only with --all-features.
  - Perturbation: change _pad to [u8;2] so size becomes 36, paste the red const-assert, restore, `touch` the file, re-run green.

Done means:
  - map-engine-core builds with and without `binary`; size_of ObjectInstancePod == 32 is enforced at compile time.
  - Every archive and header round-trips byte-for-byte in tests; corrupt buffers return Err from access_checked.
  - The committed everon manifest.json parses unchanged with the new optional fields absent.
  - No emitter or loader is touched: downstream slices .2-.13 import these types unchanged.

## YOUR FILES (touch ONLY these; new files must have exactly these names)
crates/map-engine-core/Cargo.toml
crates/map-engine-core/src/world/mod.rs
crates/map-engine-core/src/world/manifest.rs
crates/map-engine-core/src/world/binary/mod.rs
crates/map-engine-core/src/world/binary/pod.rs
crates/map-engine-core/src/world/binary/chunk_container.rs
crates/map-engine-core/src/world/binary/archives.rs
If the work genuinely requires a file outside this list, STOP and report which file and why — do not widen silently.
Siblings running right now own (do NOT touch):
T-300: xtask/src/wave/mod.rs, xtask/src/platform_preflight.rs
T-277: packages/tbd-schema/rules/prefab-classify.json

## NON-NEGOTIABLE RULES — every one was learned the hard way
1. VERIFY THE DEFECT STILL EXISTS ON MAIN BEFORE WRITING CODE. If already fixed, STOP and report with the proving command.
2. PROVE NON-VACUITY BY PERTURBATION. For every test you write: break the thing it guards, capture the RED output
   VERBATIM, restore, `touch` the restored file (a git checkout restore does not re-trigger a cargo rebuild), re-run
   green. PASTE THE RED OUTPUT IN YOUR REPORT. Perturb the PRODUCTION side where you can, not only the test's oracle:
   a wave-236 slice found its first pin passed a real production break, and only the stronger oracle caught it.
3. `cargo test -p map-engine-core` ALWAYS with `--all-features` (without it feature-gated modules never compile — vacuous pass).
   Cross-check `--list` total vs run total.
4. COMMIT EARLY AND OFTEN on the slice branch with explicit paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER
   `git stash` (deletes LFS pointer files). Use `git -c filter.lfs.process= -c filter.lfs.required=false …` if git
   complains about LFS. Leave the tree COMMITTED AND CLEAN whenever you pause or finish.
5. GATE: `hcargo xtask platform wave gate --slice T-935.1` from inside the worktree; must end `SLICE GATE: PASS`.
   "REFUSING to pass — resolved to NO crate" / "rustfmt was invoked ZERO times" means your diff has no lintable Rust,
   not that your code is broken.
6. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `cargo xtask ci schema-validate` does not work
   in worktrees (`cargo xtask schema validate` does).
7. File-length allowlists (`cargo xtask verify file-length`) are never extended: new code goes in NEW files; the
   allowlisted giants grow only by call-site lines. If a file you own would cross the limit, STOP and report the count.
8. No `.py`, `.sh`, `.mjs` files committed (hard gates). Throwaway probes go in /tmp, never the source tree.
9. Edition per crate: read the crate's Cargo.toml and format with that edition (`hrustfmt --edition <n>`). Do not
   trust a brief that names an edition — a wave-236 slice was told 2021 for a crate that is 2024.
10. Class-R byte-parity pins must scrub the test module out of the haystack (`class_r_scrub::live_source`).
11. Do NOT spawn subagents. Do the work yourself.
12. You do NOT ship: no push, no merge, no ticket status edits, no docs/ or .ai/tickets/ edits, no filing tickets.
    Report findings; the command center files.
13. trunk serve (:3000) and the dev API (:8080) are running — LEAVE BOTH UP.
14. No silent deferrals: the ticket's whole acceptance is the deliverable. If a piece is genuinely blocked, STOP and
    report it precisely — do not write "follow-up".
15. If you contradict this brief, assume you looked and the command center remembered — say it plainly with the command.

## YOUR REPORT — the deliverable, every field required
pwd_branch · defect_verified_on_main [{claim, path:line, command}] · changes [{path, line, why}] ·
perturbation {red_output VERBATIM, restored_green} · gate_verdict_tail (last 15 lines, must end SLICE GATE: PASS) ·
files_outside_owns [] · found_not_fixed [{path:line, repro}] · deviations [] · commits [sha] ·
manual_checklist [one human-runnable line per behaviour you could not prove mechanically]
