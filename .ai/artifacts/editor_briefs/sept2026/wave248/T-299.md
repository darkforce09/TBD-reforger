You are the slice agent for ticket **T-299** (Single-faction compile ships a phantom opfor) on the TBD-Reforger factory.
WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-299   (branch slice/T-299)

## ENVIRONMENT — THIS RUN (read before the first command)
You are on the **HOST** (Fedora, glibc 2.43). Native `cargo` / `rustfmt` work. Do **not** wrap commands in
`distrobox-host-exec` or `hcargo` — those are for the claude-desktop container, which you are not in.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Use that for ordinary builds and tests. If you build a binary you then RUN, build THAT into
`CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-299` and delete it on cleanup.
A slice gate waits on the gate lock (`target/.tbd-gate.lock`, polls every 30 s, up to 3600 s) when a
sibling is gating — that wait is normal, not a hang.

MEASURED THIS RUN: the shared cache can hand you ANOTHER SLICE'S test binary (T-300). A `--list` that
shows tests your worktree does not contain is the tell. When it happens, re-run in the private dir above.
`packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask map tests fail
there with `bad magic [118, 101, 114, 115]` ("vers"). Environmental — do not chase it. Run
`cargo xtask schema validate`, NOT `cargo xtask ci schema-validate`.

GATE SCOPE: `wave gate --slice` is the CHEAP gate — check, wasm32 (frontend), fmt (changed), clippy
(changed crates), schema and pin gates. `trunk build` and the four `test *` steps are WAVE-level.
**NEITHER GATE COMPILES ENFUSION (T-946.17)** — if you touch `.c` files, `cargo xtask mod compile` is
yours to run and to paste. Do NOT run `cargo test -p xtask` while `mod compile` is running (T-946.13).

**T-946.27 — copy before the first `mod compile`:** `mirror_lockstep` fails in every slice worktree
over 19 untracked `Scripts/WorkbenchGame/EnfusionMCP/*.c`. They are gitignored on the framework side
(`apps/mod/.gitignore:28`) and exist only in the main checkout. Copy them in, do not commit them:
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/

FIRST THREE ACTIONS, before anything else:
  1. `cd` to the worktree, run `pwd && git branch --show-current`, paste the output in your report. If it
     does not show the worktree path and `slice/T-299`, STOP and report — do not start work.
  2. `export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target`.
  3. Read the full ticket + plan + spec — they are the handoff, there is no prior chat:
       cat .ai/tickets/T-299.toml
       docs/plans/t-299_plan.md
       docs/specs/ideas/t299_phantom_opfor.md
     Then CLAUDE.md HARD GATE and docs/platform/PLATFORM_FACTORY.md 'THE SIGNATURE DEFECT' and 'Known traps'.

## NOTE FROM THE COMMAND CENTER — READ TWICE
The pad moved. Ticket/plan cite `flatten.rs:2470-2472`. On this HEAD it is **`flatten.rs:3597-3612`**:
`// Schema requires ≥ 2 factions; pad a stub opposing faction` — stub `opfor` (or `blufor` if opfor
already present). Schema `properties.factions.minItems` is **2** (`mission.schema.json:70`).

  * You **may** edit `mission.schema.json` (you own it). Surgical string replace **only**. Do **not**
    `json.dumps` rewrite the file (reformats 1400 lines; wave 246 paid this). Change `factions`
    `minItems` 2 → 1. Leave other `minItems: 2` keys (e.g. `:184`) alone unless you prove they are
    the same factions array.
  * `cargo xtask schema validate` after the schema edit. `schema codegen` is NOT required:
    `mission.schema.json` is not a typify target (only editor-payload / registry / faction-library).
    Ignore the ticket verify line `cargo xtask ci schema-codegen` unless `verify-codegen-fresh` actually
    goes red.
  * Delete the pad. A one-faction document compiles with **exactly one** faction. Two-faction Class-R
    goldens stay **byte-identical**.
  * `faction_eliminated` already emits only when two factions HOLD SLOTS (`flatten.rs:3618-3622`) —
    keep that. Do not reintroduce an unconditional second side.
  * `TBD_MissionValidator.c` **both** trees: accept one faction; drop any ≥2 rule if present. The
    existing warning `declared but has no slots` must remain for a **declared** empty side, not for
    a side you stopped inventing.
  * Grep `apps/mod` for hard-coded `factions[1]` / second-side briefing. Anything outside the validator
    is `found_not_fixed`, not a silent extra edit.
  * Do **not** edit SpawnManager / PlacementScatter (T-679), ZoneVolume / MissionLoader / ObjectiveRegistry
    / zones_panel (T-685), or `schema_gates.rs`.

Perturbation: restore the pad; the single-faction test goes RED; restore, `touch`, green.

Requirements:
  - Single-faction compile has one faction; briefing/ORBAT show no phantom side.
  - Two-faction compiles unchanged byte for byte.
  - Validator accepts one faction; `mod compile` clean; twins exist.

Done means:
  - Schema minItems 1; pad gone; validator; goldens; perturbation pasted.

## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/mission/flatten.rs
packages/tbd-schema/schema/mission.schema.json
apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionValidator.c
apps/mod/tbd-export/Scripts/Game/TBD/Backend/TBD_MissionValidator.c
The export twin is MANDATORY. If the work genuinely requires a file outside this list, STOP and report
which file and why — do not widen silently.
Siblings running right now own (do NOT touch):
T-685: Zones/TBD_ZoneVolume.c (NEW), Backend/TBD_MissionLoader.c, Objectives/TBD_ObjectiveRegistry.c, zones_panel.rs
T-679: Backend/TBD_PlacementScatter.c (NEW), Gamemode/TBD_SpawnManager.c — both trees

## THE ENFUSION LOCKSTEP — NEW SINCE WAVE 242, AND IT WILL REFUSE A ONE-SIDED EDIT
There are TWO mod trees and you must edit BOTH when you touch `.c`:
  apps/mod/tbd-framework/...   <- the tree the shipping dedicated server actually loads
  apps/mod/tbd-export/...      <- the mirror, ASCII-ONLY, scanned as a hard gate

What the lockstep demands (xtask/src/gate_mod_compile.rs:641-711):
  * Same relative path in both trees.
  * The two copies must be identical after: comments stripped (line AND block), all whitespace collapsed,
    and 23 ASCII_FOLD substitutions applied. CODE AND STRING-LITERAL CONTENTS MUST MATCH EXACTLY.
  * The tbd-export copy must be PURE ASCII in every `Scripts/**/*.c`. WRITE ASCII IN BOTH.

Enfusion traps: `vanilla` is reserved; `Format` takes at most 9 parameters; method arguments may not be
strong refs; `FileHandle` members need `ref`; there is NO ternary operator; long boolean chains hit
"Formula too complex". A broken module kills the whole compile.

## NON-NEGOTIABLE RULES — every one was learned the hard way
1. VERIFY THE DEFECT STILL EXISTS ON MAIN BEFORE WRITING CODE. Compile a one-faction fixture and paste
   the padded opfor. If already fixed, STOP and report with the proving command.
2. PROVE NON-VACUITY BY PERTURBATION. Restore the pad; capture the RED output VERBATIM; restore, `touch`,
   green. PASTE THE RED OUTPUT IN YOUR REPORT.
3. `cargo test -p map-engine-core` ALWAYS with `--all-features`. Ad-hoc tests:
   `cargo xtask platform wave test --slice T-299 -p <pkg>`; cross-check `--list` total vs run total.
4. COMMIT EARLY AND OFTEN on the slice branch with explicit paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER
   `git stash`. Use `git -c filter.lfs.process= -c filter.lfs.required=false …` if git complains about LFS.
5. GATE: `cargo xtask platform wave gate --slice T-299` from inside the worktree; must end `SLICE GATE: PASS`.
   If you touch `.c`, also run `cargo xtask mod compile` and paste the verdict.
6. Never `cargo xtask ci ci-local`. Never `cargo xtask mk leptos-gates`. A test printing `skip:` is a FAIL.
   `cargo xtask ci schema-validate` does not work in worktrees (`cargo xtask schema validate` does).
   Ignore ticket verify lines that name `ci-local`, `leptos-gates`, or `ci schema-validate`.
7. File-length allowlists are never extended: new code goes in NEW files.
8. No `.py`, `.sh`, `.mjs` files committed. Throwaway probes go in /tmp.
9. Edition-2024 crates (map-engine-core, xtask): `rustfmt --edition 2024`. Hardcoding 2021 on xtask/core
   fails the wave gate (wave 246 paid this).
10. Class-R byte-parity pins must scrub the test module out of the haystack (`class_r_scrub::live_source`).
    Derived two-faction documents must stay byte-identical.
11. Do NOT spawn subagents. Do the work yourself.
12. You do NOT ship: no push, no merge, no ticket status edits, no docs/ or .ai/tickets/ edits, no filing tickets.
13. trunk serve (:3000) and the dev API (:8080) are running — LEAVE BOTH UP.
14. No silent deferrals. If a piece is genuinely blocked, STOP and report it precisely.
15. If you contradict this brief, assume you looked and the command center remembered — say it plainly with the command.
16. VALIDATE MEANING, NOT ONLY LAYOUT.
17. DEAD MECHANISMS ARE WORSE THAN MISSING ONES.
18. AN ANSWER ABOUT THE WORLD MUST CARRY THE WORLD IT WAS BUILT FOR.
19. Do NOT edit `xtask/src/schema_gates.rs`. You own `mission.schema.json` for the minItems change only.
20. You DO edit flatten.rs — that is the pad. Do not take the chance to "also" emit T-946.36 keys.

## YOUR REPORT — the deliverable, every field required
pwd_branch · defect_verified_on_main [{claim, path:line, command}] · changes [{path, line, why}] ·
perturbation {red_output VERBATIM, restored_green} · gate_verdict_tail (last 15 lines, must end SLICE GATE: PASS) ·
mod_compile_verdict (or "no .c touched") · files_outside_owns [] · found_not_fixed [{path:line, repro}] ·
deviations [] · commits [sha] ·
manual_checklist [one human-runnable line per in-game / in-browser behaviour you could not prove mechanically] ·
twins_confirmed [every .c path, both trees exist on disk]

Write the report to `.ai/artifacts/editor_briefs/sept2026/wave248/REPORT-T-299.md` in the worktree
(create the directory). Keep the slice branch committed and clean.
