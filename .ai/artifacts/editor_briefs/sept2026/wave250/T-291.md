You are the slice agent for ticket **T-291** (Five orphan schema fields) on the TBD-Reforger factory.
WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-291   (branch slice/T-291)

## ENVIRONMENT — THIS RUN (read before the first command)
You are on the **HOST** (Fedora, glibc 2.43). Native `cargo` / `rustfmt` work. Do **not** wrap commands in
`distrobox-host-exec` or `hcargo`.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
If you build a binary you then RUN, build THAT into CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-291 and delete it on cleanup.
A slice gate waits on the gate lock (`target/.tbd-gate.lock`) when a sibling is gating — that wait is normal.

MEASURED: shared cache can hand you another slice's test binary. `--list` mismatch is the tell; re-run in the private dir.
`packages/map-assets` LFS payloads are POINTER FILES in slice worktrees. Run `cargo xtask schema validate`, NOT `ci schema-validate`.

GATE: `wave gate --slice` is the CHEAP gate. Neither gate compiles Enfusion (T-946.17) — `cargo xtask mod compile` is yours if you touch `.c`.
Do NOT run `cargo test -p xtask` while `mod compile` runs (T-946.13).

**T-946.27 — copy before first `mod compile`:** 19 gitignored EnfusionMCP `.c` files. Copy, do not commit:
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/

FIRST THREE ACTIONS:
  1. `cd` to the worktree; `pwd && git branch --show-current` must be the worktree and `slice/T-291`.
  2. Copy EnfusionMCP if the count is not 19.
  3. Verify the defect still exists on this worktree.

## THE JOB
`environment.windDirDeg`, `factions[].color`, `roles[].radio`, `layers[]`, and `settings.{respawn,spectatorPolicy,nightVision}`
are declared and unused. spectatorPolicy is dead despite a shipped Spectator subsystem.

MUST: flatten emits `settings.spectatorPolicy`, `settings.nightVision`, `environment.windDirDeg`;
TBD_FrameworkManager.c and TBD_SpectatorController.c (BOTH trees) read the ones that belong to them;
windDirDeg applies in weather setup in FrameworkManager.

THEN: `factions[].color`, `roles[].radio`, `layers[]` are either emitted with a named reader you own,
or marked editor-only in flatten comments (do NOT invent T-290's emit-ledger table).

Ticket line numbers are stale (T-299/T-310 moved flatten). Locate live trim of spectatorPolicy yourself.

Do not implement T-936.4 weatherTimeline. Do not touch gadget flags.

Perturbation: trim spectatorPolicy again; emit test RED; restore, touch, green.

## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/mission/flatten.rs
apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_FrameworkManager.c
apps/mod/tbd-export/Scripts/Game/TBD/Gamemode/TBD_FrameworkManager.c
apps/mod/tbd-framework/Scripts/Game/TBD/Spectator/TBD_SpectatorController.c
apps/mod/tbd-export/Scripts/Game/TBD/Spectator/TBD_SpectatorController.c
Siblings (do NOT touch):
T-705: TBD_GadgetFlags.c (NEW), TBD_MissionLoader.c — both trees
T-936.4: schema, weather panel/core, TBD_WeatherRuntime.c — both trees

## THE ENFUSION LOCKSTEP
TWO mod trees; edit BOTH `.c` files:
  apps/mod/tbd-framework/...   (shipping server)
  apps/mod/tbd-export/...      (ASCII mirror)
New `.c` files need BOTH copies (`mirror_lockstep` walks export only).
JsonLoadContext allocates nested refs when the key is absent; `if (x.nested)` is always true. Presence is a sentinel or Count().

## NON-NEGOTIABLE RULES
1. Verify the defect on this tree before writing code.
2. Perturbation: break it, capture RED verbatim, restore, `touch`, green.
3. `cargo test -p map-engine-core` ALWAYS `--all-features`.
4. Commit with explicit paths. NEVER `git add -A` / `git add <dir>` / `git stash`.
5. `cargo xtask platform wave gate --slice T-291` from the worktree. Touch `.c` → also `mod compile`.
6. Never `ci-local`, never `leptos-gates`. `skip:` is a FAIL. Use `cargo xtask schema validate`.
7. File-length allowlists are never extended: new code goes in NEW files.
8. No committed `.py`/`.sh`/`.mjs`. Probes in /tmp.
9. rustfmt `--edition 2024` for xtask / map-engine-core; edition 2021 for frontend.
10. Do NOT spawn subagents. Do NOT ship, merge, push, or edit `.ai/tickets/` / docs.
11. Leave :3000 and :8080 up.
12. No silent deferrals. Dead mechanisms are worse than missing ones.
13. Do NOT edit `xtask/src/schema_gates.rs`. UNREAD FAIL after a real reader is expected; CC retires after merge.
14. Do NOT edit flatten.rs unless it is in YOUR FILES.
15. Do NOT edit packages/tbd-schema/**.

## YOUR REPORT — every field required
pwd_branch · defect_verified_on_main · changes · perturbation {red_output VERBATIM, restored_green} ·
gate_verdict_tail (must end SLICE GATE: PASS unless the brief says UNREAD FAIL is expected) ·
mod_compile_verdict · files_outside_owns [] · found_not_fixed · deviations · commits ·
manual_checklist · twins_confirmed

Write `.ai/artifacts/editor_briefs/sept2026/wave250/REPORT-T-291.md` in the worktree. Keep the slice branch committed and clean.
