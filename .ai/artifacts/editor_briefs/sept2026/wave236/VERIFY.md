You are the adversarial verifier for wave 236 of the TBD-Reforger factory. Your job is to find what the three slice agents and the command center got WRONG, not to confirm they were right. Do NOT fix. Do NOT commit. Do NOT file tickets. Leave main exactly as you found it (restore anything you mutate; `git status --porcelain` must be empty when you finish). No subagents. Token economy: read only what you attack; ranges for files over 200 lines.

Repo: /run/media/system/Disk_2/Projects/TBD-Reforger, branch main (do not create branches).

ENVIRONMENT. You are in the `claude-desktop` container (glibc 2.36); the shared cache is host-stamped (glibc 2.43), so run EVERY cargo/rustfmt command on the host through the wrappers, which pass cwd, stdin and exit code through:
  /home/Samuel/.cache/tbd-bin/hcargo <args>     /home/Samuel/.cache/tbd-bin/hrustfmt <args>
`podman` is host-only: `distrobox-host-exec podman exec tbd_reforger_db psql -U tbd -d postgres -c '<SQL>'`.
Integration tests need a DB: create `tbd_verify_w236_it`, use `TEST_DATABASE_URL=postgres://tbd:tbd@localhost:5434/tbd_verify_w236_it?sslmode=disable`, drop it when done. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL.
MEASURED THIS RUN: the shared cache can hand you ANOTHER checkout's test binary (open ticket T-300) — a `--list` showing tests your tree does not contain is the tell. And `packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask map tests fail there with `bad magic [118, 101, 114, 115]`; that is environmental, not a finding.

WAVE 236. Base marker `b6a3cfd89` (wave 235 CLOSED). Landed on main:
- `adc3eed7f` T-305 — pak entry offsets are absolute; both seeks in `tools/tbd-tools/src/world/pak.rs` drop the `data_start +` term, new `entry_data_start()` diagnostic, new synthetic-pak test module.
- `e754bcc9c` T-298 — `tools/tbd-tools/src/density.rs` seeded randomized partition test; `.github/workflows/ci.yml` new step; `xtask/src/mk_ci_tasks.rs` new `tbd-tools-test` task row (outside the ticket's owns, justified because `verify ci-shell` forbids a bare cargo `run:`).
- `b8b6804da` T-943 — `xtask/src/wave/push.rs`: `feed_and_capture` writer thread, `git lfs version` probe, `push_plan`.
Command-center code landed in the same span (`7f97c043c`, `4d80db331`, T-946): the wave-close repair — `wave::ledger::Registry::load_repo` now loads the typed corpus, `wave_lock::carry_emptied` re-seats pending labels via `base::max_close_claim`, `root::test_repo_root` resolves fixture roots under `wave::testcwd`'s lock, `ticket ship --no-repack` plus `check::require_check_ok_deferring_repack`, and `wave --close --tickets`. Attack this exactly as hard as the slices — it is unreviewed by anyone else.

Highest-risk claims to attack:
1. T-305. (a) The fix rests on "entry offsets are absolute" measured on 300 entries by T-206. Does `tools/tbd-tools/src/world/pak.rs` still parse `data_start` from a header field that some pak variant might need? Read `xtask/src/map_blueprint/pak.rs` (the OTHER reader, already absolute) and diff the two parsers for a case where they disagree. (b) The synthetic fixture builds the pak the test author believed in — compare its layout against a REAL shipped pak's first 128 bytes (`~/.cache/enfusion-mcp-root/addons/*.pak`), and say whether a fixture that agrees with the code but not the format would still pass. (c) The agent reported `apps/mod/vanilla_reference/Scripts` on disk is 2,483 rotated `.c` files produced by the old reader, and that `enf index` reads that tree by default. Verify both, and whether anything COMMITTED depends on that corrupt output.
2. T-298. (a) `density.rs` inlines a SplitMix64 `Rng`; is the sweep deterministic across platforms and does the seed actually vary the grids (print a few)? (b) Does the new test have red power over the PRODUCTION path, not just its own oracle? The agent claims perturbation B (a stride change) turned it red while the pre-existing `corner_partition_identity` stayed green — reproduce that. (c) `cargo xtask ci tbd-tools-test` must exist and run the same command ci.yml names; check `verify ci-shell` passes and that the step's YAML quoting survives. (d) The agent left `ci-local` WITHOUT the new lane (its deviation 5) — is `ci-local` now a weaker gate than ci.yml, and does anything claim they mirror each other?
3. T-943. (a) `feed_and_capture` uses `std::thread::scope`; on a spawn failure or a child that exits early, can the writer thread block forever on a full pipe and hang the scope? Construct that case. (b) The guard is SKIPPED entirely when `git lfs version` succeeds — so on a machine with git-lfs the per-commit `.gitattributes` evaluation no longer runs at all. Is that safe, or does it lose a protection T-599/T-600 added for a reason other than the missing binary? (c) The agent asserts the refusal text is byte-identical; diff the literals yourself. (d) The probe answers ABSENT under the factory's pinned PATH (Homebrew git-lfs is only on the interactive PATH) — so which branch actually runs during a real `platform wave push` here, and does the mode line tell the truth?
4. T-946 (command center). (a) `carry_emptied` now relabels pending entries to `ledger_floor + 1 + i`. Can that ever collide with an OPEN wave label, or re-issue a number some marker already claimed? Try a tree with two pending entries and a disavowed marker. (b) `require_check_ok_deferring_repack` waives every error containing ``run `cargo xtask wave repack` `` — find a check error that carries that phrase but is NOT fixed by a repack, or show none exists. (c) `root::test_repo_root` takes `wave::testcwd`'s mutex, which is NOT reentrant and is also held by `CwdGuard`. Find a test that holds a `CwdGuard` and reaches a fixture helper — that is a deadlock, and the suite passing does not prove absence. (d) `Registry::load_repo` now fails closed when `Corpus::load` errors, where the old path could still load a JSON monolith — is there a tree shape where close/land now refuses that previously worked?
5. Bookkeeping honesty. `.ai/tickets/T-305.toml`, `T-298.toml`, `T-943.toml` must each read `status = "shipped"` with a `shipped_at` sha that is really the landing merge (`adc3eed7f`, `e754bcc9c`, `b8b6804da`). `cargo xtask ticket check` must be exit 0. And `wave.lock` must describe the wave that was actually run (3 per wave, wave 236 = these three).
6. Gate vacuity. Take the wave gate's own step list and pick the three steps most likely to pass without examining the new code; prove for each whether it really ran over it. `--list` versus run totals for every crate you test.

Severity table (also the command center's triage table):
- BLOCKER: main is broken, data at risk, or a gate reported success on code it never examined.
- MAJOR: a shipped ticket does not do what it claims, or can destroy operator-authored work.
- MINOR / NIT: everything else.

Report (≤ 60 lines): findings first, each with severity, file:line, the exact command + verbatim output that proves it, and the fix shape (one line). Then an explicit list of what you attacked and FAILED to break. Then confirm main is untouched (`git status --porcelain` empty, HEAD sha).
