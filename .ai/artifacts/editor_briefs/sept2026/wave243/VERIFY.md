You are the adversarial verifier for wave 243 of the TBD-Reforger factory. Your job is to find what the three slice agents and the command centre got WRONG, not to confirm they were right. Do NOT fix. Do NOT commit. Do NOT file tickets. Leave main exactly as you found it (restore anything you mutate; `git status --porcelain` must be empty when you finish). No subagents. Token economy: read only what you attack; ranges for files over 200 lines.

Repo: /run/media/system/Disk_2/Projects/TBD-Reforger, branch main (do not create branches).

ENVIRONMENT. You are in the `claude-desktop` container (glibc 2.36); the shared cache is host-stamped (glibc 2.43), so run EVERY cargo/rustfmt command on the host through the wrappers, which pass cwd, stdin and exit code through:
  /home/Samuel/.cache/tbd-bin/hcargo <args>     /home/Samuel/.cache/tbd-bin/hrustfmt <args>
`podman` is host-only: `distrobox-host-exec podman exec tbd_reforger_db psql -U tbd -d postgres -c '<SQL>'`.
Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL. `map-engine-core` is ALWAYS `--all-features`.
Do NOT run `cargo test -p xtask` while `cargo xtask mod compile` is running — they share `~/.local/share/tbd-server-addons` and two T-878 tests fail under contention (T-946.13).
KNOWN AND ALREADY FILED — do not re-report: shared cache can serve a stale artifact (T-979); `dem::peaks` fails on an LFS-pointer DEM in a worktree but must PASS on main (T-972); `v-suite verify` fails 22/25 on drifted oracles (T-986); frontend wasm clippy pre-existing errors (T-987); `verify file-length` has 11 pre-existing SIZE-3 violations; the editor renders no forest regions (T-995); `mirror_lockstep` cannot see a framework-only NEW script, and fails in every slice worktree over 19 untracked EnfusionMCP export scripts (both filed this wave).

THE SPAN. Base marker `54153870d` (wave 242 CLOSED). Landed:
- `66b92edec` T-936.1 — win conditions: `mission.schema.json` + `mission-editor-payload.schema.json`, new `mission/{extensions,win_conditions}.rs`, `mission/{flatten,compile,mod}.rs`, new `panels/win_conditions_card.rs`, `panels/mod.rs`, new `TBD_WinConditionEvaluator.c` in BOTH mod trees.
- `34bdfe4c2` T-702 — whole-terrain zone: `panels/zones_panel.rs`, `state/operations/entity.rs`, AND `crates/map-engine-core/src/doc/store.rs` (OUTSIDE its owns).
- `c4db99648` T-675.2 — vehicles[] roster reader + crew seating: `TBD_MissionLoader.c`, `TBD_SpawnManager.c`, new `TBD_MissionVehicleStruct.c`, in BOTH mod trees.

COMMAND-CENTRE work in the span, and attack it hardest of all:
- `d9bf95230` — retired the `vehicles` UNREAD_WIRE_FIELDS row (baseline 0), RE-PINNED `seats` 8 -> 12, rewrote two `mission.schema.json` descriptions, and MOUNTED the win-conditions card in `panels/settings_modal.rs` (a file no slice owned).
- `fe1b1d4da` — shipped/stamped all three; HAND-STAMPED `created_at` onto T-675.2 and T-702, which had none, deriving the date from each ticket file's git birth.

Highest-risk claims to attack, in order:

1. **T-702's owns widening — the one place this wave left its lane.** (a) `add_polygon_zone` now delegates to `add_polygon_zone_labelled(.., None)`. Prove the draw tool's wire shape is BYTE-IDENTICAL to before `c9bf3eb31` — a `label` key that appears where none did, or key ORDER changing, is a silent contract break. (b) The claim is "one gesture = ONE undo step". Find a path where the labelled create still produces two, or where undo leaves a partial zone. (c) `terrain_rect_ring` refuses bounds that disagree with the terrain — find a caller that can reach the create WITHOUT that guard.
2. **T-675.2's double-spawn guard.** (a) It claims `uid` exact-match first, then an `alias|x|z` fingerprint. Construct an input where a crewed vehicle spawns TWICE, or where a legitimately-distinct second vehicle is wrongly claimed and never spawns: two roster rows with the same alias at the same x/z; a roster row whose uid matches an entities row of a DIFFERENT prefab; an entities twin that FAILED to spawn but was still recorded. (b) `ResetIndex()` is called at the top of `SpawnMissionEntities` before its early return — prove a reload cannot inherit a stale index. (c) The 1000 ms `CallLater` seat verification: what happens if the mission ends, the vehicle is destroyed, or the body is deleted inside that window? Find the dangling handle. (d) A rosterless mission must behave EXACTLY as before.
3. **T-936.1's parity and enum claims.** (a) "A mission without a winConditions block flattens byte-identically to today" — attack the serde-flattened `ExtensionBlocks`: an empty object emitted where nothing was, or key order shifting. (b) The compiled `mode` enum has SEVEN values (two grandfathered) while the editor is pinned to FIVE. Prove the editor genuinely cannot author the two grandfathered values, and that the lockstep test reading both enums is not vacuous. (c) `mode: timeout` projects onto `flow.timeLimitSeconds` at compile time — prove no SECOND timer exists and that an authored `flow.timeLimitSeconds` and a `timeout` mode cannot disagree. (d) `DIAG_WIN_CONDITIONS` is one id for four situations — is any of the four unreachable?
4. **The command centre, hardest of all.** (a) Is `seats` expected 12 the TRUTH? Count the identifiers yourself. If the real number is higher, I masked a reader; if lower, the pin is loose. (b) Does anything else in the tree still assert `vehicles` is reader-free now that its row is gone — a doc, a test, a comment? (c) The card mount: prove `win_conditions_card(ctrl)` actually RENDERS in the settings modal and is not inert behind a cfg or a guard — an unmounted-then-badly-mounted card is the same defect twice. (d) The hand-stamped `created_at` values: verify each against the git birth of its ticket file, and say whether treating them as fact rather than `estimated[]` matches what T-674.2/T-675.1 actually carry.
5. **Gate honesty.** (a) Prove the wave gate read ALL THREE slices' code, not a subset — `--list` vs run totals for every crate you test. (b) The gate self-mints `tbd_gate_w<N>` databases and IGNORES an operator `TBD_GATE_DB`; confirm the per-test DBs for THIS run are fresh and hold real schema, and that no step passed over an absent database. (c) `cargo xtask mod compile` reports 5742 files / 11331 classes against a 5740 / 11314 baseline. Confirm both new shared scripts are inside that count and that the FRAMEWORK copies are the ones compiled.

Severity table (also the command center's triage table):
- BLOCKER: main is broken, data at risk, or a gate reported success on code it never examined.
- MAJOR: a shipped ticket does not do what it claims, or can destroy operator-authored work.
- MINOR / NIT: everything else.

Report (<= 60 lines): findings first, each with severity, file:line, the exact command + verbatim output that proves it, and the fix shape (one line). Then an explicit list of what you attacked and FAILED to break. Then confirm main is untouched (`git status --porcelain` empty, HEAD sha).
