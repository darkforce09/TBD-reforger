You are the slice agent for ticket **T-675.2** (T-675 Enfusion reader — vehicle roster and crew seating) on the TBD-Reforger factory.
WORKTREE — work ONLY here, never in the main checkout:
  /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-675.2   (branch slice/T-675.2)

## ENVIRONMENT — THIS RUN (read before the first command)
You are inside the `claude-desktop` container (Debian, glibc 2.36). The shared warm cache
/home/Samuel/.cache/tbd-target is stamped for the HOST toolchain (Fedora, glibc 2.43). Running cargo in the
container against it is the GLIBC_2.xx-not-found trap (T-853). So EVERY cargo / rustfmt command runs on the
host through these wrappers (they pass cwd, stdin and exit code through):
   /home/Samuel/.cache/tbd-bin/hcargo <cargo args>
   /home/Samuel/.cache/tbd-bin/hrustfmt <args>
Wherever this brief or the ticket says `cargo xtask ...`, run `hcargo xtask ...`. `hcargo` forwards CARGO_TARGET_DIR
(default the shared cache), CARGO_BUILD_JOBS=4, TEST_DATABASE_URL and TBD_IT_BASE_DB from your shell.
Never run bare `cargo`; never build into a worktree-local target/. git, grep, sed and file edits run in the
container as normal. A slice gate waits on the gate lock (`target/.tbd-gate.lock`, polls every 30 s, up to
3600 s) when a sibling is gating — that wait is normal, not a hang.

MEASURED THIS RUN, and it will bite you: the shared cache can hand you ANOTHER SLICE'S test binary (this is
T-300, open). A `--list` that shows tests your worktree does not contain is the tell — cross-check
`grep -c '#\[test\]'` against the module you are testing. When it happens, re-run in a private
`CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-SLICEID` and delete it on cleanup.
Also measured: `packages/map-assets` LFS payloads are POINTER FILES in slice worktrees, so ~7 xtask
`map_blueprint::*` / `map_world_los::*` tests fail there with `bad magic [118, 101, 114, 115]` ("vers").
That is pre-existing and environmental — do not chase it, and do not claim it as your red. In this worktree
run `hcargo xtask schema validate`, NOT `hcargo xtask ci schema-validate` (the latter does not work here).

GATE SCOPE: `wave gate --slice` is the CHEAP gate — `cargo check`, `wasm32 (frontend)`, `fmt (changed)`,
`clippy (changed crates)`, `schema` and the pin gates. The `trunk build` and the four `test *` steps are
WAVE-level and will NOT run on your slice. NEITHER GATE COMPILES ENFUSION (T-946.17) — if you touch a `.c`
file, `hcargo xtask mod compile` is yours to run and to paste.

FIRST THREE ACTIONS, before anything else:
  1. `cd` there, run `pwd && git branch --show-current`, paste the output in your report. If it does not show
     the worktree path and slice/T-675.2, STOP and report — do not start work.
  2. `export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target`. If you build a binary you then RUN, build THAT into
     `CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-675.2` and delete it on cleanup.
  3. Read the full ticket + plan + spec — they are the handoff, there is no prior chat:
       cat .ai/tickets/T-675.2.toml
       docs/plans/t-675_2_plan.md
       docs/specs/existing/t675_vehicle_roster_wire.md   -> section "## Claude Code prompt — T-675.2" (:31-56)
     Then read CLAUDE.md HARD GATE and docs/platform/PLATFORM_FACTORY.md 'THE SIGNATURE DEFECT' and 'Known traps'.

## NOTE FROM THE COMMAND CENTER — READ TWICE
You write ENFUSION SCRIPT (`.c`), not Rust. Your gate is `hcargo xtask mod compile`. Baseline on main:
`OK: compiled clean` — get that verdict before you change anything, and paste both runs.

  * **THE DOUBLE-SPAWN TRAP IS THE WHOLE DIFFICULTY OF THIS SLICE.** One authored vehicle emits TWO rows:
    an `entities[]` alias row AND a `vehicles[]` roster row (crates/map-engine-core/src/mission/flatten.rs:78-83).
    `TBD_MissionLoader.SpawnMissionEntities()` (:597-655) ALREADY SPAWNS THE `entities[]` ROW. A naive roster
    reader spawns every crewed vehicle TWICE. `uid` is the join key and is carried on BOTH rows since T-946.18
    (`ModEntity::uid`, flatten.rs:32-48; `ModVehicle.uid`, flatten.rs:84-121). Write the guard, and write a
    test or a logged census that PROVES one authored vehicle produces exactly one spawned vehicle.
  * The wire you are reading (T-675.1, `c32fdd0cd`): `ModVehicle` = alias(req), uid, x, z, headingDeg(always),
    faction, seats, inventory; `ModVehicleSeat` = slotId(always — references `slots[].uid`, NEVER the derived
    `slots[].id`), role(closed enum of 7: driver commander gunner cargo pilot copilot turret), index.
    A committed payload to read against: packages/tbd-schema/golden-missions/schema-1_3-wire-fields.json:158-175.
  * T-674.2 (`757f9e3fa`) landed the identity binding on YOUR TWO SHARED FILES one wave ago. Read its diff
    (`git show 757f9e3fa`) before you edit — it is the shape your edit should match, and `68fe563cf` is the
    precedent for touching both trees in one commit.
  * Seating: there is NO precedent in the mod. `SCR_CompartmentAccessComponent` / `BaseCompartmentManagerComponent`
    exist only under apps/mod/vanilla_reference/Scripts/Game/generated/Vehicle/ — read them there, do not guess.
    Hook point: after the body loop closes (TBD_SpawnManager.c:1012) and before the settle arm (:1029).
    Seat resolution: `seats[].slotId` -> TBD_MissionLoader.GetSlotById() (:386) or match on slot.uid/Key()
    (TBD_MissionSlotStruct.c:97-102) -> `m_mSlotBodies.Get(key)` (:758).
  * `TBD_SpawnManager.c` is 3503 lines. Prefer putting new logic in `TBD_MissionVehicleStruct.c` and keeping
    SpawnManager to call-site lines.
  * EXPECTED GATE FAILURE, AND IT IS NOT YOURS TO FIX: binding `vehicles` and `seats` trips
    `UNREAD_WIRE_FIELDS` in xtask/src/schema_gates.rs (`vehicles` expected 0 at :2487, `seats` expected 8 at
    :2494). That file is NOT yours. Report the failure with its exact output; the command center retires the
    rows after landing, exactly as it did in `0b9c05c8b` for T-674.2. Do not edit schema_gates.rs or
    packages/tbd-schema.
  * IN-GAME BEHAVIOUR CANNOT BE PROVEN HERE. Every behaviour you cannot prove mechanically goes in
    `manual_checklist` as ONE human-runnable line. An empty list from this slice would be a lie.

## THE TICKET
Reader half of T-675: the compiled `vehicles[]` roster must reach the mod and drive crew spawn from authored seats.
Main goal: read the compiled vehicles[] roster in the mod and spawn crew from authored seats.

Requirements:
  - TBD_MissionLoader binds vehicles[] into a roster struct (new TBD_MissionVehicleStruct.c); TBD_SpawnManager
    spawns each vehicle and seats the referenced slots.
  - An unknown prefab or unresolved slot ref logs and skips that row; no packages/tbd-schema edits.

Done means:
  - A mission with an authored roster spawns its vehicles with crew seated as authored; rosterless missions
    spawn exactly as before. Headless compile clean; in-game seating verification sits on the human checklist.

## YOUR FILES (touch ONLY these; new files must have exactly these names)
apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionLoader.c
apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c
apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionVehicleStruct.c      (NEW)
apps/mod/tbd-export/Scripts/Game/TBD/Backend/TBD_MissionLoader.c
apps/mod/tbd-export/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c
apps/mod/tbd-export/Scripts/Game/TBD/Backend/TBD_MissionVehicleStruct.c         (NEW)
The export twins are MANDATORY — see the lockstep section. If the work genuinely requires a file outside this
list, STOP and report which file and why — do not widen silently.
Siblings running right now own (do NOT touch):
T-936.1: packages/tbd-schema/schema/{mission,mission-editor-payload}.schema.json,
  crates/map-engine-core/src/mission/{flatten,compile,extensions,win_conditions,mod}.rs,
  apps/website/frontend/src/editor/panels/{win_conditions_card,mod}.rs,
  Scripts/Game/TBD/Gamemode/TBD_WinConditionEvaluator.c (both trees)
T-702: apps/website/frontend/src/editor/panels/zones_panel.rs,
  apps/website/frontend/src/editor/state/operations/entity.rs

## THE ENFUSION LOCKSTEP — NEW SINCE WAVE 242, AND IT WILL REFUSE A ONE-SIDED EDIT
There are TWO mod trees and you must edit BOTH:
  apps/mod/tbd-framework/...   <- the tree the shipping dedicated server actually loads
  apps/mod/tbd-export/...      <- the mirror, ASCII-ONLY, scanned as a hard gate

Until wave 242 the gate compiled tbd-EXPORT's copy of all 139 shared scripts and NEVER READ tbd-framework
(T-946.23): the Enfusion VFS overlays addons by path and the last one wins, and the order was backwards.
It is `TBD_Export,TBD_Framework` now, so the framework copies are the ones the engine compiles, and the
export mirrors it no longer compiles are held by `mirror_lockstep` (xtask/src/gate_mod_compile.rs:641-711).
T-946.24 then closed four holes the verifier found in that fix.

What the lockstep demands (gate_mod_compile.rs):
  * Same relative path in both trees.
  * The two copies must be identical after: comments stripped (line AND block), all whitespace collapsed,
    and 23 ASCII_FOLD substitutions applied (em/en dash -> `-`, curly quotes, `...`, `->`, `<-`, `<=`, `>=`,
    degree, `!=`, and so on: ASCII_FOLD at :533-557). CODE AND STRING-LITERAL CONTENTS MUST MATCH EXACTLY —
    a differing resource GUID inside quotes fails, and that is deliberate (T-946.24).
  * The tbd-export copy must be PURE ASCII in every `Scripts/**/*.c`. tbd-framework is exempt from the scan,
    but WRITE ASCII IN BOTH ANYWAY: Workbench hard-rejects non-ASCII punctuation.

THE HOLE YOU MUST NOT FALL INTO: `mirror_lockstep` walks the EXPORT tree only (:671). A brand-new script
created in tbd-framework with no export twin is INVISIBLE to the gate — it passes, and it is still wrong.
Live precedent: TBD_TriggerRuntime.c is framework-only today. If you add a new `.c`, ADD BOTH COPIES.

Enfusion traps that have cost days: `vanilla` is reserved; `Format` takes at most 9 parameters; method
arguments may not be strong refs; `FileHandle` members need `ref`; there is NO ternary operator; long
boolean chains hit "Formula too complex". A broken module kills the whole compile.

THE ALLOCATE-ON-ABSENT LANDMINE, measured on a live world boot and documented three times
(TBD_MissionLoader.c:31-42, :219-243; TBD_SpawnManager.c:1266-1286): `JsonLoadContext.ReadValue` ALLOCATES a
nested `ref <class>` field EVEN WHEN THE JSON KEY IS ABSENT. So `if (x.nested)` is ALWAYS TRUE and is NOT a
presence test. Presence must be a scalar sentinel (the `Y_ABSENT = -1000000` idiom,
TBD_MissionSlotStruct.c:57) or a container `Count()`.

Binding pattern: one `JsonLoadContext` pass, `ctx.ReadValue("", s_Mission)` (TBD_MissionLoader.c:990-998),
which binds BY CLASS MEMBER NAME — the Enfusion field name IS the JSON key. Plain classes, public fields,
no macro. Read TBD_MissionSlotStruct.c:47-136 end to end as the template before you write a struct.

DO NOT run `cargo test -p xtask` while `mod compile` is running — they share ~/.local/share/tbd-server-addons
and two T-878 tests fail under contention (T-946.13).

## NON-NEGOTIABLE RULES — every one was learned the hard way
1. VERIFY THE DEFECT STILL EXISTS ON MAIN BEFORE WRITING CODE. If already fixed, STOP and report with the proving command.
2. PROVE NON-VACUITY BY PERTURBATION. For every test you write: break the thing it guards, capture the RED output
   VERBATIM, restore, `touch` the restored file (a git checkout restore does not re-trigger a cargo rebuild), re-run
   green. PASTE THE RED OUTPUT IN YOUR REPORT. Perturb the PRODUCTION side where you can, not only the test's oracle.
3. `cargo test -p map-engine-core` ALWAYS with `--all-features` (without it feature-gated modules never compile — vacuous pass).
   Cross-check `--list` total vs run total.
4. COMMIT EARLY AND OFTEN on the slice branch with explicit paths. NEVER `git add -A`, NEVER `git add <dir>`, NEVER
   `git stash` (deletes LFS pointer files). Use `git -c filter.lfs.process= -c filter.lfs.required=false ...` if git
   complains about LFS. Leave the tree COMMITTED AND CLEAN whenever you pause or finish.
5. GATE: `hcargo xtask platform wave gate --slice <ID>` from inside the worktree; must end `SLICE GATE: PASS`.
   "REFUSING to pass — resolved to NO crate" / "rustfmt was invoked ZERO times" means your diff has no lintable Rust,
   not that your code is broken.
6. Never `cargo xtask ci ci-local`. A test printing `skip:` is a FAIL.
7. File-length allowlists (`cargo xtask verify file-length`) are never extended: new code goes in NEW files; the
   allowlisted giants grow only by call-site lines. If a file you own would cross the limit, STOP and report the count.
8. No `.py`, `.sh`, `.mjs` files committed (hard gates). Throwaway probes go in /tmp, never the source tree.
9. Edition per crate: read the crate's Cargo.toml and format with that edition (`hrustfmt --edition <n>`). Do not
   trust a brief that names an edition. `apps/website/frontend` is 2021; `crates/map-engine-core` and
   `tools/tbd-tools` are 2024. The two editions order a `use` group differently, so a mismatch shows up as a
   one-line `fmt (changed) FAIL`.
10. Class-R byte-parity pins must scrub the test module out of the haystack (`class_r_scrub::live_source`).
11. Do NOT spawn subagents. Do the work yourself.
12. You do NOT ship: no push, no merge, no ticket status edits, no docs/ or .ai/tickets/ edits, no filing tickets.
    Report findings; the command center files.
13. trunk serve (:3000) and the dev API (:8080) are running — LEAVE BOTH UP.
14. No silent deferrals: the ticket's whole acceptance is the deliverable. If a piece is genuinely blocked, STOP and
    report it precisely — do not write "follow-up".
15. If you contradict this brief, assume you looked and the command center remembered — say it plainly with the command.
16. VALIDATE MEANING, NOT ONLY LAYOUT. If you read a binary archive or a wire block, check its `schema_version` as
    well as that it parses. rkyv proves the buffer is readable, not that your build agrees with the writer about what
    the fields mean.
17. DEAD MECHANISMS ARE WORSE THAN MISSING ONES. Before you add a fast path, prove it can fire. A wave-238 slice
    added a prefetch fold that ran AFTER the await it was meant to overlap. Write the test that shows the saving,
    or do not add the path.
18. AN ANSWER ABOUT THE WORLD MUST CARRY THE WORLD IT WAS BUILT FOR. If your code answers a spatial or temporal
    question, put the extent/version it was built for inside the payload and refuse a caller that disagrees.

## YOUR REPORT — the deliverable, every field required
pwd_branch · defect_verified_on_main [{claim, path:line, command}] · changes [{path, line, why}] ·
perturbation {red_output VERBATIM, restored_green} · gate_verdict_tail (last 15 lines, must end SLICE GATE: PASS) ·
files_outside_owns [] · found_not_fixed [{path:line, repro}] · deviations [] · commits [sha] ·
manual_checklist [one human-runnable line per in-game / in-browser behaviour you could not prove mechanically]
