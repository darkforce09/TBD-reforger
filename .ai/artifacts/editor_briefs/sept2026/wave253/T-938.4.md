You are the slice agent for **T-938.4** (Section cut via BVH and sparse HeightField).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-938.4  branch slice/T-938.4
## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Private run dir: /home/Samuel/.cache/tbd-target-T-938.4 (delete on cleanup).
Worktree LFS pointers: `cargo xtask schema validate` not `ci schema-validate`.
FIRST: cd worktree; pwd && git branch --show-current must be slice/T-938.4. Then prove the defect.
## THE JOB
`section_at_owned` (building_section.rs:~292) walks every triangle. New `building_section_index.rs` (register in lib.rs): y-interval query over existing BVH returning candidate triangle ids. building_section.rs uses the index; cut polygon equals brute-force on the six golden buildings.
HeightField (~:46, :66-83) becomes sparse tiled grid: f32 + NaN sentinel, tiles allocated on first write; :46 cap stays; empty field allocates no plan cells.
Verify: count triangles visited per cut and bytes allocated per HeightField on a golden — paste the numbers.
Perturbation: collapse y-interval to zero height → golden equality test RED; restore, touch, green.
## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/building_section.rs
crates/map-engine-core/src/building_section_index.rs
crates/map-engine-core/src/lib.rs
## SIBLINGS THIS PACK (do NOT touch)
T-936.6 mission/* · T-941.4 · T-941.8 · T-139 · T-937.2 store.rs · T-937.4 · T-930 slots_gpu · T-938.2 · T-938.3
## RULES
1. Verify defect first (visit/byte counts). 2. Perturbation RED verbatim, restore, touch, green.
3. `cargo test -p map-engine-core --all-features` for the goldens. 4. Explicit git paths. NEVER git add -A / stash.
5. `cargo xtask platform wave gate --slice T-938.4` from the worktree.
6. No ci-local, no leptos-gates. skip: is FAIL. rustfmt --edition 2024 map-engine-core.
7. No subagents. No ship/merge/push. Leave :3000 :8080 up. No silent deferrals. Do not edit flatten/schema.
## REPORT
pwd_branch · defect_verified (visit/byte numbers) · changes · perturbation · gate_verdict_tail · files_outside_owns [] · found_not_fixed · commits
Write `.ai/artifacts/editor_briefs/sept2026/wave253/REPORT-T-938.4.md` in the worktree. Branch committed and clean. No push.
