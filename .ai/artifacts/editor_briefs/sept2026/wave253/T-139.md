You are the slice agent for **T-139** (Lobby loadout visual preview).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-139  branch slice/T-139
## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Private run dir if you RUN a test binary: /home/Samuel/.cache/tbd-target-T-139 (delete on cleanup).
Gate lock wait is normal. Worktree LFS pointers: `cargo xtask schema validate` not `ci schema-validate`.
Touch `.c` → `cargo xtask mod compile`. Do NOT `cargo test -p xtask` during mod compile.
Copy 19 EnfusionMCP `.c` before first compile (already copied if count==19).
FIRST: cd worktree; pwd && git branch --show-current must be slice/T-139. Then prove the defect.
## THE JOB
No in-game lobby kit preview. New TBD_LoadoutPreview widget: icon grid from the selected slot's 13 gear fields (registry icon path; skip empty). Layout under UI/layouts hosted by TBD_LobbyScreen beside the slot list; updates on slot selection. No network — read already-replicated lobby data. Log `[TBD][LoadoutPreview] slot=<n> items=<k>` on each refresh.
BOTH trees for new `.c`/`.layout` and LobbyScreen. Do NOT edit TBD_UILayouts.c — HUD-local ResourceName + CreateWidgets / TBD_UILayouts.Create fallback like T-941.3.
Perturbation: missing widget name → mod compile RED; restore, touch, green. `mod world-boot` if available.
## YOUR FILES (touch ONLY these)
apps/mod/tbd-framework/Scripts/Game/TBD/UI/Lobby/TBD_LoadoutPreview.c
apps/mod/tbd-export/Scripts/Game/TBD/UI/Lobby/TBD_LoadoutPreview.c
apps/mod/tbd-framework/Scripts/Game/TBD/UI/Lobby/TBD_LobbyScreen.c
apps/mod/tbd-export/Scripts/Game/TBD/UI/Lobby/TBD_LobbyScreen.c
apps/mod/tbd-framework/UI/layouts/TBD_LoadoutPreview.layout
apps/mod/tbd-export/UI/layouts/TBD_LoadoutPreview.layout
## SIBLINGS THIS PACK (do NOT touch)
T-936.6 spawnModules · T-941.4 ObjectiveHud · T-941.8 SpawnManager · T-937.2 undo_groups · T-937.4 persist · T-930 slots_gpu · T-938.2 world_host · T-938.3 icon cull · T-938.4 building_section
## ENFUSION LOCKSTEP
Edit BOTH trees. New `.c`/`.layout` need BOTH copies.
## RULES
1. Verify defect first. 2. Perturbation RED verbatim, restore, touch, green.
3. Explicit git paths. NEVER git add -A / stash. 4. `cargo xtask platform wave gate --slice T-139` from the worktree.
5. No ci-local, no leptos-gates. skip: is FAIL. schema validate.
6. No subagents. No ship/merge/push. No `.ai/tickets/` / docs. Leave :3000 :8080 up. No silent deferrals.
## REPORT
pwd_branch · defect_verified · changes · perturbation · gate_verdict_tail · mod_compile_verdict · files_outside_owns [] · found_not_fixed · commits · twins_confirmed
Write `.ai/artifacts/editor_briefs/sept2026/wave253/REPORT-T-139.md` in the worktree. Branch committed and clean. No push.
