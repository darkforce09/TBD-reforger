You are the slice agent for **T-938.3** (GPU cull for all icon lanes).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-938.3  branch slice/T-938.3
## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Private run dir: /home/Samuel/.cache/tbd-target-T-938.3 (delete on cleanup).
Worktree LFS pointers: `cargo xtask schema validate` not `ci schema-validate`.
FIRST: cd worktree; pwd && git branch --show-current must be slice/T-938.3. Then prove the defect.
## THE JOB
Compute cull exists for WorldTrees (engine.rs ~1812-1816) only. Extend to every icon lane through the same gate. CPU `count_icons_in_frustum` (icon_cull_gpu.rs:~226) runs only when debug HUD flag is on; otherwise visible count from GPU readback. shader.wgsl:~315: workgroup-local counter reduced once per workgroup, then one atomicAdd per workgroup.
Test: GPU visible counts equal CPU reference on a fixture frustum.
Verify: CPU count still runs with debug flag off — paste RED.
Perturbation: remove workgroup barrier before reduce → count-equality test RED; restore, touch, green.
T-938.1 already pooled slot/cluster buffers in engine.rs — do not revert that; extend cull only.
## YOUR FILES (touch ONLY these)
crates/map-engine-render/src/engine.rs
crates/map-engine-render/src/icon_cull_gpu.rs
crates/map-engine-render/src/shader.wgsl
## SIBLINGS THIS PACK (do NOT touch)
T-936.6 · T-941.4 · T-941.8 · T-139 · T-937.2 · T-937.4 · T-930 · T-938.2 world_host · T-938.4
## RULES
1. Verify defect first. 2. Perturbation RED verbatim, restore, touch, green.
3. `cargo test -p map-engine-render`. 4. Explicit git paths. NEVER git add -A / stash.
5. `cargo xtask platform wave gate --slice T-938.3` from the worktree.
6. No ci-local, no leptos-gates. skip: is FAIL.
7. No subagents. No ship/merge/push. Leave :3000 :8080 up. No silent deferrals.
## REPORT
pwd_branch · defect_verified · changes · perturbation · gate_verdict_tail · files_outside_owns [] · found_not_fixed · commits
Write `.ai/artifacts/editor_briefs/sept2026/wave253/REPORT-T-938.3.md` in the worktree. Branch committed and clean. No push.
