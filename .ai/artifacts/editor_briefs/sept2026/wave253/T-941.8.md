You are the slice agent for **T-941.8** (Vehicles spawn fuelled with inventory).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-941.8  branch slice/T-941.8
## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Private run dir if you RUN a test binary: /home/Samuel/.cache/tbd-target-T-941.8 (delete on cleanup).
Gate lock wait is normal. Worktree LFS pointers: `cargo xtask schema validate` not `ci schema-validate`.
Touch `.c` → `cargo xtask mod compile`. Do NOT `cargo test -p xtask` during mod compile.
Copy 19 EnfusionMCP `.c` before first compile (gitignored, do not commit):
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/
FIRST: cd worktree; pwd && git branch --show-current must be slice/T-941.8. Then copy MCP if count != 19. Then prove the defect.
## THE JOB
Vehicles spawn with engine-default fuel and no cargo unless the roster authored lock/fuel/ammo.

Already landed, do NOT rewrite:
- T-675.2 `TBD_MissionVehicleRoster.SeatAuthoredCrews` at SpawnManager ~1054–1059 (crew stays there).
- T-680 `TBD_VehicleState.ApplySpawned()` at ~1060–1062 applies **authored** lock/fuel/ammo. Unset fuel is ABSENT and left at engine default. That file is **not** yours.

This slice: after a vehicle spawns, **default full fuel** and a **per-class default cargo** table keyed by vehicle class. Roster fuel or inventory fields, when the wire carries them, override the defaults. A missing cargo prefab logs one warning naming the prefab; the vehicle still spawns.

Put the defaults in `TBD_SpawnManager.c` (both trees). Call them so they compose with T-680 (authored values win; do not double-set or fight ABSENT). Do not edit `TBD_VehicleState.c`.

Perturbation: misspelled fuel API → `mod compile` RED; restore, touch, green.
Also `cargo xtask mod world-boot` if available.

## YOUR FILES (touch ONLY these)
apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c
apps/mod/tbd-export/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c

This wave siblings (do NOT touch):
T-936.6: mission.schema.json, spawn_modules.rs, panels/{spawn_modules,mod,settings_modal}.rs, mission/{mod,extensions,flatten,compile,weather,audio,tasks}.rs, TBD_DynamicSpawner.c both
T-941.4: TBD_ObjectivesComponent.c both, TBD_ObjectiveHud.c both, TBD_ObjectiveHud.layout both
## ENFUSION LOCKSTEP
Edit BOTH trees. Twins must stay in lockstep (export is the ASCII fold of framework if needed). JsonLoadContext allocates nested refs when absent; presence is sentinel or Count().
## RULES
1. Verify defect on this tree first (read the vehicle spawn path + T-680 call). 2. Perturbation RED verbatim, restore, touch, green.
3. Explicit git paths. NEVER git add -A / stash.
4. `cargo xtask platform wave gate --slice T-941.8` from the worktree.
5. No ci-local, no leptos-gates. skip: is FAIL. `cargo xtask schema validate`.
6. No file-length allowlist extensions. 7. No committed .py/.sh/.mjs.
8. No subagents. No ship/merge/push. No `.ai/tickets/` / docs.
9. Leave :3000 :8080 up. 10. No silent deferrals. Dead mechanisms worse than missing.
11. Do NOT edit TBD_VehicleState.c / MissionLoader / flatten / schema.
## REPORT (every field)
pwd_branch · defect_verified_on_main · changes · perturbation {red VERBATIM, restored_green} ·
gate_verdict_tail · mod_compile_verdict · files_outside_owns [] · found_not_fixed · deviations · commits ·
manual_checklist · twins_confirmed
Write `.ai/artifacts/editor_briefs/sept2026/wave253/REPORT-T-941.8.md` in the worktree. Branch committed and clean. No push.
