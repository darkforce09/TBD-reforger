You are the slice agent for **T-941.4** (Objective HUD and capture bar replace chat).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-941.4  branch slice/T-941.4
## ENVIRONMENT
HOST Fedora. Native cargo. No distrobox-host-exec, no hcargo.
  export CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target
Private run dir if you RUN a test binary: /home/Samuel/.cache/tbd-target-T-941.4 (delete on cleanup).
Gate lock wait is normal. Worktree LFS pointers: `cargo xtask schema validate` not `ci schema-validate`.
Touch `.c` → `cargo xtask mod compile`. Do NOT `cargo test -p xtask` during mod compile.
Copy 19 EnfusionMCP `.c` before first compile (gitignored, do not commit):
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/
FIRST: cd worktree; pwd && git branch --show-current must be slice/T-941.4. Then copy MCP if count != 19. Then prove the defect.
## THE JOB
All objective feedback is chat. Anchor: `TBD_ObjectivesComponent.c:32` documents `SCR_ChatComponent.SendPrivateMessage`; `:816` is the per-player pump.

1. NEW `UI/Hud/TBD_ObjectiveHud.c` and `UI/layouts/TBD_ObjectiveHud.layout` in BOTH trees: objective list with state icons and a capture bar bound to the local player's contested objective.
2. Register the layout through the HUD script's **own** ResourceName constant (T-941.3 owns `TBD_UILayouts.c` — do not edit it). `TBD_UILayouts.Create` falls back to the bare path; you may call `Create` with a HUD-local constant, or `GetGame().GetWorkspace().CreateWidgets` with the same GUID-prefix + StripGuid pattern T-941.3 uses.
3. `TBD_ObjectivesComponent.c` BOTH trees: replicate objective state and capture progress to clients; drop the per-tick chat pump. Chat keeps **only** the objective-complete line.
4. New `.c` / `.layout` need BOTH copies. First Workbench pass still required for GUID index.

Perturbation: bind the bar to a missing widget name → `mod compile` RED; restore, touch, green.
Also `cargo xtask mod world-boot` if available.

## YOUR FILES (touch ONLY these)
apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectivesComponent.c
apps/mod/tbd-export/Scripts/Game/TBD/Objectives/TBD_ObjectivesComponent.c
apps/mod/tbd-framework/Scripts/Game/TBD/UI/Hud/TBD_ObjectiveHud.c
apps/mod/tbd-export/Scripts/Game/TBD/UI/Hud/TBD_ObjectiveHud.c
apps/mod/tbd-framework/UI/layouts/TBD_ObjectiveHud.layout
apps/mod/tbd-export/UI/layouts/TBD_ObjectiveHud.layout

This wave siblings (do NOT touch):
T-936.6: mission.schema.json, spawn_modules.rs, panels/{spawn_modules,mod,settings_modal}.rs, mission/{mod,extensions,flatten,compile,weather,audio,tasks}.rs, TBD_DynamicSpawner.c both
T-941.8: TBD_SpawnManager.c both
## ENFUSION LOCKSTEP
Edit BOTH trees. New `.c` / `.layout` need BOTH copies. JsonLoadContext allocates nested refs when absent; presence is sentinel or Count().
## RULES
1. Verify defect on this tree first (read :32 and :816). 2. Perturbation RED verbatim, restore, touch, green.
3. Explicit git paths. NEVER git add -A / stash.
4. `cargo xtask platform wave gate --slice T-941.4` from the worktree.
5. No ci-local, no leptos-gates. skip: is FAIL. `cargo xtask schema validate`.
6. No file-length allowlist extensions. 7. No committed .py/.sh/.mjs.
8. No subagents. No ship/merge/push. No `.ai/tickets/` / docs.
9. Leave :3000 :8080 up. 10. No silent deferrals. Dead mechanisms worse than missing.
11. Do NOT edit TBD_UILayouts.c. Do NOT edit flatten.rs / schema.
## REPORT (every field)
pwd_branch · defect_verified_on_main · changes · perturbation {red VERBATIM, restored_green} ·
gate_verdict_tail · mod_compile_verdict · files_outside_owns [] · found_not_fixed · deviations · commits ·
manual_checklist · twins_confirmed
Write `.ai/artifacts/editor_briefs/sept2026/wave253/REPORT-T-941.4.md` in the worktree. Branch committed and clean. No push.
