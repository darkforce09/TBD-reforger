You are the slice agent for **T-212** (Typed per-side objectives with attributes).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-212  branch slice/T-212
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. Any other
variable is SILENTLY DROPPED — use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-212` — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-212
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-212 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.
Touch `.c` → `cargo xtask mod compile`. Do NOT `cargo test -p xtask` during mod compile (they share
`~/.local/share/tbd-server-addons`, T-946.13). Copy the 19 EnfusionMCP `.c` before the first compile
(gitignored, do not commit):
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-212. Then prove the defect.

## THE JOB
Make `TBD_ObjectiveRegistry` read TYPED, PER-SIDE objectives with the uniform attribute spine, without
breaking objectives-as-zones. The numeric/owner half of the spine already ships (T-685) and already
reaches the wire; what is missing is the typed entity: `objectives[]` rows carrying `type` (including
the new `defend`), `side`, `zoneId`, `label`, `framing` (per-side attacker/defender title+text), `lock`
and `autoLose`.

## WHAT THE WIRE ACTUALLY CARRIES — read this before you plan
- **Schema is DONE** (T-706, shipped). `mission.schema.json:119` declares top-level `objectives[]`;
  `$defs/objective` at `:846` is `required: [id, type]`, `additionalProperties: false`, with
  `type` enum `capture|destroy|hold|defend`, `side` (`$ref factionKey`), `zoneId`, `label`, `framing`,
  `lock`, `autoLose`, `variantId`. `$defs/zoneRules` at `:723-780` carries `attackerCount`,
  `defenderCount`, `advantagePercent`, `minHeight`, `maxHeight`, `startingOwner`, `lock`.
- **`zoneRules` DOES reach `/compiled`** — `flatten.rs:390-392` passes `ModZone.rules` through verbatim,
  populated at `:3241`. T-685 already reads it mod-side.
- **`objectives[]` DOES NOT reach `/compiled`.** `ModMissionDocument` (`flatten.rs:772-880`) has no
  `objectives` field; the word appears in `flatten.rs` only in one doc comment at `:1411`.
  `compile.rs:235` emits it into `compile_payload`, the EDITOR superset, not the mod document. Mod
  side, `TBD_MissionLoader.c:380` says it outright: *"objectives[] / editorTriggers[] are NOT on
  TBD_MissionDocumentStruct; their readers re-parse GetRawJson()"*. Emitting it is T-946.36, which is
  NOT this slice and NOT in your owns.
- **So your acceptance is the T-685 precedent**: prove the reader against the hand-staged 1.3 golden
  `packages/tbd-schema/golden-missions/schema-1_3-wire-fields.json`, which already carries an
  `objectives[]` row with `framing.attacker/defender`, `lock` and `autoLose`, and a zone whose `rules`
  carry `attackerCount:3, defenderCount:2, advantagePercent:15, minHeight:-5, maxHeight:30,
  startingOwner:"opfor"`. Do not claim it reaches a live `/compiled`; it does not.

## ANCHORS (verified 2026-09-08 against main)
- `apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectiveRegistry.c` — 994 lines. The
  `zone.type` keying is `:35-43` (`TYPE_CAPTURE`/`TYPE_DESTROY`/`TYPE_HOLD_UNTIL` and the three
  `winConditions.endOn` values), consumed by `KindOf(string zoneType)` at `:268-279`, driven from the
  `Build()` loop at `:177`. `Prepare()` `:345`. `BuildBoardForFaction()` `:974`.
- `TBD_Objective.c` — `enum TBD_EObjectiveKind` `:34`, `TBD_EObjectiveOnEmpty` `:61`, `class
  TBD_Objective` and its runtime fields `:69-156`, per-side rendering `BoardLine(viewerFaction)` `:399`,
  `StatusText(viewerFaction)` `:410`, `CaptureStatusText` `~:440`, side logic `ResolveActingFaction()`
  `:281`, `MayOwn()` `:328`.
- T-685's hooks in the registry are only 7 lines (`:132`, `:163`, `:395-396`, `:753`); its 537-line
  `TBD_ZoneVolume.c` carries the WOG INFERRED caveat unlaundered — copy that discipline.
- `xtask/src/schema_gates.rs:2449-2490` `UNREAD_WIRE_FIELDS`: rows `objectives expected 13`,
  `framing expected 0`, `autoLose expected 0`, all ticketed to T-212. Comparison is `!=` at `:2672`.
  `count_mod_readers` `:2637` regex-counts `\bname\b` in every `.c` under `apps/mod/tbd-framework`
  after stripping comments and string literals. Measured today: `objectives` 13 (all inside
  `TBD_ObjectivesComponent.c`), `framing` 0, `autoLose` 0, `lock` 6 (all `TBD_VehicleState.c`).

## TRAPS THAT WILL BITE THIS SLICE
- **`mirror_lockstep` will refuse a framework-only edit.** `xtask/src/gate_mod_compile.rs:641`, called
  unconditionally at `:427` after a clean compile, with no opt-out. It normalises (comments stripped,
  **string literals KEPT**, ASCII punctuation folded, whitespace collapsed) and compares. Both
  `TBD_ObjectiveRegistry.c` copies and both `TBD_Objective.c` copies are in lockstep today. Every code
  or string-literal change goes in BOTH trees, and the export tree must stay pure ASCII
  (`ascii_check_export`).
- **The unread-wire gate runs inside YOUR slice gate.** `schema_gates.rs` → `wave::schema::gate_schema`
  (`xtask/src/wave/schema.rs:99`) → `gate_slice` (`xtask/src/wave/gate.rs:151`). The moment you write
  `framing` or `autoLose` or a 14th `objectives`, those rows go red. Re-pin them to the MEASURED new
  counts — that file is in your owns. There is a test at `schema_gates.rs:2716-2741` that exists
  specifically to fire the day T-212 writes `framing`.
- **DO NOT TOUCH `TBD_ObjectivesComponent.c` (either tree).** It is T-946.55's this wave. All 13 pinned
  `objectives` identifiers live in it, and it holds the kind dispatch (`:167-178`) and the per-player
  HUD composition (`:806-861`). Per-side text reaching a player is a FOLLOW-ON that packs after
  T-946.55; say so in `found_not_fixed` rather than reaching for the file.
- **`mission.schema.json` is PROSE ONLY.** You may correct `:119`'s description, which still says
  `objectives[]` is read by nothing. No shape, enum or `required` change. The clause was relaxed from
  "no packages/tbd-schema edits" by the operator on 2026-09-08 for exactly this.
- A prior attempt exists and was never reviewed: `git show fc4f15121` (tag
  `salvage/t853-dropped/T-212`), one framework-only file, +390 lines. Read it for ideas; it fails
  `mirror_lockstep` as written and its counts are `objectives` 3, `framing` 9, `autoLose` 3.
- The spec's old LANGUAGE GATE said "tbd-framework only; no schema JSON". That line was WRONG and is
  corrected in this commit. `rg` does not exist here (T-556) — use `grep -E`.

## YOUR FILES (touch ONLY these)
apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectiveRegistry.c
apps/mod/tbd-export/Scripts/Game/TBD/Objectives/TBD_ObjectiveRegistry.c
apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_Objective.c
apps/mod/tbd-export/Scripts/Game/TBD/Objectives/TBD_Objective.c
xtask/src/schema_gates.rs
packages/tbd-schema/schema/mission.schema.json   (PROSE ONLY)

## SIBLINGS THIS PACK (do NOT touch)
T-946.55 TBD_ObjectivesComponent.c both trees ·
T-939.2 panels/attributes_modal.rs, state/operations{,/reassign.rs}, crates/map-engine-core/src/doc/store.rs ·
T-939.4 panels/{context_menu,top_strip,help_modal}.rs, editor/mission_editor.rs ·
T-946.86 canvas/{gestures,overlays}.rs, panels/{outliner_tree,outliner_drag,zones_panel}.rs, state/commands_hotkeys.rs

## VERIFY
`cargo xtask mod compile` — paste the verdict (expect ~5761 files / 11484 classes / 0 warnings before
your change). Then `hcargo xtask platform wave gate --slice T-212`.
## RULES
1. **Verify the defect on main FIRST** and paste the RED. A string match is not proof — open the file.
2. **Perturbation proof, every fix**: break it deliberately, paste the RED verbatim, restore, `touch`
   the file, confirm green. A restored file can be OLDER than the artifact built from the perturbed
   version, so the GREEN half of the loop can be stale — `touch` is not optional.
3. Tests go at the **BOTTOM** of their file. `class_r_scrub::live_source` cuts from the FIRST
   `#[cfg(test)]` to EOF, so anything above it is invisible to the pin. This has bitten in three
   consecutive waves (T-937.1 wave 252, T-937.4 wave 253, and again in 255).
4. Explicit git paths. **NEVER `git add -A`. NEVER `git stash`** — it deletes LFS pointer files.
   Commit early and often; an outage leaves reviewed work behind.
5. `hcargo xtask platform wave gate --slice T-212` from the worktree; must end `SLICE GATE: PASS`.
6. No `cargo xtask ci ci-local`. No `mk leptos-gates` unless this brief names it.
7. No subagents. No ship, no merge, no push. Leave :3000 and :8080 up.
8. **No silent deferrals.** If the work needs a file outside your owns, STOP and report which file and
   why. Do not widen on your own.
9. Your report is evidence, not testimony. Every claim of the form "this already works" or "a sibling
   fixed it" must carry the grep or the commit that proves it. A wave-255 report claimed a UI button
   had been added; the commit contains none.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-212.md` in the worktree.
Branch committed and clean. No push.
