You are the slice agent for **T-939.4** (Arrange tools in context menu with shortcuts).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-939.4  branch slice/T-939.4
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. Any other
variable is SILENTLY DROPPED — use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-939.4` — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-939.4
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-939.4 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-939.4. Then prove the defect.

## THE JOB
Align and distribute become reachable by right-click and by keyboard. `top_strip.rs` becomes the single
source of Arrange entries (label, chord, invoker); `context_menu.rs` renders them as a submenu when the
selection holds two or more entities; `mission_editor.rs`'s keydown gains the chords as thin callers;
`help_modal.rs` lists them.

## ANCHORS (verified 2026-09-08 against main)
- `panels/top_strip.rs:250` is the `"Arrange"` menu (T-645, described at `:106`), gated on selection
  >= 1 at `:378-380`; the align invoker lands at `:1249`
  (`crate::editor::state::operations::align_selection(edge)`). Menu bar layout at `:1365`
  (T-797: File / Edit / Arrange / Mission / Environment / Help).
- `panels/context_menu.rs` (1811 lines) contains **zero** occurrences of `Arrange` — measured today.
- `panels/help_modal.rs` carries both pins you must satisfy: `no_two_listeners_claim_the_same_chord`
  at `:941` and `every_binding_has_a_help_entry` at `:1578`. There is a warning at `:1203` telling you
  to read the first one before adding a listener. Read it.
- `editor/mission_editor.rs` (3551 lines, allowlisted SIZE-3) holds the wasm keydown closure; the
  invoker/keydown contract is documented at `:766` and `:773` — *"the five invokers mirror the keydown
  arms one-for-one so a click and the chord do the same thing"*. Follow that shape: new arms stay thin
  callers of shared invokers, never reimplementations.
- `:562` marks a RESERVED-UNBOUND chord (no keydown arm, no help row) held for a later slice. Do not
  take it.

## TRAPS THAT WILL BITE THIS SLICE
- **Both `help_modal.rs` pins fire on a careless chord.** A new listener without a help row reds
  `every_binding_has_a_help_entry`; a chord another listener already claims reds
  `no_two_listeners_claim_the_same_chord`. You own `help_modal.rs`, so add the rows in the same commit.
- **DO NOT ADD A TACTICAL-DRAW ENTRY OR CHORD.** T-946.86 runs this wave and is landing the
  `begin_tactical_draw` trigger as a button in `panels/zones_panel.rs`. A chord for it here would
  collide with that work and with the two pins above. If you think the Arrange work needs it, say so in
  `found_not_fixed`.
- `mission_editor.rs` is file-length allowlisted — do not grow it with logic that belongs in
  `top_strip.rs`. Thin arms only.
- The submenu must be **hidden** on a single selection and the chords **inert** there; that is an
  acceptance line, not a nicety, and it needs its own test.
- Tests at the BOTTOM of each file — `class_r_scrub::live_source` truncation.

## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/panels/context_menu.rs
apps/website/frontend/src/editor/panels/top_strip.rs
apps/website/frontend/src/editor/panels/help_modal.rs
apps/website/frontend/src/editor/mission_editor.rs

## SIBLINGS THIS PACK (do NOT touch)
T-212 mod Objectives .c both trees, xtask/src/schema_gates.rs, mission.schema.json ·
T-946.55 TBD_ObjectivesComponent.c both trees ·
T-939.2 panels/attributes_modal.rs, state/operations{,/reassign.rs}, crates/map-engine-core/src/doc/store.rs ·
T-946.86 canvas/{gestures,overlays}.rs, panels/{outliner_tree,outliner_drag,zones_panel}.rs, state/commands_hotkeys.rs

## VERIFY
1. Verify on main: a test asserting the context menu has no Arrange entry and the keydown ignores the
   align chord; paste the RED.
2. Perturbation: drop one chord arm; the keydown test must go red; restore, `touch`, green.
3. `hcargo xtask mk ci-local-leptos`, then `hcargo xtask platform wave gate --slice T-939.4`.
   `mk leptos-gates` is in this ticket's verify list — run it ONLY if the operator's `trunk serve` on
   :3000 is stopped, because its `trunk build --release` collides over the same `dist/`. If you cannot,
   say so in the report; do not silently skip it.
## RULES
1. **Verify the defect on main FIRST** and paste the RED. A string match is not proof — open the file.
2. **Perturbation proof, every fix**: break it deliberately, paste the RED verbatim, restore, `touch`
   the file, confirm green. A restored file can be OLDER than the artifact built from the perturbed
   version, so the GREEN half of the loop can be stale — `touch` is not optional.
3. Tests go at the **BOTTOM** of their file. `class_r_scrub::live_source` cuts from the FIRST
   `#[cfg(test)]` to EOF, so anything above it is invisible to the pin. This has bitten in three
   consecutive waves (T-937.1 wave 252, T-937.4 wave 253, and again in 255).
4. Explicit git paths. **NEVER `git add -A`. NEVER `git stash`** — it deletes LFS pointer files.
   Commit early and often; an outage leaves reviewed work behind.
5. `hcargo xtask platform wave gate --slice T-939.4` from the worktree; must end `SLICE GATE: PASS`.
6. No `cargo xtask ci ci-local`. No `mk leptos-gates` unless this brief names it.
7. No subagents. No ship, no merge, no push. Leave :3000 and :8080 up.
8. **No silent deferrals.** If the work needs a file outside your owns, STOP and report which file and
   why. Do not widen on your own.
9. Your report is evidence, not testimony. Every claim of the form "this already works" or "a sibling
   fixed it" must carry the grep or the commit that proves it. A wave-255 report claimed a UI button
   had been added; the commit contains none.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-939.4.md` in the worktree.
Branch committed and clean. No push.
