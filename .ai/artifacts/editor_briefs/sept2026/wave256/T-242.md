You are the slice agent for **T-242** (Emit T-216 slot deltas through flatten).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-242  branch slice/T-242
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-242`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-242
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-242 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-242. Then prove the defect.
## THE JOB
Ticket ask: The T-216 schema deltas (slot tag/callsign/rank/stance, group leaderSlotId, root vehicles $def)
are declared in `mission.schema.json`, but flatten allegedly drops the four slot fields (DIAG_DROP_SLOT_*);
emit them and pin with schema-validated goldens.

**CRITICAL STALENESS CHECK (RULE 1) — READ BEFORE WRITING CODE:**
Commit `573c4272406d8989a4b758eaf841fdd83c9dcb50` (T-674.1, 2026-09-06) already implemented the emit of
the four slot fields (`callsign`, `rank`, `stance`, `tag`) plus `unitName` to `ModSlot`, and `leaderSlotId` to
`ModOrbatGroup`.
Verify this immediately:
1. Run `hcargo test -p map-engine-core --all-features -- slot_identity_and_squad_leader_reach_the_compiled_wire`.
   Observe it PASSES.
2. Check `crates/map-engine-core/src/mission/flatten.rs:3498-3556`: `authored_identity` resolves `tag`, `callsign`,
   `rank`, `stance`, and `unit_name`, and pushes them directly onto `ModSlot`.
3. Check `flatten.rs:4446-4510` (`the_compile_boundary_ledger_is_checked_against_the_contract`): all six T-216
   rows already transitioned from `DeclaredPendingEmit` to `Fate::Reaches`.
4. `DIAG_DROP_SLOT_*` (`flatten.rs:885-896`) were re-purposed in T-674.1 to fire ONLY when authored values cannot
   be carried on the wire (unrepresentable / control bytes / unknown enum variants); on valid authored input,
   `doc.diagnostics` is completely empty.

If all acceptance criteria are already satisfied by shipped code: **STOP, write the report stating that the defect
is already fixed on main by commit 573c42724 (T-674.1)**, per Rule 1 of the platform runbook.
If you find any residual T-216 delta not reaching `/compiled`, document it, fix it, and provide perturbation evidence.

## ANCHORS (verified 2026-09-07 — the ticket's are stale)
- `flatten.rs:885-896`: `DIAG_DROP_SLOT_*` definitions (moved from ~699-702).
- `flatten.rs:3498-3556`: slot identity emit in `flatten_to_mod_document` (resolves `tag`, `callsign`, `rank`, `stance`, `unit_name`).
- `flatten.rs:4457-4520`: T-216 compile boundary ledger test (`the_compile_boundary_ledger_is_checked_against_the_contract`).
- `flatten.rs:8460-8497`: `slot_identity_and_squad_leader_reach_the_compiled_wire` test asserting serialized JSON wire outputs.
- `packages/tbd-schema/schema/mission.schema.json:433-465`: `$defs/slot` properties (`callsign`, `rank`, `stance`, `unitName`, `tag`).

## TRAPS THAT WILL BITE THIS SLICE
- `flatten.rs` has mid-file tests and source probes. `class_r_scrub::live_source` cuts at the first `#[cfg(test)]`.
  Never place inline tests above production items.
- Schema codegen: if schema changes are made, run `hcargo xtask ci schema-codegen`. Never hand-edit `contract/generated/`.

## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/mission/flatten.rs
packages/tbd-schema/schema/mission.schema.json

## SIBLINGS THIS PACK (do NOT touch)
T-937.5 mission-editor-payload.schema.json, mission_library.rs, operations.rs, slot_ids.rs ·
T-257 doc/store.rs ·
T-939.1 outliner_tree.rs, outliner_drag.rs, panels/mod.rs ·
T-939.3 overlays.rs, gestures.rs, gizmo_z.rs, canvas/mod.rs

## RULES
1. Verify defect first: check whether authored slot deltas are dropped. If already emitted, STOP and report.
2. Perturbation RED verbatim, restore, `touch` the file, green.
3. `hcargo test -p map-engine-core --all-features`.
4. Explicit git paths. NEVER `git add -A`, NEVER `git stash`. Commit after every self-contained step.
5. `hcargo xtask platform wave gate --slice T-242` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`.
7. No subagents. No ship/merge/push. Leave :3000 and :8080 up.
8. No silent deferrals.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-242.md` in the worktree.
Branch committed and clean. No push.
