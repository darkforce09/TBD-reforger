You are the slice agent for **T-939.3** (Canvas: Z gizmo arm and vertical drag).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-939.3  branch slice/T-939.3
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-939.3`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-939.3
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-939.3 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-939.3. Then prove the defect.
## THE JOB
The transform gizmo gains a Z arm and vertical drag affordance:
1. New `apps/website/frontend/src/editor/canvas/gizmo_z.rs` (registered in `canvas/mod.rs`):
   Pure geometry and math module (NOT wasm-gated, so it is fully covered by native unit tests):
   - Z arm geometry (vertical handle projected at the gizmo center, screen-space dimensions)
   - Hit testing (`hit_z_arm(press_x, press_y, center_x, center_y, scale) -> bool`)
   - Screen dy to world elevation (metres) conversion taking camera zoom/scale into account
   - Snapping step (e.g. snap ladder, 0.5m / 1m)
   - Height readout formatting string (e.g. `"{z:.1} m"`)
2. `overlays.rs:172-198`: In `WidgetVariant::Translate`, draw the Z arm (vertical axis arrow) in addition
   to the X and Y arms, along with the dynamic height readout when dragging.
3. `gestures.rs:545-573`: When in Translate mode with an active selection, hit-test the Z arm. If pressed,
   route the drag to vertical height adjustment instead of horizontal translation or marquee.
4. During drag, update the height readout dynamically. On pointerup, commit the Z change through the
   existing operation path (one doc transaction = one undo step). Holding Shift during the drag suspends snapping.

## ANCHORS (verified 2026-09-07 — the ticket's are stale)
- `apps/website/frontend/src/editor/canvas/overlays.rs:172-198`: `WidgetVariant::Translate` SVG drawing.
- `apps/website/frontend/src/editor/canvas/overlays.rs:216-240`: cursor-adjacent mode hint chip.
- `apps/website/frontend/src/editor/canvas/gestures.rs:545-573`: pointerdown drag start and rotate ring hit test.
- `apps/website/frontend/src/editor/canvas/gestures.rs:640-660`: translation / move drag arming.
- `apps/website/frontend/src/editor/canvas/mod.rs:16-24`: canvas module registrations. Register `pub mod gizmo_z;`.

## TRAPS THAT WILL BITE THIS SLICE
- **Keep `gizmo_z.rs` native-testable**: do not put wasm-only imports (`web_sys`, DOM handles) in `gizmo_z.rs`.
  Only `gestures.rs` and `overlays.rs` handle the wasm-specific event wiring.
- **Do not desync or corrupt X/Y translation**: X and Y arms and standard entity translation must continue
  to function unchanged when the Z arm is not hit.
- **Z-drag must be one undo step**: writing the updated Z coordinate must produce one atomic transaction
  on pointerup so Ctrl+Z restores the prior height in a single stroke.

## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/canvas/overlays.rs
apps/website/frontend/src/editor/canvas/gestures.rs
apps/website/frontend/src/editor/canvas/gizmo_z.rs   (new)
apps/website/frontend/src/editor/canvas/mod.rs

## SIBLINGS THIS PACK (do NOT touch)
T-242 flatten.rs, mission.schema.json ·
T-937.5 mission-editor-payload.schema.json, mission_library.rs, operations.rs, slot_ids.rs ·
T-257 doc/store.rs ·
T-939.1 outliner_tree.rs, outliner_drag.rs, panels/mod.rs

## RULES
1. Verify defect first: assert hit-testing the Z arm above gizmo center returns None or fails; paste the RED verbatim.
2. Perturbation: invert the dy sign in `gizmo_z.rs`; unit test must go RED; restore, `touch`, green.
3. `hcargo test -p website-frontend`.
4. Explicit git paths. NEVER `git add -A`, NEVER `git stash`. Commit after every self-contained step.
5. `hcargo xtask platform wave gate --slice T-939.3` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`.
7. No subagents. No ship/merge/push. Leave :3000 and :8080 up.
8. No silent deferrals.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-939.3.md` in the worktree.
Branch committed and clean. No push.
