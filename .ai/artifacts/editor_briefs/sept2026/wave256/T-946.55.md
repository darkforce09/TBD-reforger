You are the slice agent for **T-946.55** (Objective HUD replicates to every player at 1 Hz).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-946.55  branch slice/T-946.55
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. Any other
variable is SILENTLY DROPPED — use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-946.55` — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-946.55
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-946.55 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.
Touch `.c` → `cargo xtask mod compile`. Do NOT `cargo test -p xtask` during mod compile (shared
`~/.local/share/tbd-server-addons`, T-946.13). Copy the 19 EnfusionMCP `.c` before the first compile
(gitignored, do not commit):
  mkdir -p apps/mod/tbd-framework/Scripts/WorkbenchGame
  cp -a /run/media/system/Disk_2/Projects/TBD-Reforger/apps/mod/tbd-framework/Scripts/WorkbenchGame/EnfusionMCP \
        apps/mod/tbd-framework/Scripts/WorkbenchGame/

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-946.55. Then prove the defect.

## THE JOB
`Deliver` calls `ReplicateHud` every tick with no dirty check, sending a Reliable Owner RPC of three
string arrays **per connected player at 1 Hz**. Cost scales with players x objectives and never falls
to zero on an idle round — which can exceed the chat pump T-941.4 removed, making the fix a regression
on the metric it was chosen for. Gate the RPC on a real difference, without starving a joiner.

## ANCHORS (verified 2026-09-08 — exact and IDENTICAL in both twins, 910 lines each)
- `:682` `protected void Deliver(notnull PlayerManager players, notnull array<int> connected, notnull array<ref TBD_Objective> objectives)`
- `:692` `ReplicateHud(players, connected, objectives);` — unconditional, no dirty check
- `:747` `protected void ReplicateHud(... array<ref TBD_Objective> board)`; `:749-752` `foreach (int playerId : connected) PushHudToPlayer(players, playerId, board, 1);`
- `:788` `PushHudToPlayer(... int show)`; `:802` `controller.TBD_PushObjectiveHud(icons, titles, details, barLabel, barPercent, barVisible, show);`
- Current member state is only `m_bLive`, `m_bAnnouncedArmed`, `m_bAnnouncedNoObjectives`,
  `m_bEndTriggerAnnounced`, `m_aBroadcasts` (`:53-68`) — a `ref map<int,string>` of per-owner board
  hashes drops in with no external dependency.
- **Force-send already has a pull path**: `PushHudTo(int playerId)` `:774` is public and called from
  `TBD_ObjectiveHud.c:288 TBD_RequestObjectiveHud()` and the server RPC `:318`. You still need
  "unknown owner ⇒ send" inside `ReplicateHud`; that is a map miss, in-file.
- **Disconnect cleanup is available in-file two ways.** `SCR_BaseGameMode.OnPlayerDisconnected`
  dispatches to every `SCR_BaseGameModeComponent` — documented at `TBD_SpawnManager.c:3468`, exact
  signature at `TBD_SpawnManager.c:3348`: `override void OnPlayerDisconnected(int playerId,
  KickCauseCode cause, int timeout)`. Belt and braces: `Tick` re-reads `players.GetPlayers(connected)`
  every second (`:156-157`), so pruning keys absent from `connected` needs no hook at all. `OnDelete`
  (`:99-117`) is the world-teardown clear point.
- The RPC itself is at `TBD_ObjectiveHud.c:301` → `:326` `[RplRpc(RplChannel.Reliable, RplRcver.Owner)]`.
  It is **outside your owns and you do not need it** — gating the call is enough.

## TRAPS THAT WILL BITE THIS SLICE
- **HASH THE PER-PLAYER RENDERED SNAPSHOT, NOT A GLOBAL BOARD.** `FillHudSnapshot` (`:806`) resolves
  `factionKey` per player (`HudIcon(objective, factionKey)`, `StatusText(factionKey)`) and emits a
  capture bar gated on `objective.m_aPresentPlayers.Find(playerId)` with a live `ProgressPercent()`.
  A global dirty flag is wrong twice: it misses per-side text differences, and it suppresses a bar
  that legitimately changes every tick while someone is capturing.
- **DO NOT ADD OR REMOVE A SINGLE OCCURRENCE OF THE IDENTIFIER `objectives` IN THIS FILE.**
  `xtask/src/schema_gates.rs:2455` pins `objectives == 13` by EXACT equality and all 13 live here.
  That gate runs inside your slice gate (`xtask/src/wave/gate.rs:151`), the fix would live in
  `schema_gates.rs` which you do not own, and T-212 is re-pinning that row this wave from its own
  measurement. Renaming the `Deliver` parameter, adding a helper that takes `objectives`, or dropping
  the argument at `:692` all break it. The minimal shape — member map, hash inside
  `ReplicateHud(… board)`, gate `PushHudToPlayer` — keeps the count at 13.
- **`mirror_lockstep`** (`xtask/src/gate_mod_compile.rs:427,641`) compares both trees after comments
  are stripped, **string literals kept**, ASCII punctuation folded. The twins are identical today.
  Change both, and keep `tbd-export` pure ASCII.
- Per-owner state must be bounded by players, not by session length.

## YOUR FILES (touch ONLY these)
apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectivesComponent.c
apps/mod/tbd-export/Scripts/Game/TBD/Objectives/TBD_ObjectivesComponent.c

## SIBLINGS THIS PACK (do NOT touch)
T-212 TBD_ObjectiveRegistry.c + TBD_Objective.c both trees, xtask/src/schema_gates.rs, mission.schema.json ·
T-939.2 panels/attributes_modal.rs, state/operations{,/reassign.rs}, crates/map-engine-core/src/doc/store.rs ·
T-939.4 panels/{context_menu,top_strip,help_modal}.rs, editor/mission_editor.rs ·
T-946.86 canvas/{gestures,overlays}.rs, panels/{outliner_tree,outliner_drag,zones_panel}.rs, state/commands_hotkeys.rs

## VERIFY
Measure first: instrument or reason out the call count over an idle round and paste RPCs per player per
minute with nothing changing. Then `cargo xtask mod compile` (paste the verdict) and
`hcargo xtask platform wave gate --slice T-946.55`.
Perturbation: make the dirty check always report dirty; the idle-round assertion must go red.
## RULES
1. **Verify the defect on main FIRST** and paste the RED. A string match is not proof — open the file.
2. **Perturbation proof, every fix**: break it deliberately, paste the RED verbatim, restore, `touch`
   the file, confirm green. A restored file can be OLDER than the artifact built from the perturbed
   version, so the GREEN half of the loop can be stale — `touch` is not optional.
3. Tests go at the **BOTTOM** of their file. `class_r_scrub::live_source` cuts from the FIRST
   `#[cfg(test)]` to EOF, so anything above it is invisible to the pin. This has bitten in three
   consecutive waves (T-937.1 wave 252, T-937.4 wave 253, and again in 255).
4. Explicit git paths. **NEVER `git add -A`. NEVER `git stash`** — it deletes LFS pointer files.
   Commit early and often; an outage leaves reviewed work behind.
5. `hcargo xtask platform wave gate --slice T-946.55` from the worktree; must end `SLICE GATE: PASS`.
6. No `cargo xtask ci ci-local`. No `mk leptos-gates` unless this brief names it.
7. No subagents. No ship, no merge, no push. Leave :3000 and :8080 up.
8. **No silent deferrals.** If the work needs a file outside your owns, STOP and report which file and
   why. Do not widen on your own.
9. Your report is evidence, not testimony. Every claim of the form "this already works" or "a sibling
   fixed it" must carry the grep or the commit that proves it. A wave-255 report claimed a UI button
   had been added; the commit contains none.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-946.55.md` in the worktree.
Branch committed and clean. No push.
