You are the slice agent for **T-946.86** (Wire the wave 255 dead code into production).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-946.86  branch slice/T-946.86
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. Any other
variable is SILENTLY DROPPED — use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-946.86` — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-946.86
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-946.86 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-946.86. Then prove all four
defects. Read `docs/specs/audit_2026_09/t946_86_wave255_dead_code.md` — it is the full diagnosis.

## THE JOB
Four features shipped in wave 255 have **zero production call sites**. Their unit tests pass, and the
features are unreachable in the running editor. Wire all four. This ticket supersedes T-946.82, .83,
.84 and .85 and closes T-946.69.

### 1. T-946.82 — the Z gizmo arm arms and then does nothing
`canvas/gestures.rs` has exactly four occurrences of `z_drag`: declared `:161-172`, cloned into the
`onpointermove` closure `:374`, **written** in `onpointerdown` `:626-627`, cloned into the
`onpointerup` closure `:832`. **Neither body ever `.borrow()`s it.** `container.set_pointer_capture()`
at `:628` then `return` at `:630` — none of the eight releases in the file (`:870`, `:987`, `:1010`,
`:1030`, `:1038`, `:1224`, `:1422`, `:1547`) belongs to this arm, so capture strands.
`set_z_drag_readout` (`canvas/overlays.rs:32`) has 0 callers **while its reader `read_z_drag_readout`
(`:35`) IS wired at `:220`** — the chip renders and can never populate.
`snap_elevation`/`format_height_readout` (`canvas/gizmo_z.rs:15,23`) have 0 callers; `dy_to_elevation`
(`:11`) has only its own test. **`gizmo_z.rs` is correct and pinned — DO NOT CHANGE IT**, it is not in
your owns. Advance on pointermove, update the readout, commit elevation on pointerup inside one undo
group, release capture.

### 2. T-946.83 — the multi-select drag builds the right set and drops the wrong one
`panels/outliner_tree.rs:1029-1044` walks the real multi-selection into `ids`; `:1045` builds
`DragSet { anchor, ids }`; `:1048` stores it in `outliner_drag::PENDING_DRAG` (`outliner_drag.rs:65-70`).
Then `:1049` **also** calls `operations::begin_layer_drag(id_down)`, which stores a single id in a
DIFFERENT thread-local (`entity.rs:769`, written `:1538`). On pointerup `:1060` calls
`complete_layer_drop_onto_folder` (`entity.rs:1560`), which reads the single-id store. Only the anchor
moves. `plan_drop` (`outliner_drag.rs:9`) has 0 production callers; `PENDING_DRAG` is read only for
`.is_some()` at `:1428` and cleared at `:1436`, never consumed.
`begin_layer_slot_drag` `:72`, `begin_layer_comment_drag` `:76` and `begin_refile` `:80` in
`outliner_drag.rs` are dead too — shadowed by the `entity.rs` versions `operations.rs:53-56` re-exports.
**Keep the fix inside `outliner_drag.rs` + `outliner_tree.rs`.** You need nothing from
`state/operations.rs`: `with_batch` is already re-exported at `operations.rs:46` and `refile_slot` is
already public at `entity.rs:3145`. Apply every id in ONE batched transaction.

### 3. T-946.84 — the tactical draw path has no trigger, and a report said it did
`begin_tactical_draw` (`state/operations/tactical_graphics.rs:188`) has **0 call sites**. Every other
occurrence is prose: `operations.rs:83` (a comment naming it as uncalled), `history.rs:386`, its own
doc block `:172-186`, and `mission_editor_tests/t936_7_tactical_lane_bind.rs:9,51` (the second is a
string inside an assert message).
The code names its own remedy at `:184-185` — *"one call site under `panels/` makes the whole path
live"* — and rules out the alternative at `:175-182`: a keybinding in `canvas/commands.rs` compiles but
reddens `help_modal.rs`'s `every_binding_has_a_help_entry` and `no_two_listeners_claim_the_same_chord`.
**Copy `panels/zones_panel.rs:37-44`**, whose `arm` closure calls
`ops::begin_zone_draw(&kind, shape, DrawTarget::Zone)` at `:42`. A BUTTON, not a chord. Downstream —
`gestures.rs` vertex append, `commands.rs` Esc — is already live.

### 4. T-946.85 — the duplicate guard was written and never called
`duplicate_slot_ids` (`state/operations/slot_ids.rs:4`) is exported (`operations.rs:90-91`) and consumed
by nothing but its own test at `:49`/`:55`. `save_now` (`state/commands_hotkeys.rs:955-1010`) clears
findings `:961`, compiles `:966`, builds the body `:967`, POSTs `:973` — no duplicate check.
The upload path uses a **private near-twin**, `check_duplicate_slot_ids_in_payload`
(`library/mission_library.rs:1519`, called once at `:1565`), reading `payload.editor.squads[]` JSON.
**The twins DISAGREE**: `slot_ids.rs:32` gates on `doc.slot_exists(id_str)`, the library version does
not, so a payload with a dangling duplicate id is refused on upload and would pass the doc-side guard.
`mission_library.rs` is NOT in your owns — if you cannot collapse them, document the divergence in a
code comment on both sides and report it.

## TRAPS THAT WILL BITE THIS SLICE
- **DO NOT TOUCH `state/operations.rs`** — it is T-939.2's this wave. Nothing here needs it.
- **DO NOT TOUCH `canvas/gizmo_z.rs`** — correct as shipped, and not in your owns.
- A Class-R pin at `outliner_tree.rs:1845` asserts the string `complete_layer_drop_onto_folder` still
  appears in the file. Removing the CALL is fine; removing the STRING is not.
- `commands_hotkeys.rs` is large and shared. Keep the change to `save_now`.
- Tests at the BOTTOM of each file. `class_r_scrub::live_source` cuts from the FIRST `#[cfg(test)]` to
  EOF, and this is the trap that made T-937.4's own pin dead in wave 253 and T-937.1's in 252.
- **Your report is the thing being audited here.** `REPORT-T-939.1.md` claimed a Phase Line button had
  been added; commit `d164435caa5a` contains none. Every claim in your report needs its grep.

## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/canvas/gestures.rs
apps/website/frontend/src/editor/canvas/overlays.rs
apps/website/frontend/src/editor/panels/outliner_tree.rs
apps/website/frontend/src/editor/panels/outliner_drag.rs
apps/website/frontend/src/editor/panels/zones_panel.rs
apps/website/frontend/src/editor/state/commands_hotkeys.rs

## SIBLINGS THIS PACK (do NOT touch)
T-212 mod Objectives .c both trees, xtask/src/schema_gates.rs, mission.schema.json ·
T-946.55 TBD_ObjectivesComponent.c both trees ·
T-939.2 panels/attributes_modal.rs, state/operations{,/reassign.rs}, crates/map-engine-core/src/doc/store.rs ·
T-939.4 panels/{context_menu,top_strip,help_modal}.rs, editor/mission_editor.rs

## VERIFY
1. Paste the zero-call-site grep for all four BEFORE you change anything.
2. Perturbation, one per fix: sever the new call site, watch the pin go red verbatim, restore, `touch`,
   green. Four loops, four REDs pasted.
3. `hcargo xtask mk ci-local-leptos`, then `hcargo xtask platform wave gate --slice T-946.86`.
4. **The acceptance instrument is a grep**: every function named above must have >= 1 production call
   site, listed in the report with file:line. That is the only assertion distinguishing this repair
   from the state it repairs.
## RULES
1. **Verify the defect on main FIRST** and paste the RED. A string match is not proof — open the file.
2. **Perturbation proof, every fix**: break it deliberately, paste the RED verbatim, restore, `touch`
   the file, confirm green. A restored file can be OLDER than the artifact built from the perturbed
   version, so the GREEN half of the loop can be stale — `touch` is not optional.
3. Tests go at the **BOTTOM** of their file. `class_r_scrub::live_source` cuts from the FIRST
   `#[cfg(test)]` to EOF, so anything above it is invisible to the pin. This has bitten in three
   consecutive waves (T-937.1 wave 252, T-937.4 wave 253, and again in 255).
4. Explicit git paths. **NEVER `git add -A`. NEVER `git stash`** — it deletes LFS pointer files.
   Commit early and often; an outage leaves reviewed work behind.
5. `hcargo xtask platform wave gate --slice T-946.86` from the worktree; must end `SLICE GATE: PASS`.
6. No `cargo xtask ci ci-local`. No `mk leptos-gates` unless this brief names it.
7. No subagents. No ship, no merge, no push. Leave :3000 and :8080 up.
8. **No silent deferrals.** If the work needs a file outside your owns, STOP and report which file and
   why. Do not widen on your own.
9. Your report is evidence, not testimony. Every claim of the form "this already works" or "a sibling
   fixed it" must carry the grep or the commit that proves it. A wave-255 report claimed a UI button
   had been added; the commit contains none.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-946.86.md` in the worktree.
Branch committed and clean. No push.
