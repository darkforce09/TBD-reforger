You are the slice agent for **T-257** (Undo scope misses loadouts, items, objectives, markers).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-257  branch slice/T-257
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-257`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-257
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-257 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-257. Then prove the defect.
## THE JOB
`MissionDocCore::hydrate` clears `loadouts`, `items`, `objectives`, and `markers`, but `store.rs` constructor
`expand_scope` covers slots, squads, factions, editor_layers, meta, vehicles, entities, zones, compositions,
triggers, comments, and connections only.
The four roots (`loadouts`, `items`, `objectives`, `markers`) are currently not in `expand_scope`, making any
local user mutation to them non-undoable.
Add all four roots to `MissionDocCore`'s `expand_scope` in `from_doc_with_clock`, keep them cleared outside
history in `hydrate` (under `INIT_ORIGIN` / `set_origin_init(true)`), and provide a test per root proving
that local mutate-then-undo and redo round-trip accurately.

## ANCHORS (verified 2026-09-07 — the ticket's are stale)
- `crates/map-engine-core/src/doc/store.rs:151-240`: `MissionDocCore` struct fields. Add `loadouts: MapRef`,
  `items: MapRef`, `objectives: MapRef`, `markers: MapRef` as fields on `MissionDocCore`.
- `crates/map-engine-core/src/doc/store.rs:431-484`: `from_doc_with_clock` constructor. Instantiate the four
  maps via `doc.get_or_insert_map(...)`, call `undo_mgr.expand_scope(&doc, &...)` for each, and store them on `Self`.
- `crates/map-engine-core/src/doc/store.rs:3544-3580`: `hydrate` clear loop. Already fetches and clears them;
  ensure it reuses the stored `Self` map handles.
- `crates/map-engine-core/src/doc/store.rs:5537`: mid-file `#[cfg(test)] fn new()` inside `impl RemintMap`.
- `crates/map-engine-core/src/doc/store.rs:6963`: main `#[cfg(test)] mod tests` start.

## TRAPS THAT WILL BITE THIS SLICE — CRITICAL
- **Class-R haystack truncation (four waves running):**
  `class_r_scrub::live_source` uses `cut_test_module`, which cuts from the FIRST `#[cfg(test)]` to EOF.
  `doc/store.rs` already has a mid-file one at line 5537 with production code below it.
  Any probe searching for production items sees an empty haystack if you add tests above production code.
  **Probe the call site, never the definition!**
  **Place all new tests at the very bottom of `crates/map-engine-core/src/doc/store.rs` inside `mod tests`**,
  below all production items.
- **Do not change existing pinned signatures**: `materialize(&self) -> SlotSoa` and public store accessors
  remain pinned.
- **Hydrate must remain outside history**: `hydrate` runs within `begin()` which observes `self.init_mode`.
  Ensure `hydrate` continues to use `INIT_ORIGIN` so clearing does not pollute undo history.

## YOUR FILES (touch ONLY these)
crates/map-engine-core/src/doc/store.rs

## SIBLINGS THIS PACK (do NOT touch)
T-242 flatten.rs, mission.schema.json ·
T-937.5 mission-editor-payload.schema.json, mission_library.rs, operations.rs, slot_ids.rs ·
T-939.1 outliner_tree.rs, outliner_drag.rs, panels/mod.rs ·
T-939.3 overlays.rs, gestures.rs, gizmo_z.rs, canvas/mod.rs

## RULES
1. Verify defect first: write tests asserting mutate-then-undo on the four roots restores initial state;
   observe they fail because the roots are not undo-scoped; paste the RED verbatim.
2. Perturbation: drop one of the four `expand_scope` calls; its test must go RED; restore, `touch`, green.
3. `hcargo test -p map-engine-core --all-features doc::store`.
4. Explicit git paths. NEVER `git add -A`, NEVER `git stash`. Commit early and often.
5. `hcargo xtask platform wave gate --slice T-257` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`.
7. No subagents. No ship/merge/push. Leave :3000 and :8080 up.
8. No silent deferrals.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-257.md` in the worktree.
Branch committed and clean. No push.
