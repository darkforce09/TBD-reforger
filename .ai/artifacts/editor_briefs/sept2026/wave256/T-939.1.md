You are the slice agent for **T-939.1** (Outliner: multi-select drag between layers).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-939.1  branch slice/T-939.1
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. For any
other variable use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-939.1`  — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-939.1
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-939.1 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-939.1. Then prove the defect.
## THE JOB
The outliner drags the whole multi-selection at once instead of only a single row:
1. New `apps/website/frontend/src/editor/panels/outliner_drag.rs` (registered in `panels/mod.rs`):
   Define `DragSet { anchor: String, ids: Vec<String> }`, plus pure drop planning functions that preserve
   relative item order and reject dropping a parent/container into any of its dragged children.
   Keep `outliner_drag.rs` free of wasm-only types so its order and rejection rules are covered by native unit tests.
2. `outliner_tree.rs:1110` (`begin_layer_slot_drag`) and its peers (:664 `begin_layer_comment_drag`,
   :1022 `begin_layer_drag`, :1101 `begin_refile`): pass the full selection set when the pressed row is part of
   the current selection; if the pressed row is not selected, drag only the single pressed row.
3. Multi-item drop applies within one doc-store transaction so a single Ctrl+Z reverts the entire move.
4. Drag ghost shows a count badge when `ids.len() > 1`.

**FOLD IN T-946.69 (Tactical draw path unreachable):**
T-936.7 shipped `begin_tactical_draw(kind)` in `operations/tactical_graphics.rs`, but it currently has ZERO
call sites anywhere in the codebase.
Because T-939.1 owns `panels/mod.rs` and `outliner_tree.rs`, fold in a UI control button (e.g. in the outliner
header/toolbar) that invokes `crate::editor::state::operations::tactical_graphics::begin_tactical_draw("phase_line")`
(or opens tactical draw arming).
NOTE: Do NOT add a keyboard shortcut for this — `panels/help_modal.rs` runs keymap census tests that will fail
if a keymap is added without updating help_modal (outside your owned files). A UI button is safe and clean.

## ANCHORS (verified 2026-09-07 — the ticket's are stale)
- `apps/website/frontend/src/editor/panels/outliner_tree.rs:664`: `begin_layer_comment_drag` call.
- `apps/website/frontend/src/editor/panels/outliner_tree.rs:1022`: `begin_layer_drag` call.
- `apps/website/frontend/src/editor/panels/outliner_tree.rs:1032`: `complete_layer_drop_onto_folder` call.
- `apps/website/frontend/src/editor/panels/outliner_tree.rs:1101`: `begin_refile` call.
- `apps/website/frontend/src/editor/panels/outliner_tree.rs:1110`: `begin_layer_slot_drag` call.
- `apps/website/frontend/src/editor/panels/mod.rs`: module registrations (lines 6-50). Register `pub mod outliner_drag;`.
- `apps/website/frontend/src/editor/state/operations/tactical_graphics.rs:188`: `pub fn begin_tactical_draw(kind: &str) -> bool`.

## TRAPS THAT WILL BITE THIS SLICE
- **Native vs wasm32 tests**: `outliner_tree.rs` has wasm32 DOM bindings, but pure drag planning logic in
  `outliner_drag.rs` should be non-wasm so unit tests execute under `hcargo test -p website-frontend`.
- **Do not desync selection during drag**: DragSet must capture the IDs at pointerdown; mid-drag operations must not mutate
  the list out from under the in-flight gesture.
- Drop onto self: dragging a folder into itself or its own descendants must be cleanly rejected.

## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/panels/outliner_tree.rs
apps/website/frontend/src/editor/panels/outliner_drag.rs   (new)
apps/website/frontend/src/editor/panels/mod.rs

## SIBLINGS THIS PACK (do NOT touch)
T-242 flatten.rs, mission.schema.json ·
T-937.5 mission-editor-payload.schema.json, mission_library.rs, operations.rs, slot_ids.rs ·
T-257 doc/store.rs ·
T-939.3 overlays.rs, gestures.rs, gizmo_z.rs, canvas/mod.rs

## RULES
1. Verify defect first: dragging with 2 selected rows currently moves only the clicked row; paste the RED verbatim.
2. Perturbation: drop the `ids` tail in `DragSet`; multi-row move fails; restore, `touch`, green.
3. `hcargo test -p website-frontend`.
4. Explicit git paths. NEVER `git add -A`, NEVER `git stash`. Commit after every self-contained step.
5. `hcargo xtask platform wave gate --slice T-939.1` from the worktree; must end `SLICE GATE: PASS`.
6. No `ci-local`, no `leptos-gates`.
7. No subagents. No ship/merge/push. Leave :3000 and :8080 up.
8. No silent deferrals.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-939.1.md` in the worktree.
Branch committed and clean. No push.
