You are the slice agent for **T-939.2** (Attributes: batch faction and squad reassign).
WORKTREE ONLY: /run/media/system/Disk_2/Projects/TBD-Reforger/.ai/artifacts/worktrees/T-939.2  branch slice/T-939.2
## ENVIRONMENT — READ IT, IT IS NOT THE HOST
You are inside the **claude-desktop container, glibc 2.36**. The shared warm cache
/home/Samuel/.cache/tbd-target is **HOST-stamped (glibc 2.43)**. Running bare `cargo` against it is
the GLIBC_2.xx trap (T-853) and it reads exactly like a broken tree. Every cargo and rustfmt call
goes through the host bridge:
    /home/Samuel/.cache/tbd-bin/hcargo    <cargo args>
    /home/Samuel/.cache/tbd-bin/hrustfmt  <rustfmt args>
hcargo forwards ONLY CARGO_TARGET_DIR, CARGO_BUILD_JOBS, TEST_DATABASE_URL, TBD_IT_BASE_DB. Any other
variable is SILENTLY DROPPED — use the explicit form:
    distrobox-host-exec env PATH=/home/Samuel/.cargo/bin:/home/Samuel/.local/bin:/usr/local/bin:/usr/bin:/bin \
      CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target <VAR=value ...> cargo <args>
Gate:  `hcargo xtask platform wave gate --slice T-939.2` — the driver picks its own target dirs, so its
"ignoring inherited CARGO_TARGET_DIR" and "this is the HOST shell … distrobox-host-exec … refuses
outside a container" notes are **EXPECTED output, not errors**.
Run the gate DETACHED and poll its log: `setsid nohup <cmd> > gate.log 2>&1 &`. **Match only
`^SLICE GATE:` (or `^GATE: (PASS|FAIL)`). NEVER grep the log for `refus` or `error`** — the benign
NOTE above contains "refuses" and will trip a naive waiter twice.
Private run dir if you build a binary you then RUN: CARGO_TARGET_DIR=/home/Samuel/.cache/tbd-target-T-939.2
(delete on cleanup, and grep the binary for a string unique to your version before trusting it).
Worktree LFS pointers: `hcargo xtask schema validate`, never `ci schema-validate`.
FIVE slices run concurrently this wave on one shared target dir. Prefer
`hcargo xtask platform wave test --slice T-939.2 -p <pkg>` (private dir) over bare `hcargo test`, and
never believe a green that shows no `Compiling`/`Checking <crate>` line — that is T-596, a replayed
cached verdict, and it gets likelier with five contenders.

FIRST: cd worktree; `pwd && git branch --show-current` must be slice/T-939.2. Then prove the defect.

## THE JOB
The Attributes modal gains a **faction selector** and an **editable squad picker** that apply to the
WHOLE selection in one undo group, through a new `reassign_slots(ids, target)` in
`state/operations/reassign.rs`.

## ANCHORS (verified 2026-09-08 against main — all exact)
- `panels/attributes_modal.rs:1864-1872` renders the squad READ-ONLY (`"{n} entities"` under a
  multi-selection, else the squad name or `—`), painted as an inert div at `:1924-1929` inside a
  `<label>` whose span reads `"Squad"`.
- **There is no faction control at all.** Every faction hit in the file — `:1546`, `:1547`, `:1552`,
  `:1607` (`return "Faction default"`), `:1749`, `:1760`, `:1842`, `:1881` — belongs to `type_picker`
  (fn at `:1583`), which edits `assetId`, not faction. A Class-R pin sits at `:3315-3318`.
- **The multi-selection is already in hand**: `attrs_multi_ids(open_id)` (`operations/attrs.rs:488-513`)
  is called at `attributes_modal.rs:342` and its result becomes `targets: StoredValue<Vec<String>>` at
  `:408-412`, passed into `identity_tab(targets, …)` at `:522` — the same tab that renders the
  read-only Squad field. Siblings: `attrs_selection_len()` `attrs.rs:519`, `attrs_locked_count`
  `attrs.rs:223`.
- `with_batch<F,R>(label, f)` is `state/operations/batch.rs:11` (module `#![cfg(target_arch = "wasm32")]`
  at `:4`), registered `operations.rs:34` and re-exported `operations.rs:46`. Reachable from
  `attributes_modal.rs` as `crate::editor::state::operations::with_batch` and from `reassign.rs` as
  `super::batch::with_batch` (batch.rs itself does `use super::context::OPS_CTX;` at `:6`).
- Readable from a new sibling module with no edits elsewhere: `OPS_CTX` (`operations/context.rs:118`,
  `pub(super)`), `faction_rows` `:528`, `squad_rows` `:559`, `slot_rows` `:605`;
  `orbat_manager_snapshot()` (`entity.rs:1995`) returning `{factions, squads, slots}`, where
  `FactionRow` (`panels/outliner.rs:343-350`) carries `key` and ordered `squad_ids` — exactly what the
  picker and the cross-faction refusal need. `entity::orbat_add_squad(side)` `entity.rs:2132` is `pub`.
- Core moves: `MissionDocCore::move_slot_to_squad` **`store.rs:1138`** (doc `:1135-1137`: updates both
  `slotIds` arrays, rewrites dense `index` 0..n-1, promotes/GCs source leader). Existing frontend
  wrapper `refile_slot(slot_id, dest_squad_id)` `entity.rs:3145` (core call `:3156`).
  `slotIds` is a yrs `YArray`; constant `SLOT_IDS` at `crates/map-engine-core/src/doc/id_arrays.rs:16`.

## TWO CORRECTIONS TO THE TICKET — both measured, both already written into it
1. **SIDE KEYS ARE DERIVED, NOT STORED.** `resolve_slot_side_key` (`store.rs:6941-6966`) walks
   `slot.squadId → squad.factionId → faction.key`, defaulting `"BLUFOR"` at every missing hop;
   materialised into `SlotSoa.side_keys` (`store.rs:965-976`) and memoised per-squad by `SideKeyMemo`
   (`store.rs:360`, `:439`, `:923`). Nothing writes a side key. So the requirement is that the derived
   key FOLLOWS the move and the memo is invalidated — and the ticket's old perturbation step ("skip the
   side-key update") had nothing to skip.
2. **`store.rs` IS IN YOUR OWNS FOR ONE REASON.** `move_slot_to_squad` calls
   `garbage_collect_squad_in_txn` at `:1187-1196`, and that function (`:5367-5399`) **deletes the squad
   row** (`:5398`), **prunes it out of `faction.squadIds`** (`:5390-5397`) and **deletes every attached
   vehicle** (`:5384-5389`). `ensure_leader_invariant_in_txn` (`:5402-5412`) does the same on the dest
   side. The full public squad surface is `store.rs:1101, :1107, :1116, :1138, :1209, :1335, :1380` —
   there is no non-GC path. Requirement 3 ("an emptied squad stays") is unsatisfiable without an
   **ADDITIVE keep-source entry point** in `store.rs`. Add one. **Do not change the existing default**
   — other callers depend on the GC.
   The workaround of re-`add_squad`-ing the emptied squad from `reassign.rs` was considered and
   REJECTED: it still loses the attached vehicles, loses `leaderSlotId`, and re-appends to the tail of
   `faction.squadIds` (`add_squad:1074`) instead of restoring position.

## TRAPS THAT WILL BITE THIS SLICE
- `store.rs` is huge and shared. T-932 owns it next wave; keep your change additive and small so that
  slice does not inherit a merge surface.
- `operations.rs` is a facade — register with `pub mod reassign; pub use reassign::*;` next to the
  existing registrations at `:33-44`. **T-946.86 runs this wave and was told NOT to touch
  `operations.rs` because it is yours.**
- `reassign.rs` must compile on `wasm32-unknown-unknown`; the batch module is wasm-only.
- Tests at the BOTTOM of each file — `class_r_scrub::live_source` truncation.

## YOUR FILES (touch ONLY these)
apps/website/frontend/src/editor/panels/attributes_modal.rs
apps/website/frontend/src/editor/state/operations/reassign.rs   (new)
apps/website/frontend/src/editor/state/operations.rs
crates/map-engine-core/src/doc/store.rs

## SIBLINGS THIS PACK (do NOT touch)
T-212 mod Objectives .c both trees, xtask/src/schema_gates.rs, mission.schema.json ·
T-946.55 TBD_ObjectivesComponent.c both trees ·
T-939.4 panels/{context_menu,top_strip,help_modal}.rs, editor/mission_editor.rs ·
T-946.86 canvas/{gestures,overlays}.rs, panels/{outliner_tree,outliner_drag,zones_panel}.rs, state/commands_hotkeys.rs

## VERIFY
1. Verify on main: a test asserting the modal offers no faction control and no editable squad control;
   paste the RED.
2. Unit tests over a doc fixture: moves, cross-faction refusal with a named reason, **emptied squad
   survives WITH its attached vehicles and its position in `faction.squadIds`**, derived side key
   follows the move.
3. Perturbation: route the batch through the GC-ing `move_slot_to_squad` instead of your keep-source
   path; the emptied-squad test must go red on the deleted vehicles; restore, `touch`, green.
4. `hcargo xtask mk ci-local-leptos`, then `hcargo xtask platform wave gate --slice T-939.2`.
## RULES
1. **Verify the defect on main FIRST** and paste the RED. A string match is not proof — open the file.
2. **Perturbation proof, every fix**: break it deliberately, paste the RED verbatim, restore, `touch`
   the file, confirm green. A restored file can be OLDER than the artifact built from the perturbed
   version, so the GREEN half of the loop can be stale — `touch` is not optional.
3. Tests go at the **BOTTOM** of their file. `class_r_scrub::live_source` cuts from the FIRST
   `#[cfg(test)]` to EOF, so anything above it is invisible to the pin. This has bitten in three
   consecutive waves (T-937.1 wave 252, T-937.4 wave 253, and again in 255).
4. Explicit git paths. **NEVER `git add -A`. NEVER `git stash`** — it deletes LFS pointer files.
   Commit early and often; an outage leaves reviewed work behind.
5. `hcargo xtask platform wave gate --slice T-939.2` from the worktree; must end `SLICE GATE: PASS`.
6. No `cargo xtask ci ci-local`. No `mk leptos-gates` unless this brief names it.
7. No subagents. No ship, no merge, no push. Leave :3000 and :8080 up.
8. **No silent deferrals.** If the work needs a file outside your owns, STOP and report which file and
   why. Do not widen on your own.
9. Your report is evidence, not testimony. Every claim of the form "this already works" or "a sibling
   fixed it" must carry the grep or the commit that proves it. A wave-255 report claimed a UI button
   had been added; the commit contains none.

## REPORT
pwd_branch · defect_verified · changes · perturbation (RED verbatim) · gate_verdict_tail ·
files_outside_owns [] · found_not_fixed · deviations · commits · manual_checklist
Write `.ai/artifacts/editor_briefs/sept2026/wave256/REPORT-T-939.2.md` in the worktree.
Branch committed and clean. No push.
