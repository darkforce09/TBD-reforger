id = "T-819"
kind = "work"
title = "Crewed slots hide from map while assigned"
summary = "Crewed slots render inside the vehicle — figures leave the map while assigned"
class = "chore"
status = "shipped"
order = 5010
executor = "claude-code"
notes = """
eye-pass wave-202; pairs with T-818 in wave 210; render lane only | SHIPPED W210: derived map_render_slot_soa hide from crew assignment; materialize/compile/outliner/selection intact; mission_history wire disclosed. Completion: scrub-anchor split + T-802 pin retarget (ddbdcddd/00eea875) after gate red on duplicated MissionEditorPage literal.

--- HEAD migration_legacy (verbatim) ---

OPERATOR EYE-PASS FINDING (wave-202 pass, 2026-08-10). Slots assigned as vehicle crew keep rendering as standing figures next to the vehicle — they should be IN it (Eden hides crewed units). DIRECTION: render-lane hide DERIVED from crew assignment — a slot referenced by any placed vehicle's crew list is excluded from the map render SoA (figure + label), the T-701 editorHidden RENDER shape. ⚠️ HARD TRAPS (shipped defect families live exactly here): (1) derived state, NO new document flag — the crew assignment IS the state; unassign or vehicle delete restores visibility automatically; (2) NEVER drop crewed slots from materialize()/compile — they are crew and must compile; the T-665/T-701 materialize() drops are for operator-hidden rows, do NOT reuse them for crewed; (3) the wave-144 id-mint class: any code sourcing ids from a FILTERED view re-mints hidden ids — mint/read from slots_json only; (4) the wave-145 selection lesson: crewed slots stay outliner-reachable and selection must NOT prune them for being invisible — invisible is not gone; map-click cannot hit them (nothing rendered) but outliner click must select. ACCEPTANCE: scripted — assign 2 slots as Driver/Gunner: their figures + labels leave the map (SoA row count -2), outliner rows remain and select, compiled export still contains both with crew refs; unassign one -> figure returns at its stored position with its stored z (exact f64); delete the vehicle -> both return; undo of an assignment round-trips visibility.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["OPERATOR EYE-PASS FINDING (wave-202 pass, 2026-08-10). Slots assigned as vehicle crew keep rendering as standing figures next to the vehicle — they should be IN it (Eden hides crewed units). DIRECTION: render-lane hide DERIVED from crew assignment — a slot referenced by any placed vehicle's crew list is excluded from the map render SoA (figure + label), the T-701 editorHidden RENDER shape. ⚠️ HARD TRAPS (shipped defect families live exactly here): (1) derived state, NO new document flag — the crew assignment IS the state; unassign or vehicle delete restores visibility automatically; (2) NEVER drop crewed slots from materialize()/compile — they are crew and must compile; the T-665/T-701 materialize() drops are for operator-hidden rows, do NOT reuse them for crewed; (3) the wave-144 id-mint class: any code sourcing ids from a FILTERED view re-mints hidden ids — mint/read from slots_json only; (4) the wave-145 selection lesson: crewed slots stay outliner-reachable and selection must NOT prune them for being invisible — invisible is not gone; map-click cannot hit them (nothing rendered) but outliner click must select. ACCEPTANCE: scripted — assign 2 slots as Driver/Gunner: their figures + labels leave the map (SoA row count -2), outliner rows remain and select, compiled export still contains both with crew refs; unassign one -> figure returns at its stored position with its stored z (exact f64); delete the vehicle -> both return; undo of an assignment round-trips visibility."]

missing=0
"""
main_goal = "Derive map render hide for crew-assigned slots; figures leave map."
context = [
    "OPERATOR EYE-PASS wave-202; crew-assigned slots render standing figures beside vehicle.",
    "Eden hides crewed units; slots should be inside vehicle not on map.",
    "Render-lane hide derived from crew assignment not new document flag.",
]
requirement = [
    "Exclude crew-referenced slots from map render SoA figure plus label.",
    "Use T-701 editorHidden RENDER shape; unassign or vehicle delete restores visibility.",
    "Never drop crewed slots from materialize compile; outliner selection must remain.",
]
current_state = [
    "Wave-144 id-mint class: filtered-view ids re-mint hidden ids.",
    "Wave-145 selection: crewed slots outliner-reachable; invisible not gone.",
    "Map-click cannot hit hidden figures; outliner click must select.",
]
approach = [
    "Derive visibility from crew assignment; mint read slots_json only not filtered view.",
    "Do not reuse T-665 T-701 materialize drops for crewed rows.",
]
verify = [
    "Assign Driver Gunner: figures labels leave map SoA count minus two.",
    "Outliner rows remain selectable; compiled export contains both crew refs.",
    "Unassign one: figure returns stored position and z exact f64.",
    "Delete vehicle: both return; undo assignment round-trips visibility.",
]
acceptance = [
    "Crew-assigned slots dropped from map render SoA; figure and label counts fell while outliner rows stayed.",
    "Compiled export still contained both slots with crew refs; materialize and compile paths kept crewed rows.",
    "Unassign or vehicle delete restored figures at stored position and exact z; undo round-tripped visibility.",
]
citations = [
    "T-701 editorHidden RENDER shape",
    "T-665 T-701 materialize drops trap",
    "wave-144 id-mint filtered view",
    "wave-145 selection invisible not gone",
    "00eea875 map_render_slot_soa",
]
shipped_at = "00eea875"
created_at = "2026-08-10T00:12:13Z"
completed_at = "2026-08-11T03:00:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/editor_ops.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "map_canvas",
    "ops_undo",
]
