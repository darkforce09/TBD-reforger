id = "T-662"
kind = "work"
title = "Free right-click and Backspace input traps"
summary = "Free right-click and Backspace — the two input traps that gate the program"
class = "bug"
status = "shipped"
order = 3810
executor = "claude-code"
notes = "editor-UI program wave 1 (wave_plan.tsv row 101) — SHIPPED wave 101 (editor factory), merge 421eae91; verifier wave101.md"
depends_on = ["T-661"]
main_goal = "Fix RMB pan trap and Backspace hide-chrome binding in mission_editor."
context = [
    "Two fixes same file mission_editor.rs; two agents cannot share it.",
    "Unblocks T-664 context menu and six tickets behind it.",
]
requirement = [
    "Narrow pointerdown pan from MMB+RMB to MMB only button equals one.",
    "Remove oncontextmenu prevent_default blanket suppression.",
    "Drop Backspace alias to Delete; bind Backspace to hide-chrome signal.",
    "chrome_hidden signal gates sibling mounts lines 2060-2110.",
]
current_state = [
    "RMB pan at onpointerdown 1402; Backspace Delete alias at 1027-1029.",
    "Ctrl+F asset search deliberately out of scope owns_parity 5.4 item four.",
]
approach = ["Fix pointerdown branch; remove contextmenu prevent_default; add chrome_hidden."]
verify = ["RMB opens context path; Backspace hides chrome not deletes selection."]
citations = [
    "mission_editor onpointerdown pan",
    "oncontextmenu prevent_default comment",
    "T-664 context menu unblock",
]
shipped_at = "421eae91"
created_at = "2026-08-02T00:02:00Z"
completed_at = "2026-08-02T00:05:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/mission_editor.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
