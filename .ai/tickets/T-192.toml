id = "T-192"
kind = "work"
title = "Mission settings never reach compiled document"
summary = "Mission settings never reach the compiled document and are silently reverted"
class = "docs"
status = "shipped"
order = 2100
executor = "claude-code"
main_goal = "The author's edit is dropped on the wire and lost locally on reload."
context = ["MERGED FROM T-089: this ticket owns BOTH halves —"]
requirement = [
    "add the PATCH /missions/{id} path that writes time_of_day/weather back to the missions row,",
    "AND make the compile path read the authored value.",
    "Do not ship one without the other.",
]
current_state = [
    "The dialog writes meta.environment.{time,weather} into the CRDT doc (eden_chrome.rs:1707-1765)",
    "but flatten reads the DB missions row instead (mission_compile.rs:24-29).",
    "No editor path PATCHes the row,",
    "and apply_row_meta overwrites the doc from the row on every hydrate.",
    "The author's edit is dropped on the wire and lost locally on reload.",
]
approach = [
    "add the PATCH /missions/{id} path that writes time_of_day/weather back to the missions row,",
    "AND make the compile path read the authored value.",
]
verify = [
    "cargo test -p website-api --lib authored_environment_beats_a_stale_mission_row.",
    "Dev stack /compiled serves authored time and weather.",
]
acceptance = ["PATCH mirrored row; compile read payload environment; scrubber parsed HH:MM:SS; bad values rejected."]
citations = [
    "eden_chrome.rs:1707-1765",
    "mission_compile.rs:24-29",
    "T-089",
    "Source: commit bbb910be — row mirror compile.",
]
shipped_at = "bbb910be"
created_at = "2026-07-26T00:13:01Z"
completed_at = "2026-07-26T00:50:47Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_chrome.rs",
    "apps/website/api/src/services/mission_compile.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["layout_chrome"]
