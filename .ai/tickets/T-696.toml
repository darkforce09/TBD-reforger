id = "T-696"
kind = "work"
title = "Map bookmarks and a named-locations index"
summary = "Map bookmarks and a named-locations index"
class = "feature"
status = "shipped"
order = 4500
executor = "claude-code"
notes = """
— SHIPPED wave 116 (editor factory), 032513af; verifier wave116.md. Locations tab (named places re-read through the SAME url and parser as LabelHost::init, nothing hardcoded) plus persistent camera bookmarks storing x/y/zoom, not undoable and not document edits. ⚠️ TWO DEFECTS: its headline source pins are HOLLOW (see T-759) and fly_to rides the T-166 smoke hook in production (see T-762).

--- HEAD migration_legacy (verbatim) ---

NEW-F7 + 3den E1. The cheapest large win in the 3den sweep: a 12.8 km map with zero bookmarks today. TBD ALREADY HAS THE DATA for the locations half — world_assets/labels.rs and the Town labels layer (world_layer_prefs.rs) carry named places; nothing lets you list or fly to one. Eden has a Locations tab beside Entities.

missing=0"""
main_goal = "Ship map bookmarks and named-locations index with fly-to."
context = [
    "NEW-F7 3den E1 cheapest large 3den sweep win.",
    "12.8 km map with zero bookmarks today.",
]
requirement = [
    "Locations tab listing named places with fly-to.",
    "Persistent camera bookmarks storing x/y/zoom.",
]
current_state = [
    "world_assets/labels.rs and Town labels carry named places.",
    "Nothing lets you list or fly to one.",
    "Eden has Locations tab beside Entities.",
]
citations = [
    "NEW-F7 3den E1",
    "world_assets labels.rs",
    "world_layer_prefs Town labels",
]
shipped_at = "5b945fce"
created_at = "2026-08-07T05:18:24Z"
completed_at = "2026-08-08T00:52:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/frontend/src/eden_dock_left.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_left"]
