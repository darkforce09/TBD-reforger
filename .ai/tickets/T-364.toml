id = "T-364"
kind = "work"
title = "Telemetry COALESCE third state; CMS snippet stale"
summary = "COALESCE-on-None admits a third state, and a body-only PATCH leaves the snippet stale"
class = "bug"
status = "shipped"
order = 3180
executor = "claude-code"
notes = """

--- migration_legacy (verbatim) ---

Two narrow findings from T-348, both precise about mechanism rather than pattern-matched.

1. **telemetry.rs:488-498 and :511, :522-523.** The mechanism here is NOT `is_empty()` — it is SQL COALESCE-on-NULL keyed on Rust `None`, and per the doc comment at :259-263 an explicit `""` clear is INTENTIONAL. So the defect is narrower than the family it looks like: `Some("   ")` is non-NULL, so COALESCE admits a THIRD state that is neither keep nor clear. Fix it without breaking the deliberate two-state contract T-316 designed — read that reasoning first; T-316 chose COALESCE for heartbeats specifically because a partial heartbeat is honest.

2. **cms.rs — `snippet` goes stale after a body-only PATCH.** Create derives it via `snippet_from`; PATCH only writes it when explicitly supplied. So the list view keeps the OLD snippet under a NEW body — a preview that contradicts the article. Either derive it on PATCH whenever `body` changes, or stop storing it and derive on read. Deriving on write is probably right (the list view should not pay for it), but say which and why.

== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "Close telemetry COALESCE third-state gap for whitespace clears and derive CMS snippet on body-only PATCH so list preview matches article."
context = ["Two narrow findings from T-348, both precise about mechanism rather than pattern-matched."]
requirement = [
    'Fix telemetry Some("   ") third state without breaking deliberate two-state COALESCE contract T-316 designed.',
    "Read T-316 reasoning first — COALESCE chosen for heartbeats because partial heartbeat is honest.",
    "Derive CMS snippet on PATCH whenever body changes, or derive on read — say which and why.",
]
current_state = [
    "telemetry.rs:488-498 and :511, :522-523 — mechanism is SQL COALESCE-on-NULL keyed on Rust None.",
    'Doc comment at :259-263: explicit "" clear is INTENTIONAL.',
    'Some("   ") is non-NULL so COALESCE admits THIRD state — neither keep nor clear.',
    "cms.rs snippet goes stale after body-only PATCH — list view keeps OLD snippet under NEW body.",
    "Create derives snippet via snippet_from; PATCH only writes when explicitly supplied.",
]
approach = ["Deriving on write probably right — list view should not pay for it on read."]
citations = [
    "T-348 narrow mechanism findings",
    "T-316 COALESCE heartbeat contract",
    "telemetry.rs:488-498",
    "cms.rs snippet_from",
]
shipped_at = "522126db"
created_at = "2026-07-27T12:53:24Z"
completed_at = "2026-07-27T13:16:25Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/telemetry.rs",
    "apps/website/api/src/handlers/cms.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
