id = "T-939.4"
kind = "work"
parent = "T-939"
title = "Arrange tools in context menu with shortcuts"
summary = "Align and distribute appear in the canvas context menu and get keyboard chords in the editor keydown, with the top-strip Arrange menu as the shared source of entries."
class = "feature"
status = "ready"
order = 7890
priority = 2
spec = "docs/specs/audit_2026_09/t939_editor_usability.md"
plan = "docs/plans/t-939_4_plan.md"
executor = "claude-code"
main_goal = "Arrange tools are reachable by right-click and by keyboard."
context = [
    "Anchor 2026-09-04: panels/top_strip.rs:249-252 holds the Arrange menu (T-645), gated on selection >= 1 at :378-380; panels/context_menu.rs lacks these entries.",
    "The editor keymap is the wasm keydown closure in editor/mission_editor.rs (allowlisted SIZE-3 file); new arms stay thin callers of shared invokers.",
    "No file collides with T-939.1; depends_on keeps this slice after it so the context menu builds on the settled multi-select behaviour.",
    "Related, not re-minted: T-704 command palette.",
    "2026-09-08 recovery: editor behavior smokes and independent targeted checks pass; required v-suite fails 21/25 routes. Wave acceptance remains blocked.",
    "Unchanged captures produce identical normalized DOM on a6164c4d0 and repaired 514db787 across all 25 routes, twice each. These visual failures predate recovery.",
    "Repair missing CMS/leave-request fixtures returning invalid 200 {} envelopes and Server Control ignoring T-385's terrain field. Preserve desired shipped application behavior.",
    "Approvals also needs both seeded pending-mission detail fixtures. Preserve Cold Anvil's authored briefing and Paper Tiger's intentionally absent briefing; neither may render a fixture decode failure.",
    "Server Intel's hardcoded intelligence events have no data source. Replace fabricated reports with an honest unavailable state while retaining actual server/status/environment/modpack information.",
    "Independent screenshot review found broken vehicle profile images. Failed image loads must use the existing missing-image fallback, preserve vehicle data, and reset correctly when selection changes.",
    "Mission Library hero, cards, and dossier also need a deterministic fallback when stored or placeholder artwork fails. Preserve valid artwork, URL safety, and layout dimensions.",
    "The populated Event Schedule clips its inline hub hero through flex shrinking. Measure and repair the hero locally; preserve standalone hub layout and shared SplitPane behavior.",
    "Accept only independently reviewed intentional shipped visual divergences through per-route accept, preserving React references and specific provenance. Never bless error UI or blanket rebaseline.",
]
requirement = [
    "top_strip.rs exposes the Arrange entries as one shared list (label, chord, invoker) consumed by both menus.",
    "context_menu.rs shows an Arrange submenu when the selection holds two or more entities.",
    "mission_editor.rs keydown gains chords for align left/right/top/bottom and distribute horizontal/vertical as thin callers.",
    "help_modal.rs lists the new chords.",
]
current_state = [
    "Arrange lives only in the top-strip menu; no context-menu entry; no chord; no help row.",
]
approach = [
    "Verify on main: a test asserting the context menu has no Arrange entry and the keydown ignores the align chord; paste the red.",
    "Extract the entry list in top_strip.rs; render it in context_menu.rs; add the keydown arms; add the help rows.",
    "Perturbation: drop one chord arm; the keydown test must go red; restore, touch, green.",
]
verify = [
    "cargo xtask mk ci-local-leptos",
    "cargo xtask mk leptos-gates",
    "cargo xtask platform wave gate --slice T-939.4",
]
acceptance = [
    "Right-clicking a multi-selection shows an Arrange submenu whose entries match the top-strip menu.",
    "Each Arrange chord performs the same operation as its menu entry and appears in the help modal.",
    "With a single entity selected the submenu is hidden and the chords are inert.",
]
depends_on = ["T-939.1"]
created_at = "2026-09-05T02:33:18Z"
owns = [
    "apps/website/frontend/src/editor/panels/context_menu.rs",
    "apps/website/frontend/src/editor/panels/top_strip.rs",
    "apps/website/frontend/src/pages/admin/server_control.rs",
    "apps/website/frontend/src/pages/public/server_intel.rs",
    "apps/website/frontend/src/pages/public/vehicles.rs",
    "apps/website/frontend/src/editor/library/mission_library.rs",
    "apps/website/frontend/src/pages/operations/event_hub.rs",
    "apps/website/frontend/tests/fixtures/api/GET__cms__announcements.json",
    "apps/website/frontend/tests/fixtures/api/GET__me__leave-requests.json",
    "apps/website/frontend/tests/fixtures/api/GET__admin__leave-requests.json",
    "apps/website/frontend/tests/fixtures/api/GET__missions__00000000-0000-4000-c000-000000000004.json",
    "apps/website/frontend/tests/fixtures/api/GET__missions__00000000-0000-4000-c000-000000000005.json",
    "tools/tbd-tools/fixtures/t159/oracle-freeze/",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["top_strip", "map_canvas"]
