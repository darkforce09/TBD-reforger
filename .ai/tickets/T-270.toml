id = "T-270"
kind = "work"
title = "Wire the Server Control page — ten dead controls"
summary = "Wire the Server Control page — ten dead controls"
class = "feature"
status = "shipped"
order = 2880
executor = "claude-code"
estimated = ["scope"]
migration_legacy = ["server_control.rs is 312 lines with zero on: handlers, and selected is bound to MOCK_SERVERS[0] as a compile-time constant. Restart, Stop, Launch, four Quick Actions, the RCON input and send are all inert, and there is no Start control at all. The console renders a fabricated log including a fake RCON listener line."]
owns = ["apps/website/frontend/src/server_control.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
