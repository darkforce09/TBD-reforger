id = "T-869"
kind = "work"
title = "T-853 port: export-terrain -> map export-terrain"
summary = "T-853 port: export-terrain -> map export-terrain"
class = "chore"
status = "shipped"
order = 5500
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/map-assets/export-terrain.sh (60 lines) to `cargo xtask map export-terrain` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). terrain export orchestrator; make map-export. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/map-assets/export-terrain.sh (60 lines) to `cargo xtask map export-terrain` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). terrain export orchestrator; make map-export. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0"""
main_goal = "Port export-terrain.sh to cargo xtask map export-terrain."
context = [
    "T-853 shell->xtask slice; 60-line bash script.",
    "Terrain export orchestrator; make map-export.",
    "Callers include Makefile platform wave.sh mod wave.sh workflows.",
]
requirement = [
    "Delete script shell-inventory.txt line python-inventory.txt line.",
    "Repoint every caller Makefile wave.sh workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/map-assets/export-terrain.sh bash gate live.",
    "Makefile wave.sh workflows still invoke bash script.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and two deliberately broken trees.",
    "T-556 anti-vacuity: passing run alone is not evidence.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Port diff matches clean tree byte-for-byte.",
    "Port diff matches at least two deliberately broken trees.",
]
acceptance = [
    "Deleted export-terrain.sh; map export-terrain xtask gate replaced terrain export orchestrator and make map-export.",
    "Inventory entries removed; Makefile and world aux.rs caller repointed to xtask.",
    "Port matched bash stdout stderr and rc on clean tree and deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "d25e9023 map export-terrain port",
]
shipped_at = "d25e9023"
created_at = "2026-08-12T06:13:35Z"
completed_at = "2026-08-12T06:46:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/map-assets/export-terrain.sh",
    "xtask/src/gate_export_terrain.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
