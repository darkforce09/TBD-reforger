id = "T-410"
kind = "work"
title = "ORDER BY ASC ratchets in integration suites"
summary = "Residual ORDER BY ASC ratchets in the integration suites, and the dashboard next_event coverage hole"
class = "bug"
status = "shipped"
order = 3231
executor = "claude-code"
priority = 1
main_goal = "Eliminate ORDER BY ASC integration ratchets and restore dashboard next_event test coverage."
context = [
    "Found by T-399 agent fixing approvals ratchet; measured on live tbd_gate_it.",
    "T-399 fixed ONE instance; these are rest of the class.",
    "ASC ordering over never-pruned table is the poison.",
]
requirement = [
    "Propagate misc_integration.rs tag-scoped boot_servers pattern.",
    "factions.rs unscoped DELETE FROM user_factions wipes concurrent sibling rows.",
]
current_state = [
    "missions.rs:151 GET /missions .any(id) — ORDER BY updated_at DESC NULLS FIRST.",
    "Ratchets at 20 missions with NULL updated_at; MEASURED 2 today.",
    "missions.rs:249 ?scope=bookmarked same handler; self-reinforcing on test fail.",
    "dashboard_reads.rs:83-85 next_event weakened to key-presence only.",
    "558 events live; dashboard next operation effectively untested.",
    "events.rs:837/850 ORDER BY start_time ASC LIMIT 20 — loaded gun at 558 rows.",
    "missions.rs:1002 approvals queue page 1 — MEASURED total 26, mission on page 2.",
]
approach = [
    "handlers/audit.rs:65 id DESC — residue pushes OLD rows off page.",
    "misc_integration boot_servers(tag) deletes only T235 {tag}%.",
    "Waves 3-5 passed only via fresh cold TBD_GATE_DB each time.",
]
citations = [
    "T-399 approvals ratchet",
    "T-411 per-wave DB names",
    "misc_integration boot_servers",
    "handlers/dashboard.rs next_event",
]
shipped_at = "facd5cd7"
created_at = "2026-07-27T07:29:23Z"
completed_at = "2026-07-27T07:31:05Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/api/tests/missions.rs",
    "apps/website/api/tests/dashboard_reads.rs",
    "apps/website/api/src/handlers/dashboard.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
