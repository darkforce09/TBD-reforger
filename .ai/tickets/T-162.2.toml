id = "T-162.2"
kind = "work"
title = "T-162 — Eradicate all remaining Python (→ Rust)"
summary = "Delete registry seed, backfill, and rewrite one-shot Python scripts."
class = "feature"
status = "shipped"
order = 1590
spec = "docs/platform/t162_python_eradication.md"
executor = "cursor-docs"
notes = "Delete seed/rewrite/backfill one-shots."
parent = "T-162"
context = ["T-161 left seed_registry and rewrite one-shots out of scope."]
requirement = ["Remove seed_registry, backfill-registry-monorepo, rewrite-doc-links, rewrite-ticket-paths."]
current_state = ["Four one-shot .py helpers still under scripts/ after T-161."]
approach = ["Delete one-shots; live ports already landed in xtask on T-162.1."]
verify = ["git ls-files 'scripts/lib/*.py' 'scripts/*registry*.py' 'scripts/rewrite*.py' empty."]
acceptance = ["Four one-shot migration .py scripts deleted from scripts/ per t162 eradication inventory."]
citations = [
    "Source: commit cac7b8fe — T-162 eradication.",
    "Source: t162_python_eradication.md — deleted inventory.",
]
created_at = "2026-07-16T00:00:00Z"
completed_at = "2026-07-17T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-162 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "xtask"
