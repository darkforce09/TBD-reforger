id = "T-569"
kind = "work"
title = "T-566 Class-R hollow — match arms in raw-string decoys"
summary = "T-566 Class-R still hollow — match arms only in raw-string decoys"
class = "feature"
status = "shipped"
order = 3422
executor = "claude-code"
priority = 2
main_goal = "Close Class-R hollow bypass via raw-string match arm decoys."
context = [
    "FOUND by W66 adversarial verifier (DIRTY MAJOR) after T-566.",
    "// and /* */ arm comments now RED; ignore-helper DEV_USER_ID bind RED.",
]
requirement = [
    "Class-R must RED live match `_` while role arms exist only in raw-string decoys.",
    "Comment strip must not preserve match arm needles inside r# strings.",
]
current_state = [
    "Live match collapses to `_` while arms exist only inside r# decoys.",
    "Comment strip keeps string contents; Class-R stays GREEN.",
]
approach = [
    "Extend Class-R to detect match arms parked in raw-string decoys.",
    "Reject `_ => DEV_USER_ID` live collapse when cfg arms are decoy-only.",
]
verify = ["Repro: park arms in raw-string decoy; match `_ => DEV_USER_ID`; Class-R green."]
citations = [
    "W66 adversarial verifier DIRTY MAJOR",
    "T-566 raw-string match arm decoy",
    "DEV_USER_ID bind ignore-helper RED",
]
shipped_at = "8352bc76"
created_at = "2026-07-28T22:30:36Z"
completed_at = "2026-07-28T22:38:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/api/tests/misc_integration.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
