id = "T-1001"
kind = "work"
title = "Mod wave gate calls make targets with no Makefile"
summary = "cargo xtask mod wave gate runs four make targets through distrobox-host-exec (mod_ops/wave_execution/execution.rs), but no Makefile is tracked, so those steps always fail. Call cargo xtask ci schema-validate, enf citations, enf capability and cargo xtask verify no-crf-leak directly."
class = "chore"
status = "idea"
notes = "Consequence for landing (TF15, doc writer F15b; paths under tools_v2/xtask/src/commands): 'cargo xtask mod wave land' merges every complete slice, then runs this gate before reaping (mod_ops/wave_execution/land.rs:57-65), so with the make steps always red it stops at 'Gate FAILED after merge' and never reaps worktrees or pushes (:67-75). Each merge also needs a green receipt from 'cargo xtask platform wave gate --slice <id>' (platform/slice_worktree/git_plain.rs:414-420, verdict::land_refusal). The make calls are at mod_ops/wave_execution/execution.rs:352, :357, :362 and :367."
created_at = "2026-09-23T18:32:58Z"

[scope]
domain = "repo"
layer = "docs"
