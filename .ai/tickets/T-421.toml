id = "T-421"
kind = "work"
title = "Shared cargo target dir false-green check verdicts"
summary = "BLOCKER: the wave gate's cargo check and clippy can PASS on source they never compiled"
class = "bug"
status = "shipped"
order = 3260
executor = "claude-code"
priority = 0
main_goal = "Give cargo check and clippy private target dirs or force full-workspace touch before check."
context = [
    "THE ALWAYS-BLOCKER CLASS — most serious finding of 2026-07-26 run.",
    "Found by wave 5 adversarial verifier; two slice agents measured same mechanism.",
    "wave.sh:517 reasoning wrong — exposure is mtime-based shared fingerprint, not binaries.",
]
requirement = [
    "Check step can return verdict about file it never opened.",
    "Cargo freshness test shares .fingerprint directory across worktrees.",
    "Gate verdict cannot be trusted without private target dirs or full touch.",
]
current_state = [
    "REPRO A: touch -r keeps mtime; cargo check rc=0 over unparseable Rust.",
    "REPRO B: main cargo check Finished 0.06s; sibling rmeta has foreign marker.",
    "touch_changed rebuilds wave-range and dirty files — partial defence, keep it.",
    "Wave 5 range touched only map-engine-core, frontend, xtask — rest stale.",
    "T-420 xtask ran binary tree cannot produce; T-244 restore left stale verdict.",
    "Third and fourth measured instance of T-193/T-195 clobbering.",
]
approach = [
    "Private dirs exist: target-gate-api 8.3G, target-gate-frontend 1.8G, target-gate-schema 1.9G.",
    "cargo check --workspace and three clippy steps still on shared dir.",
    "T-420 gate_schema stamp NOT complete model — see T-422.",
]
verify = [
    "REPRO A: append unparseable Rust, touch -r original mtime; cargo check rc=0.",
    "REPRO B: sibling worktree marker in rmeta; main grep src returns 0.",
]
citations = [
    "T-193 T-195 clobbering",
    "T-420 gate_schema stamp",
    "T-422 incomplete model",
    "wave.sh touch_changed",
]
shipped_at = "570893ba"
created_at = "2026-07-26T20:27:44Z"
completed_at = "2026-07-26T20:46:41Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
