id = "T-763"
kind = "work"
title = "Wave-116 false decoy claim plus three gaps"
summary = "Wave-116 residue: a false decoy claim to strike, plus three small gaps"
class = "feature"
status = "shipped"
order = 4639
executor = "claude-code"
notes = """
filed by wave-116 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

Four items from the wave-116 verifier, grouped because each is small. (1) A FALSE CLAIM TO STRIKE FROM THE RECORD: the T-069 slice reported that a pre-existing pin had been 'passing on a DECOY — the stub sentence quoted in a comment'. IT DOES NOT REPRODUCE. At base 619e5a14 the needle 'Marker placement lands in T-069.' occurs exactly twice: the live view stub (eden_dock_right.rs:1376) and the split-literal T-695 pin (:3354), which its own source cannot satisfy. No comment quoted the sentence and both pins were passing on the real stub. The pin INVERSIONS the slice performed were correct and preserved their original index claims — only the decoy narrative is wrong. Recorded here because an agent's chat summary is not an artifact, and this one was relayed onward before it was checked. (2) missions.rs:2358: the /compiled diagnostics headers are pinned by SOURCE SCAN only — there is no response-level test asserting `x-compile-diagnostics-count: 0` on a clean mission. Verified correct by reading (unconditional insert; HeaderValue::from_str over an integer cannot fail), so this is a missing test, not a defect. (3) Marker Attributes selects by id ALONE, so a hydrated foreign payload carrying the same marker id under two different factions would edit the first match. Unreachable through this editor's own minting, which always pairs (factionId, id). (4) store.rs test prose overstates its case: it says `EditorPayload` 'declares no root key whatsoever' when it does declare zones/entities/vehicles. The load-bearing half — that it declares no root `markers` key — is true and is what the test actually proves. Diagnosis: editor_verify/wave116.md findings 5-8.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["Four items from the wave-116 verifier, grouped because each is small. (1) A FALSE CLAIM TO STRIKE FROM THE RECORD: the T-069 slice reported that a pre-existing pin had been 'passing on a DECOY — the stub sentence quoted in a comment'. IT DOES NOT REPRODUCE. At base 619e5a14 the needle 'Marker placement lands in T-069.' occurs exactly twice: the live view stub (eden_dock_right.rs:1376) and the split-literal T-695 pin (:3354), which its own source cannot satisfy. No comment quoted the sentence and both pins were passing on the real stub. The pin INVERSIONS the slice performed were correct and preserved their original index claims — only the decoy narrative is wrong. Recorded here because an agent's chat summary is not an artifact, and this one was relayed onward before it was checked. (2) missions.rs:2358: the /compiled diagnostics headers are pinned by SOURCE SCAN only — there is no response-level test asserting `x-compile-diagnostics-count: 0` on a clean mission. Verified correct by reading (unconditional insert; HeaderValue::from_str over an integer cannot fail), so this is a missing test, not a defect. (3) Marker Attributes selects by id ALONE, so a hydrated foreign payload carrying the same marker id under two different factions would edit the first match. Unreachable through this editor's own minting, which always pairs (factionId, id). (4) store.rs test prose overstates its case: it says `EditorPayload` 'declares no root key whatsoever' when it does declare zones/entities/vehicles. The load-bearing half — that it declares no root `markers` key — is true and is what the test actually proves. Diagnosis: editor_verify/wave116.md findings 5-8."]

missing=0
"""
main_goal = "Strike false decoy claim and close three wave-116 verifier gaps."
context = [
    "Wave-116 four small items grouped: false decoy narrative plus three real gaps.",
    "T-069 decoy claim does not reproduce at 619e5a14; pin inversions were correct.",
    "Agent chat summary relayed onward before check; not an artifact.",
]
requirement = [
    "Strike false decoy claim from record; preserve correct pin inversion index claims.",
    "Add response test asserting x-compile-diagnostics-count 0 on clean compiled mission.",
    "Document marker id-alone select edge case; fix store.rs test prose overstatement.",
]
current_state = [
    "Needle Marker placement lands in T-069 occurs twice: stub and split-literal T-695 pin.",
    "missions 2358 diagnostics headers pinned by source scan only no response test.",
    "Marker Attributes selects by id alone; foreign dual-faction same id edits first match.",
]
approach = [
    "Record decoy narrative correction in ticket notes not agent chat summary.",
    "Response-level test for x-compile-diagnostics-count header on clean compile export.",
    "Clarify store.rs prose: no root markers key true; zones entities vehicles declared.",
]
verify = [
    "Clean mission compile response carries x-compile-diagnostics-count 0 header asserted.",
    "store.rs test prose matches actual EditorPayload root key declarations.",
]
acceptance = [
    "/compiled route pin required compiled_diagnostics_response_headers call in get_compiled_mission body.",
    "Marker Attributes click site pinned selection addr to faction_id and id pair.",
    "store.rs EditorPayload test prose limited claim to missing root markers key only.",
]
citations = [
    "editor_verify/wave116 findings 5-8",
    "T-069 decoy claim",
    "T-695 split-literal pin",
    "619e5a14 base needle",
]
shipped_at = "20cb0009"
created_at = "2026-08-08T06:58:20Z"
completed_at = "2026-08-08T07:20:41Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/missions.rs",
    "apps/website/frontend/src/eden_dock_right.rs",
    "crates/map-engine-core/src/doc/store.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
