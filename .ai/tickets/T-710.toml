id = "T-710"
kind = "work"
title = "capture port: pin the parity-bearing pure functions with tests"
summary = "capture port: pin the parity-bearing pure functions with tests"
class = "bug"
status = "deferred"
order = 4303
executor = "claude-code"
notes = """
filed by wave-100 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

657 ported lines in tools/tbd-tools/src/capture.rs carry zero new tests; the parity claims rest on source review alone. Unpinned pure fns: ztag (replacen first-occurrence semantics mirroring JS .replace), canvas_path suffixing, crop geometry/scale math, zoom step parsing. The crate's 51 existing tests exercise none of them. Fix: table-driven #[test]s for those four; no CDP or GPU needed. Diagnosis: editor_verify/wave100.md F-5.

missing=0
"""
main_goal = "Add table-driven tests for four unpinned capture pure functions."
context = [
    "657 ported lines in capture.rs carry zero new tests.",
    "Parity claims rest on source review alone without test coverage.",
]
requirement = [
    "Table-driven tests for ztag canvas_path crop math zoom step parsing.",
    "No CDP or GPU needed; four pure functions only.",
]
current_state = [
    "Crate 51 existing tests exercise none of the four unpinned functions.",
    "ztag replacen first-occurrence mirrors JS replace semantics.",
]
approach = ["Add table-driven unit tests for ztag path suffix crop zoom parsing."]
verify = ["Four pure functions pinned by unit tests without CDP or GPU."]
citations = [
    "capture.rs unpinned pure fns",
    "ztag replacen JS replace",
    "wave100.md F-5 diagnosis",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
