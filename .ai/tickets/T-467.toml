id = "T-467"
kind = "work"
title = "verify-t438/t456 still unwired from Makefile/CI siblings"
summary = "verify-t438/t456 still unwired from Makefile/CI siblings"
class = "chore"
status = "shipped"
order = 3306
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Residual from wave 25 adversarial N2. wave.sh wires verify-t438 and verify-t456 (T-463), but Makefile/CI have no sibling targets (same shape as pre-T-452 for other verifies).

Repro: make ci-local / ci.yml pass while scripts/mod/verify-t438*.sh is deleted.

Cure: add Makefile + CI steps mirroring wave.sh, or document intentional wave-only scope with a pin.

missing=0
"""
priority = 3
main_goal = "Add Makefile + CI steps for verify-t438/t456 mirroring wave.sh."
context = [
    "Residual from wave 25 adversarial N2.",
    "wave.sh wires verify-t438 and verify-t456 (T-463).",
    "Makefile/CI have no sibling targets.",
]
requirement = [
    "Add Makefile + CI steps mirroring wave.sh.",
    "Or document intentional wave-only scope with a pin.",
]
current_state = [
    "make ci-local / ci.yml pass while verify-t438*.sh deleted.",
    "Same shape as pre-T-452 for other verifies.",
]
approach = ["Cure: add Makefile + CI steps mirroring wave.sh."]
verify = ["Repro: make ci-local / ci.yml pass while scripts/mod/verify-t438*.sh deleted."]
acceptance = ["Wired verify-t438 and verify-t456 into ci-local and CI sibling targets."]
citations = [
    "wave 25 adversarial N2",
    "T-463 verify-t438 verify-t456",
    "pre-T-452 verify pattern",
    "ci-local sibling verify targets",
]
shipped_at = "e209d501"
created_at = "2026-07-27T03:21:43Z"
completed_at = "2026-07-27T03:41:45Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "Makefile",
    ".github/workflows/ci.yml",
    "scripts/mod/verify-t438-deploy-staging-compose-path.sh",
    "scripts/mod/verify-t456-mission-rest-size-gate.sh",
]

[scope]
domain = "repo"
layer = "xtask"
