id = "T-1055"
kind = "work"
title = "Fix engine-layers gate omitting the map engine editing module"
summary = "tools_v2/xtask/src/verifications/architecture/engine_layer_rules.rs:108-110 claims ten top-level modules; RULE4_RE (:141) and RULE7_DATA_RE (:144) omit editing (map-engine lib.rs:23 declares eleven), so data/ importing crate::editing passes. editor_orbat_coherency.rs:29-33 documents features the code (:209) no longer passes."
class = "chore"
status = "idea"
created_at = "2026-09-24T15:19:02Z"
notes = "tools_v2/xtask/src/verifications/architecture/engine_layer_rules.rs:108-110 ('nine top-level modules ... exactly ten'), :141 RULE4_RE and :144 RULE7_DATA_RE list camera, diagnostics, doll, frame, io, overlay, spatial, streaming, world; apps/website/map-engine/src/lib.rs declares 11 pub modules (camera :14, data :19, editing :23, frame :37, diagnostics :41, doll :45, io :49, overlay :53, spatial :58, streaming :62, world :66). Add editing to both matchers (crate:: and super:: arms) and fix the count; the header's collision check (only 'diagnostics' collides) needs rechecking for 'editing'. tools_v2/xtask/src/verifications/architecture/editor_orbat_coherency.rs:29-33 explains --features \"doc mission\" and doc/store.rs; the code uses \"scenario store\" (:205-209, :223). The count is stated twice more: tools_v2/xtask/src/verifications/architecture/engine_layer_rules.rs:111 ('exactly ten top-level modules') and engine_layer_boundaries.rs:72 ('the crate has exactly ten top-level modules. Nine of ...', again as 'the nine sibling modules' at :99); lib.rs declares eleven. Stale matcher: engine_layer_rules.rs:46-47 (GPU_MODULE_RE, rule 3b's matcher, lists text::gpu) and :210 (RULE3B_TAIL names text::gpu), but apps/website/graphics-engine/src/text/ has no gpu module (atlas, font, layout, metrics, pack, scale)."

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
