id = "T-797"
kind = "work"
title = "Row-2 toolbar, full Edit menu, ORBAT in menu row"
summary = "Row-2 icon toolbar, completed Edit menu, ORBAT into the menu row"
class = "bug"
status = "shipped"
order = 4790
executor = "claude-code"
notes = """
depends_on T-787; operator decisions 1&2 recorded in review section 8; supersedes the F-09 pill question via T-798 pairing | SHIPPED W202: row-2 toolbar + completed Edit menu + ORBAT into the menu row + F-14/F-15/F-16-copy by the slice (buttons disabled-first — dispatch lived in sibling-owned mission_editor.rs); completion 8a527b20 added the register_editor_toolbar_dispatch bridge and wired everything live. Verifier MAJOR: the completion's TOGGLED_PLATE reactivity claim was false (mount-order subscription gap, plates frozen) — fixed 95ee8cb7 (generation-signal subscription + teardown + the missing reactivity pin), focused re-verify RESOLVED with live click/chord/snap/remount parity. Grid-label-toggle chord correctly refused (no such binding; census rule). View menu removed whole after F-14+F-15 emptied it.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-06 + operator decisions 1/2 + F-14/F-15 label fixes. VERIFIED FROM EDEN PIXELS (all 16 tooltips read, frames 164000-164147): Eden's row 2 = ~25 buttons in separator groups — file ops · undo/redo · five widget modes (No Widget (1) / Translation (2) / Rotation (3) / Area Scaling (4) / Area (5)) · coordinate-space/vertical/surface-snap · three grid toggles — and 13 of 16 tooltips carry their shortcut in parentheses; the disabled Redo still tooltips. Eden's menu-bar Edit has NO clipboard verbs (frame 163508) — TBD's context menu already matches; do NOT move clipboard into the menubar. TBD TODAY: row 2 is title/scrubber/save cluster (eden_layout.rs ~:49 ROW_TOOLS under-populated), Edit menu = Undo/Redo only (eden_top_strip.rs ~:117 MENUS, ~:85 MenuAction), widgets/snap/grid are invisible keyboard secrets, ORBAT is a row-2 button. FIX: (a) populate row 2: undo/redo · widget modes (wired to T-795's keys; ship buttons now with current 1/2 arms, T-795 renumbers) · snap toggle + steps · grid-label toggle · Save Version/Export stay right; every button tooltips 'Name (Key)'; disabled buttons keep tooltips. (b) Edit menu gains Select All on Screen (Ctrl+A), widget submenu, snap/grid toggles with shown keys. (c) ORBAT Manager becomes a menu-row entry (operator decision 2). (d) F-14: the View row 'Map layers — render host (T-159.28)' — ship it or drop it; if kept inert, it must go (drop). (e) F-15: one Controls Hint home — keep Help>Keyboard Shortcuts, drop the View duplicate. ⚠️ TRAPS: eden_layout.rs consts changed by T-787 in wave 200 — build on merged main; the floating mode pill and OPEN button are T-798's removal scope, not yours; menu labels drop the '(Mission Settings)' parentheticals (F-16-copy). ACCEPTANCE: scripted — row 2 exposes >=10 buttons each with non-empty title containing its chord where bound; Edit menu lists Select All + widget/snap/grid rows with chords; ORBAT opens from the menu row; no menu row contains a T-xxx string; exactly one Controls Hint entry across all menus.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-06 + operator decisions 1/2 + F-14/F-15 label fixes. VERIFIED FROM EDEN PIXELS (all 16 tooltips read, frames 164000-164147): Eden's row 2 = ~25 buttons in separator groups — file ops · undo/redo · five widget modes (No Widget (1) / Translation (2) / Rotation (3) / Area Scaling (4) / Area (5)) · coordinate-space/vertical/surface-snap · three grid toggles — and 13 of 16 tooltips carry their shortcut in parentheses; the disabled Redo still tooltips. Eden's menu-bar Edit has NO clipboard verbs (frame 163508) — TBD's context menu already matches; do NOT move clipboard into the menubar. TBD TODAY: row 2 is title/scrubber/save cluster (eden_layout.rs ~:49 ROW_TOOLS under-populated), Edit menu = Undo/Redo only (eden_top_strip.rs ~:117 MENUS, ~:85 MenuAction), widgets/snap/grid are invisible keyboard secrets, ORBAT is a row-2 button. FIX: (a) populate row 2: undo/redo · widget modes (wired to T-795's keys; ship buttons now with current 1/2 arms, T-795 renumbers) · snap toggle + steps · grid-label toggle · Save Version/Export stay right; every button tooltips 'Name (Key)'; disabled buttons keep tooltips. (b) Edit menu gains Select All on Screen (Ctrl+A), widget submenu, snap/grid toggles with shown keys. (c) ORBAT Manager becomes a menu-row entry (operator decision 2). (d) F-14: the View row 'Map layers — render host (T-159.28)' — ship it or drop it; if kept inert, it must go (drop). (e) F-15: one Controls Hint home — keep Help>Keyboard Shortcuts, drop the View duplicate. ⚠️ TRAPS: eden_layout.rs consts changed by T-787 in wave 200 — build on merged main; the floating mode pill and OPEN button are T-798's removal scope, not yours; menu labels drop the '(Mission Settings)' parentheticals (F-16-copy). ACCEPTANCE: scripted — row 2 exposes >=10 buttons each with non-empty title containing its chord where bound; Edit menu lists Select All + widget/snap/grid rows with chords; ORBAT opens from the menu row; no menu row contains a T-xxx string; exactly one Controls Hint entry across all menus."]

missing=0
"""
main_goal = "Populate row-2 toolbar with Eden widget snap grid controls; complete Edit menu; move ORBAT to menu row; drop duplicate Controls Hint."
context = [
    "Eden row 2 ~25 buttons; 13 of 16 tooltips carry shortcut in parentheses.",
    "TBD row 2 is title scrubber save cluster; widgets snap grid are keyboard secrets.",
    "Edit menu Undo Redo only; ORBAT is row-2 button not menu entry.",
]
requirement = [
    "Populate row 2 undo redo widget modes snap grid toggles with Name Key tooltips.",
    "Edit menu gains Select All widget submenu snap grid toggles with shown keys.",
    "ORBAT Manager becomes menu-row entry per operator decision 2.",
    "Drop View Map layers row; one Controls Hint home in Help Keyboard Shortcuts.",
]
current_state = [
    "eden_layout.rs ROW_TOOLS under-populated at row 2.",
    "eden_top_strip.rs MENUS Edit Undo Redo only no Select All.",
    "View row carries Map layers render host; duplicate Controls Hint entry.",
]
approach = [
    "Wire toolbar buttons to T-795 keys; ship with current 1/2 arms before renumber.",
    "Menu labels drop Mission Settings parentheticals per F-16-copy.",
    "Build on merged main after T-787 wave 200 eden_layout const changes.",
]
verify = [
    "Row 2 exposes >=10 buttons each title contains chord where bound.",
    "Edit menu lists Select All widget snap grid rows with chords.",
    "ORBAT opens from menu row; no menu row contains T-xxx string.",
    "Exactly one Controls Hint entry across all menus.",
]
acceptance = [
    "Row-two toolbar exposed undo widget snap and grid controls with chord tooltips; ORBAT opened from menu row.",
    "Edit menu gained Select All and widget submenu; toolbar active plates tracked live state after reactivity fix.",
]
citations = [
    "editor_hostile_ux_review F-06 F-14 F-15",
    "Eden pixels frames 164000-164147",
    "operator decisions 1 and 2",
    "T-795 widget key renumber",
    "T-787 wave 200 consts",
    "F-16-copy parenthetical labels",
    "95ee8cb7 toolbar dispatch bridge",
]
shipped_at = "95ee8cb7"
created_at = "2026-08-09T20:43:10Z"
completed_at = "2026-08-09T22:41:49Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/eden_layout.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "top_strip",
    "layout_chrome",
]
