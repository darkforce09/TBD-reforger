id = "T-762"
kind = "work"
title = "Promote fly_to off T-166 smoke hook"
summary = "fly_to rides the T-166 smoke hook in production and every failure mode is silent"
class = "bug"
status = "shipped"
order = 4638
executor = "claude-code"
notes = """
filed by wave-116 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MINOR from the wave-116 verifier. eden_dock_left.rs:821 `fly_to` moves the camera by invoking `window.__editorCamSet` — the closure `mission_editor::register_editor_cam` installs as a T-166 SMOKE HOOK. VERIFIED BEHAVIOURALLY SOUND: the hook is installed unconditionally at engine init (mission_editor.rs:2780/4572), its body is exactly `set_view` -> `on_camera_changed` -> `flush_viewport` (the shipped mover), and the 'not yet installed' window coincides with 'no engine', where `live_camera()` already no-ops the click. So there is still exactly ONE camera mover in the codebase and the dock contains zero camera math — which was the right instinct. THE RISK IS COUPLING, NOT CORRECTNESS: rename or remove that test hook and fly-to dies silently, with no error and no failing test — and the only pin tying this file to the hook name is one of the HOLLOW ones filed as T-759, so it would not catch it either. FIX: promote a real seam — `pub fn fly_to(x, y, zoom)` beside `camera_snapshot` in world_assets/mod.rs, ~8 lines using the same RENDER_CTX borrow as `apply_grid` — and repoint the dock at it. The slice could not do this: world_assets/mod.rs was in nobody's owns and mission_editor.rs was T-069's that wave. Do it before the T-672-era churn. RELATED: `world_assets::labels` is a private module and `LabelHost.towns` a private field, so the dock also re-fetches and re-parses `/map-assets/<terrain>/locations.json` that was already parsed at boot — add `pub fn named_locations()` in the same pass. Diagnosis: editor_verify/wave116.md finding 4.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["MINOR from the wave-116 verifier. eden_dock_left.rs:821 `fly_to` moves the camera by invoking `window.__editorCamSet` — the closure `mission_editor::register_editor_cam` installs as a T-166 SMOKE HOOK. VERIFIED BEHAVIOURALLY SOUND: the hook is installed unconditionally at engine init (mission_editor.rs:2780/4572), its body is exactly `set_view` -> `on_camera_changed` -> `flush_viewport` (the shipped mover), and the 'not yet installed' window coincides with 'no engine', where `live_camera()` already no-ops the click. So there is still exactly ONE camera mover in the codebase and the dock contains zero camera math — which was the right instinct. THE RISK IS COUPLING, NOT CORRECTNESS: rename or remove that test hook and fly-to dies silently, with no error and no failing test — and the only pin tying this file to the hook name is one of the HOLLOW ones filed as T-759, so it would not catch it either. FIX: promote a real seam — `pub fn fly_to(x, y, zoom)` beside `camera_snapshot` in world_assets/mod.rs, ~8 lines using the same RENDER_CTX borrow as `apply_grid` — and repoint the dock at it. The slice could not do this: world_assets/mod.rs was in nobody's owns and mission_editor.rs was T-069's that wave. Do it before the T-672-era churn. RELATED: `world_assets::labels` is a private module and `LabelHost.towns` a private field, so the dock also re-fetches and re-parses `/map-assets/<terrain>/locations.json` that was already parsed at boot — add `pub fn named_locations()` in the same pass. Diagnosis: editor_verify/wave116.md finding 4."]

missing=0
"""
main_goal = "Promote pub fly_to and named_locations seams off T-166 smoke hook."
context = [
    "Wave-116 MINOR: fly_to invokes __editorCamSet T-166 smoke hook; behaviourally sound today.",
    "Hook installed at engine init; one camera mover; dock has zero camera math.",
    "Risk is coupling not correctness: rename hook and fly-to dies silently.",
]
requirement = [
    "Add pub fn fly_to beside camera_snapshot in world_assets/mod.rs using RENDER_CTX borrow.",
    "Repoint eden_dock_left fly_to at new seam not __editorCamSet smoke hook.",
    "Add pub fn named_locations to avoid re-fetching parsed locations.json at boot.",
]
current_state = [
    "eden_dock_left 821 fly_to calls window.__editorCamSet from register_editor_cam closure.",
    "Only pin tying dock to hook name is hollow T-759 pin; would not catch rename.",
    "Dock re-parses locations.json because labels module and LabelHost.towns are private.",
]
approach = [
    "~8 lines fly_to in world_assets/mod.rs same RENDER_CTX borrow as apply_grid.",
    "named_locations exposes boot-parsed towns; repoint dock index fly-to paths.",
    "Ship before T-672-era churn while world_assets and mission_editor owns stable.",
]
verify = [
    "fly_to moves camera without calling __editorCamSet smoke hook directly.",
    "Rename __editorCamSet hook fly_to still works via pub seam not silent no-op.",
]
citations = [
    "editor_verify/wave116 finding 4",
    "T-166 smoke hook",
    "T-759 hollow pin",
    "T-672 era churn",
]
shipped_at = "ddb38718"
created_at = "2026-08-08T03:54:52Z"
completed_at = "2026-08-08T04:01:33Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/world_assets/mod.rs",
    "apps/website/frontend/src/eden_dock_left.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_left"]
