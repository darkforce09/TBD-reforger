id = "T-381"
kind = "work"
title = "Integration tests can hit live DB via unscoped DELETE"
summary = "The integration suite can point at the live database, and one delete is unscoped"
class = "bug"
status = "shipped"
order = 3197
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "Refuse integration tests unless database name matches a test pattern; scope the unscoped factions DELETE."
context = [
    "All 20 files in apps/website/api/tests/ early-return unless TEST_DATABASE_URL is set, and Makefile:121-123 points it at a dedicated `rust_it`.",
    "But **nothing validates the target database name**, so an exported `TEST_DATABASE_URL=.../tbd_reforger` runs the whole suite against the live dev DB.",
]
requirement = [
    "The one unscoped destructive statement: tests/factions.rs:18 — `DELETE FROM user_factions` with **no WHERE**. Wipes the table.",
    "Every other suite scopes to test-owned rows. Also tests/identity_link.rs:26 — `UPDATE users SET arma_id = NULL WHERE discord_id = $3` against the shared dev operator row.",
    "Two fixes, both cheap: refuse to run unless the database name matches a test pattern (`rust_it`, `*_it`, `*_probe`), and scope that DELETE.",
    "Several agents this run have created and dropped their own throwaway DBs correctly — the convention exists; enforce it.",
]
current_state = ["tests/null_tolerance.rs:240-245 already documents the shared-dev-row hazard, so the awareness exists and the guard does not."]
approach = [
    "require_test_database_url allow-list refuses tbd_reforger before any IT connect.",
    "factions setup DELETE scoped to test owner rows; identity_link uses same guard.",
]
verify = [
    "cargo test -p website-api t381_test_database_name_guard",
    "cargo test -p website-api --test factions",
]
acceptance = ["Integration tests refused live database name; unscoped DELETE FROM user_factions removed."]
citations = [
    "tests/factions.rs:18 DELETE",
    "identity_link.rs:26 UPDATE",
    "null_tolerance.rs:240 hazard",
    "Makefile:121-123 rust_it",
    "TEST_DATABASE_URL guard",
    "commit 0a5656fa7 TEST_DATABASE_URL guard",
]
shipped_at = "b2d73e1d"
created_at = "2026-07-27T13:30:10Z"
completed_at = "2026-07-27T13:34:54Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/api/tests/factions.rs",
    "apps/website/api/tests/identity_link.rs",
    "apps/website/api/tests/common/mod.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
