id = "T-705"
kind = "work"
title = "Player gadget flags — map, compass, watch, GPS, radio"
summary = "Player gadget flags — map, compass, watch, GPS, radio"
class = "feature"
status = "queued"
order = 4590
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

NEW-W1, 5 screenshot rows from Edit: General States. Eden lets a scenario decide which gadgets a player has. Every one needs a mission.schema.json field plus an Enfusion reader in apps/mod/tbd-framework/, so none is dispatchable. Grouped rather than split because they share one schema shape.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.

missing=0"""
main_goal = "Ship Enfusion gadget-flag reader plus editor UI for map compass watch GPS radio."
context = [
    "NEW-W1 five screenshot rows Edit General States.",
    "Eden scenario decides which player gadgets are available.",
    "Schema widening split to T-706; this Enfusion reader plus editor UI.",
]
requirement = [
    "Read map compass watch GPS radio gadget flags at runtime.",
    "Does not touch packages/tbd-schema; T-706 owns widening.",
    "Grouped five flags share one schema shape.",
]
current_state = [
    "None dispatchable until T-706 schema fields land.",
    "Executor corrected workbench to claude-code; .c routes Fable.",
]
approach = [
    "Implement reader in TBD_GadgetFlags.c after T-706 ships.",
    "scripts/mod/compile.sh headless compile verified no Workbench.",
]
verify = ["MissionLoader reads each gadget flag when schema fields exist."]
citations = [
    "T-706 schema widening split",
    "NEW-W1 General States",
    "TBD_GadgetFlags.c reader",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_GadgetFlags.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
