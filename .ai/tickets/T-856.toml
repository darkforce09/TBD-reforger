id = "T-856"
kind = "work"
title = "T-853 port: tbd-spawn-determinism -> mod spawn-determinism"
summary = "T-853 port: tbd-spawn-determinism -> mod spawn-determinism"
class = "chore"
status = "shipped"
order = 5370
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/tbd-spawn-determinism.sh (354 lines) to `cargo xtask mod spawn-determinism` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). spawn determinism harness; make target. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/tbd-spawn-determinism.sh (354 lines) to `cargo xtask mod spawn-determinism` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). spawn determinism harness; make target. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0"""
main_goal = "Port tbd-spawn-determinism.sh to cargo xtask mod spawn-determinism."
context = [
    "T-853 shell->xtask slice; 354-line bash script.",
    "Spawn determinism harness; make target.",
    "Callers include Makefile platform wave.sh mod wave.sh workflows.",
]
requirement = [
    "Delete script shell-inventory.txt line python-inventory.txt line.",
    "Repoint every caller Makefile wave.sh workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/mod/tbd-spawn-determinism.sh bash gate live.",
    "Makefile wave.sh workflows still invoke bash script.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and two deliberately broken trees.",
    "T-556 anti-vacuity: passing run alone is not evidence.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Port diff matches clean tree byte-for-byte.",
    "Port diff matches at least two deliberately broken trees.",
]
acceptance = [
    "Deleted tbd-spawn-determinism.sh; mod spawn-determinism xtask gate replaced the harness.",
    "Inventory line removed; Makefile and spawn-determinism docs callers repointed.",
    "assess_run census extract and uniq padding matched bash on clean and broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "1f8d5cb3 spawn-determinism port",
]
shipped_at = "1f8d5cb3"
created_at = "2026-08-12T02:52:42Z"
completed_at = "2026-08-12T03:13:09Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/mod/tbd-spawn-determinism.sh",
    "xtask/src/gate_tbd_spawn_determinism.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
