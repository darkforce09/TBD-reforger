id = "T-266"
kind = "work"
title = "No pagination anywhere in the SPA"
summary = "Audit Load more forwards next_cursor; stops silent truncation at default page."
class = "audit"
status = "shipped"
order = 2840
executor = "claude-code"
context = [
    "List endpoints return total, limit, offset; audit uses next_cursor.",
    "audit.rs fetched cursor list and discarded next_cursor at ~20 rows.",
]
requirement = [
    "Forward ?before=<cursor> and append into client-side filter set.",
    "Pin Load-more path helpers with Class-R tests.",
]
current_state = [
    "audit.rs fetched cursor list and discarded next_cursor at default page.",
    "Trail silently stopped around twenty rows with no Load more.",
]
approach = [
    "Load more refetches ?before=cursor and merge_audit_page appends rows.",
    "Class-R pins parse_next_cursor and ignoring_next_cursor_is_detectably_wrong.",
]
verify = ["Class-R pins go red if next_cursor ignored."]
acceptance = ["Audit trail paginated via Load more; next_cursor forwarded and appended."]
citations = [
    "commit cb7dc35d Load more",
    "wave 13 merge 19de55ba",
]
shipped_at = "19de55ba"
created_at = "2026-07-26T23:29:38Z"
completed_at = "2026-07-26T23:34:08Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/audit.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
