id = "T-415"
kind = "work"
title = "Mod never verifies authored loadout delivery"
summary = "The mod never checks whether it delivered the authored loadout, and IsComplete has zero callers"
class = "bug"
status = "shipped"
order = 3240
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope."
priority = 1
main_goal = "Consume IsComplete or pre-check Can* so mod verifies authored loadout delivery."
context = [
    "Durable fix for T-240 which shipped client half only.",
    "Confirmed twice 2026-07-26; re-confirmed wave 4.",
    "Mod has zero runtime capacity math under Scripts/Game/.",
]
requirement = [
    "Consume IsComplete or pre-check with Can* family.",
    "Until fixed, client-side refusals are heuristic over unread export.",
]
current_state = [
    "InsertCargo pushes at engine; no CanInsertItemInStorage on cargo path.",
    "Unfit cargo: tries container, then any storage, then DELETES entity.",
    "BREAKS row qty loop — drops entire remaining quantity, not one item.",
    "IsComplete() has ZERO CALLERS repo-wide — answer computed and discarded.",
    "SpawnSlotBody returns body unconditionally; boot proceeds to LOBBY.",
    "Only failure surface: world-boot.sh no-TBD-ERROR CI gate.",
]
approach = [
    "CargoBudget from Workbench export-time data mod never reads.",
    "Recursion confirmed NOT REACHABLE — CargoRow is three scalars.",
]
citations = [
    "T-240 client half",
    "TBD_LoadoutEquipHelper.c IsComplete",
    "TBD_RegistryScan.c CargoBudget",
    "arsenal_rules.rs CargoRow",
]
shipped_at = "a5c1040a"
created_at = "2026-07-27T13:17:00Z"
completed_at = "2026-07-27T13:29:49Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_LoadoutEquipHelper.c"]

[scope]
domain = "repo"
layer = "docs"
