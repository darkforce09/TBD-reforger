id = "T-162.1"
kind = "work"
title = "T-162 — Eradicate all remaining Python (→ Rust)"
summary = "T-162 — Eradicate all remaining Python (→ Rust)"
class = "feature"
status = "shipped"
order = 1590
spec = "docs/platform/t162_python_eradication.md"
executor = "cursor-docs"
notes = "MCP consume/socket-send/probe-sock → xtask; flip mcp-call/daemon/selftest."
parent = "T-162"
main_goal = "Port mod MCP Python helpers into xtask mcp subcommands."
context = ["mcp-consume.py and mcp-socket-send.py invoked by mcp-call.sh."]
requirement = ["xtask mcp consume, socket-send, probe-sock; flip mcp-call scripts; 19/19 selftest."]
current_state = ["scripts/mod/lib/mcp-consume.py and mcp-socket-send.py on disk."]
approach = ["Rust MCP cmds in xtask; update mcp-call, daemon, selftest wrappers."]
verify = ["bash scripts/mod/mcp-call-selftest.sh 19/19"]
citations = [
    "t162_verify_log.md",
    "t162_python_eradication.md",
    "commit cac7b8fe",
]
created_at = "2026-07-16T00:00:00Z"
completed_at = "2026-07-17T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-162 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "xtask"
