id = "T-441"
kind = "work"
title = "null_tolerance OPTION_FIELDS must allow events.server_id/modpack_id"
summary = "null_tolerance OPTION_FIELDS must allow events.server_id/modpack_id"
class = "chore"
status = "shipped"
order = 3280
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

In-wave hotfix from wave 11 gate RED after T-260. Event.server_id and Event.modpack_id are Option<Uuid>, but apps/website/api/tests/null_tolerance.rs OPTION_FIELDS only listed events.match_id. Cold gate fails no_query_as_reads_a_nullable_column_without_coalesce on dashboard.rs + deployments.rs SELECTs.

Repro: after T-260 merge, wave.sh gate → null_tolerance FAIL on server_id/modpack_id without COALESCE.

Cure: add ("events", "server_id") and ("events", "modpack_id") to OPTION_FIELDS (same claim as match_id)."""
priority = 1
main_goal = "Add events server_id and modpack_id to null_tolerance OPTION_FIELDS."
context = [
    "In-wave hotfix from wave 11 gate RED after T-260.",
    "server_id and modpack_id are Option<Uuid>.",
    "OPTION_FIELDS only listed events.match_id.",
]
requirement = [
    'Add ("events", "server_id") and ("events", "modpack_id") to OPTION_FIELDS.',
    "Same claim as match_id.",
]
current_state = [
    "Cold gate fails no_query_as_reads_a_nullable_column_without_coalesce.",
    "dashboard.rs and deployments.rs SELECTs affected.",
]
approach = ["Cure: extend OPTION_FIELDS with server_id and modpack_id pairs."]
verify = ["Repro: after T-260 merge, wave gate null_tolerance FAIL without COALESCE."]
acceptance = ["null_tolerance OPTION_FIELDS allowlisted events.server_id and events.modpack_id nullable columns for dashboard and deployments SELECTs."]
citations = [
    "T-260 wave 11 gate RED",
    "null_tolerance.rs OPTION_FIELDS",
    "dashboard deployments SELECTs",
]
shipped_at = "e7a6ef0e"
created_at = "2026-07-26T23:01:01Z"
completed_at = "2026-07-26T23:02:43Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/api/tests/null_tolerance.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
