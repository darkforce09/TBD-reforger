id = "T-275"
kind = "work"
title = "Declare mod-enforced limits in the schema"
summary = "The mod clamps MAX_NETS, label length, grace and duration limits the schema was silent about; documents could validate and still be clamped at load."
class = "feature"
status = "shipped"
order = 2930
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope. "
depends_on = ["T-241"]
context = [
    "T-241 declared minima only; every maximum was left to this ticket.",
    "MAX_NETS 32, MAX_LABEL_CHARS 48, MAX_GRACE_SECONDS 3600, MAX_DURATION_SECONDS 21600.",
]
requirement = [
    "Encode mod maxima as schema maxItems, maxLength and maximum constraints.",
    "Point mod clamp comments at the schema pins so the two cannot disagree.",
]
current_state = [
    "T-241 pinned minima only; mod silently clamped MAX_NETS 32 label 48 grace 3600 duration 21600.",
    "Documents validating against schema could still be clamped at mod load.",
]
approach = [
    "Added schema maxItems maxLength and maximum pins to mission.schema.json.",
    "Pointed mod clamp comments at schema pins in RadioPlan ZoneRegistry ObjectiveRegistry.",
]
verify = ["Slice gate PASS."]
acceptance = ["Schema maxima matched mod clamps; validating documents no longer silently clamped at load."]
citations = [
    "commit 0d5d6f81 schema pins",
    "T-241 minima only",
    "wave 16 merge a0d9aab1",
]
shipped_at = "a0d9aab1"
created_at = "2026-07-27T00:06:59Z"
completed_at = "2026-07-27T00:08:15Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Radio/TBD_RadioPlan.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Zones/TBD_ZoneRegistry.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectiveRegistry.c",
    "packages/tbd-schema/schema/mission.schema.json",
]

[scope]
domain = "repo"
layer = "docs"
