id = "T-803"
kind = "work"
title = "The placement drop-target layer is visible, always"
summary = "The placement drop-target layer is visible, always"
class = "bug"
status = "shipped"
order = 4850
executor = "claude-code"
notes = "operator O-9; keep visual distinct from T-649-era selection highlight | SHIPPED W204: 'Placing into: X' header affordance with truth-telling resolvers (names the rows.first() fallback honestly) by the slice; the ROW visual landed in the completion (2318dcf8, eden_tree.rs was off-limits mid-wave): ROW_DROP_TARGET distinct from selection on >=3 measured axes + my_location chip; create-layer default verified already-correct at ops level; Alt-click subtree intact. Verifier re-proved incl. the fallback landing."
main_goal = "Persistent drop-target row visual and placing-into affordance."
context = [
    "O-9 active layer set by outliner click; only hover tooltip signals target.",
    "Author places into a guess; layer visibility inconsistent when selected.",
    "editor_ops set_active_layer; eden_dock_left hosts row click path.",
]
requirement = [
    "Persistent visual on active drop-target row distinct from selection.",
    "Placing into layer affordance near palette or cursor during armed placement.",
    "Newly created layer becomes drop target on creation.",
]
current_state = [
    "Drop-target and selection are different states; must read differently.",
    "Alt-click select-subtree on same rows must survive.",
    "Virtualized rows must keep highlight when scrolled.",
]
approach = [
    "Distinct background plus chip glyph not colour-only on target row.",
    "Truth-telling resolver names rows.first fallback honestly.",
]
verify = [
    "Click layer B: row differs from siblings; on-screen text names B.",
    "Place unit lands under B; create C highlights C; Alt-click selects subtree.",
]
citations = [
    "editor_hostile_ux_review O-9",
    "editor_ops active_layer 2902",
    "eden_dock_left row click 108",
    "state-vocabulary two treatments",
]
shipped_at = "b008bfc8"
created_at = "2026-08-10T04:20:02Z"
completed_at = "2026-08-10T05:32:24Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/eden_dock_left.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "dock_left",
]
