id = "T-913"
kind = "program"
title = "Ticket run metrics"
summary = "created_at/completed_at on ticket files (RFC 3339 UTC, deliberate ALLOWED_NEW widen) plus per-run token files under .ai/tickets/metrics/<id>/ produced by cargo xtask platform slice-run. platform wave land stamps outcome and git_sha on the run file, then runs wave repack. ticket metrics --by agent sums elapsed and tokens_consumed.total over real files only — a missing usage object fails the run, never tokens 0."
status = "shipped"
order = 5930
spec = "docs/platform/t913_ticket_run_metrics.md"
executor = "claude-code"
notes = "Depends on T-912 landed. Serial: T-913.1 then T-913.2. No backfill of timestamps on existing tickets; shipped_at stays a SHA. LIMITS: in-chat Task dispatch (command-center subagents) is not captured — coverage is honest but partial."
depends_on = ["T-912"]
children = [
    "T-913.1",
    "T-913.2",
]
shipped_at = "efcff4e5"
completed_at = "2026-08-14T05:21:53Z"
