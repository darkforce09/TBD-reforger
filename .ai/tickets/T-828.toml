id = "T-828"
kind = "work"
title = "Marker captions drift when zoom changes without rebind"
summary = "Marker captions are world-frozen at bind-time zoom — drift past 40px until a rebind"
class = "bug"
status = "queued"
order = 5100
executor = "claude-code"
notes = "wave204 verifier; the icon half is the in-file template"
main_goal = "Caption screen offsets recompute per frame like icons."
context = [
    "wave204 MINOR: T-790 icon half is properly screen-space.",
    "Caption positions freeze at bind-time zoom; 43px/52px gaps at 1.30/1.06 m/px.",
    "Gaps exceed ~40px acceptance until doc change forces rebind.",
]
requirement = [
    "MarkerComposite caption half recomputes screen offsets per frame from live camera.",
    "Reuse icon transform; no second source; no rebind dependency.",
    "Anchors captions in same screen-space pass as icons.",
]
current_state = [
    "Caption text world-frozen at bind-time zoom not live camera.",
    "Icon half is the in-file template for correct screen-space behaviour.",
    "engine.rs MarkerComposite owns caption and icon rendering.",
]
approach = [
    "Recompute caption offsets each frame from live camera like icon half.",
    "Single transform source shared between icon and caption halves.",
]
verify = [
    "Zoom 5.6 to 1.06 m/px continuously with no doc changes.",
    "Every caption stays within 40px of its marker at every sampled frame.",
]
citations = [
    "wave204.md MINOR filing",
    "T-790 icon screen-space template",
    "MarkerComposite caption half",
    "5.6 to 1.06 m/px zoom sweep",
]
estimated = ["scope"]
owns = ["crates/map-engine-render/src/engine.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
