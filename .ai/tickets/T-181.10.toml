id = "T-181.10"
kind = "work"
title = "JSON-driven slot loadouts on player deploy"
summary = "Equip primary, optic, magazine, and cargo from compiled slot JSON; identity-keyed body reuse."
class = "feature"
status = "shipped"
order = 1810
spec = "docs/mod/t181_event_mod_program.md"
executor = "claude-code"
notes = "JSON loadouts @ 4b1ff732. optic+magazine were parsed and DISCARDED — now mounted into weapon storage. Honest per-field failure lines naming slot+item; nakedness guard. Closed a real hole: body reuse now keyed on IDENTITY, so a player taking a vacated slot cannot inherit the previous occupant's fired mags/damage, while a mid-life reconnect still gets its own body. AdminRespawn forces a fresh body. IsBodyDead treated a missing CharacterControllerComponent as ALIVE (bug). COMPILE-VERIFIED ONLY — magazine chambering and optic mounting need eyes."
parent = "T-181"
context = [
    "LoadoutEquipHelper parsed optic and magazine but discarded them.",
    "Vacated slot body reuse let the next occupant inherit fired mags.",
]
requirement = [
    "Mount optic and magazine into weapon storage from slot JSON.",
    "Key body reuse on player identity, not slot id alone.",
    "AdminRespawn and honest per-field failure lines with nakedness guard.",
]
current_state = [
    "LoadoutEquipHelper parsed optic and magazine but discarded them.",
    "Body reuse keyed on slot let next occupant inherit fired magazines.",
]
approach = [
    "Mount optic and magazine into weapon storage from slot JSON.",
    "Key body reuse on player identity; AdminRespawn forces fresh body.",
]
verify = ["scripts/mod/compile.sh exit 0; commit 4b1ff732."]
acceptance = [
    "Compile-verified JSON loadout mount and identity-keyed body reuse paths.",
    "Runtime equip-on-deploy unobserved; no PIE at ship.",
]
citations = [
    "Source: commit 4b1ff732 — JSON loadouts.",
    "Source: commit 3149a7a1 — merge slice.",
    "commit 4b1ff732 JSON loadouts",
    "spec t181_event_mod_program loadouts",
]
shipped_at = "3149a7a1"
created_at = "2026-07-25T07:04:07Z"
completed_at = "2026-07-25T07:24:32Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"

[scope]
domain = "repo"
layer = "docs"
