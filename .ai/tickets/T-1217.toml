id = "T-1217"
kind = "work"
title = "Align the file-length help text with its 500-line limit"
summary = "verify file-length help says production files stay under 500 lines and tests under 1000, but repository_access.rs:52 fails only above the limit, so exactly 500 or 1000 passes."
class = "chore"
status = "idea"
notes = "The FileLength doc comment in tools_v2/xtask/src/commands/verify/cli.rs:28 ('production files stay under 500 lines, test files under 1000') is the --help text. The gate (tools_v2/xtask/src/verifications/language_bans/node_and_file_limits/repository_access.rs:52) fails a file only when n > max_lines, with SIZE_3_PRODUCTION_MAX_LINES = 500 and SIZE_3_TEST_MAX_LINES = 1000 (node_and_file_limits.rs:14-15), so a 500-line production file or a 1000-line test file passes, which is what CLAUDE.md law 7 states ('at or under 500 lines', 'at or under 1000 lines'). Only the help text is out of date: change it to 'at or under 500 lines' and 'at or under 1000'. Source: Phase 6 documentation verifier."
created_at = "2026-09-26T06:00:22Z"

[scope]
domain = "repo"
layer = "docs"
