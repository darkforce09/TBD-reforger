id = "T-878"
kind = "work"
title = "T-853 port: setup-client-addons -> setup client-addons"
summary = "T-853 port: setup-client-addons -> setup client-addons"
class = "chore"
status = "shipped"
order = 5590
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "e303501b"
created_at = "2026-08-12T07:57:56Z"
completed_at = "2026-08-12T08:04:11Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/setup-client-addons.sh (23 lines) to `cargo xtask setup client-addons` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). client addon symlink setup. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/setup-client-addons.sh",
    "xtask/src/gate_setup_client_addons.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
