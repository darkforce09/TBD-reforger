id = "T-574"
kind = "work"
title = "T-491 host Class-R soft include_str — comment-only move_entities_and_vehicles survives"
summary = "T-491 host Class-R soft include_str — comment-only move_entities_and_vehicles survives"
class = "feature"
status = "shipped"
order = 3427
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W68 adversarial verifier (CLEAN NIT) after T-491.

`select_tool_and_mission_editor_delegate_to_atomic_mix_apis` (store.rs ~2912–2940) only string-presence / Move-arm absence checks via include_str. A comment-only `move_entities_and_vehicles` mention would keep the pin green; it also does not pin that mission_editor calls pick_slot_or_vehicle / marquee_* helpers.

Behavioral one-undo Class-R is real and PASS. Soft host-delegation pin is the residue.

Repro: replace live call with comment containing the symbol (or keep call but drop pick/marquee delegation asserts); soft pin stays green.

missing=0"""
priority = 2
main_goal = "Harden Class-R pin to require live delegation, not comment-only symbol presence."
context = [
    "W68 adversarial verifier CLEAN NIT after T-491.",
    "Behavioral one-undo Class-R real and PASS; soft host-delegation pin is residue.",
]
requirement = [
    "Pin must require mission_editor calls pick_slot_or_vehicle / marquee_* helpers.",
    "Pin must not pass on comment-only move_entities_and_vehicles mention.",
]
current_state = [
    "store.rs ~2912-2940 uses include_str string-presence / Move-arm absence only.",
    "Repro: comment containing symbol keeps soft pin green; live call dropped same.",
]
approach = ["Replace soft include_str pin with behavioral delegation assertions in store.rs Class-R."]
verify = [
    "Replace live call with comment containing symbol → pin must go RED.",
    "Drop pick/marquee delegation asserts → pin must go RED.",
]
acceptance = [
    "Soft include_str move_entities_and_vehicles host pin replaced with behavioural one-txn slot-and-vehicle move test.",
    "Comment-only move_entities_and_vehicles mention in host sources no longer kept delegation Class-R green.",
]
citations = [
    "W68 verifier CLEAN NIT T-491",
    "include_str string-presence pin",
    "move_entities_and_vehicles comment green",
]
shipped_at = "8353ba63"
created_at = "2026-07-31T23:33:44Z"
completed_at = "2026-08-01T00:05:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["crates/map-engine-core/src/doc/store.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
