id = "T-616"
kind = "work"
title = "Unify wave_plan.tsv labels for correct sort"
summary = "wave_plan.tsv mixes bare-number and wN wave labels, so current_wave() sorts the recent ones as zero"
class = "bug"
status = "shipped"
order = 3632
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by T-613 (wave 78). Cosmetic today, wrong in a way that will mislead.

`docs/platform/wave_plan.tsv` column 1 carries **two incompatible label formats**: bare numbers
(`0`-`11`, `43`-`68`, `99`) from the original packing, and `w76`/`w77`/`w78` from the recent waves.
`current_wave()` sorts with `sort -n`, under which **`w78` evaluates to 0**, so the newest waves sort
below the oldest. T-613 measured `current_wave` returning **3** from its worktree, with
`wave.sh wave` printing "wave 3 is OPEN".

**It reads correctly on `main` right now** (`wave w78 — 0/4 shipped`) because the w78 rows exist and
whatever tiebreak applies lands on the right answer -- so this is latent, not live. It is filed
because the underlying mix is real and the next person to add a differently-shaped label gets a
different wrong answer.

It also feeds `gate_wave_number`'s fallback. That path is guarded by `factory_pack_wave`, so the gate
DB naming is unaffected -- verified by T-613, not assumed.

FIX: pick one format and migrate the file, or make `current_wave()` strip a leading `w` before the
numeric sort. Prefer migrating the data -- a parser that tolerates two formats invites a third.

NOTE the new wave-78 base-derivation cross-checks read `wave_plan.tsv` as one of their oracles
(`ticket ledger`). They key on the `wN` form and worked correctly this wave ("wave 77 has 5
ticket(s)… all shipped — corroborated"), but anything that normalises these labels must keep that
check working -- see T-613's implementation before touching the format.

missing=0"""
priority = 3
main_goal = "Migrate wave_plan.tsv to one label format or strip w before numeric sort."
context = [
    "T-613 wave 78 found mixed bare-number and wN labels in column one.",
    "sort -n treats w78 as zero; latent mis-sort not live on main today.",
]
requirement = [
    "Pick one wave label format and migrate wave_plan.tsv column one.",
    "Or strip leading w in current_wave before numeric sort.",
    "Keep T-613 ticket-ledger oracle working on wN form.",
]
current_state = [
    "Bare numbers 0-11, 43-68, 99 coexist with w76 w77 w78 labels.",
    "current_wave measured returning 3; wave.sh prints wave 3 OPEN.",
    "gate_wave_number fallback guarded by factory_pack_wave per T-613.",
]
approach = [
    "Prefer data migration over dual-format parser tolerance.",
    "Read T-613 implementation before normalising labels.",
]
verify = [
    "main reads wave w78 correctly after format unification.",
    "Ticket ledger cross-check still corroborates wN rows.",
]
acceptance = [
    "wave_plan.tsv column-one labels migrated to bare integers; the wN spelling left the working tree.",
    "current_wave gained a generation floor of 76 so numeric labels still returned 80, not deferred wave 3.",
]
citations = [
    "T-613 wave 78 label mix",
    "wave 78 ticket ledger oracle",
]
shipped_at = "5a4aacff"
created_at = "2026-08-01T01:33:23Z"
completed_at = "2026-08-01T01:33:23Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
