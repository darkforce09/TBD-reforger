id = "T-680"
kind = "work"
title = "Vehicle states: lock, fuel, ammo"
summary = "Vehicle states: lock, fuel, ammo"
class = "chore"
status = "queued"
order = 4370
executor = "claude-code"
notes = """
workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row

--- HEAD migration_legacy (verbatim) ---

Three attribute ids: `ATTR-FIELD-OBJ-LOCK`, `-OBJ-FUEL`, `-OBJ-AMMO`. `$defs/entity` is closed and there is no reader. Word-boundary counts in apps/mod: `fuel` = 1, `lock` = 4, and NONE of the four is an authored vehicle lock. The natural successor to shipped T-215. Adjacent to T-076 (crew UI) and the cancelled T-077, but those are PLACEMENT tickets, not attribute tickets — keep them separate. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.

missing=0
"""
main_goal = "Implement Enfusion reader for vehicle lock, fuel, and ammo attributes."
context = [
    "Three ATTR-FIELD-OBJ-LOCK, -OBJ-FUEL, -OBJ-AMMO ids; no Enfusion reader today.",
    "Successor to shipped T-215; keep separate from T-076 placement tickets.",
    "Schema widening split to T-706; this ticket is mod reader plus editor UI.",
]
requirement = [
    "Read lock, fuel, ammo mission attributes in TBD_VehicleState.c at runtime.",
    "Add editor UI for vehicle states; do not edit packages/tbd-schema here.",
]
current_state = [
    "$defs/entity closed; apps/mod has no authored vehicle-lock reader.",
    "Word-boundary grep: fuel=1, lock=4; none is vehicle lock semantics.",
]
approach = [
    "Implement owns-path Enfusion reader after T-706 schema attribute widening lands.",
    "Headless compile via scripts/mod/compile.sh; Fable routes .c portion.",
]
verify = [
    "scripts/mod/compile.sh clean headless tbd-framework compile with new reader.",
    "Runtime applies lock fuel ammo from mission payload on vehicle spawn.",
]
citations = [
    "T-215 vehicle attribute predecessor",
    "T-706 schema widening split",
    "T-076 placement separate ticket",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Vehicles/TBD_VehicleState.c"]

[scope]
domain = "mod"
layer = "scripts"
