id = "T-509"
kind = "work"
title = "CREATE omit weather defaults Clear after T-377"
summary = "W43 hotfix: CREATE omitted weather must still default Clear after T-377"
class = "chore"
status = "shipped"
order = 3349
executor = "claude-code"
priority = 0
main_goal = "CREATE treats omitted weather as Clear; PATCH still rejects empty string."
context = [
    "Wave 43 re-gate FAIL after T-508.",
    "T-377 dropped empty string from valid_weather clear arm.",
]
requirement = [
    "PATCH empty weather no longer rewrites dense_fog to clear.",
    "Omitted weather becomes empty string; create_mission returns 400.",
    "CREATE treats empty as Clear; PATCH still rejects empty string.",
    "Do not restore empty string or clear in valid_weather.",
]
current_state = ["admin_field.rs:413 unwrap None on POST /missions without weather."]
approach = ["CREATE treats empty weather as Clear canonical new-mission default."]
verify = ["Class-R + IT pin. Perturbation RED required."]
citations = [
    "Wave 43 re-gate FAIL T-508",
    "T-377 valid_weather clear arm",
    "admin_field.rs POST without weather",
    "Class-R IT perturbation RED",
]
shipped_at = "ebf1bae7"
created_at = "2026-07-27T09:24:22Z"
completed_at = "2026-07-27T09:29:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/api/src/handlers/missions.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
