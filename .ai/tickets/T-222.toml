id = "T-222"
kind = "work"
title = "CLIENT_ID is hardcoded to 1 for every peer"
summary = "Every MissionDocCore peer shared client id 1; concurrent merges silently dropped blocks. Mint per-peer yrs identity."
class = "bug"
status = "shipped"
order = 2400
executor = "claude-code"
context = [
    "store.rs hardcoded const CLIENT_ID u64 1 for every peer.",
    "yrs orders blocks by client and clock; shared id causes silent data loss.",
    "Sound today only because no peer update ever arrives.",
]
requirement = [
    "new uses Doc::new randomized yrs ClientID on production path.",
    "with_client_id for deterministic tests; client_id accessor.",
    "apply_update refuses own id past last issued clock.",
]
current_state = [
    "Two writers sharing id emit overlapping ranges; receiver drops blocks as duplicates.",
    "Rehydration takes fresh id so two tabs restoring same blob stay distinct peers.",
]
approach = [
    "No new dependency; fastrand via yrs js feature already in lockfile.",
    "Collision guard quiet on restore into fresh doc and peer echo during re-exchange.",
]
verify = ["wave 72 GATE PASS 23/23 verifier CLEAN."]
citations = [
    "commit 3fff288c yrs client id",
    "wave 72 close commit",
]
shipped_at = "b1369fe3"
created_at = "2026-07-31T05:10:09Z"
completed_at = "2026-07-31T05:24:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["crates/map-engine-core/src/doc/store.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
