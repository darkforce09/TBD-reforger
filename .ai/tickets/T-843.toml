id = "T-843"
kind = "work"
title = "Editor suite red; rect smokes never run automated"
summary = "The editor smoke suite is red and nothing automated runs it — two rect smokes are dead guards"
class = "bug"
status = "shipped"
order = 5250
executor = "claude-code"
notes = """
wave207 verifier F1 + orchestrator ledger correction; the T-742/T-759 family — a guard that examines nothing

⟡ WAVE-211 PREP DECISION (2026-08-19, operator-approved plan default): option (b) — keep chromium OUT of every `cargo xtask platform wave gate` run; make `cargo xtask mk leptos-gates` a **required editor-factory pre-close step**. Rect smokes (`save-dialog-rect`, `entrance-motion-rect`) are proven on that path (perturb → red → restore). Update `docs/platform/EDITOR_FACTORY_FOR_CURSOR.md` (and EDITOR_GATE_RUNBOOK if the recipe clause lives there) so the factory recipe names this path. Owns retargeted off deleted Makefile / scripts/platform/wave.sh onto live xtask + smokes surfaces (T-853/T-902 cutover).

--- HEAD migration_legacy (verbatim) ---

WAVE-207 VERIFIER MAJOR (wave207.md F1) + ORCHESTRATOR CORRECTION. TWO FACTS, both verified: (1) `make leptos-gates` -> `gate editor-suite` CANNOT RUN GREEN on main — smoke #4 `cur` fails on a stale '6400.000' expectation vs the readout's ' m' suffix (T-793, landed 2026-08-09) and #19 `undo` on stale dock text (T-637/T-696, 2026-08-07); the suite dies before reaching later smokes. (2) `scripts/platform/wave.sh` contains ZERO occurrences of 'smoke' — THE WAVE GATE RUNS NO EDITOR SMOKES AT ALL. Consequence: the `save-dialog-rect` smoke (wave 203) and `entrance-motion-rect` smoke (wave 207) — both well-built, both perturbation-proven — are DEAD GUARDS: nothing automated executes them, and the one manual path that would is broken. The features behind the two red smokes are HEALTHY (cursor math exact to the metre, docks fine) — this is expectation rot, not tree damage. FIX: (a) update the two stale expectations to the shipped behaviour (' m' suffix, current dock text) so the suite runs to completion; (b) DECIDE and record where the rect smokes get executed — either add an editor-suite step to the wave gate (cost: chromium in every gate run — measure it) or make `make leptos-gates` a required pre-close step in the factory recipe; a guard nobody runs is the program's signature defect wearing a green hat. ACCEPTANCE: `gate editor-suite` exits 0 on main; both rect smokes actually execute (prove by perturbing one and watching the chosen path go red); the factory recipe names the path that runs them. ⟡ ABSORBS T-829 (Gate smoke virtual-outliner v5_orbatWindowed fails deterministically under the verifier harness — config-sensitive smoke) — deliver its acceptance too.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["WAVE-207 VERIFIER MAJOR (wave207.md F1) + ORCHESTRATOR CORRECTION. TWO FACTS, both verified: (1) `make leptos-gates` -> `gate editor-suite` CANNOT RUN GREEN on main — smoke #4 `cur` fails on a stale '6400.000' expectation vs the readout's ' m' suffix (T-793, landed 2026-08-09) and #19 `undo` on stale dock text (T-637/T-696, 2026-08-07); the suite dies before reaching later smokes. (2) `scripts/platform/wave.sh` contains ZERO occurrences of 'smoke' — THE WAVE GATE RUNS NO EDITOR SMOKES AT ALL. Consequence: the `save-dialog-rect` smoke (wave 203) and `entrance-motion-rect` smoke (wave 207) — both well-built, both perturbation-proven — are DEAD GUARDS: nothing automated executes them, and the one manual path that would is broken. The features behind the two red smokes are HEALTHY (cursor math exact to the metre, docks fine) — this is expectation rot, not tree damage. FIX: (a) update the two stale expectations to the shipped behaviour (' m' suffix, current dock text) so the suite runs to completion; (b) DECIDE and record where the rect smokes get executed — either add an editor-suite step to the wave gate (cost: chromium in every gate run — measure it) or make `make leptos-gates` a required pre-close step in the factory recipe; a guard nobody runs is the program's signature defect wearing a green hat. ACCEPTANCE: `gate editor-suite` exits 0 on main; both rect smokes actually execute (prove by perturbing one and watching the chosen path go red); the factory recipe names the path that runs them. ⟡ ABSORBS T-829 (Gate smoke virtual-outliner v5_orbatWindowed fails deterministically under the verifier harness — config-sensitive smoke) — deliver its acceptance too."]

missing=0
"""
main_goal = "Fix stale editor smoke expectations and wire rect smokes into automated gate path."
context = [
    "gate editor-suite cannot run green on main smoke 4 cur stale 6400.000 vs m suffix T-793.",
    "Smoke 19 undo stale dock text T-637 T-696 suite dies before later smokes.",
    "Wave gate (cargo xtask platform wave gate) still runs no editor smokes — rect guards dead unless leptos-gates is required.",
]
requirement = [
    "Update two stale expectations to shipped behaviour m suffix current dock text.",
    "Record option (b): cargo xtask mk leptos-gates required editor pre-close; chromium stays out of every wave gate.",
    "Deliver T-829 absorption acceptance v5_orbatWindowed deterministic both harness configs.",
]
current_state = [
    "save-dialog-rect smoke wave 203 entrance-motion-rect wave 207 dead guards.",
    "Features behind red smokes healthy cursor math exact docks fine expectation rot.",
    "Owns retargeted: Makefile and wave.sh deleted (T-853/T-902); live path is xtask mk leptos-gates + smokes.rs.",
]
approach = [
    "Fix expectations so suite runs to completion first.",
    "Wire factory recipe to require cargo xtask mk leptos-gates pre-close; do not add chromium to wave gate.",
]
verify = [
    "cargo xtask mk leptos-gates (gate editor-suite) exits 0 on main.",
    "Both rect smokes execute prove perturb one leptos-gates path goes red then restore.",
    "EDITOR_FACTORY_FOR_CURSOR.md names cargo xtask mk leptos-gates as required pre-close for rect smokes.",
]
acceptance = [
    "gate editor-suite exits 0 on main including rect smokes and v5_orbatWindowed.",
    "Factory recipe names cargo xtask mk leptos-gates / editor-suite as required editor pre-close; wave gate stays chromium-free.",
    "Perturbing a rect smoke expectation turns the editor-suite path red; restore returns green.",
]
citations = [
    "wave207 verifier F1",
    "T-742 T-759 dead guard",
    "save-dialog-rect wave 203",
    "entrance-motion-rect wave 207",
    "T-829 absorption carrier",
]
shipped_at = "33f715b11"
created_at = "2026-08-19T17:00:00Z"
completed_at = "2026-08-19T17:31:47Z"
estimated = [
    "scope",
    "tokens",
]
owns = [
    "tools/tbd-tools/src/smokes.rs",
    "xtask/src/mk_build.rs",
    "xtask/src/wave/",
    "docs/platform/EDITOR_FACTORY_FOR_CURSOR.md",
    "docs/website/EDITOR_GATE_RUNBOOK.md",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
