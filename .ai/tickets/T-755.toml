id = "T-755"
kind = "work"
title = "Wave-115 residue: three pins narrower than the claims they narrate"
summary = "Wave-115 residue: three pins narrower than the claims they narrate"
class = "feature"
status = "shipped"
order = 4631
executor = "claude-code"
notes = '''
filed by wave-115 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

Three MINOR/NIT findings from the wave-115 verifier, grouped because they are one class: a pin whose assertion is weaker than the sentence describing it. Every underlying BEHAVIOUR was independently verified correct — these are gaps in the guarantees, not live defects. (1) eden_settings.rs:2188 / :2127 (T-688): the needle `"Self::Schema {"` would miss a differently-spelled second constructor of a value-carrying default, and the FLOW_DEFAULT_* ban does not scan `from_schema_node` itself. Both holes are backstopped behaviourally by the key-for-key schema agreement test, so the GUARANTEE holds while the MECHANISM claim does not. (2) eden_toolbelt.rs:~1690 (T-670): the claim that the scrubbed-source check fails if T-639 re-bases its scale convention is overstated — exact-string pins catch an in-place edit but not an adjustment line inserted between dem_vectors.rs:121 and :122, nor an upstream re-based zoom, and the numeric half of the proof is self-referential. Residual risk is small because the ladder consumes m/px directly. NOTE the slice also cited the wrong path: dem_vectors.rs is in the FRONTEND crate, not under crates/. (3) eden_top_strip.rs:2486 (T-692, pre-existing): the ControlsHint pin checks the mount is PRESENT, not that it is inside the gated subtree — which is the whole chrome-hiding mechanism. This wave's re-layout moved that mount and the position was verified structurally by hand, so nothing broke; but the pin does not defend what it narrates, and the next re-layout may not get a hand check. Diagnosis: editor_verify/wave115.md MINOR-2, MINOR-3, NIT-1.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["""Three MINOR/NIT findings from the wave-115 verifier, grouped because they are one class: a pin whose assertion is weaker than the sentence describing it. Every underlying BEHAVIOUR was independently verified correct — these are gaps in the guarantees, not live defects. (1) eden_settings.rs:2188 / :2127 (T-688): the needle `"Self::Schema {"` would miss a differently-spelled second constructor of a value-carrying default, and the FLOW_DEFAULT_* ban does not scan `from_schema_node` itself. Both holes are backstopped behaviourally by the key-for-key schema agreement test, so the GUARANTEE holds while the MECHANISM claim does not. (2) eden_toolbelt.rs:~1690 (T-670): the claim that the scrubbed-source check fails if T-639 re-bases its scale convention is overstated — exact-string pins catch an in-place edit but not an adjustment line inserted between dem_vectors.rs:121 and :122, nor an upstream re-based zoom, and the numeric half of the proof is self-referential. Residual risk is small because the ladder consumes m/px directly. NOTE the slice also cited the wrong path: dem_vectors.rs is in the FRONTEND crate, not under crates/. (3) eden_top_strip.rs:2486 (T-692, pre-existing): the ControlsHint pin checks the mount is PRESENT, not that it is inside the gated subtree — which is the whole chrome-hiding mechanism. This wave's re-layout moved that mount and the position was verified structurally by hand, so nothing broke; but the pin does not defend what it narrates, and the next re-layout may not get a hand check. Diagnosis: editor_verify/wave115.md MINOR-2, MINOR-3, NIT-1."""]

missing=0
'''
main_goal = "Tighten three wave-115 pins so assertion matches narrated guarantee."
context = [
    "Wave-115 MINOR/NIT class: pin assertion weaker than sentence describing it.",
    "Underlying behaviour verified correct; gaps are guarantee mechanism not live defects.",
    "Grouped because same defect class across env_settings toolbelt top_strip.",
]
requirement = [
    "eden_settings FLOW_DEFAULT ban must scan from_schema_node not only Self::Schema needle.",
    "eden_toolbelt dem_vectors scrub pin must catch re-base not only exact-string edit.",
    "eden_top_strip ControlsHint pin must assert gated subtree not mere mount presence.",
]
current_state = [
    "Self::Schema needle misses alternate constructor; ban skips from_schema_node itself.",
    "dem_vectors pin cited wrong path; exact-string misses inserted adjustment line upstream re-base.",
    "ControlsHint present pin does not defend chrome-hiding subtree after re-layout.",
]
approach = [
    "Strengthen schema agreement and FLOW_DEFAULT scan beyond literal needle match.",
    "Positional or behavioural dem_vectors pin; subtree placement pin for ControlsHint.",
]
verify = [
    "Perturbed alternate constructor or from_schema_node default fails env_settings pin.",
    "Re-based zoom or inserted line fails toolbelt pin; relocated mount outside gate fails hint pin.",
]
acceptance = [
    "FLOW_DEFAULT ban scanned from_schema_node; second Schema constructor pin went RED on alias path.",
    "Contour m_per_px pin required contiguous dem_vectors bind-to-ladder without zoom rebind gap.",
    "ControlsHint pin asserted STRIP_ROWS gated subtree placement, not mount presence alone.",
]
citations = [
    "editor_verify wave115 MINOR-2",
    "editor_verify wave115 MINOR-3",
    "editor_verify wave115 NIT-1",
    "T-688 eden_settings pin",
]
shipped_at = "7f57a454"
created_at = "2026-08-08T05:40:49Z"
completed_at = "2026-08-08T05:45:15Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_settings.rs",
    "apps/website/frontend/src/eden_toolbelt.rs",
    "apps/website/frontend/src/eden_top_strip.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "env_settings",
    "toolbelt",
    "top_strip",
]
