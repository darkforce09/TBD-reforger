id = "T-675"
kind = "program"
title = "Vehicle roster reaches game — T-076 compile half"
summary = """
Cross-boundary compile half split from T-076 so crew UI could ship separately.
Closes the sixth T-216 drop-ledger row: top-level vehicles[] with seats and crew refs.
T-706 already widened the schema; this program emits and reads the roster.
Unmerged salvage 113108a1 overlaps T-674 — check subsumption before redo.
"""
status = "queued"
order = 4320
executor = "claude-code"
notes = "workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row — WAVE-A DISPATCH PIN: owns extends to crates/map-engine-core/src/mission/flatten.rs (the emit half; T-674 first, T-675 after it reports) AND the TBD_MissionValidator.c SCHEMA_1_3 allowlist bump rides whichever of these first emits 1.3 (schema description names this). T-853 2026-08-12: the prepped slice worktree for this ticket was DROPPED, not landed. It carried one unmerged commit from 2026-08-02 that had sat unlanded for 10 days and was blocking the wave queue at 150. The work was NOT reviewed and NOT merged; the ticket stays queued so it can be redone. The commit is recoverable: `git checkout -b redo/T-675 salvage/t853-dropped/T-675` (tagged before the branch was deleted). Note T-674 and T-675 pointed at the SAME commit 113108a1, so whichever is redone first should check whether it subsumes the other."
depends_on = ["T-076"]
children = [
    "T-675.1",
    "T-675.2",
]
context = [
    "Split from T-076; crew UI shipped; compile half stayed queued.",
    "T-216 ledger sixth drop: vehicles[] at document root.",
    "Schema widening shipped in T-706; emit and reader remain.",
    "Salvage commit 113108a1 shared with T-674; worktree dropped T-853.",
]
requirement = [
    "T-675.1 emits top-level vehicles[] from flatten compile boundary.",
    "T-675.2 reads wire roster and drives crew spawn at runtime.",
    "Do not touch packages/tbd-schema; T-706 owns widening.",
]
current_state = [
    "Flatten drops authored vehicle roster today; ledger pins T-675.",
    "entities[] alias path cannot carry roster without T-200 defect.",
    "Dropped slice worktree; commit recoverable on salvage tag.",
]
approach = [
    "Land T-675.1 emit after T-674 reports; then T-675.2 reader.",
    "Redo from salvage/t853-dropped/T-675; check T-674 overlap first.",
]
citations = [
    "T-076 crew UI split",
    "T-706 schema widening",
    "commit 113108a1 salvage",
    "T-216 drop ledger sixth row",
]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c"]
