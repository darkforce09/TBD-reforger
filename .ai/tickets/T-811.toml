id = "T-811"
kind = "work"
title = "Layer rename focus: F-02 landed wrong widget"
summary = "Layer rename never receives focus — the F-02 fix landed on the bookmark widget, not the layer tree"
class = "bug"
status = "shipped"
order = 4930
executor = "claude-code"
notes = '''
wave 209; verifier wave200.md F1; probe harness pattern: review section 2 F-01 + scratchpad cdp.py | SHIPPED W209: acceptance re-proved live by the Fable verifier (both rename flows focus on mount, whole-word typing, GRID immune, node survives). Its source pin shipped HOLLOW (raw include_str self-match — verifier F2, the T-759 class) — un-hollowed in-wave 27343f82, perturbation-proven RED on production node_ref deletion.

--- HEAD migration_legacy (verbatim) ---

WAVE-200 VERIFIER RE-TICKET (source: .ai/artifacts/editor_verify/wave200.md F1, MAJOR; hostile review F-02). VERIFIED LIVE (real per-char CDP): the actual Layers rename — pencil aria-label="Rename layer" eden_tree.rs:462, input ~:836-867 — relies on bare autofocus=true on a reactively-inserted node, which browsers skip ('a node created by a later reactive update is skipped' — the mechanism note T-785 itself added at eden_dock_left.rs:330-338). Creation flow (New layer -> armed inline rename): input mounts prefilled 'New Layer 1', focused_on_mount:false, activeElement=BUTTON; real 'g' keydown flipped GRID off->move; typed 'Assault Sqd' never reached the input. Pencil flow: same — value unchanged after 11 typed chars. T-785's source-pins pin the BOOKMARK rename and pass green — a green pin on the wrong widget. FIX: check FIRST whether the rename draft round-trips through a signal the tree list renders from (the T-812 remount trap) — if so decouple the draft; then NodeRef + on_load focus+select (the eden_dock_left.rs:330-338 pattern). Cover BOTH flows. TRAPS: do not touch T-785's attributes.rs text_field work; layer-row drop-target/selection clicks must keep working while a rename is open. ACCEPTANCE (re-run the verifier probe): both flows — focused_on_mount === true; type an 11-char string with spaces -> value === typed, activeElement === the input, GRID chip byte-identical; Enter/blur commits the rename; Escape abandons.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["""WAVE-200 VERIFIER RE-TICKET (source: .ai/artifacts/editor_verify/wave200.md F1, MAJOR; hostile review F-02). VERIFIED LIVE (real per-char CDP): the actual Layers rename — pencil aria-label="Rename layer" eden_tree.rs:462, input ~:836-867 — relies on bare autofocus=true on a reactively-inserted node, which browsers skip ('a node created by a later reactive update is skipped' — the mechanism note T-785 itself added at eden_dock_left.rs:330-338). Creation flow (New layer -> armed inline rename): input mounts prefilled 'New Layer 1', focused_on_mount:false, activeElement=BUTTON; real 'g' keydown flipped GRID off->move; typed 'Assault Sqd' never reached the input. Pencil flow: same — value unchanged after 11 typed chars. T-785's source-pins pin the BOOKMARK rename and pass green — a green pin on the wrong widget. FIX: check FIRST whether the rename draft round-trips through a signal the tree list renders from (the T-812 remount trap) — if so decouple the draft; then NodeRef + on_load focus+select (the eden_dock_left.rs:330-338 pattern). Cover BOTH flows. TRAPS: do not touch T-785's attributes.rs text_field work; layer-row drop-target/selection clicks must keep working while a rename is open. ACCEPTANCE (re-run the verifier probe): both flows — focused_on_mount === true; type an 11-char string with spaces -> value === typed, activeElement === the input, GRID chip byte-identical; Enter/blur commits the rename; Escape abandons."""]

missing=0'''
main_goal = "Fix layer rename focus on pencil and creation flows not bookmark widget."
context = [
    "Wave-200 F1 MAJOR: Layers rename pencil eden_tree 462 input 836-867 uses bare autofocus.",
    "Browsers skip autofocus on reactively-inserted nodes per T-785 eden_dock_left 330-338 note.",
    "T-785 source-pins bookmark rename pass green; green pin on wrong widget.",
]
requirement = [
    "Check rename draft round-trips through tree signal T-812 remount trap; decouple if needed.",
    "NodeRef plus on_load focus select eden_dock_left 330-338 pattern both creation pencil flows.",
    "Do not touch T-785 attributes.rs text_field; layer-row clicks work during open rename.",
]
current_state = [
    "Creation flow New layer input prefilled focused_on_mount false activeElement BUTTON.",
    "Real g keydown flipped GRID off move; typed Assault Sqd never reached input.",
    "Pencil flow value unchanged after eleven typed chars same autofocus skip mechanism.",
]
approach = [
    "Cover both creation armed inline rename and pencil aria-label Rename layer flows.",
    "Re-run verifier probe focused_on_mount true eleven-char string GRID byte-identical.",
    "Un-hollow source pin perturbation-proven RED on production node_ref deletion.",
]
verify = [
    "Both flows focused_on_mount true type eleven-char string value equals typed.",
    "activeElement equals input GRID chip byte-identical during typed rename.",
    "Enter blur commits rename; Escape abandons rename draft.",
]
acceptance = [
    "Layer rename pencil and creation flows focused on mount via NodeRef; decoupled draft ended remount keystroke loss.",
    "Eleven-character typed rename landed whole in the input; GRID chip stayed byte-identical while typing.",
    "Layer-rename source pin un-hollowed; deleting production node_ref perturbation went RED then restored green.",
]
citations = [
    "editor_verify wave200 F1",
    "hostile review F-02",
    "T-785 bookmark pin trap",
    "eden_dock_left 330-338 pattern",
    "27343f82 layer rename pin",
]
shipped_at = "27343f82"
created_at = "2026-08-09T15:02:49Z"
completed_at = "2026-08-09T17:26:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/frontend/src/eden_tree.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["outliner"]
