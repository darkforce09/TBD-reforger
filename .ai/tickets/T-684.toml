id = "T-684"
kind = "work"
title = "Mission parameters as first-class document objects"
summary = "Mission parameters as first-class document objects"
class = "bug"
status = "queued"
order = 4440
executor = "claude-code"
notes = "workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row. Ranked 16 of 16 by framework_synthesis.md C.5; filed as `idea` deliberately. Filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md)"
main_goal = "Design mission launch parameters as first-class document objects."
context = [
    "B9 framework_synthesis C.5 ranks last of sixteen; weakest evidence in set.",
    "OFCRA twenty-nine Params versus FNF v4 single PerformanceTweaks parameter.",
    "Promoted idea to queued 2026-08-02; FNF-DTAS parameter bugs prove drift bites.",
]
requirement = [
    "Named launch parameters with title, values, default, consuming symbol at boot.",
    "Mod-side reader selects parameter set at launch not authoring time.",
]
current_state = [
    "DTAS description.ext value label pairing bugs argue for editor rule not params.",
    "Document shape change needs T-706 widening plus TBD_MissionParams.c reader.",
    "Schema half split T-706; this ticket is Enfusion reader plus editor UI.",
]
approach = [
    "Read framework_synthesis C.3 evidence before schema or mod reader design.",
    "Ship mod reader in owns path; defer widening until T-706 program lands.",
]
verify = [
    "Headless compile clean with TBD_MissionParams.c parameter selection reader.",
    "Launch-time parameter choice reaches mod without re-baking authored mission.",
]
citations = [
    "framework_synthesis B9 rank sixteen",
    "fnf_tooling DTAS parameter bugs",
    "T-706 schema widening split",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionParams.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
