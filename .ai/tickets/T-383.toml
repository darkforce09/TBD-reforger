id = "T-383"
kind = "work"
title = "Tooling paths overwrite committed files silently"
summary = "Four tooling paths overwrite committed files with empty or wrong content"
class = "bug"
status = "shipped"
order = 3199
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now.
"1.1"` 'Latest 'refuse (T-378 --in-place` 2026-07-26 = == `"T-066"`. `flatten-orbat-slots `idea` `inject_marker_block` `loadout` `schemaVersion `tickets` `uid` a A above. actually after All Also: always are audits backlog bare be block blocks budget bug bugs, but BY call, can CANCELLED CLAUDE.md's costing could DECISION, DEFERRED developing diagnosed, do dropping empty, faster feature field findable, fixing force-stamps fully generating go goldens, good, had heading here hours. in it it, its judged keys, knowing lands. map/labels.rs:455, map/sap.rs:1007. marker means missing moved need NOT not notes Nothing nothing now now. on only OPERATOR operator's optional. original output' produced Promote ready real Recording red. reproducible right run same schema's shared shipped' ships. silently something something, spending starts status substance: T-182..T-297 tail than that The them there things This this ticket time, token value; verbatim was were what when why will with work world/aux.rs:1009/1022/1050, world/build.rs:664/958, worst). write writing wrong xtask/src/schema_gates.rs:1174-1195 xtask/src/sync.rs:403"""
priority = 2
main_goal = "Refuse structurally empty tool output before overwriting committed registry or golden files."
context = [
    "Tail of tooling cluster; T-378 has the two worst.",
    "Same shape: success path writes structurally empty or silently-lossy content over committed.",
]
requirement = ["Shared refuse-to-write-structurally-empty-output helper would close most of them."]
current_state = [
    "xtask/cmds.rs:765-774 cmd_set_status — no non-empty check, no enum.",
    'set-status T-090 "" writes status:"" over ready; registry is source of truth.',
    "xtask/sync.rs:403 + registry.rs:133-138 inject_marker_block collapses ROADMAP marker.",
    "CLAUDE.md Latest shipped falls back to HARDCODED WRONG T-066.",
    "xtask/schema_gates.rs:1174-1195 flatten-orbat-slots emits 8 of 11 slot keys.",
    "Silently drops loadout and uid from 3 of 5 committed goldens.",
    "Force-stamps schemaVersion 1.1 over deliberate 1.0 fixture.",
    "enf/apidoc.rs:168 header-only TSV overwrite exits 0.",
    "Also world/aux.rs, world/build.rs, map/labels.rs, map/sap.rs.",
]
approach = ["None urgent alone; together they explain silent golden or registry corruption."]
citations = [
    "T-378 tooling cluster",
    "xtask/cmds.rs set-status",
    "schema_gates flatten-orbat",
    "inject_marker_block ROADMAP",
]
shipped_at = "ee1070f5"
created_at = "2026-07-27T12:38:26Z"
completed_at = "2026-07-27T12:52:59Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "xtask/src/cmds.rs",
    "xtask/src/sync.rs",
    "xtask/src/schema_gates.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
