id = "T-873"
kind = "work"
title = "T-853 port: tbd-spawn-verify -> mod spawn-verify"
summary = "T-853 port: tbd-spawn-verify -> mod spawn-verify"
class = "chore"
status = "shipped"
order = 5540
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
estimated = ["scope"]
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/tbd-spawn-verify.sh (32 lines) to `cargo xtask mod spawn-verify` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). spawn verification helper. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/tbd-spawn-verify.sh",
    "xtask/src/gate_tbd_spawn_verify.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
