id = "T-181.11.2"
kind = "work"
title = "Server-authoritative admin menu screen"
summary = "Admin UI on TBD_AdminService: roster, respawn, deploy, stages, audit."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Admin menu @ c4708f72. A FRONT-END onto the same server authority as the chat commands, not a second implementation — TBD_AdminService is the single choke point and permission is checked at four entry points including the READ gate, so a non-admin's snapshot contains a refusal string and nothing else (no roster to render even with a patched client). Deploy and Respawn deliberately kept separate: AdminRespawn refuses anyone not dead and DeployPlayerEx refuses anyone who is, so one button would silently no-op half the time. Force-stage is armed-then-confirmed. Audit ring is capped at 60 with repeat refusals console-only, so a client spamming refusals cannot flush real actions out of an admin's view. Avoided a 4th modded SCR_PlayerController by extending the existing block. PERMISSION IS PROVEN BY CONSTRUCTION, NOT BY A LIVE NON-ADMIN ATTEMPT — needs the operator."
parent = "T-181"
context = ["Wave 3 admin surface; chat commands share TBD_AdminService authority."]
requirement = [
    "Server-fed owner RPC snapshot; non-admins get refusal string only.",
    "Respawn and deploy stay separate; force-stage needs confirm; audit capped.",
]
current_state = [
    "Chat admin commands shared TBD_AdminService; no screen UI existed.",
    "Non-admin snapshot leakage risk if roster rendered client-side.",
]
approach = [
    "TBD_AdminScreen on AdminService owner snapshot with READ gate.",
    "Separate respawn and deploy buttons; confirmed force-stage; audit ring capped.",
]
verify = ["scripts/mod/compile.sh exit 0; commit c4708f72."]
acceptance = [
    "Admin menu shipped at c4708f72 on single AdminService choke point.",
    "Refusal-only snapshot by construction; live non-admin attempt not run.",
]
citations = [
    "Source: commit c4708f72 — admin menu.",
    "commit c4708f72 admin menu",
    "notes permission proven by construction",
]
shipped_at = "c4708f72"
created_at = "2026-07-25T09:36:01Z"
completed_at = "2026-07-25T09:36:01Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"
owns = ["Scripts/Game/TBD/UI/Admin/** (new)"]

[scope]
domain = "repo"
layer = "docs"
