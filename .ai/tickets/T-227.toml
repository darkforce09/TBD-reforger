id = "T-227"
kind = "work"
title = "Silent zero-slot event attach allows unlimited registration"
summary = "Silent zero-slot event attach allows unlimited registration"
class = "bug"
status = "shipped"
order = 2450
executor = "claude-code"
notes = "Also flagged: a duplicate attach returns 500 rather than 409, because idx_event_mission's violation is unmapped."
main_goal = "Capacity is then count(orbat_slots)=0, so the capacity guard never fires."
context = [
    "orbat_template_for_mission returned empty vec on missing version, swallowed DB error, or unparseable orbat.",
    "add_event_mission still committed 201; events.max_slots was never consulted for capacity.",
]
requirement = [
    "Refuse zero-slot attach; return 500 on materialize_slots DB failure instead of silent 201.",
    "Wire max_slots as event-level capacity bound counted across missions.",
]
current_state = [
    "orbat_template_for_mission returns an empty vec on a missing version, a swallowed DB error or an unparseable orbat",
    "— and add_event_mission still commits and returns 201 with the admin UI toasting success.",
    "events.max_slots is validated on create and never consulted for capacity anywhere.",
    "SHIPPED. The mechanism, put better than the ticket did: the guard was `capacity > 0 && registered >= capacity`",
    "— **at 0 that clause does not protect the comparison, it switches it off.**",
    "Reproduced with 40 distinct users through the real handler with real sessions: registered=40 against capacity 0 and max_slots 8.",
]
approach = [
    "THE SWALLOWED DB ERROR REPRODUCED DELIBERATELY — the half nobody had demonstrated.",
    "One valid 3-slot published version, healthy DB -> 201 slots=3; then `mission_versions` RENAMED AWAY -> **201 slots=0**.",
    "A database failure and 'no published version' were the same answer.",
    "After the fix: 500, logged, and ZERO event_missions rows committed.",
    "SUB-BUG FOUND: an unparseable explicit orbat[] did not merely vanish",
    "— parse_orbat_template's unwrap_or_default() fell through to derive_orbat_from_editor,",
    "so a **different seating plan could be materialized under a 201.**",
    "DECISION 1, refuse — decided on a fact rather than a preference:",
    "materialize_slots is the only producer of orbat_slots through the API,",
    "so a zero-slot event_missions row can NEVER gain slots;",
    "the only recovery is detach and re-attach, which an admin must know to do after being told it succeeded.",
    "And the SPA cannot use the result either — event_hub.rs:914 disables Register while no slot is selected,",
    "so a zero-slot dossier is a dead card.",
    "DECISION 3, max_slots WIRED not removed — and it caught that MY suggested wiring ('capacity when there is no ORBAT') is DEAD CODE under decision 1.",
    "It bounds the EVENT instead: max_slots is on the container, orbat_slots on one mission,",
    "so it is a genuine second bound, counted in DISTINCT PEOPLE because a second mission of the same operation is one person attending once.",
    "That count spans missions, which is why the event row is now taken FOR UPDATE OF e,",
    "with the lock order documented (event_missions -> events, and nothing takes the reverse).",
]
verify = [
    "It had to change tests/lifecycle_extra.rs:142 — one hunk — because that test's SETUP depended on the silent zero-slot success:",
    "it attached an ORBAT-less mission and discarded the status.",
    "Now attaches an explicit one-slot ORBAT and asserts 201; the test's subject is unchanged.",
]
acceptance = ["Zero-slot attach returned error; DB failures blocked commit; max_slots enforced with event row locked."]
citations = [
    "event_hub.rs:914",
    "lifecycle_extra.rs:142",
    "idx_event_mission violation",
]
shipped_at = "28e9fbb6"
created_at = "2026-07-26T12:27:47Z"
completed_at = "2026-07-26T12:32:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/api/src/handlers/events.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
