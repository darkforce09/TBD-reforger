id = "T-946.3"
kind = "work"
title = "Mod compile gate blocked by pre-existing non-ASCII"
summary = "The mod compile gate fails its ASCII scan on three WorkbenchGame map-export scripts before any compilation runs, so no mod script change can be gated until they are transliterated."
class = "chore"
status = "shipped"
order = 7076
spec = "docs/specs/ideas/t946_3_mod_gate_ascii.md"
plan = "docs/plans/t-946_3_plan.md"
executor = "claude-code"
priority = 1
parent = "T-946"
main_goal = "The mod compile gate reaches the compiler, so Enfusion slices can be gated at all."
context = [
    "cargo xtask mod compile runs ascii_check_export over apps/mod/tbd-export/Scripts before booting the server (gate_mod_compile.rs:412).",
    "Three WorkbenchGame files carried 15 em-dashes, so the gate failed without compiling a line.",
    "The operator has ruled mod .c scripts agent-editable with this gate as the check; while it fails, nothing can be gated.",
    "Wave 240 shipped both copies of TBD_MissionValidator.c UNCOMPILED for this reason; wave 241 dispatches T-676, a new Enfusion runtime.",
]
requirement = [
    "The three tbd-export scripts contain no bytes above 0x7F.",
    "The ASCII scan itself is unchanged: it is the only pre-restart guard for the WorkbenchGame module.",
    "No logic changes; comments and Print strings still read correctly in a log line.",
]
current_state = ["cargo xtask mod compile FAILs its ASCII scan before compilation, so no Enfusion change can be gated."]
approach = [
    "Transliterate the 15 em-dashes to ASCII hyphens in TBD_WorldFullExportPlugin.c, TBD_BlueprintReconPlugin.c and TBD_MapExportObjects.c.",
    "Re-run the gate and record the engine's own verdict, which is the thing the scan was hiding.",
]
verify = [
    '''grep -rlP '[^\x00-\x7F]' apps/mod/tbd-export --include='*.c'''',
    "cargo xtask mod compile",
]
acceptance = [
    "cargo xtask mod compile gets past the ASCII scan and reports the engine verdict.",
    "No non-ASCII bytes remain in any tbd-export .c file; tbd-framework stays exempt as documented.",
]
created_at = "2026-09-06T08:12:51Z"
completed_at = "2026-09-06T09:38:18Z"
owns = [
    "apps/mod/tbd-export/Scripts/WorkbenchGame/MapExport/Objects/TBD_WorldFullExportPlugin.c",
    "apps/mod/tbd-export/Scripts/WorkbenchGame/MapExport/Objects/Buildings/TBD_BlueprintReconPlugin.c",
    "apps/mod/tbd-export/Scripts/WorkbenchGame/MapExport/Objects/TBD_MapExportObjects.c",
]

[scope]
domain = "repo"
layer = "docs"
