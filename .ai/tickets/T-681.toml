id = "T-681"
kind = "work"
title = "Entity states: health, allow-damage, show-model, size, stamina"
summary = "Entity states: health, allow-damage, show-model, size, stamina"
class = "chore"
status = "queued"
order = 4380
executor = "claude-code"
notes = "workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row"
main_goal = "Implement Enfusion entity health, damage, model, size, stamina reader."
context = [
    "Five OBJ attribute ids; entities[] has no consumer on shipped builds.",
    "mission.schema.json:72 documents zero readers for entities array today.",
    "OBJ-STAMINA needs Reforger per-character stamina API answer first.",
]
requirement = [
    "Apply health allow-damage show-model size stamina in TBD_EntityState.c.",
    "Gate on entities[] consumer existing; no packages/tbd-schema edits here.",
]
current_state = [
    "Adding fields to entities[] authors state with no runtime destination.",
    "Stamina toggle unresolved Workbench API question; size before implementation.",
]
approach = [
    "Resolve stamina API question; then implement reader after T-706 widening.",
    "Schema half remains T-706; editor UI rides this Enfusion reader ticket.",
]
verify = [
    "Headless scripts/mod/compile.sh clean with TBD_EntityState.c changes.",
    "Entity state attributes applied when entities[] consumer exists at runtime.",
]
citations = [
    "mission.schema.json entities consumer",
    "T-706 schema widening split",
    "OBJ-STAMINA unresolved API gate",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_EntityState.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
