id = "T-181.8"
kind = "work"
title = "Replicated gamemode stage state machine"
summary = "FrameworkManager stage enum, replicated SetStage, and IsFrameworkWorld mostly pre-built; BRIEFING and SAFE_START lacked content."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = 'MOSTLY ALREADY BUILT — the factory caught this before a rewrite. TBD_FrameworkManager already has enum TBD_EGameStage{LOADING,LOBBY,BRIEFING,SAFE_START,LIVE,END,DEBRIEF}, a replicated [RplProp(onRplName:"OnStageReplicated")] m_Stage, SetStage()+Replication.BumpMe(), and IsFrameworkWorld(). Remaining gap is stage CONTENT: BRIEFING and SAFE_START are enum-only (no logic).'
parent = "T-181"
context = [
    "Reforger ships no lobby, briefing, or slotting; the mod must stage the event spine.",
    "Factory review found TBD_EGameStage, replicated m_Stage, and SetStage already present.",
]
requirement = [
    "Retain replicated gamemode stage transitions across LOADING through DEBRIEF.",
    "Track BRIEFING and SAFE_START as enum-only until later slices ship stage content.",
]
current_state = [
    "Factory found TBD_EGameStage, replicated m_Stage, SetStage already present.",
    "BRIEFING and SAFE_START were enum-only without stage content handlers.",
]
approach = [
    "Retain replicated gamemode spine; document content gaps for later slices.",
    "Defer briefing and safestart bodies to T-181.8.1 supersession path.",
]
verify = ["scripts/mod/compile.sh exit 0 on factory bootstrap."]
acceptance = [
    "Replicated LOADING–DEBRIEF enum, SetStage, and IsFrameworkWorld remained live.",
    "BRIEFING and SAFE_START content gaps explicitly tracked for follow-on slices.",
]
citations = [
    "Source: t181_event_mod_program.md — state machine.",
    "commit d8fc6abc FrameworkManager bootstrap",
    "notes mostly already built factory",
]
created_at = "2026-07-25T00:00:00Z"
completed_at = "2026-07-31T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-181 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "docs"
