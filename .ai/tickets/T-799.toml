id = "T-799"
kind = "work"
title = "Exports fire once; editor and payloads agree on mission"
summary = "Exports fire once and agree with the editor about the mission"
class = "bug"
status = "shipped"
order = 4810
executor = "claude-code"
notes = """
RowMirror parents T-192/T-746 cited; ajv-clean baseline from review F-34 addendum must not regress | SHIPPED W205: exports fire once per gesture (timestamp latch; real second clicks pass — 4 stamps : 4 blobs), per-field metadata unified (maxPlayers/gameMode from row, title from live doc via compile-time override), compiled stays ajv-2020-12 clean, camelCase + filenames intact. CO-TASK delivered (strip relabel + third button + census) with the pre-declared reconciliation landed at the barrier (44eb1412).
--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-28/F-34 — VERIFIED BY BLOB INTERCEPT: Export Compiled fires TWICE per click (two createObjectURL + two anchor clicks, same 3213B payload — DOM-level double-activation; the handler itself fires once in code, mission_commands.rs ~:742/:795/:885) and the two exports DISAGREE about the same mission: JSON export carries the live editor title ('UXREVIEW-Aqqqqqq') but maxPlayers: 0 and gameMode: ''; Compiled carries playerRange [1,64] but the STALE library-row title — the RowMirror gap (eden_top_strip.rs ~:344-410 mirrors only time_of_day+weather; row meta ~:499-559). The compiled artifact itself VALIDATES CLEAN against mission.schema.json (ajv 2020-12, zero errors — verified) so this is metadata sourcing, not schema. FIX: (a) de-duplicate the compiled export activation (guard against the double pointerup/click pair — same family as the F-03/F-12 input-model note on T-785); (b) one metadata source: JSON export reads maxPlayers/gameMode from the same place the create dialog wrote (row meta), version field reports the LATEST saved version; compiled export title comes from the live doc title (extend RowMirror to PATCH title on edit, or compile-time override) — pick one source and both exports read it; cite T-192/T-746 as the RowMirror owners and reconcile rather than fork. ⚠️ TRAPS: the export JSON is the one camelCase contract exception (CLAUDE.md) — do not snake_case it; blob filename convention mission-<id>[.compiled].json stays. ACCEPTANCE: scripted intercept — one click => exactly ONE createObjectURL + ONE anchor click per export type; export both after retitling the mission: both payloads carry the new title, identical maxPlayers/gameMode/version triple, and compiled still ajv-validates clean.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-28/F-34 — VERIFIED BY BLOB INTERCEPT: Export Compiled fires TWICE per click (two createObjectURL + two anchor clicks, same 3213B payload — DOM-level double-activation; the handler itself fires once in code, mission_commands.rs ~:742/:795/:885) and the two exports DISAGREE about the same mission: JSON export carries the live editor title ('UXREVIEW-Aqqqqqq') but maxPlayers: 0 and gameMode: ''; Compiled carries playerRange [1,64] but the STALE library-row title — the RowMirror gap (eden_top_strip.rs ~:344-410 mirrors only time_of_day+weather; row meta ~:499-559). The compiled artifact itself VALIDATES CLEAN against mission.schema.json (ajv 2020-12, zero errors — verified) so this is metadata sourcing, not schema. FIX: (a) de-duplicate the compiled export activation (guard against the double pointerup/click pair — same family as the F-03/F-12 input-model note on T-785); (b) one metadata source: JSON export reads maxPlayers/gameMode from the same place the create dialog wrote (row meta), version field reports the LATEST saved version; compiled export title comes from the live doc title (extend RowMirror to PATCH title on edit, or compile-time override) — pick one source and both exports read it; cite T-192/T-746 as the RowMirror owners and reconcile rather than fork. ⚠️ TRAPS: the export JSON is the one camelCase contract exception (CLAUDE.md) — do not snake_case it; blob filename convention mission-<id>[.compiled].json stays. ACCEPTANCE: scripted intercept — one click => exactly ONE createObjectURL + ONE anchor click per export type; export both after retitling the mission: both payloads carry the new title, identical maxPlayers/gameMode/version triple, and compiled still ajv-validates clean."]

missing=0
"""
main_goal = "De-duplicate compiled export activation; unify metadata source for JSON and compiled exports."
context = [
    "Export Compiled fires twice per click via DOM double-activation.",
    "JSON and compiled exports disagree on title maxPlayers gameMode.",
    "Compiled artifact validates clean; problem is metadata sourcing not schema.",
]
requirement = [
    "Guard compiled export against double pointerup click pair.",
    "One metadata source: row meta for maxPlayers gameMode version.",
    "Compiled export title from live doc title via RowMirror or override.",
]
current_state = [
    "mission_commands.rs handler fires once; DOM triggers two downloads.",
    "eden_top_strip RowMirror mirrors only time_of_day and weather.",
    "JSON export title live; compiled uses stale library-row title.",
]
approach = [
    "Timestamp or latch de-duplication per export gesture.",
    "Reconcile with T-192 T-746 RowMirror owners not fork paths.",
    "Preserve camelCase export JSON and mission-id blob filenames.",
]
verify = [
    "One click: exactly ONE createObjectURL and ONE anchor click per export type.",
    "After retitle both payloads carry new title and identical maxPlayers gameMode version.",
    "Compiled export still ajv-validates clean.",
]
acceptance = [
    "Compiled export deduplicated to one createObjectURL and anchor click per gesture via timestamp latch.",
    "JSON and compiled exports carried aligned title maxPlayers and gameMode; compiled payload stayed ajv-2020-12 clean.",
]
citations = [
    "editor_hostile_ux_review F-28 F-34",
    "blob intercept double activation",
    "RowMirror gap eden_top_strip",
    "ajv 2020-12 zero errors baseline",
    "T-192 T-746 RowMirror parents",
    "f9e72d96e single export fire",
]
shipped_at = "44eb1412"
created_at = "2026-08-10T16:29:04Z"
completed_at = "2026-08-10T16:34:57Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_commands.rs",
    "apps/website/frontend/src/eden_top_strip.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "top_strip",
]
