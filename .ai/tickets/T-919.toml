id = "T-919"
kind = "work"
title = "Wall triage: drain migration_legacy into typed fields"
summary = "Wall triage: drain migration_legacy into typed fields"
class = "chore"
status = "shipped"
order = 5990
spec = "docs/platform/t917_ticket_schema_v2.md"
plan = "docs/plans/t-919_plan.md"
executor = "claude-code"
notes = """
Bookkeeping stream, not factory slices: batches interleave after T-917.3 as operator review allows. Walls carry recognizable FIX:/ACCEPTANCE:/Repro: idioms. Per batch: pin shrinks by exactly the batch size, remaining files still pass the reversibility join-proof, check --strict green.

--- HEAD migration_legacy (verbatim) ---

Pass 2 of the wall decomposition: AI batches of 20-30 tickets per operator-reviewed commit, each wall decomposed from migration_legacy[] into the typed body fields (context/requirement/current_state/approach/verify/citations) and migration_legacy deleted in the same edit, shrinking the T-917.3 ratchet pin every commit. Semantic work — deliberately outside the mechanical migration.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["Pass 2 of the wall decomposition: AI batches of 20-30 tickets per operator-reviewed commit, each wall decomposed from migration_legacy[] into the typed body fields (context/requirement/current_state/approach/verify/citations) and migration_legacy deleted in the same edit, shrinking the T-917.3 ratchet pin every commit. Semantic work — deliberately outside the mechanical migration."]

missing=0"""
depends_on = ["T-917.3"]
main_goal = "The quarantined 500-word walls become clearly categorised one-liners, batch by reviewed batch, until the pin hits zero and no ticket carries unprocessed prose."
context = [
    "T-917.3 quarantined 694 over-cap summaries verbatim into migration_legacy behind a shrink-only pin; the prose is preserved but uncategorised.",
    "The ten typed body fields sit empty on those tickets; pass 2 is semantic work, deliberately kept out of the mechanical migration.",
    "Pass 2 wall decomposition: operator-reviewed commits shrink T-917.3 ratchet pin every batch.",
]
requirement = [
    "Decompose every quarantined wall into the typed body fields in operator-reviewed batches until the pin hits zero.",
    "Delete migration_legacy in the same edit; shrink the ratchet pin by exactly the batch size in the same commit.",
    "Per T-920: each batch also fills the full tier set and repairs titles on the tickets it drains, shrinking those pins too.",
    "Target fields per wall: context requirement current_state approach verify citations.",
]
current_state = ["669 carriers remain after batch 1 drained 25; walls carry recognisable FIX:/ACCEPTANCE:/Repro: idioms."]
approach = [
    "AI batches of 20-30 tickets per reviewed commit; content is reorganised, never invented — lines fitting no field land in notes.",
    "The anti-blend definitions are the sorting authority; every non-whitespace token of the wall survives into some field or notes.",
]
verify = [
    "Per batch: cargo test -p tbd-tickets green — the ratchet pins enforce both directions; remaining files pass the reversibility join-proof.",
    "cargo xtask ticket check --strict prints check OK; wave.lock byte-identical per batch.",
]
acceptance = [
    "Per batch: ratchet pin shrinks by exactly the batch size in the same commit",
    "Remaining quarantined files still pass the byte-reversibility join-proof",
    "cargo xtask ticket check --strict prints check OK after every batch",
]
citations = [
    "t917_ticket_schema_v2.md",
    "T-917.3 shrink-only ratchet pin",
    "T-920 tier-set batch fills",
]
shipped_at = "9db72b75"
created_at = "2026-08-14T21:54:38Z"
completed_at = "2026-08-15T17:55:09Z"
estimated = ["tokens"]
owns = [".ai/tickets"]

[scope]
domain = "repo"
layer = "tickets"
