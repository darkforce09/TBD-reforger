id = "T-670"
kind = "work"
title = "Scale readout in the status bar"
summary = "Scale readout in the status bar"
class = "feature"
status = "shipped"
order = 4160
executor = "claude-code"
notes = "editor-UI program wave 15 (wave_plan.tsv row 115) — SHIPPED wave 115 (editor factory), f2acb1aa; verifier wave115.md. Metres-per-pixel readout in the status bar, sampled from the rAF loop but written ONLY when the formatted string changes (an unguarded per-frame write would re-render the chrome at 60fps; the perturbation was deleting that guard). Reconciled with T-667 by making the shared signal the single source — ScaleBar now resolves from it, so one number drives both the graphic bar and the numeric cell, and the bar no longer waits for the ~1Hz HUD heartbeat. The printed value IS the contour ladder's input (both literally 2^(-zoom)), not a lookalike."
depends_on = ["T-661"]
main_goal = "Add metres-per-pixel scale readout to BottomToolbelt status cell."
context = [
    "Closes STATUS-ZOOM-001; Eden prints m/px, we print nothing.",
    "Contour ladder T-639 lacks on-screen zoom check without this.",
]
requirement = [
    "Fourth scale span cell in second mono readout group.",
    "Sample zoom from rAF in mission_editor; prop into BottomToolbelt.",
    "Write only when formatted string changes not every frame.",
]
current_state = [
    "BottomToolbelt signature lacks camera or zoom prop.",
    "RenderEngine::zoom at engine.rs:1720 only from rAF sampler.",
]
approach = ["Add signal in mission_editor mount; pass zoom to eden_toolbelt prop."]
verify = ["Scale cell shows metres-per-pixel matching RenderEngine zoom."]
citations = [
    "STATUS-ZOOM-001 gap row",
    "BottomToolbelt eden_toolbelt :3672",
    "RenderEngine zoom engine.rs:1720",
]
shipped_at = "43678e6b"
created_at = "2026-08-07T04:25:43Z"
completed_at = "2026-08-07T04:35:28Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_toolbelt.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "toolbelt",
    "map_canvas",
]
