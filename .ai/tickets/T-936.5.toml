id = "T-936.5"
kind = "work"
parent = "T-936"
title = "Positional audio emitters and music cues"
summary = "audioEmitters (position, sound, radius, loop, trigger) and musicCues (event, track) become authored mission data with an editor placement panel, a core audio.rs validator and TBD_AudioEmitter.c playing them in-game."
class = "feature"
status = "ready"
order = 7510
priority = 2
spec = "docs/specs/audit_2026_09/t936_mission_logic.md"
plan = "docs/plans/t-936_5_plan.md"
executor = "claude-code"
main_goal = "Authors place sounds and music cues on the map and hear them in-game."
context = [
    "Master audit S1 lists positional audio among the missing mission logic; no audio vocabulary in mission.schema.json and no audio script under Scripts/Game/TBD.",
    "Shared files (mission.schema.json, mission/mod.rs, extensions.rs, panels/mod.rs) with T-936.1-.4; packs after T-936.4.",
]
requirement = [
    "Schema: top-level audio {emitters: [{id, x, z, y?, sound, radiusM, loop, triggerId?}], musicCues: [{id, event: mission_start|task_succeeded|task_failed|mission_end, track}]}, additionalProperties false.",
    "New mission/audio.rs (registered in mission/mod.rs): model, validator (radius above zero, known events, unique ids); registered in extensions.rs AUTHORED_BLOCKS.",
    "New panels/audio_emitters.rs (registered in panels/mod.rs): emitter list with place-on-map using the existing marker placement gesture, radius and loop fields, music cue table; undoable.",
    "New Gamemode/TBD_AudioEmitter.c: spawns one sound source per emitter at its position, honours radius and loop, plays cues on the named events.",
]
current_state = [
    "No audio block, no panel, no runtime script; missions carry no authored sound.",
]
approach = [
    "Verify on main: a payload carrying audio flattens without it; paste the red.",
    "Add schema and golden; write audio.rs with validator tests; register; build the panel; write the script; compile the mod.",
    "Perturbation: accept radius 0 in the validator; its test must go red; restore, touch, green.",
]
verify = [
    "cargo test -p map-engine-core --all-features",
    "cargo xtask ci schema-validate",
    "cargo xtask mk ci-local-leptos",
    "cargo xtask mk leptos-gates",
    "cargo xtask mod compile",
    "cargo xtask platform wave gate --slice T-936.5",
]
acceptance = [
    "A mission with two emitters and one cue flattens to an audio block; in-game each emitter plays within its radius and the cue fires on its event (checklist).",
    "Invalid radius, unknown event or duplicate id is refused in the panel; a mission without audio flattens byte-identically.",
]
depends_on = ["T-936.4"]
created_at = "2026-09-05T02:09:02Z"
owns = [
    "packages/tbd-schema/schema/mission.schema.json",
    "apps/website/frontend/src/editor/panels/audio_emitters.rs",
    "apps/website/frontend/src/editor/panels/mod.rs",
    "crates/map-engine-core/src/mission/audio.rs",
    "crates/map-engine-core/src/mission/mod.rs",
    "crates/map-engine-core/src/mission/extensions.rs",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_AudioEmitter.c",
]

[scope]
domain = "mod"
layer = "scripts"
component = "gamemode"
