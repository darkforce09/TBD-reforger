id = "T-302"
kind = "work"
title = "Prove T-182 weapon equip on a live body"
summary = "T-182's slot-indexed weapon equip compiles but was never observed in game; add a per-slot equip result log, a headless world-boot assertion that four authored weapons land in four slots, and a human checklist item."
class = "chore"
status = "ready"
order = 7670
spec = "docs/specs/ideas/t302_weapon_equip_boot.md"
plan = "docs/plans/t-302_plan.md"
executor = "claude-code"
notes = "Operator authorization 2026-09-04: agents may edit the Enfusion mod scripts; gate = cargo xtask mod compile; in-game behaviour goes on a human checklist. TBD_LoadoutEquipHelper.c is shared with T-310, which packs in a later wave."
priority = 1
main_goal = "A four-weapon loadout is observed carried, slot by slot, on a live body and in the headless boot log."
context = [
    "T-182 uses CanInsertItemInStorage/TryInsertItemInStorage with a CanReplaceItem fallback, deliberately not WeaponSlotComponent.SetWeapon; nothing has confirmed a weapon in slot 1.",
    "Anchor 2026-09-05: Gamemode/TBD_LoadoutEquipHelper.c:20-36 documents the phases; xtask/src/mod_world_boot.rs:32 run() boots a world and greps sentinels.",
]
requirement = [
    "TBD_LoadoutEquipHelper.c logs one `[TBD][Equip] slot=<n> weapon=<res> result=<ok|replaced|failed>` line per weapon row.",
    "mod_world_boot.rs asserts the four-weapon fixture produces four ok lines and zero replaced lines.",
    "The human checklist names the mission, the slot and the four weapons to inspect.",
]
current_state = ["The equip helper has no per-slot result log; the world-boot lane greps existing sentinels only, so a silent replace would pass."]
approach = [
    "Add the log line at the end of each weapon-row equip; add a four-weapon fixture loadout to the boot mission.",
    "Extend the world-boot grep with the four expected lines; perturbation: expect five lines, red, restore, touch, green.",
]
verify = [
    "cargo xtask mod compile",
    "cargo xtask mod world-boot   # exact subcommand per xtask/src/mod_world_boot.rs",
    "cargo xtask platform wave gate --slice T-302",
]
acceptance = [
    "The headless boot log shows four ok equip lines for the fixture slot and no replaced line.",
    "A human on the checklist sees all four weapons carried in game, none replacing another.",
]
created_at = "2026-09-05T03:30:00Z"
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_LoadoutEquipHelper.c",
    "xtask/src/mod_world_boot.rs",
]

[scope]
domain = "mod"
layer = "scripts"
component = "gamemode"
