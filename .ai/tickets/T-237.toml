id = "T-237"
kind = "work"
title = "ticket ship verifies nothing and check ignores the schema"
summary = "ticket ship verifies nothing and check ignores the schema"
class = "feature"
status = "shipped"
order = 2550
executor = "claude-code"
depends_on = ["T-273"]
main_goal = "ticket ship verifies nothing and check ignores the schema."
context = ["xtask ticket ship flipped status without checks; check.rs ignored schema.json so hundreds of registry violations stayed invisible."]
requirement = ["ticket ship verifies nothing and check ignores the schema."]
current_state = [
    "xtask/src/cmds.rs:534 sets status to shipped and returns.",
    "xtask/src/check.rs contains zero references to schema.json and hand-rolls a narrower subset,",
    "so 314 schema violations are invisible: program Platform, invented impact and surfaces values, shipped_at which is not in the schema at all, and 13 null active_slice entries.",
]
approach = ["Wire .ai/tickets/schema.json into ticket check via jsonschema; block ticket ship until check is clean."]
verify = ["cargo xtask ticket check validates registry against schema.json before ship."]
acceptance = ["Ship refused on red check; schema.json enforced program Platform enums and shipped_at shape."]
citations = [
    "314 schema violations invisible",
    "program Platform invented impact surfaces",
    "13 null active_slice entries",
    "ff43c6444 schema wired check",
]
shipped_at = "fe37266f"
created_at = "2026-07-27T00:04:43Z"
completed_at = "2026-07-27T00:11:44Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "xtask/src/cmds.rs",
    "xtask/src/check.rs",
    ".ai/tickets/schema.json",
]

[scope]
domain = "repo"
layer = "xtask"
component = "check"
