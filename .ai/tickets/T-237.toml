id = "T-237"
kind = "work"
title = "ticket ship verifies nothing and check ignores the schema"
summary = "ticket ship verifies nothing and check ignores the schema"
class = "feature"
status = "shipped"
order = 2550
executor = "claude-code"
depends_on = ["T-273"]
shipped_at = "fe37266f"
created_at = "2026-07-27T00:04:43Z"
completed_at = "2026-07-27T00:11:44Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
migration_legacy = ["xtask/src/cmds.rs:534 sets status to shipped and returns. xtask/src/check.rs contains zero references to schema.json and hand-rolls a narrower subset, so 314 schema violations are invisible: program Platform, invented impact and surfaces values, shipped_at which is not in the schema at all, and 13 null active_slice entries."]
owns = [
    "xtask/src/cmds.rs",
    "xtask/src/check.rs",
    ".ai/tickets/schema.json",
]

[scope]
domain = "repo"
layer = "xtask"
component = "check"
