id = "T-810"
kind = "work"
title = "Attributes Type is a searchable catalog picker with a revert path and Eden's axis colours"
summary = "Attributes Type is a searchable catalog picker with a revert path and Eden's axis colours"
class = "bug"
status = "shipped"
order = 4920
executor = "claude-code"
notes = "depends_on T-785; Eden dialog evidence frame 163121; validator ASSET-RESOLVES copy is the error-side pair | SHIPPED W206: searchable side-agnostic TYPE picker (canonical write clears ASSET-RESOLVES live), snapshot-on-open Revert via per-slot ops (differing-multi restore PROVEN — the case a batch flattens), Eden axis colours, advanced freetext single-select (hidden on differing multis, documented), 3-press Esc ladder. Retry=reload verified WORK-SAFE (draft restore + conflict modal). T-785/T-813 machinery unregressed under the new surface."
shipped_at = "3692a76e"
created_at = "2026-08-09T04:19:06Z"
completed_at = "2026-08-10T19:35:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-23 — Eden's Object:Type (frame 163121) is a searchable TREE with a magnifier — the author browses Cars/Drones/Men…; TBD asks for freetext 'Asset id — empty uses the faction default', which is expert-only recall and, combined with F-01, was the field a stray 'q' corrupted 9 slots through. Eden also commits on OK with an explicit CANCEL; TBD applies live with only ✕ — no back-out (which raised F-26's stakes). Eden colour-codes Position/Rotation axes X=red Y=green Z=blue; TBD's Transform tab is monochrome. FIX: (a) TYPE becomes a searchable picker over the live catalog (asset_catalog.rs tree source; typing filters; picking writes assetId; empty = faction default stays a first-class option); freetext remains available behind an 'advanced' affordance for unlisted ids; (b) a revert affordance: snapshot field values when the panel opens; 'Revert' restores them (live-apply semantics stay — this is a bounded undo convenience, not OK/Cancel transactionalism; state the model in the panel); (c) axis colour chips on X/Y/Z/Rotation labels per Eden. ⚠️ TRAPS: depends on T-785's healthy text_field; catalog may be empty (dev without seed) — picker shows the T-800 failure surface, not a dead list; multi-edit interaction: TYPE picker under apply-to-all writes all (F-26's batching from T-788 applies). ACCEPTANCE: scripted — open picker, type 'rifle', pick a leaf: assetId set to canonical id and validator shows no ASSET-RESOLVES error; Revert restores pre-open values across edited fields (readback equality); axis labels carry 3 distinct colours (computed styles); empty-catalog state shows cause+retry not an empty list."]
owns = [
    "apps/website/frontend/src/attributes.rs",
    "apps/website/frontend/src/asset_catalog.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "attr_panel",
    "asset_browser",
]
