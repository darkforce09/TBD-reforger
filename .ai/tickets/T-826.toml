id = "T-826"
kind = "work"
title = "Markers stop declaring sides — placing a marker must not mint a faction"
summary = "Markers stop declaring sides — placing a marker must not mint a faction"
class = "bug"
status = "queued"
order = 5080
executor = "claude-code"
notes = "wave204 verifier MAJOR; decision reserved for the operator; T-798 shipped-halves verified correct | operator decided (a) 2026-08-10 during the wave-204 eye-pass; now actionable — implement the ensure_side_faction split (marker path stores side WITHOUT minting), check flatten.rs/compile reads of an unminted marker side before landing"
estimated = ["scope"]
migration_legacy = ["WAVE-204 VERIFIER FILING (wave204.md MAJOR — 're-scope, not revert'). MEASURED truth table on a truly-fresh mission: t0 chip = '0 - No issues'; place ONE marker -> '1 error' (V1-PLAYER-SPAWN). MECHANISM: ensure_side_faction (editor_ops.rs:4654) mints faction-{SIDE} on the FIRST marker place, flipping validate.rs's deliberate declares_players gate — so the review's F-11 'error about something they didn't touch' survives T-798 (whose shipped halves all measure correct: hydrated missions show truth at t0, copy verbatim, Backspace pixel-pure, contrast fixed at plate-calc). DECISION RECORDED (operator, 2026-08-10): OPTION (a) — 'markers aren't factions.' A briefing mark must not declare a side; V1 fires only when a slot/faction is actually authored. Original decision framing: (a) markers stop minting factions — a briefing mark is not a side declaration; V1 fires only when a SLOT/faction is authored (needs an ensure_side_faction split: marker path stores side WITHOUT minting, or mints lazily at first slot); or (b) the behavior is CORRECT (marking a side commits you to spawns) and the T-798 acceptance line is re-scoped to document it. Option (a) matches the review's intent; check the compile/schema side effects of an unminted marker side before choosing (flatten.rs reads?). ACCEPTANCE (if a): fresh mission, place marker -> chip stays 0; author first slot of that side -> V1 fires; compiled export unchanged for the marker-only mission vs today minus the phantom faction."]
owns = ["apps/website/frontend/src/editor_ops.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["ops_undo"]
