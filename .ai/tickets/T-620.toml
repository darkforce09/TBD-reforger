id = "T-620"
kind = "work"
title = "Fix verify-no-python; port collisions; wire CI"
summary = "verify-no-python has been RED since the factory started, is in no CI job, and half of it cannot fire"
class = "bug"
status = "shipped"
order = 3672
executor = "claude-code"
priority = 1
main_goal = "Replace rg with grep; decide slice-collisions; wire gate to CI."
context = [
    "Operator-raised 2026-08-01; rule exists but never bound.",
    "Gate red since factory day; not in any CI job.",
]
requirement = [
    "Replace rg with grep at :29; fail closed on non-no-match exit.",
    "Port slice-collisions.py to xtask or add justified exemption.",
    "Wire verify-no-python into CI or wave-gate.",
]
current_state = [
    "Two .py files from 1e3ea1f6 factory open day.",
    "rg missing; || true swallows; interpreter half never ran.",
]
approach = [
    "Reuse gate-grep.sh exit-status handling from T-556.",
    "Expect red immediately after grep fix; that is point.",
]
verify = [
    "verify-no-python finds python3 invocations without rg.",
    "Gate runs in CI or wave.sh not ci-local only.",
]
citations = [
    "T-621 no shell rule",
    "T-611 verify-citations scope",
    "T-606 stale build pass",
]
shipped_at = "c2372092"
created_at = "2026-08-01T01:33:23Z"
completed_at = "2026-08-01T01:34:25Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "scripts/verify-no-python.sh",
    "scripts/platform/slice-collisions.py",
    "scripts/mod/slice-collisions.py",
    "xtask/src",
    "Makefile",
    "docs/platform/CODING_STANDARDS.md",
    "scripts/platform/wave.sh",
    "docs/platform/wave_plan.tsv",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
