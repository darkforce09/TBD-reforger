id = "T-897"
kind = "work"
title = "T-853 Phase 3: delete the Makefile"
summary = "T-853 Phase 3: delete the Makefile"
class = "docs"
status = "shipped"
order = 5770
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
BLOCKERS — three things READ the Makefile and evaporate silently if it is deleted without them moving in the SAME commit:
  1. xtask/src/node_free.rs:290 scans it with `let Ok(text) = read_to_string else { return }` — FAIL-OPEN, so deleting the file narrows the no-node gate to nothing without any error.
  2. scripts/platform/wave.sh:1598-1628 (and its port) parses the `schema-validate` recipe to cross-check GATE_SCHEMA_VALIDATE_GATES. Needs `xtask schema list-gates` first — T-896 owns shipping it.
  3. xtask/src/gate_t444.rs pins the `seed:` recipe body by reading the Makefile (found by T-894). Repoint at mk_db::SEEDS or that Class-R gate evaporates.
Also: 12 CI `run: make …` steps and ~363 live doc files reference make targets. The historical record (SHIPPED_HISTORY, TICKET_REGISTRY, verify-logs, registry.json ticket prose) must NOT be rewritten — it describes what was true then."""
main_goal = "Delete the Makefile last after absorbing targets into xtask; repoint CI and live docs."
context = [
    "T-853 Phase 3 final slice: delete Makefile after all targets absorbed.",
    "Deleting silently NARROWS two gates unless they move same commit.",
    "node_free.rs:290 fail-open scan; wave.sh:1598-1628 schema-validate cross-check.",
]
requirement = [
    "Delete Makefile; repoint 12 CI run: steps and live docs.",
    "Ship xtask schema list-gates first; repoint gate_t444 seed recipe.",
    "Each slice ADDS xtask subcommands; Makefile deletion is one final slice.",
]
current_state = [
    "Makefile still present; ~363 live doc files reference make targets.",
    "12 CI run: make steps; three blockers evaporate silently if deleted alone.",
]
approach = [
    "Absorb Makefile targets into xtask subcommands in file-disjoint slices.",
    "Move node_free scan gate_t444 seed pin and wave.sh parser same commit.",
    "Do NOT rewrite SHIPPED_HISTORY TICKET_REGISTRY verify-logs registry prose.",
]
verify = [
    "ACCEPTANCE: make <t> and cargo xtask ... byte-identical stdout stderr rc.",
    "Clean tree AND deliberately broken tree; harness asserts make went red.",
    "A green run alone is not evidence T-556 anti-vacuity.",
]
citations = [
    "T-853 shell->xtask waves",
    "t853_shell_to_xtask_waves.md",
    "T-556 anti-vacuity",
    "T-896 schema list-gates",
    "T-894 gate_t444 seed pin",
]
shipped_at = "16ed63e4"
created_at = "2026-08-12T20:50:33Z"
completed_at = "2026-08-12T22:52:30Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["Makefile"]

[scope]
domain = "repo"
layer = "xtask"
