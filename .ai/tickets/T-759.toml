__keys = [
    "id",
    "order",
    "program",
    "status",
    "stream",
    "targets",
    "executor",
    "surfaces",
    "impact",
    "title",
    "summary",
    "notes",
]
__ord = 732
executor = "claude-code"
id = "T-759"
impact = ["tests"]
notes = "filed by wave-116 adversarial verifier (editor factory)"
order = 4635
program = "eden"
status = "shipped"
stream = "mission-creator"
summary = """MAJOR from the wave-116 adversarial verifier, and it is this programme's signature defect aimed at the pins themselves. apps/website/frontend/src/eden_dock_left.rs:1107-1130 and :1083-1101: `SRC` is an `include_str!` of the WHOLE file, test module included. Every POSITIVE needle in `the_index_and_the_fly_to_reuse_the_shipped_paths` and `the_dock_has_two_tabs_and_defaults_to_layers` — "parse_locations_json", "__editorCamSet", "camera_snapshot", "/map-assets/{terrain}/locations.json", "let tab = RwSignal::new(LeftTab::Layers)", and all eight testids — appears VERBATIM inside the assertion that searches for it. Delete the production usage entirely and every one of those pins stays GREEN. The author knew the idiom: the single NEGATIVE needle in the same block is deliberately split (`format!("{}{}", "set_view", "(")`) with a comment saying "so the needle itself is not a hit" — the positives simply did not get the same treatment. THE PINNED FACTS ARE ALL TRUE TODAY (the verifier re-proved each independently), so nothing is broken; what is broken is the guarantee. Registry precedent T-559/T-561 rates this class DIRTY MAJOR. FIX, two options, either is cheap: split every positive needle the way the negative one already is, or scope SRC to the production half by truncating at the `#[cfg(test)]` marker — the pattern `bookmarks_and_fly_to_are_not_document_edits` three tests down in the same file already does the latter correctly and can be copied. Diagnosis: editor_verify/wave116.md finding 1. || SCOPE CORRECTION 2026-08-07 (wave 118 verifier, proved by perturbation): a wave-118 slice reported that `arsenal.rs`'s shared `live_production_src()` makes EVERY pin in that file — and by extension the repo-wide class_r_scrub machinery (21 files, 93 refs) — hollow. THAT IS FALSE AND MUST NOT BE USED TO WIDEN THIS TICKET. `class_r_scrub::scrub()`'s FIRST pass is `cut_test_module()`, which removes everything from the first `#[cfg(test)]` to EOF before any `.contains` runs. The verifier disabled that cut and 18 pins went RED across 7 files — the machinery fails LOUD. The kernel of truth is only 'do not bypass the scrubber'. This ticket remains scoped to eden_dock_left.rs's T-696 pins, which build SRC with a bare include_str! and genuinely do self-match."""
surfaces = ["LEFT"]
targets = ["website"]
title = "T-696's headline source pins are hollow — they match their own test source"
