id = "T-563"
kind = "work"
title = "T-541 admin #stage LOBBY bypasses TickRosterSettle loadout refuse"
summary = """
FOUND by W64 adversarial verifier (DIRTY MINOR|NIT) after T-541.

Automatic LOBBY entry is gated by loadout settle/IsComplete refuse. SetStage(LOBBY) via admin is not. Deploy still DENIED on m_bLoadoutDeliveryRefused; possession cannot proceed — stage chrome can open.

Repro: refuse loadout delivery at spawn boundary; admin #stage LOBBY; UI stage advances; DeployPlayerInternal still DENIED."""
status = "shipped"
order = 3416
executor = "claude-code"
priority = 2
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_FrameworkManager.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c",
]

[scope.mod]
layers = ["feature"]
