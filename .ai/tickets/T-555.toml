id = "T-555"
kind = "work"
title = "BLOCKER: migrations gate never tests populated-database replay"
summary = "BLOCKER: migrations are only ever tested forward from empty, so two live defects break every populated database"
class = "bug"
status = "shipped"
order = 3400
executor = "claude-code"
priority = 0
main_goal = "Fix migration checksum and unique-index defects; add populated-DB gate."
context = [
    "ensure_gate_db drops tbd_gate_migrate every run; forward-only.",
    "Two defects: edited 0009 checksum; 0017 unique index without dedup.",
]
requirement = [
    "Restore 0009 pre-a843905f bytes; move note to 0015.",
    "Dedup orbat_slots in 0017 before CREATE UNIQUE INDEX.",
    "Gate must apply new migrations to persistent populated DB.",
]
current_state = [
    "Dev API fails: migration 9 modified checksum error.",
    "Populated DB: duplicate assigned_to blocks idx_orbat_slots_em_assigned.",
]
approach = [
    "T-405 pattern: quarantine duplicate rows before index.",
    "Persistent tbd_gate_migrate_persist; fail on checksum drift.",
]
verify = [
    "make api against existing dev DB reproduces defect 1.",
    "Scratch populated DB reproduces defect 2.",
]
citations = [
    "ensure_gate_db forward only",
    "0009 checksum drift",
]
shipped_at = "f5f4ca38"
created_at = "2026-07-27T19:58:07Z"
completed_at = "2026-07-27T20:14:52Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/migrations/0009_role_played_not_null.sql",
    "apps/website/api/migrations/0017_orbat_slots_assigned_partial_unique.sql",
    "scripts/platform/wave.sh",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
