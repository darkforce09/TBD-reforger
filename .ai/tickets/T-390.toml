id = "T-390"
kind = "work"
title = "Shared cargo target dir false-green gate"
summary = "The shared cargo target dir clobbers artifacts across worktrees, so the gate can pass on code it never compiled"
class = "bug"
status = "shipped"
order = 3206
executor = "claude-code"
notes = """"No (/var (T-193 (`target-gate-frontend`, **252 **It **`gate_test_api` --workspace` 100% 952 992bb2696cb1aebe A ALSO AND ANOTHER ATTEMPT Bought CARGO_TARGET_DIR, CAUSE, CONSTRAINT Every FAILED FIRST FIXING GB**. GB, HIT I IN IT. MB PASS, Root SAME T-193/T-195. T-233's T-235: The This WORKTREE'S WORTH Weigh `--bin `cargo `gate_test_api` `target-gate-trunk`). after against agent already another any api` are artifact been belonging binary, both, build, built. cause changed changing cheaply class compiled.** contained contents; cost: could. database device" dir). do documents does during each edit. enforces entire error. exactly exists, failed, fails failures five fix: for forgot, free from full grep had has hash header hit hunting, inferring: instance. instance: isolation it it, machine main. many mid-gate mirroring new not notice. or own; preflight price: probably produce proved ran reads remove reported retry rlib, run). run, run. say sends several shortfall should, since slice**: slice, someone source specifically stable strings succeeded take target-gate-mapengine that that. their this tight treatment. trunk twice two unchanged; underneath use verifies version warning wave.sh's way ways website-api` website-frontend` went were what where which while why with worktree's"""
priority = 0
main_goal = "Give gate_test_api private CARGO_TARGET_DIR so gate cannot pass on foreign binary."
context = [
    "FIFTH INSTANCE OF THIS RUN'S SIGNATURE DEFECT, found by T-235, in the gate fixed four hours ago.",
    "gate_test_api runs cargo test -p website-api to clear a slice — gate can pass on code never compiled.",
    "T-235 measured three ways: foreign 4-test binary; target/debug/api size changed; compile failed then succeeded.",
    "Same shape as T-193 and T-195 frontend test step — API test step never got private target dir.",
    "Probably explains T-233 reverted slice: 126 passed / 0 failed on clean DB — stale binary would.",
]
requirement = [
    "FIX: give gate_test_api its own CARGO_TARGET_DIR like target-gate-frontend and target-gate-trunk.",
    "Consider whether every gate step should — cargo check and clippy share same dir and hazard.",
    "T-322 measured cold --bin api build at 25s / 322 crates and 2.1 GB; disk was tight.",
    "ALSO: slice agents told private target dirs but nothing enforces; wasm trap caught several.",
    "Gate step verifying binary about to run is the one just built would close the class.",
]
current_state = [
    "SHIPPED. Both test steps now private CARGO_TARGET_DIR — target-gate-api and target-gate-mapengine.",
    "cargo check and clippy need none: they emit no binary to run; hazard is RUNNING foreign binary.",
    "DISK WAS REAL CONSTRAINT: gate failed No space left on device — /var/tmp held ~116 GB orphan targets.",
    "Five slices killed by session limit before cleanup. Reclaimed 116 GB; gate run then cost 4 GB.",
    "CARGO_INCREMENTAL=0 on both — incremental caches large fraction of target dir, useless one-shot gate.",
]
approach = [
    "CLOSED THE CAUSE: removes orphan build caches sparing dirs whose slice worktree still exists.",
    "Preflight now NAMES reclaimable space rather than only reporting shortfall.",
]
citations = [
    "T-235 foreign binary",
    "T-193 T-195 frontend",
    "T-233 reverted slice",
    "gate_test_api hazard",
]
shipped_at = "c07ca278"
created_at = "2026-07-26T12:46:02Z"
completed_at = "2026-07-26T13:07:43Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 3 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
