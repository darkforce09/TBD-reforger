id = "T-655"
kind = "work"
title = "Validation panel: persistent issue list with rollup"
summary = "Validation panel: persistent issue list with rollup"
class = "chore"
status = "shipped"
order = 4090
executor = "claude-code"
notes = "editor-UI program wave 11 (wave_plan.tsv row 111) — SHIPPED wave 111 (editor factory), a6e1b2a2; verifier wave111.md"
depends_on = ["T-661"]
main_goal = "Ship floating validation panel with persistent issue list and rollup."
context = [
    "Validation group surface; product order ships first within group.",
    "FNF commented validation out; makers get no failure summary.",
]
requirement = [
    "Persistent issue list with rollup; click-to-select findings not clipboard.",
    "Severity ladder with legend; no severity fires on correct input.",
    "Validation always on; not behind debug save or export flags.",
    "New validation_panel.rs plus always-on re-evaluation tick in mission_editor.",
    "Float panel not dock to avoid dock chrome serialisation collision.",
]
current_state = [
    "No validation surface exists in SPA; wave zero pre-declares stub module.",
    "File graph permits wave three; independent packings put late; product call.",
]
approach = ["Implement validation_panel.rs overlay mount at MissionEditorPage overlay section."]
verify = ["Panel shows rollup and click-select findings; validation runs without export gate."]
citations = [
    "FNF validation HTML comment",
    "mission_editor overlay mounts",
    "T-635 telemetry gate separate",
]
shipped_at = "79d46fb1"
created_at = "2026-08-02T09:22:36Z"
completed_at = "2026-08-02T09:29:47Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/validation_panel.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "validation",
    "map_canvas",
]
