id = "T-424"
kind = "work"
title = "Fix verify-t180 zero-pass selector silence"
summary = "cargo test --lib <selector> exits 0 when the selector matches zero tests, so 25 pinned checks can silently empty"
class = "bug"
status = "shipped"
order = 3263
executor = "claude-code"
priority = 1
main_goal = "Wrapper asserting passed >= 1 per cargo test --lib selector in verify-t180."
context = [
    "Found by T-216 restoring verify-t180-coherency.sh during wave 5.",
    "Independently replicated by wave adversarial verifier.",
    "Third gate-integrity defect in that script this wave.",
]
requirement = [
    "Each pinned selector must assert at least one test passed, not just rc=0.",
    "T-216 left wrapper as third structural change — belongs here.",
]
current_state = [
    "cargo test --lib zzz_no_such_test: ok 0 passed 254 filtered rc=0.",
    "verify-t180 pins 25 selector invocations; typo prints verify-t180 OK.",
    "grep test result ok 0 passed across full run -> 0 today; nothing holds.",
]
approach = [
    "Same family: check reporting success over input never examined.",
    "Other two script defects FIXED in wave 5: --features doc, static rg bans.",
]
verify = ["verify-t180: ALL PASS rc=0 end to end after wave 5 fixes."]
citations = [
    "T-216 verify-t180 restore",
    "wave 5 adversarial verifier",
    "verify-t180-coherency.sh selectors",
]
shipped_at = "0ff46f45"
created_at = "2026-07-27T04:44:55Z"
completed_at = "2026-07-27T04:50:41Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["scripts/verify-t180-coherency.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
