id = "T-648"
kind = "work"
title = "Transform: Shift-rotate, snap grid, widget + Space cycle"
summary = "Transform: Shift-rotate, snap grid, widget + Space cycle"
class = "feature"
status = "shipped"
order = 4060
executor = "claude-code"
notes = """
editor-UI program wave 10 (wave_plan.tsv row 110) — SHIPPED wave 110 (editor factory), f711efc8; verifier wave110.md

--- HEAD migration_legacy (verbatim) ---

SUPERSEDES T-073 and T-075 (both cancelled in favour of this row) and absorbs the snapping slice. Ids: `XFORM-SHIFT-001` (Shift+drag rotates to face cursor), `TOOLBAR-GRID-MOVE-001` (translation / rotation / area-scaling snap grids with increase and decrease), `WIDGET-CYCLE-001` + `WIDGET-TRANS-001` (transformation widget), `KEY-GRID-001`. Two collisions to DECIDE INSIDE THE TICKET: in Eden `Space` cycles widget variants, in TBD `Space` is flyTo (`center_on_selection`, editor_ops.rs:354) — Eden's `1`-`5` direct keys are free, so using them dissolves the clash; and pick a TBD key for the grid toggle, do NOT copy Eden's `odiaeresis` or `;` keysym artefacts. Code: Shift+drag is a modifier branch in `onpointermove` (mission_editor.rs:1437-1643) and the Space decision is in the keydown :1005-1030; select_tool.rs holds `drag_delta` :201, `push_drag_preview` :232, `clear_drag_preview` :257; rotation and snapped positions commit through `attrs_update_position` (editor_ops.rs:667) and rotation already round-trips numerically (attributes.rs:292, editor_ops.rs:1841), so this is a gesture on an existing field. WARNING, verified twice: `GRID_CELL_M` at select_tool.rs:33 IS NOT A SNAP STEP. It is `MissionDocCore::GRID_CELL_M`, a spatial-index cell size used only at :338 and :372 to build a PointIndex. NOTHING SNAP-RELATED EXISTS: `grep -riw snap` over the frontend returns 37 hits and every single one is `let snap = read_snapshot();`. The quantiser is new and goes beside `drag_delta`.

missing=0"""
depends_on = ["T-661"]
main_goal = "Ship Shift+drag rotate, snap grids, transform widget; resolve Space/flyTo clash."
context = [
    "SUPERSEDES T-073 and T-075; absorbs snapping slice per row.",
    "Two key collisions: Space vs flyTo; pick TBD grid toggle key.",
    "Shift+drag modifier branch mission_editor.rs:1437-1643; keydown :1005-1030.",
]
requirement = [
    "XFORM-SHIFT-001: Shift+drag rotates to face cursor.",
    "TOOLBAR-GRID-MOVE-001: translation rotation area-scaling snap grids.",
    "WIDGET-CYCLE-001 and WIDGET-TRANS-001: transformation widget.",
    "KEY-GRID-001: grid toggle; use Eden 1-5 keys not Space.",
]
current_state = [
    "grep snap returns only read_snapshot; no quantiser exists yet.",
    "GRID_CELL_M at select_tool.rs:33 is spatial-index cell not snap step.",
    "drag_delta push_drag_preview clear_drag_preview at select_tool.rs :201 :232 :257.",
]
approach = [
    "New quantiser beside drag_delta in select_tool.rs.",
    "Resolve Space flyTo clash via Eden 1-5 direct keys; pick TBD grid key.",
    "Rotation snapped positions commit attrs_update_position editor_ops.rs:667.",
]
verify = ["verifier wave110.md records shipped slice evidence."]
citations = [
    "T-073 T-075 superseded cancelled",
    "XFORM-SHIFT-001 Shift drag rotate",
    "KEY-GRID-001 grid toggle key",
    "GRID_CELL_M not snap step",
]
shipped_at = "f50508af"
created_at = "2026-08-02T08:06:05Z"
completed_at = "2026-08-11T01:33:22Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/select_tool.rs",
    "apps/website/frontend/src/editor_ops.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "map_canvas",
    "tools",
    "ops_undo",
]
