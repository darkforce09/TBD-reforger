id = "T-239"
kind = "work"
title = "Unticketed XSS hardening item from the Go purge"
summary = "Unticketed XSS hardening item from the Go purge"
class = "bug"
status = "shipped"
order = 2570
executor = "claude-code"
main_goal = "Ship the exact UGCPolicy-to-ammonia allowlist mapping plus a golden corpus and a no-XSS property test."
context = [
    "T-015 is used in commits but absent from the registry.",
    "The bluemonday sanitisation survived the Go purge as ammonia (services/text.rs:10-16, wired at cms.rs:109,194),",
    "and text.rs:6-9 records an open security item: the exact UGCPolicy-to-ammonia allowlist mapping plus a golden corpus and a no-XSS property test.",
    "== FULLY SWEPT 2026-07-26 by a paid-for subagent; the slice agent was cut for budget ==",
    "**THERE IS NO LIVE STORED-XSS VIA HTML BODY.** The frontend has EXACTLY ONE raw-HTML sink repo-wide — arsenal.rs:1392 `inner_html=r.shape` —",
    "and it is fed by a hardcoded &'static str const table.",
    "Repo-wide grep for inner_html|set_inner_html|insert_adjacent_html|outer_html|srcdoc returns that one line.",
    "index.html has no inline script, and the SPA is CSR-only so there is no SSR concatenation path.",
    "A verified negative, and it reframes this whole ticket.",
    "TWO THINGS THAT ARE REAL TODAY AND ARE IN THIS TICKET'S OWN FILES:",
    "The live vector is a URL scheme, not an HTML body — filed as T-391 (P0).",
    "A fourth subagent mapped EVERY render site in the frontend by data provenance — recorded at docs/platform/frontend_data_provenance.md so it is not re-derived.",
    "It confirms T-391 is the only live vector (no HTML sink beyond arsenal.rs:1392's const table) and corrects three priors:",
    "arsenal.rs is mixed not mock, leaderboards.rs is fully API-fed since T-195 deleted its MOCK,",
    "and content.rs has ZERO API-fed renders (its list is mock_docs(), so an author cannot see what they just published — T-267).",
    "Placeholder-fallback family filed as T-392.",
]
requirement = ["Unticketed XSS hardening item from the Go purge."]
current_state = [
    "  **The one field that IS sanitised renders as ESCAPED TEXT.** announcements.body is ammonia'd at cms.rs:140/:248 and rendered at frontend/announcements.rs:80 as `{p.to_string()}` in a <p> — a Leptos text node.",
    "So ammonia buys ZERO security there, and it actively CORRUPTS display: ammonia HTML-escapes bare `<` and `&`, then Leptos escapes again,",
    "so an author writing `a < b` sees the literal `a &lt; b` on screen.",
    "That double-escape is a live bug. Either stop sanitising a field that renders as text, or stop escaping a field that arrives as HTML —",
    "doing both is what breaks it.",
    "  `snippet` bypasses its own cap: snippet_from (cms.rs:54-60) returns the caller's value verbatim, so the 200-rune limit is skipped when supplied explicitly,",
    "and PATCH never recomputes it from the new sanitised body.",
    "Notable: **wiki_pages.body_md — the largest unsanitised text column in the schema — has NO frontend consumer at all** (wiki.rs renders the hardcoded MANUALS const and never fetches /wiki).",
    "Five more fields are write-only with zero readers: missions.rejection_reason, users.ban_reason, warnings.reason, fire_missions.fp_grid, fire_missions.target_grid.",
    "Negative findings worth not re-investigating: no nickname/bio/pronouns column exists;",
    "telemetry ingests NO player display name, squad name, or chat text (only the engine GUID and role_played, confirmed against TBD_ResultsReporter.c:566-569)",
    "so the 'anyone who joins a server' attack does not exist;",
    "and handlers/servers.rs's write handlers are NOT registered in app.rs, which independently confirms T-235's handoff is still outstanding.",
]
approach = [
    "SO THE ALLOWLIST WORK IS DEFENCE-IN-DEPTH, NOT A LIVE-BUG FIX — which frees it to be strict rather than permissive.",
    "The latent exposure is real though: the moment anything goes behind inner_html, announcements.body, events.briefing, missions.briefing and wiki_pages.body_md all become live at once,",
    "and render_markdown (wiki.rs:215) is the obvious future candidate since it builds Leptos nodes today but would be the natural place to add a raw-HTML fast path.",
]
citations = [
    "T-015 absent from registry",
    "T-391 P0 URL scheme",
    "T-267 content.rs mock_docs",
    "T-392 placeholder-fallback family",
    "frontend_data_provenance.md",
    "arsenal.rs:1392 inner_html",
]
shipped_at = "b2373ecd"
created_at = "2026-07-26T13:18:16Z"
completed_at = "2026-07-26T21:42:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = [
    "apps/website/api/src/services/text.rs",
    "apps/website/api/src/handlers/cms.rs",
    "apps/website/frontend/src/announcements.rs",
    "apps/website/api/tests/cms_announcement_body.rs",
    "apps/website/api/tests/admin_field.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
