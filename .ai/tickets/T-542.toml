id = "T-542"
kind = "work"
title = "T-381 incomplete — wire require_test_database_url into every IT binary"
summary = "T-381 incomplete — wire require_test_database_url into every IT binary"
class = "feature"
status = "shipped"
order = 3382
executor = "claude-code"
priority = 1
main_goal = "Wire require_test_database_url into every IT binary."
context = [
    "W54 adversarial verifier DIRTY MAJOR after T-381.",
    "Only factions.rs and identity_link.rs wired; 22 binaries raw-read env.",
]
requirement = [
    "Replace every raw TEST_DATABASE_URL read with require_test_database_url.",
    "Class-R fails if tests/*.rs still has env::var outside common/mod.rs.",
]
current_state = [
    "22 IT binaries raw-read TEST_DATABASE_URL; parallel test mutates live DB.",
    "factions and identity_link panic; others mutate tbd_reforger.",
]
approach = [
    "require_test_database_url in every IT setup.",
    "Class-R scan banning env::var outside common/mod.rs.",
]
verify = ["rg env::var TEST_DATABASE_URL apps/website/api/tests — 22 hits."]
citations = [
    "W54 adversarial verifier DIRTY MAJOR",
    "T-381 require_test_database_url",
    "apps/website/api/tests env::var",
]
shipped_at = "74d05c92"
created_at = "2026-07-27T13:43:23Z"
completed_at = "2026-07-27T13:47:12Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/api/tests"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
