id = "T-514"
kind = "work"
title = "W44 hotfix: T-507 Class-R must not match commented trim assign"
summary = "W44 hotfix: T-507 Class-R must not match commented trim assign"
class = "docs"
status = "shipped"
order = 3354
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Wave 44 verifier MAJOR. save_trims_name_before_serialize uses find("doc.name = doc.name.trim().to_string()") which matches a commented assign before serde_json::to_value. Mutation: replace live trim with // doc.name = doc.name.trim().to_string() + trim-empty check → PASS while payload stays untrimmed. Cure: strip comments from save body before find, or require an uncommented assign precedes to_value. Bait mutation must RED; live trim must green.

missing=0"""
priority = 0
main_goal = "Fix Class-R save_trims_name to ignore commented trim assign."
context = [
    "Wave 44 verifier MAJOR on T-507 Class-R.",
    "save_trims_name_before_serialize find matches trim assign string.",
]
requirement = [
    "Strip comments from save body before find.",
    "Or require uncommented assign precedes serde_json::to_value.",
    "Bait mutation must RED; live trim must green.",
]
current_state = [
    "find matches commented assign before serde_json::to_value.",
    "Commented trim + trim-empty check → PASS while payload stays untrimmed.",
]
approach = [
    "Strip comments from save body before find.",
    "Require uncommented assign precedes to_value.",
]
verify = ["Mutation: commented trim assign → Class-R must RED not PASS."]
citations = [
    "Wave 44 verifier MAJOR",
    "T-507 Class-R trim assign",
    "save_trims_name_before_serialize",
]
shipped_at = "c9bab506"
created_at = "2026-07-27T10:00:54Z"
completed_at = "2026-07-27T10:04:49Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/frontend/src/faction_manager.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
