id = "T-882"
kind = "work"
title = "T-853 port: the LANG-2 enforcer itself"
summary = "T-853 port: the LANG-2 enforcer itself"
class = "docs"
status = "shipped"
order = 5630
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/verify-no-python.sh to `cargo xtask verify no-python` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 245 lines, 7 python3 sites - a Python ban written in bash that shells to python3. Port makes LANG-2 enforcement compiled-in. Must keep reading scripts/python-inventory.txt as its ratchet and stay fail-closed: T-620 kept it green over `rg: command not found` for four waves. Sources gate-grep.sh, so this is one of four blockers on T-880. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/verify-no-python.sh to `cargo xtask verify no-python` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 245 lines, 7 python3 sites - a Python ban written in bash that shells to python3. Port makes LANG-2 enforcement compiled-in. Must keep reading scripts/python-inventory.txt as its ratchet and stay fail-closed: T-620 kept it green over `rg: command not found` for four waves. Sources gate-grep.sh, so this is one of four blockers on T-880. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md"]

missing=0"""
main_goal = "Port verify-no-python.sh to cargo xtask verify no-python."
context = [
    "T-853 shell->xtask slice; 245 lines, 7 python3 sites.",
    "Python ban written in bash that shells to python3; LANG-2 enforcement compiled-in.",
    "Must keep reading scripts/python-inventory.txt as ratchet; fail-closed T-620 rg lesson.",
]
requirement = [
    "Delete script(s) shell-inventory.txt line(s) python-inventory.txt line(s).",
    "Repoint every caller; port to cargo xtask verify no-python.",
    "Build on crates/tbd-gate; sources gate-grep.sh T-880 blocker.",
]
current_state = [
    "scripts/verify-no-python.sh bash gate live today.",
    "T-620 kept it green over rg command not found for four waves.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
acceptance = [
    "Deleted verify-no-python.sh; verify no-python xtask gate replaced LANG-2 Python ban enforcement.",
    "Still read python-inventory.txt as ratchet; fail-closed over missing rg unlike pre-T-620 bash.",
    "Makefile and wave.sh callers repointed; gate-grep dependency removed enabling T-880 delete.",
]
citations = [
    "T-853 shell->xtask program",
    "T-620 fail-closed rg lesson",
    "T-880 gate-grep blocker",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "e9df35cf verify no-python port",
]
shipped_at = "e9df35cf"
created_at = "2026-08-12T08:53:27Z"
completed_at = "2026-08-12T09:03:02Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/verify-no-python.sh"]

[scope]
domain = "repo"
layer = "xtask"
