id = "T-277"
kind = "work"
title = "27.4% of the map catalogue is unclassified"
summary = "444 of 1,623 everon prefabs fall through to prefab-classify.json's fallback rule; vegetation and utility count zero and the road census is zero against 888 shipped road segments. Append rules until fallback is under 5% and the census matches."
class = "chore"
status = "ready"
order = 7110
spec = "docs/specs/ideas/t277_prefab_classify.md"
plan = "docs/plans/t-277_plan.md"
executor = "claude-code"
notes = "2026-09-05: owns is the rule file only; the catalogue rebuild (prefabs.json.gz, type-inventory.json) is a committed artifact regenerated by T-935.13's cutover build — the slice measures locally and pastes the numbers."
priority = 2
main_goal = "Under 5% of everon prefabs are fallback-classified and the road census matches roads.json.gz."
context = [
    "Anchor 2026-09-05: packages/tbd-schema/rules/prefab-classify.json has 73 rules and `fallback` at :3629; first match by resourceNameContains wins, so appending is safe.",
    "The file header (:3) says a rule edit changes nothing until `tbd-tools world build-objects` rebuilds the catalogue; gates validate rules, not the shipped catalogue.",
]
requirement = [
    "Appended rules classify the 444 fallback prefabs with structured gameplay, spatial and render.iconKey; vegetation and utility become non-zero.",
    "The road census counts the 888 segments across the five road classes after a rebuild.",
    "Schema and golden gates pass on the rule file.",
]
current_state = ["prefabs.json.gz carries kind/class from the last build: 444 fallback prefabs, vegetation 0, utility 0, road census 0 while roads.json.gz ships 888 segments."]
approach = [
    "List the 444 fallback resource names from objects/type-inventory.json; group by path prefix; append one rule per group.",
    "Rebuild locally to measure (not committed); paste counts. Perturbation: remove one appended rule, the rule-coverage test goes red; restore, touch, green.",
]
verify = [
    "cargo xtask ci schema-validate",
    "cargo xtask ci verify type-inventory",
    "cargo xtask ci verify map-object-enums",
    "cargo xtask platform wave gate --slice T-277",
]
acceptance = [
    "A local build-objects run reports fallback below 5% with non-zero vegetation, utility and road counts (numbers in the report).",
    "Every appended rule carries gameplay, spatial and render.iconKey and the schema gates pass.",
]
created_at = "2026-09-05T03:30:00Z"
owns = ["packages/tbd-schema/rules/prefab-classify.json"]

[scope]
domain = "schema"
layer = "registry"
