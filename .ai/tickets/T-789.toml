id = "T-789"
kind = "work"
title = "Save Version dialog: clamped on-screen, fresh state, focus moved in and trapped"
summary = "Save Version dialog: clamped on-screen, fresh state, focus moved in and trapped"
class = "bug"
status = "shipped"
order = 4710
executor = "claude-code"
notes = "depends_on T-786; review re-verification section has the exact rects; the 'blind-typeable offscreen field' note is why focus-first matters | SHIPPED W203: fresh-state on reopen, NodeRef focus-first (uncontrolled input so select survives), Tab trap both edges by the slice. ITS OFFSCREEN PREMISE-CHECK WAS WRONG-WAY: 'did not reproduce' was a class-read, not a measurement — the dialog WAS off-screen (y=-22/-184; backdrop-filter containing block, proven by causation). Fixed in-wave 940698e8: Portal to document.body + gate smoke save-dialog-rect (EDITOR_SUITE 19, rect containment at both viewports) so class-string pins can never vouch for geometry again; focused re-verify RESOLVED with live rects. Kept un-registered from modal_stack (T-726 argument, Esc ladder re-proved). Auto-bump claim refuted -> T-821."
shipped_at = "93dc7397"
created_at = "2026-08-10T00:34:00Z"
completed_at = "2026-08-10T02:59:52Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-04 — VERIFIED THREE WAYS, twice: the VERSION input sits at y=-22 and its label at y=-42 (vis:false) at BOTH 1920x1080 and 1366x768 — the one decision the dialog demands ('Versions are immutable — pick a new semver') is the one thing the author cannot see; screenshots show content starting at a clipped field edge above NOTES. The dialog also reopens showing a stale 'Saved v0.2.0' from the previous save. Keyboard: focus stays on the OPENER when the dialog opens; Tab order runs ✕ -> version(offscreen, blind-typeable) -> notes -> Save then WALKS OUT into the left dock (chevron_left -> Layers -> Locations) — no trap. MECHANISM: eden_top_strip.rs ~:1480-1601, anchored upward from the button (panel class top-full ~:70) with no viewport clamping. FIX: clamp the dialog fully into the viewport (flip to open downward or fix position centred), clear the last-save message when reopening, move initial focus to the version input, trap Tab within the dialog while open. ⚠️ TRAPS: T-786 (modal stack) landed in wave 200 — mount through its plumbing rather than a parallel mechanism; the version prefill/auto-bump behaviour is correct today (0.1.0 -> 0.2.0) — keep it; Esc-close must stay. ACCEPTANCE: scripted at 1920x1080 AND 1366x768: version input rect fully within viewport (y>=0, bottom<=innerHeight) and visible; reopened dialog shows no 'Saved vX' line until a save happens in-session; on open, activeElement === version input; 8 Tabs from open never focus an element outside the dialog subtree."]
owns = ["apps/website/frontend/src/eden_top_strip.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["top_strip"]
