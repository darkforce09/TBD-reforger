id = "T-789"
kind = "work"
title = "Save Version dialog: on-screen, fresh state, focus trapped"
summary = "Save Version dialog: clamped on-screen, fresh state, focus moved in and trapped"
class = "bug"
status = "shipped"
order = 4710
executor = "claude-code"
notes = "depends_on T-786; review re-verification section has the exact rects; the 'blind-typeable offscreen field' note is why focus-first matters | SHIPPED W203: fresh-state on reopen, NodeRef focus-first (uncontrolled input so select survives), Tab trap both edges by the slice. ITS OFFSCREEN PREMISE-CHECK WAS WRONG-WAY: 'did not reproduce' was a class-read, not a measurement — the dialog WAS off-screen (y=-22/-184; backdrop-filter containing block, proven by causation). Fixed in-wave 940698e8: Portal to document.body + gate smoke save-dialog-rect (EDITOR_SUITE 19, rect containment at both viewports) so class-string pins can never vouch for geometry again; focused re-verify RESOLVED with live rects. Kept un-registered from modal_stack (T-726 argument, Esc ladder re-proved). Auto-bump claim refuted -> T-821."
main_goal = "Clamp Save Version dialog on-screen; fresh reopen state; focus and Tab trap."
context = [
    "F-04 verified: VERSION input at y=-22 label y=-42 both viewports.",
    "Dialog reopens stale Saved v0.2.0; focus stays on opener not field.",
    "Tab walks out into left dock; no trap within dialog subtree.",
]
requirement = [
    "Clamp dialog fully in viewport flip downward or centre position.",
    "Clear last-save message on reopen; initial focus to version input.",
    "Trap Tab within dialog while open; Esc-close must stay.",
]
current_state = [
    "eden_top_strip.rs 1480-1601 anchored upward no viewport clamping.",
    "T-786 modal stack landed; mount through its plumbing not parallel.",
    "Version prefill auto-bump 0.1.0 to 0.2.0 correct today keep it.",
]
approach = [
    "Clamp panel class top-full position into viewport at both sizes.",
    "NodeRef focus-first uncontrolled input; trap Tab both edges.",
]
verify = [
    "Both viewports: version input rect y>=0 bottom<=innerHeight visible.",
    "Reopened dialog no Saved vX until save in-session.",
    "On open activeElement version input; eight Tabs stay in dialog.",
]
citations = [
    "editor_hostile_ux_review F-04",
    "eden_top_strip.rs 1480-1601",
    "T-786 modal stack wave 200",
    "Versions are immutable semver",
]
shipped_at = "93dc7397"
created_at = "2026-08-10T00:34:00Z"
completed_at = "2026-08-10T02:59:52Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/frontend/src/eden_top_strip.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["top_strip"]
