id = "T-790"
kind = "work"
title = "Markers render their icon glyph and caption on the map"
summary = "Markers render their icon glyph and caption on the map"
class = "bug"
status = "shipped"
order = 4720
executor = "claude-code"
notes = "T-069/T-213/T-673 are the marker-family parents — cite; lane format consumed by T-806/T-808 (depends_on edges) | OPERATOR DIRECTION 2026-08-10: any marker-EDITING surface this wave opens (the F-07 on-map-select fold) must be the Attributes modal (T-763 marker attrs lineage), NOT a new dock editing lane — the dock places, the outliner lists, Attributes edits. | SHIPPED W204: glyphs + captions render through the pinned T-760 lane (3 distinct signatures at 3 zooms, centroid-exact dock-vs-map, 64/64 alias table for T-806/T-808, buffer lifecycle leak-checked 12x). Disclosed outside-owns edit (mission_history.rs, 6 lines at the T-760 feed sites) audited exact by the verifier, feed pins intact. Caption screen-space drift at close zoom -> T-828."
main_goal = "Extend marker lane with glyph id and caption text on map."
context = [
    "F-03 S1 verified: every marker identical pale dot at three zooms.",
    "Caption shown on map field persists in doc but renders nothing.",
    "T-760 lane exists xy+tint only; rendering is missing half.",
]
requirement = [
    "Extend marker lane with glyph id mapping 64-alias canonical set.",
    "Render icon shape per marker plus caption screen-space culled readable.",
    "Reuse existing label path; collapse Waypoint waypoint alias duplicates.",
]
current_state = [
    "mission_editor markers_bind marker_lane_xy_tints T-760 lane only.",
    "store.rs 3831 and map-engine-render scene engine lanes consumers.",
    "WRITE-HALF format T-806 picker T-808 symbology build on this.",
]
approach = [
    "Extend lane format cleanly; side tint stays on marker.",
    "Glyph mapping table collapses case-duplicates for T-806 canonical doc.",
    "Per-side briefing sets dozens not thousands performance budget.",
]
verify = [
    "Three icons plus caption: three DISTINCT glyph signatures three zooms.",
    "Caption within 40px of marker OCR or pixel-diff vs empty control.",
    "Dock-vs-map position agreement stays exact.",
]
citations = [
    "editor_hostile_ux_review F-03",
    "T-760 marker_lane_xy_tints",
    "store.rs 3831",
    "T-806 T-808 WRITE-HALF",
]
shipped_at = "3c1eab05"
created_at = "2026-08-10T04:01:59Z"
completed_at = "2026-08-10T05:01:31Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "crates/map-engine-core/src/doc/store.rs",
    "crates/map-engine-render/src/scene.rs",
    "crates/map-engine-render/src/engine.rs",
    "crates/map-engine-render/src/lanes.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
