id = "T-790"
kind = "work"
title = "Markers render their icon glyph and caption on the map"
summary = "UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-03 — S1, VERIFIED at 5.58/1.30/1.06 m/px: every placed marker is an identical pale dot — icon choice has no visible effect and the caption field literally labelled 'Caption shown on the map' shows nothing on the map (doc state verified correct: dock row reads 'Attack (BLUFOR) · 4921, 5982' and caption persists — rendering is the only missing half). THE LANE ALREADY EXISTS: T-760 packs markers into a render lane with xy+tint ONLY (mission_editor.rs markers_bind / marker_lane_xy_tints, store ~ crates/map-engine-core/src/doc/store.rs:3831, consumers crates/map-engine-render/src/{scene,engine,lanes}.rs). FIX: extend the marker lane with a glyph id (map the 64-alias enum onto its canonical glyph set) and render the icon shape per marker plus the caption text beside it (screen-space, culled, readable at standard zooms); side tint stays. This is the WRITE-HALF format change T-806 (picker) and T-808 (unit symbology) build on — land the lane format cleanly. ⚠️ TRAPS: text rendering in the engine may need the existing label path the map uses for place names — reuse it, do not mint a second text pipeline; alias enum has case-duplicates (Waypoint/waypoint) — glyph mapping table must collapse them (canonical mapping documented for T-806); performance: markers are per-side briefing sets, expect dozens not thousands. ACCEPTANCE: scripted — place markers of 3 different icons + one caption: screenshot probe at 3 zooms shows 3 DISTINCT glyph pixel signatures and the caption string appears within 40px of its marker (OCR or pixel-diff vs empty-caption control); dock-vs-map position agreement stays exact."
status = "shipped"
order = 4720
executor = "claude-code"
notes = "T-069/T-213/T-673 are the marker-family parents — cite; lane format consumed by T-806/T-808 (depends_on edges) | OPERATOR DIRECTION 2026-08-10: any marker-EDITING surface this wave opens (the F-07 on-map-select fold) must be the Attributes modal (T-763 marker attrs lineage), NOT a new dock editing lane — the dock places, the outliner lists, Attributes edits. | SHIPPED W204: glyphs + captions render through the pinned T-760 lane (3 distinct signatures at 3 zooms, centroid-exact dock-vs-map, 64/64 alias table for T-806/T-808, buffer lifecycle leak-checked 12x). Disclosed outside-owns edit (mission_history.rs, 6 lines at the T-760 feed sites) audited exact by the verifier, feed pins intact. Caption screen-space drift at close zoom -> T-828."
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "crates/map-engine-core/src/doc/store.rs",
    "crates/map-engine-render/src/scene.rs",
    "crates/map-engine-render/src/engine.rs",
    "crates/map-engine-render/src/lanes.rs",
]

[scope.website.editor]
chrome = []
