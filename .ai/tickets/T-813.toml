id = "T-813"
kind = "work"
title = "Multi-edit differing blur stamps empty; field Escape also closes modal"
summary = "Multi-edit differing field: focus+blur with zero typing wipes the field across the selection; field Escape also closes the modal"
class = "bug"
status = "shipped"
order = 4950
executor = "claude-code"
notes = "wave 209; verifier wave200.md F3+F6; composes with T-814 Esc layering | SHIPPED W209: differing-field zero-typing blur now depth/digest-unchanged (wave-200 F3 dead); typed value still stamps the selection incl. re-typing a settled value; field-Esc two-stage verified; composed Esc ladder with T-814 peels one layer per press."
main_goal = "Differing multi-edit blur must not stamp empty; field Escape must not close modal."
context = [
    "F3 two differing ROLE slots blur without typing undo depth plus two digest emptied.",
    "attributes.rs 795-801 text_commit skip defeated when field differs seeds empty draft.",
    "F6 field Escape abandons draft but modal window listener 230-236 also closes.",
]
requirement = [
    "Operator-edited latch set on real input commit only if edited.",
    "Preserve deliberate typing stamps whole selection including re-typing one slot value.",
    "Field Escape stop_propagation first press abandons blurs second closes modal text and number.",
]
current_state = [
    "Pure focus blur on differing field stamps empty across N slots F-26 cost.",
    "Single-selection focus Enter blur depth digest unchanged verifier green.",
    "Field keydown attributes.rs 834-845 does not consume Escape event.",
]
approach = [
    "Do not edit ui.rs eden_top_strip.rs T-814 owns modal Esc consume-aware guard.",
    "ROTATION digit entry must not regress focus kept.",
    "Composes with T-814 field layer first one Esc per press ladder.",
]
verify = [
    "Differing ROLE apply-to-all focus blur zero typing undo depth digest UNCHANGED.",
    "Type real value commits to all selected slots.",
    "Field Escape draft abandoned modal STAYS open second Escape closes; ROTATION 135 focus kept.",
]
citations = [
    "wave200 F3 F6 verifier",
    "attributes.rs 795-801 text_commit",
    "F-26 multi-edit batching",
    "T-814 modal Esc ladder",
]
shipped_at = "4e7a1ef3"
created_at = "2026-08-09T15:27:47Z"
completed_at = "2026-08-09T16:09:34Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/attributes.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["attr_panel"]
