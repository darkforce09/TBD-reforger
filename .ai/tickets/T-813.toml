id = "T-813"
kind = "work"
title = "Multi-edit differing field: focus+blur with zero typing wipes the field across the selection; field Escape also closes the modal"
summary = "WAVE-200 VERIFIER RE-TICKET (wave200.md F3 MAJOR + F6 MINOR). F3 VERIFIED LIVE with digest+undo depth: 2 slots with differing roles, 'Multiple values' shown, apply-to-all ticked, click into ROLE, click the modal header (blur), NO key pressed -> undo depth +2 (one step per slot, the F-26 cost), slots_digest role fields emptied for exactly the selected slots; 2x Ctrl+Z restored byte-identical. MECHANISM attributes.rs:795-801: text_commit skips only on !gate.differs(), and a differing draft seeds from text_display()='' — the no-op skip is defeated exactly when the field differs, so a pure change-your-mind interaction stamps '' across N slots. FIX: an 'operator actually edited the draft' latch set on real input; commit only if edited. MUST PRESERVE (verifier-proven green): deliberately typing a value — including re-typing one slot's value — still stamps the whole selection; single-selection no-op skip (focus+Enter and focus+blur -> depth/digest unchanged); 'Multi Rle X' -> committed to every selected slot with no per-keystroke fan-out. F6 VERIFIED LIVE: Escape in a focused field abandons the draft (no write, no undo step — correct) but the modal ALSO closed on the same press — the field keydown (attributes.rs:834-845) does not consume the event and the modal window listener (:230-236) closes on it. FIX: field-level Escape consumes (stop_propagation): first press abandons+blurs, second closes the modal — for text_field AND number_field (same family, pre-existing there). TRAPS: ROTATION digit entry must not regress; T-814 makes the modal-side Esc consume-aware in ui.rs/eden_top_strip.rs — the field-side consume composes with it (field layer first); do NOT edit those files, T-814 owns them this wave. ACCEPTANCE (verifier probes): differing ROLE + apply-to-all + focus + blur, zero typing -> undo depth UNCHANGED, digest UNCHANGED; then type a real value -> commits to all selected; field Escape -> draft abandoned, modal STAYS open, second Escape closes it; ROTATION '135' still enters with focus kept."
status = "shipped"
order = 4950
executor = "claude-code"
notes = "wave 209; verifier wave200.md F3+F6; composes with T-814 Esc layering | SHIPPED W209: differing-field zero-typing blur now depth/digest-unchanged (wave-200 F3 dead); typed value still stamps the selection incl. re-typing a settled value; field-Esc two-stage verified; composed Esc ladder with T-814 peels one layer per press."
owns = ["apps/website/frontend/src/attributes.rs"]

[scope.website.editor]
chrome = []
