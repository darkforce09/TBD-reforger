id = "T-599"
kind = "work"
title = "wave.sh LFS guard false positive on path"
summary = "wave.sh push refuses on the LFS PATH, not on LFS attributes — a false positive that blocks a legitimate push"
class = "bug"
status = "shipped"
order = 3472
executor = "claude-code"
priority = 2
main_goal = "Fix wave.sh LFS guard to check git attr not path pattern."
context = [
    "2026-07-31 wave 74 close: wave.sh push refused map-assets path.",
    "Guard path-based; assumes all packages/map-assets is LFS.",
    "Measured 30-file range: zero files resolve filter:lfs.",
]
requirement = [
    "Use git check-attr filter; refuse only genuine LFS files.",
    "Message must name which files tripped the guard.",
]
current_state = [
    "Guard matches packages/map-assets/** regardless of .gitattributes.",
    "T-594 regenerated prefabs.json.gz and type-inventory.json as real bytes.",
    "False positive blocked correct push of 19 commits.",
]
approach = [
    "git diff --name-only range piped through git check-attr filter.",
    "Refuse only when at least one file resolves to filter:lfs.",
]
verify = [
    "Push range with only non-LFS map-assets changes succeeds.",
    "Push range with genuine LFS files still refused.",
]
citations = [
    "wave.sh LFS path guard",
    "git check-attr filter authority",
]
shipped_at = "7b3630fa"
created_at = "2026-07-31T15:36:36Z"
completed_at = "2026-07-31T16:09:19Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
