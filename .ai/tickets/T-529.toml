id = "T-529"
kind = "work"
title = "events roster SQL still allows whitespace arma_id (no btrim)"
summary = "Wave 47 verifier P2 residual from T-350. ingest_event_roster still uses u.arma_id <> '' without btrim; whitespace passes and can emit as seating key. Latent after 0016 scrub. Cure: btrim/nonempty predicate + trim seating key consistently with other arma_id sites. Owns: apps/website/api/src/handlers/events.rs (+ IT). Caution: T-343 one-sided-trim traps — read before changing."
class = "feature"
status = "shipped"
order = 3369
executor = "claude-code"
priority = 2
estimated = ["scope"]
owns = [
    "apps/website/api/src/handlers/events.rs",
    "apps/website/api/tests/t529_roster_whitespace_arma.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
