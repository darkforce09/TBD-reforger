id = "T-529"
kind = "work"
title = "events roster SQL still allows whitespace arma_id (no btrim)"
summary = "events roster SQL still allows whitespace arma_id (no btrim)"
class = "feature"
status = "shipped"
order = 3369
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Wave 47 verifier P2 residual from T-350. ingest_event_roster still uses u.arma_id <> '' without btrim; whitespace passes and can emit as seating key. Latent after 0016 scrub. Cure: btrim/nonempty predicate + trim seating key consistently with other arma_id sites. Owns: apps/website/api/src/handlers/events.rs (+ IT). Caution: T-343 one-sided-trim traps — read before changing.

missing=0"""
priority = 2
main_goal = "Fix ingest_event_roster whitespace arma_id with btrim predicate."
context = [
    "Wave 47 verifier P2 residual from T-350.",
    "ingest_event_roster uses u.arma_id <> '' without btrim.",
]
requirement = [
    "btrim/nonempty predicate on roster SQL arma_id filter.",
    "Trim seating key consistently with other arma_id sites.",
]
current_state = [
    "Whitespace arma_id passes filter and emits as seating key.",
    "Latent after 0016 scrub; T-343 one-sided-trim traps apply.",
]
approach = [
    "Add btrim/nonempty predicate to ingest_event_roster.",
    "Trim seating key like other arma_id handler sites.",
]
verify = ["cargo test -p website-api --test t529_roster_whitespace_arma"]
acceptance = [
    "ingest_event_roster filtered whitespace arma_id via btrim in roster SQL.",
    "Padded real arma_id emitted trimmed seating key matching T-350 linked checks.",
]
citations = [
    "Wave 47 verifier T-350",
    "T-343 one-sided-trim caution",
    "ingest_event_roster btrim",
]
shipped_at = "79a8b06a"
created_at = "2026-07-27T11:37:32Z"
completed_at = "2026-07-27T12:46:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/events.rs",
    "apps/website/api/tests/t529_roster_whitespace_arma.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
