id = "T-735"
kind = "work"
title = "Loadout import checker fails OPEN; blind guard pin"
summary = "Loadout import schema checker fails OPEN in three forms; its guard pin is blind to all three"
class = "bug"
status = "shipped"
order = 4611
executor = "claude-code"
notes = "filed by wave-112 adversarial verifier (editor factory)"
main_goal = "Fail-closed loadout import checker; fix blind guard pin for three traps."
context = [
    "Wave-112 verifier proved checker extracted verbatim from arsenal_rules 999-1505.",
    "T-686 claims SUPPORTED_SCHEMA_KEYWORDS fail CLOSED; three forms fail OPEN instead.",
    "Shipped loadout-export.schema.json all supported today; one defs edit away from trap.",
]
requirement = [
    "Propagate refusal-class faults from non-passing oneOf branches.",
    "Refuse non-boolean additionalProperties and non-object items not ignore.",
    "Make keyword pin walk POSITIONALLY not sniff supported keywords only.",
]
current_state = [
    "oneOf discards refusals when passing==1 return; losing branch keyword never surfaces.",
    "additionalProperties only == false; type string form silent no-op accepts x:123.",
    "Tuple items dropped via as_object miss; pin skips maxItems-only nodes entirely.",
]
approach = [
    "Surface oneOf branch refusals; refuse bad additionalProperties and items forms.",
    "Positional keyword pin walk replacing is_schema supported-keyword heuristic.",
]
verify = [
    "Three trap documents refused not Ok; verifier traps yield flags not zero.",
    "Control mixed type object maxProperties refused and flagged remains green.",
]
citations = [
    "editor_verify/wave112 MAJOR-1",
    "T-686 SUPPORTED_SCHEMA_KEYWORDS",
    "an_unimplemented_keyword pin",
    "loadout-export.schema.json",
]
shipped_at = "65244f80"
created_at = "2026-08-07T23:12:40Z"
completed_at = "2026-08-07T23:17:21Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/arsenal_rules.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
