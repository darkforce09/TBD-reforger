id = "T-258"
kind = "work"
title = "POST /versions is unaudited and does not bump updated_at"
summary = "Version save now bumps updated_at and writes an audit row; missions integration test locks the contract."
class = "feature"
status = "shipped"
order = 2760
executor = "claude-code"
context = [
    "POST /versions had no audit trail of who saved what.",
    "Mission library ordering and approvals queue both read updated_at, so saves looked stale.",
    "editor_notes was write-only — posted but never returned on the DTO.",
]
requirement = [
    "Bump mission updated_at on every version POST.",
    "Write an audit log row recording the saving user and version id.",
]
current_state = [
    "POST /versions left mission updated_at unchanged and wrote no audit row.",
    "Library ordering and approvals queue read updated_at, so saves looked stale.",
]
approach = [
    "create_version handler updates missions.updated_at then write_audit sequentially.",
    "Inserted audit_logs row recording saver user_id and saved version id.",
]
verify = ["missions IT: create_version_bumps_updated_at_and_writes_audit."]
acceptance = ["Version POST bumped updated_at and wrote audit row; missions IT locked the contract."]
citations = [
    "commit 6134e6a4 version save audit",
    "wave 11 close f2f3dfa7",
]
shipped_at = "f2f3dfa7"
created_at = "2026-07-26T22:53:08Z"
completed_at = "2026-07-26T23:07:33Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/missions.rs",
    "apps/website/api/tests/missions.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
