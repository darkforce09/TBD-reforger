id = "T-806"
kind = "work"
title = "Marker picker: one canonical row per glyph"
summary = "Marker picker: one row per canonical icon, real glyphs, slugs demoted"
class = "bug"
status = "shipped"
order = 4880
executor = "claude-code"
notes = """
depends_on T-790 (canonical glyph mapping is produced there); census T-069/T-213/T-673 cited as marker family | SHIPPED W205: picker 64->11 canonical rows, distinct SVG per glyph (11/11 verified), canonical slug stored and the map renders it (pick-Attack -> triangle-up at 3 zooms — the T-790 handshake end-to-end), alias search works, compile-time count tie to MARKER_GLYPH_COUNT. T-825 boundary respected (no new dock editing surface).

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-08 — VERIFIED: the picker is 64 rows of raw schema slugs (eden_dock_right.rs ~:2828-2839 marker_icons() straight off the $defs/marker.icon enum): every row wears the SAME generic pin glyph, the display name truncates to 'Ob…' while the raw slug gets the space, and case-duplicates render as separate rows (Waypoint/waypoint/Waypoint2/waypoint2, Objective/Obj/Target, mark/marker/point). Choosing an icon by sight is impossible — Eden's F6 shows the actual glyphs (frame 170028). FIX: render one row per CANONICAL icon using T-790's canonical glyph mapping (the case-collapse table it documents), show the real glyph per row, human name gets the width, slug moves to tooltip + search-match only; keep 'Search icons' matching slugs and names. ⚠️ TRAPS: the schema enum is CLOSED and compiled missions may carry any alias — the picker collapses DISPLAY, the doc keeps storing the canonical slug; placed markers list below the picker keeps its caption-first rows; do not touch the map render (T-790 owns it). ACCEPTANCE: scripted — picker row count == canonical glyph count (< 64, documented number from T-790's table); no two rows differ only by case; every row's glyph pixel signature differs from the generic pin; picking 'Attack' stores the canonical slug and the map (post-T-790) shows the attack glyph.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-08 — VERIFIED: the picker is 64 rows of raw schema slugs (eden_dock_right.rs ~:2828-2839 marker_icons() straight off the $defs/marker.icon enum): every row wears the SAME generic pin glyph, the display name truncates to 'Ob…' while the raw slug gets the space, and case-duplicates render as separate rows (Waypoint/waypoint/Waypoint2/waypoint2, Objective/Obj/Target, mark/marker/point). Choosing an icon by sight is impossible — Eden's F6 shows the actual glyphs (frame 170028). FIX: render one row per CANONICAL icon using T-790's canonical glyph mapping (the case-collapse table it documents), show the real glyph per row, human name gets the width, slug moves to tooltip + search-match only; keep 'Search icons' matching slugs and names. ⚠️ TRAPS: the schema enum is CLOSED and compiled missions may carry any alias — the picker collapses DISPLAY, the doc keeps storing the canonical slug; placed markers list below the picker keeps its caption-first rows; do not touch the map render (T-790 owns it). ACCEPTANCE: scripted — picker row count == canonical glyph count (< 64, documented number from T-790's table); no two rows differ only by case; every row's glyph pixel signature differs from the generic pin; picking 'Attack' stores the canonical slug and the map (post-T-790) shows the attack glyph."]

missing=0
"""
main_goal = "Collapse marker picker to one canonical glyph row each."
context = [
    "F-08 picker 64 rows raw schema slugs same generic pin glyph.",
    "Case-duplicates separate rows; display name truncates to Ob.",
    "Eden F6 frame 170028 shows actual glyphs; T-790 owns mapping.",
]
requirement = [
    "One row per canonical icon using T-790 case-collapse table.",
    "Real glyph per row; human name width; slug tooltip search only.",
    "Search icons matches slugs and names; store canonical slug.",
]
current_state = [
    "eden_dock_right marker_icons 2828-2839 off marker.icon enum.",
    "Schema enum closed; compiled missions may carry any alias.",
    "Placed markers list keeps caption-first rows; map render untouched.",
]
approach = [
    "Picker collapses display; doc stores canonical slug.",
    "Compile-time count tie to MARKER_GLYPH_COUNT.",
]
verify = [
    "Picker row count equals canonical glyph count under 64.",
    "No two rows differ only by case; each glyph differs from generic pin.",
    "Pick Attack stores canonical slug; map shows attack glyph.",
]
acceptance = [
    "Marker picker collapsed to eleven canonical rows each rendering distinct glyph from T-790 mapping.",
    "Picking Attack stored canonical slug rendered triangle-up on map at three zooms; alias search worked.",
]
citations = [
    "editor_hostile_ux_review F-08",
    "T-790 canonical glyph table",
    "Eden frame 170028 glyphs",
    "MARKER_GLYPH_COUNT compile tie",
    "c67688aa canonical marker picker",
]
shipped_at = "c67688aa"
created_at = "2026-08-10T16:20:17Z"
completed_at = "2026-08-10T16:33:08Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_dock_right.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
