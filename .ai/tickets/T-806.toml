id = "T-806"
kind = "work"
title = "Marker picker: one canonical row per glyph"
summary = "Marker picker: one row per canonical icon, real glyphs, slugs demoted"
class = "bug"
status = "shipped"
order = 4880
executor = "claude-code"
notes = "depends_on T-790 (canonical glyph mapping is produced there); census T-069/T-213/T-673 cited as marker family | SHIPPED W205: picker 64->11 canonical rows, distinct SVG per glyph (11/11 verified), canonical slug stored and the map renders it (pick-Attack -> triangle-up at 3 zooms — the T-790 handshake end-to-end), alias search works, compile-time count tie to MARKER_GLYPH_COUNT. T-825 boundary respected (no new dock editing surface)."
main_goal = "Collapse marker picker to one canonical glyph row each."
context = [
    "F-08 picker 64 rows raw schema slugs same generic pin glyph.",
    "Case-duplicates separate rows; display name truncates to Ob.",
    "Eden F6 frame 170028 shows actual glyphs; T-790 owns mapping.",
]
requirement = [
    "One row per canonical icon using T-790 case-collapse table.",
    "Real glyph per row; human name width; slug tooltip search only.",
    "Search icons matches slugs and names; store canonical slug.",
]
current_state = [
    "eden_dock_right marker_icons 2828-2839 off marker.icon enum.",
    "Schema enum closed; compiled missions may carry any alias.",
    "Placed markers list keeps caption-first rows; map render untouched.",
]
approach = [
    "Picker collapses display; doc stores canonical slug.",
    "Compile-time count tie to MARKER_GLYPH_COUNT.",
]
verify = [
    "Picker row count equals canonical glyph count under 64.",
    "No two rows differ only by case; each glyph differs from generic pin.",
    "Pick Attack stores canonical slug; map shows attack glyph.",
]
citations = [
    "editor_hostile_ux_review F-08",
    "T-790 canonical glyph table",
    "Eden frame 170028 glyphs",
    "MARKER_GLYPH_COUNT compile tie",
]
shipped_at = "c67688aa"
created_at = "2026-08-10T16:20:17Z"
completed_at = "2026-08-10T16:33:08Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_dock_right.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
