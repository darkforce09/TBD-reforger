id = "T-806"
kind = "work"
title = "Marker picker: one row per canonical icon, real glyphs, slugs demoted"
summary = "UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-08 — VERIFIED: the picker is 64 rows of raw schema slugs (eden_dock_right.rs ~:2828-2839 marker_icons() straight off the $defs/marker.icon enum): every row wears the SAME generic pin glyph, the display name truncates to 'Ob…' while the raw slug gets the space, and case-duplicates render as separate rows (Waypoint/waypoint/Waypoint2/waypoint2, Objective/Obj/Target, mark/marker/point). Choosing an icon by sight is impossible — Eden's F6 shows the actual glyphs (frame 170028). FIX: render one row per CANONICAL icon using T-790's canonical glyph mapping (the case-collapse table it documents), show the real glyph per row, human name gets the width, slug moves to tooltip + search-match only; keep 'Search icons' matching slugs and names. ⚠️ TRAPS: the schema enum is CLOSED and compiled missions may carry any alias — the picker collapses DISPLAY, the doc keeps storing the canonical slug; placed markers list below the picker keeps its caption-first rows; do not touch the map render (T-790 owns it). ACCEPTANCE: scripted — picker row count == canonical glyph count (< 64, documented number from T-790's table); no two rows differ only by case; every row's glyph pixel signature differs from the generic pin; picking 'Attack' stores the canonical slug and the map (post-T-790) shows the attack glyph."
status = "shipped"
order = 4880
executor = "claude-code"
notes = "depends_on T-790 (canonical glyph mapping is produced there); census T-069/T-213/T-673 cited as marker family | SHIPPED W205: picker 64->11 canonical rows, distinct SVG per glyph (11/11 verified), canonical slug stored and the map renders it (pick-Attack -> triangle-up at 3 zooms — the T-790 handshake end-to-end), alias search works, compile-time count tie to MARKER_GLYPH_COUNT. T-825 boundary respected (no new dock editing surface)."

[scope.website.editor]
chrome = []
