id = "T-911.2"
kind = "work"
title = "T-911 — Ticket registry redesign: per-file split + typed schema"
summary = "Phase 2 typed Scope/Status with encoding C, frozen-49, Ready prose, mapper children."
class = "feature"
status = "shipped"
order = 5910
spec = "docs/platform/t911_ticket_registry_redesign.md"
executor = "claude-code"
notes = "Phase 2: typed Scope/Status, encoding C, frozen-49, Ready prose, T-159.23 shipped, T-674/T-675 children via mapper. Depends on T-911.1 landed. No wave.lock, no metrics producer."
parent = "T-911"
context = [
    "Depends on T-911.1 isomorphic tree; crates/tbd-tickets owns typing.",
    "Re-run mapper on phase-1 tree; no wave.lock or metrics producer.",
]
requirement = [
    "Scope/Status enums; flat status plus sibling order encoding C.",
    "frozen-49 needs-operator set-equality; Ready prose on listed tickets.",
    "T-159.23 shipped; T-674/T-675 Program children via mapper.",
]
current_state = ["Shipped at 00e09ff1; MapOutcome boxed for clippy gate."]
approach = ["tbd-tickets parse render roundtrip; trybuild ModLayer::Frontend must not compile."]
verify = [
    "proptest parse(render(t))==t; deny clippy wildcard_enum_match_arm.",
    "cargo xtask ticket check --strict OK post mapper.",
]
citations = [
    "t911_ticket_registry_redesign.md",
    "commit 00e09ff1 clippy fix",
    "frozen-49 needs-operator set",
]
shipped_at = "00e09ff15"
created_at = "2026-08-13T17:28:32Z"
completed_at = "2026-08-13T17:30:21Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 2 commit subject(s)"

[scope]
domain = "repo"
layer = "xtask"
