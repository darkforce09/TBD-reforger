id = "T-476"
kind = "work"
title = "verify-t438/t456 Makefile recipes lack hollow-body tripwire"
summary = "verify-t438/t456 Makefile recipes lack hollow-body tripwire"
class = "chore"
status = "shipped"
order = 3315
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

NIT from wave 28 adversarial. T-467 wired real bash recipes into Makefile + ci-local + ci.yml, but there is no T-471/T-472-style recipe-body tripwire — a future hollow `@true` / `echo PASS` / `#fake` recipe would green until noticed.

Repro (hypothetical): replace verify-t438 recipe with `@true` → make verify-t438 PASS while script never runs.

Cure: extend verify-t468-style (or sibling) tripwire to assert Makefile verify-t438/verify-t456 recipes invoke the real bash scripts (not echo/@true/#).

missing=0"""
priority = 2
main_goal = "Extend verify-t468-style tripwire to verify-t438 and verify-t456 recipes."
context = [
    "NIT from wave 28 adversarial.",
    "T-467 wired real bash recipes; no recipe-body tripwire yet.",
]
requirement = [
    "Assert Makefile verify-t438/verify-t456 invoke real bash scripts.",
    "Reject hollow @true / echo PASS / #fake recipes.",
]
current_state = ["Hypothetical repro: @true verify-t438 recipe → make PASS, script never runs."]
approach = ["Cure: extend verify-t468-style tripwire to t438/t456 recipes."]
verify = ["Hypothetical: replace verify-t438 with @true → make PASS."]
citations = [
    "wave 28 adversarial NIT",
    "T-467 Makefile recipes",
    "verify-t468 recipe tripwire",
]
shipped_at = "c2e009a3"
created_at = "2026-07-27T03:52:19Z"
completed_at = "2026-07-27T05:03:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = [
    "Makefile",
    "scripts/mod/verify-t468-ci-schema-parity.sh",
]

[scope]
domain = "repo"
layer = "xtask"
