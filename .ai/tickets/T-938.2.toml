id = "T-938.2"
kind = "work"
parent = "T-938"
title = "Measure and ring-buffer chunk-crossing uploads"
summary = "The audit claims world_host.rs:454-525 allocates ten or more buffers per chunk crossing but nobody verified it; this slice measures first, reports if false, and otherwise ring-buffers the uploads."
class = "bug"
status = "ready"
order = 7810
priority = 2
spec = "docs/specs/audit_2026_09/t938_engine_perf.md"
plan = "docs/plans/t-938_2_plan.md"
executor = "claude-code"
main_goal = "Chunk crossings are measured, and if they burst allocations, they stop."
context = [
    "Anchor 2026-09-04: UNVERIFIED — world_host.rs exists (chunks :426) but the 454-525 burst claim was not confirmed against live code. Do not assume it.",
    "world_host.rs is owned by T-935.3 and T-935.11 (binary chunk loader and cutover); this slice depends on T-935.11 and packs after it.",
]
requirement = [
    "Step one is measurement: a buffer-allocation counter behind a debug flag on the chunk-crossing path, logged per crossing on everon.",
    "If allocations per crossing are below three, the slice ends with the numbers in its report and ships only the counter.",
    "Otherwise a ring of reusable staging buffers per lane, sized from the measured maximum, replaces per-crossing allocation.",
]
current_state = [
    "world_host.rs uploads chunk data on crossing; allocations per crossing are unmeasured on main.",
]
approach = [
    "Add the counter; cross three chunk boundaries in the dev editor; record allocations per crossing in the report as the defect verification.",
    "Only if confirmed: implement the ring; re-measure; paste before and after.",
    "Perturbation (only if implemented): shrink the ring to one entry; the reuse test must go red; restore, touch, green.",
]
verify = [
    "cargo xtask mk ci-local-leptos",
    "cargo xtask platform wave gate --slice T-938.2",
]
acceptance = [
    "The report states measured allocations per chunk crossing on main; a false claim ships only the counter and says so.",
    "If confirmed, crossings after warm-up allocate no new buffers and chunk rendering is unchanged.",
]
depends_on = ["T-935.11"]
created_at = "2026-09-05T02:09:02Z"
owns = [
    "apps/website/frontend/src/editor/world_assets/world_host.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "world_render"
surface = ["world_host"]
