__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "order",
    "executor",
    "priority",
    "stream",
    "targets",
]
__ord = 525
executor = "claude-code"
id = "T-551"
impact = [
    "tests",
    "events",
]
order = 3391
priority = 2
program = "platform"
status = "shipped"
stream = "web-platform"
summary = """
FOUND by W60 adversarial verifier (CLEAN MINOR) after T-550.

ingest_event_roster loads cargo phys catalog and omits over-capacity missions from roster seating. Coverage is Class-R source pin only; no tests/events.rs IT.

Cure: IT that seeds an over-capacity tip and asserts roster omit / error path.

Repro: rg flatten_to_mod_document_with_catalog apps/website/api/tests/events.rs — no hit."""
surfaces = [
    "API",
    "tests",
]
targets = ["website"]
title = "T-550 Class-R only — no HTTP IT for events roster over-capacity omit"
