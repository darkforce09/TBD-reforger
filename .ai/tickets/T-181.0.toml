id = "T-181.0"
kind = "work"
title = "T-181.0"
summary = "T-181.0"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "scripts/lib/hostrun.sh — agent shell is a debian:12 container; builds/game binaries route to the host. CORRECTED an earlier misdiagnosis that xtask was glibc-broken; it never was."
parent = "T-181"

[scope.repo]
layers = ["docs"]
