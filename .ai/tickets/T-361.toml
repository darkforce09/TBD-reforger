id = "T-361"
kind = "work"
title = "Gate proxy buffers SSE streams blocking browser tests"
summary = "The gate cannot browser-test any SSE page — its proxy awaits the whole body"
class = "bug"
status = "shipped"
order = 3177
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "Make gate serve proxy stream SSE with bytes_stream() and thread api_proxy through render-check with T-339 so browser tests can reach live event streams."
context = [
    "FOUND by T-306, which had to hand-roll a driver to verify its own fix.",
    "Combined with `render-check` hardcoding `api_proxy: None` (T-339), there is no supported way to browser-test a page driven by Server-Sent Events.",
    "The SSE hub is the realtime spine of Server Intel and the dashboard, and it is currently unverifiable by the gate.",
]
requirement = [
    "Make the proxy stream rather than buffer (`bytes_stream()` rather than `bytes()`), which also fixes it for any future long-poll or chunked endpoint.",
    "Sequence this with T-339, which threads `api_proxy` through `bin/gate.rs` -> RenderCheckArgs — without that, a streaming proxy still has no caller.",
    "Both are in tools/tbd-tools; do them together or T-339 first.",
]
current_state = [
    "`gate serve`'s proxy does `upstream.bytes().await`, so it CANNOT proxy an endless event stream — the request never completes.",
    "T-306 worked around it with a shim that replays captured frame bytes plus a close, so the body is finite;",
    'the bytes are real and the only deviation is the close, which the `\n\n` splitter is indifferent to.',
    "That workaround is sound for one slice but should not be the standing answer:",
    "the SSE hub is the realtime spine of Server Intel and the dashboard, and it is currently unverifiable by the gate.",
]
approach = [
    "Make the proxy stream rather than buffer (`bytes_stream()` rather than `bytes()`), which also fixes it for any future long-poll or chunked endpoint.",
    "Sequence this with T-339, which threads `api_proxy` through `bin/gate.rs` -> RenderCheckArgs — without that, a streaming proxy still has no caller.",
    "Both are in tools/tbd-tools; do them together or T-339 first.",
]
verify = ["sse_frames_arrive_incrementally_not_buffered via reqwest chunk() and Body::from_stream."]
acceptance = ["In-process SSE streaming tests proved frames arrive incrementally not buffered."]
citations = [
    "T-306 hand-roll driver",
    "T-339 api_proxy None",
    "bin/gate.rs RenderCheckArgs",
    "gate serve bytes().await",
]
shipped_at = "9c3b2655"
created_at = "2026-07-31T17:06:54Z"
completed_at = "2026-07-31T17:07:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["tools/tbd-tools/src/smokes.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
