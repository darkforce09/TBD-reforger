id = "T-801"
kind = "work"
title = "Squad tether lines follow the drag preview"
summary = "Squad tether lines follow the drag preview"
class = "bug"
status = "shipped"
order = 4830
executor = "claude-code"
notes = "mechanism note from census GPU-preview design; mid-drag screenshot method in review O-7 | SHIPPED W208: pack_squad_link_drag_preview offsets affected endpoints with sprite dx/dy; push/clear_drag_preview bind SQUAD_LINKS; Class-R packer pins perturbation-proven. Live mid-drag 4px CDP + rf<=1ms left to eye-pass (slice disclosed). MissionConnections hairline still doc_tick-only (outside owns)."
main_goal = "Preview tether/line endpoints during drag matching sprite GPU preview transform."
context = [
    "GPU preview repacks slot and vehicle sprites during drag only.",
    "Squad tether lines rebuild only on commit so endpoints lag.",
    "Architecture guarantees stale line positions until mouseup.",
]
requirement = [
    "Include affected tether line endpoints in preview pass.",
    "Apply same transform as sprite preview for dragged ids.",
    "Only affected lines re-pack; commit path unchanged.",
]
current_state = [
    "select_tool.rs preview repacks instances not line endpoints.",
    "slot_line.rs and squad_links.rs update on one-txn commit only.",
    "Multi-select drags may move both ends of one line.",
]
approach = [
    "pack_squad_link_drag_preview offsets endpoints with sprite dx dy.",
    "push clear_drag_preview bind SQUAD_LINKS during drag.",
    "Stay allocation-light per T-057 preview budget constraints.",
]
verify = [
    "Mid-drag: line endpoint within 4px of dragged sprite centre single and 2-selected.",
    "After release committed line matches preview position.",
    "FPS rf delta during 9-slot drag <= 1ms median vs baseline.",
]
citations = [
    "editor_hostile_ux_review O-7 mechanism",
    "select_tool GPU preview repack",
    "slot_line squad_links commit only",
    "T-057 preview per-frame budget",
    "multi-select both endpoints preview",
]
shipped_at = "a45b8567"
created_at = "2026-08-11T01:58:26Z"
completed_at = "2026-08-11T02:36:28Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/select_tool.rs",
    "crates/map-engine-core/src/slot_line.rs",
    "crates/map-engine-core/src/squad_links.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["tools"]
