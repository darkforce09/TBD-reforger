id = "T-756"
kind = "work"
title = "Scale readout non-finite and band-top sig figs"
summary = "Scale readout formatter corners: non-finite prints a confident number, band-top carries to 4 sig figs"
class = "bug"
status = "shipped"
order = 4632
executor = "claude-code"
notes = """
filed by wave-115 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MINOR from the wave-115 verifier. Two edge cases in T-670's format_m_per_px, both outside the live zoom clamp and neither reachable today. (1) A NON-FINITE zoom prints a confident `1.00 m/px` rather than the em-dash the degenerate path is supposed to produce. This inherits T-667's pre-existing convention rather than being introduced by T-670, but the readout now claims three significant figures, so a fabricated number reads as a measured one. (2) Band-top carry yields four significant figures instead of three: 9.996 formats as `10.00` (verified with a compiled probe). Every value INSIDE the real clamp (64.0 m/px at MIN_ZOOM down to 0.015625 at MAX_ZOOM) was verified correct at both endpoints and at the 0.0625 regression case that drove the precision rework mid-slice. Also fold in the verifier's mechanism corrections to the slice's own notes: the native fallback is misdescribed (scale_mpp is SEEDED to 4.0, not absent, and the camera_snapshot() path is now dead code with an identical outcome); the first rAF frame performs one redundant write; and the SCL cell's static fallback would diverge from ScaleBar's live fallback for a future caller that omits the prop — no such caller exists. Diagnosis: editor_verify/wave115.md MINOR-4 + NIT-3.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["MINOR from the wave-115 verifier. Two edge cases in T-670's format_m_per_px, both outside the live zoom clamp and neither reachable today. (1) A NON-FINITE zoom prints a confident `1.00 m/px` rather than the em-dash the degenerate path is supposed to produce. This inherits T-667's pre-existing convention rather than being introduced by T-670, but the readout now claims three significant figures, so a fabricated number reads as a measured one. (2) Band-top carry yields four significant figures instead of three: 9.996 formats as `10.00` (verified with a compiled probe). Every value INSIDE the real clamp (64.0 m/px at MIN_ZOOM down to 0.015625 at MAX_ZOOM) was verified correct at both endpoints and at the 0.0625 regression case that drove the precision rework mid-slice. Also fold in the verifier's mechanism corrections to the slice's own notes: the native fallback is misdescribed (scale_mpp is SEEDED to 4.0, not absent, and the camera_snapshot() path is now dead code with an identical outcome); the first rAF frame performs one redundant write; and the SCL cell's static fallback would diverge from ScaleBar's live fallback for a future caller that omits the prop — no such caller exists. Diagnosis: editor_verify/wave115.md MINOR-4 + NIT-3."]

missing=0
"""
main_goal = "Fix format_m_per_px non-finite and band-top sig-fig edge cases."
context = [
    "Wave-115 MINOR: two format_m_per_px corners outside live zoom clamp unreachable today.",
    "Non-finite zoom inherits T-667 convention; three sig figs make fabricated number look measured.",
    "Inside clamp 64.0 to 0.015625 verified correct at endpoints and 0.0625 regression.",
]
requirement = [
    "Non-finite zoom must emit em-dash not confident 1.00 m/px readout.",
    "Band-top carry 9.996 must format three sig figs not 10.00 four.",
]
current_state = [
    "Degenerate path should em-dash; non-finite prints 1.00 m/px with three figures.",
    "9.996 formats 10.00 four sig figs; verifier probe confirmed.",
    "Native fallback misdescribed: scale_mpp seeded 4.0; camera_snapshot path dead code.",
]
approach = [
    "Fix format_m_per_px degenerate and band-top rounding in eden_toolbelt.",
    "Fold verifier mechanism notes: rAF redundant write SCL static fallback divergence.",
]
verify = [
    "Compiled probe: non-finite yields em-dash; 9.996 yields three sig figs not 10.00.",
    "Clamp endpoints and 0.0625 regression cases remain correct.",
]
acceptance = [
    "Non-finite scale input produced em-dash readout instead of fabricated 1.00 m/px text.",
    "Band-top carry formatted three significant figures; 9.996 no longer printed as 10.00.",
]
citations = [
    "editor_verify wave115 MINOR-4",
    "editor_verify wave115 NIT-3",
    "T-670 format_m_per_px",
    "T-667 degenerate convention",
]
shipped_at = "e31391d0"
created_at = "2026-08-08T05:04:10Z"
completed_at = "2026-08-08T05:10:14Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_toolbelt.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["toolbelt"]
