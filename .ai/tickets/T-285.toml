id = "T-285"
kind = "work"
title = "Field tools — orphaned APIs, dead inject"
summary = "Field tools — solutions never persist and inject writes to a dead directory"
class = "bug"
status = "shipped"
order = 3030
executor = "claude-code"
main_goal = "Every computed mortar solution is lost on reload; map preview and weapon_system lie; inject writes mission.json nothing reads."
context = [
    "POST /fire-missions and GET events fire-missions orphaned since T-145; SPA only hit solve.",
    "Map preview used fixed CSS literals; weapon_system hardcoded despite returned system heading.",
    "POST /missions/{id}/inject wrote mission.json nothing reads.",
]
requirement = [
    "Persist computed mortar solutions via POST /fire-missions; hydrate from GET on operation load.",
    "Project preview from inputs; expose real weapon_system choices; reconcile stale operation pref.",
]
current_state = [
    "POST /fire-missions and GET /events/{id}/fire-missions are orphaned, so every computed solution is lost on reload.",
    "The mortar map preview is fixed CSS that never moves with the inputs, and weapon_system is hardcoded while the heading claims to show the returned system.",
    "POST /missions/{id}/inject writes a mission.json nothing reads.",
]
approach = [
    "Calculate POSTs /fire-missions when operation selected; encode coords in fp_grid/target_grid strings.",
    "SavedFor hydration_step rejects stale batches; reconcile operation picker against live /events list.",
]
verify = ["cargo test -p website-frontend hydration_refuses_a_batch_fetched_for_a_different_operation"]
acceptance = ["Mortar persisted and hydrated fire missions; preview and weapon_system fixed; stale operation pref dropped."]
citations = [
    "1df4fc9f9 fire-mission persistence",
    "453aa9a93 SavedFor hydration race",
]
shipped_at = "d91d758e"
created_at = "2026-07-31T02:23:52Z"
completed_at = "2026-07-31T02:58:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/frontend/src/mortar.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["tools"]
