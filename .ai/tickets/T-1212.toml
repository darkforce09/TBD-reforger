id = "T-1212"
kind = "work"
title = "Correct ticket-engine doc comment on gap-analysis ticket column sync"
summary = "tools_v2/ticket-engine/src/repository.rs:175-176 says ticket sync keeps the Eden gap-analysis ticket column in step, but sync/gap_analysis.rs:52 parses only tables with a priority column, which that document lacks, so sync rewrites it unchanged."
class = "docs"
status = "idea"
notes = "GAP_ANALYSIS in tools_v2/ticket-engine/src/repository.rs:175-176 says ticket sync keeps the ticket column of documentation_v2/website/frontend/apps/editor/eden_editor_reference/eden_gap_analysis.md in step with the registry, but parse_gap_analysis (sync/gap_analysis.rs:52) only collects tables whose header holds both | eden_id | and priority |; the document's tables read | eden_id | tbd_id | parity | ticket | gap_notes |, so sync_gap_analysis_ticket_column matches no table and writes the file back unchanged, and ticket gap-round-trip passes vacuously. The READMEs (tools_v2/ticket-engine/src/sync/README.md, the Eden editor reference README) already say the sync leaves the file unchanged. Either teach the parser the current header or correct the comment. Source: Phase 5 doc writer P5f, verified by P5g."
created_at = "2026-09-26T05:29:40Z"

[scope]
domain = "repo"
layer = "tickets"
