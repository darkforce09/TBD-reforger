id = "T-796"
kind = "work"
title = "Comments look like comments and can be moved"
summary = "Comments look like comments and can be moved"
class = "bug"
status = "shipped"
order = 4780
executor = "claude-code"
notes = """
T-784/T-748/T-781 comment family cited; one-txn move matches the F-26 lesson | SHIPPED W206 (drag half): comments pick/preview/move through move_comment — precedence ring→unit→comment→marquee proven at the boundaries, one undo per note, three non-commit exits leave the lane byte-identical, T-784 click regression green. The GLYPH RESTYLE (a)/(c) is T-808's by band assignment (the registry cites the bubble as its shape-language deliverable) — the amber-ring look persists until W207 BY DESIGN.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-6 — PIXEL-VERIFIED: a placed comment renders as an AMBER RING — the selection colour — so every comment permanently reads as selected (operator caught it; colour match confirmed against the selected-unit amber), there is no comment glyph at all (Eden's Comments layer uses a distinct symbol), and comments CANNOT BE DRAGGED: a 90px drag across a placed comment left its stored position unchanged (verified twice by crop comparison; move_comment exists at mission_editor.rs ~:1554 but is not wired to the drag path). FIX: (a) neutral speech-bubble/note glyph in a non-selection colour; selected state adds the standard selection treatment ON TOP so selected-vs-idle reads correctly; (b) wire comments into the drag-move path (pick -> preview -> move_comment one-txn commit) alongside slots/vehicles; (c) comment glyph participates in the marker-vs-unit shape language (distinct from both). ⚠️ TRAPS: T-784 (comment click-select) shipped this band's precedent — build on its pick lane, do not fork; comments are editor-only (never compiled) — no schema anywhere near this; delete/undo of comments has known per-txn acceptance in delete_selection comments loop — moving must be ONE step. ACCEPTANCE: scripted — placed comment pixel signature != selection amber and != marker glyph; click selects (T-784 regression green); drag moves stored x/y by the drag delta (dock/attrs readback), ONE undo restores; mid-drag the glyph follows the cursor (preview parity with slots).

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-6 — PIXEL-VERIFIED: a placed comment renders as an AMBER RING — the selection colour — so every comment permanently reads as selected (operator caught it; colour match confirmed against the selected-unit amber), there is no comment glyph at all (Eden's Comments layer uses a distinct symbol), and comments CANNOT BE DRAGGED: a 90px drag across a placed comment left its stored position unchanged (verified twice by crop comparison; move_comment exists at mission_editor.rs ~:1554 but is not wired to the drag path). FIX: (a) neutral speech-bubble/note glyph in a non-selection colour; selected state adds the standard selection treatment ON TOP so selected-vs-idle reads correctly; (b) wire comments into the drag-move path (pick -> preview -> move_comment one-txn commit) alongside slots/vehicles; (c) comment glyph participates in the marker-vs-unit shape language (distinct from both). ⚠️ TRAPS: T-784 (comment click-select) shipped this band's precedent — build on its pick lane, do not fork; comments are editor-only (never compiled) — no schema anywhere near this; delete/undo of comments has known per-txn acceptance in delete_selection comments loop — moving must be ONE step. ACCEPTANCE: scripted — placed comment pixel signature != selection amber and != marker glyph; click selects (T-784 regression green); drag moves stored x/y by the drag delta (dock/attrs readback), ONE undo restores; mid-drag the glyph follows the cursor (preview parity with slots)."]

missing=0
"""
main_goal = "Neutral comment glyph and wire drag-move through move_comment."
context = [
    "UX review O-6 pixel-verified; comment renders amber selection ring permanently.",
    "No comment glyph; comments cannot be dragged; move_comment ~1554 unwired.",
    "T-784 comment click-select shipped; build on its pick lane.",
]
requirement = [
    "Neutral speech-bubble glyph non-selection colour; selected adds standard treatment on top.",
    "Wire comments into drag path pick preview move_comment one-txn commit.",
    "Comment glyph distinct from marker and unit shape language.",
]
current_state = [
    "Placed comment amber ring reads permanently selected; 90px drag position unchanged.",
    "move_comment exists mission_editor ~1554 not wired to drag path.",
    "Comments editor-only never compiled; delete/undo per-txn in delete_selection loop.",
]
approach = [
    "Pick precedence alongside slots vehicles; mid-drag glyph follows cursor preview.",
    "Moving one undo step; three non-commit exits leave lane byte-identical.",
]
verify = [
    "Placed comment pixel signature not selection amber not marker glyph.",
    "Click selects T-784 regression; drag moves stored x/y one undo restores.",
]
acceptance = [
    "Comments wired into drag-move path through pick preview and one-transaction move_comment commit.",
    "Pick precedence ring unit comment marquee proven at boundaries; one undo per move; T-784 click-select regression green.",
]
citations = [
    "editor_hostile_ux_review O-6",
    "T-784 comment click-select",
    "T-748 comment family",
    "F-26 one-txn move lesson",
    "move_comment ~1554",
    "efbfb07c comment drag move",
]
shipped_at = "efbfb07c"
created_at = "2026-08-10T19:15:16Z"
completed_at = "2026-08-10T19:35:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/mission_editor.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
