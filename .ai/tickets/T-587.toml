id = "T-587"
kind = "work"
title = "fire_missions lacks coordinates, TOF, and charge columns"
summary = "fire_missions cannot store a solution: no coordinates, no TOF, no charge"
class = "chore"
status = "shipped"
order = 3442
executor = "claude-code"
priority = 2
main_goal = "Add fire_missions coordinate and solution columns; retire grid encoding."
context = [
    "T-285 wired /fire-missions endpoints; worked around schema with grid strings.",
    "Fresh POST shows full TOF; restored row shows honest dash — schema asymmetry.",
]
requirement = [
    "Migration: real numeric coordinate columns plus time_of_flight_s, charge, azimuth_mils.",
    "Backfill from fp_grid/target_grid encoding; then retire grid-string persistence.",
    "GET /fire-missions/weapons; move SavedFire/EventOption to dto.rs.",
]
current_state = [
    "Table stores coordinates only via fp_grid/target_grid text workaround.",
    "WEAPONS in mortar.rs duplicates charges_for in services/mortar.rs.",
    "field_tools.rs:106-112 unreachable guard safe to delete.",
]
approach = [
    "Add migration columns; backfill from fmt_grid/parse_grid round-trip encoding.",
    "Expose weapons list endpoint; relocate DTOs for R-api golden coverage.",
]
verify = [
    "Restored fire mission shows persisted TOF and charge, not fabricated zero.",
    "T-285 captured-live-response test pins SavedFire until dto.rs migration.",
]
citations = [
    "T-285 grid string workaround",
    "fmt_grid parse_grid mortar.rs",
    "WEAPONS duplicates charges_for",
]
shipped_at = "21c31f81"
created_at = "2026-08-01T02:45:54Z"
completed_at = "2026-08-01T02:56:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/api/src/services/mortar.rs",
    "apps/website/api/src/handlers/field_tools.rs",
    "apps/website/api/migrations",
    "apps/website/frontend/src/mortar.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["tools"]
