id = "T-748"
kind = "work"
title = "Comments lack map glyph and composability"
summary = "PLACE-COMMENT-001 does not fully close: comments have no map glyph and cannot be composed"
class = "bug"
status = "shipped"
order = 4624
executor = "claude-code"
notes = """
filed by wave-114 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MAJOR from the wave-114 adversarial verifier. The spec row (eden/interactions.md:214) Procedure step 3 reads 'Draggable, copy/paste, layerable, COMPOSABLE' with Postcondition 'Comment icon at position'. T-651 shipped neither of the last two. (1) NO MAP GLYPH: a comment draws nothing on the map, because it is deliberately absent from the render SoA — the same property that keeps it off the compiled mission. Drawing one needs map-engine-render, which was outside the slice's owns. The slice disclosed this by implication (it explained there is 'no glyph to grab' when narrowing drag to the outliner) but never as a postcondition it failed to meet. (2) NOT COMPOSABLE — AND THIS WAS SILENT: compositions capture is selection-based (editor_ops.rs:728 iterates the selection) and comments sit in NO selection lane (outliner.rs:99-101), so a comment can never enter a composition. That clause appears in no disclosure, commit message or field note. Silent partial delivery is precisely what CLAUDE.md's HARD GATE bars, and it is recorded here rather than left implicit. NOT a code defect: nothing is broken, no data is at risk, and the two acceptance checkboxes plus the registry-summary scope are met under the disclosed narrowings (drag -> outliner + X/Z fields, copy -> duplicate verb). This is scope accounting. FIX SHAPE: a glyph needs a render-side entry for a non-SoA entity; composability needs comments to join a selection lane, which is a larger decision than it looks because the selection lane is what keeps them out of the slot-shaped clipboard and Del-key paths. Decide whether the spec row should be re-scoped instead. Diagnosis: editor_verify/wave114.md MAJOR-1.

missing=0
"""
main_goal = "Close PLACE-COMMENT-001 map glyph and composability postconditions."
context = [
    "eden/interactions.md:214 step 3 requires composable with comment icon postcondition.",
    "T-651 met drag and copy narrowings; two postconditions silently unmet.",
    "Scope accounting not code defect; acceptance checkboxes met under disclosures.",
]
requirement = [
    "Render-side glyph entry for non-SoA comment entity on map.",
    "Comments join selection lane so composition capture can include them.",
    "Or re-scope spec row if composability decision is larger than expected.",
]
current_state = [
    "Comment absent from render SoA; draws nothing on map canvas.",
    "editor_ops.rs:728 selection-based capture; comments in no selection lane.",
]
approach = [
    "Glyph needs map-engine-render entry outside original T-651 owns.",
    "Composability touches slot clipboard and Del-key exclusion lanes.",
]
verify = ["verifier wave114.md MAJOR-1 scope accounting recorded."]
citations = [
    "eden/interactions.md:214",
    "editor_ops.rs:728 selection",
    "outliner.rs:99 no lane",
    "wave114.md MAJOR-1",
]
shipped_at = "e3147bf1"
created_at = "2026-08-08T16:12:30Z"
completed_at = "2026-08-08T16:12:50Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "crates/map-engine-render/src/draw_order.rs",
    "crates/map-engine-render/src/engine.rs",
    "apps/website/frontend/src/mission_history.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
