id = "T-570"
kind = "work"
title = "T-567 Class-R hollow — unreachable cfg and loop decoys"
summary = "T-567 Class-R still hollow — unreachable if true==false / loop-break / cfg(any())"
class = "feature"
status = "shipped"
order = 3423
executor = "claude-code"
priority = 2
main_goal = "Close Class-R hollow bypass via unreachable control-flow decoys."
context = [
    "FOUND by W67 adversarial verifier (DIRTY MAJOR) after T-567.",
    "Exact `if false { … }` now RED.",
]
requirement = [
    "Class-R must RED unreachable apply_row_meta with opt(&row.briefing) decoys.",
    "Covers if true==false, loop break, cfg(any()), while false, if !true.",
]
current_state = [
    "Live briefing args → None; unreachable wrappers keep hydrate pin GREEN.",
    "if true==false / loop break / cfg(any()) / while false / if !true all bypass.",
]
approach = [
    "Extend Class-R to reject unreachable control-flow wrapped apply_row_meta calls.",
    "Treat cfg(any()) and loop-break decoys same as if false.",
]
verify = ["Repro: live briefing → None; wrap decoy in if true==false; pin green."]
citations = [
    "W67 adversarial verifier DIRTY MAJOR",
    "T-567 unreachable apply_row_meta decoy",
    "if true==false loop-break cfg bypass",
]
shipped_at = "abd07e10"
created_at = "2026-07-28T22:46:08Z"
completed_at = "2026-07-31T17:07:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/frontend/src/mission_title_prefer.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
