id = "T-170"
kind = "work"
title = "Prod default flip to Leptos SPA"
summary = "Prod default flip to Leptos SPA"
class = "docs"
status = "queued"
order = 8580
spec = "documentation_v2/tickets/specs/t166_missed_items_handoff.md"
executor = "human"
depends_on = ["T-159"]
main_goal = "As the operator I flip production to the Leptos SPA with a tested rollback path."
context = [
    "OPERATOR-GATED (env + Discord app settings are operator-held);",
    "T-159 finish program left this as the sole operator-gated residual.",
]
requirement = ["Make the Leptos SPA the production default:"]
approach = [
    "set `SPA_DIST_DIR` to the trunk release dist in the prod env, move the Discord OAuth redirect origin, staging soak, then flip.",
    "claude-code prepares configs/runbook + verifies the staging soak gates.",
]
citations = ["Runbook anchor: documentation_v2/runbooks/local_development.md §SPA serving;"]
estimated = ["scope"]
owns = [
    "documentation_v2/runbooks/local_development.md",
    "scripts/deploy",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
