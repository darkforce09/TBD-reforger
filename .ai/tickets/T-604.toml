id = "T-604"
kind = "work"
title = "Joinable mod server missing for playtest"
summary = "Nothing in this repo starts a joinable server with the mod — the playtest cannot be run"
class = "bug"
status = "shipped"
order = 3512
executor = "claude-code"
priority = 0
main_goal = "Ship joinable mod-loaded admin-capable dedicated playtest server script."
context = [
    "FOUND writing PLAYTEST_RUNBOOK; blocks T-181.16 and T-068.14.",
    "deploy-staging.sh -addonsDir no room; -config omits mod.",
    "run-dev-server.sh grep-only stub; never launches server.",
]
requirement = [
    "One script: both -addonsDir and -config together.",
    "Document how second machine joins via LAN or backend.",
    "Admin-capable via game.admins in -config mode.",
]
current_state = [
    "Workshop path absent; TBD_WORKSHOP_MOD_ID commented in deploy.env.",
    "Measured shape at world-boot.sh:773-778; joinability unverified.",
    "No admin means T-181.16 respawn acceptance unreachable.",
]
approach = [
    "Fix deploy-staging or add run-playtest-server with both flags.",
    "Self-test local addon wins over Workshop download.",
]
verify = [
    "Server registers backend room and loads local mod GUID.",
    "#tbd admin commands work after boot with -config.",
]
citations = [
    "T-604 blocks T-181 T-068",
    "deploy-staging catch-22 modes",
    "world-boot both flags shape",
]
shipped_at = "efc3851c"
created_at = "2026-07-31T18:31:27Z"
completed_at = "2026-07-31T19:59:59Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/mod/deploy-staging.sh",
    "scripts/mod/run-dev-server.sh",
    "docs/mod/STAGING-SERVER.md",
]

[scope]
domain = "repo"
layer = "docs"
