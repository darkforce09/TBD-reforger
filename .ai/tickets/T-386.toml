id = "T-386"
kind = "work"
title = "render-check exit cannot fail browser probes"
summary = "render-check's exit code cannot fail a probe — every browser assertion has been decorative"
class = "bug"
status = "shipped"
order = 3202
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now.
'the 'The **the 2026-07-26 30-assertion = == `--assert-js` `--assert-json-path `idea` `pass: `pass` `true`; ability, above above. actually after already always an assert_value.as_ref().map(|v| assertion.** assertions assertOk audits authoritative axis. backlog be blocks budget bug bugs, but BY call, can CANCELLED claim close comment contract costing could DECISION, decorative DEFECT, DEFERRED deliberate developing diagnosed, diagnostics. doc/mission/world does echoed FAIL, failed faithfully, false`; faster feature feature. findable, FIX: fixed fixing form from fully gate's generating good, had here hours. is it it'. it, its judged knowing lands. let lines means moment most moved need NOT not Note notes Nothing nothing now now. OF only OPERATOR operator's optional. Options, order original pass, PASS; pass`. passed preference probe, produced program Promote ran rather raw read read. real Recording recurs reported reproducible return right RUN'S Same says shape, ships. siblings SIGNATURE so So Some(true) someone something, spending stand stand. starts string' substance: success surface. T-182..T-297 T-320's T-338's, T-352's, T-359's, T-368's than that The the their them there things third This THIS THROWN ticket time, to token tonight: tools/tbd-tools/src/smokes.rs:3112-3115: treat truthiness. two v.as_bool() value value; VALUES verbatim W1_reached_editor was were what What when Whichever, will work wrong { });"""
priority = 0
main_goal = "Require explicit pass verdict in render-check — stop truthiness on diagnostic objects."
context = ["THIRD INSTANCE of this run signature defect — harness every frontend slice used."]
requirement = [
    "Treat object with boolean pass field as authoritative; fail on pass:false.",
    "Or require literal true; or add --assert-json-path pass.",
    "Probe returning object with no recognised verdict field must FAIL.",
    "Keep diagnostic string ability — stop treating string as pass.",
]
current_state = [
    "tools/tbd-tools/src/smokes.rs:3112-3115 assert_ok truthy for any non-null non-false value.",
    "Including every JSON object and every non-empty string.",
    'Probe returning {"pass":false,"failed":[...]} printed assertOk:true and exited 0.',
    "Every --assert-js probe returns object of observations.",
    "Gate exit code has never enforced a single browser assertion.",
    "T-320, T-338, T-352, T-359, T-368 findings stand; gate-pass claims do not.",
]
approach = [
    "Compare wave gate DB suite with TEST_DATABASE_URL unset — 30 tests skipped PASS.",
    "Compare clippy without --features — compiled none of doc/mission/world.",
]
citations = [
    "T-320 persistence probe",
    "T-338 gate",
    "smokes.rs:3112 assert_ok",
    "T-352 T-359 probes",
]
shipped_at = "4c627119"
created_at = "2026-07-26T11:26:36Z"
completed_at = "2026-07-27T08:26:55Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = ["tools/tbd-tools/src/smokes.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
