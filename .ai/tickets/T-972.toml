id = "T-972"
kind = "work"
title = "peaks test treats an LFS pointer as a real DEM"
summary = "dem/peaks.rs returns early when the DEM file is absent but not when it is an LFS pointer, so in a slice worktree it panics on decode instead of skipping. Treat a pointer as absent."
class = "feature"
status = "idea"
created_at = "2026-09-06T03:11:34Z"

[scope]
domain = "repo"
layer = "docs"
