id = "T-750"
kind = "work"
title = "Favourites spin forever on registry fetch failure"
summary = "Favourites tab dead-ends in 'Resolving...' forever when the registry fetch fails"
class = "bug"
status = "shipped"
order = 4626
executor = "claude-code"
notes = """
filed by wave-114 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MINOR from the wave-114 verifier. mission_editor.rs:1791-1794: a failed registry fetch sets only `catalog` / `vehicle_catalog` and leaves `registry_items` at None. eden_dock_right.rs:873-881: the favourites panel treats None as 'still loading' and renders 'Resolving N favourite(s) against the catalogue...' with no failure arm, so the tab spins forever with no retry and no explanation. The behaviour is HONEST as far as it goes — T-695 deliberately made the loading state distinct so a slow load cannot mark a whole collection stale, which is the right call — but there is no terminal error state. The underlying omission (a registry fetch failure that sets no error signal) PREDATES T-695; what is new is a surface that renders that omission as an indefinite spinner. Fix: give the fetch a failure signal and add a failure arm to the panel with a retry. Diagnosis: editor_verify/wave114.md MINOR-2.

missing=0
"""
main_goal = "Add registry fetch failure signal and favourites panel retry arm."
context = [
    "Wave-114 verifier MINOR: favourites tab indefinite Resolving spinner.",
    "T-695 loading-state distinction correct; no terminal error state yet.",
    "Registry fetch failure omission predates T-695; new surface exposes it.",
]
requirement = [
    "Failed registry fetch sets failure signal not only catalog fields.",
    "Favourites panel failure arm with retry replaces infinite Resolving state.",
]
current_state = [
    "mission_editor.rs:1791-1794 leaves registry_items None on fetch fail.",
    "eden_dock_right.rs:873-881 treats None as still loading forever.",
]
approach = [
    "Preserve T-695 slow-load honesty; add terminal error not stale marking.",
    "Wire failure signal from fetch into favourites panel render branch.",
]
verify = ["verifier wave114.md MINOR-2 failure arm with retry."]
acceptance = [
    "Registry fetch Err wrote failure signal alongside catalog fields instead of leaving registry_items None.",
    "Favourites panel showed terminal error with Retry instead of indefinite Resolving spinner.",
]
citations = [
    "mission_editor.rs:1791 fetch",
    "eden_dock_right.rs:873 panel",
    "wave114.md MINOR-2",
    "T-695 loading distinction",
    "9b0060d3 retry arm",
]
shipped_at = "9b0060d3"
created_at = "2026-08-08T05:06:52Z"
completed_at = "2026-08-08T05:10:14Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/eden_dock_right.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "map_canvas",
    "dock_right",
]
