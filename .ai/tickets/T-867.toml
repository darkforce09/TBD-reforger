id = "T-867"
kind = "work"
title = "T-853 port: mission-version-upload-repro -> repro mission-upload"
summary = "T-853 port: mission-version-upload-repro -> repro mission-upload"
class = "chore"
status = "shipped"
order = 5480
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/website/mission-version-upload-repro.sh (62 lines) to `cargo xtask repro mission-upload` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). already has an xtask twin in repro.rs - confirm then delete. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/website/mission-version-upload-repro.sh (62 lines) to `cargo xtask repro mission-upload` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). already has an xtask twin in repro.rs - confirm then delete. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0"""
main_goal = "Port mission-version-upload-repro.sh to cargo xtask repro mission-upload."
context = [
    "T-853 shell->xtask slice; 62-line bash script.",
    "Already has an xtask twin in repro.rs; confirm then delete.",
    "Callers include Makefile platform wave.sh mod wave.sh workflows.",
]
requirement = [
    "Delete script shell-inventory.txt line python-inventory.txt line.",
    "Repoint every caller Makefile wave.sh workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/website/mission-version-upload-repro.sh bash gate live.",
    "Makefile wave.sh workflows still invoke bash script.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and two deliberately broken trees.",
    "T-556 anti-vacuity: passing run alone is not evidence.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Port diff matches clean tree byte-for-byte.",
    "Port diff matches at least two deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
]
shipped_at = "8f055e80"
created_at = "2026-08-12T05:36:38Z"
completed_at = "2026-08-12T05:38:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "scripts/website/mission-version-upload-repro.sh",
    "xtask/src/gate_mission_version_upload_repro.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
