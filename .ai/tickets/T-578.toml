id = "T-578"
kind = "work"
title = "Wire T-280 durable rate limiter proven inert"
summary = "Wire T-280's durable rate limiter — it is proven but inert"
class = "docs"
status = "shipped"
order = 3422
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-280 built `app::durable_ratelimit::PgRateLimiter` (a one-statement refill-and-spend token bucket;
`ON CONFLICT DO UPDATE` takes the row lock so it is atomic across processes) and PROVED it with a
perturbation that isolates durability from throttling: an in-process bucket still refuses at the
limit, and fails only on 'still refused after a restart'.

**It is deliberately NOT WIRED.** Verified by wave 69's verifier: `PgRateLimiter` is unreferenced
from `state.rs`, `bin/api.rs` and `middleware/`; the live limiter is still the in-memory `governor`
`IpLimiter`; `rate_limit_buckets` is absent from the schema and created by no migration. It stopped
because the table belongs in `migrations/`, which was T-262's file that wave, and it refused to
self-provision DDL from the request path.

TO WIRE IT: (a) a migration creating the table -- the DDL is already a `const RATE_LIMIT_BUCKETS_DDL`
in `app.rs` specifically so the bytes the tests prove and the bytes the migration lands cannot
drift; (b) two lines of wiring, recorded in the module doc.

**AN OPERATOR TRADE, NOT A SLICE AGENT'S CALL:** it adds ONE DATABASE WRITE PER REQUEST. T-280's own
recommendation is to keep the in-memory `IpLimiter` as an L1 in front so only near-limit traffic
reaches Postgres. Get sign-off on the cost before wiring.

missing=0"""
priority = 2
main_goal = "Wire T-280 PgRateLimiter with migration and L1 IpLimiter."
context = [
    "Wave 69 verifier: PgRateLimiter unreferenced from API.",
    "Live limiter remains in-memory governor IpLimiter.",
    "rate_limit_buckets absent from schema; no migration.",
]
requirement = [
    "Add migration for RATE_LIMIT_BUCKETS_DDL const in app.rs.",
    "Wire PgRateLimiter per module doc; operator sign-off on per-request write cost.",
]
current_state = [
    "PgRateLimiter proven durable but deliberately not wired.",
    "T-262 wave stopped DDL self-provision from request path.",
]
approach = [
    "Keep IpLimiter as L1; Postgres only for near-limit traffic.",
    "Migration lands same DDL bytes tests prove in app.rs const.",
]
verify = [
    "PgRateLimiter referenced from state or middleware after wiring.",
    "rate_limit_buckets exists in schema via migration.",
]
citations = [
    "Wave 69 verifier PgRateLimiter",
    "T-280 durable token bucket",
    "IpLimiter L1 recommendation",
]
shipped_at = "bae658a3"
created_at = "2026-08-01T02:55:01Z"
completed_at = "2026-08-01T02:56:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/api/src/middleware/ratelimit.rs",
    "apps/website/api/src/app.rs",
    "apps/website/api/src/handlers/telemetry.rs",
    "apps/website/api/src/services",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
