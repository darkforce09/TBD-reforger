id = "T-181.3.2"
kind = "work"
title = "Pak extract uncompressed vanilla scripts"
summary = "enf extract pulls uncompressed pak scripts by real path; PakVfs zlib-to-deflate fallback."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "enf extract — pulls vanilla scripts BY REAL PATH from the pak FILE table (operator asked 'what about the MCP?' — the MCP's game_browse DOES list them; its game_read fails). Fixed PakVfs to fall back zlib->raw deflate. Result: 2,483/6,495 scripts extracted (all the UNCOMPRESSED ones); combined vanilla index now 2,099 files / 72,226 LOC / 1,659 declarations. BARRIER: the 4,012 compressed entries use a codec that is neither zlib nor deflate, with no method flag (all-zero method bytes). enfusion-mcp only calls inflateSync so it cannot read them either — that error is why vanilla source looked unavailable. Follow-up T-181.3.3: identify the codec (LZ4 block is the hypothesis) to unlock the gameplay layer."
parent = "T-181"
context = [
    "T-181.3 byte-carve reached only 610 plaintext scripts.",
    "MCP game_read failed on compressed pak blocks.",
]
requirement = [
    "Extract by real path from pak FILE table via enf extract.",
    "Fix PakVfs inflate fallback; record 4,012 unknown-codec barrier.",
]
current_state = ["Byte-carve reached 610 scripts; MCP game_read failed on compressed pak blocks."]
approach = ["enf extract by real pak path; PakVfs zlib-to-deflate fallback; index combined tree."]
verify = ["cargo run -q -p tbd-tools --bin enf -- extract."]
acceptance = ["Pak extract pulled 2483 uncompressed scripts; 4012 unknown-codec entries recorded as barrier."]
citations = [
    "Source: vanilla_carve_coverage.md — compression barrier.",
    "Source: d8fc6abc — enf extract.",
]
created_at = "2026-07-25T00:00:00Z"
completed_at = "2026-07-31T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-181 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "xtask"
