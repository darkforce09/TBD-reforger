id = "T-558"
kind = "work"
title = "T-534 test DB residue: hidden consumer, unpruned rust_it, shared migrate"
summary = "Test-harness residue after T-534: a DB consumer the Class-R cannot see, unpruned rust_it DBs, and one shared migrate DB"
class = "bug"
status = "shipped"
order = 3411
executor = "claude-code"
priority = 2
main_goal = "Close four T-534 test-harness gaps: invisible DB consumer, unpruned rust_it DBs, shared migrate URL, concurrent race."
context = ["Four findings from T-534; same shape it fixed, not regressions."]
requirement = [
    "Extend t542_no_raw_test_database_url_reads_outside_common to scan src/, not only tests/*.rs.",
    "Prune rust_it_<suite>_it databases left by make test-it; Makefile drops only rust_it.",
    "Give db_migrate.rs and models_fromrow.rs separate MIGRATE_TEST_DATABASE_URL databases.",
]
current_state = [
    "registry_import.rs:455 tokio test reads TEST_DATABASE_URL raw; Class-R scans tests/*.rs only.",
    "make test-it recreates 25 rust_it_<suite>_it DBs per run; nothing prunes them.",
    "Two migrate binaries share one database; deterministic only via sequential cargo and loose assertions.",
    "Concurrent cargo test on same base races on <base>_<suite>_it; gate lock serialises gate only.",
]
approach = [
    "Extend Class-R guard to src/ so registry_import consumer is visible.",
    "Add Makefile prune for derived rust_it names (gate path already handled by wave.sh).",
    "Split migrate test URLs like T-534 did elsewhere.",
]
verify = [
    "t542_no_raw_test_database_url_reads_outside_common catches src/ TEST_DATABASE_URL reads.",
    "make test-it no longer leaves unpruned rust_it_<suite>_it databases.",
]
citations = [
    "T-534 test isolation",
    "T-542 Class-R guard",
    "registry_import.rs:455",
]
shipped_at = "96c765da"
created_at = "2026-07-28T21:41:23Z"
completed_at = "2026-07-28T22:56:05Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/tests/db_migrate.rs",
    "apps/website/api/tests/models_fromrow.rs",
    "apps/website/api/tests/common/mod.rs",
    "apps/website/api/src/services/registry_import.rs",
    "Makefile",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
