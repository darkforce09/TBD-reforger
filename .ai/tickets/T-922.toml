id = "T-922"
kind = "work"
title = "Full-body reconstruction over shipped history"
summary = "Drain BODY_DEBT_PIN 962 to zero: reviewed batches fill every missing ready-tier body field on shipped work tickets from real sources, then the ready-tier rule widens to shipped corpus-wide and the pin is deleted."
class = "chore"
status = "ready"
order = 6020
spec = "docs/platform/t920_body_obligations.md"
plan = "docs/plans/t-922_plan.md"
executor = "claude-code"
notes = "Operator decision 2026-08-15: full-body round chosen over transition-time-only. Selection = shipped work with ANY of the six missing (broader than the T-921 title/goal/context arms). Final zeroing commit widens check_ready_tier_body to shipped and deletes BODY_DEBT_PIN + its ratchet test."
depends_on = ["T-921"]
main_goal = "Every shipped ticket in history carries all six ready-tier body fields, honestly reconstructed, so the body rule can bind corpus-wide with no exemptions."
context = [
    "T-919/T-921 drains zeroed the title, goal and wall pins but selected only on those arms.",
    "962 shipped work tickets still miss at least one of the six body fields.",
    "The recorded corpus-wide flip on shipped cannot turn on until this debt is zero.",
]
requirement = [
    "Fill every missing ready-tier field on shipped work tickets from real sources, cited.",
    "BODY_DEBT_PIN shrinks by the measured batch amount in the same commit, every batch.",
    "Zeroing commit widens the ready-tier check rule to shipped and deletes the pin.",
]
current_state = [
    "BODY_DEBT_PIN 962 landed with instrument body_is_debt and its store ratchet test.",
    "ops::ship already refuses body-incomplete future ships, so the debt cannot grow.",
]
approach = [
    "Batches of about 20 by id order, selection = any of the six fields empty.",
    "Reconstruct from spec docs, subject commits, diffs, SHIPPED_HISTORY.md; cite sources.",
    "Thin evidence yields thin honest fields; verify stays empty only when nothing was proven.",
]
verify = [
    "Per batch: body_debt_ratchet_pin green at the new value; check --strict OK.",
    "Corpus roundtrip N/N byte-identical; wave.lock byte-identical per batch.",
]
acceptance = [
    "BODY_DEBT_PIN reaches 0 across reviewed batches, shrinking in the same commit each time",
    "Reconstructed fields cite their sources; no invented claims; existing field content never overwritten",
    "Zeroing commit: check_ready_tier_body binds on shipped corpus-wide, pin + ratchet deleted, check --strict OK",
]
created_at = "2026-08-15T18:01:48Z"
owns = [
    ".ai/tickets",
    "crates/tbd-tickets",
    "xtask/src",
]

[scope]
domain = "repo"
layer = "tickets"
