id = "T-374"
kind = "work"
title = "Byte-level persist guard allows empty doc overwrite"
summary = "The persist guard is byte-level, so an empty document overwrites a populated record"
class = "docs"
status = "shipped"
order = 3190
executor = "claude-code"
notes = """
0 A B { } // 2, 41 5s == AN IN IT So TO an as if it no of so 'no 124 :12 ONE Old RUN THE WAS all are did end has it) let now one own put set src via 'IDB (the **an 0]** 0]`, 0xff 100k 10k, Also COST KEPT Read SHIP Same That `[0, also away blob boot byte call cite code cold core doc, down end: ends errs ever exit from full lose loss maps over pub, real root side site test than then tier trap true undo used were when with (100k **The **[0, 0x00. 466ms None, THREE `pub` blob' blob, bytes code: count edit. every exact fails false level needs never pair: path. probe step, still third twice wasm. which while whose with: worth would 'never FALSE. Option Proven T-380, backup banked called cause. check. client decode delete drives filed: fixing guard, later. later: length longer module parse, rather shaped single stated there: traded wanted waves. wired, writer yields 'record 145us). CHANGE, CONTROL REFUSED RESTORE absent' already armed), because browser compile design: emptied encodes failed' flagged header: in-file itself, mission nothing passed: promise re-arms record. return; shipped sibling tested, (T-386). POSITIVE SHIPPED. UNCALLED authored catches. codebase content' document findings garbage, header's module's natively negative nothing. re-armed recovers straight verbatim BEHAVIOUR MEASURED, MECHANISM Mitigated autosave. deferred: directly; displaced document. editCount extracted functions gestures, meta-only populated predicate prevented rejected, sse.rs:25 (:238-247) (:768-775) 2026-07-26 DELIBERATE `run_save` byte-level delete-set discovered documented overwrites propagates rejections wasm-gated `get_bytes` distinction documents), overwritten #[cfg(test)] (yrs_persist ContentProbe `Vec::new()` `load_state` consulted:** implemented. non-empty**, unreplayable 50%-truncated intentionally once-per-boot (console-only, Local::Empty), `has_snapshot` `is_cancelled` render-check's (classify_local `has_content()` `undoRestore()` empty/truncated never-populated `encode_state()` `live_editor_is` `snapshot_local` bytes.is_empty() has_snapshot:774, select-all+Delete snapshot_local:746, restore_snapshot:859 (pending.get_bytes)(); `__missionBackup.restore()` (mission_hydrate.rs:740-760) frontend/src/yrs_persist.rs:516-534 mission_hydrate::classify_local:344 `MissionDocCore::new().encode_state()` (crates/map-engine-core/src/doc/store.rs:1293-1303) `encode_state_as_update_v1(&StateVector::default())`,

--- migration_legacy (verbatim) ---



The guard that exists to prevent exactly this does not implement it.

frontend/src/yrs_persist.rs:516-534 `run_save` ends with:
    let bytes = (pending.get_bytes)();
    if bytes.is_empty() { return; }  // 'never overwrite a good record with an empty/truncated blob'

`bytes.is_empty()` is a BYTE test, not a CONTENT test. `encode_state()` is `encode_state_as_update_v1(&StateVector::default())`, which writes a var-int client count then the delete set — so **an empty document encodes to `[0, 0]`, two bytes, non-empty**, and sails straight past the guard. `get_bytes` only yields `Vec::new()` when the doc Option is None, which `is_cancelled` already catches. So the module header's stated promise at :12 is not implemented.

**The predicate already exists and is not consulted:** `has_content()` (crates/map-engine-core/src/doc/store.rs:1293-1303) has exactly ONE call site — mission_hydrate::classify_local:344 — and is never used on the write path. Use it.

Same byte-level test in the snapshot pair: `snapshot_local` (mission_hydrate.rs:740-760) captures a content-empty doc, and `has_snapshot` (:768-775) reports true for it — so `__missionBackup.restore()` can put an empty document over a good live doc, and the re-armed persist writes it 5s later. Mitigated (console-only, exact `live_editor_is` guard, and the displaced doc is banked so `undoRestore()` recovers it) but the same root cause.

Also worth fixing while in there: `get_raw` (:238-247) collapses 'record absent' and 'IDB read failed' into the same None, and `load_state` reports that as 'no local content' — a false negative that then drives the boot down the cold path.

== KEPT IN THE RUN 2026-07-26 while 41 sibling findings were deferred: can lose an authored mission — empty doc overwrites a populated record. ==

SHIPPED. `MissionDocCore::new().encode_state()` = **[0, 0]** — length 2, is_empty() FALSE. Proven twice against shipped code: natively via a probe whose src is the guard functions extracted verbatim (yrs_persist is wasm-gated so an in-file #[cfg(test)] would never compile — the trap sse.rs:25 already documents), and in the real browser wasm. Old guard WRITE, new guard reject. Also rejected, all of which the byte test passed: a meta-only 124 B content-empty doc, a 50%-truncated unreplayable blob, 0xff garbage, a single 0x00.

POSITIVE CONTROL — the loss itself, prevented end to end: 8 real click+Delete gestures, beforeSlots 8 -> afterSlots 0, editCount 0 -> 8 (the writer WAS armed), guard fired, stored record unchanged at 1069 bytes. Read from assertValue directly; it did not cite render-check's exit code (T-386).

IT REFUSED TO SHIP AN UNCALLED `pub` MECHANISM for T-380, citing this module's own T-338 header: T-221 shipped purge_owner as pub, tested, documented as wired, and nothing called it for two waves.

COST MEASURED, and it shaped the design: decode is 19us at 8 slots, 35ms at 10k, 466ms at 100k — too much for a writer that re-arms on every edit. So schedule_edit_persist passes an O(1) ContentProbe over the live core and only the once-per-boot arming path pays the decode; the [0,0] blob is rejected by an O(1) var-int tier with no decode at all (100k rejections in 145us).

ONE BEHAVIOUR CHANGE, flagged rather than discovered later: a DELIBERATE select-all+Delete now also fails the guard, because at the blob level an intentionally emptied document and a never-populated one are the same content-empty document. A full delete no longer propagates to the local autosave. That is the side the codebase already errs on (classify_local maps content-empty to Local::Empty), the delete is still an undo step, and the failure traded away is an authored mission overwritten with nothing. The distinction exists if ever wanted — a never-populated doc has no delete set — but that is a delete-set parse, not a length check.

mission_hydrate.rs needs THREE instances, not the two filed: snapshot_local:746, has_snapshot:774, and restore_snapshot:859 — the third would RESTORE a content-empty backup over the live document.
"""
priority = 0
main_goal = "Guard local persist against content-empty encoded blobs, not just byte-empty Vec."
context = [
    "The guard that exists to prevent exactly this does not implement it.",
    "`bytes.is_empty()` is a BYTE test, not a CONTENT test.",
]
requirement = [
    "Use has_content() on the write path — exists at store.rs:1293-1303 but only classify_local calls it.",
    "Fix snapshot_local, has_snapshot, and restore_snapshot in mission_hydrate.rs — three instances, not two.",
]
current_state = [
    "encode_state() on empty doc writes [0, 0] — two bytes, sails past is_empty() guard.",
    "snapshot_local captures content-empty doc; has_snapshot reports true; restore can overwrite good live doc.",
    "`get_raw` collapses absent record and IDB read failure into same None.",
]
approach = [
    "schedule_edit_persist passes O(1) ContentProbe; arming path pays decode; [0,0] rejected by O(1) var-int tier.",
    "Refused uncalled pub mechanism citing T-338/T-221 purge_owner precedent.",
]
verify = [
    "MissionDocCore::new().encode_state() = [0, 0]; old guard WRITE, new guard reject.",
    "8 click+Delete gestures: beforeSlots 8 -> afterSlots 0, guard fired, stored record unchanged at 1069 bytes.",
    "Decode cost: 19us at 8 slots, 35ms at 10k — too much for per-edit writer.",
]
acceptance = ["Content-empty [0,0] encode rejected on write; eight Delete gestures left stored record at 1069 bytes."]
citations = [
    "crates/map-engine-core has_content",
    "mission_hydrate.rs snapshot paths",
    "T-338 yrs_persist header",
    "T-221 purge_owner precedent",
    "T-386 assertValue readback",
    "commit 5908058ba blob_has_content",
]
shipped_at = "28e9fbb6"
created_at = "2026-07-26T12:17:26Z"
completed_at = "2026-07-26T12:30:25Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/frontend/src/yrs_persist.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["doc_store"]
