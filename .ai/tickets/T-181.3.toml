id = "T-181.3"
kind = "work"
title = "Vanilla byte-carve and enf-carve index"
summary = "enf carve extracts uncompressed vanilla scripts; derived index committed, blobs gitignored."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "enf carve + index vanilla + make enf-carve. PARTIAL BY NATURE: only a subset of vanilla script ships uncompressed. Got 610 files / 41,958 LOC / 1,106 declarations (882 SCR_*) — AI/behaviour-tree, camera, actions, UI components, WB plugins. SCR_BaseGameMode / SCR_PossessSpawnData / SCR_PossessSpawnRequestComponent / SCR_RespawnSystemComponent / ChimeraMenuBase are ABSENT as plaintext (compressed) — recorded as known-missing in docs/mod/vanilla_carve_coverage.md, not faked. Output gitignored (BI copyright); only the derived index is committed. Follow-up: inflate compressed pak blocks via PakVfs and re-carve."
parent = "T-181"
context = [
    "Factory oracle needed greppable vanilla reference before spawn slices.",
    "Only uncompressed pak script blocks yield plaintext via byte-carving.",
]
requirement = [
    "Ship enf carve, vanilla index, and make enf-carve over game install.",
    "Record compressed gameplay classes as known-missing, not faked.",
]
current_state = ["No greppable vanilla reference; spawn classes trapped in compressed pak blocks."]
approach = ["enf carve byte-scans paks; make enf-carve; record gaps in vanilla_carve_coverage.md."]
verify = ["make enf-carve; enf index vanilla."]
acceptance = ["Vanilla carve indexed 610 plaintext files; compressed spawn classes recorded known-missing, not faked."]
citations = [
    "Source: vanilla_carve_coverage.md — lane 1.",
    "Source: commit d8fc6abc — Enfusion factory.",
    "Source: vanilla_carve_coverage.md — legacy carve.",
]
created_at = "2026-07-25T00:00:00Z"
completed_at = "2026-07-31T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-181 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "docs"
