id = "T-876"
kind = "work"
title = "T-853 port: setup-mcp-game-root -> setup mcp-game-root"
summary = "T-853 port: setup-mcp-game-root -> setup mcp-game-root"
class = "chore"
status = "shipped"
order = 5570
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/setup-mcp-game-root.sh (27 lines) to `cargo xtask setup mcp-game-root` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). MCP game root symlink setup. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/setup-mcp-game-root.sh (27 lines) to `cargo xtask setup mcp-game-root` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). MCP game root symlink setup. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0
"""
main_goal = "Port setup-mcp-game-root.sh to cargo xtask setup mcp-game-root; delete script and repoint callers."
context = [
    "T-853 shell->xtask slice ports shell scripts to cargo xtask subcommands.",
    "scripts/mod/setup-mcp-game-root.sh is 27 lines; MCP game root symlink setup.",
    "Callers: Makefile scripts/platform/wave.sh scripts/mod/wave.sh .github/workflows.",
]
requirement = [
    "Port to cargo xtask subcommand; delete script and inventory lines.",
    "Repoint every caller Makefile platform wave mod wave GitHub workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "Bash script with shell and python inventory entries today.",
    "Makefile wave.sh and workflows invoke the shell script directly today.",
]
approach = [
    "Capture bash stdout stderr rc first; diff port on clean and broken trees.",
    "T-556 anti-vacuity: a passing run alone is not evidence.",
]
verify = [
    "ACCEPTANCE BYTE-FOR-BYTE: capture bash stdout stderr rc first.",
    "Diff port against bash on clean tree and two deliberately broken trees.",
    "A passing run alone is NOT evidence T-556 anti-vacuity.",
]
citations = [
    "T-853 shell->xtask waves",
    "t853_shell_to_xtask_waves.md",
    "T-556 anti-vacuity",
    "crates/tbd-gate",
]
shipped_at = "4ee84ad5"
created_at = "2026-08-12T07:34:26Z"
completed_at = "2026-08-12T07:38:02Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "scripts/mod/setup-mcp-game-root.sh",
    "xtask/src/gate_setup_mcp_game_root.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
