id = "T-536"
kind = "work"
title = "touch_changed still masks changed_rs porcelain failure as empty list"
summary = "touch_changed still masks changed_rs porcelain failure as empty list"
class = "feature"
status = "shipped"
order = 3376
executor = "claude-code"
priority = 2
shipped_at = "6d7694cd"
created_at = "2026-07-27T12:32:01Z"
completed_at = "2026-07-27T12:46:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
migration_legacy = [
    "FOUND by W50 adversarial verifier (MINOR-NIT) after T-492.",
    "",
    "fmt_changed/clippy_changed now propagate changed_rs rc. touch_changed still does `for f in $(changed_rs …)` and can return 0 with listed=0 when porcelain fails. Same mask class.",
    "",
    "Cure: `files=$(changed_rs) || return $?` (or equivalent) in touch_changed.",
]
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
