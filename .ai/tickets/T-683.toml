id = "T-683"
kind = "work"
title = "Default-override instrumentation: which mission defaults every author changes"
summary = "Default-override instrumentation: which mission defaults every author changes"
class = "chore"
status = "shipped"
order = 3945
executor = "claude-code"
notes = """
editor-UI program wave 5 (wave_plan.tsv row 105); filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md) — SHIPPED wave 105 (editor factory), 5ca91425 + sweep a7d91fca; verifier wave105.md

--- HEAD migration_legacy (verbatim) ---

The B7 item from framework_synthesis.md C.3/C.5 rank 9, and THE ONE RECOMMENDATION IN THE CORPUS NO ARMA FRAMEWORK CAN IMPLEMENT. Evidence: WOG's single most-used API across 171 shipped missions is the one that turns a WOG feature OFF — `wog3_no_auto_long_range_radio = true` appears in 74 of 171 missions (wog.md:603, :774-775, :853, :987), and wog.md:1078 draws the conclusion verbatim: "if 43% of missions disable your default, the default is wrong — so make such toggles visible mission settings, not magic globals." WOG could only learn that because an outside analyst machine-parsed 171 shipped PBOs; TBD OWNS ITS CORPUS IN A DATABASE, so "what fraction of missions override X" is a query rather than an argument. Scope: one read-only aggregation over `mission_versions.json_payload` reporting, per authored key, the fraction of missions whose latest version differs from the schema default, plus the value histogram. THE QUERY SHAPE ALREADY EXISTS AND IS PROVEN — `apps/website/api/src/handlers/missions.rs`:1563-1564 already runs `jsonb_typeof(v.json_payload -> 'editor' -> 'slots')` + `jsonb_array_length` over exactly that column, so this is a second query beside a working one, not new machinery. The defaults side is READ-ONLY: `packages/tbd-schema/schema/mission.schema.json` already declares a `default` on every `zoneRules` key and on the flow/settings keys, so this ticket reads the schema and MUST NOT widen it (a widening would flip the executor to workbench). Admin tier. NOTE THE ABSENT `depends_on`: this is the only row in the editor-UI block that touches no editor file, so the T-661 eden_chrome split does not gate it and its owns is disjoint from all 43 other rows.

missing=0
"""
main_goal = "Report per-key mission default override fractions and value histograms."
context = [
    "B7 framework_synthesis rank 9; corpus-owned override analytics not Arma-side.",
    "wog3_no_auto_long_range_radio true in 74 of 171 missions proves default wrong.",
    "Query shape proven at missions.rs:1563 jsonb over mission_versions.json_payload.",
]
requirement = [
    "Read-only aggregation: fraction overriding schema default per authored key.",
    "Value histogram per key; admin tier; must not widen mission.schema.json.",
    "Second query beside working slots jsonb_typeof pattern in missions.rs.",
]
current_state = [
    "mission.schema.json declares default on zoneRules and flow settings keys.",
    "No depends_on; owns disjoint from editor-UI block per wave 105 ship.",
    "SHIPPED wave 105 at a7d91fca; verifier wave105.md recorded pass.",
]
approach = [
    "Reuse missions.rs jsonb query patterns over latest mission_versions rows.",
    "Read schema defaults read-only; compare against json_payload authored values.",
]
verify = [
    "Admin endpoint returns override fraction and histogram per configured key.",
    "Schema defaults read-only; no mission.schema.json widening in this ticket.",
]
acceptance = [
    "Admin GET /api/v1/admin/mission-default-overrides returns per-key override fraction and value histogram.",
    "Aggregation reads schema defaults read-only beside the existing missions.rs jsonb query shape.",
    "Non-admin callers receive 403; mission.schema.json carried no widening in this slice.",
]
citations = [
    "framework_synthesis B7 rank nine",
    "wog.md forty-three percent disable",
    "missions.rs slots jsonb query",
]
shipped_at = "a7d91fca"
created_at = "2026-08-02T02:56:50Z"
completed_at = "2026-08-02T03:09:39Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/missions.rs",
    "apps/website/api/src/models/mission.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
