id = "T-472"
kind = "work"
title = "T-471 verify-t468 still greens echo/@true/#fake Makefile recipes"
summary = "T-471 verify-t468 still greens echo/@true/#fake Makefile recipes"
class = "feature"
status = "shipped"
order = 3311
executor = "claude-code"
notes = '''
--- HEAD migration_legacy (verbatim) ---

Residual from wave 27 adversarial BLOCKER. verify-t468 recipe pin uses `^\t.*(?:\$\(MAKE\)|make)\s+schema-validate\b` — matches `@echo "$(MAKE) schema-validate"`, `schema-validate-fake`, `@true # $(MAKE) schema-validate`.

Repro: hollow echo of make lines / -fake targets / comment-only → tripwire PASS.

Cure: require real recipe invocations (tab + optional @ + $(MAKE)|make + exact target, no echo, ignore # comments); RED→GREEN on verifier recipes.

missing=0'''
priority = 1
main_goal = "Harden verify-t468 Makefile recipe pin against echo/@true/#fake hollow bodies."
context = [
    "Residual from wave 27 adversarial BLOCKER.",
    "verify-t468 pin matches echo, schema-validate-fake, @true comment-only recipes.",
]
requirement = [
    "Require real tabbed recipe invocations: optional @, $(MAKE)|make, exact target.",
    "Ignore # comments; RED→GREEN on verifier perturbations.",
]
current_state = ["Repro: hollow echo, -fake targets, comment-only → tripwire PASS."]
approach = ["Cure: tighten regex to forbid echo/@true/#fake; require real schema-validate invoke."]
verify = ["Repro: @echo MAKE schema-validate, -fake, @true comment → tripwire PASS."]
citations = [
    "wave 27 adversarial BLOCKER",
    "verify-t468 recipe pin",
    "schema-validate Makefile recipe",
]
shipped_at = "1675be2d"
created_at = "2026-07-27T03:09:40Z"
completed_at = "2026-07-27T03:16:43Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["scripts/mod/verify-t468-ci-schema-parity.sh"]

[scope]
domain = "repo"
layer = "xtask"
