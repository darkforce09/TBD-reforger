id = "T-211"
kind = "work"
title = "Zone and play-area draw tool"
summary = "Zone and play-area draw tool"
class = "docs"
status = "shipped"
order = 2290
executor = "claude-code"
depends_on = ["T-241"]
main_goal = "Needs a doc root, mutators, a map tool and an Attributes panel for rules."
context = [
    "SHIPPED 2026-07-31. Editor lacked zone doc root; mod enforced zone union via TBD_ZoneRegistry since T-241.",
    "flatten read authored zones[] since T-201; store.rs had zero zone representation before this slice.",
]
requirement = ["Needs a doc root, mutators, a map tool and an Attributes panel for rules."]
current_state = [
    "No draw tool, no circle, no polygon anywhere in the editor, and the doc has no zone root at all (store.rs:51-65).",
    "The mod already implements union semantics, per-faction boundaries, strictest-wins and base_protection.",
]
approach = [
    "Added zones root map in store.rs with schema-verbatim zone rows; nine mutators and hydrate loader.",
    "Projected zones into payloadExtras for flatten consumer; compile.rs unchanged by design.",
]
verify = [
    "cargo test -p map-engine-core --features doc,mission.",
    "cargo clippy -p map-engine-core --features doc,mission,world.",
]
acceptance = ["Zone doc root and mutators shipped in store.rs; zones round-trip through compile and hydrate with geometry intact."]
citations = ["store.rs:51-65"]
shipped_at = "d3e9e341"
created_at = "2026-07-31T01:35:51Z"
completed_at = "2026-07-31T05:24:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = ["crates/map-engine-core/src/doc/store.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
