id = "T-160"
kind = "work"
title = "wgpu GpuTimer readback err-path (unmap / in_flight)"
summary = "Found during T-159.15.1: GpuTimer map_async err-path unmaps only on res.is_ok() but clears in_flight unconditionally — latent double-map when HUD/timer returns. Editor currently uses disable_frame_timing(). Fix in map-engine-render when re-enabling frame timing."
class = "bug"
status = "shipped"
order = 1575
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope. "
depends_on = ["T-159"]
main_goal = "Fix GpuTimer readback error path before HUD re-enables frame timing."
context = ["Found in T-159.15.1; map_async cleared in_flight unconditionally on error."]
requirement = ["ReadbackLane owns in_flight; native tests cover wasm32 error arm."]
current_state = ["Latent double-map; editor runs disable_frame_timing()."]
approach = ["map_engine_render::readback::ReadbackLane; shipped bce80aa1."]
verify = ["cargo test -p map-engine-render readback."]
acceptance = ["GpuTimer readback error path moved to ReadbackLane; in_flight guard survives map_async failure before HUD re-enables timing."]
citations = [
    "commit bce80aa1",
    "t159_15_2_camera_pan.md",
]
shipped_at = "bce80aa1"
created_at = "2026-08-01T02:44:10Z"
completed_at = "2026-08-01T02:44:10Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "repo"
layer = "docs"
