__keys = [
    "id",
    "title",
    "status",
    "executor",
    "program",
    "stream",
    "targets",
    "impact",
    "surfaces",
    "order",
    "summary",
    "notes",
]
__ord = 791
executor = "claude-code"
id = "T-818"
impact = [
    "ui",
    "mc-authoring",
]
notes = "eye-pass wave-202; pairs with T-819 in wave 210; depends on T-809/W203 stopgap | SHIPPED W210: vehicle Heading/Cargo/Crew moved into Attributes modal (same mutators); DockRight Placed strip deleted; Vehicles tab catalog-only. Orphan panel module → T-851."
order = 5000
program = "eden"
status = "shipped"
stream = "mission-creator"
summary = "OPERATOR EYE-PASS FINDING (wave-202 pass, 2026-08-10; direction operator-specified). The placed-vehicle crew/heading/cargo editor lives INLINE in the right dock's Placed strip — wrong home: the right dock is the get-assets surface. DIRECTION: the vehicle's double-click Attributes modal (vehicles already open Attributes on dblclick — T-647 ATTR-OPEN vehicles-only lineage) gains the vehicle editor as its content: Heading°, Add cargo, Crew dropdowns (Driver/Gunner/Commander/Cargo 1-N) — the existing dock editor's controls MOVED, not redesigned (the operator praised the menu itself). Then DELETE the right-dock Placed strip entirely (T-809/W203 lists placed vehicles in the left outliner and deliberately kept the strip's editor as a stopgap so crew editing never had a dead window — this ticket removes the stopgap). TRAPS: the modal is modal_stack-governed (consume z_class — the O-3 class); heading is a number_field (T-785 focused/draft, blur/Enter commit); crew dropdowns are plain selects; the T-649/T-788 multi-edit machinery stays untouched (vehicles are single-edit); removing the strip must not orphan the Add-cargo data path — same mutators, new call site; slot-crew assignment writes must keep their existing digest/undo shape. ACCEPTANCE: dblclick a placed vehicle -> Attributes opens with Heading/Cargo/Crew sections; assigning a slot as Driver via the modal produces the exact same document write the old strip did (digest parity); the right dock's Vehicles tab shows ONLY catalog tree + search (no Placed section anywhere); Esc/undo per the modal contract; T-819's hidden-crew figures reappear on unassign via this modal too."
surfaces = [
    "ATTR",
    "RIGHT",
]
targets = ["website"]
title = "Vehicle crew/heading/cargo editor moves into the vehicle Attributes modal; the right-dock Placed strip dies"
