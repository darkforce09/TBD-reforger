id = "T-841"
kind = "work"
title = "Type picker popover translucent; make opaque panel"
summary = "The Type picker popover is translucent — text blurs over the map; make it an opaque panel"
class = "bug"
status = "ready"
order = 8490
spec = "docs/specs/existing/t841_type_picker_opaque.md"
plan = "docs/plans/t-841_plan.md"
executor = "claude-code"
notes = """
operator eye-pass wave-206; small; pairs with T-827 methodology

--- HEAD migration_legacy (verbatim) ---

OPERATOR EYE-PASS FINDING (wave-206, 2026-08-10, screenshot): the T-810 TYPE picker dropdown renders on a translucent plate, so the option text blurs against whatever sits behind the modal — 'that should be a normal panel so it's easier to read.' FIX: the picker popover (attributes.rs type_picker) gets an OPAQUE panel background (the modal's solid surface token, not a /nn alpha or backdrop-blur glass); check the same for its search input plate. The T-827 lesson applies: readability is measured against the LIVE composite, not the token sheet — verify computed contrast of option text on the new plate. ACCEPTANCE: the popover's computed background is fully opaque; option text contrast >=4.5:1 live; no other picker behavior changes.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["OPERATOR EYE-PASS FINDING (wave-206, 2026-08-10, screenshot): the T-810 TYPE picker dropdown renders on a translucent plate, so the option text blurs against whatever sits behind the modal — 'that should be a normal panel so it's easier to read.' FIX: the picker popover (attributes.rs type_picker) gets an OPAQUE panel background (the modal's solid surface token, not a /nn alpha or backdrop-blur glass); check the same for its search input plate. The T-827 lesson applies: readability is measured against the LIVE composite, not the token sheet — verify computed contrast of option text on the new plate. ACCEPTANCE: the popover's computed background is fully opaque; option text contrast >=4.5:1 live; no other picker behavior changes."]

missing=0
"""
main_goal = "Opaque panel behind Type picker popover for readable contrast."
context = [
    "Operator eye-pass wave-206 screenshot: T-810 TYPE picker on translucent plate.",
    "Option text blurs against whatever sits behind the modal.",
    "T-827 lesson: readability measured against LIVE composite not token sheet.",
]
requirement = [
    "Picker popover type_picker gets OPAQUE panel background solid surface token.",
    "Not slash-nn alpha or backdrop-blur glass; check search input plate same.",
    "Verify computed contrast of option text on new plate live composite.",
]
current_state = [
    "Translucent plate makes option text blur over map behind modal.",
    "Operator: should be a normal panel so it is easier to read.",
    "Pairs with T-827 live-effective contrast methodology.",
]
approach = [
    "Modal solid surface token for opaque plate behind type picker popover.",
    "Live composite contrast verification not plate-calc token sheet alone.",
]
verify = [
    "Popover computed background is fully opaque.",
    "Option text contrast greater or equal 4.5:1 live.",
    "No other picker behavior changes.",
    "cargo xtask mk leptos-gates",
]
acceptance = [
    "The Type picker popover computed background is fully opaque and option text measures at least 4.5:1 live.",
    "No other picker behaviour changes.",
]
citations = [
    "wave-206 operator eye-pass",
    "T-810 TYPE picker dropdown",
    "T-827 live composite lesson",
    "modal solid surface token",
]
estimated = ["scope"]
owns = ["apps/website/frontend/src/editor/panels/attributes_modal.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["attr_panel"]
