__keys = [
    "id",
    "title",
    "status",
    "executor",
    "program",
    "stream",
    "targets",
    "impact",
    "surfaces",
    "order",
    "summary",
    "notes",
]
__ord = 814
executor = "claude-code"
id = "T-841"
impact = ["ui"]
notes = "operator eye-pass wave-206; small; pairs with T-827 methodology"
order = 5230
program = "eden"
status = "queued"
stream = "mission-creator"
summary = "OPERATOR EYE-PASS FINDING (wave-206, 2026-08-10, screenshot): the T-810 TYPE picker dropdown renders on a translucent plate, so the option text blurs against whatever sits behind the modal — 'that should be a normal panel so it's easier to read.' FIX: the picker popover (attributes.rs type_picker) gets an OPAQUE panel background (the modal's solid surface token, not a /nn alpha or backdrop-blur glass); check the same for its search input plate. The T-827 lesson applies: readability is measured against the LIVE composite, not the token sheet — verify computed contrast of option text on the new plate. ACCEPTANCE: the popover's computed background is fully opaque; option text contrast >=4.5:1 live; no other picker behavior changes."
surfaces = ["ATTR"]
targets = ["website"]
title = "The Type picker popover is translucent — text blurs over the map; make it an opaque panel"
