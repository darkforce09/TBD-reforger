id = "T-884"
kind = "work"
title = "T-853 port: shared DB plumbing - port FIRST"
summary = "T-853 port: shared DB plumbing - port FIRST"
class = "docs"
status = "shipped"
order = 5650
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/deploy/lib/db-common.sh to `cargo xtask deploy db (lib)` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 398 lines sourced by all three backup/restore scripts. Same propagation argument as gate-grep: port the library before its callers or three ports each invent their own dump-verification. Sources gate-grep.sh (T-880 blocker). ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/deploy/lib/db-common.sh to `cargo xtask deploy db (lib)` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 398 lines sourced by all three backup/restore scripts. Same propagation argument as gate-grep: port the library before its callers or three ports each invent their own dump-verification. Sources gate-grep.sh (T-880 blocker). ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md"]

missing=0"""
main_goal = "Port db-common.sh to cargo xtask deploy db lib first."
context = [
    "T-853 shell->xtask slice; 398 lines sourced by all three backup restore scripts.",
    "Port library before callers or three ports invent their own dump-verification.",
    "Sources gate-grep.sh; T-880 blocker same propagation argument as gate-grep.",
]
requirement = [
    "Delete script(s) shell-inventory.txt line(s) python-inventory.txt line(s).",
    "Port to cargo xtask deploy db lib; repoint every caller.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/deploy/lib/db-common.sh bash library live today.",
    "All three backup restore scripts source db-common.sh today.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
acceptance = [
    "Deleted scripts/deploy/lib/db-common.sh; deploy db lib xtask module replaced shared DB plumbing.",
    "Shell inventory line removed; backup restore and drill callers repointed off bash library.",
    "Port matched bash stdout stderr and rc on clean tree and deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-880 gate-grep blocker",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "a82d0c96 db-common port",
]
shipped_at = "a82d0c96"
created_at = "2026-08-12T09:29:51Z"
completed_at = "2026-08-12T09:31:41Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/deploy/lib/db-common.sh"]

[scope]
domain = "repo"
layer = "xtask"
