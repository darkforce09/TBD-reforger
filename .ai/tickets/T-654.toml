id = "T-654"
kind = "work"
title = "Conditional inclusion: variant-gated document subtrees"
summary = "Conditional inclusion: variant-gated document subtrees"
class = "bug"
status = "queued"
order = 4610
executor = "claude-code"
notes = "not factory-dispatchable; no owns, no wave row T-853 2026-08-12: the prepped slice worktree for this ticket was DROPPED, not landed. It carried one unmerged commit from 2026-08-02 that had sat unlanded for 10 days and was blocking the wave queue at 150. The work was NOT reviewed and NOT merged; the ticket stays queued so it can be redone. The commit is recoverable: `git checkout -b redo/T-654 salvage/t853-dropped/T-654` (tagged before the branch was deleted). Note T-674 and T-675 pointed at the SAME commit 113108a1, so whichever is redone first should check whether it subsumes the other."
main_goal = "Ship Enfusion variant-predicate reader for conditional subtree inclusion."
context = [
    "Optional variant predicate on entity subtree; emit only when variant selected.",
    "FNF v3 Eden layers abandoned at v4; salvage conditional inclusion not layers.",
    "Schema widening split to T-706; this ticket Enfusion reader plus editor UI.",
    "T-853 2026-08-12 dropped worktree unmerged ten days; commit recoverable salvage tag.",
]
requirement = [
    "Implement variant predicate reader in TBD_MissionLoader.c without schema edits.",
    "Serve mode presets day-night player-count bands base-plus-delta variants.",
    "Does not touch packages/tbd-schema; T-706 owns widening pass.",
]
current_state = [
    "Promoted idea to queued 2026-08-02; holds wave row runtime half T-706.",
    "gap_analysis LAYER-CREATE-001 graded tbd_only should read partial.",
    "Executor corrected workbench to claude-code; .c routes Fable per model-routing.",
]
approach = ["Redo from salvage redo/T-654 branch; check T-674 T-675 same commit overlap."]
verify = ["MissionLoader emits subtree only when selected variant predicate matches."]
citations = [
    "T-706 schema widening split",
    "FNF v4 mode architecture",
    "salvage redo T-654 branch",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionLoader.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
