id = "T-598"
kind = "work"
title = "RCON toast wrong after T-595 delivery"
summary = "T-579 revisited: the RCON toast now lies in the opposite direction"
class = "audit"
status = "shipped"
order = 3471
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-579 was filed when `send_rcon` always answered 503 and the SPA's success path was unreachable
dead code. **T-595 made it reachable again**, and the same string is now wrong the other way.

`apps/website/frontend/src/server_control.rs:116` `rcon_accepted_message` reads
    "RCON accepted action={} (audit queued; transport pending T-269)"
The transport is no longer pending -- T-289 shipped the host agent and T-595 shipped the client. A
202 now means the command was **delivered to a host agent that confirmed the unit's state**, which is
a materially different claim from "queued, transport pending".

**IT IS PINNED, SO THE STRING AND THE PIN MOVE TOGETHER**: a Class-R at `server_control.rs:788`
asserts `SRC.contains("transport pending T-269")`. Change one without the other and the slice goes
red for the wrong reason.

The 202 body now carries `accepted`, `action`, plus new `delivered` / `state` / `detail`. The existing
`RconAccepted` DTO deserializes unchanged, but **the UI should surface `state`** -- that field is the
whole point of T-289's design (the host is documented as exiting 0 over a dead server, so the agent
re-reads `LoadState`/`ActiveState` and returns what it saw).

Also still open from T-579, unchanged: `event_manager.rs:985` still promises that deleting an event
removes its missions' ORBATs and registrations, while `handlers/events.rs:1380` soft-deletes only.

**SEQUENCING:** two operator steps must happen before any of this is exercisable end to end -- set
`Environment=GAME_AGENT_SOCKET=%t/tbd-reforger-agent.sock` in the API's systemd unit, and run
`TBD_INSTALL_AGENT=1` once. Both mutate the live host; no agent has run them.

missing=0"""
priority = 2
main_goal = "Update RCON toast and pin for delivered state, not queued transport."
context = [
    "T-579 filed when send_rcon always 503; success path dead.",
    "T-595 reachable again; T-289 shipped host agent transport.",
]
requirement = [
    "Update rcon_accepted_message at server_control.rs:116.",
    "Move Class-R pin at server_control.rs:788 with string.",
    "Surface delivered/state/detail fields in UI.",
]
current_state = [
    "Toast says transport pending T-269; 202 means host delivered.",
    "Class-R asserts transport pending T-269 string.",
    "event_manager.rs:985 promises ORBAT removal; events.rs soft-deletes.",
]
approach = [
    "Change rcon_accepted_message to delivered semantics.",
    "Update Class-R pin alongside string change.",
    "Surface state field from RconAccepted response.",
]
verify = [
    "Class-R at server_control.rs:788 passes with new message.",
    "t270 tests green after string and pin update.",
]
acceptance = [
    "rcon_accepted_message branched on delivered and state instead of claiming transport pending T-269 after T-595 shipped.",
    "RconAccepted required delivered, state, and detail fields without serde defaults so missing agent fields failed parse.",
    "post_rcon success toast and Class-R pin gated on rcon_reports_success, not merely a 2xx response code.",
]
citations = [
    "T-579 RCON toast audit",
    "T-289 host agent transport",
    "server_control.rs:116 message pin",
]
shipped_at = "2d812b01"
created_at = "2026-07-31T16:06:01Z"
completed_at = "2026-07-31T17:07:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/server_control.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
