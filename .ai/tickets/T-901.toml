id = "T-901"
kind = "work"
title = "T-853 Phase 7a: gate CI run blocks"
summary = "T-853 Phase 7a: gate the CI run: blocks before bash relocates there"
class = "docs"
status = "shipped"
order = 5810
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
main_goal = "Add cargo xtask verify ci-shell gating the 39 workflow run blocks."
context = [
    "Makefile gone; remaining hiding place is 39 run: blocks in .github/workflows/.",
    "Bash does not need a .sh file to survive.",
    "Ban is on logic no type checker and no local replay can see.",
]
requirement = [
    "Add cargo xtask verify ci-shell; parse YAML with serde_yaml serde_norway.",
    "Each run: passes iff cargo xtask args or SHORT pre-cargo allowlist.",
    "FAIL on if/for/while/case && || ; | backticks $( redirection set - heredocs.",
]
current_state = [
    "39 run: blocks in .github/workflows/ may contain bash logic today.",
    "Existing multi-line run: blocks expected to fail; port them.",
    "shell: key naming bash/sh/python or run: with uses: must fail.",
]
approach = [
    "Pin uses: allowlist so ban cannot route around composite action.",
    "Record rationale in module doc: not bash language ban.",
    "Expect multi-line run: blocks to fail; port them that is the point.",
]
verify = [
    "ACCEPTANCE: prove change BITES; perturb subject watch it go red.",
    "A green run alone is not evidence T-556 anti-vacuity.",
    "cargo xtask platform wave gate PASS; sees cross-crate breakage.",
]
citations = [
    "T-556 anti-vacuity",
    "serde_yaml serde_norway YAML parse",
    "verify_ci_shell.rs module",
    "t853_shell_to_xtask_waves.md",
]
shipped_at = "f3dc576e"
created_at = "2026-08-12T23:54:37Z"
completed_at = "2026-08-14T07:29:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = [
    "xtask/src/verify_ci_shell.rs",
    "xtask/src/main.rs",
    "xtask/Cargo.toml",
    "Cargo.lock",
    "xtask/src/mk_ci_tasks.rs",
    "xtask/src/wave/gate.rs",
    ".github/workflows",
    "xtask/src/gate_mod_compile.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "ci"
