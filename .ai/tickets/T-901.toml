id = "T-901"
kind = "work"
title = "T-853 Phase 7a: gate CI run blocks"
summary = "T-853 Phase 7a: gate the CI run: blocks before bash relocates there"
class = "docs"
status = "shipped"
order = 5810
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Bash does not need a .sh file to survive — with the Makefile gone, the remaining hiding place is the 39 `run:` blocks in .github/workflows/. Add `cargo xtask verify ci-shell`. Parse the YAML PROPERLY (serde_yaml/serde_norway) — do not regex YAML. Each `run:` passes iff every logical line is `cargo xtask <args>` or one of a SHORT in-Rust allowlist of genuine pre-cargo setup (rustup target/component add, pinned cargo install, git lfs pull, docker/podman compose). FAIL on: if/for/while/case, &&, ||, ;, |, backticks, $(, redirection, set -, >3 logical lines, heredocs, a `shell:` key naming bash/sh/python, or a `run:` on a step that also has `uses:`. Pin the `uses:` allowlist too so the ban cannot be routed around via a composite action. Rationale to record in the module doc: the ban is not on bash the language, it is on logic that no type checker and no local replay can see. Expect existing multi-line run: blocks to fail — port them, that is the point. ACCEPTANCE: prove the change BITES — perturb the new subject and watch it go red — not merely that it passes. A green run alone is not evidence (T-556). Finish with a full `cargo xtask platform wave gate` PASS: it is the only check that sees cross-crate breakage, and it has caught defects a green `cargo test -p xtask` could not. Spec: docs/platform/t853_shell_to_xtask_waves.md

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["Bash does not need a .sh file to survive — with the Makefile gone, the remaining hiding place is the 39 `run:` blocks in .github/workflows/. Add `cargo xtask verify ci-shell`. Parse the YAML PROPERLY (serde_yaml/serde_norway) — do not regex YAML. Each `run:` passes iff every logical line is `cargo xtask <args>` or one of a SHORT in-Rust allowlist of genuine pre-cargo setup (rustup target/component add, pinned cargo install, git lfs pull, docker/podman compose). FAIL on: if/for/while/case, &&, ||, ;, |, backticks, $(, redirection, set -, >3 logical lines, heredocs, a `shell:` key naming bash/sh/python, or a `run:` on a step that also has `uses:`. Pin the `uses:` allowlist too so the ban cannot be routed around via a composite action. Rationale to record in the module doc: the ban is not on bash the language, it is on logic that no type checker and no local replay can see. Expect existing multi-line run: blocks to fail — port them, that is the point. ACCEPTANCE: prove the change BITES — perturb the new subject and watch it go red — not merely that it passes. A green run alone is not evidence (T-556). Finish with a full `cargo xtask platform wave gate` PASS: it is the only check that sees cross-crate breakage, and it has caught defects a green `cargo test -p xtask` could not. Spec: docs/platform/t853_shell_to_xtask_waves.md"]

missing=0"""
main_goal = "Add cargo xtask verify ci-shell gating the 39 workflow run blocks."
context = [
    "Makefile gone; remaining hiding place is 39 run: blocks in .github/workflows/.",
    "Bash does not need a .sh file to survive.",
    "Ban is on logic no type checker and no local replay can see.",
]
requirement = [
    "Add cargo xtask verify ci-shell; parse YAML with serde_yaml serde_norway.",
    "Each run: passes iff cargo xtask args or SHORT pre-cargo allowlist.",
    "FAIL on if/for/while/case && || ; | backticks $( redirection set - heredocs.",
]
current_state = [
    "39 run: blocks in .github/workflows/ may contain bash logic today.",
    "Existing multi-line run: blocks expected to fail; port them.",
    "shell: key naming bash/sh/python or run: with uses: must fail.",
]
approach = [
    "Pin uses: allowlist so ban cannot route around composite action.",
    "Record rationale in module doc: not bash language ban.",
    "Expect multi-line run: blocks to fail; port them that is the point.",
]
verify = [
    "ACCEPTANCE: prove change BITES; perturb subject watch it go red.",
    "A green run alone is not evidence T-556 anti-vacuity.",
    "cargo xtask platform wave gate PASS; sees cross-crate breakage.",
]
citations = [
    "T-556 anti-vacuity",
    "serde_yaml serde_norway YAML parse",
    "verify_ci_shell.rs module",
    "t853_shell_to_xtask_waves.md",
]
shipped_at = "f3dc576e"
created_at = "2026-08-12T23:54:37Z"
completed_at = "2026-08-14T07:29:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
owns = [
    "xtask/src/verify_ci_shell.rs",
    "xtask/src/main.rs",
    "xtask/Cargo.toml",
    "Cargo.lock",
    "xtask/src/mk_ci_tasks.rs",
    "xtask/src/wave/gate.rs",
    ".github/workflows",
    "xtask/src/gate_mod_compile.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "ci"
