id = "T-842"
kind = "work"
title = "World glyph clamp-not-wrap breaks tree prop text rotation"
summary = "The clamp-not-wrap heading defect is live on the world lanes too (trees/props/text)"
class = "bug"
status = "shipped"
order = 5240
executor = "claude-code"
notes = """
wave-207 completion C; same family as T-832; one-line fix + sweep test; small

--- HEAD migration_legacy (verbatim) ---

WAVE-207 COMPLETION FINDING (third instance of the T-832 family, found while fixing the second). crates/map-engine-core/src/world/glyph_math.rs:105 still does the clamp-instead-of-wrap that made half the compass draw due south, and world/residency.rs:1364 feeds it deck_angle_for_rotation_deg(rotation) = -rotation — so ANY world object (tree/prop/text) authored with a rotation in 181..359 renders at due south. Same one-line shape as the fix already landed in slots_gpu.rs (wave 207, commit 0bc55dc5): fold the angle into (-180,180] before encoding; a clamp is correct for a magnitude and wrong for an angle. The slots_gpu test `yaw_encoders_agree` now DOCUMENTS this divergence rather than hiding it — update it when this lands so the two encoders agree again. ACCEPTANCE: a world object at rotation 270 renders visibly different from one at 180 (measured tip/extent bearing); the full 0..360 sweep is monotonic with no plateau (the plateau IS the defect); yaw_encoders_agree re-pins full-domain byte parity between the two encoders.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["WAVE-207 COMPLETION FINDING (third instance of the T-832 family, found while fixing the second). crates/map-engine-core/src/world/glyph_math.rs:105 still does the clamp-instead-of-wrap that made half the compass draw due south, and world/residency.rs:1364 feeds it deck_angle_for_rotation_deg(rotation) = -rotation — so ANY world object (tree/prop/text) authored with a rotation in 181..359 renders at due south. Same one-line shape as the fix already landed in slots_gpu.rs (wave 207, commit 0bc55dc5): fold the angle into (-180,180] before encoding; a clamp is correct for a magnitude and wrong for an angle. The slots_gpu test `yaw_encoders_agree` now DOCUMENTS this divergence rather than hiding it — update it when this lands so the two encoders agree again. ACCEPTANCE: a world object at rotation 270 renders visibly different from one at 180 (measured tip/extent bearing); the full 0..360 sweep is monotonic with no plateau (the plateau IS the defect); yaw_encoders_agree re-pins full-domain byte parity between the two encoders."]

missing=0
"""
main_goal = "Fix world glyph_math clamp-not-wrap so rotations 181–359 render correctly."
context = [
    "Wave-207 completion finding third T-832 family instance.",
    "glyph_math.rs 105 clamp-instead-of-wrap; residency.rs 1364 deck_angle_for_rotation_deg.",
    "World objects trees props text rotation 181..359 render due south.",
]
requirement = [
    "Fold angle into (-180,180] before encoding same slots_gpu fix shape.",
    "Update yaw_encoders_agree so two encoders agree again.",
]
current_state = [
    "Clamp correct for magnitude wrong for angle.",
    "slots_gpu fix landed wave 207 commit 0bc55dc5.",
    "yaw_encoders_agree documents divergence not hiding it.",
]
approach = ["One-line shape same as slots_gpu.rs wave 207 fix."]
verify = [
    "Rotation 270 visibly different from 180 measured tip extent bearing.",
    "Full 0..360 sweep monotonic no plateau.",
    "yaw_encoders_agree full-domain byte parity between encoders.",
]
acceptance = [
    "World glyph yaw wraps into (-180,180]; rotation 270 encoding differs from 180.",
    "Full 0..360 sweep is monotonic with no south plateau.",
    "yaw_encoders_agree pins full-domain byte parity between world and slots encoders.",
]
citations = [
    "wave-207 completion C T-832",
    "glyph_math.rs 105 clamp",
    "residency.rs 1364 deck_angle",
    "slots_gpu 0bc55dc5 fix shape",
    "yaw_encoders_agree divergence",
]
shipped_at = "eb3fc965b"
created_at = "2026-08-19T17:00:00Z"
completed_at = "2026-08-19T17:31:17Z"
estimated = [
    "scope",
    "tokens",
]
owns = ["crates/map-engine-core/src/world/glyph_math.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
