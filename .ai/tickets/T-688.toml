id = "T-688"
kind = "work"
title = "Aggregated settings view with diff-from-default filter"
summary = "Aggregated settings view: every authored setting in one list, with diff-from-default"
class = "bug"
status = "shipped"
order = 4165
executor = "claude-code"
notes = "editor-UI program wave 15 (wave_plan.tsv row 115); filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md) — SHIPPED wave 115 (editor factory), 54d5abd6; verifier wave115.md. Read-only aggregation over the document with a diff-from-default filter, defaults SCHEMA-SOURCED (the sole value-carrying constructor reads a schema node; a pin bans FLOW_DEFAULT_* by name). Found that the ticket's 'cheap by construction' premise is much weaker than stated: ZERO of the 7 mission-level keys declare a schema default and 12 of 23 zoneRules properties do not either — those render honestly as 'no default declared' rather than inventing one. ⚠️ The click-through-to-owner constraint is effectively UNDELIVERED — see T-754. The slice also surfaced T-753, a live cross-crate drift beneath its own subject."
depends_on = [
    "T-661",
    "T-668",
]
main_goal = "Read-only aggregated settings list with diff-from-default filter; schema-sourced defaults."
context = [
    "U4 framework_synthesis ranked four; prevents typed-attribute scatter regression.",
    "FNF v4 config.sqf became 22 modules; no show-all-settings view.",
    "Shipped wave 115; click-through constraint undelivered see T-754.",
]
requirement = [
    "Read-only list every authored setting regardless of owning entity.",
    "Each row: key owning entity authored value and schema default.",
    "Diff-from-default filter; not a second editing surface.",
    "Rows must click through to owning entity per wog lesson.",
]
current_state = [
    "MissionSettingsDialog holds terrain time weather flow JIP; zone.rules on entities.",
    "Zero of seven mission keys declare schema default; twelve zoneRules lack defaults.",
    "Depends T-661 and T-668 for finished state vocabulary.",
]
approach = [
    "Schema declares default on keys; comparison needs no second source.",
    "Honest no-default-declared for keys without schema default pin.",
]
verify = [
    "Aggregation lists authored settings with schema default or honest absent.",
    "Diff-from-default filter excludes unmodified keys from filtered view.",
]
citations = [
    "framework_synthesis U4 aggregation",
    "fnf_v4 scattered settings defect",
    "T-754 click-through debt",
]
shipped_at = "fbc2fa59"
created_at = "2026-08-07T04:32:34Z"
completed_at = "2026-08-07T04:35:28Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_settings.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["env_settings"]
