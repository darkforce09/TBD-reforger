id = "T-920.2"
kind = "work"
title = "Board: main_goal on top, viewer beside detail"
summary = "main_goal renders under the title in the detail header; body sections start at context. The markdown viewer becomes a third column beside the detail panel, Back collapses it, width persisted. Card tooltip shows main_goal."
class = "feature"
status = "queued"
order = 6002
spec = "docs/platform/t920_body_obligations.md"
executor = "claude-code"
notes = "Depends on the T-920.1 type rename (main_goal field). Layout model pinned by test like the T-918.3 section order."
depends_on = ["T-920.1"]
parent = "T-920"
user_story = "The operator reads a ticket's main goal first and its plan beside it, never one over the other."
acceptance = [
    "Viewer opens beside the detail panel with both visible; Back collapses the column; width persists",
    "main_goal renders under the title; body sections start at context (layout model test-pinned)",
    "git status --porcelain unchanged after a full session",
]
created_at = "2026-08-15T03:17:45Z"
owns = ["apps/ticketboard"]

[scope]
domain = "repo"
layer = "tickets"
