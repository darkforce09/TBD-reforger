id = "T-701"
kind = "work"
title = "Editor-only visibility override — hide without deleting"
summary = "3den E9. An editor-local hidden flag that never compiles, so a maker can declutter a dense area while authoring. Distinct from T-665 layer visibility: that is per-layer, this is per-entity. Eden Layer attributes carry Enable Visibility for exactly this."
class = "feature"
status = "shipped"
order = 4550
executor = "claude-code"
notes = " — SHIPPED wave 120 (editor factory), a16e9188; verifier wave120.md"
context = [
    "3den E9 editor-only visibility; distinct from T-665 per-layer hide.",
    "editorHidden never compiles; Eden Enable Visibility is editor-only.",
]
requirement = [
    "Per-entity editorHidden boolean on slot rows; omit when false.",
    "materialize() drops editorHidden slots before SoA reaches engine.",
    "Wire schemas omit editorHidden; hide and batch are one txn one undo.",
]
current_state = ["Shipped wave 120 at 27b8cbc7; eight store.rs tests added."]
approach = ["store.rs flags plus editor_ops materialize filter; not renderer-side."]
verify = [
    "materialize() sole enforcement site; editor_hidden tests red without filter.",
    "editorHidden absent from mission and editor-payload schemas.",
]
acceptance = [
    "editorHidden boolean on slot rows filters at materialize and never reaches compiled mission wire.",
    "Batch hide and show_all each cost one undo step via set_slots_editor_hidden and clear_all_editor_hidden.",
    "Wire schemas omit editorHidden; hide and batch hide share one transaction per editor_ops wrapper.",
]
citations = [
    "eden attributes Enable Visibility",
    "wave120.md T-701 verified",
    "T-665 per-layer visibility",
]
shipped_at = "27b8cbc7"
created_at = "2026-08-02T10:39:36Z"
completed_at = "2026-08-02T10:42:47Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "crates/map-engine-core/src/doc/store.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["ops_undo"]
