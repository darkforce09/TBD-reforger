id = "T-562"
kind = "work"
title = "T-560 Class-R hollow — string COALESCE decoy still passes"
summary = "T-560 Class-R still hollow — string/format! SET arma_id = COALESCE decoy"
class = "chore"
status = "shipped"
order = 3415
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W64 adversarial verifier (DIRTY MAJOR) after T-560.

Comment strip works; contains("SET arma_id = COALESCE(arma_id") keeps string literals.
Delete live UPDATE + leave needle in let _decoy = "…" or format!("…") → PASS while INSERT stays NULL.
Claimed // + bind attacks still RED.

Repro: remove COALESCE UPDATE; add string/format! decoy with the needle; Class-R green; concurrent cold race can return.

missing=0"""
priority = 2
main_goal = "Harden dev-login Class-R against string and format! COALESCE decoys."
context = ["W64 adversarial verifier DIRTY MAJOR after T-560."]
requirement = ["contains needle must fail when only string or format! literal carries SET arma_id = COALESCE."]
current_state = ["Comment strip works; string/format! decoy with needle passes while INSERT stays NULL."]
approach = ["Reject string literal and format! decoys; keep // and bind attacks red."]
verify = ["Class-R red with UPDATE removed and string or format! COALESCE decoy present."]
acceptance = [
    "dev-login Class-R required SET arma_id = COALESCE inside direct sqlx::query SQL payloads only.",
    "String and format! COALESCE decoys no longer satisfied the dev-login prime pin.",
]
citations = [
    "W64 verifier T-560",
    "SET arma_id COALESCE",
]
shipped_at = "612d39b5"
created_at = "2026-07-28T21:59:50Z"
completed_at = "2026-07-28T22:07:51Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/api/tests/common/mod.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
