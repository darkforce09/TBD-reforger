id = "T-1216"
kind = "work"
title = "Document the guest role in the dev-login docstring"
summary = "developer_login.rs:65 documents role=admin|mission_maker|leader|enlisted, but dev_login accepts guest too (:77-79); the docstring omits it."
class = "chore"
status = "idea"
notes = "apps/website/api_v2/src/identity_and_access/handlers/developer_login.rs:65 documents the handler as 'GET /api/v1/auth/dev-login?role=admin|mission_maker|leader|enlisted', while the role match at :77-79 accepts guest, enlisted, leader, mission_maker and admin and falls back to admin for anything else. CLAUDE.md section 3 already lists role=guest|enlisted|leader|mission_maker|admin and the admin fallback, so only the docstring is out of date: add guest to it and state the admin fallback. Source: Phase 6 documentation verifier."
created_at = "2026-09-26T05:59:59Z"

[scope]
domain = "repo"
layer = "docs"
