id = "T-515"
kind = "work"
title = "Cold gate db_migrate hollow for SQL-only claim migrations"
summary = "Cold gate db_migrate hollow for SQL-only claim migrations"
class = "chore"
status = "shipped"
order = 3355
executor = "claude-code"
priority = 1
shipped_at = "94537521"
created_at = "2026-07-27T10:06:52Z"
completed_at = "2026-07-27T10:47:23Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
migration_legacy = ["Wave 44 residual (CLEAN after T-513/514; MINOR from verifier). db_migrate in wave.sh only checks schema/object counts. A SQL-only claim migration like T-335 0016 can drop the claim UPDATE yet keep REFRESH and still gate-green when claimable orphans are 0. Mitigated by synthetic non-vacuity in the slice report, not by the gate. Cure: gate asserts migration body contains the claim UPDATE (Class-R on 0016) and/or runs a planted-orphan migrate probe."]
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
