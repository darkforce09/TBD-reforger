id = "T-898"
kind = "work"
title = "file-length gate scans zero legacy files"
summary = "verify file-length scans ZERO files — SIZE-1/SIZE-3 is enforced by nothing"
class = "chore"
status = "shipped"
order = 5780
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
main_goal = "Document that verify file-length scans zero files; SIZE-1 and SIZE-3 enforce nothing."
context = [
    "Found 2026-08-12 by T-896 slice porting Makefile CI lane.",
    "cargo xtask verify file-length walks ONLY apps/website .go/.ts/.tsx.",
    "After T-145 Go->Rust and T-159 React->Leptos cutovers ZERO such files remain.",
]
requirement = [
    "CURE: extend walk to Rust tree xtask/ tools/ crates/ apps/website/*/src .rs.",
    "MEASURE FIRST: turns main RED on schema_gates.rs cmds.rs smokes.rs main.rs.",
    "EITHER split those files OR allowlist entry with real reason and expiry.",
]
current_state = [
    "find apps/website -name '*.go' -o '*.ts' -o '*.tsx' | wc -l returns 0.",
    "Gate printed 0 warning(s) 0 violation(s) over EMPTY SET entire Rust era.",
    "SIZE-1 >600 warn and SIZE-3 >1000 fail enforced by nothing; gate was theatre.",
]
approach = [
    "Same defect class as T-853: success over input never examined.",
    "Line 89 .unwrap_or(0) fail-open in miniature; read failure swallowed.",
    "Do not extend walk and silence it; reproduces defect with extra steps.",
]
verify = [
    "Measured: find apps/website legacy extensions returns 0 files.",
    "Pre-existing oversize: schema_gates.rs 4044 cmds.rs 1330 smokes.rs 3852.",
    "main.rs grew past 1000 during Phase 3.",
]
citations = [
    "T-896 Makefile CI lane port",
    "T-145 Go->Rust cutover",
    "T-159 React->Leptos cutover",
    "node_free.rs:77 file-length walk",
    "node_free.rs:89 unwrap_or fail-open",
]
created_at = "2026-08-12T00:00:00Z"
completed_at = "2026-08-12T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at id-interpolated between T-897 and T-899 (midpoint, day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "xtask"
