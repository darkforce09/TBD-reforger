id = "T-792"
kind = "work"
title = "Esc cancels an in-progress zone draw"
summary = "Esc cancels an in-progress zone draw"
class = "bug"
status = "shipped"
order = 4740
executor = "claude-code"
notes = "the review's Esc matrix (6 surfaces green) is the regression baseline | SHIPPED W203: Esc cancels circle/polygon/trigger draws — cancel_pending()'s deliberate zone-survival was the mechanism (Esc never reached the draw); cancel_zone_draw wired into the keydown arm under the modal-stack yield, doc_tick bump for the hint. Trigger draw CONFIRMED to share Pending::Zone (one cancel covers both). Verifier re-proved every arm live incl. ladder order (dialog first) and the marker/composition disarm regressions."
main_goal = "Esc cancels in-progress zone/trigger draw and clears rim prompt."
context = [
    "UX review F-31 verified twice; Esc gap is permanent-object draw surface.",
    "Esc honoured on six transient surfaces and marker disarm correct today.",
    "mission_editor Esc arm ~3656 never calls zone cancel path.",
]
requirement = [
    "Esc during in-progress draw cancels pending geometry and disarms.",
    "Clear rim/vertex prompt; second Esc keeps existing close/leave behaviour.",
    "Apply same cancel to trigger arm if shares zone state machine.",
]
current_state = [
    "Circle centre placed then Esc still prompts click the rim; click completes zone.",
    "cancel path exists editor_ops ~4254; armed state ~5838; panel eden_zones ~37-42.",
    "Polygon Close button and trigger zone gesture share state machine.",
]
approach = [
    "Wire cancel_zone_draw into Esc keydown arm under modal-stack yield.",
    "cancel_pending zone survival was mechanism; Esc never reached draw.",
]
verify = [
    "Circle armed centre Esc click elsewhere: zone count unchanged no rim prompt.",
    "Polygon after two vertices same; trigger circle same.",
    "Marker Esc-disarm regression stays green.",
]
citations = [
    "editor_hostile_ux_review F-31",
    "mission_editor.rs Esc ~3656",
    "editor_ops cancel ~4254",
    "eden_zones panel ~37-42",
]
shipped_at = "8717e7fd"
created_at = "2026-08-10T00:28:03Z"
completed_at = "2026-08-10T00:48:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_zones.rs",
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "map_canvas",
]
