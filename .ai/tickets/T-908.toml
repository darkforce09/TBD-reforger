id = "T-908"
kind = "work"
title = "Split 8 xtask/tools SIZE-3 allowlist files"
summary = "Split xtask/tools SIZE-3 files (8) the T-899 allowlist is holding"
class = "chore"
status = "ready"
order = 8310
spec = "docs/platform/t853_shell_to_xtask_waves.md"
plan = "docs/plans/t-908_plan.md"
executor = "claude-code"
main_goal = "Split 8 T-899-allowlisted xtask/tools SIZE-3 files; drop allowlist rows."
context = [
    "T-899 allowlisted eight xtask and tools/tbd-tools files over 1000 lines.",
    "Do not split in waves 234-235; T-902/T-903/T-904 own overlapping paths.",
    "Not the registry-split program.",
]
requirement = [
    "Do not raise the SIZE-3 threshold.",
    "Split files so SIZE-3 allowlist rows can drop.",
]
current_state = [
    "Eight xtask/tools src files over 1000 lines on T-899 allowlist.",
    "Overlapping xtask paths blocked waves 234-235 per ticket summary.",
]
approach = [
    "Split each listed xtask/tools file over 1000 lines.",
    "Split pattern: stem.rs keeps its public surface and declares submodules in a new stem/ directory named by responsibility (no _part2 names); parent mod.rs untouched.",
    "All four split tickets own .coding-standards-allowlist.yaml, so they pack into different waves; one slice per band.",
]
verify = [
    "Split them; drop the SIZE-3 rows.",
    "cargo xtask verify file-length",
    "cargo test -p xtask",
    "cargo xtask platform wave gate --slice T-908",
]
acceptance = [
    "The eight xtask/tools files are at or under 1000 lines with their SIZE-3 rows removed; xtask subcommands and tbd-tools binaries behave identically.",
    "xtask tests and the slice gate are green; no overlap with T-902/T-903/T-904 paths beyond the listed files.",
]
citations = [
    "T-899 SIZE-3 allowlist",
    "t853_shell_to_xtask_waves.md",
    "xtask/src",
    "tools/tbd-tools/src",
]
estimated = ["scope"]
owns = [
    "xtask/src/cmds.rs",
    "xtask/src/main.rs",
    "xtask/src/schema_gates.rs",
    "tools/tbd-tools/src/map/sap.rs",
    "tools/tbd-tools/src/smokes.rs",
    "tools/tbd-tools/src/world/aux.rs",
    "tools/tbd-tools/src/world/build.rs",
    "tools/tbd-tools/src/world/gates.rs",
    ".coding-standards-allowlist.yaml",
]

[scope]
domain = "repo"
layer = "xtask"
component = "tickets"
