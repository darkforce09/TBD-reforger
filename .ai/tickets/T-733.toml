id = "T-733"
kind = "work"
title = "Merge Mission menu Ctrl+M; garrison building accessor"
summary = "Merge Mission wiring: menu item + Ctrl+M binding; garrison building-at-point accessor"
class = "feature"
status = "shipped"
order = 4326
executor = "claude-code"
notes = "filed from wave-111 residue (editor factory)"
main_goal = "Wire Merge Mission menu and Ctrl+M; add building-at-point garrison accessor."
context = [
    "Two wave-111 wiring residues in one slice; T-693 merge command complete without UI entry.",
    "T-645 drag-to-garrison kernel shipped dead_code; live wiring lacks building-at-point accessor.",
    "World store keeps only last_chunk; residency exposes pre-tessellated GPU vertex buffers.",
]
requirement = [
    "Add Merge Mission row to MENUS eden_top_strip and Ctrl+M mission_editor keydown arm.",
    "Census first using T-648 idiom for keydown binding.",
    "Add building-at-point centre half-extents rotation accessor plus drop-target lookup.",
    "Wire drag-to-garrison kernel through accessor and thin drop-target lookup.",
]
current_state = [
    "merge_mission_now picker report toast complete; no menu row or Ctrl+M binding.",
    "Drag-to-garrison kernel exists with allow dead_code citation; accessor missing.",
    "Buildings only as GPU buffers; scope discovery ticket predicted this gap.",
]
approach = [
    "eden_top_strip MENUS row plus mission_editor Ctrl+M keydown census-first.",
    "World store accessor for building-at-point; wire kernel drop-target lookup.",
]
verify = [
    "Merge Mission menu row and Ctrl+M open merge flow.",
    "Garrison drop resolves building centre extents rotation at point.",
]
citations = [
    "wave111.md residue notes",
    "T-645 drag-to-garrison report",
    "T-693 merge_mission_now",
    "T-648 keydown idiom",
]
created_at = "2026-08-08T00:00:00Z"
completed_at = "2026-08-08T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at id-interpolated between T-732 and T-735 (midpoint, day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
