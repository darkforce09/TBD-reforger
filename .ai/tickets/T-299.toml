id = "T-299"
kind = "work"
title = "Single-faction compile ships a phantom opfor"
summary = "flatten pads a stub opposing faction (flatten.rs:2470-2472) to satisfy the schema's two-faction rule, so every single-faction mission shows an unplayable side in briefing and ORBAT. Let the schema permit one faction and stop padding."
class = "bug"
status = "ready"
order = 7480
spec = "docs/specs/ideas/t299_phantom_opfor.md"
plan = "docs/plans/t-299_plan.md"
executor = "claude-code"
notes = "2026-09-05: found by T-186's compiled→mod boot lane. flatten.rs and mission.schema.json are shared with T-242/T-310/T-936.x, TBD_MissionValidator.c with T-290 → later wave. Operator authorization 2026-09-04: agents may edit the Enfusion mod scripts; gate = cargo xtask mod compile; in-game behaviour goes on a human checklist."
priority = 2
main_goal = "A single-faction mission compiles with one faction and no phantom side."
context = [
    "The validator already reports it: '[TBD][Validate] WARNING faction:opfor - declared but has no slots - nobody can play this side'.",
    "Anchor 2026-09-05: flatten.rs:2470 'Schema requires ≥ 2 factions; pad a stub opposing faction', :2472 stub = opfor; mission.schema.json factions minItems 2.",
]
requirement = [
    "mission.schema.json factions minItems becomes 1; a single-faction golden validates; codegen regenerated.",
    "flatten stops padding; a single-faction document compiles with exactly one faction.",
    "TBD_MissionValidator.c accepts one faction; existing two-faction goldens are byte-identical after the change.",
]
current_state = ["Every mission authored with one faction ships a side that appears in briefing and ORBAT that nobody can join; the document loads fine, it is just wrong."]
approach = [
    "Verify on main: compile a one-faction fixture and paste the opfor stub.",
    "Schema first (schema-validate), then remove the pad, then the validator, then goldens.",
    "Perturbation: restore the pad; the single-faction test goes red; restore, touch, green.",
]
verify = [
    "cargo test -p map-engine-core --all-features mission::flatten",
    "cargo xtask ci schema-validate",
    "cargo xtask ci schema-codegen",
    "cargo xtask mod compile",
    "cargo xtask platform wave gate --slice T-299",
]
acceptance = [
    "A single-faction mission compiles with one faction; briefing and ORBAT show no phantom side.",
    "Two-faction compiles are unchanged byte for byte; the mod validator accepts one faction.",
]
created_at = "2026-09-05T03:30:00Z"
owns = [
    "crates/map-engine-core/src/mission/flatten.rs",
    "packages/tbd-schema/schema/mission.schema.json",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionValidator.c",
]

[scope]
domain = "engine"
layer = "core"
