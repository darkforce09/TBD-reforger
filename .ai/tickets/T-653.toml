id = "T-653"
kind = "work"
title = "Preserve the three headless editor-screenshot findings"
summary = "Preserve the three headless editor-screenshot findings"
class = "bug"
status = "idea"
executor = "cursor-docs"
notes = "not factory-dispatchable; no owns, no wave row"
main_goal = "Document three headless editor-screenshot fixes in EDITOR_GATE_RUNBOOK."
context = [
    "Three non-obvious fixes needed to screenshot editor headless; rediscovery risk.",
    "Deliverable is documentation not scripts; verify-no-python blocks Python tools.",
]
requirement = [
    "Document writable XDG_CACHE_HOME for fontconfig; Chrome zero-font abort.",
    "Document --use-angle=vulkan only; swiftshader WebGPU and gl fail.",
    "Document canvas.toDataURL capture; Page.captureScreenshot returns black map.",
]
current_state = [
    "EDITOR_GATE_RUNBOOK.md exists as documentation home; cursor-docs not code.",
    "Two source artifacts disagree on disposition; idea status, no owns.",
]
approach = ["Write runbook section preserving three findings; leave idea status."]
verify = ["Runbook documents all three headless capture prerequisites without Python scripts."]
citations = [
    "KB-002 fontconfig cache",
    "EDITOR_GATE_RUNBOOK home",
    "Makefile verify-no-python gate",
]
estimated = ["scope"]

[scope]
domain = "repo"
layer = "xtask"
