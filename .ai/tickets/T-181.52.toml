id = "T-181.52"
kind = "work"
title = "Loadout log demote plus PlayableSelector oracle"
summary = "Demote intended magazine-swap log noise; wire PlayableSelector design oracle with leak gate and deploy excludes."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Log-noise demotion + oracle lane gap. Demote the magazine first-attempt 'mount refused' line from WARNING to INFO (the deterministic-swap design intends it; it reads as a fault). Document cargo:backpack DEGRADED as a mission/kit authoring mismatch, not a mod bug. Separately: scripts/mod/slice-worktree.sh links only crf_framework and vanilla_reference, so slice agents have NO PlayableSelector oracle - it sits outside the repo at ~/Projects/Archive/Reforger_Lobby/PlayableSelector-main. T-181.50 copies its design, so the lane must be linked (design-mirror ONLY - no licence, never copy code)."
parent = "T-181"
context = [
    "Magazine mount-refused WARNING misreads the deterministic swap as a fault.",
    "slice-worktree linked only two oracles; PlayableSelector design lane was missing.",
]
requirement = [
    "Demote intended swap lines to Print; keep genuine failure paths at WARNING.",
    "Add PlayableSelector oracle lane; extend verify-no-crf-leak; exclude oracles from deploy.",
]
verify = ["compile.sh clean; verify-no-crf-leak PASS with PS negative control."]
citations = [
    "Source: commit 0b305898 — oracle lane wired.",
    "Source: commit 7075c7fe — deploy excludes measured.",
]
shipped_at = "e374c27b"
created_at = "2026-07-25T19:22:16Z"
completed_at = "2026-07-25T19:35:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["scripts/mod/** + Gamemode/TBD_LoadoutEquipComponent.c"]

[scope]
domain = "mod"
layer = "scripts"
