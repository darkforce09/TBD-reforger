id = "T-760"
kind = "work"
title = "Markers render nothing — add MissionMarkers lane and rebind"
summary = "Markers draw nothing on the map — RIGHT-MODE-006 is at best half-closed and untracked"
class = "docs"
status = "shipped"
order = 4636
executor = "claude-code"
notes = """
filed by wave-116 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MAJOR (scope gap) from the wave-116 verifier. T-069 ships marker AUTHORING — place, list, retype, recaption, reposition, delete — and all four ATTR-FIELD-MRK-* rows close honestly. But a placed marker renders NOTHING: there is no marker lane in map-engine-render, and `mission_history.rs:364 after_doc_change` rebinds slots, squad links and vehicles only. RIGHT-MODE-006 is titled 'markers on the map'. THE SLICE'S REFUSAL TO ADD THE LANE WAS CORRECT AND SHOULD NOT BE SECOND-GUESSED: the sole rebind tail after undo/redo/restore is `after_doc_change`, which was outside its owns, and a lane fed only from owned call sites would show a marker the document no longer has — worse than no lane. It also verified the SoA route fails, because `slots_bind_soa` is the PICK BRIDGE (markers riding it would be hit-testable as slots, draggable in a marquee, counted against the 500-slot cluster threshold, and listed as slots in the outliner). NOTE THE ASYMMETRY THIS TICKET EXISTS TO CORRECT: wave 114's T-651 shipped editor comments with the identical no-glyph property and that gap got T-748 filed against it; markers had no equivalent, so the gap lived only in a verifier report. RECIPE, left by the T-069 agent so it is one sitting: add `LaneRole::MissionMarkers` between MissionZones and SquadLinks (above zone rings, below squad links and slots, so a marker never occludes a unit); add `markers_bind(&[f32], &[u8])` beside `vehicles_bind` reusing `upload_slot_role_lane` + `pack_icon_instance` (no new pipeline, no new atlas); add it to `clear_slot_lanes`; and ONE line each in `after_doc_change` and `rebind_engine_from_doc`. SCHEDULE WITH OR AFTER T-672 (wave 117) to share the draw_order.rs edit. Diagnosis: editor_verify/wave116.md finding 2.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["MAJOR (scope gap) from the wave-116 verifier. T-069 ships marker AUTHORING — place, list, retype, recaption, reposition, delete — and all four ATTR-FIELD-MRK-* rows close honestly. But a placed marker renders NOTHING: there is no marker lane in map-engine-render, and `mission_history.rs:364 after_doc_change` rebinds slots, squad links and vehicles only. RIGHT-MODE-006 is titled 'markers on the map'. THE SLICE'S REFUSAL TO ADD THE LANE WAS CORRECT AND SHOULD NOT BE SECOND-GUESSED: the sole rebind tail after undo/redo/restore is `after_doc_change`, which was outside its owns, and a lane fed only from owned call sites would show a marker the document no longer has — worse than no lane. It also verified the SoA route fails, because `slots_bind_soa` is the PICK BRIDGE (markers riding it would be hit-testable as slots, draggable in a marquee, counted against the 500-slot cluster threshold, and listed as slots in the outliner). NOTE THE ASYMMETRY THIS TICKET EXISTS TO CORRECT: wave 114's T-651 shipped editor comments with the identical no-glyph property and that gap got T-748 filed against it; markers had no equivalent, so the gap lived only in a verifier report. RECIPE, left by the T-069 agent so it is one sitting: add `LaneRole::MissionMarkers` between MissionZones and SquadLinks (above zone rings, below squad links and slots, so a marker never occludes a unit); add `markers_bind(&[f32], &[u8])` beside `vehicles_bind` reusing `upload_slot_role_lane` + `pack_icon_instance` (no new pipeline, no new atlas); add it to `clear_slot_lanes`; and ONE line each in `after_doc_change` and `rebind_engine_from_doc`. SCHEDULE WITH OR AFTER T-672 (wave 117) to share the draw_order.rs edit. Diagnosis: editor_verify/wave116.md finding 2."]

missing=0
"""
main_goal = "Add MissionMarkers render lane and rebind on after_doc_change."
context = [
    "Wave-116 MAJOR scope gap: T-069 ships authoring but no map-engine marker lane.",
    "after_doc_change rebinds slots squad links vehicles only; RIGHT-MODE-006 half-closed untracked.",
    "T-651 comments gap got T-748; markers had no equivalent ticket until now.",
]
requirement = [
    "Add LaneRole MissionMarkers between MissionZones and SquadLinks draw order.",
    "markers_bind beside vehicles_bind; clear_slot_lanes; one line after_doc_change and rebind_engine_from_doc.",
    "Schedule with or after T-672 to share draw_order.rs edit.",
]
current_state = [
    "Placed marker renders nothing; no marker lane in map-engine-render.",
    "SoA slots_bind_soa route fails: markers would be hit-tested as slots draggable marquee.",
]
approach = [
    "markers_bind reusing upload_slot_role_lane pack_icon_instance no new pipeline atlas.",
    "Add to clear_slot_lanes; after_doc_change and rebind_engine_from_doc one line each.",
    "T-069 recipe: above zone rings below squad links slots so marker never occludes unit.",
]
verify = [
    "Placed marker visible on map at default zoom after doc change rebind.",
    "RIGHT-MODE-006 markers-on-map requirement tracked and closed not verifier-only.",
]
acceptance = [
    "LaneRole::MissionMarkers landed between MissionZones and SquadLinks in draw order.",
    "markers_bind fed after_doc_change and rebind_engine_from_doc so undo kept map glyphs honest.",
]
citations = [
    "editor_verify/wave116 finding 2",
    "T-069 marker authoring",
    "RIGHT-MODE-006",
    "T-672 draw_order share",
]
shipped_at = "194cdebe"
created_at = "2026-08-08T03:15:08Z"
completed_at = "2026-08-08T03:41:15Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "crates/map-engine-render/src/draw_order.rs",
    "crates/map-engine-render/src/engine.rs",
    "apps/website/frontend/src/mission_history.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
