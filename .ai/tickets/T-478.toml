id = "T-478"
kind = "work"
title = "T-440 verify greened on comment name + path-echo seed recipe"
summary = "T-440 verify greened on comment name + path-echo seed recipe"
class = "chore"
status = "shipped"
order = 3317
executor = "claude-code"
priority = 1
main_goal = "Harden verify-t440 against comment name pins, path-echo recipes, and unwired wave.sh."
context = [
    "Residual from wave 29 adversarial MAJOR×2 (+ MINOR).",
    "verify-t440-faction-library-seed.sh has hollow grep and path-substring pins.",
]
requirement = [
    "Strip SQL comments before name pin; require live INSERT INTO user_factions name value.",
    "Require recipe line that redirects/applies faction_library.sql to psql.",
    "Pin wave.sh cold/slice gate paths invoke the script.",
]
current_state = [
    "(1) US Army 1980s raw grep — SQL comment + SELECT 1 still PASS.",
    "(2) echo seeds/faction_library.sql >/dev/null or path in comment still PASS.",
    "(3) wave.sh never pins cold/slice run T-440 wiring.",
    "Repro: comment-only starter name or echo-path recipe → verify-t440 PASS.",
]
approach = [
    "Strip SQL comments before name pin; require live INSERT with name column value.",
    "Pin wave.sh both gate paths invoke script; RED→GREEN on verifier perturbations.",
]
citations = [
    "wave 29 adversarial MAJOR",
    "verify-t440-faction-library-seed.sh",
    "verify-t444 recipe shape",
]
shipped_at = "70b0ec4d"
created_at = "2026-07-27T04:19:21Z"
completed_at = "2026-07-27T04:27:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/mod/verify-t440-faction-library-seed.sh",
    "scripts/platform/wave.sh",
    "Makefile",
    "apps/website/api/seeds/faction_library.sql",
]

[scope]
domain = "repo"
layer = "xtask"
