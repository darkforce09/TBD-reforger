id = "T-682"
kind = "work"
title = "Environment readers: fog, wind, view distance"
summary = "Environment readers: fog, wind, view distance"
class = "chore"
status = "ready"
order = 7350
spec = "docs/specs/existing/t682_environment_readers.md"
plan = "docs/plans/t-682_plan.md"
executor = "claude-code"
notes = """
workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row

--- HEAD migration_legacy (verbatim) ---

Three attribute ids: `ATTR-FIELD-SCN-FOG`, `-SCN-WIND`, `-SCN-VIEW-DIST`. THESE ARE REFUSED BY TEST, not merely unbuilt: `eden_chrome.rs`:4624 iterates `["viewDistance", "thermals", "windDirDeg", "fog", "wind"]` and asserts none of them is carried, and `author_env` (:271) enforces the ordering deliberately — THE MOD READER COMES FIRST, THE CONTROL SECOND. `windDirDeg` is already in the schema and `ModEnvironment` (flatten.rs:268-275) does not even serialise it. Read the `author_env` rationale before opening this. T-193 removed the view-distance and thermals controls for exactly this reason; T-663 removes their last dead DTO fields. Adding a control before a reader would re-create the defect the test exists to prevent. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.

missing=0
"""
main_goal = "Implement mod environment reader for fog, wind, view distance first."
context = [
    "Three SCN-FOG, SCN-WIND, SCN-VIEW-DIST attrs refused by eden_chrome test.",
    "author_env enforces mod reader before editor control; test at :4624.",
    "T-193 removed view-distance controls; T-663 cleans dead DTO fields.",
    "Operator: agents may edit the Enfusion mod scripts; gate = cargo xtask mod compile; in-game behaviour goes on a human checklist.",
    "eden_chrome.rs is gone; author_env and its ordering test now live in apps/website/frontend/src/editor/panels/env.rs.",
]
requirement = [
    "Ship TBD_EnvironmentReader.c before any editor fog wind view control.",
    "Serialize windDirDeg already in schema via ModEnvironment flatten path.",
]
current_state = [
    "eden_chrome.rs:4624 asserts viewDistance thermals windDirDeg fog wind absent.",
    "ModEnvironment flatten.rs:268-275 omits windDirDeg serialization today.",
]
approach = [
    "Read author_env ordering rationale; implement mod reader owns path first.",
    "Defer editor controls until reader proven; schema widening stays T-706.",
]
verify = [
    "Headless compile clean; environment attrs applied at mission boot.",
    "eden_chrome environment-carried test passes after mod reader ships.",
    "cargo xtask mod compile",
]
acceptance = [
    "Fog, wind and view distance from the payload are applied at mission boot by TBD_EnvironmentReader; missions without them boot unchanged.",
    "No packages/tbd-schema edits; compile clean; the env.rs author_env ordering test still passes.",
]
citations = [
    "eden_chrome.rs author_env test",
    "T-193 view-distance control removal",
    "T-706 schema widening split",
]
estimated = ["scope"]
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_EnvironmentReader.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionLoader.c",
    "crates/map-engine-core/src/mission/flatten.rs",
]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
