id = "T-376"
kind = "work"
title = "Registry importer NULLs columns from partial envelope"
summary = "The registry importer NULLs ten populated columns from a partial envelope"
class = "bug"
status = "shipped"
order = 3192
executor = "claude-code"
notes = """
" A ", 1. 2. 3. == BY No an be it of or to 'v2 And NOT ONE The UI. `IS `ON all and are bug had it, its now out own row run was :166 MAKE SAME THAN THIS This bind case does file gate here most need now. only path real ten; than that them this were what when will work BYTES Blast FROM` NULLs THREE about after bound bugs, call, carry close could drive fully good, icons match moved notes right there time, token value where which wrong DEFECT RATHER THINGS `idea` above. always audits budget clear: engine faster file's fixing hours. judged lands. prefab schema serves ships. starts states things ticket value; 'Absent CHOICE: Nothing Promote UPDATE, UPDATE` backlog comment costing default exactly exists: feature knowing nothing protect radius: spelled survive updates :110-114 DEFERRED OPERATOR TRIGGER. UNKNOWN, actually already: original produced spending verbatim ('curated **written CANCELLED DECISION, Recording column.** columns.' distinct, downgrade envelopes extended. findable, guessed.' icon_url. optional. precisely rationale untested. weight_kg 2026-07-26 :167-189). Option<T>` Secondary, `icon_url` `updated`. developing diagnosed, generating mechanism: operator's serialized something, statement: substance: T-182..T-297 minLength:1, reproducible re-imports'). `import_items` cargo_grid_w/h Stored-non-NULL envelope-version Preserve-semantics `#[serde(default)] registry_items.col)`, `COALESCE(EXCLUDED.col, `env.registry_items_version`. (tests/registry_compat.rs:387) (contract/generated/registry_items.rs:196-238,

--- migration_legacy (verbatim) ---



services/registry_import.rs:201-219 — the `ON CONFLICT DO UPDATE` sets ten columns that bind from `#[serde(default)] Option<T>` fields (contract/generated/registry_items.rs:196-238, bound at :167-189). Absent in the envelope -> None -> NULL -> **written over a populated column.** The file's own comment at :166 states the mechanism: 'v2 envelopes bind NULL columns.'

THREE THINGS MAKE THIS A DEFECT RATHER THAN A CHOICE:
  1. `icon_url` is the ONE column deliberately excluded from the DO UPDATE, with the rationale spelled out at :110-114 ('curated icons survive re-imports'). Preserve-semantics were reasoned about for exactly one column and never extended.
  2. The schema says absent means UNKNOWN, not clear: weight_kg/volume_cm3/max_* all carry 'Absent when the value is an engine class default not serialized in the prefab — never guessed.'
  3. `IS DISTINCT FROM` at :210-219 does not protect — it is the TRIGGER. Stored-non-NULL vs incoming-NULL IS distinct, so the row updates and counts as `updated`.

No envelope-version gate exists: `import_items` never reads `env.registry_items_version`. And the idempotency test (tests/registry_compat.rs:387) re-imports the SAME BYTES — precisely the case where the NULLs match — so the downgrade path is untested.

Blast radius: handlers/registry.rs:82-94 serves all ten; weight_kg and cargo_grid_w/h drive the Arsenal capacity UI. Fix precedent is in the file already: `COALESCE(EXCLUDED.col, registry_items.col)`, matching icon_url. Secondary, same statement: display_name/category are minLength:1, which blocks "" but not "   ", and :153 binds untrimmed.

== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now.
"""
priority = 1
main_goal = "Use COALESCE on ON CONFLICT DO UPDATE so absent envelope fields do not NULL populated registry columns."
context = [
    "services/registry_import.rs:201-219 ON CONFLICT DO UPDATE sets ten columns from #[serde(default)] Option<T> fields.",
    "Absent in envelope -> None -> NULL -> written over a populated column.",
]
requirement = [
    "Fix with COALESCE(EXCLUDED.col, registry_items.col) matching icon_url precedent at :110-114.",
    'Trim display_name/category — minLength:1 blocks "" but not whitespace; :153 binds untrimmed.',
]
current_state = [
    "icon_url deliberately excluded from DO UPDATE — preserve-semantics reasoned for one column only.",
    "Schema says absent means UNKNOWN not clear for weight_kg/volume_cm3/max_* class defaults.",
    "IS DISTINCT FROM at :210-219 is the trigger — stored-non-NULL vs incoming-NULL counts as updated.",
    "import_items never reads env.registry_items_version; idempotency test re-imports same bytes.",
]
citations = [
    "contract/generated/registry_items.rs",
    "handlers/registry.rs:82-94 serve",
    "tests/registry_compat.rs:387",
    "Arsenal capacity UI columns",
]
shipped_at = "67456a65"
created_at = "2026-07-27T08:33:48Z"
completed_at = "2026-07-27T08:40:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/api/src/services/registry_import.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
