id = "T-721"
kind = "work"
title = "Status bar under docks: blur, dead OPEN, Eden fix"
summary = "Status bar runs under both docks — readouts blurred, OPEN click-dead"
class = "bug"
status = "deferred"
order = 4314
executor = "claude-code"
notes = """
filed by wave-105 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

T-636's full-width status bar (inset-x-0 bottom-0, no z-index) runs UNDER both dock wrappers, which reach bottom-0 at z-20 with full-height glass panels: the bar's left 256px (most of CUR/X/Y/Z) is behind DockLeft's blur, and its right 320px (HUD slot + OPEN) is painted over and CLICK-DEAD under DockRight — at every viewport width (the old centred pill cleared the docks above ~1100px). Same z-order mechanism T-636's own comments cite for the old HUD corner. The in-code claim 'inside the status bar it can never be occluded' is false; source-inspection tests structurally cannot see stacking. Eden-parity fix: docks end at the bar's TOP (dock wrappers bottom-9 not bottom-0, or the bar gets z-30 — prefer the dock height change, it is the Eden geometry). Touches eden_layout/dock wrappers + pointer math consistency (TOOLBELT_BAND_PX readers). Also sweep the stale prose cluster wave105.md MINOR-1 lists (4-to-6 comment vs assert 5; t635 docstring bottom-5; STATUSBAR height claim; two-homes/three-bullets). Diagnosis: editor_verify/wave105.md MAJOR-1 + MINOR-1. NOTE: T-667 (wave 106) was briefed to mount furniture in the CLEAR centre span and anchor grid refs to the MAP PANE edges, so no rework lands on this ticket's back.

missing=0
"""
main_goal = "Fix status bar z-order under docks; Eden-parity geometry."
context = [
    "T-636 full-width status bar inset-x-0 bottom-0 no z-index.",
    "Dock wrappers bottom-0 z-20 full-height glass panels paint over bar.",
    "T-667 mounts furniture clear centre; no rework on this ticket back.",
]
requirement = [
    "Docks end at bar TOP: bottom-9 not bottom-0 preferred Eden geometry.",
    "Or bar z-30; fix left blur and right CLICK-DEAD OPEN.",
    "Sweep stale prose cluster wave105.md MINOR-1 lists.",
]
current_state = [
    "Bar left 256px behind DockLeft blur; right 320px under DockRight dead.",
    "Old centred pill cleared docks above ~1100px; now every viewport width.",
    "In-code occlusion claim false; source-inspection tests cannot see stacking.",
]
approach = [
    "Eden-parity dock height change over bar z-index bump.",
    "Touch eden_layout dock wrappers; TOOLBELT_BAND_PX pointer math consistency.",
    "Fix 4-to-6 comment, t635 docstring, STATUSBAR height prose.",
]
verify = [
    "CUR X Y Z readouts visible not blurred under DockLeft.",
    "OPEN and HUD slot clickable not under DockRight at all widths.",
]
citations = [
    "T-636 status bar layout",
    "wave105.md MAJOR-1 diagnosis",
    "T-667 centre span briefing",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
