id = "T-218"
kind = "work"
title = "Rejection reasons are silently discarded"
summary = "Frontend posted empty reject body; reviewer had no reason field. Reason now reaches the database."
class = "feature"
status = "shipped"
order = 2360
executor = "claude-code"
context = [
    "POST /approvals/:id/reject expects RejectInput with reason field.",
    "Frontend posted serde_json json empty object on every rejection.",
    "Only reviewer comment box existed; it was never POSTed.",
]
requirement = [
    "Send trimmed rejection reason in POST body.",
    "Disable Request Changes while reason blank; handler re-checks after trim.",
    "Delete mock approved and rejected tabs; counter uses server total.",
]
current_state = [
    "Empty body deserialized to empty string via serde default.",
    "Author still sees rejected only; dto MissionDetail drops rejection_reason.",
    "Mock tabs advertised missions that do not exist in database.",
]
approach = [
    "ReviewInspector component owns LocalResource per selected row.",
    "Briefing and stat tiles from GET /missions/:id instead of fiction.",
    "Scratch Notes label; reviewer comment stays local unsaved signal.",
]
verify = [
    "Dev API: reject with reason stores and returns text; empty body stores blank.",
    "cargo check, clippy wasm32, 115 frontend tests, debug trunk build.",
]
acceptance = ["Trimmed rejection reason persisted via POST; mock Approved/Rejected tabs deleted; Request Changes disabled until reason nonempty."]
citations = [
    "commit a024b702 rejection reason POST",
    "T-313 author display filed",
    "T-283 review history endpoint",
    "T-195 demo data policy",
]
shipped_at = "ca7c1319"
created_at = "2026-07-26T05:45:38Z"
completed_at = "2026-07-26T06:29:14Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 6 commit subject(s)"
owns = ["apps/website/frontend/src/approvals.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
