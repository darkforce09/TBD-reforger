id = "T-722"
kind = "work"
title = "Route sweep passes typo URIs; gate never validates concrete URIs"
summary = "Route sweep passes on a typo-d URI — completeness gate never validates URIs"
class = "bug"
status = "deferred"
order = 4315
executor = "claude-code"
notes = "filed by wave-105 adversarial verifier (editor factory)"
main_goal = "Assert route sweep rejects typo URIs; gate must validate concrete URIs."
context = [
    "Structural program-wide defect surfaced by wave-105 verifier T-683 probe.",
    "Vacuous-pass shape: typo'd URI 404s yet sweep passes GREEN.",
]
requirement = [
    "Assert per sweep entry response is NOT 404 or 405.",
    "Or resolve each concrete URI against the router before pass.",
    "Sweep entry never reaching handler must be red not green.",
]
current_state = [
    "route_sweep only FAILS on 5xx responses today.",
    "Template list validated against app.rs never concrete URI resolution.",
    "T-683 entry proven live 200 admin 403 enlisted 401 anon.",
]
approach = ["Extend every_get_route_is_swept gate with per-entry URI resolution check."]
verify = ["Typo-d URI in sweep list fails gate not passes green."]
citations = [
    "editor_verify/wave105.md NOTE-2",
    "T-683 sweep entry live probe",
    "null_tolerance route_sweep gate",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
