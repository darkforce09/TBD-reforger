id = "T-549"
kind = "work"
title = "Live /compiled still uses empty CargoPhysCatalog (T-500 trust-saved residual)"
summary = "Live /compiled still uses empty CargoPhysCatalog (T-500 trust-saved residual)"
class = "feature"
status = "shipped"
order = 3389
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W58 adversarial verifier (CLEAN MINOR) after T-500.

T-500 added flatten_to_mod_document_with_catalog + scan_cargo_capacity; the no-arg entry still uses an empty catalog (documented trust-Save). Live get_compiled_mission / events roster still call the no-arg path, so pre-T-416 over-capacity versions can still compile until re-saved.

Cure: load_cargo_phys_catalog in the compile handler(s) and call flatten_to_mod_document_with_catalog with the live table.

Repro: rg flatten_to_mod_document apps/website/api/src/handlers

missing=0"""
priority = 2
main_goal = "Live /compiled must load cargo phys catalog not empty."
context = ["W58 verifier CLEAN MINOR after T-500."]
requirement = ["No-arg flatten_to_mod_document still uses empty catalog."]
current_state = ["get_compiled_mission calls no-arg path; over-capacity compiles until re-saved."]
approach = ["Load cargo_phys_catalog; call flatten_to_mod_document_with_catalog."]
verify = ["rg flatten_to_mod_document apps/website/api/src/handlers"]
acceptance = [
    "get_compiled_mission loaded load_cargo_phys_catalog then flatten_to_mod_document_with_catalog.",
    "HTTP IT refused over-capacity tip on /compiled when registry phys table was loaded.",
]
citations = [
    "W58 verifier T-500",
    "flatten_to_mod_document_with_catalog",
]
shipped_at = "ae2d464c"
created_at = "2026-07-27T14:41:30Z"
completed_at = "2026-07-27T14:48:40Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/missions.rs",
    "apps/website/api/src/services/mission_compile.rs",
    "apps/website/api/src/services/mod.rs",
    "apps/website/api/tests/missions.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
