id = "T-556"
kind = "work"
title = "BLOCKER: gate rg resolves only via agent shell shim"
summary = "BLOCKER: the gate depends on `rg`, which is installed nowhere — it resolves only via an agent-injected shell function"
class = "bug"
status = "shipped"
order = 3401
executor = "claude-code"
priority = 0
main_goal = "Replace rg with grep -E in verify scripts; pin or install ripgrep."
context = [
    "ripgrep not installed on host or container.",
    "Agent shell injects rg function; subshells lack it.",
]
requirement = [
    "Fix verify-t456, verify-t437, verify-t296, verify-t452.",
    "Sweep scripts/mod/verify-t*.sh and scripts/platform/*.sh for rg.",
    "CI must pin ripgrep or remove dependency.",
]
current_state = [
    "T-456 REST gate FAIL: rg command not found.",
    "verify-t296/t452 fail-open when rg absent.",
]
approach = [
    "T-216 pattern: grep -E with exit-status triage.",
    "Wire or delete dead verify scripts deliberately.",
]
verify = [
    "rpm -q ripgrep absent; bash -c command -v rg absent.",
    "Gate scripts exit 1 without agent shim.",
]
citations = [
    "ripgrep absent container host",
    "T-216 grep -E pattern",
]
shipped_at = "49546c2e"
created_at = "2026-07-27T20:27:30Z"
completed_at = "2026-07-27T20:40:13Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "scripts/mod/verify-t456-mission-rest-size-gate.sh",
    "scripts/mod/verify-t437-destroy-inert-diagnostics.sh",
    "scripts/mod/verify-t296-results-reporter-identity-comments.sh",
    "scripts/mod/verify-t452-player-identity-link-comments.sh",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
