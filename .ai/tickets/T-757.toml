id = "T-757"
kind = "work"
title = "mission.schema.json is embedded twice in the wasm bundle"
summary = "mission.schema.json is embedded twice in the wasm bundle"
class = "bug"
status = "shipped"
order = 4633
executor = "claude-code"
notes = "filed by wave-115 adversarial verifier (editor factory)"
shipped_at = "42300a78"
created_at = "2026-08-08T06:13:51Z"
completed_at = "2026-08-08T06:24:12Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
migration_legacy = ["MINOR from the wave-115 verifier. mission.schema.json (91,438 bytes) is `include_str!`d from TWO places: eden_zones.rs:628 (private const MISSION_SCHEMA, pre-existing) and eden_settings.rs:1063 (T-688, this wave). Both name the SAME path, so drift is impossible and there is genuinely one source of truth — the slice's reasoning for the second embed was correct, and it could not reuse the first because that const is private to a file outside its owns. But an empirical byte count of the DEV wasm artifact finds the full 91KB present exactly twice. Release behaviour after wasm-opt is unverified and may well dedupe. Fix: promote one const to pub(crate) in a shared module and have both read it, which costs nothing and removes the question. ALSO: eden_zones.rs:626 carries a stale comment describing the embed as '~40 KB' against an actual 91 KB — a number that has drifted by more than 2x and should be derived or dropped rather than restated. Diagnosis: editor_verify/wave115.md MINOR-5."]
owns = [
    "apps/website/frontend/src/eden_zones.rs",
    "apps/website/frontend/src/eden_settings.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["env_settings"]
