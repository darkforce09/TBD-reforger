id = "T-757"
kind = "work"
title = "mission.schema.json is embedded twice in the wasm bundle"
summary = "mission.schema.json is embedded twice in the wasm bundle"
class = "bug"
status = "shipped"
order = 4633
executor = "claude-code"
notes = "filed by wave-115 adversarial verifier (editor factory)"
main_goal = "Deduplicate mission.schema.json wasm embed; fix stale size comment."
context = [
    "Wave-115 MINOR: mission.schema.json embedded twice in wasm though one path.",
    "Second embed justified; first const private outside slice owns.",
    "DEV wasm counts full 91KB twice; release wasm-opt dedupe unverified.",
]
requirement = [
    "Promote one shared pub(crate) const; both consumers read it.",
    "Fix or remove stale ~40 KB comment at eden_zones 626.",
]
current_state = [
    "include_str at eden_zones 628 MISSION_SCHEMA and eden_settings 1063 T-688.",
    "Same path so no drift; empirical DEV artifact duplicates 91438 bytes.",
]
approach = [
    "Shared module pub(crate) MISSION_SCHEMA const both files import.",
    "Derive or drop eden_zones stale embed size comment.",
]
verify = [
    "DEV wasm byte count shows single schema embed not duplicate 91KB.",
    "eden_zones comment matches actual size or removed.",
]
citations = [
    "editor_verify/wave115 MINOR-5",
    "T-688 eden_settings embed",
    "wave-115 verifier",
]
shipped_at = "42300a78"
created_at = "2026-08-08T06:13:51Z"
completed_at = "2026-08-08T06:24:12Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_zones.rs",
    "apps/website/frontend/src/eden_settings.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["env_settings"]
