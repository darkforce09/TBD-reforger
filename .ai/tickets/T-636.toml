id = "T-636"
kind = "work"
title = "Full-width status bar splits toolbelt telemetry"
summary = "Full-width status bar — stop the toolbelt conflating tools with telemetry"
class = "feature"
status = "shipped"
order = 3930
executor = "claude-code"
notes = """
editor-UI program wave 5 (wave_plan.tsv row 105) — SHIPPED wave 105 (editor factory), 8b896162; verifier wave105.md

--- HEAD migration_legacy (verbatim) ---

RESCOPED by editor_chrome_direction.md. One floating centred pill (~580 px) holds mode buttons (Select / Ruler / LoS) and numeric readouts (CUR, OBJ, SEL, SZ) — different jobs, different interaction models, no separation. Eden splits them: tools on the toolbar, readouts in a full-width status bar at the bottom of the viewport. The operator's direction is explicit — the bottom bar STAYS, content and feel unchanged, stretched to span the viewport instead of floating centred. That also creates the natural home for the scale bar and grid references (T-667) and a slot at the right end for a primary action (Eden puts PLAY SCENARIO there on its own black surface; what fills ours is OPEN and recorded as such in editor_chrome_direction.md §Open). Code: `BottomToolbelt` (post-split eden_toolbelt.rs, from eden_chrome.rs:3672-3776) — mode buttons :3705-3719 and the CUR/OBJ/SEL/SZ readouts :3721-3766 are one `<div class=TOOLBELT>`; TOOLBELT const :64. The mount + positioning wrapper is mission_editor.rs:2069-2090, and splitting one pill into a toolbar AND a status bar is two mount points, not one. OWNS WIDENED (owns_correction_chrome3.md): the band height TOOLBELT_BAND_PX lives in eden_layout.rs post-split and is read by three files — select_tool.rs uses it to convert pointer coordinates to world coordinates and mission_editor.rs uses it to size the wgpu canvas — so changing the band is an input-handling change.

missing=0"""
depends_on = ["T-661"]
main_goal = "Split BottomToolbelt into toolbar and full-width status bar."
context = [
    "Centred pill mixes Select Ruler LoS modes with CUR OBJ SEL SZ readouts.",
    "Eden separates tools toolbar from viewport-spanning status bar readouts.",
]
requirement = [
    "Stretch bottom bar full viewport; keep content and feel unchanged.",
    "Split one TOOLBELT div into toolbar plus status bar two mounts.",
    "Band height TOOLBELT_BAND_PX change affects canvas sizing and unproject.",
]
current_state = [
    "BottomToolbelt mode buttons :3705-3719 and readouts :3721-3766 one div.",
    "Mount wrapper mission_editor.rs:2069-2090 single centred floating pill ~580px.",
    "TOOLBELT_BAND_PX in eden_layout read by select_tool and mission_editor.",
]
approach = [
    "Two mount points in mission_editor.rs for toolbar and status bar.",
    "Creates home for T-667 scale grid and OPEN primary action slot.",
]
verify = [
    "Tools and readouts separated into distinct chrome regions at viewport bottom.",
    "Status bar spans viewport width instead of centred floating pill.",
]
citations = [
    "editor_chrome_direction full-width bar",
    "BottomToolbelt eden_toolbelt TOOLBELT",
    "TOOLBELT_BAND_PX eden_layout band",
]
shipped_at = "d1ab1f1b"
created_at = "2026-08-01T20:11:21Z"
completed_at = "2026-08-02T03:02:50Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_toolbelt.rs",
    "apps/website/frontend/src/eden_layout.rs",
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/select_tool.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "toolbelt",
    "layout_chrome",
    "map_canvas",
    "tools",
]
