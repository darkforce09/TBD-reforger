id = "T-181.7"
kind = "work"
title = "Foundational TBD UI framework layer"
summary = "Ship MenuStack, MenuBase, ShellScreen, pooled ListBox, UITheme, layouts, and chimeraMenus preset."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "UI framework @ fb36da81. TBD_MenuStack/TBD_MenuBase/TBD_ShellScreen/TBD_ListBox + TBD_UITheme (Aegis tokens) + 2 .layout + chimeraMenus.conf. Listbox POOLS and re-binds rows (vanilla SCR_ListBoxComponent creates/destroys per item — wrong for 128 slots). BLOCKER: chimeraMenus.conf is invisible to the engine until Workbench regenerates resourceDatabase.rdb — 'GUI (E): Menu preset TBD_UIShell not found!'. Proven by elimination across 4 path/meta variants; no script escape hatch (RegisterPreset/OpenMenuByLayout do not exist). Needs one slow-lane Workbench pass. COMPILE-VERIFIED ONLY — no visual claim."
parent = "T-181"
context = [
    "Capability row 0: lobby, briefing, spectator screens need shared UI foundation.",
    "Vanilla SCR_ListBoxComponent destroys per item; 128 slots need pooling.",
]
requirement = [
    "Add TBD UI scripts, two layouts, chimeraMenus.conf with Aegis theme tokens.",
    "ListBox pools and rebinds rows instead of create/destroy per item.",
]
current_state = [
    "No shared TBD UI layer; vanilla listbox destroyed a row per item.",
    "128-slot lobby needed pooled ListBox rebind, not create/destroy.",
]
approach = [
    "Ship MenuStack, MenuBase, ShellScreen, pooled ListBox, UITheme.",
    "Two layouts plus chimeraMenus.conf on Aegis tokens.",
]
verify = ["compile.sh exit 0; verify-no-crf-leak PASS."]
acceptance = [
    "MenuStack, ShellScreen, pooled ListBox, UITheme, layouts, and chimeraMenus preset compiled cleanly.",
    "Compile-only per notes; chimeraMenus awaits Workbench rdb regeneration.",
]
citations = [
    "Source: commit fb36da81 — UI framework.",
    "Source: commit 441912ba — merge slice.",
    "notes compile-only chimeraMenus rdb blocker",
    "t181 program wave 1 UI",
]
shipped_at = "441912ba"
created_at = "2026-07-25T07:22:50Z"
completed_at = "2026-07-25T07:24:32Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"

[scope]
domain = "repo"
layer = "docs"
