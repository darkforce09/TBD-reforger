id = "T-181.53"
kind = "work"
title = "Delete pre-slot ghost; keep camera"
summary = "Remove TBD_PreSlotBody ghost that modal-asserted out of bounds; keep pre-slot camera as load-bearing half."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Delete the pre-slot ghost; keep the pre-slot camera. OPERATOR-CONFIRMED LIVE 2026-07-25 20:5x: the ghost never worked - CELL_ALTITUDE_M=100000 (taken from PlayableSelector) is outside Everon's world bounds, so the engine raised a MODAL 'Entity out of world bounds' assertion at TBD_PreSlotBody.c:669 SpawnGhostEntity AND refused the control transfer ('the engine did not transfer control to the ghost - rolled back'). The session then ran end to end anyway: camera UP, picker OPEN, claim, DEPLOY, groundDelta=0.011. So the ghost's value was defensive and unproven while its cost was a modal dialog mid-mission, which would be fatal in a live event. T-181.50's own header already called the ghost 'the smaller half' and the camera 'the actual black screen' fix - the live run confirmed it. Keep: TBD_PreSlotCamera.c and the vanilla loading-placeholder teardown (both confirmed working in the same log)."
parent = "T-181"
context = [
    "CELL_ALTITUDE_M=100000 outside Everon raised modal assertion on ghost spawn.",
    "Operator session ran end-to-end bodyless with camera; ghost benefit unproven.",
]
requirement = [
    "Delete TBD_PreSlotBody.c and unwind SpawnManager ghost hooks.",
    "Keep TBD_PreSlotCamera and vanilla loading-placeholder teardown untouched.",
]
verify = ["world-boot PASS; ghost symbols absent from mod tree."]
citations = [
    "Source: commit 6739dbe1 — ghost deleted.",
    "Source: commit 40de0fc0 — rdb refresh.",
]
shipped_at = "40de0fc0"
created_at = "2026-07-25T20:11:37Z"
completed_at = "2026-07-25T20:15:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_PreSlot*"]

[scope]
domain = "mod"
layer = "scripts"
component = "gamemode"
