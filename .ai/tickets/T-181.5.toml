__keys = [
    "id",
    "parent",
    "targets",
    "executor",
    "status",
    "notes",
]
__ord = 0
executor = "workbench"
id = "T-181.5"
notes = "DEPRIORITISED: make mod-compile (native, 1.3s) removed most of the reason to open Workbench at all. Only world/prefab authoring and PIE visual checks still need it. Do this when a slice actually requires the editor."
parent = "T-181"
status = "deferred"
targets = [
    "mod",
    "root",
]
