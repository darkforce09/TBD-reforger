id = "T-479"
kind = "work"
title = "Flaky events IT: event_orbat_registration_and_race duplicate arma_id"
summary = "Flaky events IT: event_orbat_registration_and_race duplicate arma_id"
class = "chore"
status = "shipped"
order = 3318
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Observed wave 29 cold re-gate FAIL once on fresh tbd_wave29b_cold: event_orbat_registration_and_race panicked in common/mod.rs:217 seed_user with duplicate key idx_users_arma_id (events-arma-000000000000334002). Immediate re-gate on fresh tbd_wave29c_cold PASS (33/40/13). Not caused by T-431/477 product SQL (attendance backfill). Likely parallel seed race or leftover arma_id uniqueness collision in events IT fixtures.

Repro: intermittent under `bash scripts/platform/wave.sh gate <base>` with TBD_GATE_DB cold DB.

Cure: make seed_user idempotent on arma_id or unique arma_id per test worker; assert no shared snowflake collision across parallel tests.

missing=0
"""
priority = 2
main_goal = "Fix flaky events IT duplicate arma_id in seed_user; idempotent or per-worker unique."
context = [
    "Observed wave 29 cold re-gate FAIL once on fresh tbd_wave29b_cold.",
    "Not caused by T-431/477 product SQL (attendance backfill).",
    "Likely parallel seed race or arma_id uniqueness collision in events IT fixtures.",
]
requirement = [
    "Make seed_user idempotent on arma_id or unique arma_id per test worker.",
    "Assert no shared snowflake collision across parallel tests.",
]
current_state = [
    "event_orbat_registration_and_race panicked common/mod.rs:217 seed_user duplicate idx_users_arma_id.",
    "Immediate re-gate on fresh tbd_wave29c_cold PASS (33/40/13).",
    "Repro: intermittent under wave.sh gate with TBD_GATE_DB cold DB.",
]
approach = [
    "Make seed_user idempotent on arma_id or unique per test worker.",
    "Assert no shared snowflake collision across parallel tests.",
]
citations = [
    "wave 29 cold re-gate",
    "event_orbat_registration_and_race IT",
    "common/mod.rs seed_user",
]
shipped_at = "1ba9fb2c"
created_at = "2026-07-27T04:31:19Z"
completed_at = "2026-07-27T12:03:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/api/tests/common/mod.rs",
    "apps/website/api/tests/events.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
