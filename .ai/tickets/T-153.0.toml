id = "T-153.0"
kind = "work"
title = "T-153 — Faction Library (reusable factions + palette swap)"
summary = "T-153 — Faction Library (reusable factions + palette swap)"
class = "feature"
status = "shipped"
order = 1510
spec = "docs/specs/Mission_Creator_Architecture/t153_faction_library.md"
executor = "claude-code"
notes = "Backend: faction-library.schema.json + goldens/codegen; migration 0006 user_factions; /api/v1/factions CRUD (owner-scoped, contract-validated); tests T1-T8."
parent = "T-153"
main_goal = "Ship faction-library schema, user_factions migration, owner-scoped CRUD API."
context = ["T-074 intent; no reusable cross-mission faction storage."]
requirement = ["faction-library.schema.json; migration 0006; /api/v1/factions; tests T1–T8."]
current_state = ["Factions lived only per-mission; no contract-validated library backend."]
approach = ["Golden codegen; mission_maker+ routes; doc jsonb owner-scoped writes."]
verify = [
    "cargo xtask db test-it",
    "cargo xtask ci schema-codegen",
]
acceptance = ["Shipped faction-library schema, migration 0006, and owner-scoped /api/v1/factions CRUD; integration tests T1-T8 passed on fresh database."]
citations = [
    "t153_verify_log.md",
    "commit e2aec2de",
    "t153_faction_library.md",
    "Source: t153 verify log.",
]
shipped_at = "e2aec2de"
created_at = "2026-07-13T00:00:00Z"
completed_at = "2026-07-13T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-153 (day precision)"

[scope]
domain = "repo"
layer = "docs"
