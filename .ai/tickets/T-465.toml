id = "T-465"
kind = "work"
title = "T-447 Class-R false-green: draft filter + Content hydrate unexamined"
summary = "T-447 Class-R false-green: draft filter + Content hydrate unexamined"
class = "docs"
status = "shipped"
order = 3304
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Residual from wave 25 adversarial. Live T-447 behavior holds (SQL drafts+published, AdminUser, LocalResource hydrate), but Class-R pins are false-green:
(1) list_cms_announcements_is_drafts_plus_published_not_public_feed greends if status IN ('draft', 'published') appears anywhere in prod (incl. bait comment) while live SQL is published-only.
(2) content_boots_from_cms_list_not_mock_docs greends if LocalResource+api_get strings remain while Effect ignores opt and docs.set(hardcoded mock).
(3) AdminUser extractor not pinned; no IT asserts GET /cms/announcements returns a draft or 401 for non-admin.

Repro: published-only SQL + bait comment → Class-R PASS; Effect docs.set(mock) → Class-R PASS.

Cure: (A) IT POST draft then GET /cms/announcements must include it + public feed must not; non-admin GET → 401. (B) Harden Class-R: pin AdminUser on list handler; pin Effect docs.set(mapped) / filter_map(doc_from_announcement) chain so ignoring hydrate fails. RED→GREEN on verifier perturbations.

missing=0
"""
priority = 1
main_goal = "Harden T-447 Class-R pins for draft filter and Content hydrate."
context = [
    "Residual from wave 25 adversarial.",
    "Live T-447 behavior holds; Class-R pins false-green.",
]
requirement = [
    "IT POST draft then GET must include it; public feed must not.",
    "Non-admin GET /cms/announcements → 401.",
    "Pin AdminUser on list handler; pin Effect hydrate chain.",
]
current_state = [
    "published-only SQL + bait comment → Class-R PASS.",
    "Effect docs.set(mock) ignoring opt → Class-R PASS.",
]
approach = [
    "Harden Class-R AdminUser and Effect docs.set mapped chain.",
    "RED→GREEN on verifier perturbations.",
]
verify = [
    "cargo test -p website-api --test cms_announcement_body cms_list_includes_draft_public_feed_excludes_non_admin_forbidden",
    "cargo test -p website-frontend content_boots_from_cms_list_not_mock_docs",
]
acceptance = ["Hardened T-447 Class-R; IT proved draft on CMS list, excluded from public feed, 403 for non-admin."]
citations = [
    "wave 25 adversarial",
    "T-447 CMS announcements",
    "Content LocalResource hydrate",
    "cms_list_includes_draft IT",
]
shipped_at = "a37e9073"
created_at = "2026-07-27T02:27:02Z"
completed_at = "2026-07-27T02:33:27Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/cms.rs",
    "apps/website/api/tests/",
    "apps/website/frontend/src/content.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
