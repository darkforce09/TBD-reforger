id = "T-242"
kind = "work"
title = "Emit T-216 slot deltas through flatten"
summary = "The T-216 schema deltas (slot tag/callsign/rank/stance, group leaderSlotId, root vehicles $def) are all declared in mission.schema.json today, but flatten drops the four slot fields (DIAG_DROP_SLOT_*); emit them and pin with schema-validated goldens."
class = "feature"
status = "ready"
order = 7560
spec = "docs/specs/ideas/t242_t216_deltas.md"
plan = "docs/plans/t-242_plan.md"
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope.  | 2026-09-05 re-scope: schema side verified landed (slot callsign :411, rank :416, stance :429, tag :439; group leaderSlotId :312; vehicles :105); the residual is the flatten emit (flatten.rs:699-702 DIAG_DROP_SLOT_*). leaderSlotId emission belongs to T-674.1. apps/website/api/src/contract/generated/ is codegen (DO NOT EDIT) — verify = cargo xtask ci schema-codegen. mission.schema.json and flatten.rs are shared with T-936.x, T-299 and T-310 → later wave. Proposed scope schema/mission; note kept verbatim for the operator."
priority = 2
main_goal = "Authored slot tag, callsign, rank and stance reach the compiled document and validate."
context = [
    "Premise history: T-198 landed $defs/entityInventory (commit 2070eecdd); T-215 needed no schema change for vehicle cargo — do not re-brief off the stale summary.",
    "Anchor 2026-09-05: mission.schema.json declares slot callsign :411, rank :416, stance :429, tag :439, group leaderSlotId :312, vehicles :105; flatten.rs:699-702 drops the four slot fields.",
]
requirement = [
    "flatten emits slot tag, callsign, rank and stance when authored; the DIAG_DROP_SLOT_* diagnostics are retired.",
    "Goldens with the four fields validate against mission.schema.json and the codegen is regenerated, never hand-edited.",
    "leaderSlotId emission stays with T-674.1; this ticket does not touch it.",
]
current_state = ["The schema declares the deltas; the compile silently drops the four slot values, so authored rank, stance, callsign and tag never reach the mod."]
approach = [
    "Verify on main: a golden with rank set compiles without it; paste the diagnostic.",
    "Emit the four fields in the slot writer; update goldens; run schema-validate and schema-codegen.",
    "Perturbation: drop one field from the emit; the golden test goes red; restore, touch, green.",
]
verify = [
    "cargo test -p map-engine-core --all-features mission::flatten",
    "cargo xtask ci schema-validate",
    "cargo xtask ci schema-codegen",
    "cargo xtask platform wave gate --slice T-242",
]
acceptance = [
    "Authored slot tag, callsign, rank and stance appear in the compiled document and validate.",
    "No DIAG_DROP_SLOT_* diagnostic remains; generated contract files match the schema.",
]
created_at = "2026-09-05T03:30:00Z"
owns = [
    "crates/map-engine-core/src/mission/flatten.rs",
    "packages/tbd-schema/schema/mission.schema.json",
]

[scope]
domain = "schema"
layer = "mission"
