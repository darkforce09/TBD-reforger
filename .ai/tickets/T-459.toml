id = "T-459"
kind = "work"
title = "ticket advance-slice mutates without schema check preflight"
summary = "ticket advance-slice mutates without schema check preflight"
class = "feature"
status = "shipped"
order = 3298
executor = "claude-code"
priority = 2
main_goal = "Preflight require_check_ok in cmd_advance_slice before registry mutate when check is red."
context = [
    "Residual from T-455 / wave 21 adversarial NIT.",
    "Other cmds now call require_check_ok before write.",
]
requirement = [
    "cmd_advance_slice must call require_check_ok before save_registry + sync.",
    "Add Class-R or unit test mirroring add_refuses_invalid.",
]
current_state = [
    "cmd_advance_slice writes active_slice with no preflight while check is red.",
    "Repro: red in-memory registry → advance-slice still mutates.",
]
approach = ['Call require_check_ok(root, registry, format!("advance-slice {id}")) before mutate.']
citations = [
    "T-455 wave 21 NIT",
    "cmd_advance_slice preflight gap",
    "add_refuses_invalid mirror test",
]
shipped_at = "d87698b5"
created_at = "2026-07-27T01:10:49Z"
completed_at = "2026-07-27T01:26:03Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "xtask/src/cmds.rs",
    "xtask/src/check.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "check"
