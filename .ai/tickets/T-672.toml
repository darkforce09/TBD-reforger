__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "order",
    "route",
    "stream",
    "targets",
    "executor",
    "notes",
    "depends_on",
]
__ord = 639
depends_on = [
    "T-661",
    "T-664",
]
executor = "claude-code"
id = "T-672"
impact = [
    "ui",
    "state",
    "mc-authoring",
]
notes = "editor-UI program wave 17 (wave_plan.tsv row 117) — SHIPPED wave 117 (editor factory), 84a33a84; verifier wave117.md. SEE and CHECK shipped in full, one edge verb short, exactly as instructed: content-ordered stable listing + panel, and five rules (CONN-SELF/DANGLING/DUPLICATE/CYCLE/KIND) that fire, stay SILENT on a clean graph, and REPORT hydrated junk rather than dropping it. Connections are editor-only and never compile (T-651's absence pattern; evidence-led — the schema is additionalProperties:false over 19 keys with no relation collection). Delete cascades before remove_slots so no dangling edge can survive. The LMB-target half of CONN-START-001 is DEFERRED pending T-723 — see T-768. draw_order.rs untouched, so T-760's slot stays clean."
order = 4190
program = "eden"
route = "/missions/:id/edit"
status = "shipped"
stream = "mission-creator"
summary = """The connection-graph quarter of the split T-079. Ids: `CONN-START-001`, `CONN-SYNC-001`, `CONN-DEL-001`, `ACTION-FORM-001`, `CTX-FORMATION-001`. HARD DEPENDENCY on T-664 — two of those are context-menu items by definition. Code: drag-from-entity-to-entity is a third gesture mode in `onpointerdown` (mission_editor.rs:1395) / `onpointerup` :1644; link create / sync / delete mutators in editor_ops.rs; a link map in store.rs; menu items in context_menu.rs. THE RENDER LANE MAY ALREADY EXIST: `LaneRole::SquadLinks` (crates/map-engine-render/src/draw_order.rs:57) is "T-180.4 — squad leader->member hairline links (under slot rings)" and its doc comment :49 groups it with Contours / ForestOutline as a flat LineList lane — exactly the primitive a connection graph needs. Either reuse it and drop draw_order.rs from owns, or add a MissionConnections peer. WARNING carried from the framework corpus: `CONN-SYNC-001` is a bare `missing` row in gap_analysis.md with no note, and FNF v4's ENTIRE defect cluster lives on exactly this mechanism — the inspector and the validation rules must precede the edges. Do not ship edges you cannot see or check."""
surfaces = [
    "CONN",
    "MAP",
]
targets = ["website"]
title = "Connection graph: draw, sync, delete, formation"
