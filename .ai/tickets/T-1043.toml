id = "T-1043"
kind = "work"
title = "Remove dead frontend code: toolbelt shim, refile branch, unused helpers"
summary = "Delete dead code: BottomToolbelt (ui/docks/toolbelt/bottom_toolbelt.rs:7, re-exported at shell/eden_chrome.rs:18), orbat_refile branches full_dock.rs:149 always disables (single_row.rs:59-84,438-451), Fetched/ApiFailure (core/api/client/), personnel/role_dialog.rs:13-18,66-75, test-only cargo_defaults_by_character, and page_effects.rs:43-61's copy of arrange.rs's chord table. T-851 covers placed_vehicles_panel."
class = "chore"
status = "idea"
notes = "Mission Creator commands with no UI caller: apps/website/frontend/src/v2/apps/editor/shell/document_commands/imp/clipboard.rs:70 copy_grid_position_now, :95 copy_classnames_now, :125 copy_selection_summary_now; imp/mission_merge.rs:13 other_missions and :49 merge_mission_now (only shell/tests/document_commands/source_contracts.rs:160-177 reads it). apps/website/frontend/src/v2/apps/editor/input/tools/los_world_wasm.rs:328-341 installs window.__editorObjectWash 'so a headless probe can wait', and nothing in apps/ or tools_v2/ reads it (git grep ObjectWash); remove it or add the probe. Phase 5 doc writer F06 batch (paths under apps/website/frontend/src/v2/apps/editor): bridge/overlays/asset_picker.rs:8-13 AssetPickerState stores wx and wy (set at bridge/host_state/editor_context/installation.rs:103-110) and nothing reads them. shell/session.rs:80 read_warm has no Rust caller; only the __missionPersist.warm() window bridge reads it for a smoke probe (shell/persist/mount_persistence.rs:98-107), as session.rs:3 admits, so the warm-session marker serves only a test probe. mission_editor/transform.rs:52 snap_translate is uncalled too; T-1156 owns wiring it into the drag."
created_at = "2026-09-24T10:28:37Z"

[scope]
domain = "repo"
layer = "docs"
