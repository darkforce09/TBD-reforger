id = "T-814"
kind = "work"
title = "Transients survive external dialog open; Esc pile-up; open-order dismissal wrong"
summary = "Transients survive dialogs opened outside the strip; one Esc closes two layers; Esc dismisses the hidden dialog under the visual top"
class = "bug"
status = "shipped"
order = 4960
executor = "claude-code"
notes = """
wave 209; verifier wave200.md F4+F5; composes with T-813 field-Esc | SHIPPED W209: dialog registration fires transient closers on ANY open path; consume-aware Esc guard; open-order dismissal (Esc1 closes the visual top). Its O-3 wiring pin shipped DISARMED (stray #[test] orphaned the fn — verifier F1) — re-armed in-wave 27343f82, perturbation-proven RED on a literal-z-50 substitution. O-3/O-5 z-stacking unregressed.

--- HEAD migration_legacy (verbatim) ---

WAVE-200 VERIFIER RE-TICKET (wave200.md F4 MAJOR + F5 MINOR). F4 VERIFIED LIVE at single-keydown granularity: hint open -> canvas dblclick opens Attributes -> hint still up under the dialog (export dropdown same); then ONE Escape rawKeyDown closed BOTH the dialog AND the transient — the dialog's window keydown runs first, closes, and the strip's any_open() guard (eden_top_strip.rs:787) reads false in the SAME event: the wave-139-F3 pile-up resurrected, insertion-order-fragile by construction. close_transients (:758-767) only guards the strip's own three open paths. FIX: (a) central — every Dialog already registers in ui.rs modal_stack; make registration close transients (small transient-registry in ui.rs: the strip registers closers for menu/export/hint; dialog registration fires them). Do NOT edit attributes.rs or per-dialog open sites — hook the registration point. (b) consume-aware Esc: modal_stack marks 'a dialog consumed Escape for this event'; the strip's guard checks the mark, not live any_open(), so one press closes exactly one layer regardless of listener order. F5 VERIFIED LIVE: ORBAT (z-40, under) + Arsenal (z-50, top): Esc1 closed the HIDDEN ORBAT, Esc2 the arsenal — Esc dismisses by MOUNT order while z paints by OPEN order (ui.rs:466-474 vs :566-571): a visual no-op that silently discards context on the flagship O-3 pair. FIX: Esc closes the OPEN-ORDER topmost. One-layer-per-press stays absolute. Wave-139-era pins (T-333 family) pin mount-order — update HONESTLY to open-order without weakening 'one per press'/'no pile-up'; if an update would weaken a pin, stop and report. TRAPS: the context menu is not a Dialog and keeps its own dismissal; O-3 acceptance (elementFromPoint inside arsenal) and the O-5 pair must stay green; scrim-unreachability of z-50 ties must stay true; T-813 makes the FIELD-level Escape consume in attributes.rs — a focused-field Escape is the field's layer and composes with the consume-aware guard. ACCEPTANCE (verifier probes): hint -> canvas dblclick Attributes -> hint CLOSED (export same); Esc ladder closes exactly one surface per press in every dialog+transient combination; ORBAT -> OPEN ARSENAL -> Esc1 closes the ARSENAL, ORBAT stays, Esc2 closes ORBAT; O-5 pair (hint -> Save Version -> hint absent, 'Versions are immutable' present) and O-3 elementFromPoint still pass.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["WAVE-200 VERIFIER RE-TICKET (wave200.md F4 MAJOR + F5 MINOR). F4 VERIFIED LIVE at single-keydown granularity: hint open -> canvas dblclick opens Attributes -> hint still up under the dialog (export dropdown same); then ONE Escape rawKeyDown closed BOTH the dialog AND the transient — the dialog's window keydown runs first, closes, and the strip's any_open() guard (eden_top_strip.rs:787) reads false in the SAME event: the wave-139-F3 pile-up resurrected, insertion-order-fragile by construction. close_transients (:758-767) only guards the strip's own three open paths. FIX: (a) central — every Dialog already registers in ui.rs modal_stack; make registration close transients (small transient-registry in ui.rs: the strip registers closers for menu/export/hint; dialog registration fires them). Do NOT edit attributes.rs or per-dialog open sites — hook the registration point. (b) consume-aware Esc: modal_stack marks 'a dialog consumed Escape for this event'; the strip's guard checks the mark, not live any_open(), so one press closes exactly one layer regardless of listener order. F5 VERIFIED LIVE: ORBAT (z-40, under) + Arsenal (z-50, top): Esc1 closed the HIDDEN ORBAT, Esc2 the arsenal — Esc dismisses by MOUNT order while z paints by OPEN order (ui.rs:466-474 vs :566-571): a visual no-op that silently discards context on the flagship O-3 pair. FIX: Esc closes the OPEN-ORDER topmost. One-layer-per-press stays absolute. Wave-139-era pins (T-333 family) pin mount-order — update HONESTLY to open-order without weakening 'one per press'/'no pile-up'; if an update would weaken a pin, stop and report. TRAPS: the context menu is not a Dialog and keeps its own dismissal; O-3 acceptance (elementFromPoint inside arsenal) and the O-5 pair must stay green; scrim-unreachability of z-50 ties must stay true; T-813 makes the FIELD-level Escape consume in attributes.rs — a focused-field Escape is the field's layer and composes with the consume-aware guard. ACCEPTANCE (verifier probes): hint -> canvas dblclick Attributes -> hint CLOSED (export same); Esc ladder closes exactly one surface per press in every dialog+transient combination; ORBAT -> OPEN ARSENAL -> Esc1 closes the ARSENAL, ORBAT stays, Esc2 closes ORBAT; O-5 pair (hint -> Save Version -> hint absent, 'Versions are immutable' present) and O-3 elementFromPoint still pass."]

missing=0"""
main_goal = "Dialog registration closes transients; consume-aware Esc one layer; open-order topmost dismissal."
context = [
    "F4 hint survives canvas dblclick Attributes open one Esc closes dialog and transient pile-up.",
    "close_transients 758-767 only guards strip three open paths not external dialog opens.",
    "F5 ORBAT z-40 under Arsenal z-50 Esc dismisses mount order not visual open order.",
]
requirement = [
    "Dialog modal_stack registration fires transient closers menu export hint registry ui.rs.",
    "Consume-aware Esc modal_stack marks consumed strip guard checks mark not any_open.",
    "Esc closes open-order topmost one layer per press update T-333 pins honestly.",
]
current_state = [
    "eden_top_strip.rs 787 any_open guard reads false same event after dialog closes.",
    "ui.rs 466-474 mount order vs 566-571 open order z paints differently.",
    "Context menu not Dialog keeps own dismissal O-3 O-5 scrim ties must stay.",
]
approach = [
    "Hook registration point only do not edit attributes.rs per-dialog open sites.",
    "T-813 field Escape consume composes field layer first with consume-aware guard.",
    "Update wave-139 pins mount-order to open-order without weakening one per press.",
]
verify = [
    "Hint canvas dblclick Attributes hint CLOSED export same.",
    "Esc ladder one surface per press every dialog transient combination.",
    "ORBAT open ARSENAL Esc1 closes ARSENAL Esc2 ORBAT O-5 O-3 elementFromPoint pass.",
]
citations = [
    "wave200 F4 F5 verifier",
    "wave-139 F3 pile-up",
    "eden_top_strip 787 any_open",
    "T-333 mount-order pins",
    "T-813 field Esc compose",
]
shipped_at = "27343f82"
created_at = "2026-08-09T16:06:01Z"
completed_at = "2026-08-09T17:26:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/frontend/src/ui.rs",
    "apps/website/frontend/src/eden_top_strip.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["top_strip"]
