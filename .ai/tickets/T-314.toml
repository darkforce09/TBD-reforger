id = "T-314"
kind = "work"
title = "Nothing reads doc.kit_substitutions — the last mile of T-200"
summary = "Nothing reads doc.kit_substitutions — the last mile of T-200"
class = "bug"
status = "shipped"
order = 2314
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now.
"""
main_goal = "doc.kit_substitutions exists on compiled document but no caller surfaces it — last mile is tracing::warn! in get_compiled_mission."
context = ["T-200 attached a KitSubstitutionReport to the compiled document so the compiler stops silently swapping an authored character for the faction default (342 of 354 registry characters have no kit alias)."]
current_state = [
    "It rides the returned value with #[serde(skip)], so every existing caller already holds it and the /compiled wire contract is unchanged — pinned by a test asserting the top-level key",
    "set is exactly the schema's ten properties. But no caller surfaces it yet.",
    "The last mile is a tracing::warn! in get_compiled_mission (apps/website/api/src/handlers/missions.rs) reading doc.kit_substitutions.details(), which T-200 did not own. Until then the report exists and nobody sees it.",
    "details() returns the same shape as wire_safety::scan_editor_payload so a caller can render both identically.",
]
approach = ["tracing::warn! in get_compiled_mission reading doc.kit_substitutions.details()."]
citations = [
    "T-200 KitSubstitutionReport",
    "handlers/missions.rs get_compiled_mission",
    "wire_safety::scan_editor_payload",
]
shipped_at = "bd6cb399"
created_at = "2026-08-01T02:44:10Z"
completed_at = "2026-08-01T02:56:17Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "crates/map-engine-core",
    "crates/map-engine-render",
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/mission_hydrate.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "doc_store",
]
