id = "T-808"
kind = "work"
title = "Units render as role-and-side symbology, not dots"
summary = "Units render as role-and-side symbology, not dots"
class = "bug"
status = "shipped"
order = 4900
executor = "claude-code"
notes = """
depends_on T-790; T-157 visual-overhaul overlap cited in notes rather than absorbed (T-157 is broader) | operator note 2026-08-10: the T-790 procedural marker glyphs read "a little weird — not wrong, but weird"; when doing unit symbology, revisit marker glyph iconography in the same visual language | wave-205 verifier: slots render NO heading in any mode (lane yaw hardcoded 0) — unit symbology MUST carry orientation (T-832 is the finding; fold the yaw lane extension into this ticket, T-790 glyph-id lane as the template) | OPERATOR DIRECTION 2026-08-10 (symbology spec): slot glyph = circle WITH A FACING POINT (the point is the heading); vehicles = top-down SILHOUETTES per kind (a BTR looks like a BTR from above); rotation-widget usability depends on this — 'impossible to tell where you're rotating it' today | SHIPPED W207 (engine half + 3 completion passes): the operator's symbology language — slot disc with a FACING POINT, role knockouts (leader/medic/AT/MG), top-down vehicle silhouettes per kind, neutral comment bubble. T-832 DEAD (heading renders; 9-point sweep monotonic, wrap-continuous). Feeders wired in completion (3e19524f) off the id-sorted reader — column alignment held across delete/undo/drag mutations (verifier: 0.0 delta). A CLAMP-NOT-WRAP defect in the new code made headings 181-359 all draw south — caught by completion A, fixed 0bc55dc5; the same defect is live on the world lanes -> T-842. Marker glyphs rebuilt from the picker SVGs (the operator's 'weird'): zero border ink, anchor-honest centroids, 55/55 pairs distinct.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-10 — the map is dot soup: slots are plain circles (amber selected + heading arrow, pale blue otherwise), markers were identical circles (T-790 fixes markers), nothing distinguishes a rifleman from a medic from an AT gunner, and marker-vs-unit ambiguity was verified (operator + review agree; Eden's 2D map renders filled side-coloured entity glyphs — frame 170028 crops). A milsim author reads maps by symbology. FIX: per-slot glyph in the unit lane: side colour + role class glyph (minimum set: leader chevron, medic cross, AT, MG, rifleman default — derive role class from the slot role/kit string), distinct SHAPE class from markers (T-790's glyphs) and comments (T-796's bubble); selection ring/heading arrow render ON TOP unchanged. Consumes T-790's lane-format extension (crates/map-engine-core/src/slots_gpu.rs ~745-line instance path + crates/map-engine-render/src/engine.rs read half). ⚠️ TRAPS: engine.rs is CO-OWNED with T-790's wave — this lands 3 waves later on merged main, build on its final lane format (belt-and-braces per packing risk 5); zoom behaviour: glyphs legible at squad scale, degrade to dots only below a stated m/px; role-string mapping table lives in one place and is unit-tested (kit:us_rifleman -> rifleman etc.). ACCEPTANCE: wasm unit test on the role->glyph mapping (all seeded kits map, unknown -> default); scripted screenshot at 1.0 and 4.0 m/px: medic vs rifleman vs leader pixel signatures pairwise distinct; marker vs unit vs comment shapes pairwise distinct; selected unit still shows ring+heading.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-10 — the map is dot soup: slots are plain circles (amber selected + heading arrow, pale blue otherwise), markers were identical circles (T-790 fixes markers), nothing distinguishes a rifleman from a medic from an AT gunner, and marker-vs-unit ambiguity was verified (operator + review agree; Eden's 2D map renders filled side-coloured entity glyphs — frame 170028 crops). A milsim author reads maps by symbology. FIX: per-slot glyph in the unit lane: side colour + role class glyph (minimum set: leader chevron, medic cross, AT, MG, rifleman default — derive role class from the slot role/kit string), distinct SHAPE class from markers (T-790's glyphs) and comments (T-796's bubble); selection ring/heading arrow render ON TOP unchanged. Consumes T-790's lane-format extension (crates/map-engine-core/src/slots_gpu.rs ~745-line instance path + crates/map-engine-render/src/engine.rs read half). ⚠️ TRAPS: engine.rs is CO-OWNED with T-790's wave — this lands 3 waves later on merged main, build on its final lane format (belt-and-braces per packing risk 5); zoom behaviour: glyphs legible at squad scale, degrade to dots only below a stated m/px; role-string mapping table lives in one place and is unit-tested (kit:us_rifleman -> rifleman etc.). ACCEPTANCE: wasm unit test on the role->glyph mapping (all seeded kits map, unknown -> default); scripted screenshot at 1.0 and 4.0 m/px: medic vs rifleman vs leader pixel signatures pairwise distinct; marker vs unit vs comment shapes pairwise distinct; selected unit still shows ring+heading."]

missing=0
"""
main_goal = "Replace slot dots with side-coloured role-class unit symbology on the map."
context = [
    "F-10 dot soup: slots plain circles; rifleman medic AT indistinguishable on map.",
    "Eden renders side-coloured entity glyphs frame 170028; milsim authors read symbology.",
    "Depends T-790 marker glyphs and lane-format extension in slots_gpu engine.rs.",
]
requirement = [
    "Per-slot glyph: side colour plus role class leader medic AT MG rifleman default.",
    "Distinct shape from T-790 markers and T-796 comment bubble; ring heading on top.",
    "Role-string mapping table one place unit-tested; zoom legible degrade dots below m/px.",
]
current_state = [
    "Slots plain circles amber selected pale blue; marker unit ambiguity verified.",
    "engine.rs co-owned T-790 wave; build on final lane format three waves later.",
    "Lane yaw hardcoded zero; heading orientation folded from T-832 finding.",
]
approach = [
    "Extend slots_gpu instance path and engine.rs read half from T-790 format.",
    "Slot disc facing point; vehicle top-down silhouettes per kind operator direction.",
    "Role knockouts leader medic AT MG; rebuild marker glyphs from picker SVGs.",
]
verify = [
    "Wasm unit test role to glyph mapping all seeded kits unknown default.",
    "Screenshot 1.0 and 4.0 m/px medic rifleman leader pairwise distinct signatures.",
    "Marker unit comment shapes pairwise distinct; selected unit ring plus heading.",
]
citations = [
    "editor_hostile_ux_review F-10",
    "Eden frame 170028 entity glyphs",
    "T-790 lane format extension",
    "T-796 comment bubble shape",
]
shipped_at = "c6b875de"
created_at = "2026-08-10T15:50:25Z"
completed_at = "2026-08-11T00:00:48Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 11 commit subject(s)"
owns = [
    "crates/map-engine-core/src/slots_gpu.rs",
    "crates/map-engine-render/src/engine.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
