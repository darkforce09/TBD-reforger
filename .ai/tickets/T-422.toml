id = "T-422"
kind = "work"
title = "Fix gate_schema exclusion tripwire stamp"
summary = "gate_schema shipped with three defects: a wrongly-excluded green gate, a silently-narrowing tripwire, and an incomplete stamp"
class = "bug"
status = "shipped"
order = 3261
executor = "claude-code"
priority = 1
main_goal = "Fix gate_schema height-labels exclusion and Makefile tripwire; complete content stamp."
context = [
    "Wave 5 T-420 added first schema step the wave gate ever had.",
    "T-421 touch_workspace closes defect 3 mtime path; stamp still incomplete.",
    "Verifier reverted map-object-enums on main: schema FAIL non-vacuous end to end.",
]
requirement = [
    "Per-context height-labels inclusion for gate_slice vs flat exclusion list.",
    "Tripwire must compare SET not count zero — catch blank-line partial parses.",
    "Stamp must hash path-dep trees tbd-tools and map-engine-core.",
]
current_state = [
    "wave.sh:715-725 excludes height-labels; main DEM 71MB PNG rc=0; worktree LFS pointer.",
    "wave.sh:747-756 tripwire: blank line -> sees 3 of 9; continuation -> 8 of 9.",
    "GATE_SCHEMA_TARGET stamp: T-215/T-216 same hash while map-engine-core differs.",
    "ci.yml:133,135 runs validate+citations only; make schema-validate runs nine.",
]
approach = [
    "Remediation comment sends maintainer after git-lfs problem absent on main.",
    "GATE_SCHEMA_GATES covers nine minus declared exclusion; no sub-gate dropped.",
    "Do not copy GATE_SCHEMA_TARGET stamp as model for T-421.",
]
verify = ["main: cargo xtask schema height-labels rc=0; terrain-alignment maxDeltaM=0.204."]
citations = [
    "T-420 schema step wave gate",
    "T-421 touch_workspace mtime",
    "make schema-validate nine sub-gates",
    "PLATFORM_FACTORY testimony rule",
]
shipped_at = "2de6ca57"
created_at = "2026-07-26T21:49:30Z"
completed_at = "2026-07-26T21:54:40Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
