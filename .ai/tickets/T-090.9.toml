id = "T-090.9"
kind = "work"
title = "T-090.9 — World-object interaction (hover, inspect, filter, legend)"
summary = "T-090.9 — World-object interaction (hover, inspect, filter, legend)"
class = "feature"
status = "ready"
order = 900
spec = "docs/specs/Mission_Creator_Architecture/t090_9_world_object_interaction.md"
executor = "claude-code"
parent = "T-090"
user_story = "Static world objects become read-only context you can interrogate — hover for a tooltip, click for a read-only inspect panel with Ask AI about this object, filter/search by taxonomy, a legend, and a Z-trust badge — without ever moving them."
acceptance = [
    "Hover tooltip, click inspect, filter, legend, Z-trust badge.",
    "Move/delete/edit terrain props remain Workbench-only.",
]
estimated = ["scope"]
owns = [
    "apps/website/frontend/src/world_assets",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
