id = "T-513"
kind = "work"
title = "Class-R ignores comment bait for require_role_played"
summary = "W44 hotfix: T-506 Class-R must not count comment bait for require_role_played"
class = "chore"
status = "shipped"
order = 3353
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Wave 44 verifier MAJOR. ingest_match_results_invokes_require_role_played_at_both_sites whitespace-collapses include_str and counts require_role_played(&p.role_played) inside // comments. Mutation: delete UPSERT bind, leave // require_role_played(&p.role_played) → PASS with one live call; delete both live calls + two bait comments → PASS. Cure: strip line comments (and block comments if needed) from the extracted production body before counting, or otherwise refuse comment-only matches. Perturbation RED with bait must fail; bare live calls still green.

missing=0"""
priority = 0
main_goal = "Strip comments before counting require_role_played in Class-R test."
context = [
    "Wave 44 verifier MAJOR on telemetry ingest Class-R.",
    "Test whitespace-collapses include_str and counts inside comments.",
]
requirement = [
    "Comment bait require_role_played passes with one live call deleted.",
    "Both live calls deleted plus bait comments still passes.",
]
current_state = ["Mutation: delete UPSERT bind; comment bait alone still PASS."]
approach = ["Strip line and block comments from production body before counting."]
verify = ["Perturbation RED with bait must fail; live calls green."]
acceptance = [
    "telemetry Class-R stripped Rust comments before counting require_role_played sites.",
    "Comment bait require_role_played references no longer false-greened ingest pin.",
]
citations = [
    "Wave 44 verifier MAJOR",
    "ingest_match_results Class-R test",
    "require_role_played comment bait",
    "telemetry.rs include_str collapse",
]
shipped_at = "8b2b6272"
created_at = "2026-07-27T10:00:54Z"
completed_at = "2026-07-27T10:06:52Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/api/src/handlers/telemetry.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
