id = "T-809"
kind = "work"
title = "Asset browser: one tree per faction spanning characters and vehicles, plus recently-placed"
summary = "Asset browser: one tree per faction spanning characters and vehicles, plus recently-placed"
class = "bug"
status = "shipped"
order = 4910
executor = "claude-code"
notes = "Eden evidence frames 165952/170028; T-146 absorbed — registry note added by this batch | owns widened +eden_tree.rs (free in wave 203) for the outliner listing; fold per operator instruction 2026-08-10 | SHIPPED W203: merged per-faction tree (vehicle leaves under NATO, drag-places), recently-placed as Favourites|History pair — completion 87e1ff94 added the recorder seam so composition stamps (one titled entry per stamp) and ORBAT Add Vehicle feed it too; FOLD delivered: placed vehicles listed in the left outliner (selectable by doc-presence), Vehicles tab kept as the crew-editor stopgap (dies with T-818/W210); ORBAT picker data path verified independent. Cause-probe nuance under headless -> T-820; outliner dblclick bubble (pre-existing, widened) -> T-822; OBJ count -> T-823."
estimated = ["scope"]
migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-22 — EDEN-VERIFIED (frame 165952): Eden's F1 Objects is ONE tree per faction — under NATO sit Anti-Air, APCs, Artillery, Boats, Cars, Drones, Helicopters, Men, Planes, Tanks, Turrets — plus an Assets|History tab pair where History is the recently-placed list. TBD splits the same material across Factions / Vehicles / Objects tabs, so the author must know TBD's filing system before finding anything (a question about the tool, not the mission), and TBD has only manual Favourites, nothing automatic. FIX: (a) the Factions tree gains kind-level children per faction (characters + vehicles at minimum; objects when catalogued) so a vehicle leaf is reachable inside its faction; keep the Vehicles tab as a filtered view or retire it — one behaviour, stated; (b) add a recently-placed rail or subtab (most-recent-first, session-scoped is fine) alongside Favourites. ABSORBS the open T-146 'Asset Browser Data Wiring' scope — note the absorption in T-146 rather than double-building. ⚠️ TRAPS: search must span the merged tree (search-prefix syntax class:/mod: kept); side chips keep filtering the one tree; asset_catalog.rs is shared with T-810 in a later wave — keep catalog-shape changes minimal and additive; drag-to-place from the new vehicle leaves must reuse the existing palette-drag path. ACCEPTANCE: scripted with seeded vehicles (T-800): a vehicle leaf is reachable under NATO subtree and drag-places (OBJ+1 as a vehicle); placing any asset puts it at the head of recently-placed; search 'class:' prefix still filters; Favourites star still works from the merged rows. ⟡ OPERATOR EYE-PASS FOLD (wave-202 pass, 2026-08-10): placed vehicles do NOT belong in this dock at all — placed things live in the LEFT outliner. While rebuilding the panel: (c) list placed vehicles in the left outliner (eden_tree.rs — owns widened for this) alongside slots/groups, selectable, same row affordances the outliner gives slots; (d) KEEP the right-dock Placed strip's inline crew editor functioning THIS wave (deliberate stopgap — T-818/W210 rehomes the editor into vehicle Attributes and deletes the strip; removing it here would leave crew editing homeless for a wave). The strip's LISTING role is superseded by (c); its editor role survives one more wave."]
owns = [
    "apps/website/frontend/src/eden_dock_right.rs",
    "apps/website/frontend/src/asset_catalog.rs",
    "apps/website/frontend/src/eden_tree.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "dock_right",
    "asset_browser",
    "outliner",
]
