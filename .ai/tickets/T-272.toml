id = "T-272"
kind = "work"
title = "Server-status producer — close the SSE loop"
summary = "SSE connected but delivered zero frames; poll server_statuses and republish so /status/stream works without a game-server bridge."
class = "feature"
status = "shipped"
order = 2900
executor = "claude-code"
context = [
    "realtime hub, stream route and sse client were wired.",
    "Only publisher was POST /ingest/server-status; nothing else called it.",
    "Stream flipped connected=true and delivered zero frames forever.",
]
requirement = [
    "Poll server_statuses on boot and on an interval.",
    "hub.publish the same ServerStatus JSON ingest already fans out.",
    "Add SERVER_STATUS_PUBLISH_INTERVAL_SECS env defaulting to ten seconds.",
]
current_state = [
    "SSE hub and /status/stream client connected but ingest POST was the sole publisher.",
    "Connected clients received zero frames until a republisher polled server_statuses.",
]
approach = [
    "Added publish_all_server_statuses boot tick and interval scheduler in realtime.rs.",
    "Pinned SERVER_STATUS_PUBLISH_INTERVAL_SECS default ten seconds in .env.example.",
]
verify = ["realtime::tests scheduler_publishes_on_boot_and_interval passes."]
acceptance = ["SSE /status/stream received frames from polled server_statuses without ingest-only bridge."]
citations = [
    "commit 5e55081f SSE republisher",
    "wave 15 close 4e340548",
    "realtime boot interval publisher",
]
shipped_at = "31361970"
created_at = "2026-07-26T23:53:02Z"
completed_at = "2026-07-26T23:59:18Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/api/src/realtime.rs",
    "apps/website/frontend/src/sse.rs",
    "apps/website/api/src/bin/api.rs",
    "apps/website/api/src/handlers/telemetry.rs",
    "apps/website/api/.env.example",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
