id = "T-090.11.3"
kind = "work"
title = "T-090.11.3 — Workbench oracle: recon children, door-inclusive parity"
summary = "Recon dump records localPos/anglesDeg/pivotId/meshPath/DoorComponent params per child; parity sampling with doors + glass included (closed); instances-verify pins every xobSocket transform within 2 cm / 1 deg with recon as fallback; rotation-order test."
class = "feature"
status = "ready"
order = 900
spec = "docs/specs/Mission_Creator_Architecture/t090_091_map_terrain_program.md"
plan = "docs/plans/t-090_11_3_plan.md"
executor = "claude-code"
parent = "T-090"
depends_on = ["T-090.11.2"]
main_goal = "The offline socket/placement decode is proved against the engine, and a door-inclusive LOS oracle exists for the compound model."
context = [
    "Plan of record: ~/.claude/plans/implementation-plan-tender-seahorse.md (operator-approved 2026-09-03); Cursor authors docs/specs/Mission_Creator_Architecture/t090_11_architectural_interiors.md from it.",
    "New Workbench plugin params need an operator Script Editor compile (wb_reload cannot); after that the agent drives recon2 / parity-doors over cargo xtask mcp call.",
    "If Workbench is unavailable the script changes still land and the two fixtures are reported missing — never silently dropped.",
]
requirement = [
    "TBD_BlueprintReconPlugin.c: per child localPos, anglesDeg, pivotId, meshPath, AngleRange/ClosedAngle; TBD_BuildingTraceExtract.ExecuteParity includeDoors flag; EMCP_WB_TbdBlueprint actions recon2 + parity-doors.",
    "cargo xtask map instances-verify --instances --recon: every xobSocket instance within 2 cm / 1 deg of its recon child; --sockets-from-recon re-emit path.",
]
current_state = ["Recon dumps only root-relative position + angles + bounds; the 400-pair oracle excludes door entities and glass panes."]
approach = ["Extend the three Workbench scripts; run both actions on FarmHouse_E_1L01_Wood; commit the dumps as xtask fixtures; pin the yaw/pitch/roll composition with the tilted furniture children."]
verify = [
    "cargo xtask map instances-verify --instances packages/map-assets/everon/prefabs/buildings/FarmHouse_E_1L01_Wood.instances.json --recon xtask/tests/fixtures/FarmHouse_E_1L01_Wood_children.json",
    "cargo test -p xtask",
]
acceptance = [
    "All 25 farmhouse sockets match recon within tolerance (or the mismatch table is in the commit body and instances fall back to recon).",
    "FarmHouse_E_1L01_Wood_children.json and FarmHouse_E_1L01_Wood_parity_doors.json committed (400 pairs, doors closed).",
]
owns = [
    "apps/mod/tbd-export/Scripts/WorkbenchGame/MapExport/Objects/Buildings",
    "apps/mod/tbd-export/Scripts/WorkbenchGame/EnfusionMCP/EMCP_WB_TbdBlueprint.c",
    "xtask/tests/fixtures/FarmHouse_E_1L01_Wood_children.json",
    "xtask/tests/fixtures/FarmHouse_E_1L01_Wood_parity_doors.json",
]
created_at = "2026-09-03T14:49:12Z"

[scope]
domain = "mod"
layer = "scripts"
