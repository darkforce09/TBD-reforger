id = "T-685"
kind = "work"
title = "Zone volumes: height bounds capture counts and owner"
summary = "Zones become volumes: min/max height, attacker and defender counts, starting owner"
class = "feature"
status = "queued"
order = 4400
executor = "claude-code"
notes = "workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row. Filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md)"
main_goal = "Ship Enfusion zone volume reader: min/max height, capture/defender counts, owner."
context = [
    "U1 framework_synthesis C.2 ranked ten of sixteen; WOG WMT_Task_Point evidence.",
    "TBD zones today are 2D circle or polygon; shape carries no height.",
    "Schema widening split T-706; ticket is Enfusion reader plus editor UI.",
]
requirement = [
    "Min/max height volume semantics; CaptureCount DefCount Owner from WOG evidence.",
    "Preserve TBD zoneRules neutralizeSeconds onEmpty decayRate pauseOnEnemy resetOnEnemy.",
    "Editor zone inspector: three numeric fields and side picker for volume attrs.",
    "Do not touch packages tbd-schema; T-706 owns widening pass.",
]
current_state = [
    "WOG semantics INFERRED; wmt_main addon absent from corpus.",
    "zoneRules additionalProperties false; new keys need TBD_MissionLoader field.",
    "Executor claude-code; headless compile.sh verified no Workbench GUI.",
]
approach = [
    "Design volume semantics as TBD question; do not copy inferred WOG sentence.",
    "Implement reader in TBD_ZoneVolume.c plus ObjectiveRegistry resolve branch.",
]
verify = [
    "Headless mod compile clean with zone volume reader in owns path.",
    "Zone inspector shows min max height and capture defender counts when UI lands.",
]
citations = [
    "framework_synthesis U1 WMT_Task_Point",
    "wog.md min height invariant",
    "T-706 schema widening split",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Zones/TBD_ZoneVolume.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "zones"
