__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "order",
    "executor",
    "priority",
    "stream",
    "targets",
]
__ord = 517
executor = "claude-code"
id = "T-543"
impact = [
    "infra",
    "dx",
]
order = 3383
priority = 2
program = "platform"
status = "shipped"
stream = "web-platform"
summary = """
FOUND by W54 adversarial verifier (CLEAN MINOR-NIT) after T-328.

mcp-daemon.sh correctly builds/execs from target-dev-mcpd and discards mcpd-bin.sh stdout. Residual footgun: scripts/mod/lib/mcpd-bin.sh still hardcodes echo $ROOT/target/debug/mcpd; mcp-call-selftest.sh still consumes that echo. Cure: honor CARGO_TARGET_DIR in mcpd-bin.sh echo path.

Repro: rg 'target/debug/mcpd' scripts/mod/"""
surfaces = [
    "INFRA",
    "MOD",
]
targets = ["website"]
title = "mcpd-bin.sh still echoes shared target/debug/mcpd path"
