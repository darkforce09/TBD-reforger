id = "T-212"
kind = "work"
title = "Typed per-side objectives with attributes"
summary = "Objectives as typed, placed, per-side entities with one uniform attribute spine"
class = "bug"
status = "queued"
order = 4430
executor = "claude-code"
notes = 'workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row. EXTENDED (not superseded) 2026-08-01 from framework_synthesis.md B5 by the coverage audit (parity/coverage_audit.md); previous shape was `idea` / claude-code / order 2300 / targets [website]. T-853 2026-08-12: the prepped slice worktree for this ticket was DROPPED, not landed. It carried one unmerged commit from 2026-08-02 that had sat unlanded for 10 days and was blocking the wave queue at 150. The work was NOT reviewed and NOT merged; the ticket stays queued so it can be redone. The commit is recoverable: `git checkout -b redo/T-212 salvage/t853-dropped/T-212` (tagged before the branch was deleted). Note T-674 and T-675 pointed at the SAME commit 113108a1, so whichever is redone first should check whether it subsumes the other. EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code. SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema. THE SPA AUTHORING UI IS DELIBERATELY NOT SPLIT OUT YET — it cannot be scoped until the shape is decided, and pre-filing it would be a duplicate. Split it as a `claude-code` follow-on with its own `owns` once this lands, the way T-076/T-675 and T-079/T-676 are split.'
depends_on = [
    "T-685",
    "T-241",
    "T-257",
]
main_goal = "objectivesById exists in the doc and compiles into the payload but has no mutator and no control — a dead container."
context = [
    'EXTENDED 2026-08-01 from the framework research (framework_synthesis.md Decision 1 / C.3 B5, ranked 13 of 16 — "Very high value, Large effort, own program"). The original row predated the research',
    "from the corpus. TWO CORRECTIONS TO THE OLD RECORD, both checked against the live tree today: (a) the cited line numbers have moved — the container is `crates/map-engine-core/src/doc/store.rs`:351 and the",
    "/ meta / environment / factions / orbat / slots / radioPlan / zones / entities / layers / flow / winConditions / briefings / settings) and the root is",
    "{`objective_capture`, `objective_destroy`, `objective_hold_until`} and drives `winConditions.endOn` in {`all_objectives_captured`, `objective_destroyed`, `hold_expired`}. So this ticket is a decision about which of the two shapes wins, not a UI to hang on the",
    'dead one. THE CORPUS ANSWER, and it is the strongest architectural convergence in the research: FNF v4 and WOG independently arrived at "an objective is a typed, placed, parameterised entity',
    'with a uniform attribute spine", from opposite starting points — FNF from a scripted nine-fixed-modes ancestor it rewrote away, WOG apparently from scratch (fnf_v4.md 7, wog.md 7 / 13.1; wog.md',
    "corpus — 166 instances across 73 missions, every one carrying all 15 parameters (wog.md:382-402) — and the parameters TBD lacks are attacker count (`_CaptureCount` 4x128 / 1x16 / 2x6 /",
    "3x5), defender count (`_DefCount` 3x129 / 1x17 / 2x8 / 0x5), starting owner (`_Owner` 1x70 / 0x65 / 2x30), advantage percent (`_AdvantagePercent` 2x72 / 0x67 / 4x14 / 3x13), `_Lock`",
    "(1x136), `_AutoLose` (-1x156), and the pair that makes a zone a VOLUME rather than a disc: `_MinHeight` = -5 INVARIANT ACROSS ALL 166 and `_MaxHeight` 30x137 / 15x16 / 20x13",
    "every objective reads differently to attacker and defender, and v4 generates genuinely different task text, titles and separate My Tasks / Ally Tasks trees from it (fnf_v4.md 7). The implementation",
    """CaptureCount attackers are inside the marker's area between MinHeight and MaxHeight for Timer seconds while fewer than DefCount defenders contest it" — because `wmt_main`, the addon supplying every `WMT_*` module,""",
    "must not enter an acceptance criterion as fact. EXECUTOR IS `workbench` DELIBERATELY (changed from claude-code): whichever shape wins, this is a `packages/tbd-schema/schema/mission.schema.json` change against a root that is `additionalProperties: false`",
    "plus Enfusion work in `apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. framework_synthesis.md D.5 puts B5 in the mission-content program for exactly this reason.",
    "THE SPA AUTHORING UI IS DELIBERATELY NOT SPLIT OUT YET — it cannot be scoped until the shape is decided, and pre-filing it would be a duplicate. Split it as",
    "a `claude-code` follow-on with its own `owns` once this lands, the way T-076/T-675 and T-079/T-676 are split. OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench SCHEMA HALF",
    "SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.",
]
requirement = [
    """13.1 notes a capture-and-hold mission needs ZERO lines of script, "that is why the median WOG description.ext is empty"). TAKE WOG's SPINE. `WMT_Task_Point` is the best-evidenced objective design in the""",
    "as T-685 so it can ship against the existing zone types without waiting for this row.) TAKE FNF v4's PER-SIDE FRAMING, AND IMPLEMENT IT DIFFERENTLY. The insight is real —",
    "TWO FRAMINGS — not two modules sharing a target. TBD's stable `uid` + derived `id` already makes v4's positional-identity defect unreachable; do not regress that. MAKE THE SCRIPTING HATCH ADVANCED",
    "AND OPTIONAL: WOG's `_Condition` is `true` in 165 of 166 `Task_Point`, 71 of 71 `CapturePoint`, 14 of 14 `Destroy` and 7 of 7 `Compose` (wog.md:402, 15.5). Ship the declarative parameters",
    'first-class; put any condition or hook behind advanced disclosure. CARRY THE CAVEAT AND DO NOT LAUNDER IT: wog.md:379-381 and :403-406 mark the SEMANTICS `INFERRED:` — "a zone is captured when',
]
current_state = [
    'and read "objectivesById exists in the doc and compiles into the payload but has no mutator and no control — a dead container"; that is kept and CORRECTED, then scoped',
    "emit is `crates/map-engine-core/src/mission/compile.rs`:228, not :174 / :91; (b) more importantly, :228 sits inside `compile_payload`, the EDITOR SUPERSET — `objectives` is NOT a top-level property of `packages/tbd-schema/schema/mission.schema.json` (the 14 are schemaVersion",
    "`additionalProperties: false`, and `crates/map-engine-core/src/mission/flatten.rs` contains ZERO occurrences of the word. The container does not reach the mod at all. What DOES work today is objectives-as-zones: `TBD_ObjectiveRegistry.c`:36-42 keys on `zone.type` in",
    "— read at wog.md:405-410 as deliberately including basements and excluding aircraft. TBD's `$defs/shape` is `oneOf` circle-or-polygon and carries no height at all. (The volume + counts half is filed separately",
    'is bad in four documented ways (fnf_v4.md 14.4): identity is POSITIONAL (modules sorted by zero-padded X/Y/Z, so moving one renumbers it), two modules are "the same objective" only because they',
    "happen to sync to the same object or share a prefix string, NOTHING VALIDATES THAT BOTH HALVES EXIST, and the only tell is the lobby tool dividing every objective count",
    'by 2 — forget the second module and the count comes out fractional. fnf_v4.md:943 says it in its own steal-list: "Make it one object with per-side framing." So: ONE ENTITY,',
    "IS ENTIRELY ABSENT FROM THE CORPUS (wog.md:88-95: not in `extracted/`, not in `pbo/`). Parameter NAMES and observed VALUES are hard evidence; the reading of what they do is inference and",
]
citations = [
    "framework_synthesis.md B5",
    "store.rs:351",
    "compile.rs:228",
    "mission.schema.json",
    "flatten.rs",
    "TBD_ObjectiveRegistry.c:36-42",
    "wog.md:382-402",
    "fnf_v4.md 7",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Objectives/TBD_ObjectiveRegistry.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "objectives"
