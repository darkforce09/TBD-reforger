id = "T-261"
kind = "work"
title = "Leaderboard materialized view has no scheduled refresh"
summary = "API boot arms immediate plus interval leaderboard MV refresh; default interval fifteen minutes."
class = "chore"
status = "shipped"
order = 2790
executor = "claude-code"
context = [
    "refresh_leaderboard had exactly one caller inside results ingest.",
    "Without ingest traffic leaderboard_totals stayed empty or frozen at migration WITH NO DATA.",
    "No cron, interval, or admin refresh path existed.",
]
requirement = [
    "Schedule leaderboard MV refresh on API boot.",
    "Run an immediate refresh plus interval loop via LEADERBOARD_REFRESH_INTERVAL_SECS.",
    "Keep in-request refresh callers for telemetry and me endpoints.",
]
current_state = [
    "refresh_leaderboard had exactly one caller inside results ingest.",
    "Without ingest traffic leaderboard_totals stayed empty or frozen at migration WITH NO DATA.",
]
approach = [
    "db.rs scheduler runs immediate refresh plus interval loop on API boot.",
    "LEADERBOARD_REFRESH_INTERVAL_SECS env pins default fifteen-minute cadence.",
]
verify = ["db unit: scheduler_invokes_refresh_on_boot_and_interval."]
acceptance = ["Leaderboard MV refreshed on boot and interval; ingest callers retained."]
citations = [
    "commit c1f587be boot scheduler",
    "wave 12 close 22226505",
]
shipped_at = "22226505"
created_at = "2026-07-26T23:07:56Z"
completed_at = "2026-07-26T23:24:56Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/src/db.rs",
    "apps/website/api/src/bin/api.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
