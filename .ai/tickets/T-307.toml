id = "T-307"
kind = "work"
title = "NULL briefing 500s GET /events/:id Event Hub"
summary = "GET /events/:id 500s on a NULL mission briefing and takes the Event Hub down"
class = "feature"
status = "shipped"
order = 2307
executor = "claude-code"
main_goal = "COALESCE nullable briefing/thumbnail_url in get_event so NULL mission rows do not 500 the entire Event Hub."
context = [
    "Found by T-194 while seeding the content golden.",
    "INDEPENDENTLY REPRODUCED 2026-07-26 by T-325's sweep, which reached it from the other direction (nullable column vs non-Option model).",
]
requirement = ["Fix the dossier lookup in handlers/events.rs get_event — the one mission query in that file that does not COALESCE the nullable briefing/thumbnail_url, briefing/thumbnail_url."]
current_state = [
    "A NULL decodes into String and the whole endpoint fails:",
    "'error occurred while decoding column 3: unexpected null'.",
    "That is the entire Event Hub, not one card.",
    "The API's own create path always writes '', so only seeded rows or hand-written SQL reach it —",
    "which is exactly what a fresh environment or a restored backup looks like.",
    "T-194's seed writes '' to match the API and documents the workaround at the point of use; the handler is still wrong.",
    "Captured error at handlers/events.rs:832: `decoding column 3` (= briefing).",
    "Latent on the operator's DB because `missions` has no NULLs there —",
    "BUT `seeds/mock_data.sql:25` omits `thumbnail_url` from its INSERT column list,",
    "so ANY freshly-seeded database reproduces it. That is why it looked unreachable and is not.",
]
approach = ["Closed as duplicate of T-340; COALESCE dossier shipped there."]
verify = ["Closed by T-340; enumerator prune in 01d5225f3."]
acceptance = ["Closed as duplicate of T-340; Event Hub NULL survival shipped on T-340."]
citations = [
    "commit a01996010 closed as T-340",
    "T-194 seed golden",
    "T-325 nullable sweep",
    "handlers/events.rs:832",
    "seeds/mock_data.sql:25",
]
shipped_at = "d223ae3b"
created_at = "2026-07-26T05:22:48Z"
completed_at = "2026-07-26T08:08:36Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 2 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
