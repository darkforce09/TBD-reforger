id = "T-839"
kind = "work"
title = "Retire floating Select/Ruler/LoS bottom-centre pill"
summary = "The floating Select/Ruler/LoS pill still exists — decision 1 said it dies"
class = "bug"
status = "queued"
order = 5210
executor = "claude-code"
notes = """
eye-pass catch wave-205; decision-1 completion; strip+pill files — pack owns-aware

--- HEAD migration_legacy (verbatim) ---

EYE-PASS SCREENSHOT CATCH (wave-205, 2026-08-10): the floating bottom-centre Select/Ruler/LoS pill is still rendered — operator decision 1 (review section 8) retired it ('tools live Arma-style in the fixed chrome') and the traceability routed it to T-797/T-798, but NEITHER ticket's acceptance carried the pill removal — it fell between them (the input-never-reached-output class). FIX: move Ruler and LoS-ray into the row-2 toolbar as a tools group beside the widget cluster (BTN_ICON + chord tooltips + census rows for their chords if any); Select is the digit-1/No-Widget mode (T-835 gives it the arrow glyph) — no separate button needed; DELETE the floating pill component + its mount; Backspace hide-set shrinks accordingly. TRAPS: Ruler/LoS arming machinery (T-642/T-643 lineage) unchanged — only the BUTTONS move; the pill's pins (if any) update honestly. ACCEPTANCE: no floating pill in any mode; Ruler + LoS arm from row-2 buttons with tooltips; their gestures work unchanged; Backspace still hides all chrome.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["EYE-PASS SCREENSHOT CATCH (wave-205, 2026-08-10): the floating bottom-centre Select/Ruler/LoS pill is still rendered — operator decision 1 (review section 8) retired it ('tools live Arma-style in the fixed chrome') and the traceability routed it to T-797/T-798, but NEITHER ticket's acceptance carried the pill removal — it fell between them (the input-never-reached-output class). FIX: move Ruler and LoS-ray into the row-2 toolbar as a tools group beside the widget cluster (BTN_ICON + chord tooltips + census rows for their chords if any); Select is the digit-1/No-Widget mode (T-835 gives it the arrow glyph) — no separate button needed; DELETE the floating pill component + its mount; Backspace hide-set shrinks accordingly. TRAPS: Ruler/LoS arming machinery (T-642/T-643 lineage) unchanged — only the BUTTONS move; the pill's pins (if any) update honestly. ACCEPTANCE: no floating pill in any mode; Ruler + LoS arm from row-2 buttons with tooltips; their gestures work unchanged; Backspace still hides all chrome."]

missing=0
"""
main_goal = "Delete floating pill; Ruler and LoS move to row-2 toolbar."
context = [
    "Eye-pass wave-205: floating bottom-centre Select Ruler LoS pill still rendered.",
    "Operator decision 1 review section 8 retired pill; tools live in fixed chrome.",
    "T-797 T-798 neither acceptance carried pill removal; input-never-reached-output.",
]
requirement = [
    "Move Ruler and LoS-ray into row-2 toolbar tools group beside widget cluster.",
    "BTN_ICON chord tooltips census rows for chords; Select is digit-1 No-Widget T-835.",
    "DELETE floating pill component and mount; Backspace hide-set shrinks accordingly.",
]
current_state = [
    "Traceability routed pill removal to T-797 T-798 but fell between them.",
    "Ruler LoS arming machinery T-642 T-643 lineage unchanged only buttons move.",
    "Pill pins if any update honestly after removal.",
]
approach = [
    "No separate Select button; T-835 gives No-Widget mode the arrow glyph.",
    "Coordinate owns-aware packing with eden_top_strip and mission_editor.",
]
verify = [
    "No floating pill in any mode.",
    "Ruler LoS arm from row-2 buttons with tooltips gestures unchanged.",
    "Backspace still hides all chrome.",
]
citations = [
    "wave-205 eye-pass screenshot",
    "decision 1 review section 8",
    "T-797 T-798 gap class",
    "T-642 T-643 Ruler LoS lineage",
    "T-835 No-Widget arrow glyph",
]
estimated = ["scope"]
owns = [
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "top_strip",
    "map_canvas",
]
