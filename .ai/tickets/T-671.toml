id = "T-671"
kind = "work"
title = "Mission presentation: briefing text and thumbnail"
summary = "Mission presentation: briefing text and thumbnail"
class = "feature"
status = "shipped"
order = 4200
executor = "claude-code"
notes = "editor-UI program wave 17 (wave_plan.tsv row 117) — SHIPPED wave 117 (editor factory), e0f9cd31; verifier wave117.md. Briefing + thumbnail authored in the editor (the operator-decision alternative was the Mission Library dossier; the editor path was taken because the wave plan's owns encoded it — mission_overview.rs remains unable to author these). Found and fixed THREE live bugs, all reproduced on base by the verifier: create_mission_dialog never sent briefing though POST has always accepted it; compile_export reads meta.briefing whose only writer was boot hydrate, so an in-session edit never reached an export; Escape closed the dialog without blurring, silently discarding typed text. No backend change. The CMS upload path was checked and does NOT apply (AdminUser-gated, returns a site-relative path the server's own validator refuses)."
depends_on = ["T-661"]
main_goal = "Author briefing and thumbnail in editor MissionSettingsDialog and create flow."
context = [
    "Closes ATTR-FIELD-SCN-OVERVIEW-TEXT and SCN-PICTURE.",
    "API PATCH already accepts briefing and thumbnail_url; no caller.",
]
requirement = [
    "Third RowMirror column for briefing beside time and weather.",
    "Briefing textarea and thumbnail field in MissionSettingsDialog overflow column.",
    "create_mission_dialog must send briefing on POST.",
]
current_state = [
    "RowMirror mirrors only MIRROR_TIME and MIRROR_WEATHER today.",
    "MissionDetail.briefing and thumbnail_url exist in dto; PATCH takes both.",
    "compile_export reads meta.briefing only from boot hydrate.",
]
approach = [
    "Extend eden_settings MissionSettingsDialog and create_mission_dialog POST.",
    "Wire debounced PATCH through RowMirror debounce machine.",
]
citations = [
    "ATTR-FIELD-SCN-OVERVIEW-TEXT SCN-PICTURE",
    "RowMirror MIRROR_TIME MIRROR_WEATHER",
    "missions PATCH briefing thumbnail_url",
]
shipped_at = "b03892e3"
created_at = "2026-08-07T06:16:20Z"
completed_at = "2026-08-07T06:27:03Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_settings.rs",
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/create_mission_dialog.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "env_settings",
    "top_strip",
]
