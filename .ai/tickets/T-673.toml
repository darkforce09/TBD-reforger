id = "T-673"
kind = "work"
title = "Marker style and Area markers — the $defs/marker widening"
summary = "Marker style and Area markers — the $defs/marker widening"
class = "chore"
status = "queued"
order = 4300
executor = "claude-code"
notes = "workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row T-853 2026-08-12: the prepped slice worktree for this ticket was DROPPED, not landed. It carried one unmerged commit from 2026-08-02 that had sat unlanded for 10 days and was blocking the wave queue at 150. The work was NOT reviewed and NOT merged; the ticket stays queued so it can be redone. The commit is recoverable: `git checkout -b redo/T-673 salvage/t853-dropped/T-673` (tagged before the branch was deleted). Note T-674 and T-675 pointed at the SAME commit 113108a1, so whichever is redone first should check whether it subsumes the other."
depends_on = ["T-069"]
main_goal = "Ship Enfusion marker style reader for six MRK attrs; no schema edits."
context = [
    "$defs/marker closed at x z icon label; six attrs contract-blocked.",
    "Schema widening split to T-706; this ticket is mod reader only.",
    "Must ship after T-069 authors four schema-carried marker fields.",
]
requirement = [
    "Read six MRK attribute ids in TBD_MarkerData and TBD_MarkerClient.",
    "Editor UI for marker style attrs where applicable.",
    "Do not touch packages/tbd-schema; T-706 owns widening.",
]
current_state = [
    "Compiled schema carries 25 additionalProperties false closures.",
    "Icon-vs-Area is Eden second marker model; widening plus mod reader.",
    "Executor claude-code; headless compile.sh verified no Workbench.",
]
approach = [
    "Implement Enfusion reader after T-069 schema fields land.",
    "Fable routes .c portion per model-routing rule.",
]
verify = [
    "Marker runtime reads six attrs from mission payload at spawn.",
    "No edits under packages/tbd-schema in this slice.",
]
citations = [
    "T-069 marker schema fields",
    "T-706 schema widening split",
    "$defs/marker closed shape",
]
estimated = ["scope"]
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Markers/TBD_MarkerData.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Markers/TBD_MarkerClient.c",
]

[scope]
domain = "mod"
layer = "scripts"
component = "markers"
