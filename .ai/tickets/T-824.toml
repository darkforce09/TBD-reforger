id = "T-824"
kind = "work"
title = "Placed zones must render visibly at rest on map"
summary = "Placed zones are not visible on the map — investigate and fix the render (repro-first)"
class = "bug"
status = "queued"
order = 5060
executor = "claude-code"
notes = "operator eye-pass wave-203; repro-first — do not assume the mechanism"
main_goal = "Investigate and fix placed zone render visibility at rest."
context = [
    "Wave-203 eye-pass: placed zone not visible on map at rest.",
    "Zone drawing works draw moves zone count; render visibility unprobed.",
    "Measure browser at rest and selected at two to three zooms first.",
]
requirement = [
    "Circle and polygon zones visibly rendered at rest not selection-only.",
    "T-760 T-790 marker-lane recipe if lane missing: draw_order rebind tail.",
]
current_state = [
    "Render may be gated selection-only layer visibility or missing lane.",
    "Wave-130 141 lane lessons: feed after_doc_change plus rebind pin chain.",
]
approach = ["Investigate gating before code; T-760 template draw_order rebind tail."]
verify = [
    "Placed circle and polygon visible at rest at swept zooms.",
    "Visibility toggles round-trip; zone count chip matches render.",
]
citations = [
    "wave-203 operator eye-pass",
    "T-760 marker-lane recipe",
    "T-790 draw_order rebind",
    "wave-130 141 lane lessons",
]
estimated = ["scope"]
owns = [
    "apps/website/frontend/src/eden_zones.rs",
    "crates/map-engine-render/src/scene.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
