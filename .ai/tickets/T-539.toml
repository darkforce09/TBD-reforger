id = "T-539"
kind = "work"
title = "T-538 Class-R pins apply_* not stdout flatten_orbat_slots entrypoint"
summary = "T-538 Class-R pins apply_* not stdout flatten_orbat_slots entrypoint"
class = "bug"
status = "shipped"
order = 3379
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W52 adversarial verifier (MAJOR) after T-538.

Stdout schema/preserve Class-R calls `apply_flatten_orbat_slots` directly, not `flatten_orbat_slots(..., false)`. A post-apply stdout-only `mission["schemaVersion"] = "1.1"` stamp (exact pre-T-538 bug shape) keeps all `flatten_stdout_*` tests GREEN. Production path is shared/correct today; pin does not cover the CLI entrypoint claim.

Cure: Class-R must exercise stdout entrypoint (or source-pin that stdout path cannot reassign schemaVersion after apply). Repro: add stamp after apply in flatten_orbat_slots false branch → current tests stay green.

missing=0"""
priority = 1
main_goal = "Class-R must exercise stdout flatten_orbat_slots entrypoint, not apply_* only."
context = [
    "FOUND by W52 adversarial verifier (MAJOR) after T-538.",
    "Stdout Class-R calls apply_flatten_orbat_slots directly, not flatten_orbat_slots false.",
]
requirement = [
    "Class-R must exercise stdout entrypoint or pin schemaVersion reassignment forbidden.",
    "flatten_stdout_* tests must RED on post-apply schemaVersion stamp.",
]
current_state = [
    "Post-apply stdout-only schemaVersion 1.1 stamp keeps flatten_stdout_* GREEN.",
    "Production path shared/correct; pin does not cover CLI entrypoint.",
]
approach = ["Class-R exercises flatten_orbat_slots(..., false) stdout path."]
verify = ["Add stamp after apply in flatten_orbat_slots false branch → tests must RED."]
acceptance = [
    "flatten_stdout_* Class-R routed through flatten_orbat_slots false stdout entrypoint.",
    "Source ratchet RED on post-apply schemaVersion reassignment in stdout branch.",
]
citations = [
    "W52 verifier MAJOR T-538",
    "apply_flatten_orbat_slots bypass",
    "flatten_stdout tests false green",
]
shipped_at = "522126db"
created_at = "2026-07-27T13:06:56Z"
completed_at = "2026-07-27T13:16:25Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["xtask/src/schema_gates.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
