id = "T-439"
kind = "work"
title = "Objects palette aliases need prop:/comp: rows in mod Data/registry.json"
summary = "Objects palette aliases need prop:/comp: rows in mod Data/registry.json"
class = "feature"
status = "shipped"
order = 3278
executor = "claude-code"
notes = """
needs-operator: frozen unmappable Scope.

--- HEAD migration_legacy (verbatim) ---

Residual from T-254 / wave 9 N5. Editor synthesises prop:*/comp:* aliases for crate|other (~333 workbench kinds); mod Data/registry.json has 1 comp: (comp:checkpoint_small) and 0 prop: entries. SpawnMissionEntities warn+skips unknown aliases.

Repro: place a non-checkpoint Objects leaf → compile entities[] → world-boot → spawn warns and skips; only checkpoint_small resolves.

Cure: export/register prop: and comp: rows for Objects-eligible kinds (or narrow palette to known aliases until registry catches up)."""
priority = 2
main_goal = "Export prop:/comp: registry rows for Objects palette aliases; stop spawn warn+skip."
context = [
    "Residual from T-254 / wave 9 N5.",
    "Editor synthesises prop:*/comp:* for ~333 workbench kinds.",
    "registry.json has one comp: and zero prop: entries.",
]
requirement = [
    "Export/register prop: and comp: rows for Objects-eligible kinds.",
    "Or narrow palette to known aliases until registry catches up.",
]
current_state = [
    "SpawnMissionEntities warn+skips unknown aliases.",
    "Only comp:checkpoint_small resolves today.",
    "Repro: non-checkpoint Objects leaf spawn warns and skips.",
]
approach = ["Cure: populate registry.json prop:/comp: rows from workbench kinds."]
verify = ["Repro: place non-checkpoint Objects leaf; world-boot spawn warns and skips."]
citations = [
    "T-254 wave 9 N5",
    "registry.json prop comp rows",
    "asset_catalog Objects aliases",
]
shipped_at = "2e8673d2"
created_at = "2026-07-26T22:33:08Z"
completed_at = "2026-07-27T02:06:25Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/mod/tbd-framework/Data/registry.json",
    "apps/website/frontend/src/asset_catalog.rs",
    "scripts/mod/verify-t439-objects-registry-aliases.sh",
]

[scope]
domain = "repo"
layer = "docs"
