id = "T-885"
kind = "work"
title = "T-853 port: verified pg_dump + retention prune"
summary = "T-853 port: verified pg_dump + retention prune"
class = "docs"
status = "shipped"
order = 5660
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
main_goal = "Port backup-db.sh to cargo xtask deploy db backup."
context = [
    "T-853 shell->xtask slice; 252 lines verified pg_dump plus retention prune.",
    "T-577 requires dump VERIFIED before it counts.",
    "Test against scratch DB from make db-up never tbd_reforger.",
]
requirement = [
    "Delete script(s) shell-inventory.txt line(s) python-inventory.txt line(s).",
    "Port to cargo xtask deploy db backup; repoint every caller.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/deploy/backup-db.sh bash gate live today.",
    "Dump verification T-577 load-bearing on current bash path.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
citations = [
    "T-853 shell->xtask program",
    "T-577 verified dump requirement",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
]
shipped_at = "ac975f89"
created_at = "2026-08-12T09:42:25Z"
completed_at = "2026-08-12T09:42:48Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/deploy/backup-db.sh"]

[scope]
domain = "repo"
layer = "xtask"
