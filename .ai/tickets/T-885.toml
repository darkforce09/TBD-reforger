id = "T-885"
kind = "work"
title = "T-853 port: verified pg_dump + retention prune"
summary = "T-853 port: verified pg_dump + retention prune"
class = "docs"
status = "shipped"
order = 5660
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "ac975f89"
created_at = "2026-08-12T09:42:25Z"
completed_at = "2026-08-12T09:42:48Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/deploy/backup-db.sh to `cargo xtask deploy db backup` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 252 lines. T-577 requires the dump be VERIFIED before it counts. Test against a scratch DB from `make db-up`, never tbd_reforger. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md"]
owns = ["scripts/deploy/backup-db.sh"]

[scope]
domain = "repo"
layer = "xtask"
