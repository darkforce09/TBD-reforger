id = "T-934.7"
kind = "work"
title = "Reorg B1 — editor_ops façade split"
summary = "state/operations.rs becomes pub use façade over state/ops/{context,entity,compositions,attrs,transform,cargo}.rs; zero call-site churn."
class = "chore"
status = "shipped"
order = 6100
spec = "docs/specs/website_reorg/plan.md"
executor = "claude-code"
notes = """
entity.rs landed at 4.8k LOC: the spec's six-name partition does not name the domain CRUD
blocks (layers/comments/connections/zones/triggers/markers/ORBAT/vehicles), and splitting a
domain across files would have doubled guard risk — they went to entity.rs whole. Flagged as
a follow-on shard candidate, not silently deferred. seed_new_mission_template placed in
context.rs (boot plumbing).
"""
depends_on = ["T-934.6"]
parent = "T-934"
main_goal = "Split 7,370-LOC editor_ops without touching call sites."
context = ["Phase B opener: operations.rs (7.4k, 100% production, wasm-gated) splits behind a façade so the 44 external source guards and every call site survive."]
requirement = [
    "Façade preserves crate::editor::state::operations::* paths.",
    "All cross-guards into operations.rs re-pointed to the ops/ file holding their pattern (acceptance list).",
    "Whole ops/ tree stays wasm32-gated.",
]
current_state = ["Was: one 7,408-line module file; 44 include_str guards in 8 files pinned patterns inside it."]
approach = [
    "Façade keeps docs + cfg gate + pub use; six submodules under state/operations/ with byte-identical bodies and super:: imports.",
    "Guards repointed single-file where the pattern lives in one shard; concat includes where the test makes whole-module absence/uniqueness claims.",
    "t739 cite helper now reads operations/cargo.rs (cites 122/138); gate_t180 ban widened to façade + shards.",
]
verify = ["cargo xtask mk ci-local-leptos"]
acceptance = ["Gate green; every guard pattern found in its new home file."]
shipped_at = "1c651b073"
created_at = "2026-08-25T00:00:00Z"
completed_at = "2026-08-25T17:14:43Z"
estimated = [
    "scope",
    "tokens",
]
owns = ["apps/website/frontend/src/editor/state/"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
