id = "T-651"
kind = "work"
title = "Editor comments / annotations"
summary = "Editor comments / annotations"
class = "feature"
status = "shipped"
order = 4140
executor = "claude-code"
notes = """
editor-UI program wave 14 (wave_plan.tsv row 114) — SHIPPED wave 114 (editor factory), ef98b687; verifier wave114.md. Editor-only comment entities with their own non-compiling core root; never-compiles proven by a test that FIRES the rule (routes a comment through entities[] and asserts the leak). Outliner rows, layer filing, duplicate, delete, template seeding. ⚠️ TWO SPEC-ROW CLAUSES NOT DELIVERED — no map glyph (comments are absent from the render SoA) and NOT COMPOSABLE (compositions capture from the selection; comments sit in no selection lane). The drag/copy narrowings were disclosed by the slice; 'composable' was NOT, and that silent drop is what the HARD GATE bars. See T-748 — PLACE-COMMENT-001 does not fully close.

--- HEAD migration_legacy (verbatim) ---

Closes `PLACE-COMMENT-001` and the three fields `ATTR-FIELD-CMT-TITLE` / `-TOOLTIP` / `-POSITION` (filed separately in the attributes sweep as a new slice; it is the same ticket, derived once here). Editor-only virtual entities that appear in the outliner, support drag/copy/layers, and NEVER COMPILE into the mission — that constraint is what keeps the ticket small AND what keeps it factory-safe (it never reaches mission.schema.json), so keep it explicit. Placed via RMB empty -> Place Comment, which is why it depends on T-664. Corpus evidence, weighted honestly: FNF v3 ships 28 in-map Comment objects including a seven-paragraph tutorial (mission.sqm:4093) and per-object instructions; FNF v4 deleted the 219-line configGuide.txt and the whole 421-file template, and what survived as onboarding is literally TWO Comment entities. Against that, WOG and OFCRA have no comment equivalent — this is one community across two eras, not a four-way convergence, and must not be presented as one. ADD ONE SCOPE LINE the draft lacks: the NEW-MISSION TEMPLATE SHOULD SEED COMMENTS, the way v4's mission.sqm seeds its two reminders — that is the half of the mechanism that survived a total rewrite and it costs nothing. Code: a new editor-only entity map in store.rs (6,574 lines; `add_slot` :526 and the markersById :355 precedent are the shapes; "never compiles" is enforced by NOT declaring a root key in `flatten_to_mod_document`, a pattern store.rs:2007-2018 already documents); mutators in editor_ops.rs (none exist for any annotation type); `NodeKind` :62 / `OutlinerNode` :75 / `build_outliner` :105 in outliner.rs for "appears in the outliner"; the place gesture in mission_editor.rs; and the menu item in context_menu.rs, T-664's new module.

missing=0"""
depends_on = [
    "T-661",
    "T-664",
]
main_goal = "Editor-only comment entities; outliner rows; never compile into mission."
context = [
    "Closes PLACE-COMMENT-001 ATTR-FIELD-CMT-TITLE TOOLTIP POSITION.",
    "Depends T-664 RMB empty Place Comment gesture.",
    "FNF v3 ships 28 comments; v4 survived two Comment entities only.",
]
requirement = [
    "Editor-only virtual entities in outliner drag copy layers.",
    "NEVER COMPILE enforced by omitting root in flatten_to_mod_document.",
    "New-mission template should seed comments like v4 mission.sqm.",
]
current_state = [
    "store.rs add_slot :526 markersById :355 precedent shapes.",
    "No mutators for annotation types in editor_ops.rs yet.",
    "WOG OFCRA have no comment equivalent not four-way convergence.",
]
approach = [
    "New editor-only entity map store.rs pattern store.rs:2007-2018.",
    "NodeKind OutlinerNode build_outliner outliner.rs for outliner rows.",
    "Place gesture mission_editor.rs menu item context_menu.rs T-664.",
]
verify = ["verifier wave114.md; map glyph and composable clauses undisclosed gaps."]
acceptance = [
    "Editor-only comment entities lived in the outliner without compiling into the mission.",
    "Place Comment reached empty ground through the context menu after T-664.",
    "Never-compile rule was proven by a test routing comments through entities[].",
    "New-mission template seeded comments; map glyph and composable stayed undisclosed.",
]
citations = [
    "PLACE-COMMENT-001 comment placement",
    "T-664 RMB Place Comment",
    "T-748 composable gap follow-on",
    "flatten_to_mod_document never compile",
]
shipped_at = "7a16eca0"
created_at = "2026-08-07T03:49:31Z"
completed_at = "2026-08-07T03:52:23Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "crates/map-engine-core/src/doc/store.rs",
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/outliner.rs",
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/context_menu.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "outliner",
    "map_canvas",
    "layout_chrome",
]
