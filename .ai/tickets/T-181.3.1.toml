id = "T-181.3.1"
kind = "work"
title = "Official BI Script API mirror"
summary = "Fetch and index Bohemia Doxygen Script API; 7,990 classes with signatures."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Official BI Script API mirror + parser (enf apidoc, scripts/mod/fetch-vanilla-api.sh, make enf-apidoc). 7,990 classes indexed. CLOSES the T-181.3 gap: SCR_BaseGameMode/PossessSpawnData/PossessSpawnRequestComponent/RespawnSystemComponent/ChimeraMenuBase all resolve with full member lists incl. `static SCR_PossessSpawnData FromEntity(notnull IEntity)`. Signatures only, no bodies — CRF stays the behavioural oracle. Operator's suggestion that the data exists online was correct."
parent = "T-181"
context = [
    "T-181.3 carve missed compressed spawn-pipeline classes as plaintext.",
    "Operator hypothesis: official API docs might already exist online.",
]
requirement = [
    "Mirror enf apidoc via fetch-vanilla-api.sh; index all class signatures.",
    "Resolve SCR_BaseGameMode, PossessSpawnData, RespawnSystemComponent, ChimeraMenuBase.",
]
current_state = ["T-181.3 carve missed SCR_BaseGameMode and spawn classes as plaintext."]
approach = ["fetch-vanilla-api.sh mirrored BI Doxygen; enf apidoc indexed 7990 class signatures."]
verify = ["make enf-apidoc."]
acceptance = ["Official API mirror indexed 7990 classes; SCR_BaseGameMode and RespawnSystemComponent signatures resolved."]
citations = [
    "Source: vanilla_carve_coverage.md — API mirror.",
    "Source: commit d8fc6abc — factory bootstrap.",
]
created_at = "2026-07-25T00:00:00Z"
completed_at = "2026-07-31T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-181 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "docs"
