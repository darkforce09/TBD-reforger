__keys = [
    "id",
    "order",
    "program",
    "status",
    "stream",
    "targets",
    "executor",
    "surfaces",
    "impact",
    "title",
    "summary",
    "notes",
]
__ord = 773
executor = "claude-code"
id = "T-795"
impact = [
    "ui",
    "mc-authoring",
]
notes = "supersedes T-081; Eden numbering evidence = review F-25 table; ring-marquee destruction evidence = review F-16/rotate section | SHIPPED W205: renumber landed six-surface-consistent (binding/hint/plates/menu/help/census — verifier parity sweep exact), rotate ring is a real control (band boundary bites at 54/50px, selection preserved, one-undo), No-Widget drag-move intact, digits guarded in text fields. TWO not-reproducing claims: translate gizmo verified TRUE by the verifier; the heading-arrow claim was FALSE (made from source, not pixels — slots render yaw-less everywhere) -> T-832, folds into T-808/W207. Ring semantics shipped as face-the-aim, spec said about-centre -> operator decision T-833."
order = 4770
program = "eden"
status = "shipped"
stream = "mission-creator"
summary = "UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-25/F-16 — PIXEL-VERIFIED AGAINST EDEN (tooltips read off frames 164038-164107): Eden numbers its widget row 'No Widget (1) / Translation (2) / Rotation (3) / Area Scaling (4) / Area (5)'. TBD binds 1=Translate 2=Rotate (mission_editor.rs ~:3944-3949 from_digit arms; eden_help.rs ~:198-205 SHORTCUTS) — OFF BY ONE: an Eden author presses 1 to drop the gizmo and arms translate, 2 for translate and gets rotate, 3 for rotate and gets nothing. Three silent wrong-modes; muscle memory is the whole point of taking Eden's keys. AND the widgets themselves (review F-16, verified): translate mode renders NO gizmo at all; rotate mode renders a ring that is PURE DECORATION — dragging the ring edge (dead on the handle) starts a MARQUEE and destroys the selection, while the mode hides the heading arrow (the one thing rotation is about); no chrome anywhere shows which mode is active. FIX: (a) renumber 1=No Widget, 2=Translate, 3=Rotate; reserve 4/5; update SHORTCUTS + tooltips (coordinate with T-797's toolbar buttons — they read the same map); (b) translate mode renders the 2D axis gizmo (~:1257-1385 region has the render arms); (c) rotate ring becomes interactive: drag on the ring rotates the selection about its centre (live heading preview, one txn on release), heading arrow stays visible; ring/gizmo hit-tests BEFORE the marquee arm so a widget drag can never become a marquee; (d) active mode indicated (toolbar toggle state per T-797 + cursor-adjacent hint). SUPERSEDES T-081 (note added). ⚠️ TRAPS: 'No Widget' must still allow direct drag-move (Eden semantics — widget-less drag translates); Space stays centre-on-selection (census collision 1 resolved by digits); undo: ring rotate = one step. ACCEPTANCE: scripted — press 1/2/3: mode indicator shows none/translate/rotate; in rotate, drag from ring edge: SEL preserved, heading changes by the drag angle (numeric attrs confirm), ONE undo step reverts; in translate, gizmo visible on selection (screenshot non-empty diff at gizmo region); marquee still works from empty ground in all modes."
surfaces = [
    "XFORM",
    "MAP",
    "UI",
]
targets = ["website"]
title = "Widget keys match Eden (1 none / 2 translate / 3 rotate) and the widgets are visible and operable"
