id = "T-795"
kind = "work"
title = "Renumber Eden widget keys and render interactive gizmos"
summary = "Widget keys match Eden (1 none / 2 translate / 3 rotate) and the widgets are visible and operable"
class = "bug"
status = "shipped"
order = 4770
executor = "claude-code"
notes = """
supersedes T-081; Eden numbering evidence = review F-25 table; ring-marquee destruction evidence = review F-16/rotate section | SHIPPED W205: renumber landed six-surface-consistent (binding/hint/plates/menu/help/census — verifier parity sweep exact), rotate ring is a real control (band boundary bites at 54/50px, selection preserved, one-undo), No-Widget drag-move intact, digits guarded in text fields. TWO not-reproducing claims: translate gizmo verified TRUE by the verifier; the heading-arrow claim was FALSE (made from source, not pixels — slots render yaw-less everywhere) -> T-832, folds into T-808/W207. Ring semantics shipped as face-the-aim, spec said about-centre -> operator decision T-833.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-25/F-16 — PIXEL-VERIFIED AGAINST EDEN (tooltips read off frames 164038-164107): Eden numbers its widget row 'No Widget (1) / Translation (2) / Rotation (3) / Area Scaling (4) / Area (5)'. TBD binds 1=Translate 2=Rotate (mission_editor.rs ~:3944-3949 from_digit arms; eden_help.rs ~:198-205 SHORTCUTS) — OFF BY ONE: an Eden author presses 1 to drop the gizmo and arms translate, 2 for translate and gets rotate, 3 for rotate and gets nothing. Three silent wrong-modes; muscle memory is the whole point of taking Eden's keys. AND the widgets themselves (review F-16, verified): translate mode renders NO gizmo at all; rotate mode renders a ring that is PURE DECORATION — dragging the ring edge (dead on the handle) starts a MARQUEE and destroys the selection, while the mode hides the heading arrow (the one thing rotation is about); no chrome anywhere shows which mode is active. FIX: (a) renumber 1=No Widget, 2=Translate, 3=Rotate; reserve 4/5; update SHORTCUTS + tooltips (coordinate with T-797's toolbar buttons — they read the same map); (b) translate mode renders the 2D axis gizmo (~:1257-1385 region has the render arms); (c) rotate ring becomes interactive: drag on the ring rotates the selection about its centre (live heading preview, one txn on release), heading arrow stays visible; ring/gizmo hit-tests BEFORE the marquee arm so a widget drag can never become a marquee; (d) active mode indicated (toolbar toggle state per T-797 + cursor-adjacent hint). SUPERSEDES T-081 (note added). ⚠️ TRAPS: 'No Widget' must still allow direct drag-move (Eden semantics — widget-less drag translates); Space stays centre-on-selection (census collision 1 resolved by digits); undo: ring rotate = one step. ACCEPTANCE: scripted — press 1/2/3: mode indicator shows none/translate/rotate; in rotate, drag from ring edge: SEL preserved, heading changes by the drag angle (numeric attrs confirm), ONE undo step reverts; in translate, gizmo visible on selection (screenshot non-empty diff at gizmo region); marquee still works from empty ground in all modes.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-25/F-16 — PIXEL-VERIFIED AGAINST EDEN (tooltips read off frames 164038-164107): Eden numbers its widget row 'No Widget (1) / Translation (2) / Rotation (3) / Area Scaling (4) / Area (5)'. TBD binds 1=Translate 2=Rotate (mission_editor.rs ~:3944-3949 from_digit arms; eden_help.rs ~:198-205 SHORTCUTS) — OFF BY ONE: an Eden author presses 1 to drop the gizmo and arms translate, 2 for translate and gets rotate, 3 for rotate and gets nothing. Three silent wrong-modes; muscle memory is the whole point of taking Eden's keys. AND the widgets themselves (review F-16, verified): translate mode renders NO gizmo at all; rotate mode renders a ring that is PURE DECORATION — dragging the ring edge (dead on the handle) starts a MARQUEE and destroys the selection, while the mode hides the heading arrow (the one thing rotation is about); no chrome anywhere shows which mode is active. FIX: (a) renumber 1=No Widget, 2=Translate, 3=Rotate; reserve 4/5; update SHORTCUTS + tooltips (coordinate with T-797's toolbar buttons — they read the same map); (b) translate mode renders the 2D axis gizmo (~:1257-1385 region has the render arms); (c) rotate ring becomes interactive: drag on the ring rotates the selection about its centre (live heading preview, one txn on release), heading arrow stays visible; ring/gizmo hit-tests BEFORE the marquee arm so a widget drag can never become a marquee; (d) active mode indicated (toolbar toggle state per T-797 + cursor-adjacent hint). SUPERSEDES T-081 (note added). ⚠️ TRAPS: 'No Widget' must still allow direct drag-move (Eden semantics — widget-less drag translates); Space stays centre-on-selection (census collision 1 resolved by digits); undo: ring rotate = one step. ACCEPTANCE: scripted — press 1/2/3: mode indicator shows none/translate/rotate; in rotate, drag from ring edge: SEL preserved, heading changes by the drag angle (numeric attrs confirm), ONE undo step reverts; in translate, gizmo visible on selection (screenshot non-empty diff at gizmo region); marquee still works from empty ground in all modes."]

missing=0
"""
main_goal = "Renumber 1/2/3 widget keys; render translate and rotate gizmos."
context = [
    "UX review F-25 F-16 pixel-verified against Eden frame tooltips.",
    "Eden 1=No Widget 2=Translate 3=Rotate; TBD binds 1=Translate 2=Rotate off by one.",
    "Supersedes T-081; coordinate T-797 toolbar buttons same map.",
]
requirement = [
    "Renumber 1=No Widget 2=Translate 3=Rotate; reserve 4/5; update SHORTCUTS.",
    "Translate mode renders 2D axis gizmo; rotate ring interactive about centre.",
    "Ring hit-tests before marquee; heading arrow visible; active mode indicated.",
]
current_state = [
    "Press 1 arms translate not none; 2 arms rotate not translate; 3 nothing.",
    "Translate renders no gizmo; rotate ring decoration starts marquee destroys selection.",
    "No Widget must still allow direct drag-move; Space stays centre-on-selection.",
]
approach = [
    "mission_editor from_digit ~3944; eden_help SHORTCUTS ~198.",
    "Render arms ~1257-1385; one undo step for ring rotate on release.",
]
verify = [
    "Press 1/2/3: mode shows none/translate/rotate; rotate ring drag preserves SEL.",
    "Translate gizmo visible on selection; marquee from empty ground all modes.",
]
acceptance = [
    "Widget keys renumbered to Eden map one none two translate three rotate across six binding surfaces.",
    "Rotate ring became interactive preserving selection with one undo; translate gizmo rendered on selected units.",
]
citations = [
    "editor_hostile_ux_review F-25",
    "editor_hostile_ux_review F-16",
    "Eden widget row tooltips",
    "T-081 supersedes",
    "T-797 toolbar buttons",
    "fe7c02352 Eden widget renumber",
]
shipped_at = "44eb1412"
created_at = "2026-08-10T16:23:25Z"
completed_at = "2026-08-10T16:34:57Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/eden_help.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
