id = "T-423"
kind = "work"
title = "Fix flatten contract-floor silent rename gap"
summary = "T-216's contract-floor test cannot detect the failure its own doc comment says it exists to detect"
class = "bug"
status = "shipped"
order = 3262
executor = "claude-code"
priority = 2
main_goal = "Drive vehicle_row fixture from add_vehicle output; fix tautology tolerance assertion."
context = [
    "Two MINORs from wave 5 adversarial verifier in flatten.rs.",
    "Protection exists at gate level; not where ticket comment says.",
]
requirement = [
    "Fixture must detect writer renaming floor, not T-216 disagreeing with itself.",
    "Serialised round trip, not direct add_vehicle call per merge-break risk.",
]
current_state = [
    "the_vehicle_row_still_has_the_shape reads LEDGER_FIXTURE constant.",
    "sed rename resourceName on store.rs:551 — contract test still ok.",
    "attach_vehicle_roundtrip and apply_faction_vehicles fail on rename.",
    "Tolerance assertion clones fixture, inserts cargo; no module code runs.",
    "EditorPayload has no vehicles field; scan_editor_payload_types plain deserialize.",
]
approach = [
    "T-216 reason for not calling add_vehicle: signature change merge break.",
    "factionId/cargo extras cannot produce save-time 400 per reader design.",
]
verify = ["cargo test the_vehicle_row_still_has_the_shape passes after resourceName rename."]
citations = [
    "flatten.rs vehicle row test",
    "doc store attach_vehicle_roundtrip",
    "T-215 factionId cargo extras",
]
shipped_at = "c1d89d0e"
created_at = "2026-07-27T04:44:55Z"
completed_at = "2026-07-27T04:59:13Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["crates/map-engine-core/src/mission/flatten.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
