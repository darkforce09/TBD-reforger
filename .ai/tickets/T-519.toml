id = "T-519"
kind = "work"
title = "R-api fixture goldens for Member / RegistryCompat / FireSolution"
summary = "R-api fixture goldens for Member / RegistryCompat / FireSolution"
class = "feature"
status = "shipped"
order = 3359
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Wave 45 residual (T-360 NIT). DashboardResponse.server_status is typed Option<ServerStatusDto> end-to-end; dto::r_api::dashboard green. Remaining typed DTOs Member, RegistryCompatResponse, FireSolution still lack fixtures under apps/website/frontend/tests/fixtures/api/ (ls | rg -i 'member|compat|fire|solve' → empty). Owns when filed: apps/website/frontend/tests/fixtures/api/ + matching dto.rs R-api modules. Outside T-360 owns at ship time.

missing=0"""
priority = 2
main_goal = "Add R-api fixture goldens for Member, RegistryCompat, FireSolution."
context = [
    "Wave 45 residual NIT after T-360 dashboard typed end-to-end.",
    "Member, RegistryCompatResponse, FireSolution lack fixtures under tests/fixtures/api.",
]
requirement = [
    "Add fixture goldens under apps/website/frontend/tests/fixtures/api/.",
    "Match dto.rs R-api modules for Member, RegistryCompatResponse, FireSolution.",
]
current_state = ["dto::r_api::dashboard green; ls | rg member|compat|fire|solve → empty."]
approach = ["File fixtures under tests/fixtures/api/ for each remaining typed DTO."]
citations = [
    "Wave 45 residual T-360 NIT",
    "dto::r_api::dashboard green",
    "Member RegistryCompat FireSolution fixtures",
]
shipped_at = "67e47118"
created_at = "2026-07-27T14:41:43Z"
completed_at = "2026-07-27T14:48:40Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/frontend/tests/fixtures/api/",
    "apps/website/frontend/src/dto.rs",
    "apps/website/frontend/src/mortar.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["tools"]
