id = "T-156"
kind = "work"
title = "Session-expired UX - 401s must not masquerade as empty data"
summary = "Session-expired UX - 401s must not masquerade as empty data"
class = "bug"
status = "shipped"
order = 3722
spec = "docs/website/DEV_RUNBOOK.md"
executor = "claude-code"
main_goal = "401 auth death must not masquerade as empty catalogs."
context = [
    "Sibling of T-155 (same 2026-07-13 incident):",
    "Operator read it as data loss.",
]
requirement = ["ApiFailure/Fetched types; error UI not empty-state copy on 401."]
current_state = [
    "when auth dies, GET /registry and GET /factions fail and the UI silently degrades",
    '- the Arsenal shows the non-character guard ("Loadout applies to placed characters") because the catalog is empty,',
    'and the Factions palette shows the "No factions yet" author-CTA.',
]
approach = [
    "Fix: distinguish query-error (esp. 401 after failed refresh) from genuinely-empty responses;",
    'surface one clear "session expired - log in again" banner/toast with a dev-login shortcut in dev,',
    "and make useRegistry/useFactionLibrary consumers render an error state, never the empty-state copy, on failure.",
]
citations = [
    "commit 69ec59b7",
    "DEV_RUNBOOK.md session UX",
]
shipped_at = "69ec59b7"
created_at = "2026-07-13T10:39:18Z"
completed_at = "2026-08-01T02:44:09Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
