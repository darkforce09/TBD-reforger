id = "T-793"
kind = "work"
title = "Grid reference labels derive from the live camera every frame"
summary = "Grid reference labels derive from the live camera every frame"
class = "bug"
status = "shipped"
order = 4750
executor = "claude-code"
notes = """
review section 8 O-2 has the failing numbers; CUR unproject is the trusted oracle — the acceptance test uses it | SHIPPED W201: O-2 root-caused as text-keyed <For> DOM-node reuse (T-727 stale-node class), NOT a stale transform — fixed with a position-quantised key; verifier: 250px spacing exact, 0.00px vs the CUR oracle after the review's 240m pan, per-frame under continuous pan. Also carries F-13's status-bar ' m' suffix (orchestrator addendum), CUR metre-accuracy preserved. Wheel-zoom heartbeat lag filed T-817.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-2 — VERIFIED WRONG WITH ARITHMETIC: statically labels roughly register (within one label-anchor of CUR-derived truth), but after a pan that moved the camera 240m the label set half-updated: positions held while the world moved, and the top edge showed '090' at x1593 and '100' at x1663 — 70px apart at 4 m/px where kilometre lines MUST be 250px apart. Two adjacent labels that cannot both be true. Operator: labels also drift wrong across zoom. A milsim group reads grids aloud; wrong grid labels are worse than none. MECHANISM: eden_toolbelt.rs ~:203-306 edge_eastings/edge_northings build from a cached set not the live camera; render at ~:792. FIX: compute edge labels from the current camera transform each render (world = cam.origin + px·m_per_px; label k at the km line's screen x), cull to viewport, no caching across pan/zoom. ⚠️ TRAPS: the status-bar scale bar and CUR readout are CORRECT today (verified to the metre) — reuse their transform source; label anchor = the km line, not the text box edge; keep the constant-density spot-height lesson in mind but grids are exact, not banded. ACCEPTANCE: property test (wasm unit or scripted probe): for any camera (5 random pans + 3 zoom levels scripted), every visible label k satisfies |screen_x(k·1000) − label_x| <= 2px, adjacent labels exactly 1000/m_per_px px apart, and labels under continuous pan update every frame (two mid-pan samples differ correctly).

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-2 — VERIFIED WRONG WITH ARITHMETIC: statically labels roughly register (within one label-anchor of CUR-derived truth), but after a pan that moved the camera 240m the label set half-updated: positions held while the world moved, and the top edge showed '090' at x1593 and '100' at x1663 — 70px apart at 4 m/px where kilometre lines MUST be 250px apart. Two adjacent labels that cannot both be true. Operator: labels also drift wrong across zoom. A milsim group reads grids aloud; wrong grid labels are worse than none. MECHANISM: eden_toolbelt.rs ~:203-306 edge_eastings/edge_northings build from a cached set not the live camera; render at ~:792. FIX: compute edge labels from the current camera transform each render (world = cam.origin + px·m_per_px; label k at the km line's screen x), cull to viewport, no caching across pan/zoom. ⚠️ TRAPS: the status-bar scale bar and CUR readout are CORRECT today (verified to the metre) — reuse their transform source; label anchor = the km line, not the text box edge; keep the constant-density spot-height lesson in mind but grids are exact, not banded. ACCEPTANCE: property test (wasm unit or scripted probe): for any camera (5 random pans + 3 zoom levels scripted), every visible label k satisfies |screen_x(k·1000) − label_x| <= 2px, adjacent labels exactly 1000/m_per_px px apart, and labels under continuous pan update every frame (two mid-pan samples differ correctly)."]

missing=0
"""
main_goal = "Edge grid labels derive from live camera transform each frame."
context = [
    "UX review O-2 verified wrong with arithmetic after 240m pan.",
    "Labels half-updated: 090 and 100 labels 70px apart not 250px.",
    "Status-bar scale bar and CUR readout correct today; reuse transform source.",
]
requirement = [
    "Compute edge labels from current camera each render; cull to viewport.",
    "No caching across pan/zoom; label anchor at km line not text box.",
]
current_state = [
    "edge_eastings/edge_northings built from cached set not live camera ~203-306.",
    "Render at ~792; adjacent labels cannot both be true after pan.",
    "Operator: labels drift wrong across zoom; milsim reads grids aloud.",
]
approach = [
    "world = cam.origin + px·m_per_px; label k at km line screen x.",
    "Reuse CUR unproject transform source from status-bar scale bar.",
]
verify = [
    "Visible label k screen_x within 2px of k·1000 line.",
    "Adjacent labels exactly 1000/m_per_px apart; continuous pan updates each frame.",
]
acceptance = [
    "Edge grid labels derived from live camera each frame via position-quantised keys fixing stale DOM reuse.",
    "Adjacent kilometre labels held 250px spacing; 240m pan oracle matched CUR readout within 0.00px.",
]
citations = [
    "editor_hostile_ux_review O-2",
    "eden_toolbelt edge labels 203",
    "eden_toolbelt render 792",
    "CUR unproject oracle",
    "2ad1ad41 live camera labels",
]
shipped_at = "2ad1ad41"
created_at = "2026-08-09T18:39:22Z"
completed_at = "2026-08-09T18:40:27Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_toolbelt.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["toolbelt"]
