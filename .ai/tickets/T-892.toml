id = "T-892"
kind = "work"
title = "T-853 port: headless game-mode boot validation"
summary = "T-853 port: headless game-mode boot validation"
class = "docs"
status = "shipped"
order = 5730
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/world-boot.sh to `cargo xtask mod world-boot` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 846 lines, 4 python3 sites. Boots a real game mode headlessly and parses the log for component instantiation. Same 0/1/2 exit contract as compile.sh. Needs the engine present; verify on the machine that has it. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/world-boot.sh to `cargo xtask mod world-boot` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 846 lines, 4 python3 sites. Boots a real game mode headlessly and parses the log for component instantiation. Same 0/1/2 exit contract as compile.sh. Needs the engine present; verify on the machine that has it. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md"]

missing=0"""
main_goal = "Port world-boot.sh to cargo xtask mod world-boot; delete script and repoint callers."
context = [
    "T-853 shell->xtask slice; 846 lines 4 python3 sites.",
    "Boots real game mode headlessly parses log for component instantiation.",
    "Same 0/1/2 exit contract as compile.sh; needs engine present on verify machine.",
]
requirement = [
    "Port to cargo xtask mod world-boot; delete script(s) inventory lines.",
    "Repoint every caller; build on crates/tbd-gate not hand-roll verdicts.",
]
current_state = [
    "scripts/mod/world-boot.sh bash gate live today.",
    "4 python3 sites invoke world-boot shell path today.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "ACCEPTANCE BYTE-FOR-BYTE: capture bash stdout stderr rc first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
acceptance = [
    "Deleted scripts/mod/world-boot.sh; mod world-boot xtask gate replaced headless game-mode boot validation.",
    "ci.yml and mod-gates.yml repointed; python and shell inventory lines removed.",
    "Port matched bash stdout stderr and rc; preserved 0/1/2/3 exit contract.",
]
citations = [
    "T-853 shell->xtask program",
    "compile.sh 0/1/2 exit contract",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "d0032321e world-boot port",
]
shipped_at = "08dbe2a7"
created_at = "2026-08-12T08:35:53Z"
completed_at = "2026-08-12T22:52:30Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["scripts/mod/world-boot.sh"]

[scope]
domain = "repo"
layer = "xtask"
