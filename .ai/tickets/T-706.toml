id = "T-706"
kind = "work"
title = "Widen mission.schema.json once for the whole editor program"
summary = "Widen mission.schema.json once for the whole editor program"
class = "chore"
status = "shipped"
order = 4600
executor = "claude-code"
notes = " — SHIPPED wave 120 (editor factory), 843f3438 + ledger 9228a458 + hardening 6382c69c + prose 955a281d; verifier wave120.md (contract hardened against the hostile reading before any reader dispatched); unread-fields gate live at 53 fields"
main_goal = "Widen mission.schema.json once for sixteen mod runtime halves."
context = [
    "Sixteen mod tickets each add $def or property to mission.schema.json.",
    "Per-ticket widening forces sixteen sequential waves doubles program.",
    "Schema briefly contract ahead of consumer; entities[] warning precedent.",
]
requirement = [
    "Land every schema change one pass so runtime halves pack freely.",
    "Every new field comment names ticket that will read it.",
    "Test asserts each new field currently unread fail-on-demand.",
    "Additive only no existing field shape change no migration.",
]
current_state = [
    "Unread-fields gate live at 53 fields post ship.",
    "Each of sixteen keeps own ticket Enfusion reader owns drops schema.",
]
approach = [
    "Run early before runtime halves; verifier wave120 hardened contract.",
    "Sixteen tickets T-212 through T-705 schema halves absorbed here.",
]
verify = [
    "verifier wave120.md contract hardened hostile reading.",
    "Unread-fields test fails day reader lands forces comment removal.",
]
citations = [
    "mission.schema.json:72 entities warning",
    "sixteen runtime halves absorbed",
    "T-705 gadget flags schema",
]
shipped_at = "9228a458"
created_at = "2026-08-02T10:41:10Z"
completed_at = "2026-08-02T10:53:30Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["packages/tbd-schema/schema/mission.schema.json"]

[scope]
domain = "schema"
layer = "mission"
