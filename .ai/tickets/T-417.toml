id = "T-417"
kind = "work"
title = "Export Compiled resort, 401 trap, arsenal gaps"
summary = "T-243's compiled-export artifact re-sorts keys, and its own comment says it does not"
class = "docs"
status = "shipped"
order = 3242
executor = "claude-code"
priority = 1
main_goal = "Fix Export Compiled key resort, false whitespace comment, and 401 misdiagnosis in mission_commands."
context = [
    "Wave 4 adversarial verifier findings plus T-240 structural issues in frontend authoring.",
    "Export Compiled re-parses to serde_json::Value; preserve_order enabled nowhere in workspace.",
    "Round trip re-sorts every object's keys alphabetically via BTreeMap.",
]
requirement = [
    "mission_commands.rs:353-359 invites byte-compare against live GET /compiled — verifier trap.",
    "Export Compiled misdiagnoses expired session as no saved server row yet.",
    "Arsenal has no save button; cargo persists immediately via persist_cargo.",
    "Cargo in no-garment-worn container undeliverable; both ends quiet.",
]
current_state = [
    "MEASURED mission 6d291619: /compiled 2006 bytes; export 3006 bytes; different key order.",
    "Deep-equal after recursive key sort: TRUE — mod still loads, nothing lost.",
    "client_twin_is_byte_identical compares compact to_vec — genuinely non-vacuous.",
    "ROW_META doc lists local-only id, 404, offline boot — not 401.",
    "T-243 report: 1 placed slots, 11 refused; measured 13 live, 12 x 409.",
    "CONFIRMED NON-ISSUE: /api/v1/health never existed — app.rs:297 registers /healthz.",
]
approach = [
    "Fix false whitespace comment at mission_commands.rs:174-178.",
    "Fix ROW_META doc and Export Compiled 401 message at :92-97.",
    "arsenal_rules.rs:800-808 — T-240 rule stays silent on no-garment capacity.",
    "TBD_LoadoutEquipHelper.c:1105-1106 Degrades with kit wears no message.",
]
verify = [
    "REPRO: fresh profile never visited /auth/callback; call compiled_document_json().",
    "REPRO: open Arsenal, click + on cargo row, reload — qty persisted without save.",
    "REPRO: clear vest row, add magazine to vest cargo, export.",
]
citations = [
    "T-243 compiled export",
    "T-240 arsenal structural",
    "wave 4 adversarial verifier",
    "mission_commands ROW_META",
]
shipped_at = "8899912b"
created_at = "2026-07-27T08:20:11Z"
completed_at = "2026-07-27T08:24:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/mission_commands.rs",
    "apps/website/frontend/src/main.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["ops_undo"]
