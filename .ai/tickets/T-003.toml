id = "T-003"
kind = "work"
title = "Dev login shortcut"
summary = "Development dev-login endpoint for local testing."
class = "feature"
status = "shipped"
order = 30
executor = "cursor-docs"
main_goal = "Local development logs in without Discord: dev-login mints a real session exactly like the OAuth callback."
context = ["Local testing needed a login that works without real Discord credentials."]
requirement = ["A development-only login endpoint that mirrors the real Discord callback."]
current_state = ["The only login path was the live Discord OAuth callback wired in T-002."]
approach = [
    "GET /auth/dev-login registers only when APP_ENV=development and is re-guarded at request time; it upserts a stable dev user with the role selectable via ?role=.",
    "Redirects to the SPA callback with the token fragment, mirroring the Discord callback so the frontend handles both paths identically.",
]
verify = ["Integration tests cover the redirect, the token fragment and role defaulting."]
acceptance = ["Local login without Discord worked via the development-only endpoint."]
citations = [
    "Source: commit 1e41ae34.",
    "Source: docs/platform/SHIPPED_HISTORY.md §T-003.",
]
shipped_at = "1e41ae34"
created_at = "2026-06-18T17:13:26Z"
completed_at = "2026-06-18T17:13:26Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
