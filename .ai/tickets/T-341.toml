id = "T-341"
kind = "work"
title = "deployments.rs model site has bare * and silent decode errors"
summary = "deployments.rs has a bare SELECT * and swallows decode errors, in the file held up as the good example"
class = "bug"
status = "shipped"
order = 3157
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "Close deployments.rs bare SELECT *, silent mission_title_terrain decode swallow, and vacuous dashboard_reads /dashboard assertion."
context = [
    "FOUND by T-329 while fixing the sibling bug.",
    "The irony is the point: deployments.rs:103 is what every other read site was told to copy,",
    "and two other statements in the same file are the defects it was supposed to model against.",
]
requirement = [
    "1. **`deployments.rs:76`** — `SELECT event_registrations.*`, the identical bare-`*` shape that made dashboard.rs 500 in production.",
    "2. **`deployments.rs:55`** — `mission_title_terrain` swallows every error via `.ok().flatten()`, so a decode failure degrades silently to `None` instead of surfacing.",
    "3. **`tests/dashboard_reads.rs:80`** asserts /dashboard is 200 but passes VACUOUSLY — same reachability defect T-329 found in null_tolerance.rs:",
    "it authenticates as one user and the assertion depends on rows belonging to another.",
    "Fix it the way T-329 fixed its own: mint a session whose identity owns the seeded rows.",
    "T-329's static enumerator will name :76 once it runs; check whether it is in the tolerance list and prune it as part of this.",
]
current_state = [
    "Benign only because `slot_id` HAPPENS to be `Option<Uuid>` today. Any future nullable column on that table makes this a live 500, and nothing warns you.",
    "That is the same class as the gate defects this program has fixed repeatedly: something reporting success while examining less than it claims.",
    "A missing mission title reads as 'no mission' rather than 'this query is broken'.",
]
approach = [
    "Copy coalescing column list from deployments.rs:103 for bare `*` at :76.",
    "Surface decode failures instead of `.ok().flatten()` on mission_title_terrain.",
    "Mint session owning seeded rows; prune :76 from null_tolerance list if present.",
]
verify = ["cargo test -p website-api --test dashboard_reads dashboard_leaderboards_deployments_loa_audit."]
acceptance = ["deployments.rs bare * closed; decode errors surfaced; dashboard_reads mints session owning seeded rows."]
citations = [
    "T-329 sibling bug",
    "deployments.rs:103 model",
    "null_tolerance reachability",
    "T-329 static enumerator",
]
shipped_at = "ee28bd88"
created_at = "2026-07-27T11:43:44Z"
completed_at = "2026-07-27T11:57:11Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/deployments.rs",
    "apps/website/api/tests/dashboard_reads.rs",
    "apps/website/api/src/handlers/dashboard.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
