id = "T-918.4"
kind = "work"
title = "In-app markdown viewer"
summary = "In-app markdown viewer"
class = "feature"
status = "shipped"
order = 5984
spec = "docs/platform/t917_ticket_schema_v2.md"
plan = "docs/plans/t-918_4_plan.md"
executor = "claude-code"
notes = "egui_commonmark is a new dependency family — named supply-chain event, version + tree delta reported like eframe was at T-915.1. Read-only: the viewer never edits files."
depends_on = ["T-917.2"]
parent = "T-918"
user_story = "The operator reads a ticket's plan without leaving the board — click, read, back."
acceptance = [
    "Clicking spec/plan renders the markdown in-pane; headings, lists, code blocks legible",
    "A deliberately broken file renders raw text with a note, never a crash",
    "External-open remains available as secondary",
    "Dependency delta reported: crate versions + new package count in Cargo.lock",
    "git status --porcelain unchanged after a full session",
]
created_at = "2026-08-14T21:54:38Z"
completed_at = "2026-08-15T00:41:40Z"
migration_legacy = ["Clicking spec, plan or a citation path renders the markdown file inside a board pane via egui_commonmark — no external hop. Raw-text fallback on parse trouble; external-open still offered as a secondary action. Renders the T-917.6 plan documents as the primary use case."]
owns = ["apps/ticketboard"]

[scope]
domain = "repo"
layer = "tickets"
