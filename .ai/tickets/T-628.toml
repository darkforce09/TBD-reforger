id = "T-628"
kind = "work"
title = "One byte-metered boot bar from zero to one hundred"
summary = "One monotonic 0–100 boot bar weighted by byte budgets across four segments; no per-stage reset or sweep."
class = "feature"
status = "shipped"
order = 3772
executor = "claude-code"
priority = 3
context = [
    "T-627 left hydrate, terrain, and world objects on indeterminate sweep.",
    "Operator: sweep carries no information at one or ninety-nine percent.",
    "Mission document can reach 142 MB at 367k slots per T-060.",
]
requirement = [
    "Four BootSeg segments: mission GET, DEM stream, satellite tiles, world files.",
    "Weight segments by declared byte budgets; high-water mark prevents rewind.",
    "Terrain via fetch_bytes_streamed against content-length; world declares 634 files upfront.",
]
current_state = [
    "T-627 left hydrate, terrain, and world on indeterminate sweeps; only satellite was determinate.",
    "Hydrate terrain and world bars reset between stages; satellite fill did not.",
    "Operator saw one or ninety-nine percent with no byte meaning.",
]
approach = [
    "Four BootSeg segments weighted by bytes; world used approximate weight plus exact file counts.",
    "High-water mark on BootProgress::raw prevents bar rewind on overlapping fetches.",
    "Inline boot_progress mod in mission_editor.rs drives overlay from completed bytes.",
]
verify = ["the_bar_can_never_exceed_one_hundred BootProgress unit tests."]
acceptance = [
    "One monotonic zero-to-one-hundred bar spanned mission GET DEM satellite and world file loads.",
    "BootSeg byte weights and high-water mark kept the bar from resetting between segments.",
    "BootProgress unit tests pinned the bar never exceeding one hundred percent.",
]
citations = [
    "commit 646a54de unified boot bar",
    "T-627 indeterminate satellite step",
    "T-060 142 MB mission payload",
]
shipped_at = "646a54de"
created_at = "2026-08-01T06:45:22Z"
completed_at = "2026-08-01T06:45:22Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
