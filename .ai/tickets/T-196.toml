id = "T-196"
kind = "work"
title = "T-181 slices[] lists 39 of 54 slices"
summary = "Backfill T-181 slices[] to 66 entries and clear stale active_slice before factory dispatch."
class = "feature"
status = "shipped"
order = 2140
executor = "claude-code"
context = [
    "Platform factory Phase 0: registry and CLAUDE §Status were stale enough to misroute agents.",
    "T-181 slices[] listed 39 entries; active_slice pointed at shipped T-181.15.",
]
requirement = [
    "Backfill T-181 slices[] from slice_plan to 66 entries.",
    "Set active_slice null; defer T-068 and T-181 with human-only open slices.",
    "Backfill status on ten T-068 phase-1 shipped slices missing status field.",
]
current_state = [
    "T-181 slices[] had 39 entries; active_slice pointed at shipped T-181.15.",
    "Ten T-068 phase-1 slices shipped without status field.",
]
approach = [
    "Backfill T-181 slices[] from slice_plan to 66 entries.",
    "Clear active_slice; defer T-068 and T-181 with human-only open slices.",
    "Rewrite five wrong ticket bodies; merge duplicates; close delivered tickets.",
]
verify = ["./scripts/ticket check OK; 113 runnable tickets."]
acceptance = ["Registry matched shipped reality; factory Phase 0 unblocked; stale bodies corrected without deleting fixtures."]
citations = [
    "Source: commit 7b98232b — registry true.",
    "PLATFORM_FACTORY Phase 0 dispatch gate",
]
shipped_at = "7b98232b"
created_at = "2026-07-26T00:00:00Z"
completed_at = "2026-07-26T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at id-interpolated between T-195 and T-197 (midpoint, day precision)"
owns = [".ai/tickets/registry.json"]

[scope]
domain = "repo"
layer = "xtask"
