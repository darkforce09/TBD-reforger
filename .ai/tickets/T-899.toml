id = "T-899"
kind = "work"
title = "T-853 Phase 1.5: make verify file-length actually scan something"
summary = "T-853 Phase 1.5: make verify file-length actually scan something"
class = "bug"
status = "shipped"
order = 5790
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "b53ac8a6"
created_at = "2026-08-12T23:25:32Z"
completed_at = "2026-08-13T00:30:42Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 6 commit subject(s)"
migration_legacy = ["T-898 is the finding; this is the fix. Extend the walk from `apps/website` + .go/.ts/.tsx (zero files) to the Rust tree: xtask/, tools/, crates/, apps/website/*/src, keeping `.rs`. Fix the `.unwrap_or(0)` fail-open at node_free.rs:89 while you are in there. MEASURE FIRST: this turns main RED on at least schema_gates.rs 4044, cmds.rs 1330, smokes.rs 3852 and main.rs (>1000 after Phase 3). The slice is EITHER split those OR give each an allowlist entry with a real reason and an expiry. Do NOT extend the walk and then silence it — that reproduces the defect with extra steps. Prove it bites: plant a 1200-line file and watch it fail. ACCEPTANCE: prove the change BITES — perturb the new subject and watch it go red — not merely that it passes. A green run alone is not evidence (T-556). Finish with a full `cargo xtask platform wave gate` PASS: it is the only check that sees cross-crate breakage, and it has caught defects a green `cargo test -p xtask` could not. Spec: docs/platform/t853_shell_to_xtask_waves.md"]
owns = [
    "xtask/src/node_free.rs",
    ".coding-standards-allowlist.yaml",
]

[scope]
domain = "repo"
layer = "xtask"
