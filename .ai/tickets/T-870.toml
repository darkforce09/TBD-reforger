id = "T-870"
kind = "work"
title = "T-853 port: bootstrap-staging-server -> mod bootstrap-staging"
summary = "T-853 port: bootstrap-staging-server -> mod bootstrap-staging"
class = "chore"
status = "shipped"
order = 5510
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/bootstrap-staging-server.sh (55 lines) to `cargo xtask mod bootstrap-staging` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). staging server bootstrap. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/bootstrap-staging-server.sh (55 lines) to `cargo xtask mod bootstrap-staging` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). staging server bootstrap. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0"""
main_goal = "Port bootstrap-staging-server.sh to cargo xtask mod bootstrap-staging."
context = [
    "T-853 shell->xtask slice; 55-line bash script.",
    "Staging server bootstrap.",
    "Callers include Makefile platform wave.sh mod wave.sh workflows.",
]
requirement = [
    "Delete script shell-inventory.txt line python-inventory.txt line.",
    "Repoint every caller Makefile wave.sh workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/mod/bootstrap-staging-server.sh bash gate live.",
    "Makefile wave.sh workflows still invoke bash script.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and two deliberately broken trees.",
    "T-556 anti-vacuity: passing run alone is not evidence.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Port diff matches clean tree byte-for-byte.",
    "Port diff matches at least two deliberately broken trees.",
]
acceptance = [
    "Deleted bootstrap-staging-server.sh; mod bootstrap-staging xtask gate replaced staging server bootstrap.",
    "Inventory entries removed; mod docs and STAGING-SERVER callers repointed to xtask.",
    "Port matched bash stdout stderr and rc on clean tree and deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "86b30bcf bootstrap-staging port",
]
shipped_at = "45e57cbb"
created_at = "2026-08-12T06:11:06Z"
completed_at = "2026-08-12T06:12:59Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "scripts/mod/bootstrap-staging-server.sh",
    "xtask/src/gate_bootstrap_staging_server.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
