id = "T-934.13"
kind = "work"
title = "Reorg B7 — EditorGestureContext + canvas/gestures.rs"
summary = "Bundle ~35 captured handles into EditorGestureContext; extract onpointerdown/move/up, onwheel, ondblclick, oncontextmenu closures as gestures::attach_*(&ctx). Audit §4 Phase 2."
class = "chore"
status = "shipped"
order = 6100
spec = "docs/specs/website_reorg/plan.md"
executor = "claude-code"
depends_on = ["T-934.12"]
parent = "T-934"
main_goal = "Highest-risk child: gesture extraction with reactive-capture fidelity."
context = ["Phase B2: the six canvas gesture closures leave MissionEditorPage behind a cloneable context struct (audit §4.2), sequenced last because reactive capture only proves out in the browser."]
requirement = [
    "Context struct per audit §4.2 sketch (NodeRefs, engine Rc, DocHandle, gesture Rc, pan_px, tool/los/snap/widget signals, context_menu, asset_picker).",
    "Class S closure-body guards (only_body onpointerup etc.) re-pin to gestures.rs.",
]
current_state = ["Was: onpointerdown/move/up, onwheel, ondblclick, oncontextmenu inline in the page, capturing ~24 handles; closure-body pins across 14 evacuated test modules."]
approach = [
    "EditorGestureContext (24 fields, Clone) + attach_canvas_gestures(&ctx) whose let-belt mirrors the page environment name-for-name — closure bodies moved byte-identical.",
    "gestures.rs imports go through mission_editor's re-export hub (keeps the T-703 census invariant); 14 test modules re-anchored via page+gestures concat haystacks; one cross-crate store.rs pin concat.",
    "onpointercancel/leave/onresize stay page-side (outside the six-closure scope); onkeydown is .14's lane.",
]
verify = [
    "cargo xtask mk ci-local-leptos",
    "cargo xtask mk leptos-gates",
    "Manual smoke per gesture: pan, marquee, drag, rotate, wheel-zoom, context menu.",
]
acceptance = ["All gestures behave identically; browser gates green."]
shipped_at = "5ecda424e"
created_at = "2026-08-25T00:00:00Z"
completed_at = "2026-08-25T19:39:15Z"
estimated = [
    "scope",
    "tokens",
]
owns = ["apps/website/frontend/src/editor/canvas/"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
