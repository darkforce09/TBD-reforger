id = "T-535"
kind = "work"
title = "W50 T-385: positive IT for GET /servers terrain join"
summary = "T-385: live IT must assert GET /servers terrain from match join (positive)"
class = "bug"
status = "shipped"
order = 3375
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W50 adversarial verifier (MAJOR) after T-385.

T-385 ships LEFT JOIN matches.terrain and Class-R/golden pins, but the only live IT path asserts terrain is null on create. A regression that always returns None (broken join / wrong column) stays green. Cure: seed a match with terrain + point server_statuses.current_match_id at it, then GET /servers and assert the row's terrain equals the seeded value (e.g. everon).

Repro: delete LEFT JOIN from SERVER_STATUS_SELECT_* → golden/source pins may still pass if not re-run; HTTP create-null IT still passes. Need positive IT RED on join removal.

missing=0"""
priority = 1
main_goal = "Positive IT asserts GET /servers terrain from seeded match join."
context = [
    "FOUND by W50 adversarial verifier (MAJOR) after T-385.",
    "T-385 ships LEFT JOIN matches.terrain and Class-R/golden pins.",
]
requirement = [
    "Seed match with terrain; point server_statuses.current_match_id at it.",
    "GET /servers; assert row terrain equals seeded value (e.g. everon).",
    "Positive IT must RED when LEFT JOIN removed from SERVER_STATUS_SELECT.",
]
current_state = [
    "Only live IT path asserts terrain is null on create.",
    "Regression returning None from broken join or wrong column stays green.",
]
approach = ["Seed match with terrain; wire current_match_id; GET /servers assert terrain."]
verify = [
    "Delete LEFT JOIN from SERVER_STATUS_SELECT_* → positive IT RED.",
    "HTTP create-null IT alone is insufficient; need join-removal RED.",
]
citations = [
    "W50 adversarial verifier MAJOR",
    "T-385 LEFT JOIN matches.terrain",
    "SERVER_STATUS_SELECT join removal",
]
shipped_at = "4d73f562"
created_at = "2026-07-27T12:32:01Z"
completed_at = "2026-07-27T12:37:20Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["apps/website/api/tests/misc_integration.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
