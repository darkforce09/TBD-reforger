__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "order",
    "stream",
    "targets",
    "executor",
]
__ord = 225
executor = "claude-code"
id = "T-251"
impact = ["infra"]
order = 2690
program = "infra"
status = "shipped"
stream = "infra"
summary = "No Dockerfile, no docker-compose.staging.yml (referenced by deploy.env.example and HOME_SERVER.md but absent), no deploy-website.sh, no deploy job in any of the four CI workflows, no TLS or reverse-proxy config in-repo, no pg_dump backup script, and no metrics, Sentry or OTel dependency."
surfaces = ["DATA"]
targets = ["root"]
title = "Production deployment story"
