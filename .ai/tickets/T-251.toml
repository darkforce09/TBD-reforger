id = "T-251"
kind = "work"
title = "Production deployment story"
summary = "Dockerfile, staging compose, deploy-website.sh, Caddyfile; deploy.env.example updated."
class = "chore"
status = "shipped"
order = 2690
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope. "
depends_on = ["T-209"]
context = [
    "deploy.env.example and HOME_SERVER.md referenced Dockerfile and staging compose that did not exist.",
    "No deploy-website.sh, CI deploy job, TLS reverse-proxy config, or backup script in-repo.",
]
requirement = [
    "Ship release api image, Postgres staging compose, rsync deploy script refusing prairielearn.",
    "Add COOP/COEP Caddyfile; point deploy.env.example at files that now exist.",
]
current_state = [
    "deploy.env.example referenced Dockerfile and staging compose files absent from the repo.",
    "No deploy script, TLS reverse-proxy config, or prairielearn refuse guard existed in-tree.",
]
approach = [
    "Shipped apps/website Dockerfile, docker-compose.staging.yml, and COOP/COEP Caddyfile.",
    "Added rsync deploy-website.sh refusing prairielearn; updated deploy.env.example paths.",
]
verify = ["bash scripts/deploy/deploy-website.sh --dry-run with filled deploy.env."]
acceptance = ["Deploy stack files existed; deploy.env.example pointed at live Dockerfile and compose paths."]
citations = [
    "commit 9adeb05c deploy stack",
    "wave 9 close 22bc2fe5",
]
shipped_at = "22bc2fe5"
created_at = "2026-07-26T22:14:10Z"
completed_at = "2026-07-26T22:33:08Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "scripts/deploy/deploy-website.sh",
    "apps/website/Dockerfile",
    "apps/website/docker-compose.staging.yml",
    "scripts/deploy/deploy.env.example",
    "scripts/deploy/Caddyfile.website",
]

[scope]
domain = "repo"
layer = "docs"
