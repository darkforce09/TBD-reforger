id = "T-181.54"
kind = "work"
title = "Mission loader HTTP-code warning branches"
summary = "Branch fallback warning on HTTP status so staged golden is note not stale-cache fault; add test-mission.sh."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Correct a mission-loader warning that told the operator a falsehood, and add scripts/mod/test-mission.sh. OPERATOR-REPORTED LIVE: staging a golden produced 'RUNNING A CACHED MISSION - this is the last document the backend served successfully, NOT THE ONE CONFIGURED'. That last clause can NEVER be true: LoadFromProfileFile reads $profile:missions/<missionId>.json, so the file is KEYED BY the configured id and the mission identity always matches. What can differ is the VERSION. Worse, it labelled a deliberate stage as a fault. MEASURED discriminator against the live API: a golden's msn_* id returns 400 {invalid id} because the API validates the id SHAPE before lookup, while a valid-but-absent uuid returns 404 {mission not found} - so 400 means this id was never a backend mission and the disk file is intended, not stale. Loader now threads the http code into TryProfileFallbackAfterRestFailure and branches: 400 -> event-level 'staged on disk' note (plus the no-schema-validation caveat), anything else -> the stale-VERSION warning. NOT VERIFIABLE BY THE GATE: world-boot --mission= forces backendUrl='' so it never takes the REST error path; the branch is confirmed on the operator's next live reload."
parent = "T-181"
context = [
    "Cached-mission warning claimed wrong mission loaded; disk file is keyed by configured id.",
    "Golden msn_* ids return 400 invalid-id; absent uuid returns 404 not found.",
]
requirement = [
    "Thread http code into TryProfileFallbackAfterRestFailure; 400=staged note, else stale VERSION.",
    "Add test-mission.sh for backend versus golden mission switching.",
]
current_state = [
    "Cached-mission warning claimed wrong mission loaded; disk file keyed by configured id.",
    "Golden msn_* ids return 400 invalid-id; absent uuid returns 404 not found.",
]
approach = [
    "Thread http code into TryProfileFallbackAfterRestFailure.",
    "Branch 400=staged note, else stale VERSION warning; add test-mission.sh.",
]
verify = ["400/404/200 discriminator measured against live API."]
acceptance = [
    "Loader branched on HTTP status; in-game note not gate-verified.",
    "test-mission.sh added for backend versus golden switching.",
]
citations = ["Source: commit 9c5a5b2c — loader warning fix."]
shipped_at = "9c5a5b2c"
created_at = "2026-07-25T21:24:36Z"
completed_at = "2026-07-25T21:24:36Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionLoader.c + scripts/mod"]

[scope]
domain = "mod"
layer = "scripts"
component = "backend"
