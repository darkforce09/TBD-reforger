id = "T-484"
kind = "work"
title = "Reject whitespace-only DISCORD_CLIENT_SECRET and DISCORD_REDIRECT_URL"
summary = "Reject whitespace-only DISCORD_CLIENT_SECRET and DISCORD_REDIRECT_URL"
class = "feature"
status = "shipped"
order = 3323
executor = "claude-code"
priority = 2
main_goal = "Reject whitespace-only discord_client_secret and discord_redirect_url in validate."
context = [
    "Wave 32 verifier residual; T-481 shipped client_id trim.",
    "Production validate uses bare is_empty() for secret/redirect.",
]
requirement = [
    "Whitespace-only secret/redirect validates Ok.",
    "Same disguise class as pre-T-481 client id.",
]
current_state = ['Repro: env=production, secret=" " or redirect=" " → validate() Ok.']
approach = ["trim().is_empty() (or reject trim-empty) for both; Class-R pins."]
verify = ["Repro: whitespace-only secret or redirect → validate() Ok."]
citations = [
    "Wave 32 verifier residual T-481",
    "production validate is_empty",
    "whitespace-only secret redirect Ok",
]
shipped_at = "076b5222"
created_at = "2026-07-27T05:09:55Z"
completed_at = "2026-07-27T05:18:24Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = ["apps/website/api/src/config.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
