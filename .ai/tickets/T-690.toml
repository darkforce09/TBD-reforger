id = "T-690"
kind = "work"
title = "Compile becomes a compiler: structured diagnostics instead of a toast"
summary = "Compile becomes a compiler: structured diagnostics instead of a toast"
class = "chore"
status = "shipped"
order = 4185
executor = "claude-code"
notes = "editor-UI program wave 16 (wave_plan.tsv row 116); filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md) — SHIPPED wave 116 (editor factory), cc4a00eb; verifier wave116.md. Structured diagnostics alongside the bytes (severity, stable rule id, owning entity id, message), reusing T-657's Finding vocabulary; the six T-216 silent drops now emit. Deliberately NOT registry rules — the verifier confirmed the exhaustiveness argument: ORBAT-SQUAD-HAS-LEADER fires for a squad with no leader and COMPILE-DROP-SQUAD-LEADER for one with a leader, so as always-on rules every compiled squad trips one and the panel could never go green. One compile body with three projections. API surfaces findings as two headers + a warn! per finding, never a body key (the schema closes the document root). Touched validation_panel.rs outside owns (additive seam, 60 lines, no pin weakened)."
depends_on = [
    "T-661",
    "T-655",
]
main_goal = "Return structured compile diagnostics alongside export bytes for validation panel."
context = [
    "U7 framework_synthesis C.2 ranked seven of sixteen; feeds T-655 panel.",
    "FNF v4 export hook treats editor as deliberate compilation front-end.",
    "TBD has build step via mission_commands compile.rs but no structured diagnostics.",
]
requirement = [
    "Compile returns findings with severity rule id entity id message alongside bytes.",
    "Render diagnostics in T-655 panel not pass fail toast only.",
    "Emit six T-216 flatten drop ledger diagnostics until T-674 T-675 land.",
    "Diagnostics never debug-gated; no severity fires on correct input.",
]
current_state = [
    "flatten.rs 2584-2649 records six silent drops; verify-t180 stayed green.",
    "compiled_export_text compiled_document_json download path flatten pass fail.",
]
approach = ["Own compile.rs and mission_commands.rs; panel stays T-655 single claimant."]
verify = ["Structured findings returned with bytes; six drop diagnostics emitted."]
citations = [
    "framework_synthesis.md C.2 U7",
    "fnf_v4.md 13.1 export hook",
    "flatten.rs T-216 drop ledger",
    "fnf_tooling.md 1.3 severity ladder",
]
shipped_at = "eebeebfc"
created_at = "2026-08-07T05:25:00Z"
completed_at = "2026-08-07T05:32:35Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "crates/map-engine-core/src/mission/compile.rs",
    "apps/website/frontend/src/mission_commands.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["ops_undo"]
