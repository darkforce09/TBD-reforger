id = "T-686"
kind = "work"
title = "Loadout import closes export-only round-trip gap"
summary = "Loadout import — close the round-trip TBD has only half of"
class = "bug"
status = "shipped"
order = 4115
executor = "claude-code"
notes = """
editor-UI program wave 12 (wave_plan.tsv row 112); filed 2026-08-01 by the framework_synthesis coverage audit (parity/coverage_audit.md) — SHIPPED wave 112 (editor factory), 2a3288c3; verifier wave112.md. Import gate reuses the shipped loadout-export v2 schema via include_str!, refuses whole-document on any fault, runs validate_loadout/attachment/capacity before commit, one undo step via set_loadout. NOTE: the hand-rolled schema subset checker fails OPEN in three forms — see T-735.

--- HEAD migration_legacy (verbatim) ---

The U2 item from framework_synthesis.md C.2, ranked 6 of 16 ("Medium-high value, Small effort"). WOG is the ONLY framework in the corpus with a loadout ROUND-TRIP — GUI arsenal -> `WOG: EXPORT` -> clipboard -> `tpl/<faction>/<role>.sqf`, and `WOG: IMPORT` reads it back — and wog.md:814 states the gap it closes: "Most frameworks give you export only." TBD is currently in the export-only class: `apps/website/frontend/src/arsenal.rs`:1058-1066 gates through `try_export` (the T-240 refusal gate — a refusal produces no bytes) and hands the result to `download_json("loadout-export.json", ...)`; THERE IS NO INGEST PATH ANYWHERE (no file input in arsenal.rs). NO NEW FORMAT IS NEEDED: the document written is `packages/tbd-schema/schema/loadout-export.schema.json` v2, which already carries 7 closed objects, and `arsenal.rs`:489-596 already holds the file-vs-doc-field distinction that T-199 established. Scope: a file picker, a parse against the shipped schema, and one apply path onto the current Arsenal picks — plus the refusal behaviour that mirrors the export gate (a document that does not validate applies nothing and says why, rather than half-applying). REQUIRED, from the same corpus: re-use `rules::validate_loadout` / `cargo_capacity_errors` (arsenal_rules.rs:350-368) on the imported picks before commit — OFCRA's ~11 silent loadout data bugs are all schema errors a typed importer makes unrepresentable (ofcra_omtk.md 5.9, 14.1), and importing without validating would re-import them. Import must be one undo step.

missing=0
"""
depends_on = ["T-661"]
main_goal = "Close loadout import round-trip: parse v2 schema, validate, apply one undo step."
context = [
    "U2 framework_synthesis C.2 ranked six of sixteen; WOG only round-trip corpus.",
    "TBD export-only via try_export; no ingest path in arsenal.rs today.",
    "Shipped wave 112; gate reuses loadout-export v2 via include_str.",
]
requirement = [
    "File picker parse against loadout-export.schema.json v2 shipped schema.",
    "Refusal mirrors export gate: invalid doc applies nothing and explains why.",
    "Run validate_loadout attachment capacity checks before commit.",
    "One undo step via set_loadout on successful import.",
]
current_state = [
    "arsenal.rs file-vs-doc-field distinction from T-199 established.",
    "Hand-rolled schema subset checker fails open; see T-735 three forms.",
    "Depends T-661; owns arsenal.rs and arsenal_rules.rs.",
]
approach = [
    "Reuse rules validate_loadout cargo_capacity_errors arsenal_rules before commit.",
    "Mirror try_export refusal behaviour for invalid import documents.",
]
verify = [
    "Invalid loadout document applies nothing with refusal message.",
    "Valid import applies picks in one undo step via set_loadout.",
]
acceptance = [
    "File picker ingests loadout-export.schema.json v2 via include_str and mirrors export refusal on fault.",
    "validate_loadout and cargo capacity checks run before commit; successful import applies one set_loadout undo step.",
    "Round-trip closes on golden export bytes; invalid documents apply nothing with a named refusal message.",
]
citations = [
    "framework_synthesis U2 loadout",
    "loadout-export.schema.json v2",
    "arsenal_rules validate_loadout",
]
shipped_at = "6ed9dd4e"
created_at = "2026-08-07T02:07:19Z"
completed_at = "2026-08-07T02:10:01Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/arsenal.rs",
    "apps/website/frontend/src/arsenal_rules.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
