id = "T-492"
kind = "work"
title = "fmt_changed/clippy_changed ignore changed_rs rc (empty→SKIP)"
summary = "fmt_changed/clippy_changed ignore changed_rs rc (empty→SKIP)"
class = "chore"
status = "shipped"
order = 3331
executor = "claude-code"
priority = 2
shipped_at = "50604e62"
created_at = "2026-07-27T06:49:10Z"
completed_at = "2026-07-27T12:24:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
migration_legacy = ['Wave 36 adversarial NIT. T-401 made git_porcelain_paths fail-loud, and wasm_changed/refuse_empty_range check rc. fmt_changed and clippy_changed still do files="$(changed_rs …)" and treat empty as SKIP (return 0) without checking $?. Gate path is still safe because refuse_empty_range runs first. Cure: propagate changed_rs failure in those helpers (or share a wrapper that dies on rc≠0).']
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "repo"
layer = "xtask"
