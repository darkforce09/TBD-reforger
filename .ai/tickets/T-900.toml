__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "executor",
    "targets",
    "stream",
    "parallel_ok",
    "order",
    "spec",
]
__ord = 873
executor = "claude-code"
id = "T-900"
impact = ["dx"]
order = 5800
parallel_ok = true
program = "infra"
spec = "docs/platform/t853_shell_to_xtask_waves.md"
status = "shipped"
stream = "infra"
summary = "The Makefile sweep rewrote 137 live docs but did not catch references to tools deleted EARLIER. Example found 2026-08-13: apps/mod/README.md:64 says `make web  # Vite dev server on :5173` — the React app was deleted at T-159.29.3 and Vite with it. Sweep for references to: make targets, the React/Vite app, `scripts/*.sh` paths (only wave.sh and hostrun.sh remain), and `node`/`npm` outside the enfusion-mcp floor. Rewrite LIVE docs only. Do NOT touch the historical record: SHIPPED_HISTORY, generated TICKET_*.md, registry.json ticket prose, .ai/artifacts/** incl. every verify-log, wave_plan.tsv, applied migrations, and the byte-for-byte port narratives in xtask/src/mk_*.rs. Those describe what was true then. ACCEPTANCE: prove the change BITES — perturb the new subject and watch it go red — not merely that it passes. A green run alone is not evidence (T-556). Finish with a full `cargo xtask platform wave gate` PASS: it is the only check that sees cross-crate breakage, and it has caught defects a green `cargo test -p xtask` could not. Spec: docs/platform/t853_shell_to_xtask_waves.md"
surfaces = ["CI"]
targets = ["root"]
title = "T-853 Phase 4: retire doc references to things that no longer exist"
