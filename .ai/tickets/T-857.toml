id = "T-857"
kind = "work"
title = "T-853 port: mcp-wb-logs -> mcp wb-logs"
summary = "T-853 port: mcp-wb-logs -> mcp wb-logs"
class = "chore"
status = "shipped"
order = 5380
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "29b836d9"
created_at = "2026-08-12T03:22:44Z"
completed_at = "2026-08-12T04:00:19Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/mcp-wb-logs.sh (320 lines) to `cargo xtask mcp wb-logs` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). Workbench MCP log tail/scan. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/mcp-wb-logs.sh",
    "xtask/src/gate_mcp_wb_logs.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
