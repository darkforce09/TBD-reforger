id = "T-913.1"
kind = "work"
title = "created_at and completed_at on the ticket file"
summary = "created_at and completed_at on the ticket file"
class = "feature"
status = "shipped"
order = 5930
spec = "docs/platform/t913_ticket_run_metrics.md"
executor = "claude-code"
notes = "New test: every on-disk TOML key is a mapped encoding-C key or in ALLOWED_NEW — planting created_at without the widen commit is check-red. Timestamp parser crate arrives in the same commit as the fields (no chrono/time in xtask or tbd-tickets today). shipped_at stays a SHA."
parent = "T-913"
user_story = "Ticket lifecycle carries real timestamps so run metrics can measure elapsed time without inventing clocks."
acceptance = [
    "ticket add writes RFC 3339 created_at; ship/done/cancel write completed_at; malformed timestamps are parse errors, never now",
    "Planting created_at without the ALLOWED_NEW + schema widen is check-red; the widen commit makes it green",
    "No backfill: existing shipped tickets carry no timestamps; shipped_at stays a SHA",
    "cargo xtask ticket check --strict prints check OK",
]
shipped_at = "b82949b7"
completed_at = "2026-08-14T04:51:39Z"
estimated = ["scope"]
migration_legacy = ["Optional created_at/completed_at (RFC 3339 UTC) on ticket files; malformed is a parse error, never now. One deliberate commit adds ALLOWED_NEW = {created_at, completed_at}, widens schema.json + TicketFile + WorkTicket/ProgramTicket, and lands the writers: ticket add stamps created_at, ship/done/cancel stamp completed_at. No backfill."]
owns = [
    ".ai/tickets/schema.json",
    "crates/tbd-tickets",
    "xtask/src/cmds.rs",
    "xtask/src/check.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "check"
