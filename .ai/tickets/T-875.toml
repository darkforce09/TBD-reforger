id = "T-875"
kind = "work"
title = "T-853 port: setup-workbench-linux -> setup workbench"
summary = "T-853 port: setup-workbench-linux -> setup workbench"
class = "chore"
status = "shipped"
order = 5560
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
estimated = ["scope"]
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/setup-workbench-linux.sh (28 lines) to `cargo xtask setup workbench` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). Workbench linux setup. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/setup-workbench-linux.sh",
    "xtask/src/gate_setup_workbench_linux.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
