id = "T-724"
kind = "work"
title = "ATTR-OPEN residue: objects zones need dblclick attributes not picker"
summary = "ATTR-OPEN-001 residue: objects and zones still cannot open Attributes; dblclick on them opens the picker"
class = "feature"
status = "shipped"
order = 4317
executor = "claude-code"
notes = """
filed by wave-106 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

T-647's one-call-site swap covers vehicles only (pick_slot_or_vehicle = slot SoA + vehiclesById). Objects (entitiesById) and zones still cannot open Attributes by double-click — worse, a dblclick on them now MISSES and opens the asset picker over the entity. Needs a pick that resolves objects/zones (select_tool has the geometry; zones are polygons — point-in-zone hit test) routed to the same open_attributes entry, and the picker's open guarded to genuine empty-ground misses. Registry honesty: T-647 marked shipped-with-partials for this id. Diagnosis: editor_verify/wave106.md MAJOR-4.

missing=0
"""
main_goal = "Route objects and zones dblclick to open_attributes; guard picker to empty misses."
context = [
    "T-647 swap covers vehicles only; ATTR-OPEN-001 residue remains.",
    "Registry honesty: T-647 shipped-with-partials for this gap.",
]
requirement = [
    "Pick resolves objects via entitiesById and zones point-in-polygon hit.",
    "Route hits to same open_attributes entry as vehicles.",
    "Guard asset picker open to genuine empty-ground misses only.",
]
current_state = [
    "Objects and zones cannot open Attributes on double-click.",
    "Dblclick on entity MISSES and opens picker over the entity.",
    "pick_slot_or_vehicle covers slot SoA and vehiclesById only.",
]
approach = ["Extend pick in select_tool geometry; wire zones polygon hit test."]
verify = ["Dblclick object or zone opens attributes not asset picker."]
citations = [
    "editor_verify/wave106.md MAJOR-4",
    "T-647 one-call-site swap",
    "ATTR-OPEN-001 residue",
]
created_at = "2026-08-08T00:00:00Z"
completed_at = "2026-08-08T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at id-interpolated between T-723 and T-726 (midpoint, day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
