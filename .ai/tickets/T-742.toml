id = "T-742"
kind = "work"
title = "Shared CARGO_TARGET_DIR lets slices run sibling tests"
summary = "Concurrent slice worktrees share CARGO_TARGET_DIR and can execute each other's test binary"
class = "bug"
status = "shipped"
order = 4618
executor = "claude-code"
notes = "filed by the T-649 slice agent, wave 112 (editor factory) — factory infrastructure, needs operator decision"
main_goal = "Isolate slice test binaries so ad-hoc cargo test cannot run sibling code."
context = [
    "Every slice worktree exports shared CARGO_TARGET_DIR at tbd-target cache.",
    "T-649 cargo test reported failure in test absent from its tree tracking T-686.",
    "Per-slice gate builds private dir unaffected; shared dir load-bearing for disk.",
]
requirement = [
    "Ad-hoc cargo test must not resolve sibling slice website_frontend test binary.",
    "Preserve shared target disk savings; fix test profile or gate path only.",
]
current_state = [
    "All worktrees share one CARGO_TARGET_DIR; same test binary hash across slices.",
    "Verifier trusting bare cargo test line can read wrong slice result.",
    "wave.sh gate --slice uses private dir and is not affected.",
]
approach = [
    "Per-worktree test target dir, cargo --target-dir override, or brief forbids ad-hoc.",
    "Operator decision among options; not simply stop sharing.",
]
verify = [
    "Ad-hoc cargo test from one worktree does not run sibling compiled tests.",
    "Per-slice gate --slice still builds into private dir unchanged.",
]
citations = [
    "T-649 slice wave-112",
    "editor factory infrastructure",
    "wave.sh gate --slice",
    "preflight 8 worktrees 4GB",
]
shipped_at = "caa9531b"
created_at = "2026-08-08T14:01:54Z"
completed_at = "2026-08-08T14:23:21Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/platform/wave.sh",
    "docs/platform/EDITOR_SLICE_BRIEF.md",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
