id = "T-917.6"
kind = "work"
title = "Ship gate ON + stamp-sha verb + plan ready-gate"
summary = "The hard gate lands in check + ops::ship: shipped requires created_at/completed_at (RFC 3339 UTC) + shipped_at (SHA-shaped 7-40 hex) each measured or in estimated[], and tokens = receipt XOR estimate file. ticket stamp-sha <id> <sha> writes shipped_at canonically AND auto-generates the diff_loc estimate when no receipt exists — ship -> commit -> stamp-sha -> green. Plan ready-gate: plan key required on ready-class, file must exist on disk; the live ready tickets get plans written this slice. Strict prints honesty counters with per-source breakdown."
class = "chore"
status = "queued"
order = 5976
spec = "docs/platform/t917_ticket_schema_v2.md"
executor = "claude-code"
notes = "Gate must land last (S.2-S.5 make the tree gate-satisfiable first) and WITH stamp-sha or every future ship wedges between the ship edit and the SHA stamp. Program shipped_at lives inside Status::Shipped while work carries the field — gate reads through both."
depends_on = [
    "T-917.4",
    "T-917.5",
]
parent = "T-917"
user_story = "A shipped ticket without stamps and tokens cannot exist green anymore, the ship workflow closes itself with one verb after commit, and nothing goes ready without its own plan document."
acceptance = [
    "Planted shipped ticket missing completed_at reds naming ticket and field",
    "Scratch end-to-end: ship -> commit -> stamp-sha -> check green, auto-estimate written with matching factor",
    "ops::ship on a created_at-less ticket refuses pre-write with the corpus byte-untouched",
    "mark-ready without a plan file refuses naming the path; live ready tickets carry plans",
    "check --strict prints shipped tokens measured/estimated: K/E plus per-source breakdown",
]
created_at = "2026-08-14T21:54:38Z"
owns = [
    "xtask/src",
    "crates/tbd-tickets",
    "docs/plans",
]

[scope]
domain = "repo"
layer = "tickets"
