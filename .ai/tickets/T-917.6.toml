id = "T-917.6"
kind = "work"
title = "Ship gate ON + stamp-sha verb + plan ready-gate"
summary = "Ship gate ON + stamp-sha verb + plan ready-gate"
class = "chore"
status = "shipped"
order = 5976
spec = "docs/platform/t917_ticket_schema_v2.md"
plan = "docs/plans/t-917_6_plan.md"
executor = "claude-code"
notes = "Gate must land last (S.2-S.5 make the tree gate-satisfiable first) and WITH stamp-sha or every future ship wedges between the ship edit and the SHA stamp. Program shipped_at lives inside Status::Shipped while work carries the field — gate reads through both."
depends_on = [
    "T-917.4",
    "T-917.5",
]
parent = "T-917"
main_goal = "A shipped ticket without stamps and tokens cannot exist green anymore, the ship workflow closes itself with one verb after commit, and nothing goes ready without its own plan document."
context = [
    "Hard gate lands in check + ops::ship for shipped tickets.",
    "Gate must land last S.2-S.5; WITH stamp-sha or every future ship wedges.",
]
requirement = [
    "shipped requires created_at/completed_at RFC 3339 UTC + shipped_at SHA 7-40 hex.",
    "Each stamp measured or in estimated[]; tokens = receipt XOR estimate file.",
    "Plan ready-gate: plan key on ready-class; file must exist on disk.",
]
current_state = ["Program shipped_at lives inside Status::Shipped; work carries the field."]
approach = [
    "ticket stamp-sha <id> <sha> writes shipped_at AND auto-generates diff_loc estimate.",
    "ship -> commit -> stamp-sha -> green when no receipt exists.",
    "Live ready tickets get plans written this slice.",
]
verify = [
    "Planted shipped ticket missing completed_at reds naming ticket and field.",
    "Scratch end-to-end ship commit stamp-sha check green with auto-estimate.",
]
acceptance = [
    "Planted shipped ticket missing completed_at reds naming ticket and field",
    "Scratch end-to-end: ship -> commit -> stamp-sha -> check green, auto-estimate written with matching factor",
    "ops::ship on a created_at-less ticket refuses pre-write with the corpus byte-untouched",
    "mark-ready without a plan file refuses naming the path; live ready tickets carry plans",
    "check --strict prints shipped tokens measured/estimated: K/E plus per-source breakdown",
]
citations = [
    "t917_ticket_schema_v2.md",
    "ops::ship",
    "ticket stamp-sha",
    "RFC 3339 UTC",
]
shipped_at = "5e0b48fb"
created_at = "2026-08-14T21:54:38Z"
completed_at = "2026-08-15T00:20:44Z"
estimated = ["tokens"]
owns = [
    "xtask/src",
    "crates/tbd-tickets",
    "docs/plans",
]

[scope]
domain = "repo"
layer = "tickets"
