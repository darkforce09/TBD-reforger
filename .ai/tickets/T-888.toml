id = "T-888"
kind = "work"
title = "T-853 port: setsid + AF_UNIX socket lifecycle"
summary = "T-853 port: setsid + AF_UNIX socket lifecycle"
class = "docs"
status = "shipped"
order = 5690
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/mcp-daemon.sh to `cargo xtask mcp daemon` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 90 lines. tools/tbd-tools already ships an `mcpd` binary - prefer wiring to it over reimplementing. Sources lib/mcpd-bin.sh and lib/xtask-run.sh, so this is half of T-879's unblock. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/mcp-daemon.sh to `cargo xtask mcp daemon` and delete the script(s), their scripts/shell-inventory.txt line(s), any scripts/python-inventory.txt line(s), and repoint every caller. 90 lines. tools/tbd-tools already ships an `mcpd` binary - prefer wiring to it over reimplementing. Sources lib/mcpd-bin.sh and lib/xtask-run.sh, so this is half of T-879's unblock. ACCEPTANCE IS BYTE-FOR-BYTE: capture bash stdout+stderr+rc first, then diff on a clean tree AND on >=2 deliberately broken trees; the harness must assert the BASH side went red before believing a diff. A green run alone is not evidence (T-556). Build on crates/tbd-gate. Spec: docs/platform/t853_shell_to_xtask_waves.md"]

missing=0"""
main_goal = "Port mcp-daemon.sh to cargo xtask mcp daemon; wire existing mcpd."
context = [
    "T-853 shell->xtask slice; 90 lines setsid AF_UNIX socket lifecycle.",
    "tools/tbd-tools already ships mcpd binary; prefer wiring over reimplementing.",
    "Sources lib/mcpd-bin.sh lib/xtask-run.sh; half of T-879 unblock.",
]
requirement = [
    "Delete script(s) shell-inventory.txt line(s) python-inventory.txt line(s).",
    "Port to cargo xtask mcp daemon; repoint every caller.",
    "Prefer wiring tools/tbd-tools mcpd over reimplementing.",
]
current_state = [
    "scripts/mod/mcp-daemon.sh bash daemon live today.",
    "mcpd-bin.sh and xtask-run.sh dependencies block T-879.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
acceptance = [
    "Deleted scripts/mod/mcp-daemon.sh; mcp daemon xtask wired existing mcpd for setsid socket lifecycle.",
    "MCP call gates and dev-bootstrap repointed; shell inventory line removed.",
    "Port matched bash stdout stderr and rc on clean tree and deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-879 unblock dependency",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "759b6926 mcp-daemon port",
]
shipped_at = "759b6926"
created_at = "2026-08-12T10:06:22Z"
completed_at = "2026-08-12T10:07:44Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/mod/mcp-daemon.sh"]

[scope]
domain = "repo"
layer = "xtask"
