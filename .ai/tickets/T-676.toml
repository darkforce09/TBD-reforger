id = "T-676"
kind = "work"
title = "Trigger activation and effects — the Enfusion runtime"
summary = "Trigger activation and effects — the Enfusion runtime"
class = "chore"
status = "queued"
order = 4330
executor = "claude-code"
notes = """
workbench program (program 2) - NOT factory-dispatchable; no owns, no wave row

--- HEAD migration_legacy (verbatim) ---

Twelve of the thirteen `ATTR-FIELD-TRG-*` ids: everything except the geometry/palette half, which is the factory ticket T-079. An activation model (condition, repeat, timeout, owner side) and an effects model are a NEW ENFUSION RUNTIME in `apps/mod/tbd-framework/` plus the schema keys to carry them. Geometry can ride the shipped T-582 zone draw tool, which is why the editor half was separable at all. Read `$defs/zoneRules`' 16 keys before designing this: they have no ATTR-FIELD id and were never triaged against Eden, but they are the closest thing TBD already has to trigger semantics. EXECUTOR IS `workbench` DELIBERATELY: this modifies `packages/tbd-schema/schema/mission.schema.json` and/or `apps/mod/tbd-framework/`, which the CLAUDE.md executor gate puts out of reach of any coding agent. The factory filters on executor, so this tagging is the safety mechanism — do not flip it to claude-code.

EXECUTOR CORRECTED 2026-08-02: was filed `workbench` on the mistaken reading that any .c file needs the Arma Workbench GUI. It does not — scripts/mod/compile.sh compiles tbd-framework against the native Linux dedicated server headlessly, verified: "OK: compiled clean, 5707 files, 11182 classes, 832 ms, no Workbench". Schema edits are plain JSON. This is factory work. Per the model-routing rule, the .c portion routes to Fable 5 even though the executor is claude-code.

SCHEMA HALF SPLIT OUT to T-706 (one widening pass for the whole program). This ticket is the Enfusion reader plus any editor UI; it does not touch packages/tbd-schema.

missing=0
"""
depends_on = ["T-079"]
main_goal = "Ship Enfusion trigger activation and effects runtime in tbd-framework."
context = [
    "Twelve ATTR-FIELD-TRG ids except geometry palette half in T-079.",
    "zoneRules 16 keys lack ATTR id; closest existing trigger semantics.",
    "Schema split to T-706; this ticket is Enfusion runtime only.",
]
requirement = [
    "Activation model: condition repeat timeout owner side in TriggerRuntime.",
    "Effects model plus schema keys to carry them at runtime.",
    "Do not touch packages/tbd-schema; T-706 owns widening.",
]
current_state = [
    "Geometry rides shipped T-582 zone draw tool; editor half separable.",
    "New Enfusion runtime in apps/mod/tbd-framework Zones.",
    "Executor claude-code; headless compile verified no Workbench.",
]
approach = [
    "Read zoneRules keys before designing activation semantics.",
    "Implement TBD_TriggerRuntime.c reader after T-079 geometry lands.",
]
verify = [
    "Trigger runtime activates zones per mission payload attrs.",
    "No packages/tbd-schema edits in this slice.",
]
citations = [
    "T-079 zone geometry palette",
    "zoneRules sixteen keys untriaged",
    "T-706 schema widening split",
]
estimated = ["scope"]
owns = ["apps/mod/tbd-framework/Scripts/Game/TBD/Zones/TBD_TriggerRuntime.c"]

[scope]
domain = "mod"
layer = "scripts"
component = "zones"
