id = "T-1133"
kind = "work"
title = "Consolidate developer-tools duplicate helpers, repo-root lookup and fixture paths"
summary = "js_num exists twice (forest_contours.rs:24, json_number_formatting.rs:8), REQUIRED_EVERON_TOWNS twice (map_labels.rs:26, map_verification/labels.rs:26); world export and raster code import repo_root from browser_testing::server; fixture paths bypass repository_layout; census and validate-exports spawn cargo run -p xtask."
class = "chore"
status = "idea"
notes = "Paths under tools_v2/developer-tools/src. (1) Two js_num: world_export_pipeline/forest_contours.rs:24 returns Null for a non-finite value, world_export_pipeline/json_number_formatting.rs:8 panics ('finite'); keep one. (2) Two REQUIRED_EVERON_TOWNS [&str; 7]: map_raster_pipeline/map_labels.rs:26 (private) and map_verification/labels.rs:26 (pub), same seven names; the pipeline should use the verifier's constant or a shared one. (3) repo_root lives in browser_testing/server.rs:434 while repository_layout.rs holds every other checkout path; world_export_pipeline (chunk_partitioner.rs:22, chunk_partitioner/object_partitioning.rs:12, classify.rs:14, export_preparation.rs:18, mathematical_verification.rs:19, reclassify.rs:48, roads_emit.rs:48 and tests), map_raster_pipeline (aerial_orthophoto.rs:18, cartographic_rendering.rs:13), enfusion_tooling/mcp_broker.rs:185 and browser_testing/dom_oracle/fixture_router.rs:17 import it from there; move it to repository_layout. (4) Fixture paths spelled outside repository_layout: browser_testing/dom_oracle/routes.rs:108 (fixtures/dom_oracle/oracle-freeze), browser_testing/route_drift.rs:19 (fixtures/dom_oracle/manifests/routes.csv) and :17 (apps/website/frontend/src/router.rs), browser_testing/dom_oracle/fixture_router.rs:17 (apps/website/frontend/tests/fixtures/api), and the blueprint tests' test_fixtures/blueprint paths (blueprint/tests/module/tests.rs:6, tests/rotation_pin/tests.rs:27, tests/verify/tests.rs:201). (5) world census (world_export_pipeline/export_preparation/object_census.rs:209) and world validate-exports (export_preparation/export_validation/artifact_integrity.rs:276) spawn 'cargo run -q -p xtask -- schema type-inventory', and map verify-cartographic spawns cargo xtask steps (map_raster_pipeline/cartographic_rendering/build_tile_pyramid.rs:216-222): a nested cargo build inside a running tool, on whatever toolchain and target directory the shell has. xtask depends on developer-tools, so decide between moving the shared check into developer-tools and leaving the orchestration to xtask. Source: README writers R20.1 (R20.1r) and R20.2."
created_at = "2026-09-25T08:59:41Z"

[scope]
domain = "repo"
layer = "tools"
