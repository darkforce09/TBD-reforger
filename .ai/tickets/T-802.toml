__keys = [
    "id",
    "order",
    "program",
    "status",
    "stream",
    "targets",
    "executor",
    "surfaces",
    "impact",
    "title",
    "summary",
    "notes",
]
__ord = 779
executor = "claude-code"
id = "T-802"
impact = [
    "ui",
    "perf",
]
notes = "T-057 removal history is the trap; HUD is the perf oracle per CLAUDE.md editor bench notes | SHIPPED W207: hover cursor via a 40ms-throttled query over the EXISTING pick point-sets, hysteresis keyed off the pick radius (T-808-proof), suppressed during drag/draw/armed. Perf measured two ways (HUD rf +0.06ms; direct handler cost ~0.03ms/move) with a within-build control because cross-build variance exceeds the 1ms threshold. MARKERS DELIBERATELY EXCLUDED — route_target has no marker arm, so claiming pickability would be the T-754 dead-click lie; independently corroborates T-838."
order = 4840
program = "eden"
status = "shipped"
stream = "mission-creator"
summary = "UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-8 — VERIFIED: canvas cursor is 'auto' over a unit, over empty ground, everywhere — nothing signals pickability. History: hover picking was DELIBERATELY REMOVED for perf at T-057 — so this is not a CSS one-liner; it needs a cheap hover hit-test. FIX: throttled (e.g. 30-60ms) spatial-hash lookup over slot/vehicle/marker/comment points on pointermove (the marquee/pick paths already have the point sets — reuse their source, do not re-derive), set cursor pointer/grab over a pickable, default otherwise; suppress during drags/draw modes. ⚠️ TRAPS: perf budget is the whole reason this was removed — the acceptance includes a frame-time guard; the HUD rf <ms> readout is the oracle; no per-frame full picks: spatial hash or radius query only; cursor must not flicker at cell boundaries (hysteresis). ACCEPTANCE: scripted — computed cursor over a unit = pointer (or grab), over empty = default; sweep the pointer across the 9-slot line at speed: HUD rf delta vs baseline <= 1ms median (telemetry HUD Ctrl+Alt+D readback); no cursor churn (>=3 changes) while hovering one entity."
surfaces = [
    "MAP",
    "UI",
]
targets = ["website"]
title = "The cursor tells the hand what is grabbable"
