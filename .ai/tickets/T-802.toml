id = "T-802"
kind = "work"
title = "The cursor tells the hand what is grabbable"
summary = "The cursor tells the hand what is grabbable"
class = "bug"
status = "shipped"
order = 4840
executor = "claude-code"
notes = """
T-057 removal history is the trap; HUD is the perf oracle per CLAUDE.md editor bench notes | SHIPPED W207: hover cursor via a 40ms-throttled query over the EXISTING pick point-sets, hysteresis keyed off the pick radius (T-808-proof), suppressed during drag/draw/armed. Perf measured two ways (HUD rf +0.06ms; direct handler cost ~0.03ms/move) with a within-build control because cross-build variance exceeds the 1ms threshold. MARKERS DELIBERATELY EXCLUDED — route_target has no marker arm, so claiming pickability would be the T-754 dead-click lie; independently corroborates T-838.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-8 — VERIFIED: canvas cursor is 'auto' over a unit, over empty ground, everywhere — nothing signals pickability. History: hover picking was DELIBERATELY REMOVED for perf at T-057 — so this is not a CSS one-liner; it needs a cheap hover hit-test. FIX: throttled (e.g. 30-60ms) spatial-hash lookup over slot/vehicle/marker/comment points on pointermove (the marquee/pick paths already have the point sets — reuse their source, do not re-derive), set cursor pointer/grab over a pickable, default otherwise; suppress during drags/draw modes. ⚠️ TRAPS: perf budget is the whole reason this was removed — the acceptance includes a frame-time guard; the HUD rf <ms> readout is the oracle; no per-frame full picks: spatial hash or radius query only; cursor must not flicker at cell boundaries (hysteresis). ACCEPTANCE: scripted — computed cursor over a unit = pointer (or grab), over empty = default; sweep the pointer across the 9-slot line at speed: HUD rf delta vs baseline <= 1ms median (telemetry HUD Ctrl+Alt+D readback); no cursor churn (>=3 changes) while hovering one entity.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-8 — VERIFIED: canvas cursor is 'auto' over a unit, over empty ground, everywhere — nothing signals pickability. History: hover picking was DELIBERATELY REMOVED for perf at T-057 — so this is not a CSS one-liner; it needs a cheap hover hit-test. FIX: throttled (e.g. 30-60ms) spatial-hash lookup over slot/vehicle/marker/comment points on pointermove (the marquee/pick paths already have the point sets — reuse their source, do not re-derive), set cursor pointer/grab over a pickable, default otherwise; suppress during drags/draw modes. ⚠️ TRAPS: perf budget is the whole reason this was removed — the acceptance includes a frame-time guard; the HUD rf <ms> readout is the oracle; no per-frame full picks: spatial hash or radius query only; cursor must not flicker at cell boundaries (hysteresis). ACCEPTANCE: scripted — computed cursor over a unit = pointer (or grab), over empty = default; sweep the pointer across the 9-slot line at speed: HUD rf delta vs baseline <= 1ms median (telemetry HUD Ctrl+Alt+D readback); no cursor churn (>=3 changes) while hovering one entity."]

missing=0
"""
main_goal = "Throttled hover cursor over pickables without perf regression."
context = [
    "O-8 canvas cursor auto everywhere; nothing signals pickability.",
    "T-057 deliberately removed hover picking for perf.",
    "Marquee pick paths already have point sets; reuse them.",
]
requirement = [
    "Throttled spatial-hash lookup on pointermove over slot vehicle comment points.",
    "Cursor pointer or grab over pickable; default otherwise.",
    "Suppress during drags draw modes; HUD rf delta <=1ms median.",
]
current_state = [
    "route_target has no marker arm; markers deliberately excluded.",
    "T-754 dead-click lie if claiming marker pickability.",
    "Hysteresis keyed off pick radius to avoid flicker.",
]
approach = [
    "Reuse existing pick point sets; no per-frame full picks.",
    "40ms throttle with hysteresis; suppress during drag draw armed.",
]
verify = [
    "Computed cursor over unit pointer; over empty default.",
    "Sweep 9-slot line: HUD rf delta <=1ms; no cursor churn hovering one entity.",
]
citations = [
    "editor_hostile_ux_review O-8",
    "T-057 hover removal",
    "T-808 hysteresis proof",
    "CLAUDE editor bench rf",
]
shipped_at = "00eea875"
created_at = "2026-08-10T22:20:50Z"
completed_at = "2026-08-11T03:00:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/frontend/src/mission_editor.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["map_canvas"]
