id = "T-181.11"
kind = "work"
title = "One-life death model in SpawnManager"
summary = "m_bOneLife terminal death; IsPlayerDead and CountAliveForFaction primitives."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "ONE LIFE in TBD_SpawnManager: m_bOneLife attribute (default on), m_mDeadPlayers map, OnPlayerKilled now terminal — slot STAYS claimed so the seat is not recycled and reconnect still resolves. Added IsPlayerDead() and CountAliveForFaction() (the primitive T-181.13 side-eliminated needs; counts claimed slots so a never-deployed player cannot silently end the round). m_bAutoDeploy path retained for PIE."
parent = "T-181"
context = ["Event loop needs one life before spectator and faction elimination."]
requirement = [
    "OnPlayerKilled terminal; slot stays claimed through death and reconnect.",
    "CountAliveForFaction counts claimed slots; m_bAutoDeploy kept for PIE.",
]
current_state = [
    "Death recycled slots; no terminal one-life map or faction alive count.",
    "Event loop lacked IsPlayerDead and CountAliveForFaction primitives.",
]
approach = [
    "Default m_bOneLife on; terminal OnPlayerKilled keeps slot claimed.",
    "Add IsPlayerDead and CountAliveForFaction; retain m_bAutoDeploy for PIE.",
]
verify = ["scripts/mod/compile.sh on d8fc6abc (one-life)."]
acceptance = [
    "One-life terminal death shipped; slot stayed claimed through death and reconnect.",
    "CountAliveForFaction counted claimed undeployed slots so rounds could not silently end.",
]
citations = [
    "Source: commit d8fc6abc — factory bootstrap.",
    "commit d8fc6abc one life bootstrap",
    "notes m_bAutoDeploy retained for PIE",
]
shipped_at = "cc7ab8fd"
created_at = "2026-07-25T09:37:59Z"
completed_at = "2026-07-25T09:37:59Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "repo"
layer = "docs"
