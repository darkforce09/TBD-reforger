id = "T-169"
kind = "work"
title = "Leptos VirtualOutliner at mission scale"
summary = "Windowed VirtualOutliner for Editor Layers and ORBAT at mission scale."
class = "bug"
status = "shipped"
order = 1660
spec = ".ai/artifacts/missed_items_handoff.md"
executor = "claude-code"
depends_on = ["T-159"]
main_goal = "As a mission maker with a 100k+ slot mission the outliner stays instant instead of freezing the tab."
context = ["Large slot trees built N DOM rows and froze the editor tab."]
requirement = [
    "outliner.rs flatten pre-order rows; VIRTUAL_SLOT_THRESHOLD equals 50.",
    "eden_chrome virtual_tree: eager below threshold, spacer-windowed scroll above.",
    "Both Editor Layers and ORBAT docks; __outlinerStats and smoke_virtual_outliner.",
]
current_state = [
    "SHIPPED @ tag T-169 (T-064 parity): outliner.rs gains `flatten` (pre-order rows) + `VIRTUAL_SLOT_THRESHOLD=50`;",
    "eden_chrome renders each dock tree via `virtual_tree` — eager below the threshold,",
    "a fixed-height scroll container drawing only the visible slice (+overscan) between spacer divs above it, so a mission-scale tree never builds N DOM rows.",
    "Applies to BOTH Editor Layers and ORBAT;",
]
approach = [
    "outliner.rs flatten pre-order plus VIRTUAL_SLOT_THRESHOLD 50.",
    "eden_chrome virtual_tree spacer window; __outlinerStats for both docks.",
]
verify = [
    "`window.__outlinerStats.{editorLayers,orbat} = {total,rendered,threshold}` for the gate.",
    "New smoke_virtual_outliner (6/6): eager==total below, rendered<total above, threshold 50, both trees windowed, windowed row still selects.",
    "`__missionDoc.seed_slots(n)` smoke hook added to push past the threshold.",
]
acceptance = [
    "VirtualOutliner windowed both Editor Layers and ORBAT docks.",
    "smoke_virtual_outliner 6/6 pass; rendered rows below total above threshold 50.",
]
citations = ["Source: commit 47ec9fb9."]
shipped_at = "47ec9fb9"
created_at = "2026-07-17T22:56:54Z"
completed_at = "2026-07-17T22:56:54Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
