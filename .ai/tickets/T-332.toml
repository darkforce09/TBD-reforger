id = "T-332"
kind = "work"
title = "Event PATCH cannot clear briefing or re-attach mission"
summary = "PATCH /events/:id cannot clear a briefing or banner, and a mission cannot be re-attached"
class = "bug"
status = "shipped"
order = 3148
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "PATCH /events/:id can clear briefing and banner_image_url, and admins can re-attach a mission after detaching the last one from an event."
context = [
    "Two gaps found by T-226 while wiring the Event Manager's edit path.",
    "Both are backend; T-226 owned only the frontend file and correctly left them.",
]
requirement = [
    "1. **No null path on PATCH.** Decide the shape — a nullable wrapper, an explicit clear list,",
    'or bless `""` as the documented clear signal — then make the UI match.',
    "2. **A mission cannot be attached after creation.** Add an attach path to the Event Manager's Attached Missions roster, next to Detach.",
]
current_state = [
    "Every field on `PatchEventInput` is `Option<String>` where None means 'do not touch', so there is no way to CLEAR `briefing` or `banner_image_url`.",
    'The UI T-226 shipped sends `""` to blank them, which works but is a workaround standing in for a missing contract.',
    "`POST /events/:id/missions` has a caller ONLY in the create flow, so an admin who detaches the last mission must delete and recreate the whole operation.",
    "The endpoint is not dead, which is why it fell outside T-226's three bullets —",
    "but T-226 shipping the detach button makes this reachable and therefore much more visible than it was.",
]
approach = [
    'Decide PATCH clear contract: nullable wrapper, explicit clear list, or bless `""` as documented clear signal.',
    "Make the Event Manager UI match the chosen PATCH clear contract.",
    "Add attach path to Event Manager Attached Missions roster, next to Detach.",
]
verify = ["cargo test -p website-api --test events patch_clears_briefing_banner_and_mission_reattach_works."]
acceptance = ['PATCH cleared briefing and banner via ""; admins re-attached missions after detaching the last one.']
citations = [
    "T-226 Event Manager edit",
    "PatchEventInput Option<String>",
    "POST /events/:id/missions",
]
shipped_at = "019e5ddd"
created_at = "2026-07-27T11:12:41Z"
completed_at = "2026-07-27T15:16:48Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 7 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/events.rs",
    "apps/website/frontend/src/event_manager.rs",
    "apps/website/api/tests/events.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
