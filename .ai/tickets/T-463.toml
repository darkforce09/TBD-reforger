id = "T-463"
kind = "work"
title = "verify-t438/t456 shell gates still unwired from wave cold gate"
summary = "verify-t438/t456 shell gates still unwired from wave cold gate"
class = "chore"
status = "shipped"
order = 3302
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Residual from T-462 / wave 24. wave.sh now runs verify-t439 and verify-t444 after schema, but scripts/mod/verify-t438-deploy-staging-compose-path.sh and verify-t456-mission-rest-size-gate.sh remain agent-local only — cold gate never executes them.

Repro: corrupt deploy-staging compose path or strip REST size check → wave.sh gate still PASS until those scripts are invoked manually.

Cure: wire both into wave.sh gate_slice + cmd_gate next to the T-439/T-444 steps (same pattern).

missing=0
"""
priority = 2
main_goal = "Wire verify-t438 and verify-t456 into wave.sh cold gate."
context = [
    "Residual from T-462 / wave 24.",
    "wave.sh runs verify-t439 and verify-t444 after schema.",
]
requirement = [
    "verify-t438 and verify-t456 remain agent-local only.",
    "Cold gate never executes verify-t438 or verify-t456.",
    "Wire both into wave.sh gate_slice + cmd_gate.",
]
current_state = [
    "Corrupt deploy-staging compose → wave.sh gate still PASS.",
    "Strip REST size check → PASS until scripts invoked manually.",
]
approach = [
    "Wire verify-t438 into wave.sh gate_slice + cmd_gate.",
    "Wire verify-t456 next to T-439/T-444 steps (same pattern).",
]
citations = [
    "T-462 wave 24",
    "verify-t438 compose path",
    "verify-t456 REST size",
]
shipped_at = "3269faf5"
created_at = "2026-07-27T02:06:43Z"
completed_at = "2026-07-27T02:12:48Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/platform/wave.sh",
    "scripts/mod/verify-t438-deploy-staging-compose-path.sh",
    "scripts/mod/verify-t456-mission-rest-size-gate.sh",
]

[scope]
domain = "repo"
layer = "xtask"
