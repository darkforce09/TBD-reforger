__keys = [
    "id",
    "order",
    "program",
    "status",
    "stream",
    "targets",
    "executor",
    "surfaces",
    "impact",
    "title",
    "summary",
    "notes",
]
__ord = 819
executor = "claude-code"
id = "T-846"
impact = [
    "ui",
    "auth",
]
notes = "wave208 verifier F1 MINOR; toast-on-redirect already works"
order = 5280
program = "eden"
status = "deferred"
stream = "mission-creator"
summary = "WAVE-208 VERIFIER MINOR (wave208.md F1). Denial redirect appends `?role_notice=mission_maker` and comments claim it remains for deep links, but no SPA consumer reads the query — only the navigate-time toast fires. Cold open of `/missions/:id?role_notice=mission_maker` shows no toast (param inert). Primary UX (redirect + toast on denial) works. FIX: either consume the query on the mission overview (re-show notice on refresh/deep-link) or drop the dead-param writers/comments/asserts. ACCEPTANCE: either a cold open of the overview URL with role_notice shows the notice once, OR `rg role_notice` under apps/website/frontend/src finds zero hits."
surfaces = ["SHELL"]
targets = ["website"]
title = "role_notice query is written on editor denial but never read"
