id = "T-181.36"
kind = "work"
title = "Add kit:us_medic to close golden fixture"
summary = "Register kit:us_medic with measured Character_US_Medic.et GUID so slot-loadout-coverage golden passes gate."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "Fixed by adding kit:us_medic to Data/registry.json (+ mirrored to kit-aliases.json) rather than editing the fixture, because a US medic is ALREADY a real thing this project ships — seeded at api/seeds/registry_dev.sql:20 and served by GET /api/v1/registry. The prefab GUID was MEASURED not invented: {C9E4FEAF5AAC8D8C}Character_US_Medic.et agrees across the T-068.10.2 Workbench census, registry-items.workbench.json and the API seed. Inventing a GUID would have been strictly worse than the bug — the validator would pass and the spawn would fail. slot-loadout-coverage now boots PASS (errors=0, warnings=2 at baseline, 4/4 repeats); bridgehead stays PASS at 18. LATENT SECOND INSTANCE FOUND: last-stand-at-montfort.json carries four dangling kit:uk_* aliases. Reforger vanilla ships no UK faction so they cannot be added honestly — waived with that reason in KNOWN_UNRESOLVABLE_KITS. They are ORBAT-only today and go live the moment that mission is flattened to slots."
parent = "T-181"
context = [
    "T-181.34 kit-alias gate rejected dangling kit in slot-loadout-coverage golden.",
    "US medic already seeded in registry_dev.sql and served by GET /api/v1/registry.",
]
requirement = [
    "Add kit:us_medic to Data/registry.json and mirror kit-aliases.json.",
    "Use measured Character_US_Medic.et GUID from Workbench census, not invented.",
    "Waive last-stand-at-montfort uk_* aliases as KNOWN_UNRESOLVABLE_KITS.",
]
current_state = ["Kit-alias gate flagged dangling kit:us_medic in slot-loadout-coverage golden."]
approach = ["Register kit:us_medic with measured Character_US_Medic.et GUID; waive uk_* as KNOWN_UNRESOLVABLE_KITS."]
verify = ["slot-loadout-coverage gate PASS errors=0 warnings=2."]
acceptance = ["slot-loadout-coverage gate passed errors=0; bridgehead stayed pass at eighteen kit-only slots."]
citations = [
    "Source: commit 35c391d4 — us_medic registry add.",
    "Source: commit c612e416 — slot-loadout golden.",
    "35c391d4 us_medic registry",
]
shipped_at = "35c391d4"
created_at = "2026-07-25T14:38:01Z"
completed_at = "2026-07-25T14:38:01Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"
owns = ["packages/tbd-schema/golden-missions/**"]

[scope]
domain = "repo"
layer = "xtask"
