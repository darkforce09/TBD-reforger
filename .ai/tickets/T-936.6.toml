id = "T-936.6"
kind = "work"
parent = "T-936"
title = "Dynamic AI spawning and garrison modules"
summary = "spawnModules (wave or garrison kind, faction, group template, position or zone, count, interval, maxAlive, trigger) become authored mission data with an editor panel, a core validator and TBD_DynamicSpawner.c running waves and garrisons in-game."
class = "feature"
status = "ready"
order = 7520
priority = 2
spec = "docs/specs/audit_2026_09/t936_mission_logic.md"
plan = "docs/plans/t-936_6_plan.md"
executor = "claude-code"
main_goal = "Authors define AI waves and garrisons that the framework spawns at runtime."
context = [
    "Master audit S1 lists dynamic spawn and garrison modules among the missing mission logic; no spawner script exists under Scripts/Game/TBD/Gamemode.",
    "Related, not duplicated: T-299 phantom opfor (AI faction placeholder), T-678 group AI behaviour, T-676 triggers (used as spawn triggers here).",
    "Shared files with T-936.1-.5; packs after T-936.5.",
]
requirement = [
    "Schema: top-level spawnModules [{id, kind: wave|garrison, factionKey, groupTemplate, x, z or zoneId, count, intervalSeconds?, maxAlive?, triggerId?}], additionalProperties false.",
    "New mission/spawn_modules.rs (registered in mission/mod.rs): model, validator (known faction, position or zone exclusive, positive counts); registered in extensions.rs AUTHORED_BLOCKS.",
    "New panels/spawn_modules.rs (registered in panels/mod.rs): module list with kind, faction, template, placement and trigger pickers; undoable.",
    "New Gamemode/TBD_DynamicSpawner.c: server spawns wave groups on interval or trigger up to maxAlive; garrison kind spawns once and holds; cleans up on mission end.",
]
current_state = [
    "No spawn vocabulary, panel or runtime; every AI unit is placed statically.",
]
approach = [
    "Verify on main: a payload carrying spawnModules flattens without it; paste the red.",
    "Add schema and golden; write spawn_modules.rs with validator tests; register; build the panel; write the spawner; compile the mod.",
    "Perturbation: allow both x/z and zoneId at once; the exclusivity test must go red; restore, touch, green.",
]
verify = [
    "cargo test -p map-engine-core --all-features",
    "cargo xtask ci schema-validate",
    "cargo xtask mk ci-local-leptos",
    "cargo xtask mk leptos-gates",
    "cargo xtask mod compile",
    "cargo xtask platform wave gate --slice T-936.6",
]
acceptance = [
    "A mission with one wave module and one garrison flattens to a spawnModules block; in-game the wave respects interval and maxAlive and the garrison holds (checklist).",
    "A module naming an unknown faction or both a position and a zone is refused in the panel.",
]
depends_on = ["T-936.5"]
created_at = "2026-09-05T02:09:02Z"
owns = [
    "packages/tbd-schema/schema/mission.schema.json",
    "apps/website/frontend/src/editor/panels/spawn_modules.rs",
    "apps/website/frontend/src/editor/panels/mod.rs",
    "crates/map-engine-core/src/mission/spawn_modules.rs",
    "crates/map-engine-core/src/mission/mod.rs",
    "crates/map-engine-core/src/mission/extensions.rs",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_DynamicSpawner.c",
]

[scope]
domain = "mod"
layer = "scripts"
component = "gamemode"
