id = "T-181.2.1"
kind = "work"
title = "CRF capability matrix with untriaged gate"
summary = "docs/mod/capability_verdicts.tsv plus verify-capability; every CRF capability must have a TBD verdict."
class = "feature"
status = "shipped"
order = 1810
spec = "docs/mod/t181_event_mod_program.md"
executor = "claude-code"
notes = "docs/mod/capability_verdicts.tsv + make verify-capability. 57 capabilities, zero untriaged. A forgotten CRF capability is now a build error."
parent = "T-181"
context = ["Factory gates must catch CRF capabilities with no TBD replacement plan."]
requirement = [
    "Commit capability_verdicts.tsv covering every indexed CRF capability.",
    "verify-capability fails on any UNTRIAGED row; fifty-seven verdicts, zero gaps.",
]
current_state = ["Indexed CRF capabilities lacked TBD verdicts; UNTRIAGED rows slipped through."]
approach = ["capability_verdicts.tsv plus enf capability gate; forgot row fails build."]
verify = ["verify-capability: fifty-seven capabilities, zero untriaged."]
acceptance = ["Fifty-seven capabilities triaged; verify-capability exited 0 with zero UNTRIAGED rows."]
citations = [
    "Source: commit d8fc6abc — Enfusion factory.",
    "Source: t181_event_mod_program.md — T-181.2.1 row.",
]
created_at = "2026-07-25T00:00:00Z"
completed_at = "2026-07-31T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-181 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "docs"
