id = "T-694"
kind = "work"
title = "Mission shape editable after creation — game mode, min/max players"
summary = "Mission shape editable after creation — game mode, min/max players"
class = "feature"
status = "shipped"
order = 4480
executor = "claude-code"
notes = """
— SHIPPED wave 113 (editor factory), f91d8854; verifier wave113.md. REINTERPRETED BY THE OPERATOR: game_mode editable after creation + player count DERIVED from MissionDocCore::slot_count(); NO min_players, no migration, no backend change (dto.rs is comment-only, R-api goldens untouched). Both figures shown side by side with the disagreement stated and neither enforced; a source-scan pin fails the build if clamp/128/.min(/.max(/min_players ever appears in that section. The authoritative-player-cap question is left open for the operator.

--- HEAD migration_legacy (verbatim) ---

NEW-F6. Rows DLG-MP-001..003, the one genuine gap the Edit: Multiplayer dialog surfaced. game_mode and max_players are create-dialog only (create_mission_dialog.rs:158-166, :199-215) and reach a compiled mission through the row, not the document (dto.rs:895). A mission cannot change shape after creation. PATCH /missions/:id already accepts both.

missing=0
"""
main_goal = "Make game_mode editable after creation; show derived player count."
context = [
    "NEW-F6 rows DLG-MP-001 through DLG-MP-003 Edit Multiplayer dialog gap.",
    "game_mode and max_players create-dialog only; PATCH missions id accepts both.",
]
requirement = [
    "game_mode editable after creation not create-dialog only.",
    "Player count derived from MissionDocCore slot_count shown beside max_players.",
    "No min_players migration or backend change; dto.rs comment-only.",
]
current_state = [
    "create_mission_dialog.rs 158-166 199-215; dto.rs 895 row not document.",
    "Mission cannot change shape after creation today.",
]
approach = ["Edit multiplayer settings in eden_settings; show both counts unstated disagreement."]
verify = ["Source-scan pin fails if clamp min max min_players appears in section."]
acceptance = [
    "game_mode select PATCHes the missions row after creation using the server enum table only.",
    "Derived slot_count displays beside max_players with disagreement noted when the two figures differ.",
    "dto.rs stayed comment-only; no min_players field, migration, or backend shape change shipped.",
]
citations = [
    "DLG-MP-001 multiplayer dialog",
    "create_mission_dialog.rs game_mode",
    "dto.rs row not document",
]
shipped_at = "62a76146"
created_at = "2026-08-07T02:49:14Z"
completed_at = "2026-08-07T02:57:15Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_settings.rs",
    "apps/website/frontend/src/dto.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["env_settings"]
