id = "T-697"
kind = "work"
title = "Selection filter and document search"
summary = "Selection filter and document search"
class = "feature"
status = "shipped"
order = 4510
executor = "claude-code"
notes = """
— SHIPPED wave 119 (editor factory), cf11d3c9; verifier wave119.md. Document search over all eight placeable collections plus a selection filter, REUSING T-084's grammar (the matcher is private, so it hands filter_catalog a faithful two-node projection per candidate) and routing T-696's matches_query through the same call, so the editor now has ONE query language across the layers tree, bookmarks, locations and search. Applies the T-754 lesson from the other side: DocKind::is_selectable encodes the router's real limit once, selectable hits are buttons and everything else is inert with aria-disabled and a stated reason. Dock width arithmetic done against T-637's consts rather than eyeballed — no third tab, because one does not fit. Also caught T-742 live with a sharper repro than the original.

--- HEAD migration_legacy (verbatim) ---

3den E4 + 3DEN-TOOL-011. Search the text attributes of every entity in the mission, and filter a selection down by type or faction. TBD has search over the asset catalogue (T-055) but nothing over placed entities. At 19,627 slots median 137 per mission in the WOG corpus, find-by-name is not optional.

missing=0"""
main_goal = "Document search over placed entities plus selection filter by type or faction."
context = [
    "3den E4 3DEN-TOOL-011 gap row.",
    "WOG corpus 19627 slots median 137 per mission.",
]
requirement = [
    "Search text attributes of every entity in mission.",
    "Filter selection down by type or faction.",
]
current_state = [
    "T-055 search over asset catalogue only.",
    "Nothing over placed entities; find-by-name not optional.",
]
approach = [
    "Reuse T-084 query grammar via private matcher projection; route matches_query through one call site.",
    "DocKind::is_selectable gates selectable hits; non-selectable rows render inert with aria-disabled reason.",
]
verify = [
    "cargo test -p website-frontend the_placed_document_is_searchable_by_text",
    "cargo test -p website-frontend a_faction_only_hit_names_faction_not_the_first_text_attribute",
]
acceptance = [
    "Document search covers eight placeable collections with T-084 grammar shared across dock layers.",
    "Selection filter narrows by type or faction; non-selectable hits stay inert with stated reason per T-754 lesson.",
    "T-696 matches_query routes through the same matcher so one query language spans layers, bookmarks, and search.",
]
citations = [
    "3den E4 3DEN-TOOL-011",
    "T-055 catalogue search",
    "WOG 19627 slots median",
]
shipped_at = "dc3acbf6"
created_at = "2026-08-07T08:25:32Z"
completed_at = "2026-08-07T08:29:27Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/eden_dock_left.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "dock_left",
]
