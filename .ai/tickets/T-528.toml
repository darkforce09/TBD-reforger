id = "T-528"
kind = "work"
title = "W47 hotfix: me.rs arma_linked still is_some() after T-350"
summary = "W47 hotfix: me.rs arma_linked still is_some() after T-350"
class = "bug"
status = "shipped"
order = 3368
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Wave 47 verifier P1/MAJOR vs T-350 ticket claim. T-350 fixed auth/oauth (+ leave reason) but title/summary name four linked sites including me.rs:87 and me.rs:170 which still use u.arma_id.is_some() — planted whitespace arma_id reports linked=true on GET /me and /me/link/status. Cure: use arma_id_is_linked (or same trim/nonempty helper) at both me.rs sites; Class-R/IT that RED on is_some()-only. Owns: apps/website/api/src/handlers/me.rs (and tests if needed). Do not touch events.rs roster (T-529).

missing=0"""
priority = 0
main_goal = "Use arma_id_is_linked at me.rs sites; not is_some()."
context = [
    "Wave 47 verifier P1/MAJOR vs T-350 claim.",
    "me.rs:87 and me.rs:170 still use u.arma_id.is_some().",
]
requirement = [
    "Use arma_id_is_linked or trim/nonempty helper at both sites.",
    "Class-R/IT must RED on is_some()-only check.",
]
current_state = [
    "Planted whitespace arma_id reports linked=true on GET /me.",
    "Same on /me/link/status.",
]
approach = ["Replace is_some() with arma_id_is_linked at both me.rs sites."]
verify = ["Class-R/IT RED on is_some()-only; whitespace arma_id not linked."]
citations = [
    "Wave 47 verifier P1/MAJOR",
    "T-350 linked sites claim",
    "me.rs is_some whitespace linked",
]
shipped_at = "35c13199"
created_at = "2026-07-27T11:37:32Z"
completed_at = "2026-07-27T11:41:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/me.rs",
    "apps/website/api/tests/t528_me_whitespace_linked.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
