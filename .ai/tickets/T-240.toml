id = "T-240"
kind = "work"
title = "Cargo capacity is warn-only and never blocks"
summary = "Cargo capacity is warn-only and never blocks"
class = "bug"
status = "shipped"
order = 2580
executor = "claude-code"
main_goal = "CargoBudget over() never blocks save, export or compile, and nothing is recursive."
context = [
    "== MOD BEHAVIOUR ANSWERED 2026-07-26 (paid-for subagent result; parent slice was cut for budget) ==",
    "== RECURSION ANSWERED: NOT REACHABLE (second paid-for subagent result) ==",
    "SO T-240 IS NOW FULLY SCOPED WITHOUT AN AGENT: recursion needs nothing; the frontend block is the wrong layer (the mod silently drops the row and nobody reads IsComplete);",
    "the two findings above are the only actionable items and neither is what the ticket asked for. Re-scope or close on next read.",
]
requirement = ["Cargo capacity is warn-only and never blocks."]
current_state = [
    "A real capacity model exists — CARGO_CONTAINERS (arsenal_rules.rs:595), grid dims, VOLUME_PER_CELL_CM3=50, CargoBudget (:721-754) — but over() (:729) never blocks save, export or compile, and nothing is recursive.",
    "The mod has NO runtime capacity math at all — zero volume/weight/grid arithmetic under Scripts/Game/.",
    "InsertCargo pushes at the engine and reads the bool; it does not even call CanInsertItemInStorage (that pre-check exists only on the weapon-slot path, :491/:495).",
    "On a cargo item that will not fit (TBD_LoadoutEquipHelper.c:1108-1145): it tries the authored container, then ANY storage on the character (logged Degrade/WARNING);",
    "if nothing accepts it, it deletes the entity, BREAKS the row qty loop, logs one ERROR naming slot+item+'no storage would accept unit N/M (character full)', and continues.",
    "So it drops THE ENTIRE REMAINING QUANTITY of that row, not one item. Never aborts the loadout, never leaves the character naked, never refuses to spawn the body.",
    "NOTHING READS THE RESULT. ReportVerdict only prints. bool IsComplete() (:197-200) has ZERO callers repo-wide — the answer to 'did we deliver the authored JSON' is computed and discarded.",
    "SpawnSlotBody returns the body unconditionally (:1070); MaterializeSlotBodies counts a failure only when the body is null; boot proceeds to LOBBY regardless.",
    "The only surface is log text, and the only thing that turns it into a failure is world-boot.sh's no-TBD-ERROR check — a CI gate, not runtime.",
    "SO: the operator gets a body in the field missing kit they authored, with a log line nobody reads.",
    "That makes frontend warn-only genuinely insufficient — but the durable fix is mod-side (consume IsComplete, or pre-check with Can*), not a client block.",
    "The website's CargoBudget model derives from Workbench export-time data (TBD_RegistryScan.c:896-909, cells = Ceil(maxVolume/50), hardcoded grid w=4) which the mod NEVER READS BACK.",
    "Also: recursion is a non-question at runtime — there is no capacity model to recurse through.",
    "  CargoRow is three scalars (container, item, qty) with NO id/parent/path — a row cannot reference another row, so a tree is not expressible even in principle (arsenal_rules.rs:586).",
    "  mission.schema.json:90-93 cargoContainer is a flat 4-token enum, and :282-297 sets additionalProperties:false — a children/parentId key is rejected outright. loadout-export.schema.json mirrors it byte-identically.",
    "  Enforced, not documented: golden-missions-invalid/cargo-container-typo.json is a negative golden, and validated_compiled_body (missions.rs:1071-1116) 500s on any finding.",
    "  The mod mirrors it independently with a flat struct and a 4-way if-chain (TBD_LoadoutEquipHelper.c:1035-1051) that never descends.",
    "  faction-library.schema.json:108-119 does NOT close the container enum — it is an open string, diverging from its two sibling schemas.",
    "  So an arbitrary token survives validate_faction_library_doc and dies later as a 500 at the compiled gate: a server error where an author-time 400 belongs.",
    "  The SEED path bypasses the UI kind filter entirely. Of 16,223 character_default_cargo edges, 1,946 have a from_node kind outside CARGO_ADD_KINDS (arsenal.rs:843).",
    "  It happens not to seed any backpack/vest — data luck, not a rule.",
    "AND: real nesting EXISTS in the source scan and is deliberately flattened away.",
    "cargo_container_from_evidence (arsenal_rules.rs:180-192) keeps only the first path segment, discarding 3,462 four-segment edges like Vest/Vest_ALICE_GL.et/MagPouch/... — a magazine inside a pouch inside a vest, collapsed to 'vest'.",
]
approach = [
    "Blocked by schema in three independent places, so this half of the ticket needs no work:",
    "TWO REAL FINDINGS, both unenforced convention rather than constraint:",
]
verify = ["cargo test -p website-frontend cargo_over_capacity_is_a_fault_a_verdict_can_refuse_on."]
acceptance = ["Over-capacity became a RowError; export refused with no bytes written."]
citations = [
    "TBD_LoadoutEquipHelper.c:1108-1145",
    "TBD_RegistryScan.c:896-909",
    "mission.schema.json cargoContainer enum",
    "golden-missions-invalid cargo-container-typo",
    "faction-library.schema.json:108-119",
    "e071d43c7 cargo fault tests",
]
shipped_at = "33a7aa85"
created_at = "2026-07-26T13:11:33Z"
completed_at = "2026-07-26T18:27:15Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 8 commit subject(s)"
owns = ["apps/website/frontend/src/arsenal_rules.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
