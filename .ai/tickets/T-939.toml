id = "T-939"
kind = "program"
title = "Editor usability: selection, gizmo, arrange, templates"
summary = "Fixes the audit's S4 findings in the mission editor UI: outliner multi-select drag, batch faction and squad reassign, a Z gizmo, Arrange shortcuts, squad templates, canvas diagnostics with connection wires, vehicle-panel virtualization and Ctrl+F search."
class = "feature"
status = "queued"
order = 7859
priority = 1
spec = "docs/specs/audit_2026_09/t939_editor_usability.md"
plan = "docs/plans/t-939_plan.md"
executor = "claude-code"
children = [
    "T-939.1",
    "T-939.2",
    "T-939.3",
    "T-939.4",
    "T-939.5",
    "T-939.6",
    "T-939.7",
    "T-939.8",
]
main_goal = "Common editing tasks take one gesture, one dialog or one chord, and the canvas shows problems and links where the entities are."
context = [
    "Master audit S4 (2026-09-04), verified against main @ 072988d57: attributes_modal.rs:1864-1872, outliner_tree.rs:1110, overlays.rs:172-210 and :654-679, top_strip.rs:249-252, dock_left.rs:134-144, asset_catalog.rs:321/:460, outliner.rs:39, vehicles_panel.rs:231-276.",
    "Frontend dir apps/website/frontend/src/editor/; the editor keymap is the wasm keydown closure in mission_editor.rs (allowlisted SIZE-3 file).",
    "Existing tickets referenced, not re-minted: T-704 command palette, T-142/T-157/T-158 shell, T-129 floor selector, T-131 route planner, T-141 slot naming, T-080 connection UI, T-070 vehicles placeable, T-309 faction squads.",
]
requirement = [
    "Every slice verifies its defect on main with a failing test before coding and pastes the red verbatim.",
    "No file-length allowlist grows; new logic goes into the new files each slice names.",
    "Every multi-entity edit applies as one doc-store transaction so one Ctrl+Z reverts it.",
]
verify = [
    "cargo xtask mk ci-local-leptos",
    "cargo xtask mk leptos-gates",
    "cargo xtask platform wave gate --slice T-939.N   # per child",
]
acceptance = [
    "A multi-selection drags, reassigns, aligns and undoes as one unit; height is draggable on the canvas.",
    "Squad templates place whole squads; validation badges and connection wires appear on the canvas; Ctrl+F reaches search.",
]
created_at = "2026-09-05T02:33:18Z"
