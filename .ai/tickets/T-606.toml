id = "T-606"
kind = "work"
title = "Stale greps misreport healthy server"
summary = "Three ops greps report a healthy server as broken, and the T-068.14 spec names a log tag that does not exist"
class = "bug"
status = "shipped"
order = 3532
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND while writing `docs/platform/PLAYTEST_RUNBOOK.md` (2026-07-31). Individually trivial, together
they guarantee a confusing playtest: the operator greps for what the docs say, gets nothing, and
concludes the feature is broken when it worked.

1. **The loadout tag is wrong in the spec.** `TBD_SpawnManager.c:1179` prints `[TBD][Loadout][Slot]`.
   The T-068.14 spec AND `TBD_LoadoutEquipComponent.c:17` both say `[Player]` -- a string that appears
   in **no `Print` anywhere in the codebase**. Following the spec returns zero lines and reads exactly
   like "the loadout never applied", which is the single most important thing T-068.14 must confirm.

2. **`scripts/mod/remote-log-grep.sh:48`** requires `Mission loaded`. That string now survives only
   inside `TBD_FrameworkManager.c:488`'s **error** string -- so the health check passes only when
   something failed.

3. **`docs/mod/STAGING-SERVER.md:192-203`** requires `built slot spawn`, which has been **deleted**.

`docs/platform/PLAYTEST_RUNBOOK.md` §5 already carries corrected greps for all three; this ticket is
to fix them at the source so the runbook is not the only correct copy.

ALSO, same family, documented in the runbook §6 rather than filed separately:
- **Radio automatic tuning cannot work** -- `worlds/TBD_Dev_POC.ent` is a 62-byte bare SubScene with
  no `RadioManagerEntity`. Net assignment and display work; tuning does not. Known limitation.
- **`modded class SCR_PlayerController` runtime coexistence has never been observed at any N.** Six
  blocks now exist and no gate can see it -- `world-boot.sh` boots with zero players. If one screen
  works and another silently does nothing during the playtest, this is the first suspect.

missing=0
"""
priority = 2
main_goal = "Fix loadout tag, remote-log-grep, STAGING-SERVER stale strings."
context = [
    "PLAYTEST_RUNBOOK §5 has corrected greps; sources still wrong.",
    "Operator greps per spec; zero lines reads broken feature.",
    "T-068.14 spec names nonexistent Player log tag.",
]
requirement = [
    "Align loadout Print tag with TBD_SpawnManager actual output.",
    "Fix remote-log-grep Mission loaded inverted health check.",
    "Remove deleted built slot spawn from STAGING-SERVER.",
]
current_state = [
    "Spec says Player tag; codebase prints TBD Loadout Slot.",
    "Mission loaded only in TBD_FrameworkManager error string.",
    "Radio tuning blocked: TBD_Dev_POC missing RadioManagerEntity.",
]
approach = [
    "Fix three greps at source not runbook-only copy.",
    "Document radio and PlayerController coexistence limitations.",
]
verify = [
    "remote-log-grep exits zero on healthy server log.",
    "STAGING-SERVER grep strings match current build output.",
]
acceptance = [
    "remote-log-grep.sh matched stable tag prefixes instead of deleted sentences and could exit zero on a healthy boot log.",
    "STAGING-SERVER.md and run-playtest-server.sh greps were reconciled to current [TBD][Loadout][Slot] and LOBBY stage output.",
    "remote-log-grep gained --file and --selftest modes that proved the verdict could fail on inverted Mission loaded checks.",
]
citations = [
    "TBD_SpawnManager Loadout Slot tag",
    "remote-log-grep inverted check",
    "T-068.14 spec wrong tag",
]
shipped_at = "ea977bbe"
created_at = "2026-07-31T20:39:15Z"
completed_at = "2026-07-31T20:53:05Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "scripts/mod/remote-log-grep.sh",
    "docs/mod/STAGING-SERVER.md",
    "scripts/mod/run-playtest-server.sh",
]

[scope]
domain = "repo"
layer = "docs"
