id = "T-493"
kind = "work"
title = "T-397 Class-R blind to sum(COALESCE(deaths,0)) on leaderboard_totals"
summary = "T-397 Class-R blind to sum(COALESCE(deaths,0)) on leaderboard_totals"
class = "chore"
status = "shipped"
order = 3332
executor = "claude-code"
priority = 2
main_goal = "Close Class-R blind spot for sum(COALESCE(deaths,0)) on leaderboard_totals."
context = [
    "Wave 37 adversarial NIT.",
    "insert_without_counters_stores_null_not_zero holds.",
]
requirement = [
    "leaderboard_mv_does_not_invent_deaths_from_null RED when INSERT bound 0.",
    "Poisoning MV to sum(COALESCE(deaths,0)) with mixed fixture stayed GREEN.",
    "Cure: pin all-NULL → kd_ratio IS NULL; ratchet migration 0014 / FILTER deaths IS NOT NULL.",
]
current_state = ["Mixed NULL+measured fixture: Class-R GREEN (0+3=3) despite COALESCE poisoning."]
approach = [
    "Pin all-NULL row set → kd_ratio IS NULL.",
    "Source-ratchet migration 0014 / live MV FILTER (WHERE deaths IS NOT NULL).",
    "Remove COALESCE(deaths,0) from aggregate.",
]
verify = ["Poison MV sum(COALESCE(deaths,0)) → Class-R stayed GREEN (0+3=3)."]
citations = [
    "Wave 37 adversarial NIT",
    "leaderboard_mv COALESCE blind spot",
    "migration 0014 FILTER deaths",
]
shipped_at = "9bc44470"
created_at = "2026-07-27T07:18:21Z"
completed_at = "2026-08-01T00:05:32Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/api/tests/telemetry.rs",
    "apps/website/api/migrations/0014_nullable_match_player_stat_counters.sql",
    "apps/website/api/tests/identity_link.rs",
    "apps/website/api/src/handlers/telemetry.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
