id = "T-639"
kind = "work"
title = "Zoom-adaptive contour interval — hold spacing constant in screen space"
summary = "Zoom-adaptive contour interval — hold spacing constant in screen space"
class = "bug"
status = "shipped"
order = 3830
executor = "claude-code"
notes = """
editor-UI program wave 1 (wave_plan.tsv row 101) — SHIPPED wave 101 (editor factory), merge 1e1da547; verifier wave101.md

--- HEAD migration_legacy (verbatim) ---

Operator-requested; the highest visible payoff in the map-legibility group. Eden doubles the GROUND contour interval as you zoom out so on-screen spacing stays inside a 14-19 px band — measured off the status bar's printed m/pix across the 75-screenshot corpus: ~5 m at ~1.03-1.30 m/pix, ~10 m at ~3.41, ~20 m at ~6.20, every one landing 14-19 px. Ours is a fixed zoom ladder: `contour_interval_for_zoom(deck_zoom)` at `crates/map-engine-core/src/world/lod_gates.rs:82`, with exactly one caller — `apps/website/frontend/src/world_assets/dem_vectors.rs:110`, feeding `contour_grid_reductions` :115 -> `contour_levels` :118 -> `contour_segments` :119. Pinning to a screen-space band needs metres-per-pixel rather than a zoom rung, so the call signature changes at the caller. `crates/map-engine-core/src/world/mod.rs:71` re-exports the symbol — keep the name and it stays read-only. This EXTENDS rather than replaces the ladder in t090_render_lod_contract.md §N3 (20 m @ 0..+1, 10 m @ +1..+3). Acceptance: measure rendered spacing at >=4 zoom levels and assert every one lands in the band.

missing=0"""
depends_on = ["T-661"]
main_goal = "Pin contour spacing to Eden 14-19px screen-space band not zoom ladder."
context = [
    "Eden doubles ground interval zooming out; 14-19px across 75-screenshot corpus.",
    "Fixed ladder contour_interval_for_zoom at lod_gates.rs:82; one caller dem_vectors.rs:110.",
]
requirement = [
    "Call signature uses metres-per-pixel at dem_vectors.rs caller not deck zoom.",
    "Keep symbol name via mod.rs:71 re-export; extends t090_render_lod_contract §N3.",
]
current_state = [
    "dem_vectors.rs :110 feeds contour_grid_reductions contour_levels contour_segments chain.",
    "Zoom-rung interval not screen-space metres-per-pixel band.",
]
approach = ["Replace zoom ladder with m/pix targeting 14-19px rendered contour spacing."]
verify = ["Measure rendered spacing at >=4 zoom levels; assert each lands in band."]
citations = [
    "t090_render_lod_contract §N3 ladder",
    "Eden 14-19px spacing band",
]
shipped_at = "1e1da547"
created_at = "2026-08-02T00:04:39Z"
completed_at = "2026-08-02T00:05:29Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "crates/map-engine-core/src/world/lod_gates.rs",
    "apps/website/frontend/src/world_assets/dem_vectors.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
