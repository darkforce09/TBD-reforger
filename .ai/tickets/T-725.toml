id = "T-725"
kind = "work"
title = "Zoom labels lag; northings under bar; hide Failed scale fallback"
summary = "Furniture polish: zoom channel lags 1s on still-pointer wheel; northings hide under the bar; Failed-state fallback"
class = "feature"
status = "deferred"
order = 4318
executor = "claude-code"
notes = "filed by wave-106 adversarial verifier (editor factory)"
main_goal = "Fix zoom label lag, northings under bar, hide Failed scale fallback."
context = [
    "T-667 residue bundled as one furniture polish ticket.",
    "Wave-106 MINOR-1 plus NOTEs diagnosis source.",
]
requirement = [
    "Wire real zoom signal same channel CUR uses on pointer-move.",
    "Clamp northings to map-pane bottom minus status bar height.",
    "Hide ScaleBar cosmetic 500 m fallback when camera_snapshot None Failed boot.",
]
current_state = [
    "Still-pointer wheel updates scale bar labels on ~1 Hz debug-HUD only.",
    "Northings enumerate past viewport; last chip hides under status bar.",
    "Grid refs render nothing Failed boot but ScaleBar shows 500 m fallback.",
]
approach = ["Emit zoom from existing wheel handler through CUR pointer-move channel."]
verify = ["Labels track wheel zoom immediately; northings clamped; Failed hides scale."]
citations = [
    "editor_verify/wave106.md MINOR-1",
    "T-667 residue polish bundle",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
