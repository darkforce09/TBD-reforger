id = "T-744"
kind = "work"
title = "Hidden slot closes Attributes modal like undo-away"
summary = "Hiding a slot silently closes its open Attributes modal, indistinguishable from undo-away"
class = "bug"
status = "shipped"
order = 4620
executor = "claude-code"
notes = "filed by wave-113 adversarial verifier (editor factory)"
main_goal = "Keep Attributes modal open when slot hidden via layer or H key."
context = [
    "Wave-113 verifier MINOR; reported by T-082 slice agent not caused by it.",
    "materialize drops T-665 layer-hidden and T-701 editorHidden rows store.rs 596.",
]
requirement = [
    "Attributes modal must not close when slot still exists but hidden.",
    "User must distinguish hide from undo-away; preserve modal tab and draft.",
]
current_state = [
    "read_attrs locates slot through materialize; hidden slot returns None.",
    "Modal None arm calls close_attributes same path as slot undone away.",
    "Repro: open Attributes hide slot H or Outliner layer; modal vanishes.",
]
approach = [
    "read_attrs consult RAW rows for existence; SoA fields still from materialize.",
    "T-082 already parses raw rows for assetId description accessor exists.",
]
verify = [
    "Open Attributes hide slot modal stays open for still-existing entity.",
    "Undo-away path still closes modal; hide and undo remain distinguishable.",
]
citations = [
    "editor_verify wave113 F-2",
    "store.rs materialize 596",
    "T-665 layer-hidden",
    "T-701 editorHidden",
]
shipped_at = "e72047db"
created_at = "2026-08-08T06:15:10Z"
completed_at = "2026-08-08T06:24:12Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/attributes.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "attr_panel",
]
