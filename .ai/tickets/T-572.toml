id = "T-572"
kind = "work"
title = "T-569 Class-R still hollow — #[cfg(any())] match arms"
summary = "T-569 Class-R still hollow — #[cfg(any())] match arms"
class = "feature"
status = "shipped"
order = 3425
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

FOUND by W67 adversarial verifier (DIRTY MAJOR) after T-569.

r#/br#/concat decoys RED. Still GREEN: `#[cfg(any())] match { live arms }` + `#[cfg(not(any()))] match { _ }` or cfg on each role arm with live `_`.

Repro: cfg-out live arms; leave `_ => DEV_USER_ID` live; Class-R green.

missing=0"""
priority = 2
main_goal = "Close Class-R hollow bypass via #[cfg(any())] match arms."
context = [
    "FOUND by W67 adversarial verifier (DIRTY MAJOR) after T-569.",
    "r#/br#/concat decoys RED.",
]
requirement = [
    "Class-R must RED cfg-out live match arms with live `_ => DEV_USER_ID`.",
    "Covers #[cfg(any())] match plus #[cfg(not(any()))] match split.",
]
current_state = [
    "Live arms cfg-out; `_ => DEV_USER_ID` live; Class-R stays GREEN.",
    "cfg on each role arm with live `_` also bypasses pin.",
]
approach = [
    "Extend Class-R to require live role arms not cfg-gated out.",
    "Reject split cfg(any()) / cfg(not(any())) match decoy pairs.",
]
verify = ["Repro: cfg-out live arms; leave `_ => DEV_USER_ID` live; Class-R green."]
acceptance = [
    "Role-arm pin required runtime dev_login identity per role, rejecting cfg(any()) gated live arms with underscore fallback.",
    "cfg(not(any())) split-match decoy with live underscore DEV_USER_ID arm went RED on runtime role proof.",
]
citations = [
    "W67 adversarial verifier DIRTY MAJOR",
    "T-569 cfg(any()) match arm decoy",
    "DEV_USER_ID live collapse bypass",
]
shipped_at = "734b175c"
created_at = "2026-07-28T22:46:08Z"
completed_at = "2026-07-31T23:41:18Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/api/tests/misc_integration.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
