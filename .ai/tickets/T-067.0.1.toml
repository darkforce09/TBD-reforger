id = "T-067.0.1"
kind = "work"
title = "Revert CPU viewport cull"
summary = "T-067.0.1"
class = "feature"
status = "shipped"
order = 670
parent = "T-067"
main_goal = "Restore pan-stable getBaseIcons; revert CPU chunk cull."
context = ["T-067.0 CPU cull rebuilt ~360k IconLayer each chunk boundary crossing."]
requirement = ["Render path uses getBaseIcons only; no getBaseIconsForBbox wiring."]
current_state = ["CPU cull dropped pan 165 idle to 21fps @ 367k zoom -2."]
approach = ["Revert useIconLayer TacticalMap cull; keep dormant 512m chunk buckets."]
verify = ["Pan ~160fps @ 367k zoom -2 user verified per spec."]
acceptance = [
    "CPU viewport cull reverted; render path uses getBaseIcons only.",
    "Pan restored ~160fps at 367k zoom -2.",
]
citations = [
    "t067_spatial_chunks.md §0.1",
    "d2128cf3 ship commit",
    "SHIPPED_HISTORY §T-067",
]
created_at = "2026-06-23T00:00:00Z"
completed_at = "2026-06-26T00:00:00Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-067 (day precision); shipped_at left absent — no subject commits, a SHA is never invented"

[scope]
domain = "repo"
layer = "docs"
