id = "T-250"
kind = "work"
title = "Warn on unconsumed mission-document keys"
summary = "TBD_MissionValidator has no unconsumed-key warning, so nothing tells an author that environment, settings, entities, layers, tickets or roles[].radio were silently discarded on load."
status = "shipped"
order = 2680
executor = "claude-code"
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/Backend/TBD_MissionValidator.c",
    "xtask/src/schema_gates.rs",
]

[scope.mod]
layers = ["feature"]
