id = "T-918.1"
kind = "work"
title = "Breadcrumb scope + facet filters + class chips"
summary = "Breadcrumb scope + facet filters + class chips"
class = "bug"
status = "shipped"
order = 5981
spec = "docs/platform/t917_ticket_schema_v2.md"
executor = "claude-code"
notes = "Reads the vocab file for facet values — display only, never validates (check is the authority). Depends on the T-917.2 cutover types."
depends_on = ["T-917.2"]
parent = "T-918"
main_goal = "The operator reads website > frontend > mission_creator > attr_panel at a glance and slices the board by any level of it."
context = [
    "Cards and detail render scope as domain > layer > component > surface breadcrumb.",
    "Reads vocab file for facet values — display only, never validates.",
]
requirement = [
    "Filters become per-facet: one dropdown per level from vocab file.",
    "Composable with existing text/executor/kind filters.",
    "Class renders colored chip bug/feature/chore/audit/docs on cards.",
]
current_state = ["Depends on T-917.2 cutover types; check is the authority not board."]
approach = [
    "One dropdown per scope facet level; values from scope-vocab file.",
    "Class chip on every work card; class facet filter composes with others.",
]
verify = [
    "Breadcrumb on card and detail equals four scope fields joined.",
    "git status --porcelain unchanged after full session.",
]
acceptance = [
    "Breadcrumb on card and detail equals the four scope fields joined; empty surface renders visibly as such",
    "Facet filters compose with text/executor/kind filters; clearing restores the full measured count",
    "Class chip on every work card; class facet filter works",
    "git status --porcelain unchanged after a full session",
]
citations = [
    "t917_ticket_schema_v2.md",
    "scope-vocab.toml",
    "T-917.2 cutover types",
]
shipped_at = "ec17a8d1"
created_at = "2026-08-14T21:54:38Z"
completed_at = "2026-08-14T23:31:09Z"
estimated = ["tokens"]
owns = ["apps/ticketboard"]

[scope]
domain = "repo"
layer = "tickets"
