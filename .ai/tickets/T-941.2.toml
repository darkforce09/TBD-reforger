id = "T-941.2"
kind = "work"
title = "Lobby: deploy holders on BRIEFING, reopen path"
summary = "Players stay bodiless during LOBBY; on LOBBY to BRIEFING every claimed slot holder auto-deploys exactly once, the deploy race is closed, and the lobby reopens to change slot."
class = "bug"
status = "ready"
order = 7590
spec = "docs/specs/audit_2026_09/t941_mod_lifecycle.md"
plan = "docs/plans/t-941_2_plan.md"
executor = "claude-code"
priority = 0
parent = "T-941"
main_goal = "Deploy happens exactly once per claimed slot, and slots stay changeable."
context = [
    "Anchor 2026-09-04: UI/Lobby/TBD_LobbyScreen.c:211-212 acknowledges the deploy race; DEPLOY gating at :492-507 (ShouldStandDown + IsDeployPending) closes the screen instead of locking.",
    "Gamemode/TBD_SpawnManager.c m_bAutoDeploy fires ~250 ms into LOBBY. It is also owned by queued T-675.2 and by T-941.8; this slice packs apart from both.",
    "Operator authorization: agents may edit the Enfusion mod scripts; gate = cargo xtask mod compile; in-game behaviour goes on a human checklist.",
]
requirement = [
    "TBD_SpawnManager.c: auto-deploy moves from LOBBY entry to the LOBBY to BRIEFING transition and deploys each claimed slot holder once, tracked per player id.",
    "TBD_LobbyScreen.c: DEPLOY is disabled with a reason while a deploy is pending; the screen reopens from the pause menu to change slot, which despawns and redeploys.",
    "The :211-212 race note becomes a dated decision naming the guard.",
]
current_state = ["Auto-deploy during LOBBY; race acknowledged; the screen closes rather than locks."]
approach = [
    "Verify on main: read :211-212 and the m_bAutoDeploy timing; record both as defect evidence (no script unit test lane).",
    "Move the deploy trigger; add the per-player deployed set; disable the button; add the reopen action; compile; boot.",
    "Perturbation: declare the deployed set with a mismatched type; mod compile must go red; restore, touch, green.",
]
verify = [
    "cargo xtask mod compile",
    "cargo xtask mod world-boot",
    "cargo xtask platform wave gate --slice T-941.2",
]
acceptance = [
    "The mod compiles and boots; no body spawns during LOBBY.",
    "Checklist: on BRIEFING every claimed slot holder appears once; double-clicking DEPLOY never yields two bodies; reopening the lobby and changing slot redeploys.",
]
created_at = "2026-09-05T02:33:18Z"
owns = [
    "apps/mod/tbd-framework/Scripts/Game/TBD/UI/Lobby/TBD_LobbyScreen.c",
    "apps/mod/tbd-framework/Scripts/Game/TBD/Gamemode/TBD_SpawnManager.c",
]

[scope]
domain = "mod"
layer = "scripts"
component = "gamemode"
