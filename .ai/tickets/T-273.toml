id = "T-273"
kind = "work"
title = "Regenerate ticket schema from live registry"
summary = "schema.json predated slice_plan.status, notes and shipped_at; additionalProperties:false rejected most current tickets."
class = "feature"
status = "shipped"
order = 2910
executor = "claude-code"
context = [
    "Last touched 2026-06-26 with stale enums and missing slice_plan fields.",
    "additionalProperties:false would reject nearly every live registry row.",
]
requirement = [
    "Rewrite schema.json from the surveyed live registry shape.",
    "jsonschema must validate every ticket in the registry.",
]
current_state = [
    "schema.json carried stale enums and omitted slice_plan.status notes and shipped_at.",
    "additionalProperties:false rejected nearly every live registry row before rewrite.",
]
approach = [
    "Rewrote .ai/tickets/schema.json from surveyed live registry shape.",
    "Validated every ticket row with jsonschema after the rewrite.",
]
verify = ["jsonschema validates all 423 tickets."]
acceptance = ["Ticket schema accepted slice_plan.status notes shipped_at; all registry rows validated."]
citations = [
    "commit d3ec3f9e schema rewrite",
    "wave 15 merge 9bc47d14",
    "T-237 blocked on schema",
]
shipped_at = "9bc47d14"
created_at = "2026-07-26T23:55:24Z"
completed_at = "2026-07-26T23:59:18Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [".ai/tickets/schema.json"]

[scope]
domain = "repo"
layer = "xtask"
