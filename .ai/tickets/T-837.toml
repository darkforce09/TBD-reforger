id = "T-837"
kind = "work"
title = "Vehicles cannot be deleted — slots can, vehicles cannot"
summary = "Vehicles cannot be deleted — slots can, vehicles cannot"
class = "bug"
status = "queued"
order = 5190
executor = "claude-code"
notes = "operator eye-pass wave-205; census XFORM-DEL-001; coordinates with T-819 — wave 210/211 packing"
estimated = ["scope"]
migration_legacy = ["OPERATOR EYE-PASS BUG (wave-205, 2026-08-10): a placed vehicle cannot be deleted from the editor — slots delete fine (Del key, context menu); vehicles have no working delete path. This is the census XFORM-DEL-001 lead T-800's seed made browser-testable — and it fails. AUDIT FIRST: does delete_selection/Del cover vehicle ids in ctx.selection (the wave-145 prune lesson says vehicle ids LIVE there)? does the context menu offer Delete on a vehicle? the outliner PLACED VEHICLES row (W203) has no delete affordance? Find which paths exist-but-skip vs are absent. FIX: Del key + context menu + outliner row delete all remove a selected vehicle in ONE undo step (T-779 ack discipline: confirm the write); crew assignments release cleanly (crewed slots reappear — coordinates with T-819's derived hide). ACCEPTANCE: place vehicle, select (map or outliner), Del -> gone from map/outliner/compiled export, one undo restores it WITH its crew assignments. ⟡ ABSORBS T-844 (Delete does nothing to a selected vehicle — delete_selection partitions comments and slots only) — deliver its acceptance too."]
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/context_menu.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "map_canvas",
    "layout_chrome",
]
