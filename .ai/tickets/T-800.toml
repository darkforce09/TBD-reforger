id = "T-800"
kind = "work"
title = "Catalog fail names cause+retry; Add Vehicle explains; seed vehicles"
summary = "Catalog failure states name the cause and offer retry; Add Vehicle explains itself; dev seed gets vehicles"
class = "bug"
status = "shipped"
order = 4820
executor = "claude-code"
notes = "unblocks in-browser closure of the census XFORM-DEL-001 vehicle-delete lead the review could only settle in source | SHIPPED W202: both catalog Failed arms share a named-cause+Retry view (no-modpack vs request-failed, in-dock 404 probe since the fetch discards the cause in sibling-owned code), grammar chips hidden while failed; Add Vehicle empty-catalog explainer inline (no z exposure); 4 vehicle seed rows applied idempotently (second apply INSERT 0 0). Verifier proved the full flip/restore cycle live incl. Retry-without-reload. T-084 grammar needle updated honestly with a compensating hides-while-failed pin."
main_goal = "Main-tree Failed arm names cause with Retry; Add Vehicle explainer on empty catalog; seed dev vehicle rows."
context = [
    "No modpack shows flat Could not load catalog with no Retry.",
    "Dev seed has zero vehicle rows; Add Vehicle is silent no-op.",
    "Favourites tab already ships named-cause Retry pattern in-file.",
]
requirement = [
    "Main-tree Failed arm names no-modpack vs request-failed with Retry.",
    "Hide search-syntax chips while catalog failed.",
    "Add Vehicle opens explainer when vehicle catalog empty.",
    "registry_dev.sql gains 3-5 idempotent vehicle seed rows.",
]
current_state = [
    "Main tree Failed arm at eden_dock_right ~:1451 flat text only.",
    "Registry 404 no current modpack configured swallowed from UI.",
    "Do not touch asset_catalog.rs; error surface in dock file.",
]
approach = [
    "Reuse Favourites Retry arm pattern for main-tree Failed.",
    "orbat_manager small explainer instead of silent Add Vehicle.",
    "ON CONFLICT DO NOTHING vehicle rows NATO US_Army Vehicles.",
]
verify = [
    "Modpack cleared: panel names the cause and Retry present.",
    "After seed re-applied Retry populates tree without reload.",
    "Add Vehicle on empty catalog shows the explainer.",
    "After new seed Add Vehicle inserts vehicle row Vehicles 1.",
]
citations = [
    "editor_hostile_ux_review F-05 F-21",
    "Favourites Retry arm pattern",
    "registry_dev zero vehicle rows",
    "orbat_manager Add Vehicle no-op",
    "T-809 T-810 asset_catalog later",
]
shipped_at = "c4b7e0e6"
created_at = "2026-08-09T20:44:44Z"
completed_at = "2026-08-11T02:43:47Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_dock_right.rs",
    "apps/website/frontend/src/orbat_manager.rs",
    "apps/website/api/seeds/registry_dev.sql",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
