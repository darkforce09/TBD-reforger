id = "T-800"
kind = "work"
title = "Catalog fail names cause+retry; Add Vehicle explains; seed vehicles"
summary = "Catalog failure states name the cause and offer retry; Add Vehicle explains itself; dev seed gets vehicles"
class = "bug"
status = "shipped"
order = 4820
executor = "claude-code"
notes = """
unblocks in-browser closure of the census XFORM-DEL-001 vehicle-delete lead the review could only settle in source | SHIPPED W202: both catalog Failed arms share a named-cause+Retry view (no-modpack vs request-failed, in-dock 404 probe since the fetch discards the cause in sibling-owned code), grammar chips hidden while failed; Add Vehicle empty-catalog explainer inline (no z exposure); 4 vehicle seed rows applied idempotently (second apply INSERT 0 0). Verifier proved the full flip/restore cycle live incl. Retry-without-reload. T-084 grammar needle updated honestly with a compensating hides-while-failed pin.
--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-05/F-21 — VERIFIED: with no current modpack the Factions/Vehicles tabs show one flat line 'Could not load the catalog.' — no cause, no retry, while the search-syntax chips keep rendering above the corpse (registry 404 'no current modpack configured' swallowed). AND the committed dev seed has ZERO vehicle rows (8 characters, 13 gear — GROUP BY confirmed), so Vehicles reads 'No placeable vehicles.' even after a correct seed; ORBAT>Add Vehicle is then a SILENT NO-OP (real click at true coords: Vehicles: 0 unchanged, no picker, no toast — VERIFIED). THE FIX PATTERN ALREADY EXISTS IN-FILE: the Favourites tab ships a Retry arm at eden_dock_right.rs ~:887-928; the main tree's Failed arm (~:1451) renders flat text. FIX: (a) main-tree Failed arm distinguishes 'no modpack configured' vs request-failed, names it, offers Retry (reuse the Favourites pattern), hides search-syntax chips while failed; (b) orbat_manager.rs Add Vehicle with an empty vehicle catalog opens a small explainer ('no placeable vehicles in the active modpack') instead of no-op; (c) apps/website/api/seeds/registry_dev.sql gains 3-5 vehicle rows (category NATO/US_Army/Vehicles, kind vehicle) so dev environments can author vehicles. ⚠️ TRAPS: seed must stay ON CONFLICT DO NOTHING idempotent; do not touch asset_catalog.rs (T-809/T-810 own it later — the error surface lives in the dock file); the search-prefix hint line doubles as filter help when healthy — keep it in the healthy state. ACCEPTANCE: scripted with modpack cleared: panel names the cause + Retry present; after seed re-applied Retry populates the tree without reload; Add Vehicle on empty catalog shows the explainer; after new seed, Add Vehicle inserts a vehicle row (Vehicles: 1) and the census XFORM-DEL-001 lead becomes browser-testable.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-05/F-21 — VERIFIED: with no current modpack the Factions/Vehicles tabs show one flat line 'Could not load the catalog.' — no cause, no retry, while the search-syntax chips keep rendering above the corpse (registry 404 'no current modpack configured' swallowed). AND the committed dev seed has ZERO vehicle rows (8 characters, 13 gear — GROUP BY confirmed), so Vehicles reads 'No placeable vehicles.' even after a correct seed; ORBAT>Add Vehicle is then a SILENT NO-OP (real click at true coords: Vehicles: 0 unchanged, no picker, no toast — VERIFIED). THE FIX PATTERN ALREADY EXISTS IN-FILE: the Favourites tab ships a Retry arm at eden_dock_right.rs ~:887-928; the main tree's Failed arm (~:1451) renders flat text. FIX: (a) main-tree Failed arm distinguishes 'no modpack configured' vs request-failed, names it, offers Retry (reuse the Favourites pattern), hides search-syntax chips while failed; (b) orbat_manager.rs Add Vehicle with an empty vehicle catalog opens a small explainer ('no placeable vehicles in the active modpack') instead of no-op; (c) apps/website/api/seeds/registry_dev.sql gains 3-5 vehicle rows (category NATO/US_Army/Vehicles, kind vehicle) so dev environments can author vehicles. ⚠️ TRAPS: seed must stay ON CONFLICT DO NOTHING idempotent; do not touch asset_catalog.rs (T-809/T-810 own it later — the error surface lives in the dock file); the search-prefix hint line doubles as filter help when healthy — keep it in the healthy state. ACCEPTANCE: scripted with modpack cleared: panel names the cause + Retry present; after seed re-applied Retry populates the tree without reload; Add Vehicle on empty catalog shows the explainer; after new seed, Add Vehicle inserts a vehicle row (Vehicles: 1) and the census XFORM-DEL-001 lead becomes browser-testable."]

missing=0
"""
main_goal = "Main-tree Failed arm names cause with Retry; Add Vehicle explainer on empty catalog; seed dev vehicle rows."
context = [
    "No modpack shows flat Could not load catalog with no Retry.",
    "Dev seed has zero vehicle rows; Add Vehicle is silent no-op.",
    "Favourites tab already ships named-cause Retry pattern in-file.",
]
requirement = [
    "Main-tree Failed arm names no-modpack vs request-failed with Retry.",
    "Hide search-syntax chips while catalog failed.",
    "Add Vehicle opens explainer when vehicle catalog empty.",
    "registry_dev.sql gains 3-5 idempotent vehicle seed rows.",
]
current_state = [
    "Main tree Failed arm at eden_dock_right ~:1451 flat text only.",
    "Registry 404 no current modpack configured swallowed from UI.",
    "Do not touch asset_catalog.rs; error surface in dock file.",
]
approach = [
    "Reuse Favourites Retry arm pattern for main-tree Failed.",
    "orbat_manager small explainer instead of silent Add Vehicle.",
    "ON CONFLICT DO NOTHING vehicle rows NATO US_Army Vehicles.",
]
verify = [
    "Modpack cleared: panel names the cause and Retry present.",
    "After seed re-applied Retry populates tree without reload.",
    "Add Vehicle on empty catalog shows the explainer.",
    "After new seed Add Vehicle inserts vehicle row Vehicles 1.",
]
acceptance = [
    "Catalog Failed arms named no-modpack versus request-failed cause with Retry; grammar chips hidden while failed.",
    "Add Vehicle on empty catalog showed inline explainer; four idempotent vehicle rows seeded into registry_dev.",
]
citations = [
    "editor_hostile_ux_review F-05 F-21",
    "Favourites Retry arm pattern",
    "registry_dev zero vehicle rows",
    "orbat_manager Add Vehicle no-op",
    "T-809 T-810 asset_catalog later",
    "4ce95f907 catalog retry seed",
]
shipped_at = "c4b7e0e6"
created_at = "2026-08-09T20:44:44Z"
completed_at = "2026-08-11T02:43:47Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_dock_right.rs",
    "apps/website/frontend/src/orbat_manager.rs",
    "apps/website/api/seeds/registry_dev.sql",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
