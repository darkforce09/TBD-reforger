id = "T-854"
kind = "work"
title = "T-853 port: verify-t437-destroy-inert-diagnostics -> verify t437"
summary = "T-853 port: verify-t437-destroy-inert-diagnostics -> verify t437"
class = "chore"
status = "shipped"
order = 5350
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

T-853 shell->xtask slice. Port scripts/mod/verify-t437-destroy-inert-diagnostics.sh (433 lines) to `cargo xtask verify t437` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). T-437 destroy-inert registry diagnostics; wave.sh x2; 7 python3 call sites. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/verify-t437-destroy-inert-diagnostics.sh (433 lines) to `cargo xtask verify t437` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). T-437 destroy-inert registry diagnostics; wave.sh x2; 7 python3 call sites. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]

missing=0"""
main_goal = "Port verify-t437-destroy-inert-diagnostics.sh to cargo xtask verify t437."
context = [
    "T-853 shell->xtask slice; 433-line bash script.",
    "T-437 destroy-inert registry diagnostics; wave.sh x2; seven python3 call sites.",
    "Callers include Makefile platform wave.sh mod wave.sh workflows.",
]
requirement = [
    "Delete script shell-inventory.txt line python-inventory.txt line.",
    "Repoint every caller Makefile wave.sh workflows.",
    "Build on crates/tbd-gate; do not hand-roll verdicts or process handling.",
]
current_state = [
    "scripts/mod/verify-t437-destroy-inert-diagnostics.sh bash gate live.",
    "Makefile wave.sh workflows still invoke bash script.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and two deliberately broken trees.",
    "T-556 anti-vacuity: passing run alone is not evidence.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Port diff matches clean tree byte-for-byte.",
    "Port diff matches at least two deliberately broken trees.",
]
acceptance = [
    "Deleted verify-t437-destroy-inert-diagnostics.sh; verify t437 xtask gate replaced the bash script.",
    "Shell and python inventory lines removed; wave.sh caller repointed.",
    "Port matched bash stdout stderr and rc on clean tree and deliberately broken trees.",
]
citations = [
    "T-853 shell->xtask program",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
    "9ddb591e verify t437 port",
]
shipped_at = "9ddb591e"
created_at = "2026-08-12T02:25:36Z"
completed_at = "2026-08-12T02:55:19Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "scripts/mod/verify-t437-destroy-inert-diagnostics.sh",
    "xtask/src/gate_verify_t437_destroy_inert_diagnostics.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
