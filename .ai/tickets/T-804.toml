id = "T-804"
kind = "work"
title = "Draft-saved chip makes local persistence legible"
summary = "The author can see their work is safe: draft persistence surfaced"
class = "bug"
status = "shipped"
order = 4860
executor = "claude-code"
notes = "review F-24 + Eden preferences frame 163916; complements F-04 fix (T-789) as the save-story pair | SHIPPED W206: draft-saved chip with real ack discipline (error branch cannot stamp — single call site, source-verified), 1s coarse ticker, inline mount (rect smoke green with the chip up), two-layer tooltip copy, bridge accessor. beforeunload copy consciously left (already honestly documented as browser-ignored). Boot-flush chip nuance -> T-840."
main_goal = "Surface draft-saved recency chip beside dirty dot."
context = [
    "F-24 Eden autosave preference; TBD only invisible IndexedDB draft.",
    "Draft restores faithfully; legibility absent beyond dirty dot.",
    "__missionPersist bridge exists; surface last-flush via signal.",
]
requirement = [
    "Quiet draft saved Ns ago chip fed by yrs_persist flush timestamp.",
    "Chip tooltip explains local draft vs Save Version two-layer model.",
    "Expose timestamp only in yrs_persist; debounce semantics unchanged.",
]
current_state = [
    "eden_top_strip regions 724-726 and 1122-1130 are chip home.",
    "beforeunload copy browser ignores; chip must stay accurate.",
    "T-805 wave holds yrs_persist read-only elsewhere.",
]
approach = [
    "Additive one signal for last-flush time not polling.",
    "1s coarse ticker; inline mount beside dirty dot.",
]
verify = [
    "Edit wait <=6s: chip shows draft saved recency <=6s.",
    "Idle 30s recency counts up; reload restores; flush bridge works.",
    "No chip on mission never edited.",
]
citations = [
    "editor_hostile_ux_review F-24",
    "Eden frame 163916 autosave",
    "__missionPersist flush bridge",
    "T-789 save-story pair",
]
shipped_at = "aab595fd"
created_at = "2026-08-10T19:19:58Z"
completed_at = "2026-08-10T19:35:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/yrs_persist.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "top_strip",
    "doc_store",
]
