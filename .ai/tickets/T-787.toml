id = "T-787"
kind = "work"
title = "Docks stop at the status bar instead of overlapping it"
summary = "Docks stop at the status bar instead of overlapping it"
class = "bug"
status = "shipped"
order = 4690
executor = "claude-code"
notes = """
review section 8 O-1; the 1032px dock height sat unflagged in the responsive sweep — acceptance intentionally re-runs that sweep | SHIPPED W200 CLEAN: every acceptance clause re-measured (dock.bottom == bar.y exactly at 3 viewports, hit-tests, scroll, chevrons); 36px STATUSBAR_H_PX inset vindicated over 96px by measurement. Its two probe missions in the dev DB -> T-815. Verifier: wave200.md.

--- HEAD migration_legacy (verbatim) ---

UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-1 — VERIFIED WITH NUMBERS: both docks render y48->y1080 while the status bar starts at y1044 — 36px overlap at every viewport swept (2560x1440..1280x720), and elementFromPoint at the bar's left end (120,1055) and right end (1800,1055) returns DOCK elements — the transparent dock containers eat clicks aimed at the readouts and right-end controls. MECHANISM: eden_layout.rs ~:64-82 dock height consts never subtract the toolbelt band (eden_toolbelt.rs ~:459 StatusBar). FIX: dock containers end at the bar's top edge — subtract TOOLBELT_BAND_PX (or equivalent) in eden_layout.rs so dock.bottom <= bar.y; docks read the consts via accessors so no dock-file edit should be needed. ⚠️ TRAPS: keep the collapse chevrons and dock scroll behaviour intact; the validation panel and mode pill live in the band area and are NOT this ticket (T-798 owns their future); do not touch eden_dock_left.rs / eden_dock_right.rs (other tickets own them) — if an edit there seems required, the layout-const route is the intended one. ACCEPTANCE: scripted at 1920x1080 + 1366x768 + 2560x1440: dockRect.bottom <= barRect.y for both docks; elementFromPoint((120, barY+10)) and ((width-120, barY+10)) resolve inside the status bar subtree; dock inner scroll still reaches its last row.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). O-1 — VERIFIED WITH NUMBERS: both docks render y48->y1080 while the status bar starts at y1044 — 36px overlap at every viewport swept (2560x1440..1280x720), and elementFromPoint at the bar's left end (120,1055) and right end (1800,1055) returns DOCK elements — the transparent dock containers eat clicks aimed at the readouts and right-end controls. MECHANISM: eden_layout.rs ~:64-82 dock height consts never subtract the toolbelt band (eden_toolbelt.rs ~:459 StatusBar). FIX: dock containers end at the bar's top edge — subtract TOOLBELT_BAND_PX (or equivalent) in eden_layout.rs so dock.bottom <= bar.y; docks read the consts via accessors so no dock-file edit should be needed. ⚠️ TRAPS: keep the collapse chevrons and dock scroll behaviour intact; the validation panel and mode pill live in the band area and are NOT this ticket (T-798 owns their future); do not touch eden_dock_left.rs / eden_dock_right.rs (other tickets own them) — if an edit there seems required, the layout-const route is the intended one. ACCEPTANCE: scripted at 1920x1080 + 1366x768 + 2560x1440: dockRect.bottom <= barRect.y for both docks; elementFromPoint((120, barY+10)) and ((width-120, barY+10)) resolve inside the status bar subtree; dock inner scroll still reaches its last row."]

missing=0
"""
main_goal = "Subtract toolbelt band in eden_layout so dock.bottom <= status bar y."
context = [
    "O-1 verified: both docks y48 to y1080; status bar starts y1044.",
    "36px overlap at every viewport; elementFromPoint returns dock at bar ends.",
    "Transparent dock containers eat clicks on readouts and right controls.",
]
requirement = [
    "Subtract TOOLBELT_BAND_PX in eden_layout.rs so dock.bottom <= bar.y.",
    "Docks read consts via accessors; no eden_dock_left or right edits.",
    "Keep collapse chevrons and dock inner scroll behaviour intact.",
]
current_state = [
    "eden_layout.rs 64-82 dock height consts never subtract toolbelt band.",
    "Validation panel and mode pill in band area are T-798 not this ticket.",
    "Layout-const route intended if dock-file edit seems required.",
]
approach = [
    "Edit eden_layout.rs height consts; eden_toolbelt StatusBar defines band.",
    "Accessor path so dock files need no direct height edits.",
]
verify = [
    "Three viewports: dockRect.bottom <= barRect.y for both docks.",
    "elementFromPoint at bar ends resolves inside status bar subtree.",
    "Dock inner scroll still reaches its last row.",
]
acceptance = [
    "Dock bottom inset used STATUSBAR_H_PX 36 so dock.bottom equaled status bar top, not TOOLBELT_BAND_PX 96.",
    "elementFromPoint at bar ends resolved status bar not dock eating readout clicks.",
]
citations = [
    "editor_hostile_ux_review O-1",
    "eden_layout.rs 64-82",
    "eden_toolbelt StatusBar 459",
    "T-798 validation panel",
    "42203df1 STATUSBAR_H inset",
]
shipped_at = "42203df1"
created_at = "2026-08-09T04:57:01Z"
completed_at = "2026-08-09T05:07:31Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_layout.rs",
    "apps/website/frontend/src/eden_toolbelt.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "layout_chrome",
    "toolbelt",
]
