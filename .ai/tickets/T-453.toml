id = "T-453"
kind = "work"
title = "CI rust-backend fmt still misses workspace cargo fmt --all"
summary = "CI rust-backend fmt still misses workspace cargo fmt --all"
class = "chore"
status = "shipped"
order = 3292
executor = "claude-code"
priority = 2
main_goal = "Align CI rust-backend fmt with workspace cargo fmt --all --check."
context = [
    "Residual from T-297 / wave 18 adversarial MINOR.",
    "Local make rust-fmt runs cargo fmt --all --check.",
]
requirement = [
    "ci.yml rust-backend uses working-directory apps/website/api plus package-scoped fmt.",
    "wave.sh still comments cargo fmt --all deliberately unused.",
    "Cure: root/workspace fmt step in ci.yml; refresh wave.sh comment.",
]
current_state = [
    "GitHub CI can miss tbd-tools/xtask drift.",
    "Repro: format-break tools/tbd-tools/src/lib.rs; local RED; CI GREEN.",
]
approach = ["Root/workspace fmt step in ci.yml or drop working-directory for fmt."]
citations = [
    "T-297 wave 18 adversarial",
    "ci.yml rust-backend fmt",
    "wave.sh stale comment",
]
shipped_at = "88f60bc6"
created_at = "2026-07-27T00:28:43Z"
completed_at = "2026-07-27T00:46:28Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    ".github/workflows/ci.yml",
    "scripts/platform/wave.sh",
    "Makefile",
]

[scope]
domain = "repo"
layer = "xtask"
