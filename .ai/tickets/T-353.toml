id = "T-353"
kind = "work"
title = "Four SPA gaps outside T-232 owned files"
summary = "Four SPA gaps T-232 could not reach from its own files"
class = "bug"
status = "shipped"
order = 3169
executor = "claude-code"
notes = """
== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now.

--- migration_legacy (verbatim) ---

All found by T-232 while writing the populated render branches; each needs a file it did not own.

1. **`event_hub.rs:182` `fn event_hub_view` is private.** The /events surface spec wants the full inline-ORBAT hub in the detail column. T-232 built a self-contained dossier summary plus a deep link instead. Making it `pub(crate)` gets events.rs to spec parity without duplicating anything.

2. **No `/announcements/:id` route exists** (`router.rs`), so the dashboard intel feed row — spec element 20 — links to `/announcements` rather than deep-linking the post. Needs the route plus URL-driven selection in announcements.rs. Note T-232 deliberately left announcements.rs WITHOUT auto-selection, so a deep link is the intended way in.

3. **`server_intel.rs:30` `format_uptime` is private**, so dashboard.rs now carries a duplicate. Wants a shared home in `datefmt.rs` — and `log_stamp` from audit.rs wants to go with it. Two definitions of a displayed timestamp drifting apart is the same class as the two definitions of 'a deployment' T-326 refused to create.

4. **`POST /me/leave-requests` exists (`api/src/app.rs:100`) with NO CALLER in the SPA** — the deployments spec's LOA button, item 8. Identical shape to the dead PATCH/DELETE T-226 found and wired. T-232 correctly left it: that slice was render branches, not mutations.

== DEFERRED 2026-07-26 BY OPERATOR DECISION, NOT CANCELLED ==
Recording a bug is most of its value; fixing it now is optional. The audits that produced this ticket were generating work faster than the run could close it, and the original T-182..T-297 feature backlog had not moved in hours. The operator's call, verbatim in substance: there will always be bugs, that is what developing is — knowing them is good, but spending the token budget on things that do not need fixing right now means nothing ships.
This is findable, fully diagnosed, and reproducible from the notes above. Promote it to `idea` when it actually blocks something, when it starts costing real time, or after the feature backlog lands. Nothing here was judged wrong — only not now."""
priority = 2
main_goal = "Wire four SPA gaps T-232 could not reach — event_hub visibility, announcements route, shared datefmt, leave-requests POST."
context = ["All found by T-232 while writing populated render branches; each needs a file it did not own."]
requirement = [
    "Make event_hub_view pub(crate) for /events inline-ORBAT hub in detail column.",
    "Add /announcements/:id route plus URL-driven selection in announcements.rs.",
    "Move format_uptime to datefmt.rs; log_stamp from audit.rs follows.",
    "Wire POST /me/leave-requests — deployments spec LOA button, item 8.",
]
current_state = [
    "event_hub.rs:182 fn event_hub_view is private; T-232 built dossier summary + deep link.",
    "No /announcements/:id route; dashboard intel feed links to /announcements only.",
    "server_intel.rs:30 format_uptime private; dashboard.rs carries duplicate.",
    "POST /me/leave-requests exists at app.rs:100 with NO SPA caller.",
]
citations = [
    "T-232 render branches",
    "T-226 PATCH/DELETE pattern",
    "deployments spec item 8",
    "routes.csv manifest",
]
shipped_at = "2902bc43"
created_at = "2026-07-27T13:51:44Z"
completed_at = "2026-07-27T14:02:59Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/frontend/src/event_hub.rs",
    "apps/website/frontend/src/router.rs",
    "apps/website/frontend/src/app_routes.rs",
    "apps/website/frontend/src/datefmt.rs",
    "apps/website/frontend/src/announcements.rs",
    "apps/website/frontend/src/server_intel.rs",
    "apps/website/frontend/src/dashboard.rs",
    "apps/website/frontend/src/events.rs",
    "apps/website/frontend/src/audit.rs",
    "tools/tbd-tools/fixtures/t159/manifests/routes.csv",
]

[scope]
domain = "website"
layer = "frontend"
component = "shell"
surface = ["router"]
