id = "T-577"
kind = "work"
title = "Database backups: none exist, and the spec is written"
summary = "Database backups: none exist, and the spec is written"
class = "chore"
status = "shipped"
order = 3421
executor = "claude-code"
priority = 1
main_goal = "Ship verified backup/restore scripts, Makefile targets, schedule, and restore drill."
context = [
    "T-280 found no pg_dump/pg_restore tooling; only three comments in repo.",
    "Backups live outside app.rs; T-280 already specified this work precisely.",
]
requirement = [
    "New backup-db.sh: podman exec pg_dump -Fc; retention by count; verify dump written.",
    "New restore-db.sh: pg_restore with T-381 allow-list guard from tests/common/mod.rs:88.",
    "Makefile db-backup / db-restore beside db-up/seed.",
    "tbd-website-backup.timer + .service paired with API systemd unit.",
    "Restore drill in CI or wave gate: dump → restore scratch DB → db_migrate.",
]
current_state = ["No pg_dump/pg_restore tooling anywhere; only comment hits for Go→Rust parity."]
approach = [
    "backup-db.sh through compose container; pg_restore --list + non-zero-rows verify.",
    "restore-db.sh with T-381 allow-list; install backup timer like deploy-website.sh:82.",
]
verify = [
    "Restore drill: dump → restore scratch DB → db_migrate against it.",
    "Without drill, backup untested until incident.",
]
citations = [
    "T-280 no pg_dump tooling",
    "T-381 allow-list mod.rs:88",
    "deploy-website.sh systemd unit pattern",
]
shipped_at = "5f23206a"
created_at = "2026-07-31T02:18:11Z"
completed_at = "2026-07-31T02:58:53Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "scripts/deploy/backup-db.sh",
    "Makefile",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
