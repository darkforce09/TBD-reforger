id = "T-915.4"
kind = "work"
title = "Mutation UI over subprocess xtask verbs"
summary = "Mutation UI over subprocess xtask verbs"
class = "feature"
status = "shipped"
order = 5964
spec = "docs/platform/t915_ticketboard_design.md"
executor = "claude-code"
notes = "Depends on T-916.2 for the child-resolving verbs and add-child/remove flags. Any nonzero exit = reload + show stderr verbatim (several refusal paths are process::exit(1) before anyhow). Mid-verb crash recovery is operator-run wave repack, shown as text."
depends_on = [
    "T-915.3",
    "T-916.2",
]
parent = "T-915"
main_goal = "The operator steers the registry from the board — promote, ship, cancel, defer, mint — with every write going through the one audited CLI path and every refusal shown verbatim."
context = [
    "Depends on T-916.2 child-resolving verbs and add-child/remove flags.",
    "Mid-verb crash recovery is operator-run wave repack shown as text.",
    "App must not become a second wave writer; no auto-repack ever.",
]
requirement = [
    "Transition affordances mirroring CLI verbs through subprocess xtask.",
    "idea->queued drag-after-anchor reorder; queued->ready Ready-prose form.",
    "ship/cancel/defer demotions with confirm; add-ticket and add-child [--promote].",
    "running hidden manual target; raw set-status behind advanced affordance.",
]
current_state = ["Several refusal paths are process::exit(1) before anyhow."]
approach = [
    "Single-flight subprocess queue; UI compare-and-swap before dispatch.",
    "Verbatim stderr on any nonzero exit; reload plus show stderr.",
    "Ready-prose form surfaces spec-on-disk user_story acceptance deps-unshipped refusal.",
]
verify = [
    "Every transition maps to one xtask verb; refusals stream verbatim; nonzero reloads stderr.",
    "Ready promotion through Ready-prose form surfaces deps-unshipped refusal.",
    "running not offered manual target; raw set-status behind advanced.",
    "CAS guard: changed file since render refuses dispatch with reload prompt.",
    "Mid-verb SIGKILL: check-red wave-stale error text recovery command; no auto-repack.",
]
acceptance = [
    "Every offered transition maps to one xtask verb; refusals stream verbatim; any nonzero exit reloads and shows stderr",
    "Ready promotion goes through the Ready-prose form and surfaces the deps-unshipped refusal",
    "running is not offered as a manual target; raw set-status is behind an advanced affordance",
    "CAS guard: a card whose file changed since render refuses dispatch with a reload prompt",
    "Mid-verb SIGKILL between save and repack: the app shows check-red with the wave-stale error text and the recovery command; it does not auto-repack",
]
citations = [
    "t915_ticketboard_design.md",
    "T-916.2 child verbs",
    "wave repack recovery",
    "Ready-prose deps-unshipped",
]
shipped_at = "99441394"
created_at = "2026-08-14T08:39:30Z"
completed_at = "2026-08-14T10:27:28Z"
estimated = ["tokens"]
owns = ["apps/ticketboard"]

[scope]
domain = "repo"
layer = "tickets"
