__keys = [
    "id",
    "title",
    "summary",
    "program",
    "surfaces",
    "impact",
    "status",
    "order",
    "stream",
    "targets",
    "executor",
    "priority",
]
__ord = 471
executor = "claude-code"
id = "T-497"
impact = [
    "security",
    "api",
]
order = 3332
priority = 2
program = "platform"
status = "shipped"
stream = "web-platform"
summary = "Wave 38 residual from T-408. update_mission now MissionMakerUser. Still AuthUser+can_edit only: delete_mission, submit_mission, create_version, set_armory in handlers/missions.rs. Demotion still leaves those paths open. Cure: same MissionMakerUser tier (or deliberate ownership-outlives-role Class-R per path)."
surfaces = ["API"]
targets = ["website"]
title = "Mission mutators still AuthUser+can_edit after T-408 PATCH tier fix"
