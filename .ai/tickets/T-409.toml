id = "T-409"
kind = "work"
title = "wave.sh's new non-vacuity guards false-red on legitimate slices"
summary = "wave.sh's new non-vacuity guards false-red on legitimate slices"
class = "audit"
status = "shipped"
order = 3230
executor = "claude-code"
priority = 1
main_goal = "Fix wave.sh non-vacuity guards that false-red legitimate slice shapes."
context = [
    "Found by wave 3 adversarial verifier against merged main.",
    "T-406 closed gate ways of passing code it never examined.",
    "Three new HARD FAILS refuse work that is genuinely fine.",
    "Inverse of signature defect: false red, not false green.",
]
requirement = [
    "Gate refusing legitimate slices blocks every subsequent wave.",
    "Agents learn gate failures are noise — most expensive failure mode.",
]
current_state = [
    "F1: clippy_changed :477 — is_http_url_cases.rs resolves to no crate.",
    "T-405 created that file; three crates include! it. fmt_changed handles fine.",
    "F2: fmt_changed :385 and touch_changed :428 — deletion-only slice hard-fails.",
    "clippy_changed stays green for deletion; documents why at :470-474.",
    "F3: TBD_GATE_ALLOW_UNSERIALISED=1 inert for full gate vs slice gate.",
    "NIT: refuse_empty_range rejects gate --slice with working-tree changes, no commits.",
]
approach = [
    "T-406 report framed as repo-root .rs file; missed T-405 sibling instance.",
    "Asymmetry: fmt handles is_http_url_cases.rs; only clippy reds.",
]
verify = [
    "F1 REPRO: append probe to is_http_url_cases.rs; clippy rc=1.",
    "F2 REPRO: rm aar_replay_url_backfill.rs; fmt=1 touch=1 clippy=0.",
    "F3 REPRO: TBD_GATE_LOCK=/proc/1/nope/x; lock HELD=0 ensure=2.",
]
citations = [
    "T-406 vacuity hardening",
    "T-405 is_http_url_cases.rs",
    "wave.sh clippy_changed",
    "wave.sh fmt_changed",
]
shipped_at = "f5cf4c90"
created_at = "2026-07-26T21:30:13Z"
completed_at = "2026-07-26T21:32:40Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
