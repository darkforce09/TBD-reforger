id = "T-153.1"
kind = "work"
title = "T-153 — Faction Library (reusable factions + palette swap)"
summary = "T-153 — Faction Library (reusable factions + palette swap)"
class = "feature"
status = "shipped"
order = 1510
spec = "docs/specs/Mission_Creator_Architecture/t153_faction_library.md"
executor = "claude-code"
notes = "Frontend: FactionManagerDialog (+ArsenalPicksPanel extraction), buildFactionTree palette (vanilla dump deleted), drop path ensureFaction/ensureSquadFor + role loadout on slot. Vehicles listed, placement = T-070."
parent = "T-153"
main_goal = "FactionManagerDialog + buildFactionTree palette; retire vanilla character dump."
context = ["Backend T-153.0 shipped; MC palette still enumerated registry characters."]
requirement = ["ArsenalPicksPanel; ensureFaction drops; role loadout on slot; vanilla purge test."]
current_state = ["AssetBrowser used buildCatalogTree registry dump before ship."]
approach = ["Library-only palette; side accents; vehicle list T-070 badge only."]
verify = ["cd apps/website/frontend && npm test"]
acceptance = ["Shipped FactionManagerDialog and buildFactionTree palette; deleted vanilla character dump; role drops carried loadout and factionRef via ensureFaction path."]
citations = [
    "t153_verify_log.md",
    "commit c4ccb9c3",
    "t153_faction_library.md",
    "Source: t153 verify log.",
]
shipped_at = "c4ccb9c3"
created_at = "2026-07-13T00:00:00Z"
completed_at = "2026-07-13T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "no subject commits; created_at/completed_at from parent T-153 (day precision)"

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
