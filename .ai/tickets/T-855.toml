id = "T-855"
kind = "work"
title = "T-853 port: remote-log-grep -> mod remote-logs"
summary = "T-853 port: remote-log-grep -> mod remote-logs"
class = "chore"
status = "shipped"
order = 5360
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "6fde71be"
created_at = "2026-08-12T02:48:46Z"
completed_at = "2026-08-12T02:55:19Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/remote-log-grep.sh (418 lines) to `cargo xtask mod remote-logs` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). remote server log search over ssh. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/remote-log-grep.sh",
    "xtask/src/gate_remote_log_grep.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
