id = "T-420"
kind = "work"
title = "Wave gate validates no schema sub-gates"
summary = "The wave gate validates no schema — 11 green steps over a schema change it never examined"
class = "bug"
status = "shipped"
order = 3250
executor = "claude-code"
priority = 0
main_goal = "Add meaningful green schema sub-gates to wave.sh; record exclusions for red ones."
context = [
    "BLOCKER: gate reports success on code it never examined.",
    "Found 2026-07-26 wave 5 when T-244 turned xtask schema map-object-enums RED.",
    "grep -c xtask schema scripts/platform/wave.sh -> 0.",
]
requirement = [
    "Step must cover sub-gates that actually fail — validate alone is vacuous.",
    "On T-244 branch validate PASSED while map-object-enums FAILED.",
    "Determine which sub-gates are meaningful AND currently green.",
]
current_state = [
    "cmd_gate eleven steps: cargo check, wasm32, fmt, clippy, test, trunk, registry.",
    "Wave 4 GATE PASS 11/11 on T-241 mission.schema.json — only agent testimony.",
    "T-244 slice gate passed — diff 0 .rs files, fmt/clippy examined nothing.",
    "height-labels fails PNG decode — DEM is LFS pointer, git-lfs absent.",
    "Adding already-red step would red every future wave — T-409 failure mode.",
]
approach = [
    "Record why each excluded sub-gate is excluded.",
    "NOT THIS TICKET: T-409 covers false-red hard-fails from T-406.",
    "Same file, different defect — sequence them, do not merge.",
]
citations = [
    "T-244 map-object-enums failure",
    "T-241 mission.schema.json",
    "T-409 false-red guards",
    "PLATFORM_FACTORY testimony rule",
]
shipped_at = "77214f04"
created_at = "2026-07-26T19:10:02Z"
completed_at = "2026-07-26T19:40:22Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
