id = "T-540"
kind = "work"
title = "T-384 Class-R only — no IT for attendance retract on match re-point"
summary = "T-384 Class-R only — no IT for attendance retract on match re-point"
class = "feature"
status = "shipped"
order = 3380
executor = "claude-code"
priority = 2
created_at = "2026-07-28T00:00:00Z"
completed_at = "2026-07-28T00:00:00Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "no subject commits; created_at/completed_at id-interpolated between T-539 and T-541 (midpoint, day precision); shipped_at left absent — no subject commits, a SHA is never invented"
migration_legacy = [
    "FOUND by W53 adversarial verifier (CLEAN MINOR-NIT) after T-384.",
    "",
    "ingest_match_results retracts prior event_mission attendance on re-point with a NOT EXISTS other-match guard. Coverage is Class-R source pin only; pin comment falsely cites an IT in tests/telemetry.rs. Cure: HTTP/IT that re-POSTs a match EV1→EV2 and asserts EV1 returns to registered while EV2 is attended.",
    "",
    "Repro: rg retract_from apps/website/api — no IT exercises the path.",
]
owns = [
    "apps/website/api/tests/telemetry.rs",
    "apps/website/api/src/handlers/telemetry.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
