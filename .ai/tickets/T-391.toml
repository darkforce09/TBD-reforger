id = "T-391"
kind = "work"
title = "XSS via aar_replay_url; no API URL validation"
summary = "Live javascript: XSS via aar_replay_url, and zero URL validation anywhere in the API"
class = "bug"
status = "shipped"
order = 3207
executor = "claude-code"
notes = """(browsers (decl (read **CSV **There **`<a **executes **no **public-tier**, :361) :447) ALSO CDN Excel/Sheets. FOUND, Latent So That The `+`, `-` `:113`. `<a `<img `=`, `@` `Url::parse|scheme()|starts_with("http|javascript:|sanitize_url` `aar_replay_url` `announcements.thumbnail_url`, `events.banner_image_url`, `format!`-interpolating `frontend/src/deployments.rs:471` `handlers/audit.rs:111` `handlers/telemetry.rs:659`/`:628` `javascript:` `missions.thumbnail_url`, `modpacks.workshop_url` `rel="noreferrer"` `users.avatar_url` a across an any apps/website/api/src are argument as audit-log avatar beginning built click**. cms.rs:64. column. crate** different do entirely, export field. frontend/src/event_hub.rs:236 guard, has href>` href>`**. injection** is it kind**, last live mechanism: of one one-line or patch path path/query/fragment raw registry_import.rs:82's run same service-token, sink, so src) src>` sweep, that the token unescaped, unguarded. unpopulated validation, value was written zero"""
priority = 0
main_goal = "Add shared http/https URL guard at API write boundary; fix javascript: XSS."
context = [
    "LIVE. Found by paid-for subagent during T-239 sweep (parent slice cut for budget).",
    "frontend/deployments.rs:471 binds aar_replay_url into <a href>; telemetry.rs writes with no validation.",
    "javascript: URL executes on click. rel=noreferrer does not help.",
    "Zero URL validation in entire API crate — grep Url::parse scheme starts_with sanitize_url returns NOTHING.",
]
requirement = [
    "FIX: validate scheme at write boundary (allow http/https only), reject at 400 rather than storing.",
    "Do once in shared helper — five columns and five sinks share this.",
    "Tier: service-token — needs game-server token; real but not anonymous.",
    "img src siblings weaker but share absent guard: thumbnail_url, banner_image_url, avatar_url.",
    "users.avatar_url public-tier — format! Discord hash with no character validation.",
    "modpacks.workshop_url reaches <a href> at event_hub.rs:236 but NO WRITER — latent sink.",
    "ALSO: CSV formula injection at audit.rs:111 and :113 — user-controlled text unescaped for Excel.",
]
current_state = [
    "registry_import.rs:82 INSERT omits workshop_url column — unpopulated.",
    "Discord webhook consumer at cms.rs:64 also escapes Leptos-escaping argument.",
]
citations = [
    "T-239 security sweep",
    "deployments.rs:471 href",
    "telemetry.rs:659 writer",
    "audit.rs CSV injection",
]
shipped_at = "51924bf8"
created_at = "2026-07-26T13:18:16Z"
completed_at = "2026-07-26T17:06:05Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 8 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/telemetry.rs",
    "apps/website/api/src/services/text.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
