id = "T-090.4"
kind = "work"
title = "T-090.4 — Z placement audit (buried / floating objects)"
summary = "T-090.4 — Z placement audit (buried / floating objects)"
status = "ready"
order = 900
spec = "docs/specs/Mission_Creator_Architecture/t090_4_z_placement_audit.md"
executor = "claude-code"
parent = "T-090"
user_story = "Phase A: offline tool compares each exported map object's pivot Z against the T-091 DEM at (x,y) — fast pass over 1M+ Eden objects. This slice is Phase A only — detect, no auto-fix."
acceptance = [
    "Each catalog instance gets demZ vs z with per-kind warn/fail thresholds.",
    "Missing z is warn; no auto-fix (geometry-aware pass is T-090.6).",
]
owns = [
    "tools/tbd-tools/src/world",
    "packages/map-assets/everon/objects",
]

[scope.repo]
layers = ["xtask"]
