id = "T-664"
kind = "work"
title = "Right-click context menu — the slice that unblocks six tickets"
summary = "The unblocking slice. Once T-662 has narrowed the pan branch to MMB and removed the blanket `prevent_default`, `oncontextmenu` at `apps/website/frontend/src/mission_editor.rs`:1844-1847 opens a menu at the event pixel instead of swallowing the event. A new module `apps/website/frontend/src/context_menu.rs` carries the item model, hit-target resolution and keyboard dismissal — INFERRED module name, following the flat one-concern-per-file convention (select_tool.rs, world_layer_prefs.rs, mission_size.rs); wave 0 pre-declares the `mod` stub so this ticket does not touch main.rs. Verbatim Eden item lists for both the empty-ground and on-entity takes are transcribed at `.ai/artifacts/eden_screenshots/batch01_context_menu.md:119-292`. Directly closes `PLACE-COMMENT-001`'s entry point and UNBLOCKS `CREW-SEAT-001`, `CONN-START-001`, `CTX-FORMATION-001`, `ATTR-MULTI-001`, `COMP-SAVE-001` and `KEY-WP-001` — six tickets, which is why it runs at wave 2 and every dependent runs at wave 6 or later. Deliberately claims NO `eden_*` module: a context menu is a floating overlay mounted beside the other overlays at mission_editor.rs:2060-2110, not dock chrome, and claiming a dock would collide it with the docks for nothing."
status = "shipped"
order = 3840
executor = "claude-code"
notes = "editor-UI program wave 2 (wave_plan.tsv row 102) — SHIPPED wave 102 (editor factory), slice 705920f5; verifier wave102.md (4 MAJOR deferred: T-713..T-716)"
depends_on = [
    "T-661",
    "T-662",
]
owns = [
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/context_menu.rs",
]

[scope.website.editor]
chrome = []
