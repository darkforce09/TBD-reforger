id = "T-740"
kind = "work"
title = "Help prose miscounts bindings; redo mislabels chord"
summary = "Help table prose miscounts the bindings and mislabels the redo chord"
class = "bug"
status = "shipped"
order = 4616
executor = "claude-code"
notes = '''
filed by wave-112 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

NIT from the wave-112 verifier. eden_help.rs:5 says the editor binds 'sixteen' codes; the verifier re-derived by hand and the real count on merged main is SEVENTEEN (15 editor codes + KeyZ/KeyY). The table itself documents all 17 and both direction-pins are genuinely satisfied — only the prose is wrong, and it went wrong in the wave-112 merge fix, which added the KeyA row without updating the sentence. The ticket summary's original 'TBD binds 10 keyboard shortcuts' is stale twice over. Also eden_help.rs:166: the redo row reads 'Ctrl + Y' but the guard is ctrl||meta (mission_history.rs:490), so Cmd+Y redoes too and the row under-documents it. Diagnosis: editor_verify/wave112.md NIT-2, NIT-3.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["""NIT from the wave-112 verifier. eden_help.rs:5 says the editor binds 'sixteen' codes; the verifier re-derived by hand and the real count on merged main is SEVENTEEN (15 editor codes + KeyZ/KeyY). The table itself documents all 17 and both direction-pins are genuinely satisfied — only the prose is wrong, and it went wrong in the wave-112 merge fix, which added the KeyA row without updating the sentence. The ticket summary's original 'TBD binds 10 keyboard shortcuts' is stale twice over. Also eden_help.rs:166: the redo row reads 'Ctrl + Y' but the guard is ctrl||meta (mission_history.rs:490), so Cmd+Y redoes too and the row under-documents it. Diagnosis: editor_verify/wave112.md NIT-2, NIT-3."""]

missing=0
'''
main_goal = "Fix help prose binding count and redo chord ctrl-or-meta label."
context = [
    "eden_help 5 says sixteen codes; merged main has seventeen bindings.",
    "Table documents all seventeen; direction-pins satisfied; prose wrong after KeyA row.",
    "Redo row reads Ctrl+Y but guard is ctrl||meta at mission_history 490.",
]
requirement = [
    "Update intro prose to seventeen codes count matching table.",
    "Redo row documents Ctrl+Y and Cmd+Y per ctrl||meta guard.",
]
current_state = [
    "Prose says sixteen after wave-112 merge added KeyA without sentence update.",
    "Redo help under-documents Cmd+Y though meta chord redoes too.",
]
approach = ["Correct binding count sentence at eden_help 5; fix redo row at 166."]
verify = [
    "Intro prose states seventeen bindings consistent with table and pins.",
    "Redo row shows Ctrl+Y and Cmd+Y matching mission_history guard.",
]
acceptance = [
    "KeyY redo help chord documented Ctrl/Cmd plus Y matching mission_history ctrl-or-meta guard.",
    "Class-R pin failed when redo row omitted Cmd on KeyY alternative chord text.",
]
citations = [
    "editor_verify/wave112 NIT-2",
    "editor_verify/wave112 NIT-3",
    "wave-112 KeyA merge",
    "mission_history.rs 490 guard",
    "c91789951 KeyY Cmd chord",
]
shipped_at = "ee8302d5"
created_at = "2026-08-08T15:49:22Z"
completed_at = "2026-08-08T15:49:41Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/eden_help.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
