id = "T-798"
kind = "work"
title = "Top-bar validation chip; V1 at load; Backspace hides all UI"
summary = "Validation moves to a top-bar error chip; V1 evaluates from load; Backspace hides everything"
class = "bug"
status = "shipped"
order = 4800
executor = "claude-code"
notes = "depends_on T-797; operator decision 3 + praised copy pinned; contrast numbers from review F-36 second pass | SHIPPED W204: chip in row 2 (anchored transient — no containing-block exposure), headless engine survives Backspace (pixel-pure hide verified), load-time truth on hydrated missions, V1 copy byte-verbatim, plate-calc contrast fixed. THE ACCEPTANCE LINE 'place a marker: count unchanged' IS UNACHIEVABLE AS WRITTEN — the first marker mints faction-{SIDE} outside this ticket's owns and V1 legitimately fires; measured 0->1 by the verifier; decision ticket T-826 (operator). Live-over-glass contrast 4.0-4.3 -> T-827."
main_goal = "Move validation to top-bar chip with load-time V1 eval; include chip in chrome_hidden; fix error contrast >=4.5:1."
context = [
    "Empty mission shows No issues until first marker summons V1 error.",
    "Floating validation panel is bottom furniture; Backspace leaves legend visible.",
    "Red count text fails WCAG at 3.9:1 on dark plate.",
]
requirement = [
    "Evaluate validation rules from editor load at hydrate t0.",
    "Top-strip chip N errors M warnings; click drops findings; retire floating panel.",
    "Include chip and dropdown in chrome_hidden hide set.",
    "Error red on plate contrast >=4.5:1; keep V1 message copy verbatim.",
]
current_state = [
    "validation_panel.rs mount eval fires only on doc_tick not at load.",
    "Backspace hide reduces buttons but validation legend persists on screen.",
    "Chip home depends on T-797 row-2 toolbar landing first.",
]
approach = [
    "Seed PayloadSource at hydrate for load-time rule evaluation.",
    "Wire chip in eden_top_strip reading chrome_hidden without mission_editor edit.",
    "Darken plate or brighten red one step for contrast fix.",
]
verify = [
    "Fresh empty mission: chip shows 1 error V1 at load.",
    "Place a marker: count unchanged no error-from-nowhere.",
    "Backspace: zero non-map UI pixels including legend.",
    "Contrast calc on the chip >=4.5:1.",
    "V1 copy string unchanged.",
]
citations = [
    "editor_hostile_ux_review F-11 F-35",
    "validation_panel mount eval doc_tick",
    "operator decision 3 top-bar chip",
    "WCAG red count 3.9:1 failure",
    "T-786 stack Esc dismiss semantics",
]
shipped_at = "fcec12e7"
created_at = "2026-08-10T04:58:48Z"
completed_at = "2026-08-10T05:01:31Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/validation_panel.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "top_strip",
    "validation",
]
