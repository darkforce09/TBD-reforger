id = "T-798"
kind = "work"
title = "Validation moves to a top-bar error chip; V1 evaluates from load; Backspace hides everything"
summary = "Validation moves to a top-bar error chip; V1 evaluates from load; Backspace hides everything"
class = "bug"
status = "shipped"
order = 4800
executor = "claude-code"
notes = "depends_on T-797; operator decision 3 + praised copy pinned; contrast numbers from review F-36 second pass | SHIPPED W204: chip in row 2 (anchored transient — no containing-block exposure), headless engine survives Backspace (pixel-pure hide verified), load-time truth on hydrated missions, V1 copy byte-verbatim, plate-calc contrast fixed. THE ACCEPTANCE LINE 'place a marker: count unchanged' IS UNACHIEVABLE AS WRITTEN — the first marker mints faction-{SIDE} outside this ticket's owns and V1 legitimately fires; measured 0->1 by the verifier; decision ticket T-826 (operator). Live-over-glass contrast 4.0-4.3 -> T-827."
estimated = ["scope"]
migration_legacy = ["UX-REVIEW BATCH (source: .ai/artifacts/editor_hostile_ux_review.md, commit 5d30c651; operator pass 2026-08-09). F-11/F-35/F-36 + operator decision 3. VERIFIED: an empty mission reports 'No issues' and placing ONE MARKER flips it to red '1 error — V1-PLAYER-SPAWN' (the empty mission was equally unspawnable — validation_panel.rs ~:885 mount eval only fires on doc content via ~:787 doc_tick, so the first mark an author makes summons an error about something they didn't touch). The panel floats bottom-left as one of FOUR bottom furniture pieces; operator decision: a top-bar count chip that drops down on click. Backspace hide-UI leaves the expanded validation legend on screen (VERIFIED: buttons 62->19 but legend persists) — the clean-screenshot feature keeps a debug panel in frame. And the red count text is the ONE WCAG failure in the app: rgb(239,68,68) on rgba(31,41,55,0.7) = 3.9:1 (every other measured pair passes; worst legit 5.96:1). FIX: (a) evaluate rules from editor load (PayloadSource seeded at hydrate) so t0 shows the true state; (b) chip in the top strip: 'N errors · M warnings', click drops the findings list; retire the floating panel; include the chip+dropdown in the chrome_hidden hide set (the strip already reads that signal — no mission_editor edit); (c) error red on its plate >=4.5:1 (darken plate or brighten red one step). KEEP THE MESSAGE COPY VERBATIM — the V1 text is the best writing in the product (operator + review agree). ⚠️ TRAPS: depends on T-797's row-2 toolbar landing first (chip home); Esc/dismiss semantics via the T-786 stack; the severity legend content stays (in the dropdown). ACCEPTANCE: scripted — fresh empty mission: chip shows '1 error' (V1) at load; place a marker: count unchanged (no error-from-nowhere); Backspace: zero non-map UI pixels including legend (screenshot diff vs pure-map baseline); contrast calc on the chip >=4.5:1; V1 copy string unchanged."]
owns = [
    "apps/website/frontend/src/eden_top_strip.rs",
    "apps/website/frontend/src/validation_panel.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "top_strip",
    "validation",
]
