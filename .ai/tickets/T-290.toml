id = "T-290"
kind = "work"
title = "Nine dead flatten fields mod never reads"
summary = """
meta.author, meta.templateId, meta.playerRange, the whole environment block, factions[].tickets, orbat[].type.
winConditions.mode, flow.briefingSeconds (advisory only), and the entire orbat block (parity-check only).
Either add mod readers or annotate them non-consumed so the next audit does not re-derive this.
"""
class = "audit"
status = "idea"
executor = "claude-code"
notes = "needs-operator: frozen unmappable Scope. "
context = [
    "Idea audit owning the flatten emit ledger; pack_last sole occupant of factory wave 15.",
    "Operator-frozen unmappable scope; notes flag needs-operator triage.",
]
requirement = ["Add mod readers or annotate nine dead outputs non-consumed so audits do not re-derive."]
current_state = ["Nine fields listed in summary: meta.*, environment, factions.tickets, orbat.type, winConditions.mode, briefingSeconds, whole orbat."]
approach = ["Wire Enfusion readers per field or document non-consumed status in the emit ledger."]
citations = [
    "TICKET_REGISTRY.md T-290 field list",
    "T-912.1 pack_last wave 15",
    "PLATFORM_FACTORY.md wave 15",
]
owns = ["crates/map-engine-core/src/mission/flatten.rs"]
pack_last = true

[scope]
domain = "repo"
layer = "docs"
