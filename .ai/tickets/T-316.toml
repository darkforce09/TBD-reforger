id = "T-316"
kind = "work"
title = "Telemetry re-ingest reverts a completed match and zeroes player stats"
summary = "Telemetry re-ingest reverts a completed match and zeroes player stats"
class = "bug"
status = "shipped"
order = 3132
executor = "claude-code"
priority = 0
main_goal = "Telemetry re-ingest must not revert completed matches, zero player stats, or write false heartbeat samples."
context = ["FOUND by T-218's sweep."]
requirement = ["Fix: require the fields, or make the writes COALESCE-style partial updates. Decide per field which is right — a heartbeat legitimately omits fields; a match result does not."]
current_state = [
    "Three defaulted structs in handlers/telemetry.rs reach destructive writes and do NOT self-heal:",
    "  :172-177 MatchInput.outcome / winning_faction / aar_replay_url — re-POSTing a match id with a partial body reverts a completed, won match to `pending` with no winner and drops the AAR link.",
    "  :186-196 PlayerStatInput counters — re-ingest zeroes kills/deaths/etc, and that propagates into `users` stats and the leaderboard materialized view.",
    "  :41-57 ServerStatusInput — a partial heartbeat writes a permanent `player_count=0, server_fps=0` history sample and fires a false `server.low_fps` audit WARN.",
    "These are service-token endpoints, so a game-server bug or a retried request corrupts history silently.",
]
citations = [
    "T-218 sweep",
    "handlers/telemetry.rs MatchInput",
    "handlers/telemetry.rs PlayerStatInput",
    "handlers/telemetry.rs ServerStatusInput",
]
shipped_at = "7da9f742"
created_at = "2026-07-26T06:55:56Z"
completed_at = "2026-07-26T14:36:38Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "tokens",
]
estimate_note = "created_at/completed_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/src/handlers/telemetry.rs",
    "apps/website/api/tests/telemetry.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
