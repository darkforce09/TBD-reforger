id = "T-575"
kind = "work"
title = "wave.sh still exports dead MIGRATE_TEST_DATABASE_URL after T-558"
summary = "wave.sh still exports dead MIGRATE_TEST_DATABASE_URL after T-558"
class = "docs"
status = "shipped"
order = 3428
executor = "claude-code"
priority = 3
main_goal = "Drop dead MIGRATE_TEST_DATABASE_URL export from wave.sh and stale docs."
context = [
    "W68 adversarial verifier CLEAN NIT after T-558.",
    "Harmless leftover; not a silent gate skip.",
]
requirement = [
    "Drop MIGRATE_TEST_DATABASE_URL export from wave.sh.",
    "Remove docs still describing the shared migrate URL.",
]
current_state = [
    "wave.sh ~341 still exports MIGRATE_TEST_DATABASE_URL.",
    "db_migrate / models_fromrow use per-binary provision; consumers gone.",
]
approach = ["Delete export from wave.sh; update any stale docs referencing shared migrate URL."]
verify = [
    "rg MIGRATE_TEST_DATABASE_URL wave.sh → no export.",
    "rg in api tests → consumers still absent.",
]
citations = [
    "W68 verifier CLEAN NIT T-558",
    "MIGRATE_TEST_DATABASE_URL dead export",
    "per-binary provision db_migrate",
]
shipped_at = "d9c666e1"
created_at = "2026-07-28T23:03:36Z"
completed_at = "2026-07-31T20:40:08Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/platform/wave.sh"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
