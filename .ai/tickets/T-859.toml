id = "T-859"
kind = "work"
title = "T-853 port: manual-test -> mod manual-test"
summary = "T-853 port: manual-test -> mod manual-test"
class = "chore"
status = "shipped"
order = 5400
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "cbcd549d"
created_at = "2026-08-12T03:26:47Z"
completed_at = "2026-08-12T03:51:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/manual-test.sh (230 lines) to `cargo xtask mod manual-test` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). manual mod test suite. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/manual-test.sh",
    "xtask/src/gate_manual_test.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
