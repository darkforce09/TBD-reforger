id = "T-595"
kind = "work"
title = "API Unix client for T-289 game agent"
summary = "The API side of the game-server agent: T-289 built the channel, nothing calls it"
class = "bug"
status = "shipped"
order = 3462
executor = "claude-code"
priority = 1
main_goal = "Wire API UnixStream client to T-289 agent; map send_rcon to 202/409/503 with post-outcome audit."
context = [
    "T-289 shipped token-guarded agent in deploy-staging.sh; API cannot reach it yet.",
    "API and game server sibling systemctl --user units; SocketMode 0600 is credential.",
]
requirement = [
    "Add GAME_AGENT_SOCKET in config.rs; services/game_agent.rs over UnixStream.",
    "Client timeout must exceed agent eight-second dwell or slow answers read unreachable.",
    "send_rcon maps Accepted/Rejected/Unreachable; audit records outcome not attempt.",
    "Update admin.rs:517 comment; refuted separate-host premise from T-289 analysis.",
]
current_state = [
    "send_rcon returns unconditional 503 RCON_NO_TRANSPORT; SPA success path dead.",
    "kick unbuildable until RconInput gains player field; agent install opt-in.",
]
approach = [
    "game_agent.rs over tokio::net::UnixStream; no new dependency.",
    "handlers/admin.rs outcome mapping replaces T-269 unconditional 503 stub.",
]
verify = [
    "server_control.rs Ok arm and rcon_accepted_message reachable after wire.",
    "TBD_INSTALL_AGENT=1 required before end-to-end exercise on live host.",
]
citations = [
    "T-289 deploy-staging agent spec",
    "T-269 RCON_NO_TRANSPORT stub",
    "T-579 RconAccepted DTO",
    "STAGING-SERVER systemctl exit zero",
]
shipped_at = "c7a3ff78"
created_at = "2026-07-31T14:49:47Z"
completed_at = "2026-07-31T15:04:08Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = [
    "apps/website/api/src/services/game_agent.rs",
    "apps/website/api/src/handlers/admin.rs",
    "apps/website/api/src/config.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
