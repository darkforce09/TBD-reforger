id = "T-788"
kind = "work"
title = "Multi-edit: one undo, dblclick reachable, panel follows selection"
summary = "Multi-edit: one undo step, reachable from double-click, and the panel follows the selection"
class = "bug"
status = "shipped"
order = 4700
executor = "claude-code"
notes = "depends_on T-785; T-649 shipped the inversion this hardens; wave-144-style: the panel copy is the best in the product — preserve verbatim | SHIPPED W202: F-27 keep-multi (both dblclick arms) + F-29 panel-follows-selection by the slice; F-26 SPLIT by premise check — position path was already atomic (T-732, not re-implemented), the identity path the review measured was completed in-wave via the new core update_slots_attr_batch (one begin(), 8dbad948, T-732 pattern, wrappers delegate byte-identically). Verifier proved one-undo live for uniform AND ticked-differing 8-slot applies; batch reads the raw slot map (hidden-slot family checked); T-813 latch and armed-pointerup intact."
main_goal = "Batch apply-to-all one undo; dblclick opens multi-edit; panel follows selection."
context = [
    "F-26 F-27 F-29 three defects on T-649 multi-edit shipped inversion.",
    "F-26: apply-to-all writes one undo step per slot not one transaction.",
    "F-27: dblclick collapses SEL9 to SEL1 before multi-edit can open.",
]
requirement = [
    "Batch apply-to-all into one transaction; nine-slot apply one Ctrl+Z.",
    "Fix dblclick arms so click inside selection does not replace it.",
    "Re-render panel on selection change or close when SEL changes.",
]
current_state = [
    "Multi-edit opens only via RMB Attributes; natural gestures destroy SEL.",
    "Panel stale on Ctrl+A shows old slot while SEL is nine.",
    "T-785 text_field lands first; keep Multiple values locked semantics.",
]
approach = [
    "editor_ops attrs_update_position_multi and slot_multi batch one txn.",
    "mission_editor dblclick arms preserve selection inside current SEL.",
    "attrs_open selection-change effect re-renders header against live SEL.",
]
verify = [
    "Select nine apply-to-all one Ctrl+Z reverts all nine validator count.",
    "Ctrl+A map-dblclick opens N slots selected multi-edit SEL unchanged.",
    "Panel open plus Ctrl+A header switches multi within one frame.",
]
citations = [
    "editor_hostile_ux_review F-26",
    "editor_ops 1617 2466",
    "open_attrs_modal 1235-1258",
    "T-649 multi-edit inversion",
]
shipped_at = "8dbad948"
created_at = "2026-08-09T20:41:43Z"
completed_at = "2026-08-09T21:07:50Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/editor_ops.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "ops_undo",
    "map_canvas",
]
