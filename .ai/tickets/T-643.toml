id = "T-643"
kind = "work"
title = "Line of Sight: point-to-point ray"
summary = "Line of Sight: point-to-point ray"
class = "feature"
status = "shipped"
order = 4030
executor = "claude-code"
notes = """
editor-UI program wave 9 (wave_plan.tsv row 109) — SHIPPED wave 109 (editor factory), slice efe403b4; verifier wave109.md

--- HEAD migration_legacy (verbatim) ---

Operator-requested; ships BEFORE the viewshed. The second of the two dead buttons — the LoS stub is eden_chrome.rs:3715-3717 (`TOOL_DISABLED disabled=true`, MaterialIcon name="visibility"), adjacent to the ruler stub, which is why this and T-642 are one wave apart. Click observer, click target -> clear/blocked plus the terrain profile between them. Samples the loaded DEM (`crates/map-engine-core`, 6400^2 uint16, +/-0.204 m against 11 survey anchors at T-091.0). Cheap, immediately useful for checking a firing position, and it PROVES the DEM sampling path that T-644 then reuses. Code: the button in post-split eden_toolbelt.rs; two-click capture in the same pointer handlers as the ruler (mission_editor.rs); the ray walk and profile in a NEW `apps/website/frontend/src/los_tool.rs` (module name INFERRED; wave 0 pre-declares the stub); `crates/map-engine-core/src/dem/sample.rs` (232 lines, holds DemManifest and the elevation sampler) needs a SEGMENT sampler, not a point sampler. DECIDE INSIDE THE TICKET: eye height above ground for observer and target — a standing soldier is not the terrain surface — and whether the profile renders as an inline chart or an overlay. Same honesty rule as T-642: do not un-disable the button until the tool works.

missing=0
"""
depends_on = ["T-661"]
main_goal = "Point-to-point LoS ray with terrain profile; prove DEM segment sampling for T-644."
context = [
    "Operator-requested; ships before T-644 viewshed.",
    "LoS stub eden_chrome.rs:3715-3717 TOOL_DISABLED adjacent to T-642 ruler.",
    "DEM 6400² uint16 ±0.204m vs 11 survey anchors at T-091.0.",
]
requirement = [
    "Click observer then target; show clear/blocked plus terrain profile.",
    "Wire eden_toolbelt.rs button; two-click capture in mission_editor.rs handlers.",
    "Ray walk in new los_tool.rs; segment sampler in dem/sample.rs not point.",
]
current_state = [
    "LoS button disabled MaterialIcon visibility; wave 0 pre-declares los_tool stub.",
    "dem/sample.rs 232 lines holds DemManifest and elevation point sampler only.",
    "T-644 hard depends on this proving DEM sampling path first.",
]
approach = [
    "DECIDE INSIDE TICKET: eye height above ground for observer and target.",
    "DECIDE INSIDE TICKET: profile as inline chart or overlay.",
    "Do not un-disable button until tool works; same honesty rule as T-642.",
]
verify = [
    "Clear/blocked result and terrain profile between observer and target clicks.",
    "Segment sampler walks loaded DEM elevation along the ray path.",
]
acceptance = [
    "visibility toolbelt button enabled; two-click observer-target capture wired in mission_editor.",
    "Point-to-point ray reported clear or blocked plus terrain profile along segment sampler path.",
    "dem/sample.rs segment sampler walked distances monotonically for T-644 viewshed reuse.",
]
citations = [
    "T-642 adjacent dead button stub",
    "eden_chrome.rs:3715 LoS stub",
    "T-091.0 DEM survey anchors",
    "T-644 reuses DEM sampling path",
]
shipped_at = "ec561cc4"
created_at = "2026-08-02T06:56:47Z"
completed_at = "2026-08-02T07:04:13Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = [
    "apps/website/frontend/src/eden_toolbelt.rs",
    "apps/website/frontend/src/mission_editor.rs",
    "apps/website/frontend/src/los_tool.rs",
    "crates/map-engine-core/src/dem/sample.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "toolbelt",
    "map_canvas",
    "tools",
]
