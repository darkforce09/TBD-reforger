id = "T-652"
kind = "work"
title = "Rocks are not rendered"
summary = "Rocks are not rendered"
class = "feature"
status = "deferred"
order = 4240
executor = "claude-code"
notes = """
not factory-dispatchable; no owns, no wave row

--- HEAD migration_legacy (verbatim) ---

DEFERRED by operator decision — "doesn't matter if they're rocks, that's fine." Recorded because it is cheaper than it sounds and someone will re-derive it otherwise: the data ALREADY SHIPS. `packages/map-assets/everon/manifest.json` lists `P4_rocks` in `importPhaseShipped` and carries a `rockLarge` type-inventory entry, so the chunks contain rocks and nothing draws them. This is a RENDER gap, not an export gap. No framework evidence bears on it. If promoted it would land in `crates/map-engine-render/src/engine.rs` plus the world glyph atlas — INFERRED, not derived; it carries no owns and takes no wave slot.

missing=0"""
main_goal = "Deferred operator decision; rocks data ships but render draws nothing."
context = [
    "DEFERRED by operator: doesn't matter if they're rocks.",
    "P4_rocks in everon manifest importPhaseShipped rockLarge inventory.",
    "Render gap not export gap; no framework evidence.",
]
requirement = ["No work while deferred; promotion would target map-engine-render engine.rs."]
current_state = [
    "packages/map-assets/everon/manifest.json lists P4_rocks shipped phase.",
    "Chunks contain rocks; nothing draws them on map.",
    "No owns; takes no wave slot; not factory-dispatchable.",
]
approach = ["If promoted land engine.rs plus world glyph atlas INFERRED not derived."]
verify = ["Operator deferral recorded; cheaper than re-deriving later."]
citations = [
    "everon manifest P4_rocks shipped",
    "operator deferral rocks fine",
    "map-engine-render engine.rs inferred",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
