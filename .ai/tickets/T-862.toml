id = "T-862"
kind = "work"
title = "T-853 port: fetch-vanilla-source -> fetch vanilla-source"
summary = "T-853 port: fetch-vanilla-source -> fetch vanilla-source"
class = "chore"
status = "shipped"
order = 5430
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
shipped_at = "1a4351fb"
created_at = "2026-08-12T04:26:24Z"
completed_at = "2026-08-12T04:48:45Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
migration_legacy = ["T-853 shell->xtask slice. Port scripts/mod/fetch-vanilla-source.sh (103 lines) to `cargo xtask fetch vanilla-source` and delete the script, its scripts/shell-inventory.txt line, any scripts/python-inventory.txt line, and repoint every caller (Makefile, scripts/platform/wave.sh, scripts/mod/wave.sh, .github/workflows). mirror vanilla Enfusion sources. ACCEPTANCE IS BYTE-FOR-BYTE: capture the bash stdout+stderr+rc first, then diff the port against it on a clean tree AND on at least two deliberately broken trees. A passing run alone is NOT evidence (T-556 anti-vacuity). Build on crates/tbd-gate; do not hand-roll verdicts or process handling."]
owns = [
    "scripts/mod/fetch-vanilla-source.sh",
    "xtask/src/gate_fetch_vanilla_source.rs",
]

[scope]
domain = "repo"
layer = "xtask"
component = "gates"
