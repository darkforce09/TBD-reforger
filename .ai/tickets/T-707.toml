id = "T-707"
kind = "work"
title = "wave.sh test-split comment transposes its own measurement"
summary = "wave.sh test-split comment transposes its own measurement"
class = "bug"
status = "deferred"
order = 4300
executor = "claude-code"
notes = "filed by wave-100 adversarial verifier (editor factory)"
main_goal = "Fix transposed xtask versus tbd-tools counts in wave.sh header."
context = [
    "wave.sh test xtask+tbd-tools header records 51 xtask plus 33 tbd-tools inverted.",
    "Wave-100 verifier: tbd-tools runs 51 tests; xtask 33 at measurement.",
]
requirement = [
    "Correct header comment to 51 tbd-tools and 33 xtask; one line.",
    "No behavior change; comment fix only per wave100 F-2.",
]
current_state = [
    "Comment cites T-361 serve tests living in tbd-tools serve.rs.",
    "xtask count 40 today after T-611 and T-623 additions.",
]
approach = ["Swap transposed counts in wave.sh test-split header comment."]
verify = ["Header comment matches measured tbd-tools 51 and xtask test totals."]
citations = [
    "wave.sh test header comment",
    "wave100.md F-2 diagnosis",
    "tbd-tools serve.rs T-361",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
