id = "T-890"
kind = "work"
title = "T-853 port: the MOD wave driver (NOT platform/wave.sh)"
summary = "T-853 port: the MOD wave driver (NOT platform/wave.sh)"
class = "docs"
status = "shipped"
order = 5710
spec = "docs/platform/t853_shell_to_xtask_waves.md"
executor = "claude-code"
main_goal = "Port mod wave.sh to cargo xtask mod wave; do not touch platform wave."
context = [
    "T-853 shell->xtask slice; 232 lines one python3 site MOD wave driver.",
    "Reads docs/mod/wave_plan.tsv; manages .ai/artifacts/worktrees.",
    "Lower risk than platform/wave.sh; T-181 mod program complete.",
]
requirement = [
    "Delete script(s) shell-inventory.txt line(s) python-inventory.txt line(s).",
    "Port to cargo xtask mod wave; repoint every caller.",
    "DO NOT touch scripts/platform/wave.sh beyond repointing call sites.",
]
current_state = [
    "scripts/mod/wave.sh bash wave driver live today.",
    "platform/wave.sh separate; repoint call sites only.",
]
approach = [
    "Capture bash stdout stderr rc first as byte-for-byte baseline.",
    "Diff port on clean tree and >=2 deliberately broken trees.",
    "Harness must assert BASH side went red before believing diff.",
]
verify = [
    "Capture bash stdout stderr rc baseline recorded first.",
    "Diff on clean tree AND >=2 deliberately broken trees.",
    "Harness asserts BASH side went red; green run alone not evidence T-556.",
]
citations = [
    "T-853 shell->xtask program",
    "T-181 mod program complete",
    "T-556 anti-vacuity",
    "t853_shell_to_xtask_waves.md",
    "crates/tbd-gate",
]
shipped_at = "29621118"
created_at = "2026-08-12T10:40:09Z"
completed_at = "2026-08-12T10:40:39Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["scripts/mod/wave.sh"]

[scope]
domain = "repo"
layer = "xtask"
