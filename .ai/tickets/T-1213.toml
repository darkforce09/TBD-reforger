id = "T-1213"
kind = "work"
title = "Delete ticket-id history comments from root Cargo and toolchain manifests"
summary = "Root Cargo.toml:2-3 cites 'T-0xx Phase 2A folded website-mission-core' and rust-toolchain.toml:2-6 opens with 'T-177' and 'Before this only ...' history; comments must state present-tense invariants without ticket ids or transitions."
class = "chore"
status = "idea"
notes = "Cargo.toml:2-3 (workspace root): 'T-0xx Phase 2A folded `website-mission-core` into `website-map-engine`; the headless mission domain is now that crate's `data/` module' - a placeholder ticket id plus a transition; the present-tense fact is that the mission domain is `website-map-engine`'s `data/` module, gated on the `scenario` and `store` features. rust-toolchain.toml:2-6: 'T-177 - pin the WHOLE workspace toolchain. Before this only apps/website/api_v2/rust-toolchain.toml pinned a channel ...' - a ticket id plus history; keep the invariant (one pinned channel for the workspace, kept in lockstep with tools_v2/developer-tools/gate-env.json and apps/website/api_v2/rust-toolchain.toml; rustup resolves it per checkout). CLAUDE.md Law 8 bans ticket references and historical transitions in comments. No existing test scans root manifests (tooling_prose_rules.rs covers tools_v2/, architecture_rules.rs the API sources, doc_audit the frontend src/v2 tree). The member-crate manifests (apps/website/map-engine/Cargo.toml, apps/website/frontend/Cargo.toml) are T-1004's; the ticket-engine and verification-core manifest comments are T-1139's. Source: Phase 6 finding, ticket batch TF16."
created_at = "2026-09-26T05:51:21Z"

[scope]
domain = "repo"
layer = "tools"
