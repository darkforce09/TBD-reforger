id = "T-728"
kind = "work"
title = "Save-composition affordance reads a channel selection never bumps"
summary = "Save-composition affordance reads a channel selection never bumps"
class = "bug"
status = "deferred"
order = 4321
executor = "claude-code"
notes = """
filed by wave-108 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

T-650's 'Save composition…' affordance gates on selection_len() but subscribes to doc_tick, and selection changes never bump doc_tick: the canonical flow (place, marquee-select, open Compositions tab) shows the empty state against a live selection until an unrelated doc edit; inversely a stale Save press no-ops silently. Fix: subscribe to the selection channel (the SEL readout has one — reuse it) or bump a selection_tick from the selection mutators; test the canonical flow as an event sequence. Diagnosis: editor_verify/wave108.md MINOR-2.

missing=0"""
main_goal = "Fix Save composition affordance ignoring selection channel bumps."
context = [
    "T-650 Save composition affordance gates on selection_len().",
    "Subscribes to doc_tick; selection changes never bump doc_tick.",
]
requirement = [
    "Subscribe to selection channel or bump selection_tick from mutators.",
    "Test canonical flow place marquee-select open Compositions tab.",
]
current_state = [
    "Canonical flow shows empty state against live selection until doc edit.",
    "Stale Save press no-ops silently when selection unchanged.",
]
approach = [
    "Reuse SEL readout selection channel subscription.",
    "Or bump selection_tick from selection mutators.",
]
verify = ["Event sequence test canonical place select Compositions flow."]
citations = [
    "T-650 Save composition affordance",
    "wave108.md MINOR-2 diagnosis",
]
estimated = ["scope"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
