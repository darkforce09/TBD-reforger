id = "T-181.13"
kind = "work"
title = "JSON faction_eliminated win tick"
summary = "Mission winConditions endOn; TickWinConditions every 2s on LIVE."
class = "feature"
status = "shipped"
order = 1810
executor = "claude-code"
notes = "JSON-driven round end. TBD_MissionWinConditionsStruct {mode, endOn[]} added to the mission document (@contract mission.schema.json#/$defs/winConditions) + TBD_MissionLoader.HasEndTrigger(). TickWinConditions in TBD_FrameworkManager (2s, armed only on LIVE and only if the mission declared faction_eliminated). Guards: ignores factions with 0 claimed slots (unfielded != eliminated), requires >=2 contesting factions (a solo test cannot end the round), unregisters itself off LIVE. time_limit and the other three enum triggers parse but are not evaluated yet — declared, not silently dropped."
parent = "T-181"
context = ["Wave 0 round end driven by mission JSON, not admin-only stop."]
requirement = [
    "HasEndTrigger gates tick; ignore unfielded factions; need two contestants.",
    "time_limit and other endOn modes parse but stay unevaluated.",
]
citations = ["Source: commit d8fc6abc — win tick."]
shipped_at = "e35bc686"
created_at = "2026-07-25T14:59:39Z"
completed_at = "2026-07-25T14:59:39Z"
estimated = [
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 1 commit subject(s)"

[scope]
domain = "repo"
layer = "docs"
