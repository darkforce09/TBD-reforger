id = "T-255"
kind = "work"
title = "Asset palette is not side-aware"
summary = "Asset palette is not side-aware"
class = "chore"
status = "shipped"
order = 2730
executor = "claude-code"
main_goal = "Asset palette accepts BLUFOR chips with USSR characters because build_catalog_tree takes no side."
context = [
    "T-153 specified the Faction Library should replace the raw registry dump here;",
    "the Leptos port reverted it and documents the revert in its own header.",
]
requirement = ["Asset palette is not side-aware."]
current_state = [
    "build_catalog_tree (asset_catalog.rs:60) takes no side and filters kind==character,",
    "so a BLUFOR chip plus a USSR character is accepted silently — even though the registry categories already encode the side.",
]
approach = [
    "build_catalog_tree now takes active Eden side; character_matches_eden_side filters cross-side rows.",
    "mission_editor Effect rebuilds catalog Ready tree whenever active_side chip flips.",
]
verify = [
    "cargo test -p website-frontend side_filter_excludes_cross_side_characters.",
    "cargo test -p website-frontend eden_chip_side_rebuilds_filtered_catalog.",
]
acceptance = ["BLUFOR chip excluded USSR leaves; OPFOR chip rebuilt palette without NATO characters."]
citations = [
    "T-153",
    "asset_catalog.rs:60",
]
shipped_at = "df33e106"
created_at = "2026-07-26T23:08:06Z"
completed_at = "2026-07-26T23:15:28Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = [
    "apps/website/frontend/src/asset_catalog.rs",
    "apps/website/frontend/src/eden_chrome.rs",
    "apps/website/frontend/src/mission_editor.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = [
    "asset_browser",
    "layout_chrome",
    "map_canvas",
]
