id = "T-781"
kind = "work"
title = "Comments still cannot be composed — PLACE-COMMENT-001's last undelivered clause"
summary = "Comments still cannot be composed — PLACE-COMMENT-001's last undelivered clause"
class = "docs"
status = "shipped"
order = 4657
executor = "claude-code"
notes = "WIDENED 2026-08-08 by the operator's paste ruling. In addition to the composable clause, this ticket now owns the COMPOSITION Z: place_composition hard-codes `position_any(wx, wy, 0.0, rot)` in ALL THREE arms (slot, vehicle, object) in crates/map-engine-core/src/doc/store.rs. T-777 deferred it here explicitly because store.rs was a sibling's file that wave. The operator ruled (T-743/T-777) that the JS byte-parity was a migration safety net, not a contract, and that a pasted entity keeps its authored elevation — the same reasoning applies to a placed composition. Nothing re-samples terrain afterwards (terrainZ died with the React deletion), so a 0.0 written here is final. Wave-127 rules bind: reuse slot_z, read exact f64 rows and NOT the f32 SoA (which omits hidden-layer slots per T-665), and if you build a zs vector prove zs[i] matches ids[i] structurally."
main_goal = "Comments composable in selection; authoring metadata not compiled output."
context = [
    "PLACE-COMMENT-001 composable clause undelivered; T-748 closed icon glyph.",
    "Comments absent from selection lane; compositions capture selection only.",
    "Comments absent from compiled mission; must ride authoring metadata not flatten.",
]
requirement = [
    "Enable comment in composition capture from selection with entities.",
    "place_composition carries comment as authoring metadata not compiled emit.",
    "Do not change paste composition z landing; carry operator T-743 T-777 ruling.",
]
current_state = [
    "Composable clause silent drop per T-748 diagnosis HARD GATE bars.",
    "place_composition store captures replays; comment cannot ride rows today.",
    "place_composition z=0.0 on operator decision surface with T-743 see T-777.",
]
approach = [
    "Check place_composition capture replay whether comment rides rows sans flatten.",
    "Composition comment as authoring metadata absent from compiled mission output.",
    "Wave-127 slot_z exact f64 not f32 SoA T-665 if building zs vector.",
]
verify = [
    "Select comment with entities compose place comment at right offset.",
    "Comment survives undo redo restore not in compiled mission output.",
]
citations = [
    "PLACE-COMMENT-001",
    "T-748 wave 141 residue",
    "T-651 wave 114",
    "T-777 composition z rule",
]
shipped_at = "ab805357"
created_at = "2026-08-08T16:34:37Z"
completed_at = "2026-08-08T20:10:27Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 4 commit subject(s)"
owns = [
    "crates/map-engine-core/src/doc/store.rs",
    "apps/website/frontend/src/eden_dock_right.rs",
]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
surface = ["dock_right"]
