id = "T-466"
kind = "work"
title = "CMS Content list fails closed to empty with no retry"
summary = "CMS Content list fails closed to empty with no retry"
class = "feature"
status = "shipped"
order = 3305
executor = "claude-code"
notes = """
--- HEAD migration_legacy (verbatim) ---

Residual from wave 25 adversarial N1. Content LocalResource uses .ok(); failed GET → empty 'No announcements yet', list_seeded=true, no retry.

Repro: force GET /cms/announcements 500/network fail → UI looks like zero announcements forever.

Cure: surface error + retry; do not set list_seeded on Err.

missing=0
"""
priority = 3
main_goal = "Surface CMS Content list errors with retry; avoid empty false-green."
context = [
    "Residual from wave 25 adversarial N1.",
    "Content LocalResource uses .ok(); failed GET → empty state.",
]
requirement = [
    "Surface error + retry on GET failure.",
    "Do not set list_seeded on Err.",
]
current_state = [
    "GET /cms/announcements 500/network fail → zero announcements UI.",
    "No announcements yet with list_seeded=true; no retry.",
]
approach = [
    "Replace .ok() fail-closed with error surface and retry.",
    "Withhold list_seeded on Err.",
]
verify = ["cargo test -p website-frontend content_list_error_does_not_seed_as_empty_success"]
acceptance = ["Surfaced CMS list fetch errors with Retry UI; list_seeded set only on Ok, not on failed GET."]
citations = [
    "wave 25 adversarial N1",
    "Content LocalResource list",
    "content_list_error_no_seed test",
]
shipped_at = "cc79f890"
created_at = "2026-07-27T02:35:46Z"
completed_at = "2026-07-27T02:51:06Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 5 commit subject(s)"
owns = ["apps/website/frontend/src/content.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
