id = "T-737"
kind = "work"
title = "Import refusals drop RowError key row identity"
summary = "Import refusals drop RowError.key, so the author cannot tell which row failed"
class = "chore"
status = "shipped"
order = 4613
executor = "claude-code"
notes = '''
filed by wave-112 adversarial verifier (editor factory)

--- HEAD migration_legacy (verbatim) ---

MINOR from the wave-112 verifier. arsenal.rs:1031 renders import refusals through |e| e.message only, discarding RowError.key. Two stranded rows therefore both print 'Not compatible with the selected Primary weapon', indistinguishably — the author is told what is required but not which of their rows is at fault. Schema faults are unaffected (their messages carry the JSON path). Related and confirmed reachable: the deliberate export/import asymmetry T-686 flagged for review — export refuses on capacity only (arsenal.rs:743-750) while re-import additionally refuses compat when the compatibility feed is Ready, so a loadout with a stranded optic CAN be exported and CANNOT be re-imported. That asymmetry is defensible (the import gate's job is not to let an outside document put the editor into a state the author did not author) but it is exactly the case where naming the offending row matters most. Diagnosis: editor_verify/wave112.md MINOR-3.

--- HEAD migration_legacy line (verbatim) ---

migration_legacy = ["""MINOR from the wave-112 verifier. arsenal.rs:1031 renders import refusals through |e| e.message only, discarding RowError.key. Two stranded rows therefore both print 'Not compatible with the selected Primary weapon', indistinguishably — the author is told what is required but not which of their rows is at fault. Schema faults are unaffected (their messages carry the JSON path). Related and confirmed reachable: the deliberate export/import asymmetry T-686 flagged for review — export refuses on capacity only (arsenal.rs:743-750) while re-import additionally refuses compat when the compatibility feed is Ready, so a loadout with a stranded optic CAN be exported and CANNOT be re-imported. That asymmetry is defensible (the import gate's job is not to let an outside document put the editor into a state the author did not author) but it is exactly the case where naming the offending row matters most. Diagnosis: editor_verify/wave112.md MINOR-3."""]

missing=0
'''
main_goal = "Surface RowError key in import refusal messages so authors know which row failed."
context = [
    "Wave-112 MINOR: arsenal 1031 renders import refusals via message only.",
    "Two stranded rows print identical compat text; author cannot tell which row.",
    "T-686 export-import asymmetry: export cap-only; re-import also refuses compat.",
]
requirement = [
    "Include RowError.key in import refusal rendering alongside message.",
    "Schema fault messages with JSON path remain unchanged.",
]
current_state = [
    "Refusals use |e| e.message only; RowError.key discarded at arsenal 1031.",
    "Stranded optic loadout exports but cannot re-import when compat feed Ready.",
]
approach = ["Render row key with refusal message in import path at arsenal 1031."]
verify = ["Two stranded rows show distinguishable refusal messages naming offending row."]
acceptance = ["Import refusal lines included RowError key so two stranded rows showed distinguishable messages."]
citations = [
    "editor_verify/wave112 MINOR-3",
    "T-686 export import asymmetry",
    "RowError key discard",
    "arsenal.rs 743-750 export cap",
    "f1c46457 refusal_line key",
]
shipped_at = "f1c46457"
created_at = "2026-08-08T00:43:45Z"
completed_at = "2026-08-08T00:52:46Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 2 commit subject(s)"
owns = ["apps/website/frontend/src/arsenal.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
