id = "T-473"
kind = "work"
title = "T-470 Class-R still greens spaced/indirect Err seed + if-false Retry"
summary = "T-470 Class-R still greens spaced/indirect Err seed + if-false Retry"
class = "feature"
status = "shipped"
order = 3312
executor = "claude-code"
priority = 1
main_goal = "Harden Class-R list_seeded pin against spaced Err seed and if-false Retry."
context = [
    "Residual from wave 27 adversarial BLOCKER/MAJOR.",
    "Class-R pins exact list_seeded.set(true) only.",
]
requirement = [
    "Normalize whitespace for seed pin.",
    "Forbid list_seeded.set outside Ok arm; ban if false Retry.",
]
current_state = [
    "Repro: spaced set( true ) before match → Class-R green.",
    "Err set(true), set((true)), set(seed), if false Retry all PASS.",
]
approach = ["Cure: normalize whitespace; ban indirect set and unreachable Retry wrapper."]
verify = ["Repro: early set( true ) plus Err arm set → Class-R PASS."]
citations = [
    "wave 27 BLOCKER MAJOR",
    "content.rs list_seeded pin",
    "if false Retry wrapper",
]
shipped_at = "451883cb"
created_at = "2026-07-27T03:09:40Z"
completed_at = "2026-07-27T03:16:43Z"
estimated = [
    "scope",
    "created_at",
    "completed_at",
    "shipped_at",
    "tokens",
]
estimate_note = "created_at/completed_at/shipped_at git_subject-mined from 3 commit subject(s)"
owns = ["apps/website/frontend/src/content.rs"]

[scope]
domain = "website"
layer = "frontend"
component = "mission_creator"
