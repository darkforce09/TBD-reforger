//! Debug / test-bench surfaces — URL-only, never in the nav: the building-blueprint viewer
//! (`/debug/building-viewer`, Phase A of the blueprint extraction program) and the world
//! occluder bench (`/debug/world-los`, T-090.12.5).

pub mod building_interior;
pub mod building_viewer;
pub mod world_los;
pub mod world_los_scene;
