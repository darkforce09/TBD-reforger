//! Debug / test-bench surfaces — URL-only, never in the nav. Currently the building-blueprint
//! viewer (`/debug/building-viewer`, Phase A of the blueprint extraction program).

pub mod building_viewer;
