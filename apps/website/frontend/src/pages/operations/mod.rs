//! Operations surfaces still outside the `v2` tree. The module declares nothing: the routed
//! operations pages live under `v2::pages::operations`, and the editor's ORBAT and faction
//! authoring dialogs — which were never routed — live under `v2::apps::editor::ui::modals`.
