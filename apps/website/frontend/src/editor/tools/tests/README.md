# Tests

Native regression cases exercising `website-map-engine`'s `data::store` placement through the editor's public helper imports. The cases preserve coordinate, orientation, and deterministic seed contracts.
