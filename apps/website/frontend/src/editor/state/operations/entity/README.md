# entity

Frontend adapters for editor operations. These modules read Leptos signals, own host interaction state, call headless mission-core operations, and refresh the editor after successful mutations. CRDT transaction logic lives in mission-core.
