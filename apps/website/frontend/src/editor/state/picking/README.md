# Picking adapters

Convert pointer coordinates and pixel tolerances with the graphics camera, query spatial indices, then pass row candidates to mission-core for identity and visibility resolution. The camera snapshot and row order are shared across each pick.
