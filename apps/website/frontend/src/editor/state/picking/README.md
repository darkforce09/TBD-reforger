# Picking adapters

Convert pointer coordinates and pixel tolerances with the graphics camera, query spatial indices, then pass row candidates to `website-map-engine`'s `data::store` for identity and visibility resolution. The camera snapshot and row order are shared across each pick.
