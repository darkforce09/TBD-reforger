# entity

Document queries, identity allocation, and entity mutations grouped by authored resource. Clipboard and placement operations take ID counters and host policy callbacks explicitly. Rendering, selection signals, and post-edit refresh remain the caller's responsibility.
