# entity

Document queries, identity allocation, and entity mutations grouped by authored resource. Clipboard and placement operations take ID counters and host policy callbacks explicitly, and `armed_placement` is the one machine a canvas release runs: it commits whichever kind was picked up and reports what the host owes afterwards. Rendering, selection signals, and post-edit refresh remain the caller's responsibility.
