# id arrays

Ordered native Yrs arrays used for entity membership. The array helpers preserve idempotent insertion and stable removal order. Tests cover both standalone arrays and mission-document hydration.
