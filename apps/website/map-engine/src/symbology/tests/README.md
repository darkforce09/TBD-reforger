# symbology/tests

Cases for the marker icon vocabulary, its rasterised atlas, and caption placement.

## Boundaries

Tests keep the original assertions and fixtures; run the crate suite with `--all-features`.
