# Tests

Regression fixtures for the parent module. These cases exercise the production implementation, preserving authored JSON, ID order, numeric precision, and undo behavior. Run the crate's tests with all features enabled.
