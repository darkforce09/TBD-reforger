// Package models holds the GORM models whose snake_case JSON tags are the API
// contract (see docs/platform/DOCUMENTATION_STANDARDS.md §2 — running code is the
// source of truth). A model that projects a packages/tbd-schema definition carries
// an @contract tag pointing at the schema it mirrors.
package models
