# Template for apps/website/api/.env — copy, then fill in. `.env` is gitignored; keep real
# secrets out of this file. `Config::load` (src/config.rs) reads the process environment and
# loads `.env` best-effort, so an exported shell var wins over anything written here.
#
# Only DATABASE_URL and JWT_SECRET hard-fail on boot. Everything else degrades quietly, which
# is why "required for which path" is called out per var below: an empty Discord var does not
# stop the API, it silently disables — or subtly changes — one path.
#
# WORKTREES: `.env` is gitignored, so a fresh git worktree has none. Copy the main checkout's
# `apps/website/api/.env` into the worktree before `make api`.

# ─────────────────────────────── Server ───────────────────────────────
PORT=8080
# development | production. `development` additionally: exposes GET /api/v1/auth/dev-login, and
# drops `; Secure` from the oauth_state cookie so the OAuth flow works over plain http.
APP_ENV=development

# ────────────────────────── Frontend integration ──────────────────────
# SPA base URL. The OAuth + dev-login callbacks 302 here with the session in the URL *fragment*:
#   {FRONTEND_URL}/auth/callback#access_token=…&arma_linked=…&expires_at=…&refresh_token=…
# Use the SAME HOST STRING you browse the SPA on, and the same one as DISCORD_REDIRECT_URL below:
# `localhost` and `127.0.0.1` are different cookie hosts, and mixing them strands the oauth_state
# cookie → every login fails with #error=invalid_state. See DEV_RUNBOOK.md §Discord OAuth2.
#
# T-303 — THIS LINE SHIPPED MISMATCHED (`127.0.0.1` here, `localhost` on DISCORD_REDIRECT_URL), so
# every fresh checkout's first live Discord login failed `invalid_state` — which reads as CSRF
# tampering, not misconfiguration, and cost the operator real time. `localhost` is the host that
# moved here rather than the redirect moving to `127.0.0.1`, because DISCORD_REDIRECT_URL is the
# value with an EXTERNAL byte-exact contract: it must match a Redirect registered in the Discord
# Developer Portal, so changing it invalidates an already-working app registration and needs a
# manual out-of-repo step. FRONTEND_URL has no such contract — it is read only by this API. Move
# the value that is free to move. (`DATABASE_URL` below is already on `localhost` too.)
# `handlers/oauth.rs` now refuses to start the flow in development when these two disagree.
FRONTEND_URL=http://localhost:3000
# CORS allow-list (comma-separated). Dev uses the Trunk proxy, so this matters in production.
ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# T-159 Leptos SPA cutover (leave blank in dev — `make leptos` / `trunk serve` owns the SPA on :3000):
#   - SPA_DIST_DIR: when set, the API serves the built Leptos SPA statically (COOP/COEP + SPA
#     fallback) AND /map-assets, so the app runs same-origin with the API (no separate web host).
#     At the flip, set to the leptos dist and point FRONTEND_URL/ALLOWED_ORIGINS/the Discord app
#     redirect URI at the API's own origin (e.g. https://app.example.com) instead of :3000.
#   - MAP_ASSETS_DIR: the map-assets dir served at /map-assets (defaults to ../../../packages/map-assets
#     relative to the api/ working directory).
# SPA_DIST_DIR=../frontend/dist
# MAP_ASSETS_DIR=../../../packages/map-assets

# ────────────────────────────── Database ──────────────────────────────
# REQUIRED — the API refuses to boot without it (ConfigError::Missing).
DATABASE_URL=postgres://tbd:tbd@localhost:5434/tbd_reforger?sslmode=disable

# ──────────────────────────────── Auth ────────────────────────────────
# REQUIRED — the API refuses to boot without it. Signs the JWT access token; changing it
# invalidates every issued session. Generate: openssl rand -base64 36
JWT_SECRET=change-me-to-a-long-random-string
JWT_ACCESS_TTL_MIN=15

# ───────────────────── Discord OAuth2 + role sync ─────────────────────
# Discord Developer Portal → your application. Full operator procedure — including how to tell
# the failure modes apart — is docs/website/DEV_RUNBOOK.md §Discord OAuth2 — live round-trip.
#
# Required for: signing in as a real Discord user. NOT required for `make api` to boot, and not
# required for dev-login — leave all of these blank and the platform still runs on dev-login.

# OAuth2 → General → Client ID (a numeric snowflake; 19 digits today).
# Blank ⇒ /auth/discord/login short-circuits to #error=oauth_unconfigured rather than bouncing
# the user to an opaque Discord error page (services/discord.rs::authorize_url).
DISCORD_CLIENT_ID=

# OAuth2 → General → Client Secret (32 chars). Server-side only, used in the token exchange.
# Wrong or rotated ⇒ Discord 401s the exchange ⇒ the callback lands on #error=discord_unreachable —
# the SAME code a network outage gives, and the exchange error is not logged. Pre-flight the
# pair before blaming the network (no browser needed; 200 = good, 401 invalid_client = bad):
#   curl -su "$DISCORD_CLIENT_ID:$DISCORD_CLIENT_SECRET" \
#     -d grant_type=client_credentials -d scope=identify https://discord.com/api/oauth2/token
DISCORD_CLIENT_SECRET=

# Must be registered BYTE-EXACT under OAuth2 → Redirects in the portal — it is sent twice
# (authorize, then token exchange) and Discord string-compares both times. A mismatch fails at
# Discord's own consent screen, so you never reach the callback and see no app-side error.
#
# T-303 — its HOST must equal FRONTEND_URL's host (line 24). In dev the SPA on :3000 proxies /api
# to :8080, so the browser is on FRONTEND_URL's host when the flow starts and the host-only
# `oauth_state` cookie is scoped there; Discord then returns the browser to THIS host, and if the
# two differ the cookie is never sent → #error=invalid_state. Ports are irrelevant to cookies —
# :3000 vs :8080 is fine, `localhost` vs `127.0.0.1` is not. In DEVELOPMENT the API now refuses to
# start the OAuth flow when they disagree (handlers/oauth.rs); in production it only warns, because
# a split-host deployment (SPA on app.example.com, API on api.example.com) is legitimate there.
DISCORD_REDIRECT_URL=http://localhost:8080/api/v1/auth/discord/callback

# The guild whose roles decide the caller's web tier (Developer Mode on → right-click the server
# → Copy Server ID). Requested scopes are `identify guilds.members.read` (services/discord.rs:15).
# Blank ⇒ role sync is SKIPPED: the stored snapshot and the user's tier are left untouched, a
# tracing::error! is emitted, and an `auth.role_sync_skipped` WARN audit row is written. (Before
# T-185 a blank id instead demoted the whole community to enlisted, one login at a time, silently.)
# Setting this is necessary but not sufficient — the guild's role snowflakes must ALSO be mapped
# in seeds/discord_roles.sql, or every user resolves to enlisted on an otherwise perfect login.
DISCORD_GUILD_ID=

# UNUSED TODAY. Loaded into Config (config.rs:46,87) and read by no consumer — this tree has no
# bot, no gateway connection and no interactions endpoint. Reserved for T-279; leave blank.
DISCORD_BOT_TOKEN=

# Channel webhook that announcements are pushed to as embeds (services/webhook.rs). Independent
# of OAuth — it needs no client id/secret. Blank ⇒ pushing is disabled: the announcement still
# saves, and publishing records a CRIT audit row instead of posting.
DISCORD_WEBHOOK_URL=

# ─────────────────── Game-server telemetry ingest auth ────────────────
# Shared secret for the X-Service-Token endpoints (telemetry ingest, GET /missions/:id/compiled).
# Must match TBD_GAME_SERVER_TOKEN on the Reforger server side.
SERVICE_TOKEN=change-me-service-token

# ─────────────────── Server-status SSE republish (T-272) ───────────────
# Cadence (seconds) for the boot + interval poll of `server_statuses` → SSE hub.
# Closes the live Server Intel stream when nothing calls POST /ingest/server-status
# (no game-server bridge in-repo yet). Ingest still publishes immediately on write.
# Default 10. Invalid / zero / negative → default. See realtime::server_status_publish_interval.
# SERVER_STATUS_PUBLISH_INTERVAL_SECS=10

# Cadence (seconds) for the scheduled `leaderboard_totals` MV refresh (T-261). Default 900 (15m).
# LEADERBOARD_REFRESH_INTERVAL_SECS=900

# Cadence (seconds) for the scheduled Discord → web role resync (T-428). Default 86400 (24h / nightly).
# Invalid / zero / negative → default. See role_sync::ROLE_RESYNC_INTERVAL_ENV.
# ROLE_RESYNC_INTERVAL_SECS=86400
