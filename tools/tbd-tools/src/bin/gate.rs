//! `gate` — the Rust CDP gate harness entrypoint (T-165.5/.6). Scaffold: subcommands land
//! with their slices; until then this prints the suite this bin will own.
fn main() {
    println!(
        "tbd-tools gate — scaffold (T-165.0). Suites land at T-165.5 (v-suite) / T-165.6 (smokes)."
    );
}
