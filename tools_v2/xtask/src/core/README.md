# Core

Shared repository discovery, primary-checkout target directory resolution, host execution, and isolated test environments. Operational command handlers use these helpers without owning ticket storage or map processing.

Source modules: `cargo_target_directory.rs`, `host_execution.rs`, `mod.rs`, `repository_root.rs`, `test_environment.rs`.
