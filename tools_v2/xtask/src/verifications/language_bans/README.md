# Language Bans

Tracked-language and CI shell policy checks. Repository walk failures cannot become a clean scan.

Source modules: `mod.rs`, `node_and_file_limits.rs`, `python_scripts.rs`, `shell_scripts.rs`.
