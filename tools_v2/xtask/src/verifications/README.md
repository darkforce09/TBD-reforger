# Verifications

Repository checks are grouped by the invariant they enforce. These checks return explicit failures for unreadable inputs; operational commands live in `commands/`. Engine-backed asset checks delegate to developer-tools.

Source modules: `mod.rs`.
