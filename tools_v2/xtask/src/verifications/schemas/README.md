# Schemas

Contract checks are split under `checks/` into citations, enumerations, type inventory, kit references, wire-field readers, glyphs, and contract-family validation. Mission flattening lives in `commands/schema/mission_flattening.rs`; generated Rust types live under `commands/generate/`.

Source modules: `checks.rs`, `mod.rs`.
