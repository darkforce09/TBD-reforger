# Ci

CI schema parity checks keep the independent schema invocation wired into the CI composite. Tests reject removed, bypassed, and self-referential enforcement.

Source modules: `mod.rs`, `schema_parity.rs`, `workflow_shell.rs`, `workflow_shell_rules.rs`.
