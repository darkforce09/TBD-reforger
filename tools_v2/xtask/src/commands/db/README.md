# Db

Database lifecycle, schema seeds, milestone announcements, integration-test isolation, and database self-tests. Recipe rendering and execution retain their command order and exit behavior.

Source modules: `milestone_announcement.rs`, `mod.rs`, `operations.rs`.
