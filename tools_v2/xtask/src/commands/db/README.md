# Db

Database lifecycle, schema seeds, milestone announcements, integration-test isolation, migration checksum repair (`db repair-migration-checksum`), and database self-tests. Recipe rendering and execution retain their command order and exit behavior.

Source modules: `milestone_announcement.rs`, `mod.rs`, `operations.rs`; under `operations/`: `ab.rs`, `recipes.rs`, `repair_migration_checksum.rs`, `selftest.rs`, `test_it.rs`.
