# Ticket-engine tests

Unit tests live in sibling `src/**/tests/` files, each below 1,000 lines. This directory holds the compile-fail contract and byte-preserved execution receipt fixtures shared with xtask. Migrated tests cover validation, mutation refusal, child shipping, wave history/packing/drift, migrations, synchronization, metrics, and estimates.
