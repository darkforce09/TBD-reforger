# Ticket wave scheduling

Owns deterministic lock encoding, dependency packing, reservations, emptied waves, drift checks, collision selection, and historical plan decoding. `history` supplies the shared close-marker rules consumed by the compiler and platform wave checks. Platform orchestration remains in xtask.
