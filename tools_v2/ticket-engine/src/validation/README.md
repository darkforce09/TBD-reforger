# Ticket validation

Owns schema, vocabulary, scope, body, readiness, shipping, hierarchy, debt, and repository-reference checks. `check` returns ordered findings; `cmd_check` retains the CLI output and exit behavior. All checks receive the active repository root. Tests live in sibling `tests/` directories.
