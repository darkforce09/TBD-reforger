# Typed ticket model

The live public model is implemented in `../model/` and re-exported from the crate root. Encoding, vocabulary resolution, and UTC timestamp helpers retain their existing public interfaces.
