# Browser Testing

Chromium lifecycle and CDP transport, static asset serving and API proxying, diagnostics, DOM verification, route drift, captures, and editor scenarios. The `gate` and `capture` entrypoints delegate here.

Source modules: `capture_cli.rs`, `cdp.rs`, `cli.rs`, `diagnostics.rs`, `dom_oracle.rs`, `editor_smoke_tests.rs`, `fixture_injection.rs`, `mod.rs`, `route_drift.rs`, `screen_capture.rs`, `server.rs`.
