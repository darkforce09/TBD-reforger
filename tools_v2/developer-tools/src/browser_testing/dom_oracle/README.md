# Dom Oracle

DOM route inventory and browser verification modes share the parent oracle types. Route collection and mode execution live in separate modules.

Source modules: `routes.rs`, `run_modes.rs`.
