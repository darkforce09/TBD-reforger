# Cdp

Chromium discovery, browser/page lifecycle, WebSocket CDP request routing, and bounded polling helpers. The parent module owns transport state and exported browser APIs.

Source modules: `sleep_ms.rs`.
