# Bvh

BVH construction, batch processing, prefab composition, instance verification, world instance loading, and rotation checks. Public blueprint command functions are re-exported by the blueprint root.

Source modules: `batch_processing.rs`, `construction.rs`, `instance_pairs.rs`, `instance_verification.rs`, `prefab_catalog.rs`, `rotation_validation.rs`, `world_instances.rs`.
